{
  "url": "https://bugs.mysql.com/bug.php?id=102839",
  "title": "Index out of bounds in regular expression search.",
  "vulnerable_database": "MySQL Server: DML",
  "date": "2021-03-06 11:03:00",
  "vulnerable_version": "8.0.23",
  "description": "This bug looks like this (closed) bug:\nhttps://bugs.mysql.com/bug.php?id=9288\nI have this code (because of a question on stackoverflow.com):\r\n\r\nset @q=\"'в работе','на списание','списано'\";\r\nWITH RECURSIVE cte as (\r\n   select 1 as a union all\r\n   select a+1 from cte where a<35\r\n   )\r\nselect distinct  regexp_substr(@q,'''[^,]*''',a) as E from cte;\r\n\r\nWhen setting higher values for the number where U currently entered '35' and error \"ERROR 3686 (HY000): Index out of bounds in regular expression search.\"\r\nis returned.\nHow to repeat:\nWhen doing a single line like this:\r\nselect regexp_substr(\"a,b,c\",'''[^,]*''',100) as E ;\r\n\r\nThe error is also shown.\nSuggested fix:\nDo not throw this error ...",
  "poc": [
    "set @q=\"'в работе','на списание','списано'\";",
    "select 1 as a union all\nselect a+1 from cte where a<35\n)\nselect distinct  regexp_substr(@q,'''[^,]*''',a) as E from cte;",
    "select regexp_substr(\"a,b,c\",'''[^,]*''',100) as E ;"
  ],
  "key_bug_features": [
    "Index out of bounds in regexp_substr function"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), value DECIMAL(10,2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.50), (2, 'test2', NULL), (3, 'test3', 20.00);",
    "SELECT regexp_substr(name, '''[^,]*''', 1) AS extracted FROM test_table WHERE value IS NOT NULL;",
    "SELECT regexp_substr('a,b,c', '''[^,]*''', 50) AS test_case;",
    "WITH RECURSIVE cte AS (SELECT 1 AS a UNION ALL SELECT a + 1 FROM cte WHERE a < 100) SELECT DISTINCT regexp_substr('example1,example2,example3', '''[^,]*''', a) AS result FROM cte;"
  ]
}