{
  "url": "https://bugs.mysql.com/bug.php?id=105784",
  "title": "update a set not_null_column = null, not_null_column = 2 should not raise an err",
  "vulnerable_database": "MySQL Server: DML",
  "date": "2021-12-03 03:53:00",
  "vulnerable_version": "8.0, 5.7",
  "description": "create table t(a int not null,b int);\r\ninsert into t values(1,1);\r\nupdate t set a=null,a=2;\r\n\r\nmysql> update t set a=null,a=2;\r\nERROR 1048 (23000): Column 'a' cannot be null\r\n\r\nThe assignment is evaluated from left to right, we can update `a` using `2` without raising the not-null error message.\nHow to repeat:\ncreate table t(a int not null,b int);\r\ninsert into t values(1,1);\r\nupdate t set a=null,a=2;\nSuggested fix:\nupdate t set a = 2 without raising an error",
  "poc": [
    "create table t(a int not null,b int);",
    "insert into t values(1,1);",
    "update t set a=null,a=2;",
    "update `a` using `2` without raising the not-null error message.\nHow to repeat:\ncreate table t(a int not null,b int);"
  ],
  "key_bug_features": [
    "updating not null column with null then valid value"
  ],
  "bug_features_seed": [
    "CREATE TABLE t(a INT NOT NULL, b INT, c VARCHAR(10));",
    "INSERT INTO t VALUES(1, 1, 'test');",
    "UPDATE t SET a = NULL, b = 2 WHERE c = 'test';",
    "UPDATE t SET a = 3 WHERE b IS NOT NULL;",
    "SELECT * FROM t WHERE a IS NOT NULL;"
  ]
}