{
  "url": "https://www.sqlite.org/src/tktview/80e031a00f",
  "title": "Result of IN should never be NULL if the RHS is empty",
  "description": "未找到User Comments标签",
  "date": "2010-07-14 15:48:4815.08 years ago",
  "vulnerable_version": "3.6.23.1",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255), age INT, salary DECIMAL(10,2), created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table (name, age, salary) VALUES ('Alice', 30, 50000.00);",
    "INSERT INTO test_table (name, age, salary) VALUES ('Bob', NULL, 60000.00);",
    "SELECT * FROM test_table WHERE id IN (SELECT id FROM test_table WHERE age IS NULL AND salary > 55000.00);",
    "SELECT * FROM test_table WHERE id IN (SELECT id FROM test_table WHERE name = 'Charlie' AND age IS NOT NULL) AND salary IS NULL;"
  ],
  "key_bug_features": [
    "IN clause returns NULL with empty right-hand side"
  ]
}