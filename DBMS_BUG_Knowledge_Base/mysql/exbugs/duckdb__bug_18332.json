{
  "url": "https://github.com/duckdb/duckdb/issues/18332",
  "title": "Implicit casting for VARINT on arithmetic operations lead to wrong results",
  "description": "### What happens?\n\nWhen using `VARINT` and e.g. adding 1 to a big number the results ist wrong.\n\nResults are implicitly casted to `DOUBLE`. Casting back to `VARINT` seems to loose precision. \n\nMaybe I am missing something but it seems like precise arithmetic operations are not possible with `VARINT`.\n\n### To Reproduce\n\n```sql\nCREATE TABLE numbers ( x varint );\nINSERT INTO numbers (x) VALUES (9223372036854775808), (1);\nSELECT sum(x)::varint FROM numbers;\n```\n\nresults in\n\n```text\n┌────────────────────────┐\n│ CAST(sum(x) AS VARINT) │\n│         varint         │\n├────────────────────────┤\n│ 9223372036854775808    │\n└────────────────────────┘\n```\n\ninstead of `9223372036854775809`.\n\nNot casting gives value with type `DOUBLE` as result:\n\n```sql\nSELECT sum(x) FROM numbers;\n```\n\nresult:\n\n```text\n┌───────────────────────┐\n│        sum(x)         │\n│        double         │\n├───────────────────────┤\n│ 9.223372036854776e+18 │\n└───────────────────────┘\n```\n\nAlso, using `+` instead of the aggregation function `sum` operator leads to the same behavior:\n\n```sql\nSELECT (9223372036854775808::VARINT + 1::VARINT)::VARINT;\n```\n\nresults in\n\n```text\n┌───────────────────────────────────────────────────────────────────────────┐\n│ CAST((CAST(9223372036854775808 AS VARINT) + CAST(1 AS VARINT)) AS VARINT) │\n│                                  varint                                   │\n├───────────────────────────────────────────────────────────────────────────┤\n│ 9223372036854775808                                                       │\n└───────────────────────────────────────────────────────────────────────────┘\n```\n\n\n### OS:\n\nMacOS 15.5 (M2, aarch64)\n\n### DuckDB Version:\n\nv1.3.2\n\n### DuckDB Client:\n\nCLI\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nStefan Fast\n\n### Affiliation:\n\nEducational / Research\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a stable release\n\n### Did you include all relevant data sets for reproducing the issue?\n\nYes\n\n### Did you include all code required to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [x] Yes, I have",
  "date": "2025-07-19 19:23:28",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE test_numbers (a BIGINT, b DOUBLE, c INT);",
    "INSERT INTO test_numbers (a, b, c) VALUES (9223372036854775808, 1.5, 2);",
    "SELECT CAST(a + c AS SIGNED) FROM test_numbers;",
    "SELECT SUM(b) FROM test_numbers;",
    "SELECT CAST(a + 1 AS SIGNED) FROM test_numbers WHERE c IS NOT NULL;"
  ],
  "key_bug_features": [
    "Implicit casting of VARINT to DOUBLE during arithmetic operations"
  ]
}