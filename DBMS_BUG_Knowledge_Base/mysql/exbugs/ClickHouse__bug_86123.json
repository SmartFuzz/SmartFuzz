{
  "url": "https://github.com/ClickHouse/ClickHouse/issues/86123",
  "title": "Vertical merge of Nested arrays corrupts data",
  "description": "### Company or project name\n_No response_\n### Describe what's wrong\nVertical merging of nested arrays results in silent data corruption, while horizontal merging works fine. The example for reproducing the error will show that the data is completely lost, but on product data we also see a variant when the data is replaced by data from another order by key. Also at the end there will be a workaround in which everything is merged ok.\n### Does it reproduce on the most recent release?\nYes\n### How to reproduce\nhttps://fiddle.clickhouse.com/b52d196b-57db-45e8-a67e-d861f130393a\n1) Create table\n```sql\nCREATE TABLE bug (\nid UInt32,\n`n1.nums`Array(UInt32),\n`n1.numsplus`Array(UInt32),\n`n2.nums`Array(UInt32),\n`n2.numsplus`Array(UInt32)\n) ENGINE=ReplacingMergeTree() ORDER BY (id)\nSETTINGS\nmin_bytes_for_wide_part = 1, -- wide parts instead of compact\nvertical_merge_algorithm_min_rows_to_activate = 1,\nvertical_merge_algorithm_min_bytes_to_activate = 1,\nvertical_merge_algorithm_min_columns_to_activate = 1; -- vertical merge instead of horizontal\n```\n2) Disable merges (for test)\n```sql\nSYSTEM STOP MERGES bug;\n```\n3) Insert data\n```sql\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (1, [1,1,1], [2,2,2], [11,11,11], [22,22,22]);\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (2, [2,2,2], [3,3,3], [22,22,22], [33,33,33]);\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (3, [3,3,3], [4,4,4], [33,33,33], [44,44,44]);\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (4, [4,4,4], [5,5,5], [44,44,44], [55,55,55]);\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (5, [5,5,5], [6,6,6], [55,55,55], [66,66,66]);\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (6, [6,6,6], [7,7,7], [66,66,66], [77,77,77]);\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (7, [7,7,7], [8,8,8], [77,77,77], [88,88,88]);\nINSERT INTO bug (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`)\nVALUES (8, [8,8,8], [9,9,9], [88,88,88], [99,99,99]);\n```\n4) Add nested arrays\n```sql\nALTER TABLE bug ADD COLUMN `n1.urls` Array(Array(String));\nALTER TABLE bug ADD COLUMN `n1.domains` Array(Array(String)) DEFAULT arrayMap(x -> arrayMap(y -> domain(y), x), `n1.urls`);",
  "date": "2025-08-25T13:00:09Z",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "ClickHouse",
  "bug_features_seed": [
    "CREATE TABLE bug_test (id INT UNSIGNED, `n1.nums` JSON, `n1.numsplus` JSON, `n2.nums` JSON, `n2.numsplus` JSON, `n3.dates` JSON);",
    "INSERT INTO bug_test (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`, `n3.dates`) VALUES (1, JSON_ARRAY(1, 2, 3), JSON_ARRAY(4, 5, 6), JSON_ARRAY(7, 8, 9), JSON_ARRAY(10, 11, 12), JSON_ARRAY('2023-01-01', '2023-01-02', '2023-01-03'));",
    "INSERT INTO bug_test (id, `n1.nums`, `n1.numsplus`, `n2.nums`, `n2.numsplus`, `n3.dates`) VALUES (2, JSON_ARRAY(4, 5, 6), JSON_ARRAY(7, 8, 9), JSON_ARRAY(10, 11, 12), JSON_ARRAY(13, 14, 15), JSON_ARRAY('2023-01-04', '2023-01-05', '2023-01-06'));",
    "ALTER TABLE bug_test ADD COLUMN `n1.urls` JSON;",
    "SELECT id, JSON_UNQUOTE(JSON_EXTRACT(`n1.nums`, '$[0]')) AS nums, JSON_UNQUOTE(JSON_EXTRACT(`n1.numsplus`, '$[0]')) AS numsplus FROM bug_test WHERE id = 1;"
  ],
  "key_bug_features": [
    "Vertical merging of nested arrays causes data corruption"
  ]
}