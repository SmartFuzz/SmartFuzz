{
  "url": "https://www.sqlite.org/src/tktview/981329adee",
  "title": "Stack overflow while parsing deeply nested JSON",
  "description": "drh added on 2017-04-11 18:09:39:\nThe following query causes a stack overflow in the JSON parser:\nWITH RECURSIVE c(x) AS (VALUES(1) UNION ALL SELECT x+1 FROM c WHERE x<400000)\nSELECT json_valid(group_concat('[', '')) FROM c;\nThis problem was reported on the SQLite mailing list by Ralf Junker and is\nbased on the n_structure_100000_opening_arrays.json test case from the\nJSONTestSuite.",
  "date": "2017-04-11 18:09:398.33 years ago",
  "vulnerable_version": "3.18.0",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INT PRIMARY KEY AUTO_INCREMENT, value TEXT, depth INT NOT NULL DEFAULT 0);",
    "INSERT INTO test_table (value, depth) VALUES ('Initial', 0);",
    "SET @x = 1;",
    "WHILE @x < 400000 DO SET @x = @x + 1;",
    "END WHILE;",
    "SELECT JSON_VALID(GROUP_CONCAT('[', '')) AS valid_json;"
  ],
  "key_bug_features": [
    "Recursive CTE with large depth causes stack overflow"
  ]
}