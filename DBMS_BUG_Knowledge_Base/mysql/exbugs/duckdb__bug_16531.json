{
  "url": "https://github.com/duckdb/duckdb/issues/16531",
  "title": "MODE() function tries to loads entire data in memory, causing session crash",
  "description": "### What happens?\n\nThe MODE() function in DuckDB tries to load the entire dataset into memory, leading to excessive memory usage and session crashes when applied to large datasets.\n\n### To Reproduce\n\nCreating sample data to reproduce the error\n```\nATTACH 'duckdb/db';\nuse db;\nSET temp_directory = 'duckdb/temp_table';\n\nCREATE TABLE db.sample_data AS \nWITH numbers AS (FROM range(1, 100_000_000) t(id))\nSELECT \n    id,\n    (RANDOM() % 3) AS download,   -- Generates 0, 1, or 2 for download usage\n    (RANDOM() % 3) AS upload,     -- Generates 0, 1, or 2 for upload usage\n    'Application_' || (RANDOM() % 1000) AS app,  -- Generates 1000 unique apps\n    LEFT(md5(random()::text), 44) AS key  -- 44-byte unique string with cardinality 2000\nFROM numbers;\n— It creates 3.9Gb data in persistent disk \n```\n\nnow let's create duckdb cli session again with below settings \n```\n.timer on\nSET memory_limit = '1GB';\nSET max_memory = '1GB';\nSET enable_progress_bar = true;\nSET enable_fsst_vectors=true;\nSET preserve_insertion_order = false;\n\nATTACH 'duckdb/db';\nuse db;\nSET temp_directory = 'duckdb/temp_table';\n\nselect app, key, mode(download), mode(upload) from db.sample_data group by app, key;\n \n```\nIt will be killed right away \n\nI've found WORKAROUND for this behavior \n```\nWITH download_counts AS (\n    SELECT \n        app, \n        key, \n        SUM(CASE WHEN download = 0 THEN 1 ELSE 0 END) AS low_count,\n        SUM(CASE WHEN download = 1 THEN 1 ELSE 0 END) AS average_count,\n        SUM(CASE WHEN download = 2 THEN 1 ELSE 0 END) AS high_count\n    FROM db.sample_data\n    GROUP BY app, key\n),\nupload_counts AS (\n    SELECT \n        app, \n        key, \n        SUM(CASE WHEN upload = 0 THEN 1 ELSE 0 END) AS low_count,\n        SUM(CASE WHEN upload = 1 THEN 1 ELSE 0 END) AS average_count,\n        SUM(CASE WHEN upload = 2 THEN 1 ELSE 0 END) AS high_count\n    FROM db.sample_data\n    GROUP BY app, key\n)\n\nSELECT \n    d.app, \n    d.key,\n    CASE \n        WHEN d.low_count = GREATEST(d.low_count, d.average_count, d.high_count) THEN 0\n        WHEN d.average_count = GREATEST(d.low_count, d.average_count, d.high_count) THEN 1\n        ELSE 2 \n    END AS mode_download,\n    CASE \n        WHEN u.low_count = GREATEST(u.low_count, u.average_count, u.high_count) THEN 0\n        WHEN u.average_count = GREATEST(u.low_count, u.average_count, u.high_count) THEN 1\n        ELSE 2 \n    END AS mode_upload\nFROM download_counts d\nJOIN upload_counts u \nON d.app = u.app AND d.key = u.key;\n\n```\n\nBut it worked because i had categorical column and i knew all possible distinct values. \nCan you fix this issue in general.\n \n\n### OS:\n\nlinux\n\n### DuckDB Version:\n\nv1.2.0\n\n### DuckDB Client:\n\nCli\n\n### Hardware:\n\nI have 24cpu and 48 GB ram and used it on 55GB dataset but it failed\n\n### Full Name:\n\nSuraj Pattewar\n\n### Affiliation:\n\nStudent\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a stable release\n\n### Did you include all relevant data sets for reproducing the issue?\n\nNot applicable - the reproduction does not require a data set\n\n### Did you include all code required to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [x] Yes, I have",
  "date": "2025-03-06 07:38:32",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE test_data (id INT, download INT, upload INT, app VARCHAR(255), `key` VARCHAR(44));",
    "INSERT INTO test_data SELECT id, FLOOR(RAND() * 3) AS download, FLOOR(RAND() * 3) AS upload, CONCAT('App_', FLOOR(RAND() * 100)) AS app, LEFT(MD5(RAND()), 44) AS `key` FROM (SELECT @rownum := @rownum + 1 AS id FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t2, (SELECT @rownum := 0) r) AS numbers LIMIT 100000);",
    "SELECT app, `key`, COUNT(*) AS count_download, SUM(CASE WHEN download = 0 THEN 1 ELSE 0 END) AS low_count, SUM(CASE WHEN download = 1 THEN 1 ELSE 0 END) AS average_count, SUM(CASE WHEN download = 2 THEN 1 ELSE 0 END) AS high_count FROM test_data GROUP BY app, `key`;",
    "WITH download_summary AS (SELECT app, `key`, SUM(CASE WHEN download = 0 THEN 1 ELSE 0 END) AS low_count, SUM(CASE WHEN download = 1 THEN 1 ELSE 0 END) AS average_count, SUM(CASE WHEN download = 2 THEN 1 ELSE 0 END) AS high_count FROM test_data GROUP BY app, `key`) SELECT app, `key`, CASE WHEN low_count = GREATEST(low_count, average_count, high_count) THEN 0 WHEN average_count = GREATEST(low_count, average_count, high_count) THEN 1 ELSE 2 END AS mode_download FROM download_summary;",
    "SELECT app, `key`, COALESCE(NULLIF(MAX(download), 0), 1) AS max_download FROM test_data GROUP BY app, `key` HAVING COUNT(*) > 10;"
  ],
  "key_bug_features": [
    "MODE() function loads entire dataset into memory"
  ]
}