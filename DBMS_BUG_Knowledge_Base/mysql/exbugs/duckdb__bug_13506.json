{
  "url": "https://github.com/duckdb/duckdb/issues/13506",
  "title": "Unexpected result when casting negative integer to `BIT`",
  "description": "### What happens?\n\nConsider the following test case. The third query should return an empty result, as `CAST(CAST(t1.c1 AS BIT) AS INTEGER)` is evaluated to `-1` and the expression should be false. However, the query returns `-1`, which is unexpected. \r\n\r\nI found in PostgreSQL, the second query would be evaluated as `true`. Either the second or the third query is unexpected.\n\n### To Reproduce\n\n```sql\r\nCREATE TABLE  t1( c1 INTEGER);\r\nINSERT INTO t1(c1) VALUES (-1);\r\n\r\nSELECT t1.c1 FROM t1; -- -1\r\nSELECT (1 BETWEEN -1 AND CAST(CAST(t1.c1 AS BIT) AS INTEGER)) FROM t1; -- false\r\nSELECT t1.c1 FROM t1 WHERE (1 BETWEEN -1 AND CAST(CAST(t1.c1 AS BIT) AS INTEGER));\r\n-- Expected: empty\r\n-- Actual: -1\r\n```\n\n### OS:\n\nubuntu 22.04\n\n### DuckDB Version:\n\nv0.0.1 367aa8d\n\n### DuckDB Client:\n\nCLI\n\n### Full Name:\n\nSuyang Zhong\n\n### Affiliation:\n\nNUS\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a nightly build\n\n### Did you include all relevant data sets for reproducing the issue?\n\nYes\n\n### Did you include all code required to reproduce the issue?\n\n- [X] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [X] Yes, I have",
  "date": "2024-08-22 04:32:30",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE t2(c1 INTEGER, c2 TINYINT(1));",
    "INSERT INTO t2(c1, c2) VALUES (-1, CAST(-1 AS SIGNED));",
    "SELECT c1 FROM t2 WHERE (1 BETWEEN -1 AND CAST(CAST(c1 AS SIGNED) AS SIGNED));",
    "CREATE TABLE t3(c1 INTEGER NOT NULL, c2 INTEGER DEFAULT 0);",
    "INSERT INTO t3(c1) VALUES (NULL), (-1);"
  ],
  "key_bug_features": [
    "Casting negative integer to BIT produces unexpected results"
  ]
}