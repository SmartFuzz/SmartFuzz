{
  "url": "https://www.sqlite.org/src/tktview/4feb3159c6",
  "title": "Crash due to misuse of window functions.",
  "description": "drh added on 2019-02-22 18:44:24:\nThe following SQL statement causes a crash due to attempt to free memory\nthat was not obtained from malloc():\nSELECT +sum(0)OVER() ORDER BY +sum(0)OVER();\nThis problem has existed in all prior versions of SQLite that support\nwindow functions, beginning with version 3.25.0 (2018-09-15).",
  "date": "2019-02-22 18:44:246.47 years ago",
  "vulnerable_version": "3.27.1",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INT AUTO_INCREMENT PRIMARY KEY, value INT, category VARCHAR(255));",
    "INSERT INTO test_table (value, category) VALUES (1, 'A'), (2, 'B'), (3, 'A'), (NULL, 'C');",
    "SELECT value, (SELECT SUM(value) FROM test_table WHERE category = t.category) AS category_sum FROM test_table t ORDER BY category_sum;",
    "SELECT SUM(value) AS total_sum FROM test_table WHERE value IS NOT NULL;",
    "SELECT SUM(value) AS total_sum FROM test_table ORDER BY total_sum;"
  ],
  "key_bug_features": [
    "Misuse of window functions in SQL statement"
  ]
}