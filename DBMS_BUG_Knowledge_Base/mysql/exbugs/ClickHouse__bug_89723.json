{
  "url": "https://github.com/ClickHouse/ClickHouse/issues/89723",
  "title": "`INSERT VALUES` reports incorrect row count when table has Materialized View",
  "description": "### Problem\n```sql\nCREATE TABLE test(id UInt64, name String) ENGINE = Memory;\nCREATE MATERIALIZED VIEW mv ENGINE = Memory AS SELECT id FROM test;\nINSERT INTO test VALUES (1,1);\n```\n> 1 row in set. Elapsed: 0.002 sec.\nThis should return 2 as row was inserted in table and MV. If we, however, do\n```sql\nINSERT INTO test SELECT number AS id, toString(number) AS name FROM numbers(1);\n```\nthen we correctly see\n> 2 rows in set. Elapsed: 0.002 sec.\n### Related\nFound when working on https://github.com/ClickHouse/ClickHouse/pull/79462",
  "date": "2025-11-07T14:28:30Z",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "ClickHouse",
  "bug_features_seed": [
    "CREATE TABLE test2(id BIGINT, name VARCHAR(255), age TINYINT);",
    "CREATE TABLE mv2 AS SELECT id, age FROM test2;",
    "INSERT INTO test2 VALUES (2, 'Alice', 30);",
    "INSERT INTO test2 SELECT number AS id, CAST(number AS CHAR) AS name, number % 100 AS age FROM (SELECT 1 AS number UNION SELECT 2 UNION SELECT 3);",
    "INSERT INTO test2 VALUES (NULL, 'Bob', NULL);"
  ],
  "key_bug_features": [
    "INSERT VALUES with Materialized View reports incorrect row count"
  ]
}