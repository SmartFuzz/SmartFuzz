{
  "url": "https://github.com/duckdb/duckdb/issues/19411",
  "title": "DuckDB Produces a CSV it cannot read when single quote column is present",
  "description": "### What happens?\n\nWe can get DuckDB to produce a CSV which it cannot read with the same settings as the writer. ON write,\n\n* we write one test column of each type with mock data\n* `|` as a delimiter, default newline, header on, and `\"` as the escape\n* on read with the exact same configurations, we get an `Invalid Input Error:` that indicates that DuckDB read the CSV as being one line, even though we have `new_line = \\n (Set By User)` and the generated csv properly has `\\n` where expected\n* this goes away if we omit `chr(39)` from test_unicode, which makes little sense because `'` is neither the escape, delimiter, or newline character. \n\n\n### To Reproduce\n\n```sql\nINSTALL SPATIAL;\nLOAD SPATIAL;\n\nCOPY (\n\tSELECT * FROM (\n\t\tSELECT\n\t\t\tmd5(CAST(range AS TEXT)) as id,\n\t\t\t(to_timestamp(range))::timestamptz as updated_at,\n\t\t\trpad('hello_', CAST(range AS INTEGER), '0') as test_string,\n\t\t\tchr(CAST(range AS INTEGER)) as test_unicode,\n\t\t\tCAST(to_timestamp(range)::timestamp AS TIME) as test_time,\n\t\t\tCAST(md5(CAST(range AS TEXT)) AS BLOB) as test_bytes,\n\t\t\tCAST(range % 32767 AS SMALLINT) as test_smallint,\n\t\t\tCAST(range AS INTEGER) as test_integer,\n\t\t\tCAST(range AS BIGINT) as test_bigint,\n\t\t\tCAST(range / 3.0 AS REAL) as test_real,\n\t\t\tCAST(range / 3.0 AS DOUBLE) as test_double,\n\t\t\tCAST(range AS DECIMAL) as test_decimal,\n\t\t\tCASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END AS test_boolean,\n\t\t\tCAST(DATE '2022-02-24' - TO_DAYS(CAST(range % 3650 AS INTEGER)) AS DATE) as test_date,\n\t\t\tjson_object('number', range, 'string', md5(CAST(range AS TEXT)))::TEXT as test_json,\n\t\t\tST_GeomFromGeoJSON(json_object('type', 'Point', 'coordinates', [CAST( 180.0 / (range + 1.0) AS DOUBLE), CAST(90.0 / (range + 1.0) AS DOUBLE)]::FLOAT[2])::STRING) as test_geography,\n\t\t\t[CAST(range / 1.0 AS DOUBLE), CAST(range / 2.0 AS DOUBLE), CAST(range / 3.0 AS DOUBLE), CAST(range / 4.0 AS DOUBLE)]::DOUBLE[4] as test_vector,\n\t\t\tCASE range % 6 WHEN 0 THEN 'madison' WHEN 1 THEN 'boston' WHEN 2 THEN 'redmond' WHEN 3 THEN 'sacramento' WHEN 4 THEN 'anchorage' WHEN 5 THEN 'reno' END as tenant_id\n\t\tFROM range(10000)\n\t) generated\n\tORDER BY updated_at ASC LIMIT 10000) TO 'fails.csv' (HEADER TRUE, DELIMITER '|', ESCAPE '\"');\n\nSELECT * FROM read_csv('fails.csv', header=TRUE, new_line='\\n', delim='|', escape='\"', strict_mode=FALSE, types={'id': 'text', 'updated_at': 'timestamptz', 'test_bytes': 'blob', 'test_string': 'text', 'test_unicode': 'text', 'test_smallint': 'integer', 'test_integer': 'integer', 'test_bigint': 'bigint', 'test_real': 'double', 'test_double': 'double', 'test_boolean': 'boolean', 'test_decimal': 'text', 'test_date': 'date', 'test_time': 'time', 'test_json': 'text', 'test_vector': 'text', 'test_geography': 'text', 'tenant_id': 'text'}) ORDER BY updated_at;\n\n\n\nCOPY (\n\tSELECT * FROM (\n\t\tSELECT\n\t\t\tmd5(CAST(range AS TEXT)) as id,\n\t\t\t(to_timestamp(range))::timestamptz as updated_at,\n\t\t\trpad('hello_', CAST(range AS INTEGER), '0') as test_string,\n\t\t\tCASE WHEN range = 39 THEN NULL ELSE chr(CAST(range AS INTEGER)) END as test_unicode,\n\t\t\tCAST(to_timestamp(range)::timestamp AS TIME) as test_time,\n\t\t\tCAST(md5(CAST(range AS TEXT)) AS BLOB) as test_bytes,\n\t\t\tCAST(range % 32767 AS SMALLINT) as test_smallint,\n\t\t\tCAST(range AS INTEGER) as test_integer,\n\t\t\tCAST(range AS BIGINT) as test_bigint,\n\t\t\tCAST(range / 3.0 AS REAL) as test_real,\n\t\t\tCAST(range / 3.0 AS DOUBLE) as test_double,\n\t\t\tCAST(range AS DECIMAL) as test_decimal,\n\t\t\tCASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END AS test_boolean,\n\t\t\tCAST(DATE '2022-02-24' - TO_DAYS(CAST(range % 3650 AS INTEGER)) AS DATE) as test_date,\n\t\t\tjson_object('number', range, 'string', md5(CAST(range AS TEXT)))::TEXT as test_json,\n\t\t\tST_GeomFromGeoJSON(json_object('type', 'Point', 'coordinates', [CAST( 180.0 / (range + 1.0) AS DOUBLE), CAST(90.0 / (range + 1.0) AS DOUBLE)]::FLOAT[2])::STRING) as test_geography,\n\t\t\t[CAST(range / 1.0 AS DOUBLE), CAST(range / 2.0 AS DOUBLE), CAST(range / 3.0 AS DOUBLE), CAST(range / 4.0 AS DOUBLE)]::DOUBLE[4] as test_vector,\n\t\t\tCASE range % 6 WHEN 0 THEN 'madison' WHEN 1 THEN 'boston' WHEN 2 THEN 'redmond' WHEN 3 THEN 'sacramento' WHEN 4 THEN 'anchorage' WHEN 5 THEN 'reno' END as tenant_id\n\t\tFROM range(10000)\n\t) generated\n\tORDER BY updated_at ASC LIMIT 10000) TO 'succeeds.csv' (HEADER TRUE, DELIMITER '|', ESCAPE '\"');\n\nSELECT * FROM read_csv('succeeds.csv', header=TRUE, new_line='\\n', delim='|', escape='\"', strict_mode=FALSE, types={'id': 'text', 'updated_at': 'timestamptz', 'test_bytes': 'blob', 'test_string': 'text', 'test_unicode': 'text', 'test_smallint': 'integer', 'test_integer': 'integer', 'test_bigint': 'bigint', 'test_real': 'double', 'test_double': 'double', 'test_boolean': 'boolean', 'test_decimal': 'text', 'test_date': 'date', 'test_time': 'time', 'test_json': 'text', 'test_vector': 'text', 'test_geography': 'text', 'tenant_id': 'text'}) ORDER BY updated_at;\n```\n\n### OS:\n\nLinux AMD64, OSX ARM64\n\n### DuckDB Version:\n\nv1.4.1\n\n### DuckDB Client:\n\nCLI, Golang\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nNiger Little-Poole\n\n### Affiliation:\n\nPrequel\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a stable release\n\n### Did you include all relevant data sets for reproducing the issue?\n\nYes\n\n### Did you include all code required to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [x] Yes, I have",
  "date": "2025-10-15 14:14:20",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE test_data (id VARCHAR(255), updated_at DATETIME, test_string TEXT, test_unicode TEXT, test_time TIME, test_bytes BLOB, test_smallint SMALLINT, test_integer INT, test_bigint BIGINT, test_real FLOAT, test_double DOUBLE, test_decimal DECIMAL(10,2), test_boolean BOOLEAN, test_date DATE, test_json TEXT, test_vector JSON, test_geography TEXT, tenant_id VARCHAR(255));",
    "INSERT INTO test_data (id, updated_at, test_string, test_unicode, test_time, test_bytes, test_smallint, test_integer, test_bigint, test_real, test_double, test_decimal, test_boolean, test_date, test_json, test_vector, test_geography, tenant_id) SELECT MD5(CAST(range AS CHAR)), NOW(), RPAD('hello_', CAST(range AS UNSIGNED), '0'), CHAR(CAST(range AS UNSIGNED)), CAST(NOW() AS TIME), MD5(CAST(range AS CHAR)), (range % 32767), range, range, (range / 3.0), (range / 3.0), CAST(range AS DECIMAL(10,2)), (CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END), DATE_SUB('2022-02-24', INTERVAL (range % 3650) DAY), JSON_OBJECT('number', range, 'string', MD5(CAST(range AS CHAR))), JSON_ARRAY(CAST(range / 1.0 AS DECIMAL(10,2)), CAST(range / 2.0 AS DECIMAL(10,2)), CAST(range / 3.0 AS DECIMAL(10,2)), CAST(range / 4.0 AS DECIMAL(10,2))), ST_GeomFromText(CONCAT('POINT(', CAST(180.0 / (range + 1.0) AS CHAR), ' ', CAST(90.0 / (range + 1.0) AS CHAR), ')')) FROM (SELECT @rownum := @rownum + 1 AS range FROM (SELECT @rownum := 0) r, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t2 LIMIT 10000) AS ranges;",
    "SELECT * FROM test_data WHERE test_unicode IS NOT NULL INTO OUTFILE 'valid_data.csv' FIELDS TERMINATED BY '|' ENCLOSED BY '\"' LINES TERMINATED BY '\\n';",
    "SELECT * FROM test_data ORDER BY updated_at;",
    "INSERT INTO test_data (id, updated_at, test_string, test_unicode, test_time, test_bytes, test_smallint, test_integer, test_bigint, test_real, test_double, test_decimal, test_boolean, test_date, test_json, test_vector, test_geography, tenant_id) SELECT MD5(CAST(range AS CHAR)), NOW(), RPAD('hello_', CAST(range AS UNSIGNED), '0'), (CASE WHEN range = 39 THEN NULL ELSE CHAR(CAST(range AS UNSIGNED)) END), CAST(NOW() AS TIME), MD5(CAST(range AS CHAR)), (range % 32767), range, range, (range / 3.0), (range / 3.0), CAST(range AS DECIMAL(10,2)), (CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END), DATE_SUB('2022-02-24', INTERVAL (range % 3650) DAY), JSON_OBJECT('number', range, 'string', MD5(CAST(range AS CHAR))), JSON_ARRAY(CAST(range / 1.0 AS DECIMAL(10,2)), CAST(range / 2.0 AS DECIMAL(10,2)), CAST(range / 3.0 AS DECIMAL(10,2)), CAST(range / 4.0 AS DECIMAL(10,2))), ST_GeomFromText(CONCAT('POINT(', CAST(180.0 / (range + 1.0) AS CHAR), ' ', CAST(90.0 / (range + 1.0) AS CHAR), ')')) FROM (SELECT @rownum := @rownum + 1 AS range FROM (SELECT @rownum := 0) r, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t2 LIMIT 10000) AS ranges;"
  ],
  "key_bug_features": [
    "CSV read failure with single quote in column data"
  ]
}