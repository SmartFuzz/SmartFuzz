{
  "url": "https://github.com/openlink/virtuoso-opensource/issues/80",
  "title": "Some graphs aren't listed on VOS7",
  "description": "In develop/7 and stable/7, some graphs aren't listed via the below sparql.\nSELECT DISTINCT ?g WHERE { GRAPH ?g { ?s ?p ?o }}\n\nFor example, there are 22 graphs but it returns only 16 graphs.\nAnd Conductor (Linked Data - Graphs) displays same list.\nBut this query returns all graph names.\nSELECT ?g WHERE { GRAPH ?g { ?s ?p ?o }} GROUP BY ?g\n\nWhat is the difference between two queries?",
  "date": "2013-08-18 15:39:48",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "mysql",
  "source_database": "virtuoso",
  "bug_features_seed": [
    "CREATE TABLE graphs (id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO graphs (id, name) VALUES (1, 'Graph A'), (2, 'Graph B'), (3, 'Graph C'), (4, 'Graph D'), (5, 'Graph E');",
    "SELECT DISTINCT name FROM graphs WHERE id IN (SELECT id FROM graphs WHERE name LIKE 'Graph%') ORDER BY name;",
    "SELECT name FROM graphs WHERE created_at IS NULL OR created_at < '2023-01-01' GROUP BY name;",
    "SELECT DISTINCT name FROM graphs WHERE id NOT IN (SELECT id FROM graphs WHERE name = 'Graph C') LIMIT 1;"
  ],
  "key_bug_features": [
    "SELECT DISTINCT ?g WHERE { GRAPH ?g { ?s ?p ?o }}"
  ]
}