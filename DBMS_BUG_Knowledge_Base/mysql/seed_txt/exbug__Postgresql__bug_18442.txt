CREATE TABLE t1 (a INT, b INT, c TEXT DEFAULT 'default');
INSERT INTO t1 SELECT NULL, i, 'value' FROM (SELECT @rownum:=@rownum + 1 AS i FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) AS a, (SELECT @rownum:=0) AS r) AS numbers WHERE i <= 100;
CREATE INDEX idx_t1_a_b ON t1 (a, b);
EXPLAIN SELECT * FROM t1 WHERE a IS NULL ORDER BY b;
EXPLAIN SELECT * FROM t1 WHERE a = 1 OR a IS NULL ORDER BY c;
