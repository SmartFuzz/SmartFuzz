CREATE TABLE async_pt (a INT, b INT, c VARCHAR(255) DEFAULT 'default' NOT NULL) PARTITION BY RANGE (a);
CREATE TABLE base_tbl2 (a INT, b INT, c VARCHAR(255) UNIQUE);
CREATE TABLE async_p2 (a INT, b INT, c VARCHAR(255) DEFAULT 'default' NOT NULL) PARTITION OF async_pt FOR VALUES BETWEEN (2000) AND (3000);
INSERT INTO async_pt (a, b) SELECT i, CAST(i AS CHAR) FROM (SELECT 3000 + n AS i FROM (SELECT @rownum := @rownum + 1 AS n FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT @rownum := 0) t2 LIMIT 101) AS seq) AS numbers;
DELETE FROM async_pt WHERE a IS NOT NULL;
