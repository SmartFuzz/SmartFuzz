CREATE TABLE test_data (id INT AUTO_INCREMENT PRIMARY KEY, value1 INT, value2 INT, value3 INT, group_id INT NOT NULL);
INSERT INTO test_data (value1, value2, value3, group_id) VALUES (10, 20, 30, 1), (15, 25, 35, 1), (5, 10, 15, 2), (20, 30, 40, 2);
SELECT group_id, SUM(value1 + value2) AS aggr2 FROM test_data GROUP BY group_id;
SELECT group_id, SUM(value1 + value2 + value3) AS aggr3 FROM test_data GROUP BY group_id;
SELECT group_id, SUM(value1 + value2 + value3 + value1) AS aggr4 FROM test_data GROUP BY group_id;
