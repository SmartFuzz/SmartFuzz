CREATE TABLE test_table (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(255), value FLOAT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, value) VALUES ('Alice', 10.5), ('Bob', 20.0), ('Charlie', NULL), ('David', 15.0);
CREATE TABLE another_table (ref_id INT, description VARCHAR(255), FOREIGN KEY(ref_id) REFERENCES test_table(id));
INSERT INTO another_table (ref_id, description) VALUES (1, 'First'), (2, 'Second'), (3, 'Third'), (NULL, 'Fourth');
SELECT (SELECT 'mmm' UNION SELECT DISTINCT max(name) FROM test_table WHERE value IS NOT NULL) FROM information_schema.tables;
