CREATE TABLE test_table (id INT PRIMARY KEY, value DOUBLE, description VARCHAR(255));
INSERT INTO test_table (id, value, description) VALUES (1, RAND(), 'First entry'), (2, RAND(), 'Second entry'), (3, RAND(), 'Third entry');
CREATE FUNCTION random_value() RETURNS DOUBLE DETERMINISTIC RETURN RAND();
SELECT random_value();
SELECT (SELECT RAND()) AS random_result, id FROM test_table;
