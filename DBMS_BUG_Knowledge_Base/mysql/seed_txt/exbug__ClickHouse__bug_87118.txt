CREATE TABLE t_proj_test1 (id INT UNSIGNED, name VARCHAR(255), value DOUBLE, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t_proj_test1 SELECT number, CONCAT('Name', number), number * 1.5, NOW() FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t2, (SELECT @rownum := 0) r) AS numbers LIMIT 10000);
CREATE TABLE t_proj_test2 (user_id INT UNSIGNED, score INT, timestamp DATE DEFAULT CURRENT_DATE);
INSERT INTO t_proj_test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, CURDATE() FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t2, (SELECT @rownum := 0) r) AS numbers LIMIT 5000);
SELECT user_id, SUM(score) AS total_score FROM t_proj_test2 GROUP BY user_id ORDER BY total_score DESC;
