CREATE TABLE delta_table (id TINYINT, val VARCHAR(255));
INSERT INTO delta_table (id, val) VALUES (1, 'X'), (2, 'Y'), (3, 'Z');
CREATE TABLE distributed_table (id TINYINT, val VARCHAR(255)) ENGINE = InnoDB;
INSERT INTO distributed_table (id, val) SELECT id, val FROM delta_table;
SELECT a.val, b.val FROM distributed_table AS a JOIN delta_table AS b ON a.id = b.id;
SELECT a.val, b.val FROM delta_table AS b JOIN distributed_table AS a ON a.id = b.id;
