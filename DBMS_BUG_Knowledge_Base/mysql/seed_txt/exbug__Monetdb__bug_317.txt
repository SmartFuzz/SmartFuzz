CREATE TABLE test_intervals (id INT PRIMARY KEY, duration INT, description VARCHAR(255));
INSERT INTO test_intervals (id, duration, description) VALUES (1, 365, 'One year interval');
INSERT INTO test_intervals (id, duration, description) VALUES (2, NULL, 'Null interval');
SELECT id, duration FROM test_intervals WHERE duration IS NOT NULL;
SELECT 0 AS interval_test;
