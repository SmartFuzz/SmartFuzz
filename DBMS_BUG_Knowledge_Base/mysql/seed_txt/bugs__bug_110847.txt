CREATE DATABASE IF NOT EXISTS test_fuzzing;
USE test_fuzzing;
CREATE TABLE IF NOT EXISTS mixed_types_table (id INT PRIMARY KEY, name VARCHAR(50), created_at DATETIME DEFAULT CURRENT_TIMESTAMP, is_active BOOLEAN);
INSERT INTO mixed_types_table (id, name, is_active) VALUES (1, 'Test User', TRUE), (2, 'Another User', FALSE);
PREPARE stmt2 FROM 'SELECT name, (CASE WHEN is_active THEN ''Active'' ELSE ''Inactive'' END) AS status FROM mixed_types_table WHERE id = ?';
