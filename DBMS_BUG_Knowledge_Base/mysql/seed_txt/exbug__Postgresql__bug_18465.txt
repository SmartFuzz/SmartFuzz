CREATE TABLE a (col_int INT, col_text VARCHAR(255));
INSERT INTO a VALUES (1, 'one'), (2, 'two'), (NULL, 'three');
CREATE TABLE b (col_int INT, col_float FLOAT);
INSERT INTO b VALUES (1, 1.1), (2, 2.2), (3, NULL);
SELECT (SELECT MIN(col_int) FROM b WHERE b.col_int = a.col_int) AS min_col_int, a.col_int FROM a;
