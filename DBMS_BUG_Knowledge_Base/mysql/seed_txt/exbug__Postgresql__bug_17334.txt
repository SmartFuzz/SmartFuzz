CREATE TABLE point_tbl(f1 POINT NOT NULL, f2 INT CHECK (f2 > 0));
CREATE INDEX gpointind ON point_tbl(f1);
INSERT INTO point_tbl (f1, f2) SELECT POINT(0, 0), n FROM (SELECT @rownum := @rownum + 1 AS n FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT @rownum := 0) t2 LIMIT 185) AS numbers;
INSERT INTO point_tbl VALUES (POINT(0, NULL), 1);
SELECT ST_Distance(f1, POINT(0, 0)) AS dist FROM point_tbl WHERE f2 IS NOT NULL ORDER BY dist;
