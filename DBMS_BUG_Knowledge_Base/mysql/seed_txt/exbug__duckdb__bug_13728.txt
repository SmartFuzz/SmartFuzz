CREATE TABLE t2 (a INT, b VARCHAR(255), c DOUBLE, d BOOLEAN);
INSERT INTO t2 VALUES (1, 'test', 3.14, TRUE), (2, 'example', NULL, FALSE);
CREATE PROCEDURE unpivot_test(IN vars VARCHAR(255)) BEGIN SET @sql = CONCAT('SELECT * FROM t2 WHERE ', vars, ' IS NOT NULL');
PREPARE stmt FROM @sql;
EXECUTE stmt;
DEALLOCATE PREPARE stmt;
