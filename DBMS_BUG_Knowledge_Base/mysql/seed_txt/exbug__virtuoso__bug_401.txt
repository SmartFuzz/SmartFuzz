CREATE TABLE test_dates (id INT PRIMARY KEY, event_date DATE NOT NULL);
INSERT INTO test_dates (id, event_date) VALUES (1, CURDATE()), (2, CURDATE() - INTERVAL 1 DAY), (3, CURDATE() - INTERVAL 2 DAY);
SELECT id, event_date, CURDATE() - INTERVAL 1 DAY AS yesterday FROM test_dates WHERE event_date < CURDATE();
SELECT id, event_date FROM test_dates WHERE event_date = CURDATE() - INTERVAL 1 DAY OR event_date IS NULL;
SELECT DISTINCT CURDATE() AS today, CURDATE() - INTERVAL 1 DAY AS yesterday;
