CREATE TABLE test_table (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255), value FLOAT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, value) VALUES ('Sample A', 10.5), ('Sample B', NULL), ('Sample C', 20.0);
CREATE INDEX idx_value ON test_table (value);
SELECT id, name, value, RANK() OVER (ORDER BY value) as rank FROM test_table WHERE value IS NOT NULL HAVING rank <= 2;
SELECT COUNT(*) FROM test_table WHERE value > (SELECT AVG(value) FROM test_table WHERE value IS NOT NULL) OR name IS NULL;
