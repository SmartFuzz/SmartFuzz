DROP TABLE IF EXISTS my_table;
CREATE TABLE my_table (int_column INT NOT NULL, str_column VARCHAR(10));
INSERT INTO my_table VALUES (1, 'one'), (2, 'two'), (3, 'three'), (4, 'four');
PREPARE stmt FROM 'SELECT * FROM my_table WHERE int_column < ?';
SET @a = '2.5';
EXECUTE stmt USING @a;
