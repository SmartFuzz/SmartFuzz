CREATE TABLE `sales` ( `year` int(11) NOT NULL, `country` varchar(20) NOT NULL, `product` varchar(32) NOT NULL, `profit` int(11) DEFAULT 0, PRIMARY KEY (`year`, `country`, `product`) );
INSERT INTO `sales` (`year`, `country`, `product`, `profit`) VALUES (2020, 'USA', 'Gadget', 1500), (2020, 'USA', 'Widget', 2000), (2021, 'Canada', 'Gadget', 2500);
SELECT year, country, product, IFNULL(GROUPING(year), 0) AS grouping_value FROM sales GROUP BY year, country, product WITH ROLLUP HAVING GROUPING(year) = 0;
CREATE TABLE `products` ( `product_id` int(11) NOT NULL AUTO_INCREMENT, `product_name` varchar(50) NOT NULL, PRIMARY KEY (`product_id`) );
INSERT INTO `products` (`product_name`) VALUES ('Gadget'), ('Widget');
