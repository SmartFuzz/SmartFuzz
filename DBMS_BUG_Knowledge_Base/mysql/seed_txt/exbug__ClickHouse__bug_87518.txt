CREATE TABLE nats_test_jetstream_src (number INT, message VARCHAR(255), timestamp DATETIME);
CREATE TABLE nats_test_jetstream_dst (number INT, message VARCHAR(255), timestamp DATETIME);
INSERT INTO nats_test_jetstream_src (number, message, timestamp) VALUES (1, 'Test message 1', NOW()), (2, 'Test message 2', NOW()), (3, 'Test message 3', NOW());
CREATE TRIGGER nats_test_jetstream_mv AFTER INSERT ON nats_test_jetstream_src FOR EACH ROW INSERT INTO nats_test_jetstream_dst (number, message, timestamp) VALUES (NEW.number, NEW.message, NEW.timestamp);
SELECT * FROM nats_test_jetstream_dst WHERE timestamp > NOW() - INTERVAL 1 DAY;
