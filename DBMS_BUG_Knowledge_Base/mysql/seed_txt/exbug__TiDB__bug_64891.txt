SET SESSION sql_mode = 'STRICT_TRANS_TABLES';
CREATE TABLE t2 (id INT PRIMARY KEY, name VARCHAR(50) NOT NULL, age INT CHECK (age >= 0), salary DECIMAL(10,2) CHECK (salary > 0));
ALTER TABLE t2 DROP CHECK age;
INSERT INTO t2 (id, name, age, salary) VALUES (1, 'Alice', 30, 50000.00);
SELECT * FROM t2;
