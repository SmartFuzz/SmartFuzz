CREATE TABLE test_table (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(255), value BLOB, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, value) VALUES ('test1', UNHEX(REPEAT('00', 512)));
CREATE VIEW test_view AS SELECT name, value FROM test_table WHERE value IS NOT NULL;
SELECT DISTINCT name FROM test_view WHERE CHAR_LENGTH(value) IN (SELECT CHAR_LENGTH(UNHEX(REPEAT('00', 1024))) );
UPDATE test_table SET value = NULL WHERE id = 1;
