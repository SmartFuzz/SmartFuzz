CREATE TABLE source (sid INT PRIMARY KEY, sname VARCHAR(255) NOT NULL);
INSERT INTO source (sid, sname) VALUES (1, 'Source1');
CREATE TABLE target (tid INT PRIMARY KEY, tval INT DEFAULT 0 CHECK (tval >= 0));
INSERT INTO target (tid, tval) VALUES (1, 1);
UPDATE target t JOIN (SELECT sid FROM source) AS s ON t.tid = s.sid SET tval = tval + 1;
