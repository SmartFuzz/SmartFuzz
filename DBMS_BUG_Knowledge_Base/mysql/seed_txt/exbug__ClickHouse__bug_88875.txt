CREATE TABLE users (uid SMALLINT, name VARCHAR(255), age SMALLINT, email VARCHAR(255) NULL);
INSERT INTO users VALUES (1231, 'John', 33, NULL);
INSERT INTO users VALUES (6666, 'Ksenia', 48, 'ksenia@example.com');
CREATE VIEW v_users AS SELECT uid AS id, email AS user_email FROM users WHERE age > 30;
SHOW COLUMNS FROM v_users;
