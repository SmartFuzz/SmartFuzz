DROP TABLE IF EXISTS xyz;
CREATE TABLE xyz (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255), age INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO xyz (name, age) VALUES ('Alice', 30), ('Bob', NULL);
CREATE FUNCTION get_age() RETURNS INT BEGIN DECLARE user_record xyz;
DECLARE done INT DEFAULT FALSE;
DECLARE cur CURSOR FOR SELECT * FROM xyz WHERE age IS NOT NULL;
