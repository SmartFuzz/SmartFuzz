CREATE DATABASE sbtest;
CREATE TABLE `ARRAY`(id INT NOT NULL, value VARCHAR(255) DEFAULT NULL, PRIMARY KEY (id));
CREATE TABLE `FULL`(id INT NOT NULL, description TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
CREATE TABLE `INTERSECT`(id INT NOT NULL, amount DECIMAL(10, 2) CHECK (amount >= 0), PRIMARY KEY (id));
INSERT INTO `FULL` (id, description) VALUES (1, 'Test entry for FULL');
