CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), description TEXT);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description.');
SELECT IFNULL(REGEXP_REPLACE(description, '[*]', 'X'), description) FROM test_table WHERE id = 1;
SELECT name FROM test_table WHERE id = 1 AND name REGEXP '[*]';
SELECT IFNULL(REGEXP_REPLACE(NULL, '[*]', 'X'), 'NULL') AS result;
