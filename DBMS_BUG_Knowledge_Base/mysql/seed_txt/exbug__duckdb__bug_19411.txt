CREATE TABLE test_data (id VARCHAR(255), updated_at DATETIME, test_string TEXT, test_unicode TEXT, test_time TIME, test_bytes BLOB, test_smallint SMALLINT, test_integer INT, test_bigint BIGINT, test_real FLOAT, test_double DOUBLE, test_decimal DECIMAL(10,2), test_boolean BOOLEAN, test_date DATE, test_json TEXT, test_vector JSON, test_geography TEXT, tenant_id VARCHAR(255));
INSERT INTO test_data (id, updated_at, test_string, test_unicode, test_time, test_bytes, test_smallint, test_integer, test_bigint, test_real, test_double, test_decimal, test_boolean, test_date, test_json, test_vector, test_geography, tenant_id) SELECT MD5(CAST(range AS CHAR)), NOW(), RPAD('hello_', CAST(range AS UNSIGNED), '0'), CHAR(CAST(range AS UNSIGNED)), CAST(NOW() AS TIME), MD5(CAST(range AS CHAR)), (range % 32767), range, range, (range / 3.0), (range / 3.0), CAST(range AS DECIMAL(10,2)), (CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END), DATE_SUB('2022-02-24', INTERVAL (range % 3650) DAY), JSON_OBJECT('number', range, 'string', MD5(CAST(range AS CHAR))), JSON_ARRAY(CAST(range / 1.0 AS DECIMAL(10,2)), CAST(range / 2.0 AS DECIMAL(10,2)), CAST(range / 3.0 AS DECIMAL(10,2)), CAST(range / 4.0 AS DECIMAL(10,2))), ST_GeomFromText(CONCAT('POINT(', CAST(180.0 / (range + 1.0) AS CHAR), ' ', CAST(90.0 / (range + 1.0) AS CHAR), ')')) FROM (SELECT @rownum := @rownum + 1 AS range FROM (SELECT @rownum := 0) r, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t2 LIMIT 10000) AS ranges;
SELECT * FROM test_data WHERE test_unicode IS NOT NULL INTO OUTFILE 'valid_data.csv' FIELDS TERMINATED BY '|' ENCLOSED BY '"' LINES TERMINATED BY '\n';
SELECT * FROM test_data ORDER BY updated_at;
INSERT INTO test_data (id, updated_at, test_string, test_unicode, test_time, test_bytes, test_smallint, test_integer, test_bigint, test_real, test_double, test_decimal, test_boolean, test_date, test_json, test_vector, test_geography, tenant_id) SELECT MD5(CAST(range AS CHAR)), NOW(), RPAD('hello_', CAST(range AS UNSIGNED), '0'), (CASE WHEN range = 39 THEN NULL ELSE CHAR(CAST(range AS UNSIGNED)) END), CAST(NOW() AS TIME), MD5(CAST(range AS CHAR)), (range % 32767), range, range, (range / 3.0), (range / 3.0), CAST(range AS DECIMAL(10,2)), (CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END), DATE_SUB('2022-02-24', INTERVAL (range % 3650) DAY), JSON_OBJECT('number', range, 'string', MD5(CAST(range AS CHAR))), JSON_ARRAY(CAST(range / 1.0 AS DECIMAL(10,2)), CAST(range / 2.0 AS DECIMAL(10,2)), CAST(range / 3.0 AS DECIMAL(10,2)), CAST(range / 4.0 AS DECIMAL(10,2))), ST_GeomFromText(CONCAT('POINT(', CAST(180.0 / (range + 1.0) AS CHAR), ' ', CAST(90.0 / (range + 1.0) AS CHAR), ')')) FROM (SELECT @rownum := @rownum + 1 AS range FROM (SELECT @rownum := 0) r, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t2 LIMIT 10000) AS ranges;
