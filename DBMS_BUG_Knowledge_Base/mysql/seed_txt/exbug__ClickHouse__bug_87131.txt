CREATE TABLE test2 (id BIGINT, value INT, arr JSON NULL) ENGINE = InnoDB;
INSERT INTO test2 (id, value, arr) SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, JSON_ARRAY('a', 'b', 'c') FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) AS numbers, (SELECT @rownum := 0) AS r) AS numbers;
CREATE TABLE test3 LIKE test2;
INSERT INTO test3 SELECT * FROM test2;
SELECT arr, JSON_LENGTH(arr) FROM test2 WHERE value IS NOT NULL ORDER BY value LIMIT 3;
SELECT id, value, JSON_UNQUOTE(JSON_EXTRACT(arr, '$[0]')) FROM test3 WHERE JSON_LENGTH(arr) > 0;
