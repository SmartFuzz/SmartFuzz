CREATE TABLE test_distinct_oom_v2 ( id BIGINT, value VARCHAR(255), created_at DATETIME DEFAULT CURRENT_TIMESTAMP );
INSERT INTO test_distinct_oom_v2 (id, value) SELECT number, CASE WHEN number % 2 = 0 THEN CAST(number AS CHAR) ELSE NULL END FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT @rownum := 0) t2 LIMIT 100000000) AS numbers;
SELECT DISTINCT value FROM test_distinct_oom_v2;
CREATE TABLE test_distinct_oom_v3 ( id INT, value DOUBLE, category VARCHAR(255) DEFAULT 'unknown' );
INSERT INTO test_distinct_oom_v3 (id, value, category) SELECT number, number / 3.14, CAST(number % 10 AS CHAR) FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT @rownum := 0) t2 LIMIT 1000000) AS numbers;
