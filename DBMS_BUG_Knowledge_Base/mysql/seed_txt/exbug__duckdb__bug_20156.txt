CREATE TABLE test_data (id INT, value DOUBLE, category VARCHAR(255));
INSERT INTO test_data (id, value, category) VALUES (1, 50.0, 'A'), (2, 50.0, 'B'), (3, NULL, 'A'), (4, 100.0, 'B');
CREATE TEMPORARY TABLE union_data AS SELECT value FROM test_data WHERE category = 'A' UNION ALL SELECT value FROM test_data WHERE category = 'B';
SELECT value, @cumulative_sum := IFNULL(@cumulative_sum, 0) + value AS cumulative_sum FROM union_data, (SELECT @cumulative_sum := 0) AS init;
CREATE TABLE boundary_test (num INT NOT NULL CHECK (num >= 0));
