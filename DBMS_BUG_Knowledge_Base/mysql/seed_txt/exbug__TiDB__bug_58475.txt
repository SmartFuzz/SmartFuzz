DROP TABLE IF EXISTS tp;
CREATE TABLE tp (id INT, c1 JSON, c2 JSON AS (c1), c3 INT NOT NULL DEFAULT 0, KEY idx_31 (id)) PARTITION BY RANGE (id) (PARTITION p0 VALUES LESS THAN (2), PARTITION p1 VALUES LESS THAN (4532022));
INSERT INTO tp (id, c1, c3) VALUES (1, '[1]', 10);
INSERT INTO tp (id, c1) VALUES (0, '[2]');
SELECT id FROM tp WHERE JSON_CONTAINS(c2, '1') OR c3 IS NOT NULL GROUP BY id HAVING id IN (0, 1);
