CREATE TABLE colltest2 ( id INT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT, cbin VARBINARY(140), ctxt TEXT CHARACTER SET utf8mb3 COLLATE utf8mb3_bin, cstr VARCHAR(255) CHARACTER SET utf8mb4, cnum INT DEFAULT NULL);
INSERT INTO colltest2 (cbin, ctxt, cstr, cnum) VALUES (B'asdf', 'asdf', 'asdf', NULL);
SELECT * FROM colltest2 WHERE cbin RLIKE cstr;
SELECT * FROM colltest2 WHERE cbin RLIKE ctxt;
SELECT * FROM colltest2 WHERE cnum IS NULL;
