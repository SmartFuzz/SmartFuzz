CREATE TABLE partitioned_table (id INT AUTO_INCREMENT PRIMARY KEY, value INT, description TEXT);
CREATE TABLE partitioned_table_1 LIKE partitioned_table;
INSERT INTO partitioned_table_1 (value, description) SELECT value, description FROM partitioned_table WHERE value >= 1 AND value < 10;
CREATE INDEX idx_value ON partitioned_table (value);
INSERT INTO partitioned_table (value, description) VALUES (5, NULL), (NULL, 'Test'), (15, 'Boundary Test');
SELECT * FROM partitioned_table WHERE value IS NULL OR value < 10;
