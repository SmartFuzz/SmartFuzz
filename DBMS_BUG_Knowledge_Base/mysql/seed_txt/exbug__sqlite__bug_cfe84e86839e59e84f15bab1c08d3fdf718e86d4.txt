CREATE TABLE parent_view (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255) NOT NULL);
CREATE TABLE child_view (id INT AUTO_INCREMENT PRIMARY KEY, parent_id INT, value INT, FOREIGN KEY(parent_id) REFERENCES parent_view(id));
INSERT INTO parent_view (name) VALUES ('Parent 1'), ('Parent 2');
INSERT INTO child_view (parent_id, value) VALUES (1, 10), (1, 20), (2, NULL);
CREATE VIEW nested_view AS SELECT p.name, c.value FROM parent_view p LEFT JOIN child_view c ON p.id = c.parent_id ORDER BY p.id, c.value DESC;
