CREATE TABLE example (id INT AUTO_INCREMENT PRIMARY KEY, direction ENUM('UP', 'DOWN') NOT NULL, below BOOLEAN NOT NULL);
ALTER TABLE example ADD CONSTRAINT ck_example_direction CHECK ((direction = 'DOWN' AND below) OR (NOT below));
ALTER TABLE example DROP CHECK ck_example_direction;
ALTER TABLE example MODIFY direction ENUM('UP', 'DOWN', 'UNKNOWN');
ALTER TABLE example ADD CONSTRAINT ck_example_direction CHECK ((direction = 'DOWN' AND below) OR (NOT below));
