CREATE TABLE test_intervals (id INT AUTO_INCREMENT PRIMARY KEY, interval_col VARCHAR(7), description TEXT);
INSERT INTO test_intervals (interval_col, description) VALUES (CONCAT('1-2'), 'Valid interval');
INSERT INTO test_intervals (interval_col, description) VALUES (NULL, 'NULL interval');
SELECT id, interval_col FROM test_intervals WHERE interval_col IS NOT NULL;
SELECT id, CONCAT('2-3') AS new_interval FROM test_intervals;
