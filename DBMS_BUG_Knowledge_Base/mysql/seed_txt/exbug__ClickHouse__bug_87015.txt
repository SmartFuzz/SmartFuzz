CREATE TABLE test_table1 (id INT, name VARCHAR(255), created_at DATETIME);
SELECT IF(EXISTS(SELECT * FROM information_schema.engines WHERE engine = 'NonExistentEngine'), 'Engine exists', 'Engine does not exist') AS engine_check;
GRANT ALL PRIVILEGES ON test_table1 TO user1;
CREATE TABLE test_table2 (value FLOAT, description VARCHAR(255));
GRANT ALL PRIVILEGES ON test_table2 TO user2;
SELECT * FROM test_table1 WHERE id = (SELECT MAX(id) FROM test_table1);
