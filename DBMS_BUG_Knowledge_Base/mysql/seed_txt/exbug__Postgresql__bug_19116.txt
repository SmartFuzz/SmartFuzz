CREATE TABLE t(p BOOLEAN, q INT, r TEXT DEFAULT 'default');
INSERT INTO t(p, q) VALUES (NULL, 1);
SELECT GET_LOCK('lock_2', 10);
UPDATE t JOIN (SELECT @row_id := id FROM t WHERE p IS NULL FOR UPDATE) AS lock_t ON t.id = @row_id SET q = q + 1;
UPDATE t SET r = 'updated' WHERE id = (SELECT id FROM t FOR UPDATE LIMIT 1);
SELECT RELEASE_LOCK('lock_2');
