CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, amount DECIMAL(10, 2), status BOOLEAN);
INSERT INTO test_table (id, name, amount, status) VALUES (1, 'Test Product', 99.99, TRUE);
INSERT INTO test_table (id, name, amount, status) VALUES (2, 'Another Product', NULL, FALSE);
SELECT name, amount FROM test_table WHERE amount IS NOT NULL ORDER BY amount DESC LIMIT 5;
SELECT * FROM (SELECT * FROM test_table ORDER BY created_at DESC LIMIT 10) AS subquery LIMIT 3;
