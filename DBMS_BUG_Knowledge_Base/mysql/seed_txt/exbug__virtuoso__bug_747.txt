CREATE TABLE language_tags (id INT PRIMARY KEY, tag VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO language_tags (id, tag) VALUES (1, 'en'), (2, 'fr'), (3, 'de'), (4, NULL), (5, 'es');
UPDATE language_tags SET tag = 'it' WHERE id = 3;
SELECT DISTINCT tag, COALESCE(tag, 'unknown') AS lang FROM language_tags WHERE tag IS NOT NULL;
DELETE FROM language_tags WHERE tag IS NULL;
