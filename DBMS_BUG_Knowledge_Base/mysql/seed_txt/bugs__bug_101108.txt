CREATE TABLE test_table (id INT PRIMARY KEY, value FLOAT, status VARCHAR(10) DEFAULT 'active');
INSERT INTO test_table (id, value) VALUES (1, NULL), (2, RAND() * 10000), (3, RAND() * 10000), (4, NULL), (5, RAND() * 10000);
SET SESSION optimizer_switch = 'derived_merge=on';
SELECT id, IF(RAND() < 0.5, value, NULL) AS computed_value FROM test_table WHERE computed_value IS NOT NULL;
SET SESSION optimizer_switch = 'derived_merge=off';
