CREATE TABLE comment (id INT AUTO_INCREMENT PRIMARY KEY, path VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO comment (path) VALUES ('0.1.2.3'), ('0.1.2.4'), (NULL);
INSERT INTO comment (path) SELECT CONCAT('0.', n) FROM (SELECT @row := @row + 1 AS n FROM (SELECT 0 UNION ALL SELECT 0) t1, (SELECT 0 UNION ALL SELECT 0) t2, (SELECT @row := 0) r) AS numbers WHERE n <= 500) AS seq;
CREATE INDEX idx_path ON comment (path);
SELECT * FROM comment WHERE path IS NOT NULL;
