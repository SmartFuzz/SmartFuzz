CREATE TABLE test_strings (id INT PRIMARY KEY, title VARCHAR(255) NOT NULL, lang_title VARCHAR(255));
INSERT INTO test_strings (id, title, lang_title) VALUES (1, 'Sample Title', 'Sample Title');
SELECT id, title, SUBSTRING_INDEX(lang_title, '@', -1) AS lang FROM test_strings WHERE lang_title IS NOT NULL;
SELECT CASE WHEN lang_title IS NOT NULL THEN 'rdf:langString' ELSE NULL END AS datatype FROM test_strings WHERE id = 1;
SELECT 'datatype' AS datatype WHERE EXISTS (SELECT 1 FROM test_strings WHERE lang_title IS NOT NULL LIMIT 1);
