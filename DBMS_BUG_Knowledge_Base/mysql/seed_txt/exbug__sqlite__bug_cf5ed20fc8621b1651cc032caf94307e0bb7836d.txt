CREATE TABLE user ( id INT NOT NULL, active TINYINT NOT NULL, account INT, PRIMARY KEY(id) );
CREATE TABLE account ( id INT NOT NULL, name VARCHAR(255), balance FLOAT DEFAULT 0.0, PRIMARY KEY(id) );
INSERT INTO user VALUES (1, 1, 1);
INSERT INTO account VALUES (1, 'test', 100.0);
SELECT user.id FROM user INNER JOIN account ON user.account = account.id WHERE user.active = 1;
