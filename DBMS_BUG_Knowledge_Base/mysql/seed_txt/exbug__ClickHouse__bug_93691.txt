CREATE TABLE test_enum1 (status ENUM('active', 'inactive', 'pending') DEFAULT 'active', status_value INT DEFAULT 0) ENGINE=InnoDB;
CREATE TABLE test_enum2 (level ENUM('low', 'medium', 'high') DEFAULT 'low', level_value INT DEFAULT 0) ENGINE=InnoDB;
INSERT INTO test_enum1 (status, status_value) VALUES ('active', 0), ('inactive', 0), ('pending', 1);
INSERT INTO test_enum2 (level, level_value) VALUES ('low', 0), ('medium', 10), ('high', 20);
SELECT * FROM test_enum1 WHERE status = 'pending';
