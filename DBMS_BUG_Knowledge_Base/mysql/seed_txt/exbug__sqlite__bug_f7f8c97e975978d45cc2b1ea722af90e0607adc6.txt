CREATE TABLE t2(a INT, b FLOAT, c VARCHAR(255), d INT DEFAULT 0);
INSERT INTO t2 VALUES(1, 2.5, 'test1', NULL), (2, 3.5, 'test2', 1), (3, NULL, 'test3', 2);
SELECT a, SUM(b) AS total_b FROM t2 GROUP BY a HAVING total_b > 2.0;
CREATE TABLE t3(e INT PRIMARY KEY, f VARCHAR(255) NOT NULL);
INSERT INTO t3 (e, f) SELECT a, c FROM t2 WHERE b IS NOT NULL;
