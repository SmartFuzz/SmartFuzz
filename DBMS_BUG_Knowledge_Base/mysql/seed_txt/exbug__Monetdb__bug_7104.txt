CREATE SCHEMA test_schema;
USE test_schema;
CREATE TABLE test_table(id INT PRIMARY KEY, name VARCHAR(50), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table(id, name, value) VALUES (1, 'A', 10.5), (2, 'B', 20.5), (3, 'C', 30.5), (4, 'D', 40.5), (5, 'E', 50.5);
SELECT id, name, value, FLOOR((ROW_NUMBER() OVER(ORDER BY value) - 1) / (COUNT(*) OVER() / 3)) + 1 AS bucket FROM test_table;
