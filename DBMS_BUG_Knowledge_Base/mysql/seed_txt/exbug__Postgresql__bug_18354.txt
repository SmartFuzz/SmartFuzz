SET SESSION max_heap_table_size = 102400;
CREATE TABLE t (a TEXT, b INT, c TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t (a, b) VALUES ('test', 1);
INSERT INTO t (a, b) SELECT CONCAT('test_', n.n), n.n FROM (SELECT @rownum := @rownum + 1 AS n FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT @rownum := 0) t2 LIMIT 20) n;
SELECT * FROM t WHERE b IS NULL OR c > NOW() - INTERVAL 1 DAY;
