DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (id INT, name VARCHAR(255), description TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2 (id, name, description) VALUES (1, 'Test', REPEAT('x', 600)), (2, 'Sample', REPEAT('y', 700));
SELECT id, GROUP_CONCAT(name) AS concatenated_names FROM t2 GROUP BY id;
SELECT id, GROUP_CONCAT(description) AS concatenated_descriptions FROM t2 GROUP BY id UNION SELECT 3, 'Extra data';
