CREATE FUNCTION cube (p INT) RETURNS INT DETERMINISTIC BEGIN RETURN p * p * p;
END;
CREATE FUNCTION myfunc2 (p INT) RETURNS INT DETERMINISTIC BEGIN RETURN cube(p) + 2;
END;
CREATE TABLE numbers (value INT NOT NULL CHECK (value > 0));
INSERT INTO numbers (value) VALUES (5), (10), (15);
