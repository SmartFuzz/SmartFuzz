CREATE TABLE test_table (id INT, value BINARY(8));
INSERT INTO test_table VALUES (1, BINARY '101'), (2, NULL), (3, BINARY '110');
SELECT GROUP_CONCAT(value) FROM test_table WHERE value IS NOT NULL;
SELECT GROUP_CONCAT(value) FROM test_table GROUP BY id;
SELECT GROUP_CONCAT(value) FROM (SELECT NULL AS value UNION ALL SELECT BINARY '111' AS value) AS subquery;
