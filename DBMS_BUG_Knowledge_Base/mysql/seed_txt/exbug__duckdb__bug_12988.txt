CREATE TABLE employees (id INT PRIMARY KEY, name VARCHAR(255), salary DECIMAL(10, 2), department_id INT);
CREATE TABLE departments (id INT PRIMARY KEY, name VARCHAR(255) NOT NULL);
INSERT INTO employees (id, name, salary, department_id) VALUES (1, 'John Doe', 50000.00, 1), (2, 'Jane Smith', 60000.00, 2), (3, 'Alice Johnson', 55000.00, 1);
INSERT INTO departments (id, name) VALUES (1, 'Engineering'), (2, 'Marketing');
SELECT * FROM departments d JOIN (SELECT *, RANK() OVER (ORDER BY salary) AS salary_rank FROM employees) ranked_employees ON d.id = ranked_employees.department_id;
