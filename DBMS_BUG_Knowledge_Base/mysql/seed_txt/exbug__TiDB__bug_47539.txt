SET SESSION sql_mode = 'STRICT_TRANS_TABLES,NO_ENGINE_SUBSTITUTION';
CREATE TABLE t2 ( id INT, name VARCHAR(50), created_at DATETIME DEFAULT CURRENT_TIMESTAMP, score FLOAT CHECK (score >= 0));
INSERT INTO t2(id, name, score) VALUES (1, 'Alice', 95.5), (2, 'Bob', NULL), (3, 'Charlie', 0);
ALTER TABLE t2 ADD UNIQUE INDEX idx_name(name);
SELECT * FROM t2 WHERE score IS NULL OR score > 90;
