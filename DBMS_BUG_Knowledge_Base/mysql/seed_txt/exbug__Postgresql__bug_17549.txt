CREATE TABLE def (x INT, y TEXT, z BOOLEAN DEFAULT TRUE);
INSERT INTO def SELECT FLOOR(RAND() * 10000), CONCAT(FLOOR(RAND() * 10000), '');
CREATE INDEX idx_def_x_lower_y ON def(x, LOWER(y));
SET @app_x = 1;
SELECT * FROM def WHERE x = @app_x AND LOWER(y) = '1234';
