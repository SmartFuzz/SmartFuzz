CREATE TABLE t4 (id INT NOT NULL AUTO_INCREMENT, name VARCHAR(100) NOT NULL, value DECIMAL(10,2) NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO t4 (name, value) VALUES ('test1', 123.45);
INSERT INTO t4 (name, value) VALUES ('test2', NULL);
SELECT id, name, @row_num := @row_num + 1 AS row_num FROM (SELECT id, name, created_at FROM t4 WHERE value IS NOT NULL ORDER BY created_at DESC) AS temp, (SELECT @row_num := 0) AS r;
SELECT id, name, @row_num := @row_num + 1 AS row_num FROM (SELECT id, name, value, created_at FROM t4 ORDER BY created_at ASC) AS temp, (SELECT @row_num := 0) AS r GROUP BY value;
