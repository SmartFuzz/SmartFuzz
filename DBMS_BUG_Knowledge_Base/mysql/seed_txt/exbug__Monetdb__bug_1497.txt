CREATE TABLE test_table (id BIGINT PRIMARY KEY, name VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name) VALUES (1, 'Test Entry');
CREATE TABLE a_sequence (id INT AUTO_INCREMENT PRIMARY KEY);
INSERT INTO a_sequence VALUES (NULL);
SELECT id, name FROM test_table WHERE id = (SELECT MAX(id) FROM test_table);
