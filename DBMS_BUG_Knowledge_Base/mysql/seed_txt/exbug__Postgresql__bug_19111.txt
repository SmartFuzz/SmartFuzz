CREATE TABLE part4 (key INT, val TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO part4 VALUES (3, 'initial value');
CREATE TABLE part5 (key INT PRIMARY KEY, val TEXT NOT NULL, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO part5 (key, val) VALUES (4, NULL);
INSERT INTO part4 (key, val) SELECT s.key, s.val FROM (SELECT 3 AS key, 'merge3' AS val) s WHERE NOT EXISTS (SELECT 1 FROM part4 t WHERE t.key = s.key);
UPDATE part4 t JOIN (SELECT 3 AS key, 'merge3' AS val) s ON s.key = t.key SET t.val = COALESCE(t.val, '') || ' updated';
