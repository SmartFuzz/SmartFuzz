CREATE TABLE t(p POINT, id INT AUTO_INCREMENT PRIMARY KEY);
INSERT INTO t(p) SELECT POINT(g, g) FROM (SELECT @rownum := @rownum + 1 AS g FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) AS a, (SELECT @rownum := 0) AS r LIMIT 1000) AS series;
INSERT INTO t(p) VALUES (POINT(0, NULL));
CREATE INDEX spgist_idx ON t(p);
SELECT * FROM t WHERE p IS NULL;
