CREATE TABLE `employees` ( `emp_id` int(11) NOT NULL, `emp_name` varchar(50) NOT NULL, `salary` decimal(10,2) NOT NULL, `dept_id` int(11) NOT NULL, PRIMARY KEY (`emp_id`) ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
INSERT INTO employees (emp_id, emp_name, salary, dept_id) VALUES (1, 'John Doe', 75000.00, 1);
INSERT INTO employees (emp_id, emp_name, salary, dept_id) VALUES (2, 'Jane Smith', NULL, 2);
SELECT COUNT(DISTINCT dept_id) AS unique_departments FROM employees WHERE salary IS NOT NULL;
ANALYZE TABLE employees;
