CREATE DATABASE polaris_iceberg;
CREATE TABLE polaris_iceberg.`teslasource_kafkav6_source-data` (id INT UNSIGNED, name VARCHAR(255), created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO polaris_iceberg.`teslasource_kafkav6_source-data` (id, name, created_at) VALUES (1, 'test1', NOW()), (2, 'test2', NOW());
SELECT * FROM polaris_iceberg.`teslasource_kafkav6_source-data` WHERE id IS NOT NULL;
SHOW TABLES IN polaris_iceberg;
