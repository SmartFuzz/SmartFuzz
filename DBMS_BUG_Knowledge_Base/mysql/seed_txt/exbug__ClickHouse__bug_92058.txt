CREATE TABLE dst (e ENUM('a', 'b'), f INT) ENGINE = MEMORY;
CREATE TABLE src (e ENUM('a'), g INT NULL) ENGINE = MEMORY;
CREATE VIEW mv AS SELECT e, g FROM src;
INSERT INTO src (e, g) SELECT CASE WHEN number % 1 = 0 THEN 'a' ELSE NULL END AS e, number % 10 AS g FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t, (SELECT @rownum := 0) r) AS random_numbers LIMIT 10;
INSERT INTO dst (e, g) SELECT e, g FROM mv WHERE g IS NOT NULL;
