CREATE TABLE users (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);
CREATE TABLE orders (id INT AUTO_INCREMENT PRIMARY KEY, user_id INT, amount DECIMAL(10, 2), FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO orders (user_id, amount) VALUES (1, 100.50), (2, 200.75), (1, NULL);
SELECT * FROM (SELECT * FROM users WHERE age IS NOT NULL UNION SELECT * FROM orders WHERE amount > 100) AS combined LIMIT 1;
