CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name) SELECT id, CONCAT('Name ', id) FROM (SELECT @rownum := @rownum + 1 AS id FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) AS a, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) AS b, (SELECT @rownum := -1) AS r) AS numbers WHERE id <= 2500000000;
CREATE TABLE another_table (value INT, description TEXT, is_active BOOLEAN DEFAULT TRUE);
INSERT INTO another_table (value, description) SELECT id, CONCAT('Description ', id) FROM test_table WHERE id < 2147483647;
SELECT COUNT(*) FROM another_table WHERE value IS NOT NULL;
