DROP PROCEDURE IF EXISTS p_json_test;
CREATE PROCEDURE p_json_test() BEGIN DECLARE jdata LONGTEXT;
SET jdata = JSON_OBJECT('test', (SELECT NULL AS region));
END;
CALL p_json_test();
SELECT jdata FROM (SELECT JSON_OBJECT('test', (SELECT NULL AS region)) AS jdata) AS x;
