CREATE TABLE tradeDate(date INT NOT NULL, price DECIMAL(10, 2) NOT NULL, PRIMARY KEY (date));
INSERT INTO tradeDate(date, price) VALUES (20230101, 100.50);
CREATE TABLE tradeDetails(trade_id INT NOT NULL, trade_date INT NOT NULL, quantity INT NOT NULL, PRIMARY KEY (trade_id, trade_date), FOREIGN KEY (trade_date) REFERENCES tradeDate(date));
INSERT INTO tradeDetails(trade_id, trade_date, quantity) VALUES (1, 20230101, 10);
SELECT @row_num := @row_num + 1 AS row_num, td.trade_id FROM tradeDetails td JOIN tradeDate t ON td.trade_date = t.date, (SELECT @row_num := 0) AS r ORDER BY td.quantity;
