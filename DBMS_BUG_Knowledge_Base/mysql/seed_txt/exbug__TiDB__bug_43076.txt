CREATE TABLE tx(s1 BLOB, s2 BLOB, s3 VARCHAR(255) NOT NULL DEFAULT 'default');
INSERT INTO tx VALUES (AES_ENCRYPT('a', 'b'), AES_ENCRYPT('c', 'd'), NULL);
INSERT INTO tx VALUES (AES_ENCRYPT('e', 'f'), AES_ENCRYPT('g', 'h'), 'test');
SELECT * FROM tx WHERE AES_DECRYPT(s1, 'b') IS NULL OR AES_DECRYPT(s2, 'd') IS NULL;
SELECT * FROM tx WHERE AES_DECRYPT(s1, 'b')='' AND s3='default';
