CREATE TABLE test_table(id INT, name VARCHAR(255), value FLOAT, created_at TIMESTAMP);
INSERT INTO test_table(id, name, value, created_at) VALUES (1, 'Alice', 10.5, NOW()), (2, 'Bob', 20.0, NOW()), (3, 'Charlie', NULL, NOW());
SELECT @row_num := @row_num + 1 AS row_num, name FROM (SELECT name FROM test_table WHERE value IS NOT NULL GROUP BY name) AS subquery, (SELECT @row_num := 0) AS r;
CREATE TABLE another_table(ref_id INT, description VARCHAR(255));
INSERT INTO another_table(ref_id, description) VALUES (1, 'Test 1'), (2, 'Test 2'), (3, NULL);
