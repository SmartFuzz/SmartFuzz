CREATE DATABASE test_db COLLATE latin1_swedish_ci;
USE test_db;
CREATE TABLE test_table (id INT UNSIGNED NOT NULL PRIMARY KEY, name VARCHAR(100) NOT NULL, description VARCHAR(200) NOT NULL COLLATE utf8mb4_unicode_ci);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description');
PREPARE stmt2 FROM 'SELECT * FROM test_table WHERE name LIKE ? OR description LIKE ?';
