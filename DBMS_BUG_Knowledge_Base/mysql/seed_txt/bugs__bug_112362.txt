CREATE TABLE c (c_id INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT, value VARCHAR(100) NOT NULL COLLATE utf8mb4_bin, UNIQUE KEY ix_value_unique (value));
CREATE TABLE d (d_id INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT, value VARCHAR(100) NOT NULL COLLATE utf8mb4_bin, KEY ix_value (value));
INSERT INTO c (value) SELECT CONCAT('value_', seq) FROM (SELECT @rownum := @rownum + 1 AS seq FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t2, (SELECT @rownum := 0) r) AS seq_table LIMIT 1000);
INSERT INTO d (value) SELECT CONCAT('value_', seq) FROM (SELECT @rownum := @rownum + 1 AS seq FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t2, (SELECT @rownum := 0) r) AS seq_table LIMIT 1000000);
SELECT DISTINCT value FROM d WHERE value NOT IN (SELECT value FROM c);
