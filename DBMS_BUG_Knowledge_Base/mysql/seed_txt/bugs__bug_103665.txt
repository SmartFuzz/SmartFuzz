CREATE DATABASE db2;
CREATE TABLE db2.users(id INT PRIMARY KEY, name VARCHAR(50), age INT);
INSERT INTO db2.users VALUES (1, 'Alice', 30), (2, 'Bob', NULL);
UPDATE db2.users AS u1, (SELECT 1 AS id) AS u2 SET u1.age = 25 WHERE u1.id = u2.id;
UPDATE (SELECT 1 AS id) AS u2, db2.users AS u1 SET u1.name = 'Charlie' WHERE u1.id = u2.id;
