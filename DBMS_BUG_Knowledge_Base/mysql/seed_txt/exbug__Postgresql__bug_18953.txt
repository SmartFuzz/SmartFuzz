CREATE TABLE tbl1(a INT NOT NULL, b TEXT DEFAULT 'default');
CREATE TABLE tbl2(b INT CHECK (b >= 0), c FLOAT);
CREATE TABLE tbl3(c INT PRIMARY KEY, d VARCHAR(50) UNIQUE);
INSERT INTO tbl1(a, b) VALUES (0, NULL), (1, 'test'), (2, 'example');
SELECT * FROM tbl1 LEFT JOIN (SELECT CASE WHEN a IS NULL THEN 1 ELSE subq_3.cc END AS result FROM tbl1, (SELECT 1 FROM tbl2 t1, tbl2 t2, tbl2 t3, tbl2 t4) AS subq_1, (SELECT tbl3.c AS cc FROM tbl3, tbl2 t1, tbl2 t2 LIMIT 1) AS subq_3) AS subq_4 ON TRUE;
