DROP TABLE IF EXISTS employee_data;
CREATE TABLE employee_data (id INT UNSIGNED, name VARCHAR(255), salary FLOAT);
INSERT INTO employee_data (id, name, salary) VALUES (1, 'Alice', 50000.0), (2, 'Bob', NULL), (3, 'Charlie', 60000.5);
ALTER TABLE employee_data ADD COLUMN IF NOT EXISTS bonus FLOAT;
UPDATE employee_data SET bonus = CASE WHEN salary IS NULL THEN NULL ELSE salary * 0.1 END WHERE id = 2;
