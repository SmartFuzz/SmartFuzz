CREATE TABLE `EXAMPLE_1` (id INT PRIMARY KEY, name VARCHAR(100), age INT, created_at TIMESTAMP);
INSERT INTO `EXAMPLE_1` VALUES (1, 'Virtuoso', 5, CURRENT_TIMESTAMP);
CREATE TABLE `EXAMPLE_2` (id INT PRIMARY KEY, description TEXT, is_active BOOLEAN);
INSERT INTO `EXAMPLE_2` VALUES (1, 'Test description', TRUE);
SELECT e1.name, e2.description FROM `EXAMPLE_1` e1 JOIN `EXAMPLE_2` e2 ON e1.id = e2.id WHERE e1.age IS NOT NULL;
