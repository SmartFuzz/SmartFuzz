CREATE TABLE rt1 (id INT AUTO_INCREMENT PRIMARY KEY, minX INT, minY INT);
CREATE VIEW v1 AS SELECT 2 AS id;
INSERT INTO rt1(minX, minY) VALUES (1, 1);
SELECT * FROM v1 CROSS JOIN rt1 ON rt1.minX IN (SELECT 2) WHERE rt1.minY > 0;
INSERT INTO rt1(minX, minY) VALUES (NULL, 2);
