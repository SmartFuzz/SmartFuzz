CREATE TABLE dp_organisation_extended (OrganisationId INT PRIMARY KEY, ParentId INT, Name VARCHAR(100), Active BOOLEAN DEFAULT TRUE);
INSERT INTO dp_organisation_extended VALUES (1, NULL, 'Org A', TRUE), (2, 1, 'Org B', TRUE), (3, 2, 'Org C', FALSE);
CREATE TABLE dp_member (MemberId INT PRIMARY KEY, OrganisationId INT, JoinDate DATE, FOREIGN KEY (OrganisationId) REFERENCES dp_organisation_extended(OrganisationId));
INSERT INTO dp_member VALUES (100, 1, '2023-01-01'), (200, 2, '2023-02-01'), (300, 3, '2023-03-01');
SELECT * FROM dp_member m WHERE m.OrganisationId IN (SELECT OrganisationId FROM dp_organisation_extended WHERE Active = TRUE);
