CREATE TABLE t1 (id INT, name VARCHAR(255), age TINYINT, salary DOUBLE, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t1 (id, name, age, salary) VALUES (1, 'Alice', 30, 70000.50);
INSERT INTO t1 (id, name, age, salary) SELECT number, CONCAT('User', number), number % 100, number * 1000.0 FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) AS numbers, (SELECT @rownum := 0) AS r) AS temp;
CREATE TABLE t2 (product_id INT, product_name VARCHAR(255), price FLOAT, stock INT NULL);
INSERT INTO t2 (product_id, product_name, price, stock) SELECT id, name, price, NULL FROM (SELECT number AS id, CONCAT('Product', number) AS name, number * 10.5 AS price FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) AS numbers, (SELECT @rownum := 0) AS r) AS temp);
