CREATE TABLE repl_bar (id INT UNSIGNED, value DOUBLE, created_at DATETIME);
INSERT INTO repl_bar VALUES (1, 3.14, NOW()), (2, 2.71, NOW()), (3, NULL, NOW());
CREATE TABLE repl_bar_dist AS SELECT * FROM repl_bar;
SELECT value FROM repl_bar_dist WHERE id = 2 ORDER BY created_at DESC LIMIT 1;
SELECT COUNT(*) FROM (SELECT DISTINCT value FROM repl_bar_dist WHERE value IS NOT NULL) AS subquery;
