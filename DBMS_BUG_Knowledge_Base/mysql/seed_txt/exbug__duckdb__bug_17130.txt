CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name) VALUES (1, 'Alice');
CREATE VIEW test_view AS SELECT @rownum := @rownum + 1 AS view_rowid, id, name FROM test_table, (SELECT @rownum := 0) r WHERE id IS NOT NULL;
SELECT * FROM test_view;
SHOW COLUMNS FROM test_view;
