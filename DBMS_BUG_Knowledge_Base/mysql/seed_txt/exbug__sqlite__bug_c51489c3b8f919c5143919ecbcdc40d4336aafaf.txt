CREATE TABLE t (label VARCHAR(10), step INT, value FLOAT NOT NULL DEFAULT 0.0);
INSERT INTO t (label, step, value) VALUES('a', 1, 1.1);
INSERT INTO t (label, step, value) VALUES('a', 1, 1.1);
INSERT INTO t (label, step, value) VALUES('b', 1, 2.2);
WITH RECURSIVE cte AS (SELECT DISTINCT label, step FROM t UNION ALL SELECT label, step + 1 FROM cte WHERE step < 3) SELECT * FROM cte;
