CREATE TABLE rocksDB (A BIGINT UNSIGNED, B VARCHAR(255));
INSERT INTO rocksDB (A, B) VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
SELECT A, B FROM rocksDB WHERE A = 1;
SELECT A, B FROM rocksDB WHERE A IS NULL;
