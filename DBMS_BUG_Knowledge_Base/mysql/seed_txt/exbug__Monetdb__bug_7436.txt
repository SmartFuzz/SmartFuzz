CREATE TABLE v1 ( id INT PRIMARY KEY, value VARCHAR(255) NOT NULL );
INSERT INTO v1 (id, value) VALUES (1, 'test'), (2, NULL), (3, 'example');
CREATE TABLE v2 ( a DOUBLE, b INT DEFAULT 0, c DATE );
INSERT INTO v2 (a, b, c) VALUES (NULL, 1, '2023-01-01'), (5.5, 2, NULL), (3.14, NULL, '2023-01-03');
DELETE FROM v2 WHERE ( a IS NULL OR ( SELECT COUNT(*) FROM (SELECT a, DENSE_RANK() OVER ( PARTITION BY b ORDER BY a ) AS rank FROM v2) AS ranked WHERE rank < 2 ) > 0 );
