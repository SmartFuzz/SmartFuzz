CREATE TABLE t2(id INT PRIMARY KEY, name VARCHAR(50), age INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2(name, age) VALUES ('Alice', 30), ('Bob', 25);
SHOW COLUMNS FROM t2 LIKE 'name';
SHOW COLUMNS FROM t2 LIKE 'a%';
SHOW COLUMNS FROM t2 LIKE 'age';
