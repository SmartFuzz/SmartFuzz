CREATE TABLE test_intervals (id INT AUTO_INCREMENT PRIMARY KEY, duration INT NOT NULL);
INSERT INTO test_intervals (duration) VALUES (60);
SELECT duration, SEC_TO_TIME(duration) AS time_value FROM test_intervals;
SELECT id, SEC_TO_TIME(duration) AS time_value FROM test_intervals WHERE duration IS NOT NULL;
INSERT INTO test_intervals (duration) VALUES (120);
