CREATE TABLE test_graphs (id INT PRIMARY KEY, graph_name VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_graphs (id, graph_name) VALUES (1, 'graph1'), (2, 'graph2'), (3, 'graph3');
DELETE FROM rdf_quad WHERE g IN (SELECT graph_name FROM test_graphs WHERE id IN (1, 2));
DELETE FROM test_graphs WHERE id IN (1, 2);
OPTIMIZE TABLE rdf_quad;
