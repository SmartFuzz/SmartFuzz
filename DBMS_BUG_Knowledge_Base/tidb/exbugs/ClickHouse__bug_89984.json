{
  "url": "https://github.com/ClickHouse/ClickHouse/issues/89984",
  "title": "Trailing commas don't work for `GROUP BY`",
  "description": "### Company or project name\nClickHouse Inc.\n### Use case\nFor consistency, it would be nice to use trailing commas in different parts of SELECT statements\n### Describe the solution you'd like\n```sql\nCREATE TABLE users (uid Int16, name String, age Int16) ENGINE = Memory;\nINSERT INTO users VALUES (1231, 'John', 33);\nINSERT INTO users VALUES (6666, 'Ksenia', 48);\nINSERT INTO users VALUES (8888, 'Alice', 50);\n-- This works\nSELECT\nuid, avg(age),  -- trailing comma\nFROM users\nGROUP BY uid;\n-- This doesn't work\nSELECT\nuid, avg(age)\nFROM users\nGROUP BY uid,      -- trailing comma\n```",
  "date": "2025-11-13T12:18:11Z",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "TiDB",
  "source_database": "ClickHouse",
  "bug_features_seed": [
    "CREATE TABLE products (id INT, name VARCHAR(255), price DECIMAL(10, 2), created_at DATETIME);",
    "INSERT INTO products VALUES (1, 'Laptop', 999.99, NOW());",
    "INSERT INTO products VALUES (2, 'Smartphone', 499.99, NOW());",
    "SELECT id, AVG(price) FROM products GROUP BY id;",
    "SELECT id, COUNT(*) FROM products GROUP BY id, created_at;"
  ],
  "key_bug_features": [
    "Trailing commas in GROUP BY clause"
  ]
}