{
  "url": "https://github.com/ClickHouse/ClickHouse/issues/87841",
  "title": "`INSERT SELECT` with implicit `GROUP BY` by destination partition key",
  "description": "### Company or project name\n_No response_\n### Use case\nThere's \"Too many partitions for single INSERT block\" problem for inserts with keys redistribution into partitions.\nIn sample there's a table for 10 years of ordered by some key data that we want to redistribute into partitions by months. IRL there're lots of splits into small parts in partitions on insert that lead to troubles with merges.\n```\nCREATE TABLE table_src\n(\norder Int64,\nrandom_date Date\n)\nENGINE = MergeTree()\nORDER BY order;\nCREATE TABLE table_dst\n(\norder Int64,\nrandom_date Date\n)\nENGINE = MergeTree()\nPARTITION BY toStartOfMonth(random_date)\nORDER BY tuple();\n-- 10 years of random dates in source table\nINSERT INTO table_src\nSELECT number AS order, toDate(rand() % 3650) AS random_date\nFROM numbers(1000000);\n-- leads to 120 months as partitions in dst table - Too many partitions for single INSERT block\nINSERT INTO table_dst\nSELECT * FROM table_src; -- { serverError TOO_MANY_PARTS }\n-- The same INSERT but groupped by dst partition key & splitted into smaller blocks works well\nINSERT INTO table_dst\nSELECT * FROM table_src\nORDER BY toStartOfMonth(random_date)\nSETTINGS min_insert_block_size_rows = 500000;\n```\n### Describe the solution you'd like\n`INSERT SELECT` should have optional implicit `GROUP BY` by destination table's `PARTITION BY` key. So it could buffer some records in buckets, make insert per bucket logic. So there would be one part/partition for insert and no \"Too many partitions for single INSERT block\" problem.\n1. Set to use N buckets for INSERT\n2. When (N+1)'th bucket needed, push the biggest bucket into destination to free one\n### Describe alternatives you've considered\nIt's possible to insert such data avoiding \"Too many partitions for single INSERT block\" with (optimized) intermediate table sorted by destination's partition key. Or just adding `ORDER BY` with `min_insert_block_size_rows`.\n### Additional context\n_No response_",
  "date": "2025-09-29T15:57:24Z",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "TiDB",
  "source_database": "ClickHouse",
  "bug_features_seed": [
    "CREATE TABLE table_src (order_id BIGINT, random_date DATE, amount FLOAT) ENGINE = InnoDB;",
    "CREATE TABLE table_dst (order_id BIGINT, random_date DATE, amount FLOAT) PARTITION BY RANGE (YEAR(random_date) * 100 + MONTH(random_date)) (PARTITION p0 VALUES LESS THAN (202301), PARTITION p1 VALUES LESS THAN (202302), PARTITION p2 VALUES LESS THAN (202303), PARTITION p3 VALUES LESS THAN (202304), PARTITION p4 VALUES LESS THAN (202305), PARTITION p5 VALUES LESS THAN (202306), PARTITION p6 VALUES LESS THAN (202307), PARTITION p7 VALUES LESS THAN (202308), PARTITION p8 VALUES LESS THAN (202309), PARTITION p9 VALUES LESS THAN (202310), PARTITION p10 VALUES LESS THAN (202311), PARTITION p11 VALUES LESS THAN (202312)) ENGINE = InnoDB;",
    "INSERT INTO table_src SELECT number AS order_id, DATE_ADD('2000-01-01', INTERVAL FLOOR(RAND() * 3650) DAY) AS random_date, RAND() * 100 AS amount FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t1, (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) t2, (SELECT @rownum := 0) t3) AS numbers LIMIT 1000000;",
    "INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src WHERE amount > 50.0;",
    "INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src ORDER BY YEAR(random_date) * 100 + MONTH(random_date) LIMIT 500000;"
  ],
  "key_bug_features": [
    "`INSERT SELECT` without implicit `GROUP BY` for partition key"
  ]
}