{
  "url": "https://github.com/openlink/virtuoso-opensource/issues/974",
  "title": "Wrong behaviour of SQL WHERE",
  "description": "While working on a study comparing different approaches to query the neXtProt dataset, I have observed that Virtuoso returns wrong results in some cases.\nIf I create the table —\ncreate table test (\n    id              varchar not null,\n    pos_start  integer,\n    pos_end    integer,\n    PRIMARY KEY(id)\n);\n\ninsert into test values ('ID1', NULL, NULL);\ninsert into test values ('ID2', 10, 20);\n\n— then the SQL query —\nselect id from test where 0 < (pos_end - pos_start) and 100 > (pos_end - pos_start);\n— returns an empty result. However, 'ID2' should be returned.",
  "date": "2021-07-27 11:48:20",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "TiDB",
  "source_database": "virtuoso",
  "bug_features_seed": [
    "CREATE TABLE test2 (id VARCHAR NOT NULL, pos_start INT, pos_end INT, status BOOLEAN, PRIMARY KEY(id));",
    "INSERT INTO test2 VALUES ('ID3', NULL, NULL, TRUE);",
    "INSERT INTO test2 VALUES ('ID4', 5, 15, FALSE);",
    "SELECT id FROM test2 WHERE (pos_end - pos_start) IS NOT NULL AND (pos_end - pos_start) > 0;",
    "SELECT id FROM test2 WHERE (pos_end - pos_start) < 10 OR status IS NULL;"
  ],
  "key_bug_features": [
    "SQL WHERE clause with NULL values in calculations"
  ]
}