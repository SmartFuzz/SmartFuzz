{
  "url": "https://www.sqlite.org/src/tktview/7d7525cb01",
  "title": "Compatibility break on INTEGER PRIMARY KEY columns.",
  "description": "drh added on 2016-04-18 15:24:18:\nSQLite as always allowed the datatype of a column to be quoted.  For\nexample:\nCREATE TABLE t1(\n  a \"INTEGER\" PRIMARY KEY,\n  b \"TEXT\"\n);\nBut beginning with version 3.12.0, quoting the \"INTEGER\" datatype causes\nSQLite to fail to recognize that the column is the INTEGER PRIMARY KEY that\nhas special properties.  This means that legacy database files that were\ncreated using INTEGER PRIMARY KEYs where the \"INTEGER\" keywords was quoted\nbecome unreadable.\nThe fact that legacy versions of SQLite allowed datatype names to be quoted\nis undocumented and untested, and was a fact unknown to the developers,\nwhich is how this problem managed to slip through testing.",
  "date": "2016-04-18 15:24:189.31 years ago",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "TiDB",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE t1(a INT PRIMARY KEY, b TEXT);",
    "INSERT INTO t1 (a, b) VALUES (1, 'test');",
    "CREATE TABLE t2(c INT, d TEXT, FOREIGN KEY(c) REFERENCES t1(a));",
    "INSERT INTO t2 (c, d) VALUES (1, 'reference');",
    "SELECT * FROM t1 JOIN t2 ON t1.a = t2.c WHERE t1.a IS NOT NULL;"
  ],
  "key_bug_features": [
    "Quoting INTEGER datatype breaks PRIMARY KEY recognition."
  ]
}