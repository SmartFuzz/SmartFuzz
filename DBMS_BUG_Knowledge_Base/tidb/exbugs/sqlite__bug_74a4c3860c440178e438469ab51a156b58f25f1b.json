{
  "url": "https://www.sqlite.org/src/tktview/74a4c3860c",
  "title": "DBSTAT query computes incorrect result for name column",
  "description": "mrigger added on 2020-01-04 16:09:08:\nConsider the following test case:\nCREATE VIRTUAL TABLE stat USING dbstat;\nSELECT * FROM stat WHERE stat.name = NULL; -- unexpected: row is fetched\nUnexpectedly, a row is fetched, even though the predicate should evaluate to FALSE:\nSELECT stat.name = NULL FROM stat; -- NULL\nThis issue seems to be related to [727074e2ae].",
  "date": "2020-01-04 16:09:085.60 years ago",
  "vulnerable_version": "3.30.0",
  "poc_type": "sql_text",
  "vulnerable_database": "TiDB",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), value INT);",
    "INSERT INTO test_table (name, value) VALUES (NULL, 10);",
    "INSERT INTO test_table (name, value) VALUES ('example', NULL);",
    "SELECT * FROM test_table WHERE name IS NULL;",
    "SELECT * FROM test_table WHERE name IS NULL OR name = 'example';"
  ],
  "key_bug_features": [
    "Incorrect handling of NULL in WHERE clause"
  ]
}