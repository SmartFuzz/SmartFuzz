{
  "url": "https://www.sqlite.org/src/tktview/ad2559db38",
  "title": "JSON does not properly escape control characters",
  "description": "drh added on 2016-02-04 10:14:04:\nThe JSON specification requires control characters in strings to be escaped,\nbut the JSON1 extension does not do this.  For example:\nSELECT json_array(char(1,8,9,10,12,13,31));\nThe output should be [\"\\u0001\\b\\t\\n\\f\\r\\u001f\"] but\nthe JSON1 extension leaves the characters unescaped.\nThis error arose because when I was coding up the JSON1 extension\nI missed the part in the JSON specification that says that control\ncharacters must be escaped.  I understood the escapes to be optional\nfor all characters other than \" and \\.",
  "date": "2016-02-04 10:14:049.52 years ago",
  "vulnerable_version": "3.10.2",
  "poc_type": "sql_text",
  "vulnerable_database": "TiDB",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_json (id INT PRIMARY KEY, data TEXT NOT NULL);",
    "INSERT INTO test_json (data) VALUES (JSON_ARRAY(CHAR(1,8,9,10,12,13,31)));",
    "SELECT data FROM test_json WHERE id = 1;",
    "UPDATE test_json SET data = JSON_ARRAY(CHAR(0, 7, 11, 14, 15, 16)) WHERE id = 1;",
    "SELECT JSON_ARRAY(CHAR(2, 3, 4, 5, 6, 7)) AS json_output FROM test_json;"
  ],
  "key_bug_features": [
    "JSON1 extension fails to escape control characters"
  ]
}