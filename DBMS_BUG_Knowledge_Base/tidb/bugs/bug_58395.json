{
  "url": "https://github.com/pingcap/tidb/issues/58395",
  "title": "`execute` statement get \"invalid memory address or nil pointer dereference\" error",
  "vulnerable_database": "tidb",
  "date": "2024-12-18T13:30:21Z",
  "vulnerable_version": "Unknown",
  "description": "## Bug Report\n\nPlease answer these questions before submitting your issue. Thanks!\n\n### 1. Minimal reproduce step (Required)\n\n```sql\nset GLOBAL tidb_enable_instance_plan_cache=1;\n\ndrop table t3;\nCREATE TABLE t3 (c datetime, PRIMARY KEY (c));\n\nuse test\nprepare p4 from \"select * from t3 where c in (?, ? , '2033-11-23')\";\nset @i0 = '2027-12-17', @i1 = '1986-12-03';\nexecute p4 using @i0, @i1;\n```\n\nopen another session and run\n\n```sql\nuse test\nprepare p4 from \"select * from t3 where c in (?, ?, '2033-11-23')\";\nset @i0 = 'a', @i1 = 'b';\nexecute p4 using @i0, @i1;\n```\n\n<!-- a step by step guide for reproducing the bug. -->\n\n### 2. What did you expect to see? (Required)\n\n```\nEmpty set (0.00 sec)\n```\n\n### 3. What did you see instead (Required)\n\n```sql\nError 1105 (HY000): runtime error: invalid memory address or nil pointer dereference\n```\n\nIf you run the second `execute` in the same session, there will be no error. As you can in the warnings, plan cache is skipped for this SQL:\n\n```\nWarning | 1105 | skip prepared plan-cache: Batch/PointGet plans may be over-optimized\n```\n\n\n### 4. What is your TiDB version? (Required)\n\nmaster\n\n<!-- Paste the output of SELECT tidb_version() -->\n\n",
  "poc": [
    "set GLOBAL tidb_enable_instance_plan_cache=1;",
    "drop table t3;",
    "CREATE TABLE t3 (c datetime, PRIMARY KEY (c));",
    "use test",
    "prepare p4 from \"select * from t3 where c in (?, ? , '2033-11-23')\";",
    "set @i0 = '2027-12-17', @i1 = '1986-12-03';",
    "execute p4 using @i0, @i1;"
  ],
  "poc_type": "sql_text",
  "state": "closed",
  "key_bug_features": [
    "Executing prepared statement with nil pointer dereference"
  ],
  "bug_feature_seed": [
    "SET GLOBAL tidb_enable_instance_plan_cache = 1;",
    "DROP TABLE IF EXISTS t3;",
    "CREATE TABLE t3 (c DATETIME, PRIMARY KEY (c));",
    "INSERT INTO t3 VALUES ('1986-12-03'), ('2027-12-17'), ('2033-11-23');",
    "PREPARE p1 FROM \"SELECT * FROM t3 WHERE c IN (?, ?, '2033-11-23')\";",
    "SET @a = '2027-12-17', @b = '1986-12-03';",
    "EXECUTE p1 USING @a, @b;",
    "SET @a = 'a', @b = 'b';",
    "EXECUTE p1 USING @a, @b;",
    "DEALLOCATE PREPARE p1;",
    "PREPARE p2 FROM \"SELECT * FROM t3 WHERE c IN (?, ?)\";",
    "SET @x = '0000-00-00', @y = NULL;",
    "EXECUTE p2 USING @x, @y;",
    "SET @x = '2033-11-23', @y = '9999-12-31';",
    "EXECUTE p2 USING @x, @y;"
  ],
  "bug_features_seed": [
    "CREATE TABLE t4 (id INT PRIMARY KEY, name VARCHAR(50), created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO t4 (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, NULL);",
    "CREATE TABLE t5 (ref_id INT, value DECIMAL(10, 2), FOREIGN KEY (ref_id) REFERENCES t4(id));",
    "INSERT INTO t5 (ref_id, value) VALUES (1, 10.50), (2, 20.75), (3, NULL);",
    "SELECT * FROM t5 WHERE ref_id IN (?, ?);"
  ]
}