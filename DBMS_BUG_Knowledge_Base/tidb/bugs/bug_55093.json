{
  "url": "https://github.com/pingcap/tidb/issues/55093",
  "title": "Create interval partition not report error when partition numbers exceed limit number",
  "vulnerable_database": "tidb",
  "date": "2024-07-31T08:17:21Z",
  "vulnerable_version": "Unknown",
  "description": "## Bug Report\r\n\r\nPlease answer these questions before submitting your issue. Thanks!\r\n\r\n### 1. Minimal reproduce step (Required)\r\n\r\n```\r\nCREATE TABLE employees (\r\n    id int unsigned NOT NULL\r\n) PARTITION BY RANGE (id) INTERVAL (1) FIRST PARTITION LESS THAN (1) LAST PARTITION LESS THAN (81920);\r\n```\r\n\r\n### 2. What did you expect to see? (Required)\r\n\r\n```\r\nreport error\r\n```\r\n\r\n### 3. What did you see instead (Required)\r\n\r\n```\r\nno error, created a table with 8192 partitions.\r\n```\r\n\r\n### 4. What is your TiDB version? (Required)\r\n\r\n<!-- Paste the output of SELECT tidb_version() -->\r\n\r\n",
  "poc": [
    "CREATE TABLE employees (",
    "    id int unsigned NOT NULL",
    ") PARTITION BY RANGE (id) INTERVAL (1) FIRST PARTITION LESS THAN (1) LAST PARTITION LESS THAN (81920);"
  ],
  "poc_type": "sql_text",
  "state": "closed",
  "key_bug_features": [
    "Creating partitions exceeding the limit without error"
  ],
  "bug_features_seed": [
    "CREATE TABLE employees (id int unsigned NOT NULL, name varchar(255) NOT NULL) PARTITION BY RANGE (id) INTERVAL (1) FIRST PARTITION LESS THAN (1) LAST PARTITION LESS THAN (81920);",
    "INSERT INTO employees (id, name) VALUES (1, 'Alice');",
    "INSERT INTO employees (id, name) VALUES (8192, 'Bob');",
    "SELECT COUNT(*) FROM employees WHERE id IS NULL;",
    "CREATE TABLE departments (dept_id int unsigned NOT NULL, dept_name varchar(255) NOT NULL) PARTITION BY HASH (dept_id) PARTITIONS 8;"
  ]
}