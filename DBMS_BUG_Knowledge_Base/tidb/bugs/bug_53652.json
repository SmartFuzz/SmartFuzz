{
  "url": "https://github.com/pingcap/tidb/issues/53652",
  "title": "a new table with FK will not create stats_meta by ddl event",
  "vulnerable_database": "tidb",
  "date": "2024-05-29T09:02:05Z",
  "vulnerable_version": "Unknown",
  "description": "## Bug Report\r\n\r\nPlease answer these questions before submitting your issue. Thanks!\r\n\r\n### 1. Minimal reproduce step (Required)\r\n\r\n```\r\ncreate table t_parent (id int primary key);\r\ncreate table t_child (id int primary key, pid int, foreign key (pid) references t_parent(id) on delete cascade on update cascade);\r\nshow stats_meta;\r\n```\r\n<!-- a step by step guide for reproducing the bug. -->\r\n\r\n### 2. What did you expect to see? (Required)\r\n\r\n```t_parent``` and ```t_child```\r\n\r\n### 3. What did you see instead (Required)\r\n\r\nonly ```t_parent```\r\n### 4. What is your TiDB version? (Required)\r\n\r\nmaster \r\n<!-- Paste the output of SELECT tidb_version() -->\r\n\r\n",
  "poc": [
    "create table t_parent (id int primary key);",
    "create table t_child (id int primary key, pid int, foreign key (pid) references t_parent(id) on delete cascade on update cascade);",
    "show stats_meta;"
  ],
  "poc_type": "sql_text",
  "state": "closed",
  "key_bug_features": [
    "new table with foreign key does not create stats_meta"
  ],
  "bug_features_seed": [
    "CREATE TABLE t_parent (id INT PRIMARY KEY, name VARCHAR(50) NOT NULL);",
    "CREATE TABLE t_child (id INT PRIMARY KEY, pid INT, age INT CHECK (age >= 0), FOREIGN KEY (pid) REFERENCES t_parent(id) ON DELETE CASCADE ON UPDATE CASCADE);",
    "INSERT INTO t_parent (id, name) VALUES (1, 'Parent1');",
    "INSERT INTO t_child (id, pid, age) VALUES (1, 1, 10);",
    "SHOW STATS_META;"
  ]
}