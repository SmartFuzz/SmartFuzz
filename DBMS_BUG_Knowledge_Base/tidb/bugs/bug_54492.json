{
  "url": "https://github.com/pingcap/tidb/issues/54492",
  "title": "optimistic transaction does not respect `tidb_constraint_check_in_place` ",
  "vulnerable_database": "tidb",
  "date": "2024-07-08T07:03:59Z",
  "vulnerable_version": "Unknown",
  "description": "## Bug Report\r\n\r\nPlease answer these questions before submitting your issue. Thanks!\r\n\r\n### 1. Minimal reproduce step (Required)\r\n\r\n```\r\ncreate table t(id int primary key, u int unique);\r\ninsert into t values (1, 1), (2, 2);\r\nset @@tidb_constraint_check_in_place=0;\r\nbegin optimistic;\r\nupdate t set u=2 where id=1;\r\n```\r\n\r\n### 2. What did you expect to see? (Required)\r\n\r\nupdate should not report any error in optimistic transaction because `tidb_constraint_check_in_place` is off\r\n\r\n### 3. What did you see instead (Required)\r\n\r\n```\r\n> update t set u=2 where id=1;\r\n(1062, \"Duplicate entry '2' for key 't.u'\")\r\n```\r\n\r\n### 4. What is your TiDB version? (Required)\r\n\r\n<!-- Paste the output of SELECT tidb_version() -->\r\n\r\n",
  "poc": [
    "create table t(id int primary key, u int unique);",
    "insert into t values (1, 1), (2, 2);",
    "set @@tidb_constraint_check_in_place=0;",
    "begin optimistic;",
    "update t set u=2 where id=1;"
  ],
  "poc_type": "sql_text",
  "state": "closed",
  "key_bug_features": [
    "optimistic transaction ignores tidb_constraint_check_in_place setting"
  ],
  "bug_features_seed": [
    "CREATE TABLE t1(id INT PRIMARY KEY, u INT UNIQUE, v VARCHAR(10));",
    "INSERT INTO t1 VALUES (1, 1, 'a'), (2, 2, 'b');",
    "SET @@tidb_constraint_check_in_place=0;",
    "BEGIN OPTIMISTIC;",
    "UPDATE t1 SET u=2 WHERE id=1;"
  ]
}