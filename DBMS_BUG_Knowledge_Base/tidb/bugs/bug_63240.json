{
  "url": "https://github.com/pingcap/tidb/issues/63240",
  "title": "Inconsistent BINARY cast behavior between VIEW and subquery",
  "vulnerable_database": "tidb",
  "date": "2025-08-28T00:12:04Z",
  "vulnerable_version": "Unknown",
  "description": "## Bug Report\n\n### 1. Minimal reproduce step (Required)\n```sql\nCREATE TABLE test1(c0 VARCHAR(4));\nINSERT INTO test1 VALUES ('11');\nCREATE VIEW v AS (SELECT HEX(CAST(c0 AS BINARY)) AS c0 FROM test1);\nSELECT c0 FROM v;\nDROP VIEW v;\nSELECT c0 FROM (SELECT HEX(CAST(c0 AS BINARY)) AS c0 FROM test1) AS v;\n```\n### 2. What did you expect to see? (Required)\nBoth queries should produce the same results\nIn MySQL, both approaches return the same result `3131`\n### 3. What did you see instead (Required)\n```\n+----------+\n| c0       |\n+----------+\n| 31310000 |\n+----------+\n```\n\n```\n+------+\n| c0   |\n+------+\n| 3131 |\n+------+\n```\n### 4. What is your TiDB version? (Required)\n```\ntiup playground nightly --host 0.0.0.0 --kv 3 --tiflash 3\n```\n```\n8.0.11-TiDB-v9.0.0-beta.2.pre-384-g5fab436\n```\n\n",
  "poc": [
    "CREATE TABLE test1(c0 VARCHAR(4));",
    "INSERT INTO test1 VALUES ('11');",
    "CREATE VIEW v AS (SELECT HEX(CAST(c0 AS BINARY)) AS c0 FROM test1);",
    "SELECT c0 FROM v;",
    "DROP VIEW v;",
    "SELECT c0 FROM (SELECT HEX(CAST(c0 AS BINARY)) AS c0 FROM test1) AS v;"
  ],
  "poc_type": "sql_text",
  "state": "open",
  "key_bug_features": [
    "Inconsistent BINARY cast behavior between VIEW and subquery"
  ],
  "bug_features_seed": [
    "CREATE TABLE test2(c1 VARCHAR(10) NOT NULL, c2 INT DEFAULT 0);",
    "INSERT INTO test2 VALUES ('test', NULL), ('data', 5);",
    "CREATE VIEW v2 AS (SELECT HEX(CAST(c1 AS BINARY)) AS c1 FROM test2 WHERE c2 IS NOT NULL);",
    "SELECT c1 FROM v2;",
    "SELECT c1 FROM (SELECT HEX(CAST(c1 AS BINARY)) AS c1 FROM test2 WHERE c2 IS NULL) AS v3;"
  ]
}