CREATE TABLE test_table (id INT PRIMARY KEY, value INT NOT NULL);
INSERT INTO test_table (id, value) VALUES (1, 10), (2, 20), (3, 10), (4, 30);
SELECT DISTINCT value FROM test_table ORDER BY value;
SELECT DISTINCT value % 3 FROM test_table ORDER BY id;
SELECT DISTINCT CASE WHEN value > 15 THEN 'high' ELSE 'low' END FROM test_table ORDER BY value;
