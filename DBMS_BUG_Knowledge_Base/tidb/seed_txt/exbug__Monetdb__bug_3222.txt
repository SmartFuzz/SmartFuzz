CREATE TABLE testtable (dim_id BIGINT AUTO_INCREMENT PRIMARY KEY, dim_type VARCHAR(100) NOT NULL, dim_value VARCHAR(100) NOT NULL, UNIQUE KEY unique_dimension (dim_type, dim_value));
INSERT INTO testtable (dim_type, dim_value) VALUES ('user', 'user1');
SELECT dim_id, dim_type, dim_value FROM testtable WHERE dim_type='user' AND dim_value='user1';
CREATE TABLE test (roe_id BIGINT AUTO_INCREMENT PRIMARY KEY, roe_type BIGINT NOT NULL, node_event_id BIGINT NOT NULL, dc_servername VARCHAR(50), roe_start_date_withtz VARCHAR(50), usr_id BIGINT, customgroup_id BIGINT, queuename_id BIGINT, exitstatus_id BIGINT);
INSERT INTO test (roe_type, node_event_id, dc_servername) VALUES (1, 100, 'server1');
