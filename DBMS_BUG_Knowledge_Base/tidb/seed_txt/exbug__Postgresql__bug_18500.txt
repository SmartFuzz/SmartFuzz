CREATE TABLE t (a INT, b TEXT) PARTITION BY RANGE (a);
CREATE TABLE tp (a INT PRIMARY KEY, c BOOLEAN NOT NULL DEFAULT TRUE);
ALTER TABLE t ADD PARTITION tp FOR VALUES FROM (0) TO (1000);
CREATE UNIQUE INDEX t_a_idx ON t (a, b);
ALTER TABLE t ADD PARTITION tp_pkey FOR VALUES FROM (0) TO (1000);
