CREATE TABLE releases (version FLOAT COMMENT 'Version of the registry.' PRIMARY KEY) CHARACTER SET utf8;
CREATE TABLE releases_v2 (version FLOAT COMMENT 'Version of the registry.') PRIMARY KEY(version);
CREATE TABLE releases_v3 (id INT UNSIGNED, version FLOAT, description VARCHAR(255) COMMENT 'Description of the release.') PRIMARY KEY(id);
INSERT INTO releases_v3 (id, version, description) VALUES (1, 1.0, 'Initial release');
SELECT * FROM releases_v3 WHERE version > 0.5;
