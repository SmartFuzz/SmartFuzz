CREATE TABLE TestTable (ID INT PRIMARY KEY, Value DOUBLE, Category VARCHAR(50));
INSERT INTO TestTable (ID, Value, Category) VALUES (1, RAND(), 'A');
INSERT INTO TestTable (ID, Value, Category) SELECT ID + 1, CASE WHEN RAND() < 0.5 THEN NULL ELSE RAND() END, 'B' FROM TestTable;
SELECT TRUNCATE(Value, 1) AS rounded_value, COUNT(*) AS count FROM TestTable GROUP BY TRUNCATE(Value, 1);
SELECT TRUNCATE(Value, 1) AS rounded_value, COUNT(*) AS count FROM TestTable GROUP BY TRUNCATE(Value, 1) UNION ALL SELECT NULL, COUNT(*) FROM TestTable;
