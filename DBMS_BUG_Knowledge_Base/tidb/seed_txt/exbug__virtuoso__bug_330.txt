CREATE TABLE test_table (id INT PRIMARY KEY, value VARCHAR(255), positive_val INT CHECK (positive_val >= 0));
INSERT INTO test_table (id, value, positive_val) VALUES (1, 'Test Value', 3);
SELECT CASE WHEN positive_val IS NOT NULL THEN 'http://www.w3.org/2001/XMLSchema#integer' END AS TYPE FROM test_table WHERE id = 1;
SELECT * FROM test_table WHERE positive_val IS NOT NULL;
UPDATE test_table SET positive_val = NULL WHERE id = 1;
