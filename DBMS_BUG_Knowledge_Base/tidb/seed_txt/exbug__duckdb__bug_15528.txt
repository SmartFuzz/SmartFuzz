CREATE TABLE one (a INT, b VARCHAR(255));
CREATE TABLE two (a INT, b FLOAT);
INSERT INTO one (a, b) VALUES (1, 'test1'), (2, 'test2');
INSERT INTO two (a, b) VALUES (1, 1.1), (2, 2.2);
SELECT one.a AS one_a, two.a AS two_a FROM one JOIN two ON one.a = two.a;
