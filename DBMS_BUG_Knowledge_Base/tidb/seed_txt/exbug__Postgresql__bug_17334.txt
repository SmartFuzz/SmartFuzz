CREATE TABLE point_tbl(f1 varchar(50) NOT NULL, f2 integer CHECK (f2 > 0));
CREATE INDEX gpointind ON point_tbl(f1);
INSERT INTO point_tbl SELECT '0,0', gs.num FROM (SELECT seq AS num FROM information_schema.columns LIMIT 185) AS gs;
INSERT INTO point_tbl VALUES ('0,NaN', 1);
SELECT f1, ABS(CAST(SUBSTRING_INDEX(f1, ',', 1) AS DECIMAL) - 0) + ABS(CAST(SUBSTRING_INDEX(f1, ',', -1) AS DECIMAL) - 0) AS dist FROM point_tbl WHERE f2 IS NOT NULL ORDER BY dist;
