CREATE TABLE test_table(id INT, value BIGINT, flag BIT(64), description VARCHAR(255));
INSERT INTO test_table VALUES(1, -1, b'1', 'Negative one'), (2, 18446744073709551615, b'1', 'Max unsigned bigint');
SELECT id, value = flag AS value_equals_flag, value = CAST(flag AS UNSIGNED) AS value_equals_flag_unsigned FROM test_table;
SELECT t1.id, t2.flag FROM test_table t1 JOIN test_table t2 ON t1.value = CAST(t2.flag AS UNSIGNED);
SELECT * FROM test_table WHERE flag IS NULL OR value IS NOT NULL;
