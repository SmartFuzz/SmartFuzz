DROP FUNCTION IF EXISTS python_aggregate;
CREATE FUNCTION python_aggregate(val INT) RETURNS INT BEGIN DECLARE result INT DEFAULT 0;
IF (SELECT COUNT(*) FROM test_empty) = 0 THEN RETURN result;
END IF;
SELECT SUM(val) INTO result FROM test_empty;
RETURN result;
