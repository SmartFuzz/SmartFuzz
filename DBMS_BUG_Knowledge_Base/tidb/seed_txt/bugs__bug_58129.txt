CREATE TABLE b (id INT NOT NULL, name VARCHAR(50), created_at DATETIME DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO b VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie'), (4, NULL);
CREATE TABLE c (id INT NOT NULL, score FLOAT, FOREIGN KEY (id) REFERENCES b(id));
INSERT INTO c VALUES (1, 95.5), (2, 88.0), (3, NULL), (4, 76.5);
SELECT b.id, b.name, @row_num := IF(@prev_id = b.id, @row_num + 1, 1) AS rn, @prev_id := b.id FROM b, (SELECT @row_num := 0, @prev_id := NULL) r ORDER BY b.id;
