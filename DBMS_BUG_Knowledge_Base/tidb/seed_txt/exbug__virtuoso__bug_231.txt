CREATE TABLE proteins (id INT PRIMARY KEY, featureId VARCHAR(255), sequence TEXT, annotation VARCHAR(255));
INSERT INTO proteins (id, featureId, sequence, annotation) VALUES (1, 'PRO_0000024369', 'MKTAYIAKQRQISFVKSHFSRQD', 'http://purl.uniprot.org/annotation/PRO_0000024369');
CREATE TABLE ranges (id INT PRIMARY KEY, begin INT, end INT, protein_id INT, FOREIGN KEY (protein_id) REFERENCES proteins(id));
INSERT INTO ranges (id, begin, end, protein_id) VALUES (1, 1, 10, 1);
SELECT CONCAT('>', SUBSTRING(CAST(p.featureId AS CHAR), 33), '#', 'PRO_0000024369', '\n', SUBSTRING(p.sequence, r.begin, r.end - r.begin + 1)) AS fasta FROM proteins p JOIN ranges r ON p.id = r.protein_id WHERE p.featureId = 'PRO_0000024369' LIMIT 10;
