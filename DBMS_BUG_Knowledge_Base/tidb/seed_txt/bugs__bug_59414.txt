CREATE TABLE test_rounding (id INT PRIMARY KEY, value DECIMAL(10, 5));
INSERT INTO test_rounding (id, value) VALUES (1, 123.4);
SELECT ROUND(value, 5) FROM test_rounding WHERE id = 1;
SELECT ROUND(value, 0) FROM test_rounding;
SELECT ROUND(NULL, 5) AS rounded_value;
