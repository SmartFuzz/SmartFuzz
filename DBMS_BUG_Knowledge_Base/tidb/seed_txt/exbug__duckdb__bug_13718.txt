CREATE TABLE IF NOT EXISTS employees (id INT PRIMARY KEY, name VARCHAR(255), department_id INT);
CREATE TABLE IF NOT EXISTS departments (id INT PRIMARY KEY, department_name VARCHAR(255));
INSERT INTO departments VALUES (1, 'HR'), (2, 'Engineering'), (3, 'Sales');
INSERT INTO employees VALUES (1, 'Alice', 1), (2, 'Bob', 2), (3, 'Charlie', 3), (4, 'David', NULL);
SELECT * FROM employees LEFT JOIN departments ON employees.department_id = departments.id AND departments.department_name IS NOT NULL;
