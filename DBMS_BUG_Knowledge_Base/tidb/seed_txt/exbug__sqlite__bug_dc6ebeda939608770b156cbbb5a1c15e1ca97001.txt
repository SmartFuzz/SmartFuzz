CREATE TABLE t2(y INT NOT NULL, z VARCHAR(255) DEFAULT 'default');
INSERT INTO t2 VALUES(1, 'one'), (2, 'two'), (3, 'three');
DELETE FROM t2 WHERE EXISTS(SELECT 1 FROM t2 AS v WHERE v.y=t2.y-1);
SELECT * FROM t2;
CREATE TABLE t3(a FLOAT, b INT, c VARCHAR(255));
