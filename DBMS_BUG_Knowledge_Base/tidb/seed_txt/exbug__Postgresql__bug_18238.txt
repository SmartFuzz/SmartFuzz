CREATE TABLE t (a int, b text);
CREATE TABLE tp1 (a int, b text) PARTITION BY LIST (a);
CREATE TABLE tp2 (a int, b text) PARTITION BY LIST (a);
INSERT INTO t VALUES (1, 'one');
UPDATE t SET b = 'updated' WHERE a = 1;
