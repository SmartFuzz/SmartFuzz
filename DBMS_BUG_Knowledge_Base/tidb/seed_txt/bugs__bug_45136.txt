CREATE TABLE users (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100) NOT NULL);
ALTER TABLE users MODIFY COLUMN id INT AUTO_INCREMENT DEFAULT 1;
INSERT INTO users (name) VALUES ('Alice');
SELECT * FROM users WHERE id IS NULL;
CREATE TABLE orders (order_id INT AUTO_INCREMENT PRIMARY KEY, user_id INT, amount DECIMAL(10, 2) DEFAULT 0.00);
