CREATE TABLE IF NOT EXISTS user_data (user_id INT PRIMARY KEY, username VARCHAR NOT NULL, age INT CHECK (age >= 0));
CREATE UNIQUE INDEX idx_username ON user_data(upper(username));
INSERT INTO user_data(user_id, username, age) VALUES (1, 'Alice', 30);
INSERT INTO user_data(user_id, username, age) VALUES (2, 'Bob', NULL) ON DUPLICATE KEY UPDATE age = COALESCE(VALUES(age), user_data.age);
SELECT * FROM user_data WHERE age IS NOT NULL;
