CREATE TABLE users(id BIGINT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT CHECK(age >= 0));
CREATE TABLE orders(order_id BIGINT PRIMARY KEY, user_id BIGINT, amount DECIMAL(10, 2), FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO users(name, age) VALUES('Alice', 30);
INSERT INTO orders(user_id, amount) VALUES(1, 100.50);
RENAME TABLE orders TO purchases;
