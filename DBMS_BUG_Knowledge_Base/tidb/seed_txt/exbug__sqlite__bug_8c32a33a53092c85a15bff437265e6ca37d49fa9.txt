CREATE TABLE test_table (id INT PRIMARY KEY, value VARCHAR(255) NOT NULL, flag TINYINT DEFAULT 0);
INSERT INTO test_table (value, flag) VALUES ('test', 1);
SELECT value IS NULL FROM test_table WHERE id = 1;
SELECT value FROM test_table WHERE flag = 1 AND value IS NOT NULL;
SELECT CASE WHEN 1 IS NULL THEN 'true' ELSE 'false' END AS is_null_check;
