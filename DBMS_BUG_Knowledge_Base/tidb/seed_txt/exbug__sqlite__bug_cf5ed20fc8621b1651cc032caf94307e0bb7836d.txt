CREATE TABLE user ( id INT NOT NULL, active INT NOT NULL, account INT, PRIMARY KEY(id) );
CREATE TABLE account ( id INT NOT NULL, name VARCHAR(255), balance DECIMAL(10, 2) DEFAULT 0.00, PRIMARY KEY(id) );
INSERT INTO user (id, active, account) VALUES (1, 1, 1);
INSERT INTO account (id, name, balance) VALUES (1, 'test', 100.00);
SELECT user.id FROM user INNER JOIN account ON user.account = account.id WHERE user.active = 1;
