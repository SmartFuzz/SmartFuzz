DROP SCHEMA IF EXISTS myschema CASCADE;
CREATE SCHEMA myschema;
CREATE TABLE myschema.pktable (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE myschema.fktable (customer_id INT, FOREIGN KEY (customer_id) REFERENCES myschema.pktable(id));
INSERT INTO myschema.pktable (name) VALUES ('Test Name');
SELECT * FROM myschema.pktable;
