CREATE TABLE mytable (id INT PRIMARY KEY, j VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO mytable VALUES (1, 'a', NULL);
CREATE VIEW first_view AS SELECT id, j FROM mytable WHERE j IS NOT NULL;
CREATE VIEW second_view AS SELECT id, j FROM first_view WHERE id > 0;
USE demo;
