DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INT UNSIGNED, name VARCHAR(5), value DOUBLE, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) SELECT number, LEFT(UUID(), 5), RAND() FROM (SELECT @rownum := @rownum + 1 AS number FROM (SELECT @rownum := 0) r, information_schema.tables LIMIT 100) AS numbers;
SELECT * FROM test_table ORDER BY name, CHAR_LENGTH(name);
DROP TABLE test_table;
