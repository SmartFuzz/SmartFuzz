CREATE TABLE tab1 (a INT NOT NULL, b TEXT DEFAULT 'default');
INSERT INTO tab1 (a, b) VALUES (1, NULL), (2, 'text'), (3, 'another text');
CREATE TABLE ftab1 (b INT) AS SELECT a FROM tab1;
CREATE TABLE ftab2 AS SELECT * FROM ftab1;
SELECT a FROM tab1 WHERE a IN (SELECT b FROM ftab1);
