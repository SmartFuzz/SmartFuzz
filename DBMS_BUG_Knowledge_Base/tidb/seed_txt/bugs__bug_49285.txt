CREATE TABLE t4(id INT, sala CHAR(10), name VARCHAR(100), age INT, PRIMARY KEY(id, sala)) PARTITION BY RANGE (age) (PARTITION p0 VALUES LESS THAN (30), PARTITION p1 VALUES LESS THAN (60));
INSERT INTO t4 VALUES (1, 'a', 'Alice', 25), (2, 'b', 'Bob', 35), (3, 'a', 'Charlie', 45);
ALTER TABLE t4 SET TIFLASH REPLICA 1;
EXPLAIN SELECT /*+ read_from_storage(tiflash[t4]) */ * FROM t4 WHERE sala='a' AND age > 20;
SELECT COUNT(*) FROM t4 WHERE sala IS NULL OR sala = 'b';
