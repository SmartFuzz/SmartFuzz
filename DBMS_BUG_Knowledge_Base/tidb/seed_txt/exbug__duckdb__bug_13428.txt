CREATE TABLE IF NOT EXISTS Orders (order_id integer AUTO_INCREMENT PRIMARY KEY, product_name varchar(255) NOT NULL, quantity integer CHECK (quantity > 0));
INSERT INTO Orders (product_name, quantity) VALUES ('Product A', 10);
INSERT INTO Orders (order_id, product_name, quantity) VALUES (2, 'Product B', 5);
ALTER TABLE Orders MODIFY COLUMN order_id INT DEFAULT NULL;
ALTER TABLE Orders MODIFY COLUMN order_id INT DEFAULT (SELECT MAX(order_id) + 1 FROM Orders);
