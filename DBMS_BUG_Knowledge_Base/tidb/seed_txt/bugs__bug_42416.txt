CREATE TABLE test_dates (id INT PRIMARY KEY, event_date DATETIME NOT NULL);
INSERT INTO test_dates (id, event_date) VALUES (1, '2023-03-21 12:00:00');
SELECT event_date + INTERVAL (1+2)*3 DAY FROM test_dates WHERE id = 1;
SELECT event_date + INTERVAL (5-2) DAY FROM test_dates WHERE id = 1;
SELECT event_date + INTERVAL (SELECT COUNT(*) FROM test_dates) DAY FROM test_dates WHERE id = 1;
