CREATE TABLE IF NOT EXISTS stats.ledgers_test (interval VARCHAR(255), interval_start DATETIME, owner VARCHAR(255), mint VARCHAR(255), amount DECIMAL(38, 0), after BIGINT, decimals INT) ENGINE=InnoDB;
INSERT INTO stats.ledgers_test (interval, interval_start, owner, mint, amount, after, decimals) VALUES ('15m', NOW(), 'owner1', 'mint1', 100, 200, 18);
INSERT INTO stats.ledgers_test (interval, interval_start, owner, mint, amount, after, decimals) VALUES ('15m', NOW() - INTERVAL 1 HOUR, 'owner2', 'mint2', 150, 250, 20);
SELECT mint, MAX(after) AS latest_after FROM stats.ledgers_test WHERE interval = '15m' GROUP BY mint;
SELECT * FROM stats.ledgers_test WHERE amount > 0 AND interval_start >= NOW() - INTERVAL 1 DAY;
