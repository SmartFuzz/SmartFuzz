CREATE TABLE geospatial_data (id INT PRIMARY KEY, geom GEOMETRY NOT NULL, description VARCHAR(255));
INSERT INTO geospatial_data (id, geom, description) VALUES (1, ST_GeomFromText('POLYGON((21.21482082946 38.604241103633, 21.214753050773 38.603677052906))'), 'Sample polygon');
CREATE TABLE measurements (id INT AUTO_INCREMENT PRIMARY KEY, geom_id INT, radius FLOAT CHECK (radius > 0), FOREIGN KEY (geom_id) REFERENCES geospatial_data(id));
INSERT INTO measurements (geom_id, radius) VALUES (1, 1.0);
SELECT g.id, g.geom, m.radius, ST_Buffer(g.geom, m.radius) AS buffered_geom FROM geospatial_data g JOIN measurements m ON g.id = m.geom_id;
