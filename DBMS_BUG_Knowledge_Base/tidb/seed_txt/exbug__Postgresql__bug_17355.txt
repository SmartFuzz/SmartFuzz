CREATE TABLE async_pt (a int, b int, c text DEFAULT 'default' NOT NULL) PARTITION BY RANGE (a);
CREATE TABLE base_tbl2 (a int, b int, c varchar(255) UNIQUE);
CREATE TABLE async_p2 PARTITION OF async_pt FOR VALUES BETWEEN (2000) AND (3000);
INSERT INTO async_pt (a, b) SELECT i, CAST(i AS CHAR) FROM (SELECT 3000 + seq AS i FROM (SELECT ROW_NUMBER() OVER () AS seq FROM information_schema.columns LIMIT 101) AS seqs) AS series;
DELETE FROM async_pt WHERE a IS NOT NULL;
