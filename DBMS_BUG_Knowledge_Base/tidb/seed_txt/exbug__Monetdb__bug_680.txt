CREATE TABLE test_intervals (id INT PRIMARY KEY, duration INT);
INSERT INTO test_intervals (id, duration) VALUES (1, 1);
SELECT duration * 2 AS multiplied_duration FROM test_intervals WHERE id = 1;
SELECT (1 + 1) AS total_duration;
SELECT (SELECT duration FROM test_intervals WHERE id = 1) * 2 AS multiplied_duration;
