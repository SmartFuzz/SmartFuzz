CREATE TABLE test.user_table (id BIGINT, name VARCHAR(255));
CREATE TABLE datamart.some_table (id BIGINT, description VARCHAR(255));
CREATE VIEW test.user_mv AS SELECT id, description FROM datamart.some_table WHERE id IS NOT NULL;
INSERT INTO datamart.some_table (id, description) VALUES (1, 'First entry'), (NULL, 'Second entry');
INSERT INTO test.user_table (id, name) VALUES (1, 'User One');
