DROP TABLE IF EXISTS t;
CREATE TABLE t(a INT, b VARCHAR(10), c DECIMAL(10,2), INDEX idx1(a, c), INDEX idx2(b, c));
INSERT INTO t(a, b, c) VALUES (1, 'one', 1.00), (2, 'two', 2.50), (NULL, 'three', NULL);
SELECT /*+ USE_INDEX_MERGE(t, idx1, idx2) */ * FROM t WHERE a IS NULL OR b = 'one' ORDER BY c LIMIT 10;
UPDATE t SET c = c + 1 WHERE b LIKE 't%';
