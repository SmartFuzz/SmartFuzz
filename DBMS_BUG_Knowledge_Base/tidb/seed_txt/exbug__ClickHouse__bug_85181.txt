CREATE DATABASE demo;
CREATE TABLE demo.test_local (code INT, name VARCHAR(255) DEFAULT 'unknown') ENGINE = ReplicatedMergeTree() ORDER BY code;
INSERT INTO demo.test_local (code, name) VALUES (1, 'value1'), (2, 'value2'), (3, NULL);
SELECT code, CASE WHEN code IS NOT NULL THEN 'value' ELSE NULL END AS dict_value FROM demo.test_local WHERE code IN (SELECT code FROM demo.test_local WHERE name IS NOT NULL);
