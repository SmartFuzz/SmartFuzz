CREATE DATABASE IF NOT EXISTS window_lag_bug;
USE window_lag_bug;
DROP TABLE IF EXISTS data_table;
CREATE TABLE IF NOT EXISTS data_table (uid INT);
INSERT INTO data_table (uid) VALUES (100), (101);
SELECT (LAST_VALUE(a.uid) OVER outer_window) = a.uid AS result FROM (SELECT LAG(uid) OVER inner_window AS uid_lag, uid AS uid FROM data_table WINDOW inner_window AS (ORDER BY uid)) AS a WINDOW outer_window AS (ORDER BY a.uid);
