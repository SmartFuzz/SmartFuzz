CREATE TABLE parent (key INT PRIMARY KEY, name VARCHAR(50));
CREATE TABLE child (parent_key INT, value FLOAT NOT NULL, FOREIGN KEY (parent_key) REFERENCES parent (key) ON DELETE CASCADE);
INSERT INTO parent (key, name) VALUES (1, 'Parent 1');
INSERT INTO child (parent_key, value) VALUES (1, 10.5);
DELETE FROM parent WHERE key = 1;
SELECT * FROM child;
