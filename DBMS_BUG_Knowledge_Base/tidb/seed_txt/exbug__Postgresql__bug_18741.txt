CREATE TABLE pt1 (a INT PRIMARY KEY, b TEXT NOT NULL);
CREATE TABLE p1 (a INT, b TEXT NOT NULL, PRIMARY KEY (a)) PARTITION BY RANGE (a);
CREATE TABLE pt2 (a INT, c DECIMAL CHECK (c >= 0));
CREATE TABLE p2 (a INT, FOREIGN KEY (a) REFERENCES pt1 (a) ON DELETE CASCADE);
ALTER TABLE pt2 ADD PARTITION p2 FOR VALUES FROM (0) TO (1);
