DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INT NOT NULL, value DECIMAL(10, 2) NULL, category VARCHAR(50) NOT NULL);
INSERT INTO test_table (id, value, category) VALUES (1, 10.50, 'A'), (2, NULL, 'B'), (3, 20.00, 'A'), (4, 30.00, 'C');
SELECT FIRST_VALUE(value) OVER (PARTITION BY category ORDER BY id RANGE BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS first_val FROM test_table;
SELECT FIRST_VALUE(value) OVER (PARTITION BY category ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS first_val FROM test_table;
