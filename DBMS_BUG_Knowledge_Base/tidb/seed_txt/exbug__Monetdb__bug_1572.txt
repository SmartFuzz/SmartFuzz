CREATE TABLE Customers (CustId INT PRIMARY KEY, Name VARCHAR(100), Balance DECIMAL(10, 2), CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO Customers (CustId, Name, Balance) VALUES (1, 'John Doe', 1000.00);
INSERT INTO Customers (CustId, Name, Balance) VALUES (2, 'Jane Smith', NULL);
SELECT CONCAT('<Customer id="', c.CustId, '" name="', COALESCE(c.Name, 'Unknown'), '">', '<!-- Customer record for ', COALESCE(c.Name, 'Unknown'), ' -->') AS xml_output FROM Customers c;
SELECT CONCAT('<Customer id="', c.CustId, '">', '<!-- Balance is ', COALESCE(CAST(c.Balance AS CHAR), 'not available'), ' -->') AS xml_output FROM Customers c WHERE c.Balance IS NOT NULL;
