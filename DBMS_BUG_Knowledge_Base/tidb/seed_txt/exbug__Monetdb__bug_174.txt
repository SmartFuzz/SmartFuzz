CREATE TABLE users (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100), age INT CHECK (age >= 0));
INSERT INTO users (name, age) VALUES ('Alice', 30);
INSERT INTO users (name, age) VALUES ('Bob', NULL);
CREATE VIEW user_view AS SELECT id, name, IFNULL(age, 0) AS age FROM users;
SELECT COUNT(*) FROM user_view;
