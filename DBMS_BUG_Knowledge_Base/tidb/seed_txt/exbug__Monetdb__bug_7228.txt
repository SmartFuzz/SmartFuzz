CREATE TABLE c(x INT, y FLOAT, z VARCHAR(255), PRIMARY KEY (x));
INSERT INTO c VALUES (1, 1.1, 'test1');
CREATE TABLE d(m INT, n BIGINT, UNIQUE KEY (m, n));
INSERT INTO d VALUES (1, 10000000000);
UPDATE c SET y = NULL WHERE x = 1;
