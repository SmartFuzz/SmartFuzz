CREATE TABLE def (x INT, y TEXT, z BOOLEAN DEFAULT TRUE);
INSERT INTO def SELECT FLOOR(RAND() * 10000), FLOOR(RAND() * 10000), (RAND() > 0.5);
CREATE INDEX idx_def ON def(x, LOWER(y));
SET @app_x = 1;
CREATE TABLE def_rls_policy (x INT, y TEXT, z BOOLEAN DEFAULT TRUE);
