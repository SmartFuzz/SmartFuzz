CREATE TABLE delta_table (id TINYINT, val VARCHAR(255));
INSERT INTO delta_table VALUES (1, 'X'), (2, 'Y'), (3, 'Z');
CREATE TABLE distributed_table (id TINYINT, val VARCHAR(255)) ENGINE = Distributed('default', 'delta_table');
SELECT a.val, b.val FROM distributed_table AS a JOIN delta_table AS b ON a.id = b.id;
SELECT b.val, a.val FROM delta_table AS b JOIN distributed_table AS a ON a.id = b.id;
