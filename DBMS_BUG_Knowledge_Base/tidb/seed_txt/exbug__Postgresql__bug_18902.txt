CREATE TABLE test_table (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, status ENUM('foo', 'bar', 'buz'));
INSERT INTO test_table (name, status) VALUES ('test1', 'foo'), ('test2', 'bar'), ('test3', NULL);
CREATE TABLE foreign_table (id INT, description TEXT);
SELECT * FROM test_table WHERE status IS NOT NULL AND created_at > NOW() - INTERVAL 1 DAY;
UPDATE test_table SET status = 'buz' WHERE id IN (SELECT id FROM foreign_table WHERE description LIKE '%test%');
