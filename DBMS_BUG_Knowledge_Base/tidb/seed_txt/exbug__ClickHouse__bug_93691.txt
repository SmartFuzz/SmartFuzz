CREATE TABLE test_enum1 (status ENUM('active', 'inactive', 'pending', 'suspended') NOT NULL DEFAULT 'active');
CREATE TABLE test_enum2 (level ENUM('low', 'medium', 'high') NOT NULL DEFAULT 'low');
INSERT INTO test_enum1 (status) VALUES ('active'), ('inactive'), ('pending');
INSERT INTO test_enum2 (level) VALUES ('low'), ('medium'), ('high');
SELECT * FROM test_enum1 WHERE status = 'pending';
