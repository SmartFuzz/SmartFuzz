CREATE DATABASE test_db;
USE test_db;
CREATE TABLE users (id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY, username VARCHAR(50) NOT NULL, email VARCHAR(100) UNIQUE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE VIEW active_users AS SELECT id, username FROM users WHERE created_at > NOW() - INTERVAL 30 DAY;
SELECT * FROM active_users;
