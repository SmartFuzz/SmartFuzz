CREATE TABLE g(id BIGINT AUTO_INCREMENT PRIMARY KEY, name TEXT NOT NULL, age INT CHECK (age >= 0));
CREATE TABLE h(id BIGINT AUTO_INCREMENT PRIMARY KEY, description VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO g(name, age) VALUES('Alice', 30), ('Bob', NULL), (NULL, 25);
INSERT INTO h(description) VALUES('Test entry'), (NULL), ('Another entry');
SELECT * FROM g LEFT JOIN h ON g.id = h.id WHERE g.age IS NOT NULL AND h.description IS NOT NULL;
