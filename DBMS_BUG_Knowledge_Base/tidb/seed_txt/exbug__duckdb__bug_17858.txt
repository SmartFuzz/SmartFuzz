CREATE TABLE orders (order_id INT PRIMARY KEY, user_id INT, amount DECIMAL(10, 2), order_date DATETIME, status VARCHAR(20) DEFAULT 'pending');
INSERT INTO orders VALUES (1, 1, 250.00, '2024-01-05 09:00:00', 'completed'), (2, 2, 150.50, '2024-01-06 10:30:00', NULL), (3, 3, 75.75, '2024-01-07 14:45:00', 'pending');
CREATE TABLE users (user_id INT PRIMARY KEY, name VARCHAR(50) NOT NULL, email VARCHAR(100) UNIQUE);
INSERT INTO users VALUES (1, 'Alice', 'alice@example.com'), (2, 'Bob', 'bob@example.com'), (3, 'Charlie', 'charlie@example.com');
SELECT o.order_id, u.name, o.amount FROM orders o JOIN users u ON o.user_id = u.user_id WHERE o.status IS NULL;
