CREATE TABLE product_v2 (value INT NOT NULL AUTO_INCREMENT, code VARCHAR(255) NOT NULL, name VARCHAR(255) DEFAULT NULL, comment VARCHAR(255) DEFAULT NULL, platform VARCHAR(50) DEFAULT NULL, UNIQUE KEY unique_code_name (code, name), KEY idx_key (value));
CREATE TABLE temp_product (temp_id INT NOT NULL AUTO_INCREMENT, temp_code VARCHAR(255), temp_name VARCHAR(255), PRIMARY KEY (temp_id));
INSERT INTO product_v2 (code, name) VALUES ('test1', 'Test Product 1');
INSERT INTO temp_product (temp_code, temp_name) SELECT code, name FROM product_v2 WHERE code IS NOT NULL;
SELECT COUNT(*) FROM (SELECT value FROM product_v2 GROUP BY code, name, comment, platform) AS derived;
