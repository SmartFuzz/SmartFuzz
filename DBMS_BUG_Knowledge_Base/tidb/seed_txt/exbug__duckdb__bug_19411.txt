CREATE TABLE test_data (id TEXT, updated_at TIMESTAMP, test_string TEXT, test_unicode TEXT, test_time TIME, test_bytes BLOB, test_smallint SMALLINT, test_integer INT, test_bigint BIGINT, test_real FLOAT, test_double DOUBLE, test_decimal DECIMAL(10, 2), test_boolean BOOLEAN, test_date DATE, test_json TEXT, test_vector JSON, test_geography TEXT, tenant_id TEXT);
INSERT INTO test_data SELECT md5(CAST(range AS CHAR)), (FROM_UNIXTIME(range)) AS updated_at, LPAD('hello_', CAST(range AS UNSIGNED) + 5, '0'), CHAR(CAST(range AS UNSIGNED)), CAST(FROM_UNIXTIME(range) AS TIME), CAST(md5(CAST(range AS CHAR)) AS BLOB), CAST(range % 32767 AS SMALLINT), CAST(range AS INT), CAST(range AS BIGINT), CAST(range / 3.0 AS FLOAT), CAST(range / 3.0 AS DOUBLE), CAST(range AS DECIMAL(10, 2)), CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END, DATE_SUB('2022-02-24', INTERVAL CAST(range % 3650 AS UNSIGNED) DAY), JSON_OBJECT('number', range, 'string', md5(CAST(range AS CHAR))), JSON_ARRAY(CAST(range / 1.0 AS DOUBLE), CAST(range / 2.0 AS DOUBLE), CAST(range / 3.0 AS DOUBLE), CAST(range / 4.0 AS DOUBLE)), ST_GeomFromGeoJSON(JSON_OBJECT('type', 'Point', 'coordinates', JSON_ARRAY(CAST(180.0 / (range + 1.0) AS DOUBLE), CAST(90.0 / (range + 1.0) AS DOUBLE)))) FROM (SELECT range FROM seq_0_to_9999) AS range_table;
SELECT * FROM test_data WHERE test_unicode IS NOT NULL INTO OUTFILE 'valid_data.csv' FIELDS TERMINATED BY '|' ENCLOSED BY '"' LINES TERMINATED BY '\n';
SELECT * FROM test_data ORDER BY updated_at;
INSERT INTO test_data SELECT md5(CAST(range AS CHAR)), (FROM_UNIXTIME(range)) AS updated_at, LPAD('hello_', CAST(range AS UNSIGNED) + 5, '0'), CASE WHEN range = 39 THEN NULL ELSE CHAR(CAST(range AS UNSIGNED)) END, CAST(FROM_UNIXTIME(range) AS TIME), CAST(md5(CAST(range AS CHAR)) AS BLOB), CAST(range % 32767 AS SMALLINT), CAST(range AS INT), CAST(range AS BIGINT), CAST(range / 3.0 AS FLOAT), CAST(range / 3.0 AS DOUBLE), CAST(range AS DECIMAL(10, 2)), CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END, DATE_SUB('2022-02-24', INTERVAL CAST(range % 3650 AS UNSIGNED) DAY), JSON_OBJECT('number', range, 'string', md5(CAST(range AS CHAR))), JSON_ARRAY(CAST(range / 1.0 AS DOUBLE), CAST(range / 2.0 AS DOUBLE), CAST(range / 3.0 AS DOUBLE), CAST(range / 4.0 AS DOUBLE)), ST_GeomFromGeoJSON(JSON_OBJECT('type', 'Point', 'coordinates', JSON_ARRAY(CAST(180.0 / (range + 1.0) AS DOUBLE), CAST(90.0 / (range + 1.0) AS DOUBLE)))) FROM (SELECT range FROM seq_0_to_9999) AS range_table;
