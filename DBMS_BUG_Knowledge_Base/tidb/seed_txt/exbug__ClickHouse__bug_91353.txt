CREATE TABLE test_table1 (id INT, value FLOAT, created_at DATETIME);
CREATE TABLE test_table2 (id INT, description VARCHAR(255), is_active TINYINT);
CREATE MATERIALIZED VIEW test_mv1 AS SELECT id, CAST(value AS FLOAT) FROM test_table2 WHERE is_active = 1;
CREATE TRIGGER test_mv1_trigger AFTER INSERT ON test_table2 FOR EACH ROW BEGIN INSERT INTO test_table1 (id, value, created_at) VALUES (NEW.id, NEW.value, NOW());
END;
INSERT INTO test_table2 (id, description, is_active) VALUES (1, 'Test Entry', 1), (2, 'Another Entry', 0);
