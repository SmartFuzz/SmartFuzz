CREATE TABLE test2 (id1 int unsigned not null primary key, id2 int unsigned not null, id3 int unsigned, name varchar(30), name2 varchar(30) not null, created_at timestamp not null default current_timestamp);
INSERT INTO test2(id1, id2, id3, name, name2) VALUES(1, 2, NULL, 'example1', 'example1');
INSERT INTO test2(id1, id2, id3, name, name2) VALUES(3, 4, 5, 'example2', 'example2');
SELECT COUNT(1) OVER() AS count, q.* FROM (SELECT 1) d LEFT JOIN (SELECT * FROM test2) q ON d.1 = 1 ORDER BY name;
SELECT COUNT(1) OVER() AS count, q.* FROM (SELECT 1) d LEFT JOIN (SELECT * FROM test2 WHERE id3 IS NOT NULL) q ON d.1 = 1 ORDER BY name2;
