CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(50), created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name) VALUES (1, 'Test Name');
SET @snapshot_time = NOW();
DROP TABLE test_table;
SELECT * FROM test_table WHERE created_at < @snapshot_time;
