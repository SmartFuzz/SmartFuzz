{
  "url": "https://www.postgresql.org/message-id/17819-1e12c61d0829db39%40postgresql.org",
  "title": "BUG #17819: The assert failed #17334 has not been repaired",
  "vulnerable_database": "postgresql",
  "date": "2023-03-02 11:46:06",
  "vulnerable_version": "Unknown",
  "description": "The following bug has been logged on the website:\nBug reference:      17819\nLogged by:          xin wen\nEmail address:      xinwen(at)stu(dot)scu(dot)edu(dot)cn\nPostgreSQL version: 15.2\nOperating system:   Ubuntu 20.04\nDescription:\nThe assert failed reported in #17334\n(\nhttps://www.postgresql.org/message-id/17334-135f485c21739caa%40postgresql.org\n)\nhas not been repaired.\nWhen executing the following query:\nCREATE TABLE point_tbl(f1 point);\nCREATE INDEX gpointind ON point_tbl USING gist(f1);\nINSERT INTO point_tbl SELECT ('0,0') FROM generate_series(1, 185);\nINSERT INTO point_tbl VALUES ('0,NaN');\nSET enable_seqscan=off; SELECT f1 <-> point '(0,0)' AS dist FROM point_tbl\nORDER BY dist;\nThe FailedAssertion(\"box->low.y <= box->high.y\", File:\n\"/backend/access/gist/gistproc.c\") will still occur.",
  "poc": [
    "CREATE TABLE point_tbl(f1 point);",
    "CREATE INDEX gpointind ON point_tbl USING gist(f1);",
    "INSERT INTO point_tbl SELECT ('0,0') FROM generate_series(1, 185);",
    "INSERT INTO point_tbl VALUES ('0,NaN');"
  ],
  "poc_type": "sql_text",
  "key_bug_features": [
    "INSERT INTO point_tbl VALUES ('0,NaN');"
  ],
  "bug_features_seed": [
    "CREATE TABLE point_tbl(f1 point, f2 integer CHECK (f2 >= 0));",
    "CREATE INDEX gpointind ON point_tbl USING gist(f1);",
    "INSERT INTO point_tbl(f1, f2) VALUES ('(1,1)', 1), ('(2,2)', 2), ('(3,3)', 3);",
    "INSERT INTO point_tbl(f1, f2) SELECT ('0,NaN'), NULL FROM generate_series(1, 5);",
    "SELECT f1, f2 FROM point_tbl WHERE f2 IS NOT NULL ORDER BY f2 DESC;"
  ]
}