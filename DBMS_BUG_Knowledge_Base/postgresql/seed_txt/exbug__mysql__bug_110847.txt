CREATE DATABASE test_fuzzing;
DROP TABLE IF EXISTS mixed_types_table;
CREATE TABLE mixed_types_table (id SERIAL PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, is_active BOOLEAN);
INSERT INTO mixed_types_table (id, name, is_active) VALUES (1, 'Test User', TRUE), (2, 'Another User', FALSE);
SELECT name, (CASE WHEN is_active THEN 'Active' ELSE 'Inactive' END) AS status FROM mixed_types_table WHERE id = 1;
