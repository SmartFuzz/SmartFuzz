CREATE TABLE test_table (id SERIAL PRIMARY KEY, value FLOAT8, description VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (value, description) VALUES (-99999998430674944.000000, 'Initial value'), (20.5, 'Second value'), (NULL, 'Null value');
SELECT MIN(value + 1) OVER (ORDER BY value) AS min_value, CASE WHEN value IS NULL THEN 'NULL CASE' ELSE 'VALID CASE' END AS case_result FROM test_table;
SELECT id, value, (SELECT COUNT(*) FROM test_table WHERE value > 0) AS positive_count FROM test_table WHERE value IS NOT NULL;
SELECT value, COALESCE((SELECT AVG(value) FROM test_table WHERE value IS NOT NULL), 0) AS avg_value FROM test_table ORDER BY value DESC;
