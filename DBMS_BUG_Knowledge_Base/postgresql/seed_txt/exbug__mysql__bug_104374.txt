CREATE TABLE Employees (EmployeeID SERIAL PRIMARY KEY, Name VARCHAR(100) NOT NULL, HireDate DATE, Salary NUMERIC(10, 2), DepartmentID INT);
INSERT INTO Employees (EmployeeID, Name, HireDate, Salary, DepartmentID) VALUES (1, 'John Doe', '2021-01-15', 60000.00, NULL);
CREATE TABLE Departments (DepartmentID SERIAL PRIMARY KEY, DepartmentName VARCHAR(100) NOT NULL);
INSERT INTO Departments (DepartmentID, DepartmentName) VALUES (1, 'Sales'), (2, 'Engineering');
SELECT e.Name, d.DepartmentName FROM Employees e LEFT JOIN Departments d ON e.DepartmentID = d.DepartmentID WHERE e.Salary > 50000;
