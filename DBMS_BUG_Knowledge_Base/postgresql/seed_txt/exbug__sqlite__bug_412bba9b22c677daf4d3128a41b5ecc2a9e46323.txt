CREATE TABLE test_table (id SERIAL PRIMARY KEY, value NUMERIC, description TEXT);
INSERT INTO test_table (value, description) VALUES (CAST('.' AS NUMERIC), 'Test case for dot as numeric');
SELECT -value FROM test_table WHERE id = 1;
SELECT value + 0 FROM test_table WHERE description LIKE 'Test%';
SELECT -CAST('.' AS NUMERIC) AS result;
