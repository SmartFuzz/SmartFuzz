DROP TABLE IF EXISTS demo;
CREATE TABLE demo(val BIGINT, name TEXT, created_at TIMESTAMP DEFAULT NOW());
CREATE INDEX demo_idx ON demo(val);
INSERT INTO demo(val, name) SELECT val, 'Name ' || val FROM generate_series(1, 10000000) val;
ANALYZE demo;
