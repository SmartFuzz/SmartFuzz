CREATE USER user03 WITH PASSWORD 'user03';
GRANT SELECT, INSERT ON test01.c TO user03;
CREATE TABLE tbl03 (id SERIAL PRIMARY KEY, b INT NOT NULL DEFAULT 0, c VARCHAR(255) DEFAULT NULL);
INSERT INTO tbl03 (id, b) VALUES (1, NULL);
CREATE TRIGGER trg02 BEFORE INSERT ON tbl03 FOR EACH ROW EXECUTE PROCEDURE set_new_c();
CREATE OR REPLACE FUNCTION set_new_c() RETURNS TRIGGER AS $$ BEGIN NEW.c := (SELECT a FROM test01.c WHERE id = 1);
