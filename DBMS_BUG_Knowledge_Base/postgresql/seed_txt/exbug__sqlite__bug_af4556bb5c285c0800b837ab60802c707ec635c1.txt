CREATE TABLE a(b INTEGER, c TEXT NOT NULL);
INSERT INTO a(b, c) VALUES (1, 'test'), (NULL, 'example'), (3, NULL);
CREATE TABLE b(x REAL, y INTEGER);
INSERT INTO b(x, y) VALUES (2.5, 1), (3.5, 2), (NULL, 3);
SELECT c FROM a GROUP BY c HAVING (SELECT SUM(b) OVER (ORDER BY b) IS NOT NULL AND SUM(b) OVER (PARTITION BY MIN(c) ORDER BY b) IS NOT NULL);
