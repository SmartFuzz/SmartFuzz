CREATE TABLE orders (order_id SERIAL PRIMARY KEY, user_id INTEGER, amount DECIMAL(10, 2), order_date TIMESTAMP, status VARCHAR(20) DEFAULT 'pending');
INSERT INTO orders (user_id, amount, order_date, status) VALUES (1, 250.00, '2024-01-05 09:00:00', 'completed'), (2, 150.50, '2024-01-06 10:30:00', NULL), (3, 75.75, '2024-01-07 14:45:00', 'pending');
CREATE TABLE users (user_id SERIAL PRIMARY KEY, name VARCHAR(50) NOT NULL, email VARCHAR(100) UNIQUE);
INSERT INTO users (name, email) VALUES ('Alice', 'alice@example.com'), ('Bob', 'bob@example.com'), ('Charlie', 'charlie@example.com');
SELECT o.order_id, u.name, o.amount FROM orders o JOIN users u ON o.user_id = u.user_id WHERE o.status IS NULL;
