CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(255), value DECIMAL(10, 2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, value) VALUES ('alpha', 10.50), ('beta', 20.75), ('gamma', 15.00);
SELECT DISTINCT name FROM test_table ORDER BY name ASC LIMIT 2;
SELECT DISTINCT name FROM test_table ORDER BY name DESC LIMIT 2;
SELECT DISTINCT name FROM test_table WHERE value > 15 ORDER BY name ASC LIMIT 1;
