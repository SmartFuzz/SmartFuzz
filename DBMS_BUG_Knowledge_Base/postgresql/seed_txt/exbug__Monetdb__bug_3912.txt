CREATE TABLE foo_bar (baz INT NOT NULL, id SERIAL PRIMARY KEY);
CREATE TABLE foo (bar_baz INT, id SERIAL PRIMARY KEY);
INSERT INTO foo (bar_baz) VALUES (1), (NULL), (2147483647);
INSERT INTO foo_bar (baz) VALUES (2), (NULL), (0);
SELECT f.id, f.bar_baz, fb.baz FROM foo f LEFT JOIN foo_bar fb ON f.id = fb.id;
