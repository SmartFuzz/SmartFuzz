CREATE TABLE test_dataset (id SERIAL PRIMARY KEY, modified TIMESTAMP WITH TIME ZONE, description VARCHAR(255));
INSERT INTO test_dataset (modified, description) VALUES (TIMESTAMP WITH TIME ZONE '2013-10-30 14:37:00+01:00', 'Test dataset for timezone handling');
SELECT modified FROM test_dataset WHERE id = 1;
CREATE EXTENSION IF NOT EXISTS dblink;
SELECT modified FROM dblink('dbname=your_db_name', 'SELECT modified FROM test_dataset WHERE id = 1') AS t(modified TIMESTAMP WITH TIME ZONE);
