CREATE TABLE users(id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK(age >= 0));
INSERT INTO users(name, age) VALUES('Alice', 30);
CREATE TABLE temp_users AS SELECT * FROM users WHERE age > 25;
CREATE TABLE orders(order_id SERIAL PRIMARY KEY, user_id INTEGER, amount REAL NOT NULL, FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO orders(user_id, amount) VALUES((SELECT id FROM users WHERE name = 'Alice'), 99.99);
