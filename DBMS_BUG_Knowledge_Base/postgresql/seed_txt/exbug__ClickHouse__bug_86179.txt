CREATE TABLE t1 (id INT, name VARCHAR, age SMALLINT, salary DOUBLE PRECISION, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t1 (id, name, age, salary) VALUES (1, 'Alice', 30, 70000.50);
INSERT INTO t1 (id, name, age, salary) SELECT number, concat('User', number::text), number % 100, number * 1000.0 FROM generate_series(0, 9) AS number;
CREATE TABLE t2 (product_id INT, product_name VARCHAR, price REAL, stock INT);
INSERT INTO t2 (product_id, product_name, price, stock) SELECT id, name, price, NULL FROM (SELECT number AS id, concat('Product', number::text) AS name, number * 10.5 AS price FROM generate_series(0, 4) AS number) AS subquery;
