CREATE TABLE a (name VARCHAR(100), age INT CHECK (age >= 0));
CREATE TABLE b (id INT PRIMARY KEY, description VARCHAR(255));
INSERT INTO a (name, age) VALUES ('Alice', 30), ('Bob', NULL), (NULL, 25);
INSERT INTO b (id, description) VALUES (1, 'Test'), (2, 'Sample');
SELECT a.name, json_build_object('user', a.name, 'age', COALESCE(a.age::TEXT, 'NULL')) as json FROM a JOIN b ON a.age = b.id;
