CREATE TABLE test_table (id SERIAL PRIMARY KEY, value NUMERIC(10,2), description VARCHAR(255));
INSERT INTO test_table (id, value, description) VALUES (1, 12.34, 'First entry');
INSERT INTO test_table (id, value, description) VALUES (2, 56.78, 'Second entry');
SELECT ROUND(value, 0) FROM test_table WHERE id = 1;
SELECT ROUND(12::NUMERIC, 0) AS truncated_value, ROUND(NULL::NUMERIC, 0) AS null_truncation;
