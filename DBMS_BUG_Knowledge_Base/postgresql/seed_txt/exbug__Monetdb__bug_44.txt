CREATE TABLE c (id SERIAL PRIMARY KEY, name VARCHAR(20), age INT CHECK (age >= 0));
INSERT INTO c (name, age) VALUES ('Alice', 30);
CREATE TABLE d (id INT, description VARCHAR(50), FOREIGN KEY (id) REFERENCES c(id));
INSERT INTO d (id, description) VALUES (1, 'Description for Alice');
SELECT * FROM c LEFT JOIN d ON c.id = d.id WHERE c.age IS NULL;
