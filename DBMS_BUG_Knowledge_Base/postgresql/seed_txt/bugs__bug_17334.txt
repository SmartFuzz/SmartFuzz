CREATE TABLE point_tbl(f1 point NOT NULL, f2 integer CHECK (f2 > 0));
CREATE INDEX gpointind ON point_tbl USING gist(f1);
INSERT INTO point_tbl SELECT ('0,0'), generate_series(1, 185);
INSERT INTO point_tbl VALUES ('0,NaN', 1);
SELECT f1 <-> point '(0,0)' AS dist FROM point_tbl WHERE f2 IS NOT NULL ORDER BY dist;
