DROP TABLE IF EXISTS t2;
CREATE TABLE t2(id SERIAL PRIMARY KEY, c1 VARCHAR(255), c2 FLOAT, c3 DATE);
INSERT INTO t2(c1, c2, c3) VALUES ('test1', 1.5, '2023-01-01'), ('test2', NULL, '2023-02-01'), ('test3', 2.5, NULL);
SELECT LAST_VALUE(c1) OVER (ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_value FROM t2 CROSS JOIN t0;
SELECT LAST_VALUE(c2) OVER (ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_value FROM t2 CROSS JOIN t1;
