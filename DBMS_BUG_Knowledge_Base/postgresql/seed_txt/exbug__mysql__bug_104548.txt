CREATE TABLE test2 (id1 SERIAL PRIMARY KEY, id2 INT NOT NULL, id3 INT, name VARCHAR(30), name2 VARCHAR(30) NOT NULL, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test2(id1, id2, id3, name, name2) VALUES(1, 2, NULL, 'example1', 'example1');
INSERT INTO test2(id1, id2, id3, name, name2) VALUES(3, 4, 5, 'example2', 'example2');
SELECT count(1) OVER(), q.* FROM (SELECT 1) d LEFT JOIN (SELECT * FROM test2) q ON d.1 = 1 ORDER BY name;
SELECT count(1) OVER(), q.* FROM (SELECT 1) d LEFT JOIN (SELECT * FROM test2 WHERE id3 IS NOT NULL) q ON d.1 = 1 ORDER BY name2;
