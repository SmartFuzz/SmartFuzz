CREATE TABLE test_table (id SERIAL PRIMARY KEY, text_column TEXT NOT NULL, num_column INT DEFAULT 0);
INSERT INTO test_table (text_column, num_column) VALUES ('new*\n*line', 10);
INSERT INTO test_table (text_column, num_column) VALUES ('another*\n*example', NULL);
SELECT id, text_column ~ 'new\*.*line' AS match_result FROM test_table WHERE num_column IS NOT NULL;
SELECT id FROM test_table WHERE text_column ~ '.*' AND num_column < 20;
