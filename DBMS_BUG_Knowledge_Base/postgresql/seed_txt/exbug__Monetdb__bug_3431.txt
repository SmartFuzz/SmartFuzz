CREATE TABLE test_numeric (id SERIAL PRIMARY KEY, value NUMERIC(8,3) CHECK (value >= 0));
INSERT INTO test_numeric (value) VALUES (1234.567), (9876.543), (1111.111);
CREATE TABLE test_aggregate (agg_value NUMERIC(10,2));
INSERT INTO test_aggregate (agg_value) SELECT TRUNC(value::NUMERIC, 2) FROM test_numeric;
SELECT TRUNC(agg_value::NUMERIC, 1) AS truncated_agg FROM test_aggregate WHERE agg_value IS NOT NULL;
