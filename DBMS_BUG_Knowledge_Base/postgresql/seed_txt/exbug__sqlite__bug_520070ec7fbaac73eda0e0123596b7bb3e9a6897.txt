CREATE TABLE t2(x SERIAL PRIMARY KEY, y TEXT NOT NULL, z REAL DEFAULT 0.0, w BYTEA);
INSERT INTO t2 VALUES(1, 'test', NULL, decode('001122334455', 'hex'));
CREATE INDEX idx_t2_y ON t2(y);
SELECT * FROM t2 WHERE y IS NULL OR z > 0.0;
DELETE FROM t2 WHERE x IN (SELECT x FROM t2 WHERE y LIKE 't%');
