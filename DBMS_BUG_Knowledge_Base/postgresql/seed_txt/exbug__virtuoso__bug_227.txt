CREATE TABLE test_labels (id SERIAL PRIMARY KEY, label VARCHAR(100) NOT NULL);
INSERT INTO test_labels (label) VALUES ('leden'), ('únor'), ('Leden'), ('Únor');
SELECT label FROM test_labels WHERE label ~ '^\\p{Ll}';
SELECT (label ~ '^\\p{Lu}') AS matches FROM test_labels WHERE label IS NOT NULL;
SELECT COUNT(*) FROM test_labels WHERE label ~ '^\\p{Ll}' OR label IS NULL;
