CREATE TABLE test_hashing (id SERIAL PRIMARY KEY, data VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_hashing (data) VALUES (CONCAT('Test', MD5('Sample Data')), NULL, CONCAT('Boundary', ENCODE(DIGEST('Edge Case', 'sha256'), 'hex')));
SELECT id, MD5(data::text) AS md5_hash FROM test_hashing WHERE data IS NOT NULL;
SELECT COUNT(*) FROM test_hashing WHERE ENCODE(DIGEST(data, 'sha256'), 'hex') IS NOT NULL;
SELECT (SELECT COUNT(*) FROM test_hashing) AS total, (SELECT MD5(CONCAT(data, 'Extra')) FROM test_hashing) AS md5_hash FROM test_hashing LIMIT 1;
