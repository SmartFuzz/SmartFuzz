CREATE TABLE t2(y TEXT NOT NULL, z INTEGER DEFAULT 0);
INSERT INTO t2(y, z) VALUES('right', 0),('wrong', 0), (NULL, 0),('right', 0);
SELECT DISTINCT y FROM (SELECT y FROM t2 GROUP BY y HAVING COUNT(z) > 0) AS subquery WHERE y='right' ORDER BY y;
CREATE TABLE t3(a REAL, b TEXT);
INSERT INTO t3(a, b) VALUES(1.0, 'test'), (2.0, 'test'), (NULL, 'test');
