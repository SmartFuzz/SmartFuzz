CREATE TABLE three (id SERIAL PRIMARY KEY, name VARCHAR(255), count INT NOT NULL CHECK (count >= 0));
INSERT INTO three (name, count) VALUES ('one', 1);
INSERT INTO three (name, count) VALUES ('two', 2);
INSERT INTO three (name, count) VALUES ('three', 3);
SELECT t1.id, t1.name, t1.count, t2.id AS id2, t2.name AS name2 FROM three t1 LEFT JOIN three t2 ON t1.count = t2.id;
