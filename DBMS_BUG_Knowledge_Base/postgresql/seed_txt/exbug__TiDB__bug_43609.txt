CREATE TABLE thash2(a INT, b VARCHAR(10), c FLOAT, d DATE, CONSTRAINT idx_ab UNIQUE(a, b), CONSTRAINT idx_cd UNIQUE(c, d));
INSERT INTO thash2 VALUES (1, 'test', 3.14, '2023-01-01');
INSERT INTO thash2 VALUES (NULL, 'example', NULL, '2023-01-02');
SELECT * FROM thash2 WHERE a = 1 OR b = 'test' ORDER BY c LIMIT 2;
SELECT * FROM thash2 WHERE a IS NULL OR d = '2023-01-02' ORDER BY b DESC;
