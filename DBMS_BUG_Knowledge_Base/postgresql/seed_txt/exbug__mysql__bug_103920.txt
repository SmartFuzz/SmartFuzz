CREATE TABLE t_fts_extended (id SERIAL PRIMARY KEY, title VARCHAR(255) NOT NULL, content TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t_fts_extended (title, content) VALUES ('Example Title 1', 'This is some example content for testing purposes.');
INSERT INTO t_fts_extended (title, content) SELECT 'Example Title ' || id, 'Content for ID ' || id FROM generate_series(1, 5) AS id;
SELECT id FROM t_fts_extended WHERE to_tsvector('english', title || ' ' || content) @@ to_tsquery('example:*') LIMIT 10;
DELETE FROM t_fts_extended WHERE id IN (SELECT id FROM t_fts_extended ORDER BY created_at DESC LIMIT 5);
