CREATE TABLE t2 (a INT NOT NULL, b VARCHAR(255) NOT NULL, c FLOAT DEFAULT 0.0);
INSERT INTO t2 (a, b, c) VALUES (1, 'test1', 10.5), (2, 'test2', 20.5), (3, 'test3', 30.5);
CREATE TABLE t3 (a INT NOT NULL, b INT DEFAULT NULL, CONSTRAINT unique_a_b UNIQUE (a, b));
INSERT INTO t3 (a, b) VALUES (1, NULL), (2, 100), (3, 200);
EXPLAIN ANALYZE SELECT SUM(CASE WHEN t2.a > 0 THEN (SELECT COUNT(*) FROM t3 WHERE t2.a = t3.a GROUP BY t3.b HAVING SUM(b) IS NOT NULL) ELSE 0 END) FROM t2 LEFT JOIN t3 ON t2.a = t3.a WHERE t3.b IS NOT NULL GROUP BY t2.a;
