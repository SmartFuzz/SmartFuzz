DROP AGGREGATE IF EXISTS python_aggregate;
CREATE OR REPLACE FUNCTION python_aggregate(val INTEGER[]) RETURNS INTEGER AS $$ DECLARE result INTEGER;
BEGIN SELECT COALESCE(SUM(val), 0) INTO result;
RETURN result;
END;
$$ LANGUAGE plpgsql;
