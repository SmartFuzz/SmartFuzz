CREATE TABLE Powerplant (id SERIAL PRIMARY KEY, name VARCHAR(255), annual_energy_output DECIMAL(10,2), owner_company VARCHAR(255), operator VARCHAR(255));
INSERT INTO Powerplant (name, annual_energy_output, owner_company, operator) VALUES ('Plant A', 1500.00, 'Company A', 'Operator A');
INSERT INTO Powerplant (name, annual_energy_output, owner_company, operator) VALUES ('Plant B', NULL, 'Company B', NULL);
SELECT SUM(annual_energy_output) AS total_output FROM Powerplant WHERE owner_company IS NOT NULL;
SELECT name, COALESCE(annual_energy_output, 0) AS output FROM Powerplant WHERE COALESCE(annual_energy_output, 0) > 1000;
