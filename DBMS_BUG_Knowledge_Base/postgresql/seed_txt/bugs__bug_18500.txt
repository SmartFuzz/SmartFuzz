CREATE TABLE t (a integer, b text) PARTITION BY RANGE (a);
CREATE TABLE tp (a integer PRIMARY KEY, c boolean NOT NULL DEFAULT true);
ALTER TABLE t ATTACH PARTITION tp FOR VALUES FROM (0) TO (1000);
CREATE UNIQUE INDEX t_a_idx ON t (a, b);
ALTER INDEX t_a_idx ATTACH PARTITION tp_pkey;
