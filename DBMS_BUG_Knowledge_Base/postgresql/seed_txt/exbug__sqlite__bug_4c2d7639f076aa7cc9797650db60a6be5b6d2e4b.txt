CREATE TABLE test_table (id SERIAL PRIMARY KEY, value TEXT NOT NULL);
INSERT INTO test_table (value) VALUES ('-'), ('-0'), ('+'), ('/');
SELECT id, CAST(value AS NUMERIC) AS numeric_value FROM test_table;
SELECT id FROM test_table WHERE NULLIF(CAST(value AS NUMERIC), 0) IS NULL;
SELECT id, value FROM test_table WHERE COALESCE(CAST(value AS NUMERIC), 0) = 0;
