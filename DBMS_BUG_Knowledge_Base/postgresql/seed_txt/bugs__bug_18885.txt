CREATE TABLE g(id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INT CHECK (age >= 0));
CREATE TABLE h(id SERIAL PRIMARY KEY, description VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO g(name, age) VALUES('Alice', 30), ('Bob', -5), (NULL, 25);
INSERT INTO h(description) VALUES('Test entry'), (NULL), ('Another entry');
SELECT * FROM g LEFT JOIN h ON g.id = h.id WHERE g.age IS NOT NULL AND h.description IS NOT NULL;
