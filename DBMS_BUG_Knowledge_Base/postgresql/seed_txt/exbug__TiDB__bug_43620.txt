CREATE TABLE users(id SERIAL PRIMARY KEY, name VARCHAR(50), age INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users VALUES(1, 'Alice', 30, NULL);
CREATE TABLE orders(order_id SERIAL PRIMARY KEY, user_id INT, amount DECIMAL(10, 2), FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO orders VALUES(101, 1, 99.99);
PREPARE stmt1 AS SELECT * FROM users WHERE age > $1 AND id IN (SELECT user_id FROM orders WHERE amount > $2);
