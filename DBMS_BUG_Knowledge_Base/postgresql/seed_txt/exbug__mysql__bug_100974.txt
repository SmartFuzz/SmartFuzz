CREATE TABLE users (user_id SERIAL PRIMARY KEY, name VARCHAR(50), dob DATE, email VARCHAR(100) NOT NULL);
INSERT INTO users (user_id, name, dob, email) VALUES (1, 'Ganesh', '1980-04-27', 'ganesh@example.com');
SELECT json_build_object('user_id', user_id, 'name', name, 'dob', dob) AS output FROM users WHERE user_id = 1;
SELECT json_build_object('email', email, 'dob', dob, 'name', name) AS output FROM users WHERE user_id = 1;
SELECT json_build_object('user_id', user_id, 'name', name, 'dob', dob) AS output FROM (SELECT * FROM users) AS subquery WHERE user_id = 1;
