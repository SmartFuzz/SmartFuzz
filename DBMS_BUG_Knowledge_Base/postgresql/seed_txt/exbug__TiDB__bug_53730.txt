CREATE TABLE users (id SERIAL PRIMARY KEY, username VARCHAR(30), email VARCHAR(50) NOT NULL);
INSERT INTO users (username, email) VALUES ('user1', 'user1@example.com'), ('user2 ', 'user2@example.com'), ('user3', 'user3@example.com');
CREATE TABLE orders (order_id SERIAL PRIMARY KEY, user_id INT, amount DECIMAL(10, 2), FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO orders (user_id, amount) VALUES (1, 99.99), (2, 150.00), (1, 200.50);
SELECT u.id, u.username FROM users u WHERE TRIM(u.username) = 'user1' AND LENGTH(u.username) = 5;
