CREATE TABLE test_table (id SERIAL PRIMARY KEY, name TEXT, value BYTEA, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, value) VALUES ('test1', repeat(E'\x00', 1024));
CREATE VIEW test_view AS SELECT name, value FROM test_table WHERE value IS NOT NULL;
SELECT DISTINCT name FROM test_view WHERE LENGTH(value) IN (SELECT LENGTH(repeat(E'\x00', 2048)));
UPDATE test_table SET value = NULL WHERE id = 1;
