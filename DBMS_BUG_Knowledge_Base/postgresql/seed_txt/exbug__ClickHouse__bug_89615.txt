CREATE TABLE set_idx_rand_v2 (id SERIAL PRIMARY KEY, s TEXT, d TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO set_idx_rand_v2 (id, s) SELECT generate_series(1, 100), (generate_series(1, 100) % 10)::TEXT;
SELECT * FROM set_idx_rand_v2 WHERE (random() % 1000) = 1;
SELECT * FROM set_idx_rand_v2 WHERE d IS NULL OR d > NOW() - INTERVAL '1 DAY';
SELECT COUNT(*) FROM set_idx_rand_v2 WHERE s IN (SELECT s FROM set_idx_rand_v2 WHERE id < 50) AND (random() % 1000) = 1;
