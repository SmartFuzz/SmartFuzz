DROP TABLE IF EXISTS employee_data;
CREATE TABLE employee_data (id SERIAL PRIMARY KEY, name VARCHAR, salary FLOAT8);
INSERT INTO employee_data (name, salary) VALUES ('Alice', 50000.0), ('Bob', NULL), ('Charlie', 60000.5);
ALTER TABLE employee_data ADD COLUMN IF NOT EXISTS bonus FLOAT8;
UPDATE employee_data SET bonus = CASE WHEN salary IS NULL THEN NULL ELSE salary * 0.1 END WHERE id = 2;
