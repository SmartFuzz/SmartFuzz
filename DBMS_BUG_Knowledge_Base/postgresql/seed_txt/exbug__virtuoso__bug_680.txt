CREATE TABLE Albums (id SERIAL PRIMARY KEY, artist VARCHAR(255), releaseDate DATE, recordLabel VARCHAR(255));
INSERT INTO Albums (artist, releaseDate, recordLabel) VALUES ('Elvis Presley', '1956-01-01', 'RCA'), ('Elvis Presley', '1957-01-01', 'RCA'), ('Elvis Presley', NULL, 'RCA');
SELECT DISTINCT recordLabel FROM Albums WHERE artist = 'Elvis Presley' AND releaseDate IS NOT NULL ORDER BY releaseDate ASC LIMIT 1;
SELECT DISTINCT recordLabel FROM Albums WHERE artist = 'Elvis Presley' ORDER BY releaseDate ASC LIMIT 1;
SELECT DISTINCT recordLabel FROM Albums WHERE artist = 'Elvis Presley' AND (releaseDate IS NULL OR releaseDate < '1957-01-01') ORDER BY releaseDate ASC LIMIT 1;
