DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INT, name VARCHAR, age SMALLINT, score REAL, created_at TIMESTAMP);
INSERT INTO test_table VALUES (1, 'Alice', 25, 88.5, NOW()), (2, 'Bob', 30, 92.0, NOW());
INSERT INTO test_table SELECT number, number::TEXT, number % 100, number / 10.0, NOW() FROM generate_series(0, 4) AS number;
SELECT id FROM test_table WHERE name = 'Bob' AND age < 30;
