CREATE TABLE test_mvi (id SERIAL PRIMARY KEY, type VARCHAR(10) NOT NULL, hid INT DEFAULT NULL, pat VARCHAR(10) DEFAULT NULL);
CREATE INDEX ix_type_hid_pat ON test_mvi (type, hid, pat);
INSERT INTO test_mvi (type, hid, pat) VALUES ('T1', 1, 'A'), ('T1', 2, 'B'), ('T2', 3, 'C'), ('T1', 4, 'D'), ('T1', NULL, 'E');
SELECT COUNT(*) FROM test_mvi WHERE hid IS NULL;
SELECT COUNT(*) FROM test_mvi WHERE type='T1' AND hid > 0;
SELECT COUNT(*) FROM test_mvi WHERE type='T1' OR pat IS NOT NULL;
