CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name) VALUES (1, 'Test Name');
SELECT * FROM test_table WHERE id = 1;
CREATE VIEW non_existent_view AS SELECT * FROM (SELECT NULL AS id, NULL AS name, NULL AS created_at) AS temp_table;
SELECT * FROM non_existent_view;
