CREATE TABLE users (id SERIAL PRIMARY KEY, name VARCHAR(255), age SMALLINT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (id, name, age, created_at) VALUES (1, 'Alice', 30, NULL);
CREATE TABLE orders (order_id SERIAL PRIMARY KEY, user_id INT, amount DOUBLE PRECISION, order_date DATE);
INSERT INTO orders (order_id, user_id, amount, order_date) VALUES (101, 1, 99.99, '2023-10-01');
SELECT u.name, o.amount FROM users u JOIN orders o ON u.id = o.user_id WHERE u.age > 25;
