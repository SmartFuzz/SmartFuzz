CREATE TABLE t1(id SERIAL PRIMARY KEY, name VARCHAR(50), active BOOLEAN);
INSERT INTO t1 (id, name, active) VALUES (1, 'Alice', TRUE), (2, 'Bob', FALSE);
DO $$ DECLARE x INTEGER := NULL;
y INTEGER := 1;
BEGIN EXECUTE 'SELECT (NULLIF($1, t1.id) OR t1.active) FROM t1 WHERE t1.id = $2' USING x, y;
END $$;
