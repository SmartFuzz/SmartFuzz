CREATE TABLE test2(id BIGINT, name TEXT, age SMALLINT);
CREATE MATERIALIZED VIEW mv2 AS SELECT id, age FROM test2;
INSERT INTO test2 VALUES (2, 'Alice', 30);
INSERT INTO test2 SELECT number AS id, to_char(number) AS name, number % 100 AS age FROM generate_series(1, 3) AS number;
INSERT INTO test2 VALUES (NULL, 'Bob', NULL);
