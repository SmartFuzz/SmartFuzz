CREATE TABLE persons (id SERIAL PRIMARY KEY, first_name VARCHAR(100));
CREATE TABLE functions (id SERIAL PRIMARY KEY, function_name VARCHAR(100));
INSERT INTO persons (id, first_name) VALUES (1, 'Alice'), (2, 'Bob');
INSERT INTO functions (id, function_name) VALUES (1, 'Developer'), (2, NULL);
SELECT p.first_name, COALESCE(f.function_name, NULL) AS function_label FROM persons p LEFT JOIN functions f ON p.id = f.id;
