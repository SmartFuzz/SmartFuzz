CREATE TABLE t_tmp (a int, b text, c timestamp DEFAULT NOW());
CREATE INDEX idx_a ON t_tmp (a);
CREATE TEMPORARY TABLE t_tmp (LIKE t_tmp INCLUDING ALL);
INSERT INTO t_tmp (a, b) VALUES (1, 'test'), (2, NULL), (3, 'example');
SELECT * FROM pg_catalog.pg_indexes WHERE tablename LIKE 't_tmp%';
