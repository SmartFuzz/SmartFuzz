CREATE TABLE sales ( year int NOT NULL, country varchar(20) NOT NULL, product varchar(32) NOT NULL, profit int DEFAULT 0, PRIMARY KEY (year, country, product) );
INSERT INTO sales (year, country, product, profit) VALUES (2020, 'USA', 'Gadget', 1500), (2020, 'USA', 'Widget', 2000), (2021, 'Canada', 'Gadget', 2500);
SELECT year, country, product, GROUPING(year) AS year_grouping FROM sales GROUP BY year, country, product WITH ROLLUP HAVING GROUPING(year) = 0;
CREATE TABLE products ( product_id serial PRIMARY KEY, product_name varchar(50) NOT NULL );
INSERT INTO products (product_name) VALUES ('Gadget'), ('Widget');
