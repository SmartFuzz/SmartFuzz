CREATE TABLE user (id BIGSERIAL PRIMARY KEY, data JSONB NOT NULL, name VARCHAR(255) DEFAULT NULL);
INSERT INTO user (data, name) VALUES (jsonb_build_object('key1', 'value1'), 'Alice');
INSERT INTO user (data, name) VALUES (NULL, 'Bob');
SELECT id, data FROM user WHERE data->>'key1' = 'value1';
UPDATE user SET data = jsonb_set(data, '{key2}', '"value2"') WHERE name IS NOT NULL;
