CREATE TABLE v1 ( id SERIAL PRIMARY KEY, value TEXT NOT NULL );
INSERT INTO v1 (value) VALUES ('A'), ('B'), ('C');
CREATE TABLE v2 ( id SERIAL PRIMARY KEY, ref_id INTEGER, amount REAL );
INSERT INTO v2 (ref_id, amount) VALUES (1, 10.5), (1, 20.0), (2, NULL);
SELECT DISTINCT value, lead(value) OVER() FROM v1 ORDER BY value;
