CREATE TABLE products (id SERIAL PRIMARY KEY, name TEXT, price REAL, quantity INTEGER NOT NULL DEFAULT 0);
INSERT INTO products (name, price, quantity) VALUES ('Widget', 19.99, 10);
SELECT COUNT(id) FROM (SELECT id, (price * quantity) AS total_price FROM products WHERE (price * quantity) > 100) AS subquery;
CREATE TABLE orders (order_id SERIAL PRIMARY KEY, product_id INTEGER, order_date DATE, FOREIGN KEY(product_id) REFERENCES products(id));
INSERT INTO orders (product_id, order_date) VALUES (1, '2023-10-01');
