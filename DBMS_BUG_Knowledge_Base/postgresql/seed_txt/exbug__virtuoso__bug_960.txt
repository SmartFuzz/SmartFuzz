CREATE TABLE datasets (id SERIAL PRIMARY KEY, title VARCHAR(255), description TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO datasets (title, description) VALUES ('Dataset One', 'This is the first dataset.');
INSERT INTO datasets (title, description) VALUES ('Dataset Two', NULL);
SELECT * FROM datasets WHERE title IS NOT NULL AND description IS NULL;
UPDATE datasets SET description = 'Updated description for Dataset One.' WHERE id = 1;
