CREATE TABLE test_json (id INTEGER, data JSONB, is_active BOOLEAN, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_json (id, data, is_active) VALUES (1, '{"key": "value"}'::JSONB, true);
SELECT jsonb_pretty(data) FROM test_json WHERE is_active = true;
SELECT jsonb_pretty(data) FROM test_json WHERE id = 1;
SELECT jsonb_pretty(data) FROM test_json WHERE created_at < CURRENT_TIMESTAMP;
