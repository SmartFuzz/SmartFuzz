CREATE TABLE test_uuid (id SERIAL PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_uuid (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
SELECT id, name, gen_random_uuid() AS uuid FROM test_uuid WHERE id IN (SELECT id FROM test_uuid WHERE name LIKE 'A%');
SELECT DISTINCT gen_random_uuid() AS str_uuid, name FROM test_uuid WHERE created_at > NOW() - INTERVAL '1 DAY';
SELECT id, gen_random_uuid() AS uuid FROM test_uuid WHERE id = (SELECT MAX(id) FROM test_uuid);
