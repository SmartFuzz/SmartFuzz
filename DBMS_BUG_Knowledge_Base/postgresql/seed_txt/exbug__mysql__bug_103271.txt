CREATE DATABASE test_fuzzing;
CREATE TABLE users (id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT CHECK (age >= 0), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', -1);
SELECT * FROM users WHERE age IS NULL OR age < 0;
CREATE TABLE t(id SERIAL PRIMARY KEY) PARTITION BY HASH(id);
