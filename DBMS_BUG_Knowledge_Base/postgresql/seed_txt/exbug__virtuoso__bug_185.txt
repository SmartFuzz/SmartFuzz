CREATE TABLE election_results (id SERIAL PRIMARY KEY, refPeriod DATE, party_name VARCHAR(255), votes INT);
INSERT INTO election_results (id, refPeriod, party_name, votes) VALUES (1, '2006-01-01', 'Party A', 1500);
INSERT INTO election_results (id, refPeriod, party_name, votes) VALUES (2, '2007-01-01', 'Party B', 2000);
SELECT id, TO_CHAR(refPeriod, 'YYYY') AS year, party_name FROM election_results WHERE TO_CHAR(refPeriod, 'YYYY') = '2006';
SELECT COUNT(*) FROM election_results WHERE votes IS NULL OR votes < 0;
