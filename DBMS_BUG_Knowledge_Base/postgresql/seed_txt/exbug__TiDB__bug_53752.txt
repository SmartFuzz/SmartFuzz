SET SESSION synchronous_commit TO OFF;
CREATE TABLE departments (id SERIAL PRIMARY KEY, name VARCHAR(50) NOT NULL, budget DECIMAL(10,2) NOT NULL);
INSERT INTO departments (id, name, budget) VALUES (1, 'HR', 100000.00), (2, 'Engineering', 250000.00), (3, 'Sales', 150000.00);
CREATE TABLE employees (id SERIAL PRIMARY KEY, fname VARCHAR(25) NOT NULL, lname VARCHAR(25) NOT NULL, store_id INT NOT NULL, department_id INT NOT NULL, FOREIGN KEY (department_id) REFERENCES departments(id));
INSERT INTO employees (fname, lname, store_id, department_id) VALUES ('Alice', 'Smith', 1, 1), ('Bob', 'Johnson', 2, 2), ('Charlie', 'Brown', 3, 3);
