CREATE TABLE table_src (order_id BIGINT, random_date DATE, amount FLOAT8);
CREATE TABLE table_dst (order_id BIGINT, random_date DATE, amount FLOAT8) PARTITION BY DATE_TRUNC('month', random_date);
INSERT INTO table_src SELECT generate_series(1, 1000000) AS order_id, (CURRENT_DATE - (random() * 3650)::int) AS random_date, (random() * 100) AS amount;
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src WHERE amount > 50.0;
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src ORDER BY DATE_TRUNC('month', random_date);
