CREATE TABLE test.c (id SERIAL PRIMARY KEY, value VARCHAR(64) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test.c (value) VALUES ('x'), ('y'), (NULL), ('z');
CREATE TABLE test.d (id INT, value VARCHAR(64), FOREIGN KEY (id) REFERENCES test.c(id));
INSERT INTO test.d (id, value) VALUES (1, 'a'), (2, 'b'), (3, 'c');
SELECT * FROM test.c AS t1 JOIN test.d AS t2 ON t1.id = t2.id WHERE t1.value IS NOT NULL;
