CREATE TABLE test_vals(grp INTEGER NOT NULL, value INTEGER CHECK(value >= 0));
INSERT INTO test_vals VALUES (1, 100), (2, 200), (1, NULL), (2, 300), (3, -50);
CREATE OR REPLACE FUNCTION jit_sum_variadic(variadic values INTEGER[]) RETURNS BIGINT AS $$ DECLARE result BIGINT := 0;
BEGIN FOREACH val IN ARRAY values LOOP result := result + COALESCE(val, 0);
END LOOP;
RETURN result;
