CREATE TABLE test_table(id SERIAL PRIMARY KEY, value BIGINT, flag BIT(64), description VARCHAR(255));
INSERT INTO test_table(id, value, flag, description) VALUES(1, -1, B'1', 'Negative one'), (2, 18446744073709551615, B'1', 'Max unsigned bigint');
SELECT id, value = CAST(flag AS INTEGER), value = CAST(flag AS BIGINT) FROM test_table;
SELECT t1.id, t2.flag FROM test_table t1 JOIN test_table t2 ON t1.value = CAST(t2.flag AS BIGINT);
SELECT * FROM test_table WHERE flag IS NULL OR value IS NOT NULL;
