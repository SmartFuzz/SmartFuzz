SET work_mem = '1024kB';
CREATE TABLE t1(id SERIAL PRIMARY KEY, name TEXT NOT NULL, value REAL DEFAULT 0.0);
INSERT INTO t1(name, value) VALUES('test1', 1.5);
SAVEPOINT sp1;
INSERT INTO t1(name, value) SELECT 'test' || x, x * 1.1 FROM (SELECT 1 AS x UNION SELECT 2 UNION SELECT 3) AS subquery;
