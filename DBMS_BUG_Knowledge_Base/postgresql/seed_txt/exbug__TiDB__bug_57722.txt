CREATE TABLE t1(id SERIAL PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t1(id, name, created_at) VALUES(1, 'test', NULL);
ALTER TABLE t1 ADD COLUMN age INT DEFAULT 0;
SET SESSION synchronous_commit TO OFF;
EXPLAIN SELECT * FROM t1 WHERE id = 1 AND age IS NOT NULL;
