CREATE TABLE test2 (id SERIAL PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test2 (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
SELECT id, width_bucket(id, 1, 3, 5) AS bucket FROM test2;
SELECT * FROM pg_stats WHERE tablename = 'test2';
SELECT COUNT(*) FROM test2 WHERE id IS NULL;
