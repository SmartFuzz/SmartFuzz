CREATE TABLE test_table (id INTEGER, data JSONB);
INSERT INTO test_table VALUES (1, '[1, 2, 3]'), (2, 'not_a_list');
SELECT CASE WHEN data IS NOT NULL THEN jsonb_array_elements(data) ELSE '0' END AS x FROM test_table WHERE id = 1;
SELECT CASE WHEN id = 2 THEN jsonb_array_elements(data) ELSE 'No List' END AS x FROM test_table;
SELECT CASE WHEN id < 3 THEN jsonb_array_elements(data) ELSE NULL END AS x FROM test_table;
