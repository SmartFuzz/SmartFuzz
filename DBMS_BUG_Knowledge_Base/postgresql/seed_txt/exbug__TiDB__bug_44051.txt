CREATE TABLE t3 ( id SERIAL PRIMARY KEY, value FLOAT DEFAULT NULL );
INSERT INTO t3 VALUES (1, 1.1), (2, NULL), (3, 3.3);
CREATE TABLE t4 ( id SERIAL PRIMARY KEY, description TEXT DEFAULT NULL );
INSERT INTO t4 VALUES (1, 'first'), (2, 'second'), (3, NULL);
WITH tmp AS (SELECT id FROM t4 WHERE description IS NOT NULL) SELECT * FROM t3 WHERE id IN (SELECT id FROM tmp);
