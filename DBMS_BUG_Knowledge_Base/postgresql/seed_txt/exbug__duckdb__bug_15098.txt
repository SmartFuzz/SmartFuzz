CREATE TABLE test_data (id SERIAL PRIMARY KEY, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (value) VALUES ('first'), ('second'), ('third'), ('fourth');
SELECT value FROM test_data WHERE id IS NOT NULL ORDER BY id DESC LIMIT 1;
COPY test_data (value) FROM 'seq.txt' WITH (FORMAT csv, HEADER false);
SELECT value FROM test_data ORDER BY created_at DESC LIMIT 1;
