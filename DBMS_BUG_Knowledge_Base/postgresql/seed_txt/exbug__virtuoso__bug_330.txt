CREATE TABLE test_table (id SERIAL PRIMARY KEY, value VARCHAR(255), positive_val INT CHECK (positive_val >= 0));
INSERT INTO test_table (value, positive_val) VALUES ('Test Value', 3);
SELECT pg_typeof(3) AS type;
SELECT * FROM test_table WHERE positive_val IS NOT NULL;
UPDATE test_table SET positive_val = NULL WHERE id = 1;
