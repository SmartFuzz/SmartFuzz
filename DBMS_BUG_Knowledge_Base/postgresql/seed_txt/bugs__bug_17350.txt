CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(50) NOT NULL, description TEXT);
INSERT INTO test_table (name, description) VALUES ('foo', 'A test entry'), ('BAR', 'Another test entry');
CREATE INDEX test_idx ON test_table USING gist(lower(name) gist_trgm_ops) INCLUDE (description);
SELECT lower(name) FROM test_table WHERE lower(name) LIKE '%o%';
