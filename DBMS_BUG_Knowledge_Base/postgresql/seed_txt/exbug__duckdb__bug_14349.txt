CREATE TABLE test_order_by (id INTEGER, value DOUBLE PRECISION, created_at TIMESTAMP, category VARCHAR, is_active BOOLEAN);
INSERT INTO test_order_by VALUES (1, 10.5, '2023-01-01 10:00:00', 'A', TRUE), (2, 20.0, '2023-01-01 09:00:00', 'B', FALSE), (3, 15.0, '2023-01-01 11:00:00', 'A', TRUE);
CREATE TABLE partitioned_data AS SELECT category, COUNT(*) AS count FROM test_order_by GROUP BY category;
COPY (SELECT * FROM partitioned_data ORDER BY count DESC) TO STDOUT WITH (FORMAT CSV, HEADER);
CREATE TABLE large_data AS SELECT generate_series(1, 1000000) AS num, random() AS rand_value;
