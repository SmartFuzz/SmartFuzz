DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data (id BIGINT, value TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data SELECT generate_series(1, 100) AS number, (generate_series(1, 100)::TEXT) AS value;
SELECT id, value FROM test_data WHERE id % 2 = 0;
ALTER TABLE test_data ADD COLUMN updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP;
