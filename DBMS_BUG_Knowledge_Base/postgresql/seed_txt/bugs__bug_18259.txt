CREATE TABLE test_table(id SERIAL PRIMARY KEY, name TEXT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table(name) VALUES ('test1'), ('test2'), (NULL), ('test3');
CREATE UNLOGGED TABLE filler(a INT, b TEXT);
INSERT INTO filler SELECT g, repeat('x', 1000) FROM generate_series(1, 50000) g;
DROP TABLE IF EXISTS filler;
