CREATE TABLE t2(x BYTEA, y BYTEA, z INT, w INT NOT NULL DEFAULT 0);
INSERT INTO t2 VALUES (decode('1010', 'hex'), decode('ff', 'hex'), 1, 1), (decode('0000', 'hex'), decode('00', 'hex'), 0, NULL);
SELECT MAX(CASE WHEN z IS NULL THEN x ELSE y END) FROM t2 GROUP BY w;
CREATE TABLE t3(a INT, b BYTEA, c VARCHAR(10));
INSERT INTO t3 VALUES (10, decode('11001', 'hex'), 'test'), (20, decode('00000', 'hex'), 'example');
