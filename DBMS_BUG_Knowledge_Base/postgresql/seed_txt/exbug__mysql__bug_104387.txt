CREATE TABLE colltest2 ( id SERIAL PRIMARY KEY, cbin BYTEA, ctxt TEXT COLLATE "utf8mb3_bin", cstr VARCHAR(255), cnum INT DEFAULT NULL);
INSERT INTO colltest2 (cbin, ctxt, cstr, cnum) VALUES (decode('61736466', 'hex'), 'asdf', 'asdf', NULL);
SELECT * FROM colltest2 WHERE cbin::TEXT LIKE cstr;
SELECT * FROM colltest2 WHERE cbin::TEXT LIKE ctxt;
SELECT * FROM colltest2 WHERE cnum IS NULL;
