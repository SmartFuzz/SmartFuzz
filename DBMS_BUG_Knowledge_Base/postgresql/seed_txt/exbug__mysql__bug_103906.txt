CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(255), data BYTEA);
INSERT INTO test_table (id, name, data) VALUES (1, 'Test', decode('', 'hex'));
SELECT id, name, data FROM test_table WHERE data IS NULL;
UPDATE test_table SET data = decode('', 'hex') WHERE id = 1;
SELECT decode('', 'hex') AS binary_data, CASE WHEN data IS NULL THEN 'NULL' ELSE 'NOT NULL' END AS null_check FROM test_table;
