CREATE TABLE buildings (id SERIAL PRIMARY KEY, geometry VARCHAR(255), measuredHeight DOUBLE PRECISION);
INSERT INTO buildings (geometry, measuredHeight) VALUES ('geometry1', 14.07), ('geometry2', 15.20);
CREATE TABLE heights (building_id INT, height DOUBLE PRECISION, FOREIGN KEY (building_id) REFERENCES buildings(id));
INSERT INTO heights (building_id, height) VALUES (1, 14.07), (2, 15.20);
SELECT b.id, h.height FROM buildings b JOIN heights h ON b.id = h.building_id LIMIT 2;
