CREATE DATABASE test_db;
CREATE TABLE t2(id SERIAL PRIMARY KEY, col_varchar VARCHAR(10), col_date DATE, col_decimal DECIMAL(10,2));
INSERT INTO t2(col_varchar, col_date, col_decimal) VALUES(NULL, '2023-01-01', 10.50);
SELECT col_varchar, col_date, col_decimal FROM t2 WHERE (col_varchar, col_date, col_decimal) IS NOT DISTINCT FROM (SELECT NULL, NULL, 10.50);
