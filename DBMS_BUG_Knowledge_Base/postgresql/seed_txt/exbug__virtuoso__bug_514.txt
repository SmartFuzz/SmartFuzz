CREATE TABLE test_property_paths (id SERIAL PRIMARY KEY, subject TEXT, predicate TEXT, object TEXT);
INSERT INTO test_property_paths (subject, predicate, object) VALUES ('dbr:Berlin', '<foo>', 'dbr:Paris');
INSERT INTO test_property_paths (subject, predicate, object) VALUES ('dbr:Berlin', '<foo>', 'dbr:London');
SELECT * FROM test_property_paths WHERE subject = 'dbr:Berlin' AND (predicate = '<foo>' OR predicate = '!<foo>') LIMIT 10;
SELECT DISTINCT object FROM test_property_paths WHERE subject = 'dbr:Berlin' AND (predicate = '<foo>' OR predicate = '!<foo>') LIMIT 10;
