CREATE TABLE C (x int, y varchar, z float);
INSERT INTO C VALUES (1, 'a', 1.1), (NULL, 'b', NULL), (3, 'c', 3.3), (NULL, NULL, 4.4);
CREATE TABLE D (x int, y varchar);
INSERT INTO D VALUES (3, 'c'), (NULL, 'd'), (1, NULL);
SELECT * FROM C WHERE (x, y) NOT IN (SELECT x, y FROM D);
