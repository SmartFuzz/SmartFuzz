CREATE TABLE t2(x INTEGER, y TEXT COLLATE "C", z REAL);
INSERT INTO t2 VALUES(1, 'Hello', 1.1);
INSERT INTO t2 VALUES(2, 'hello', 2.2);
SELECT y='HELLO', rank() OVER (ORDER BY z) FROM t2;
SELECT x, z FROM t2 WHERE y IS NULL;
