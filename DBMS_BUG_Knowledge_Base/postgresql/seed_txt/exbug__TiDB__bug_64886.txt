CREATE TABLE t2(c1 INT NOT NULL, c2 VARCHAR(255) DEFAULT NULL, PRIMARY KEY(c1));
INSERT INTO t2(c1, c2) VALUES (1, 'test'), (2, NULL), (3, 'example');
SELECT 5 AS b INTO TEMP TABLE temp_b;
SELECT CASE WHEN b IS NOT NULL THEN t2.c2 ELSE t2.c2 END FROM t2, temp_b WHERE t2.c1 > 0 GROUP BY t2.c1;
DROP TABLE temp_b;
