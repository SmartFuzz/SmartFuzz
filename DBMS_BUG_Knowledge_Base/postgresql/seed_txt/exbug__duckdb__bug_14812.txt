CREATE TABLE test_data (id SERIAL PRIMARY KEY, name VARCHAR, age INTEGER, salary FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (name, age, salary) VALUES ('Alice', 30, 70000.50);
INSERT INTO test_data (name, age, salary) VALUES ('Bob', NULL, 50000.00);
CREATE TABLE test_trip AS SELECT * FROM parquet_file('s3://BUCKET/PATH/TO/*.parquet');
SELECT * FROM test_data WHERE age IS NULL OR salary > 60000.00;
