CREATE TABLE test_places (id SERIAL PRIMARY KEY, name VARCHAR(255), type VARCHAR(50));
CREATE TABLE test_agents (id SERIAL PRIMARY KEY, name VARCHAR(255), type VARCHAR(50));
INSERT INTO test_places (name, type) VALUES ('Central Park', 'Place'), ('Eiffel Tower', 'Place');
INSERT INTO test_agents (name, type) VALUES ('Agent Smith', 'Agent'), ('Agent Johnson', 'Agent');
SELECT COUNT(DISTINCT p.id) AS number FROM test_places p JOIN test_agents a ON p.type = a.type WHERE p.type IS NOT NULL AND a.type IS NOT NULL;
