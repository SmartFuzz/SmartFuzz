CREATE TABLE TEST_AGG_V2(CID INTEGER, USERS BYTEA, STATUS VARCHAR);
INSERT INTO TEST_AGG_V2 VALUES (1, pg_aggregate_uniq_theta_state(ARRAY[1, 2]), 'active'), (2, pg_aggregate_uniq_theta_state(ARRAY[2, 3]), NULL), (3, pg_aggregate_uniq_theta_state(ARRAY[4, 5]), 'inactive');
SELECT COUNT(*) FROM TEST_AGG_V2 WHERE STATUS IS NULL;
SELECT finalize_uniq_theta_aggregation(uniq_theta_intersect(uniq_theta_merge_state_if(USERS, CID = 1), uniq_theta_merge_state_if(USERS, CID = 2))) FROM TEST_AGG_V2;
SELECT finalize_uniq_theta_aggregation(uniq_theta_intersect(uniq_theta_merge_state_if(USERS, CID = 1), uniq_theta_merge_state_if(USERS, CID = 4))) FROM TEST_AGG_V2;
