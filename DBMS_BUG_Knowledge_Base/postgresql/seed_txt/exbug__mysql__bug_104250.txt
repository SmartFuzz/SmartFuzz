CREATE DATABASE test_db;
CREATE TABLE users(id SERIAL PRIMARY KEY, name VARCHAR(100) NOT NULL, age INT CHECK (age >= 0));
INSERT INTO users (name, age) VALUES('Alice', 30);
INSERT INTO users (name, age) VALUES(NULL, 25);
SELECT * FROM users;
