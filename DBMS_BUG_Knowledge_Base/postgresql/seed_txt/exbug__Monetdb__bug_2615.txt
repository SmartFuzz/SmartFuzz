CREATE TABLE uitspraken (id SERIAL PRIMARY KEY, ljn CHAR(6) NOT NULL, gepubliceerd BOOLEAN NOT NULL, instantie VARCHAR(63) NOT NULL, datum DATE NOT NULL, publicatie DATE, zaaknummers VARCHAR(100) NOT NULL, uitspraak BYTEA, conclusie BYTEA, zittingsplaats VARCHAR(16), rechtsgebied VARCHAR(24), sector VARCHAR(20), soort VARCHAR(32), indicatie BYTEA, kop BYTEA, CONSTRAINT uitspraken_ljn_unique UNIQUE (ljn));
INSERT INTO uitspraken (id, ljn, gepubliceerd, instantie, datum, zaaknummers) VALUES (1, 'ABC123', TRUE, 'Instance1', '2023-01-01', 'ZN12345');
INSERT INTO uitspraken (id, ljn, gepubliceerd, instantie, datum, zaaknummers) VALUES (2, 'DEF456', FALSE, 'Instance2', '2023-01-02', 'ZN67890');
SELECT * FROM uitspraken WHERE gepubliceerd IS NULL;
UPDATE uitspraken SET datum = NULL WHERE id = 1;
