CREATE TABLE IF NOT EXISTS test_v2 (list_id UUID, item TEXT, created_at TIMESTAMP, price FLOAT8, is_active SMALLINT);
INSERT INTO test_v2 (list_id, item, created_at, price, is_active) SELECT gen_random_uuid(), g.item, g.created_at, g.price, g.is_active FROM generate_series(1, 100) AS s, (SELECT item, created_at, price, is_active FROM generate_series(1, 100) AS g(item TEXT, created_at TIMESTAMP, price FLOAT8, is_active SMALLINT));
CREATE INDEX idx_list_id_created_at ON test_v2 (list_id, created_at);
SELECT * FROM test_v2 WHERE list_id = '019a369d-0a96-7fbd-853c-a4335fd2ee9f' ORDER BY created_at LIMIT 100 OFFSET 6000;
