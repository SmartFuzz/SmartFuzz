CREATE TABLE IF NOT EXISTS http_log (timestamp TIMESTAMPTZ, sip INET, sport SMALLINT, dip INET, dport SMALLINT, host TEXT);
INSERT INTO http_log (timestamp, sip, sport, dip, dport, host) VALUES (NOW(),'1:1:1:1',40001,'2:2:2:2',8080,'api.example.com'), (NOW(),'1:1:1:1',40001,'2:2:2:2',8080,'blog.example.com'), (NOW(),'1:1:1:1',40001,'2:2:2:2',8080,'x.com'), (NOW(),'1:1:1:1',40001,'2:2:2:2',8080,'myownck.com');
CREATE TABLE IF NOT EXISTS domain_whitelist (domain TEXT);
INSERT INTO domain_whitelist (domain) VALUES ('example.com'), ('x.com'), ('test.com');
SELECT host FROM http_log a WHERE NOT EXISTS (SELECT 1 FROM domain_whitelist WHERE a.host LIKE '%' || domain);
