CREATE TABLE test_table (id SERIAL PRIMARY KEY, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (value) VALUES ('a'), ('a'), ('b'), ('a');
SELECT s(array_agg(value ORDER BY id), 'a', 2, 'X') FROM test_table;
SELECT s(array_agg(value ORDER BY id), 'a', 2, 'Y') FROM test_table;
SELECT s(array_agg(value ORDER BY id), 'a', 2, 'X') AS result_x, s(array_agg(value ORDER BY id), 'a', 2, 'Y') AS result_y FROM test_table;
