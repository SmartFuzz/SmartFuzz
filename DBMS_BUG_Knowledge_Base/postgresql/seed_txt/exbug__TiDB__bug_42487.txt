CREATE TABLE table2 (id SERIAL PRIMARY KEY, name VARCHAR(50), value NUMERIC(10,2));
INSERT INTO table2 (id, name, value) VALUES (1, 'test', 10.50);
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
BEGIN;
UPDATE table2 SET value = 20.75 WHERE id = 1;
SELECT * FROM table2 WHERE id = 1;
