CREATE DATABASE test_db;
CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(50), age INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);
SELECT id, name, age FROM test_table ORDER BY age;
