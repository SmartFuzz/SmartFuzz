CREATE RESOURCE GROUP rg2 WITH (max_rate = 200);
SET search_path TO test;
CREATE TABLE t2(id SERIAL PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE VIEW v_t2 AS SELECT * FROM t2;
SELECT * FROM v_t2 WHERE id IS NULL;
