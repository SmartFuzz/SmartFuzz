CREATE TABLE Bug(id BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY, data JSONB NOT NULL);
INSERT INTO Bug(data) VALUES ('["123", "456"]');
CREATE TABLE AnotherBug(id SERIAL PRIMARY KEY, bug_id BIGINT, FOREIGN KEY (bug_id) REFERENCES Bug(id));
INSERT INTO AnotherBug(bug_id) VALUES (1);
SELECT * FROM Bug WHERE 123 = ANY(data::jsonb[] ) AND EXISTS (SELECT 1 FROM AnotherBug WHERE bug_id = Bug.id);
