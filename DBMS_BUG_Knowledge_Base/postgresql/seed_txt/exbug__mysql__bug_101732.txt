CREATE TABLE sbtest1 (id SERIAL PRIMARY KEY, k INT NOT NULL);
CREATE TABLE sbtest2 (id SERIAL PRIMARY KEY, a INT DEFAULT NULL);
INSERT INTO sbtest1 (k) SELECT seq * 2 FROM generate_series(1, 10000000) AS seq;
INSERT INTO sbtest2 DEFAULT VALUES FROM generate_series(1, 10000000);
SELECT COUNT(*) FROM sbtest1 WHERE k IN (SELECT a FROM sbtest2 WHERE a IS NOT NULL);
