CREATE TABLE test_table (id SERIAL PRIMARY KEY, text_column VARCHAR(255) NOT NULL);
INSERT INTO test_table (id, text_column) VALUES (1, 'Find this pattern'), (2, NULL), (3, 'Another pattern');
CREATE OR REPLACE FUNCTION func(arg VARCHAR) RETURNS INT AS $$ BEGIN RETURN POSITION('pattern' IN arg);
END;
$$ LANGUAGE plpgsql;
SELECT func(text_column) FROM test_table WHERE id = 1;
