CREATE TABLE test_blob(COL1 bytea DEFAULT NULL, COL2 varchar(255) DEFAULT NULL);
INSERT INTO test_blob VALUES(decode('FF', 'hex')), (decode('7F', 'hex')), (decode('F9', 'hex')), (decode('FA', 'hex')), (NULL);
SELECT COL1, encode(COL1, 'escape') FROM test_blob WHERE encode(COL1, 'escape') NOT LIKE encode(decode('FF', 'hex'), 'escape');
SELECT COL1, lower(encode(COL1, 'escape')) FROM test_blob WHERE COL2 IS NULL;
SELECT COL1 FROM test_blob WHERE encode(COL1, 'escape') LIKE encode(decode('7F', 'hex'), 'escape');
