CREATE TABLE t2(c1 INT, c2 VARCHAR(50), c3 DATE, c4 FLOAT DEFAULT NULL);
INSERT INTO t2 VALUES(1, 'test1', '2023-01-01', 1.1), (2, 'test2', NULL, 2.2), (3, 'test3', '2023-03-03', NULL);
WITH RECURSIVE rownum_cte AS (SELECT c1, c2, c3, c4, ROW_NUMBER() OVER () AS r FROM t2 WHERE c2 IS NOT NULL) SELECT * FROM rownum_cte WHERE r < 3;
SET SESSION optimizer_switch = 'derived_condition_pushdown=off';
