CREATE TABLE t2(id SERIAL PRIMARY KEY, data BYTEA, info TEXT NOT NULL DEFAULT 'default');
CREATE INDEX t2data_info ON t2(data, info);
INSERT INTO t2(id, data, info) VALUES(1, repeat(E'\x00', 16), 'test');
INSERT INTO t2(id, data, info) VALUES(2, repeat(E'\x00', 0), NULL);
SELECT id, data, octet_length(data), info FROM t2;
