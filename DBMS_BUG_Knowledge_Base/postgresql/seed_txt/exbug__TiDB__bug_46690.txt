CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(255) COLLATE "C", description VARCHAR(255) COLLATE "C");
INSERT INTO test_table (name, description) VALUES ('example', 'sample data');
CREATE TABLE another_table (ref_id INT, value VARCHAR(255) COLLATE "C", FOREIGN KEY (ref_id) REFERENCES test_table(id));
INSERT INTO another_table (ref_id, value) VALUES (1, 'test      ');
SELECT * FROM test_table WHERE name = 'example' AND description IS NOT NULL;
