CREATE SCHEMA fuzz_testing;
CREATE OR REPLACE FUNCTION fuzz_testing.rad(d DOUBLE PRECISION) RETURNS DOUBLE PRECISION AS $$ BEGIN RETURN d * PI() / 180;
END;
$$ LANGUAGE plpgsql;
SELECT 1;
SELECT fuzz_testing.rad(55.81689);
