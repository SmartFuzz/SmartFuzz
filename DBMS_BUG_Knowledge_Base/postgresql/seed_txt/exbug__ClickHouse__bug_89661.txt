CREATE TABLE test_table (id INT, name TEXT, other TEXT);
INSERT INTO test_table SELECT number, to_char(number % 5), CASE WHEN number % 2 = 0 THEN NULL ELSE to_char(number % 5) END FROM generate_series(1, 100) AS number;
SELECT * FROM test_table WHERE (id, other) = (2, '1');
SELECT * FROM test_table WHERE id = 3 OR other IS NULL;
EXPLAIN SELECT * FROM test_table WHERE (id, other) = (5, NULL);
