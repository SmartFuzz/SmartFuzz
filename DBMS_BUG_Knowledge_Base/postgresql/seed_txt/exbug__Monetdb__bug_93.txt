CREATE TABLE users(id SERIAL PRIMARY KEY, name VARCHAR(100), age INT, email VARCHAR(100));
INSERT INTO users (id, name, age, email) VALUES(1, 'Alice', NULL, 'alice@example.com');
INSERT INTO users (id, name, age, email) VALUES(2, NULL, 25, NULL);
SELECT * FROM users WHERE age IS NULL;
SELECT name FROM users WHERE email IS NOT NULL;
