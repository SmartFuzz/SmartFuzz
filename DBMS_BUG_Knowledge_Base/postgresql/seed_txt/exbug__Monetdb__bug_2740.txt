CREATE TABLE users (id SERIAL PRIMARY KEY, name VARCHAR(100), age INTEGER CHECK (age >= 0));
INSERT INTO users (id, name, age) VALUES (1, 'Alice', 30);
CREATE TABLE orders (order_id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users(id), total_amount DECIMAL(10, 2));
INSERT INTO orders (order_id, user_id, total_amount) VALUES (101, 1, 250.75);
SELECT name, age, (SELECT COUNT(*) FROM orders WHERE user_id = users.id) AS order_count FROM users;
