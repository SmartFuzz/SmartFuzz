CREATE TABLE test_table (id SERIAL PRIMARY KEY, name TEXT COLLATE "C", value REAL NOT NULL);
INSERT INTO test_table (name, value) VALUES ('Alice', 10.5);
CREATE INDEX idx_name ON test_table (name COLLATE "C");
SELECT * FROM pg_catalog.pg_indexes WHERE tablename = 'test_table';
REINDEX INDEX idx_name;
