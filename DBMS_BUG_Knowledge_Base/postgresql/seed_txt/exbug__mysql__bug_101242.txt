CREATE DATABASE test_db;
CREATE TABLE my_table (id SERIAL PRIMARY KEY, my_column BYTEA NOT NULL, my_text_column TEXT DEFAULT NULL);
INSERT INTO my_table (my_column, my_text_column) VALUES (NULL, 'Sample Text');
INSERT INTO my_table (my_column) VALUES (decode('Boundary Test', 'escape'));
SELECT id, my_column, my_text_column FROM my_table WHERE my_column IS NOT NULL;
