CREATE TABLE users(id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER);
INSERT INTO users(name, age) VALUES('Alice', 30);
UPDATE users SET age = age + 1 WHERE name = 'Alice' RETURNING id, name, age;
CREATE TABLE orders(order_id SERIAL PRIMARY KEY, user_id INTEGER, amount REAL);
INSERT INTO orders(user_id, amount) VALUES(1, 99.99) RETURNING order_id, user_id, amount;
