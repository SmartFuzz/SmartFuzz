CREATE TABLE test_strings (id SERIAL PRIMARY KEY, str_col VARCHAR(255) NOT NULL);
INSERT INTO test_strings (str_col) VALUES ('HELLO'), ('WORLD'), ('POSTGRESQL');
SELECT ENCODE(CONVERT_TO(str_col, 'UTF8'), 'hex') AS hex_str_col FROM test_strings;
UPDATE test_strings SET str_col = NULL WHERE id = 1;
SELECT id, LENGTH(str_col), ENCODE(CONVERT_TO(str_col, 'UTF8'), 'hex') AS hex_str_col FROM test_strings WHERE str_col IS NOT NULL;
