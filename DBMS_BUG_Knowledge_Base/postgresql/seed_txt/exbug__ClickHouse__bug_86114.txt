CREATE TABLE test_table1 (id BIGINT, value TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE test_table2 (total DOUBLE PRECISION, id_bit BYTEA, description TEXT);
INSERT INTO test_table1 SELECT number AS id, number::TEXT AS value, CURRENT_TIMESTAMP FROM generate_series(1, 100000);
INSERT INTO test_table2 SELECT 1.5 AS total, (SELECT array_agg(id) FROM generate_series(1, 100) AS id) AS id_bit, NULL AS description;
SELECT COUNT(DISTINCT unnest(id_bit)) FROM test_table2 WHERE total > 1.0;
