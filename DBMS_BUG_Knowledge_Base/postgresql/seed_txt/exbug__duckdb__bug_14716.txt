CREATE TABLE points (id SERIAL PRIMARY KEY, value FLOAT NOT NULL, description TEXT);
INSERT INTO points (id, value, description) VALUES (1, 10.5, 'First point'), (2, NULL, 'Second point'), (3, 20.0, 'Third point');
CREATE TABLE relationships (source_id INTEGER REFERENCES points(id), target_id INTEGER REFERENCES points(id));
INSERT INTO relationships (source_id, target_id) VALUES (1, 2), (2, 3), (3, 1);
WITH RECURSIVE point_chain AS (SELECT id, value FROM points WHERE id = 1 UNION ALL SELECT r.target_id, p.value FROM relationships r JOIN point_chain pc ON r.source_id = pc.id JOIN points p ON r.target_id = p.id) SELECT * FROM point_chain;
