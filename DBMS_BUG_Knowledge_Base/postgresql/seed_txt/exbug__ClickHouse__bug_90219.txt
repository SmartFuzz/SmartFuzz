CREATE TABLE test1 (a INTEGER[], b INTEGER);
INSERT INTO test1 SELECT (generate_series(0, 99) % 10), generate_series(0, 99);
CREATE TABLE test2 (x FLOAT8, y TEXT);
INSERT INTO test2 SELECT number::FLOAT8, number::TEXT FROM generate_series(0, 49) AS number;
CREATE TABLE test3 (id BIGINT, data TEXT[]);
