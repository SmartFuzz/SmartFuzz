CREATE TABLE test_table (id SERIAL PRIMARY KEY, value FLOAT, status VARCHAR(10) DEFAULT 'active');
INSERT INTO test_table (id, value) VALUES (DEFAULT, NULL), (DEFAULT, RANDOM() * 10000), (DEFAULT, RANDOM() * 10000), (DEFAULT, NULL), (DEFAULT, RANDOM() * 10000);
SET LOCAL work_mem = '64MB';
SELECT id, CASE WHEN RANDOM() < 0.5 THEN value ELSE NULL END AS computed_value FROM test_table WHERE computed_value IS NOT NULL;
SET LOCAL work_mem = '64MB';
