DROP DATABASE IF EXISTS test_db;
CREATE DATABASE test_db;
CREATE TABLE test_db.sample_table (id BIGINT, name TEXT, created_at TIMESTAMP, score FLOAT8) WITH (OIDS=FALSE);
CREATE VIEW test_db.sample_view AS SELECT id, name, created_at FROM test_db.sample_table WHERE score > 50.0;
INSERT INTO test_db.sample_table SELECT number, concat('Name', to_char(number)), now(), number % 100 FROM generate_series(1, 100) AS number;
