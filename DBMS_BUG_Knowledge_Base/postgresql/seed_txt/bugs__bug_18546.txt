CREATE TABLE t (a int, b text);
CREATE VIEW v AS SELECT a, b, a + 1 AS a1 FROM t;
INSERT INTO v VALUES (default, 'test');
CREATE TABLE u (x int, y numeric);
INSERT INTO v SELECT default, 'example' FROM u;
