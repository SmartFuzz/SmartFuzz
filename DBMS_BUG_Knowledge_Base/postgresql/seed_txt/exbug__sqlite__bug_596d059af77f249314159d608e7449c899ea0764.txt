CREATE TABLE users(id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK(age >= 0));
CREATE TABLE orders(order_id SERIAL PRIMARY KEY, user_id INTEGER, amount REAL, FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO users(name, age) VALUES('Alice', 30);
INSERT INTO orders(user_id, amount) VALUES(1, 100.50);
ALTER TABLE orders RENAME TO purchases;
