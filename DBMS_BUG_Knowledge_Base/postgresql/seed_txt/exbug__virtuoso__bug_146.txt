CREATE TABLE vocab_Book (id SERIAL PRIMARY KEY, title VARCHAR(255) NOT NULL);
INSERT INTO vocab_Book (id, title) VALUES (1, 'Book 1');
SELECT DISTINCT id AS me, title AS var3, pg_typeof(title) AS var4 FROM vocab_Book WHERE id = 1;
SELECT DISTINCT id AS me, title AS var3, pg_typeof(title) AS var4 FROM vocab_Book WHERE title IS NULL;
