CREATE TABLE test_files (id SERIAL PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_files (name) VALUES ('File A'), ('File B'), ('File C'), ('File D'), ('File E');
CREATE TABLE test_people (id SERIAL PRIMARY KEY, name VARCHAR(255), age INT CHECK (age >= 0));
INSERT INTO test_people (name, age) VALUES ('Alice', 30), ('Bob', 25), ('Charlie', NULL);
SELECT DISTINCT * FROM test_files WHERE EXISTS (SELECT 1 FROM test_people WHERE test_people.id = test_files.id) LIMIT 1;
