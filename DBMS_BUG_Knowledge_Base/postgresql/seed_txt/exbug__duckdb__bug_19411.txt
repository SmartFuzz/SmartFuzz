CREATE TABLE test_data (id TEXT, updated_at TIMESTAMPTZ, test_string TEXT, test_unicode TEXT, test_time TIME, test_bytes BYTEA, test_smallint SMALLINT, test_integer INTEGER, test_bigint BIGINT, test_real REAL, test_double DOUBLE PRECISION, test_decimal DECIMAL, test_boolean BOOLEAN, test_date DATE, test_json TEXT, test_vector DOUBLE PRECISION[], test_geography TEXT, tenant_id TEXT);
INSERT INTO test_data SELECT md5(CAST(range AS TEXT)), (to_timestamp(range))::timestamptz, rpad('hello_', CAST(range AS INTEGER), '0'), chr(CAST(range AS INTEGER)), CAST(to_timestamp(range)::timestamp AS TIME), md5(CAST(range AS TEXT))::BYTEA, CAST(range % 32767 AS SMALLINT), CAST(range AS INTEGER), CAST(range AS BIGINT), CAST(range / 3.0 AS REAL), CAST(range / 3.0 AS DOUBLE PRECISION), CAST(range AS DECIMAL), CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END, (DATE '2022-02-24' - (CAST(range % 3650 AS INTEGER) * INTERVAL '1 day'))::DATE, json_build_object('number', range, 'string', md5(CAST(range AS TEXT)))::TEXT, ARRAY[CAST(range / 1.0 AS DOUBLE PRECISION), CAST(range / 2.0 AS DOUBLE PRECISION), CAST(range / 3.0 AS DOUBLE PRECISION), CAST(range / 4.0 AS DOUBLE PRECISION)], ST_GeomFromGeoJSON(json_build_object('type', 'Point', 'coordinates', ARRAY[CAST(180.0 / (range + 1.0) AS DOUBLE PRECISION), CAST(90.0 / (range + 1.0) AS DOUBLE PRECISION)]::FLOAT[2])::TEXT) FROM generate_series(0, 9999) AS range;
COPY (SELECT * FROM test_data WHERE test_unicode IS NOT NULL) TO 'valid_data.csv' WITH (FORMAT CSV, HEADER TRUE, DELIMITER '|', QUOTE '"');
SELECT * FROM read_csv('valid_data.csv', header=TRUE, new_line='\n', delim='|', escape='"', strict_mode=FALSE, types='(id text, updated_at timestamptz, test_bytes bytea, test_string text, test_unicode text, test_smallint integer, test_integer integer, test_bigint bigint, test_real double precision, test_double double precision, test_boolean boolean, test_decimal text, test_date date, test_time time, test_json text, test_vector text, test_geography text, tenant_id text)') ORDER BY updated_at;
INSERT INTO test_data SELECT md5(CAST(range AS TEXT)), (to_timestamp(range))::timestamptz, rpad('hello_', CAST(range AS INTEGER), '0'), CASE WHEN range = 39 THEN NULL ELSE chr(CAST(range AS INTEGER)) END, CAST(to_timestamp(range)::timestamp AS TIME), md5(CAST(range AS TEXT))::BYTEA, CAST(range % 32767 AS SMALLINT), CAST(range AS INTEGER), CAST(range AS BIGINT), CAST(range / 3.0 AS REAL), CAST(range / 3.0 AS DOUBLE PRECISION), CAST(range AS DECIMAL), CASE WHEN range % 2 = 0 THEN TRUE ELSE FALSE END, (DATE '2022-02-24' - (CAST(range % 3650 AS INTEGER) * INTERVAL '1 day'))::DATE, json_build_object('number', range, 'string', md5(CAST(range AS TEXT)))::TEXT, ARRAY[CAST(range / 1.0 AS DOUBLE PRECISION), CAST(range / 2.0 AS DOUBLE PRECISION), CAST(range / 3.0 AS DOUBLE PRECISION), CAST(range / 4.0 AS DOUBLE PRECISION)], ST_GeomFromGeoJSON(json_build_object('type', 'Point', 'coordinates', ARRAY[CAST(180.0 / (range + 1.0) AS DOUBLE PRECISION), CAST(90.0 / (range + 1.0) AS DOUBLE PRECISION)]::FLOAT[2])::TEXT) FROM generate_series(0, 9999) AS range;
