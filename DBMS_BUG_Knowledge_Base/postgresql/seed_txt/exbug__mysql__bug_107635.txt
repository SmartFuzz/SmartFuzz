CREATE TABLE test_events (id SERIAL PRIMARY KEY, event_name VARCHAR(255) NOT NULL, event_time TIMESTAMP NOT NULL, is_active BOOLEAN DEFAULT TRUE);
INSERT INTO test_events (event_name, event_time) VALUES ('Test Event 1', NOW() + INTERVAL '1 second');
DO $$ BEGIN PERFORM pg_sleep(1);
INSERT INTO test_events (event_name, event_time) VALUES ('Scheduled Event', NOW());
END $$;
SELECT pg_terminate_backend(pg_stat_activity.pid) FROM pg_stat_activity WHERE pg_stat_activity.state = 'idle' AND pg_stat_activity.query = '<IDLE>';
