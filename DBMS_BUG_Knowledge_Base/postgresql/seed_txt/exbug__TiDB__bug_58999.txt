DROP TABLE IF EXISTS t2;
CREATE TABLE t2(c0 INT, c1 VARCHAR(50) DEFAULT NULL, c2 DATE);
INSERT INTO t2 VALUES (1, 'test', NULL), (2, NULL, '2023-01-01'), (3, 'example', '2023-01-02');
SELECT CASE WHEN c1 IS NULL THEN 'NULL' ELSE c1 END AS result FROM t2 WHERE c0 = 1;
SELECT TRUE FROM t2 LEFT JOIN (SELECT NULL::INT AS c0) AS subquery ON true WHERE (CASE c0 WHEN 1 THEN NULL ELSE c1 END) IS NOT NULL;
