CREATE TABLE testtable (dim_id bigserial PRIMARY KEY, dim_type varchar(100) NOT NULL, dim_value varchar(100) NOT NULL, CONSTRAINT unique_dimension UNIQUE(dim_type, dim_value));
INSERT INTO testtable (dim_type, dim_value) VALUES ('user', 'user1');
SELECT dim_id, dim_type, dim_value FROM testtable WHERE dim_type='user' AND dim_value='user1';
CREATE TABLE test (roe_id bigserial PRIMARY KEY, roe_type bigint NOT NULL, node_event_id bigint NOT NULL, dc_servername varchar(50), roe_start_date_withtz varchar(50), usr_id bigint, customgroup_id bigint, queuename_id bigint, exitstatus_id bigint);
INSERT INTO test (roe_type, node_event_id, dc_servername) VALUES (1, 100, 'server1');
