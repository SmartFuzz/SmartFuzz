CREATE TABLE test_data (id SERIAL PRIMARY KEY, value DOUBLE PRECISION, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (value, name) SELECT random(), 'Name ' || i FROM generate_series(1, 200000000) AS i;
CREATE TABLE filtered_data AS SELECT * FROM test_data WHERE value IS NOT NULL;
COPY (SELECT * FROM filtered_data ORDER BY value DESC) TO '/tmp/filtered_test.parquet' WITH (FORMAT 'csv', DELIMITER ',', HEADER);
SELECT COUNT(*) FROM filtered_data WHERE value BETWEEN 0.0 AND 1.0;
