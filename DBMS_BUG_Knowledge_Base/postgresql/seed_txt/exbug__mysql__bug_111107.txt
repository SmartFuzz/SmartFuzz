CREATE TABLE test.data_types (id SERIAL PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, amount NUMERIC(10, 2) NOT NULL);
INSERT INTO test.data_types (id, name, amount) VALUES (1, 'Test', 100.50);
CREATE OR REPLACE PROCEDURE test.exampleProcedure(a INT, b VARCHAR(50), c NUMERIC(10, 2), d TIMESTAMP) LANGUAGE plpgsql AS $$ BEGIN SELECT a, b, c, d;
END;
$$;
CALL test.exampleProcedure(1, 'Sample', NULL, NOW());
