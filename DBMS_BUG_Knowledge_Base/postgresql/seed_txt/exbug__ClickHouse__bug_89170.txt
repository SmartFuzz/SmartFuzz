CREATE TABLE test_distinct_oom_v2 ( id BIGINT, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP );
INSERT INTO test_distinct_oom_v2 SELECT number, CASE WHEN number % 2 = 0 THEN CAST(number AS VARCHAR) ELSE NULL END FROM generate_series(1, 100000000) AS number;
SELECT DISTINCT value FROM test_distinct_oom_v2;
CREATE TABLE test_distinct_oom_v3 ( id INT, value FLOAT8, category VARCHAR DEFAULT 'unknown' );
INSERT INTO test_distinct_oom_v3 SELECT number, number::FLOAT8 / 3.14, (number % 10)::VARCHAR FROM generate_series(1, 1000000) AS number;
