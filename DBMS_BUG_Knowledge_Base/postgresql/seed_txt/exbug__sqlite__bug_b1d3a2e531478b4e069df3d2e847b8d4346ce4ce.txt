CREATE TABLE parent (id SERIAL PRIMARY KEY, name TEXT NOT NULL);
CREATE TABLE child (id SERIAL PRIMARY KEY, parent_id INTEGER REFERENCES parent(id) ON DELETE CASCADE);
INSERT INTO parent (name) VALUES ('Parent 1');
INSERT INTO child (parent_id) VALUES (1);
SELECT * FROM parent;
DROP TABLE parent;
