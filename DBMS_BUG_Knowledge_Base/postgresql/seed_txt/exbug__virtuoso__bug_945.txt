CREATE TABLE test_transitive (id SERIAL PRIMARY KEY, name VARCHAR(255), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_transitive (name, value) VALUES ('Node A', 10.5), ('Node B', 20.0), ('Node C', NULL);
UPDATE test_transitive SET value = value + 5 WHERE id = 1;
SELECT name FROM test_transitive WHERE value IS NOT NULL AND created_at > NOW() - INTERVAL '1 DAY';
SELECT x, syn FROM (SELECT x, syn FROM test_transitive AS t1 JOIN test_transitive AS t2 ON t1.id = t2.id) AS subquery WHERE x = 'http://dbpedia.org/resource/New_York';
