CREATE TABLE test_table (id SERIAL PRIMARY KEY, uri VARCHAR(255), label VARCHAR(255), lang VARCHAR(10));
INSERT INTO test_table (uri, label, lang) VALUES ('http://dbpedia.org/resource/Semantic_Web', 'Web sémantique', 'fr');
INSERT INTO test_table (uri, label, lang) VALUES ('http://dbpedia.org/resource/Peer-to-peer', 'Pair à pair', 'fr');
SELECT uri, label FROM test_table WHERE lang = 'fr' AND uri IS NOT NULL ORDER BY label;
SELECT DISTINCT uri, label FROM test_table WHERE id IN (SELECT id FROM test_table WHERE lang = 'fr') ORDER BY label;
