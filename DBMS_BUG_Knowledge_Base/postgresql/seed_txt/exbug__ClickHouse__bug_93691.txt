CREATE TYPE test_enum1_status AS ENUM ('active', 'inactive', 'pending', 'suspended');
CREATE TABLE test_enum1 (status test_enum1_status);
CREATE TYPE test_enum2_level AS ENUM ('low', 'medium', 'high');
CREATE TABLE test_enum2 (level test_enum2_level);
INSERT INTO test_enum1 (status) VALUES ('active'), ('inactive'), ('pending');
SELECT * FROM test_enum1 WHERE status = 'pending';
