CREATE TABLE test_partitioned_table (t_date CHAR(8) DEFAULT NULL, o_no VARCHAR(32) NOT NULL, c_code VARCHAR(32) DEFAULT NULL, id SERIAL PRIMARY KEY, t_date_partition DATE NOT NULL) PARTITION BY RANGE (t_date_partition);
INSERT INTO test_partitioned_table (t_date, o_no, c_code, t_date_partition) VALUES ('20181219', 'order1', 'code1', '2018-12-19');
INSERT INTO test_partitioned_table (t_date, o_no, c_code, t_date_partition) VALUES ('20181220', 'order2', 'code2', '2018-12-20');
SELECT COUNT(*) AS total_rows, MIN(id) AS min_id, MAX(id) AS max_id FROM test_partitioned_table;
ALTER TABLE test_partitioned_table ADD COLUMN new_col VARCHAR(50) DEFAULT NULL;
