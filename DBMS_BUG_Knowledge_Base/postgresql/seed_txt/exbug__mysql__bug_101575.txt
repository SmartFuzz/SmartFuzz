CREATE DATABASE test_db;
CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(100) NOT NULL, description VARCHAR(200) NOT NULL);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description');
SELECT * FROM test_table WHERE name ILIKE $1 OR description ILIKE $2;
