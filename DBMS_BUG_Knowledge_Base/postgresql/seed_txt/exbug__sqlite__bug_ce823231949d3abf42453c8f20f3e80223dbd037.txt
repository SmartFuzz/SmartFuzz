CREATE TABLE t1(id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK(age >= 0));
INSERT INTO t1 (id, name, age) VALUES (1, 'john', 30);
INSERT INTO t1 (id, name, age) VALUES (2, 'james', NULL);
CREATE VIEW v2 AS WITH t4(Name) AS (VALUES ('A'), ('B')) SELECT Name FROM t4;
CREATE VIEW v3 AS WITH t4(Att, Val, Act) AS (VALUES ('C', 'D', 'E'), ('F', 'G', 'H')) SELECT D.id, P.name, T.Att, T.Val, T.Act FROM t1 D CROSS JOIN v2 P CROSS JOIN t4 T;
