CREATE TABLE users(id SERIAL PRIMARY KEY, name VARCHAR(50), age INT);
INSERT INTO users(name, age) VALUES('Alice', 30), ('Bob', NULL), ('Charlie', 25);
CREATE TABLE orders(order_id SERIAL PRIMARY KEY, user_id INT, amount DECIMAL(10, 2), FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO orders(user_id, amount) VALUES(1, 100.50), (2, 200.75);
SELECT * FROM users u LEFT JOIN orders o ON u.id = o.user_id OFFSET 1 LIMIT 1;
