CREATE TABLE test_data (id INT, name VARCHAR, value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, name, value, created_at) VALUES (1, 'Alice', 10.5, '2023-01-01 10:00:00'), (2, 'Bob', NULL, '2023-01-02 11:00:00'), (3, 'Charlie', 15.0, NULL);
CREATE OR REPLACE FUNCTION read(fn TEXT) RETURNS TABLE(id INT, name VARCHAR, value FLOAT, created_at TIMESTAMP) AS $$ BEGIN RETURN QUERY EXECUTE FORMAT('SELECT * FROM read_csv_auto(%L)', fn);
END;
$$ LANGUAGE plpgsql;
SELECT * FROM test_data WHERE value IS NULL;
