CREATE TABLE test_table ( id SERIAL PRIMARY KEY, value DOUBLE PRECISION, description TEXT );
INSERT INTO test_table (value, description) VALUES (NULL, 'first'), (NULL, 'second'), (1.5, 'third');
CREATE TABLE another_table ( ref_id INTEGER REFERENCES test_table(id), score DOUBLE PRECISION );
INSERT INTO another_table (ref_id, score) VALUES (1, NULL), (2, 2.5), (3, NULL);
SELECT percentile_cont(0.25) WITHIN GROUP (ORDER BY value), percentile_cont(0.5) WITHIN GROUP (ORDER BY value) FROM test_table WHERE id > 3;
