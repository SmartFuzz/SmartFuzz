BEGIN;
CREATE TABLE test_table (id SERIAL PRIMARY KEY, name TEXT NOT NULL, value REAL DEFAULT 0.0);
CREATE TABLE temp_table AS SELECT * FROM test_table WHERE value > (SELECT AVG(value) FROM test_table);
CREATE TABLE faulty_table AS SELECT pg_read_binary_file('path/to/zeroblob') AS blob_data WHERE false;
SELECT pg_catalog.pg_table_is_visible('test_table'::regclass);
ROLLBACK;
