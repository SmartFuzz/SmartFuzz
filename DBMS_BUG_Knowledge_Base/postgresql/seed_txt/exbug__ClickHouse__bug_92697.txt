CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, value FLOAT8);
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.5), (2, 'test2', NULL), (3, 'test3', 20.0);
CREATE TABLE another_table (ref_id INT, description VARCHAR(255), amount INT);
INSERT INTO another_table (ref_id, description, amount) SELECT id, name || ' description', CAST(value AS INT) FROM test_table WHERE value IS NOT NULL;
SELECT * FROM generate_series(1, 99) AS counter;
