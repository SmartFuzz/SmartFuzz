CREATE TABLE automobiles (id SERIAL PRIMARY KEY, engine VARCHAR(255));
INSERT INTO automobiles (engine) VALUES ('123.45'), ('http://example.com/engine1'), (NULL);
SELECT id, engine, (CASE WHEN engine ~ '^[0-9]+(\.[0-9]+)?$' THEN ABS(CAST(engine AS DECIMAL)) END AS abs_value) FROM automobiles;
SELECT id, engine FROM automobiles WHERE engine IS NOT NULL;
SELECT id, engine FROM automobiles WHERE engine LIKE 'http://%';
