CREATE TABLE products (id INT, name VARCHAR, price FLOAT8, created_at TIMESTAMP);
INSERT INTO products VALUES (1, 'Laptop', 999.99, NOW());
INSERT INTO products VALUES (2, 'Smartphone', 499.99, NOW());
SELECT id, AVG(price) FROM products GROUP BY id;
SELECT id, COUNT(*) FROM products GROUP BY id, created_at;
