CREATE TABLE rocksDB(A BIGINT, B TEXT);
INSERT INTO rocksDB VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
CREATE TABLE test_rocksDB_Complex(A BIGINT PRIMARY KEY, B TEXT);
INSERT INTO test_rocksDB_Complex SELECT A, B FROM rocksDB;
SELECT B FROM test_rocksDB_Complex WHERE A = 1;
SELECT B FROM test_rocksDB_Complex WHERE A IS NULL;
