CREATE TABLE a (x VARCHAR(10) NOT NULL, y VARCHAR(10));
INSERT INTO a VALUES ('aaa', 'AAA');
CREATE TABLE b (x VARCHAR(10) UNIQUE, y VARCHAR(10));
INSERT INTO b VALUES ('aaa', 'AAA'), ('aAa', 'aAa'), ('aA', 'aA');
SELECT a.x, b.x FROM a JOIN b ON a.y ILIKE b.y WHERE a.x ILIKE b.x;
