CREATE TABLE users(id SERIAL PRIMARY KEY, username TEXT NOT NULL, email TEXT UNIQUE);
INSERT INTO users (username, email) VALUES ('user1', 'user1@example.com'), ('user2', 'user2@example.com');
CREATE TABLE orders(order_id SERIAL, user_id INT, amount NUMERIC(10, 2), FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO orders (user_id, amount) VALUES (1, 99.99), (2, 149.49);
SELECT u.username, COUNT(o.order_id) FROM users u LEFT JOIN orders o ON u.id = o.user_id GROUP BY u.username HAVING COUNT(o.order_id) > 0;
