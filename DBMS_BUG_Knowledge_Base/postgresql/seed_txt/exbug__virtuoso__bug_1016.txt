CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name) VALUES ('Test Name');
SELECT * FROM test_table WHERE id = (SELECT MAX(id) FROM test_table);
UPDATE test_table SET name = NULL WHERE id = 1;
SELECT EXISTS(SELECT 1 FROM test_table);
