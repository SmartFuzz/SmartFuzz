CREATE TABLE test_table (id SERIAL PRIMARY KEY, text_column VARCHAR, nullable_column VARCHAR);
INSERT INTO test_table (text_column, nullable_column) VALUES ('Sample Text', NULL);
INSERT INTO test_table (text_column, nullable_column) VALUES ('Another Example', 'Non-NULL');
SELECT LPAD(text_column, 2147483647, 'X') FROM test_table WHERE text_column IS NOT NULL;
SELECT COALESCE(LPAD(NULL::VARCHAR, 10, 'Y'), 'NULL'), LPAD(text_column, 0, 'Z') FROM test_table;
