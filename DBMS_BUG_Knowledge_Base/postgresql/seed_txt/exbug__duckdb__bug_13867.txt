CREATE TABLE test_random (id SERIAL PRIMARY KEY, value DOUBLE PRECISION);
INSERT INTO test_random (value) SELECT random() FROM generate_series(1, 100000);
SELECT COUNT(DISTINCT value) FROM test_random;
SELECT COUNT(*) FROM (SELECT DISTINCT random() AS a FROM generate_series(1, 100000)) AS subquery;
SELECT COUNT(*) FROM test_random WHERE value IS NOT NULL;
