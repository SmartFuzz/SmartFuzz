DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL, value DECIMAL(10, 2) DEFAULT 0.00);
INSERT INTO test_table (name, value) VALUES ('A', 10.50), ('B', 20.75), ('C', NULL), ('D', 30.00);
SELECT name, COUNT(*) FROM test_table GROUP BY name ORDER BY COUNT(*) LIMIT 5;
SELECT name, SUM(value) FROM test_table GROUP BY name HAVING SUM(value) IS NOT NULL;
