CREATE TABLE users (id bigserial NOT NULL PRIMARY KEY, name varchar(255) NOT NULL, age integer DEFAULT NULL);
INSERT INTO users(name, age) VALUES ('Alice', 30);
CREATE TABLE orders (order_id bigint NOT NULL, user_id bigint NOT NULL, amount numeric(10,2) NOT NULL, UNIQUE (order_id, user_id));
INSERT INTO orders(order_id, user_id, amount) VALUES (1, 1, 99.99);
DELETE FROM orders WHERE (order_id, user_id) IN ((1, 1));
