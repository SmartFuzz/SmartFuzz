CREATE TABLE additional_table (id INTEGER, ts TIMESTAMP, symbol VARCHAR, price DECIMAL, volume INTEGER);
INSERT INTO additional_table VALUES (1, '2024-01-01 10:00:00', 'A', 150.00, 1000);
INSERT INTO additional_table VALUES (2, '2024-01-01 10:05:00', 'A', 151.00, 1500);
SELECT * FROM left_table l FULL JOIN additional_table a ON l.symbol = a.symbol AND l.ts >= a.ts AND l.price + a.price > 300;
SELECT * FROM left_table l WHERE EXISTS (SELECT 1 FROM right_table r WHERE l.symbol = r.symbol AND l.ts >= r.ts AND l.price + r.bid IS NULL);
