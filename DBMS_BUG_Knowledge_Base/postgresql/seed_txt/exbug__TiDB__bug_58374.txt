CREATE TABLE IF NOT EXISTS testPartition2 ( ID SERIAL PRIMARY KEY, OrderDate TIMESTAMP, Status VARCHAR(10) DEFAULT 'Pending' );
INSERT INTO testPartition2 (ID, OrderDate) VALUES (DEFAULT, NULL);
INSERT INTO testPartition2 (ID, OrderDate, Status) VALUES (DEFAULT, '2024-01-01', 'Completed');
SELECT * FROM testPartition2 WHERE OrderDate IS NULL;
SELECT COUNT(*) FROM testPartition2 WHERE Status = 'Pending' OR OrderDate IS NULL;
