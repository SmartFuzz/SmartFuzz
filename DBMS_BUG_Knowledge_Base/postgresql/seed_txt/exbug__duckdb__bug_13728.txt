CREATE TABLE t2 (a INTEGER, b VARCHAR, c DOUBLE PRECISION, d BOOLEAN);
INSERT INTO t2 VALUES (1, 'test', 3.14, TRUE), (2, 'example', NULL, FALSE);
CREATE OR REPLACE FUNCTION unpivot_test(vars TEXT[]) RETURNS TABLE(a INTEGER, b VARCHAR) AS $$ BEGIN RETURN QUERY EXECUTE FORMAT('SELECT * FROM t2 UNPIVOT (value FOR column IN (%L))', vars);
END;
$$ LANGUAGE plpgsql;
SELECT * FROM unpivot_test(ARRAY['a', 'b']);
