CREATE TABLE v1 ( id SERIAL PRIMARY KEY, value VARCHAR(255) NOT NULL );
INSERT INTO v1 (id, value) VALUES (1, 'test'), (2, NULL), (3, 'example');
CREATE TABLE v2 ( a DOUBLE PRECISION, b INT DEFAULT 0, c DATE );
INSERT INTO v2 (a, b, c) VALUES (NULL, 1, '2023-01-01'), (5.5, 2, NULL), (3.14, NULL, '2023-01-03');
DELETE FROM v2 WHERE ( a IS NULL OR ( SELECT DENSE_RANK() OVER ( PARTITION BY b ORDER BY a ) FROM v2 ) < 2 );
