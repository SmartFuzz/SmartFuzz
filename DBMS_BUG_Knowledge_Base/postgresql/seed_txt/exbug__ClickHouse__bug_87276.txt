CREATE TABLE t1 (id SERIAL PRIMARY KEY, name VARCHAR, created_at TIMESTAMP) WITH (OIDS=FALSE);
CREATE TABLE t2 (user_id INT, score DOUBLE PRECISION, status VARCHAR NULL);
INSERT INTO t1 (name, created_at) VALUES ('Alice', NOW()), ('Bob', NOW());
INSERT INTO t2 (user_id, score, status) VALUES (1, 95.5, 'active'), (2, 88.0, NULL);
SELECT t1.name, t2.score FROM t1 JOIN t2 ON t1.id = t2.user_id WHERE t2.status IS NOT NULL;
