CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT, age INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (name, age) VALUES ('Alice', 30);
INSERT INTO users (name, age) VALUES ('Bob', NULL);
SELECT * FROM users WHERE age::TEXT = '30';
SELECT * FROM users WHERE created_at IS NOT NULL;
