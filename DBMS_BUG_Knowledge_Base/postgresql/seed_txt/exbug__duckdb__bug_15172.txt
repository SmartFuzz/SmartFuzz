CREATE TABLE test_data (id SERIAL PRIMARY KEY, name VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, name) VALUES (1, 'Sample Data');
SELECT * FROM test_data WHERE id = 1;
SELECT * FROM pg_catalog.pg_secrets WHERE secret_name = 'my_test_secret';
DROP SECRET my_test_secret;
