CREATE TABLE first (id int NOT NULL, value varchar(255), CONSTRAINT first_id_pkey PRIMARY KEY (id));
CREATE TABLE second (id int NOT NULL, ref int, data float, CONSTRAINT second_id_pkey PRIMARY KEY (id), CONSTRAINT second_ref_fkey FOREIGN KEY (ref) REFERENCES first (id));
INSERT INTO first (id, value) VALUES (1, 'test');
INSERT INTO second (id, ref, data) VALUES (1, NULL, 3.14);
SELECT * FROM second WHERE ref IS NULL;
