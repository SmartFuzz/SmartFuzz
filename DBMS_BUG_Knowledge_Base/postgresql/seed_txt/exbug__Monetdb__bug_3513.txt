CREATE TABLE test_large_file (id SERIAL PRIMARY KEY, data BYTEA NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_large_file (data) VALUES (decode('large_data', 'escape'));
CREATE TABLE temp_data (foo DOUBLE PRECISION, bar VARCHAR(255));
INSERT INTO temp_data (foo, bar) SELECT 1.0, 'test' UNION ALL SELECT 2.0, NULL;
COPY test_large_file(data) FROM '/path/to/large_file' WITH (FORMAT binary);
