CREATE TABLE test_blob (id SERIAL PRIMARY KEY, data BYTEA NOT NULL);
INSERT INTO test_blob (id, data) VALUES (1, DECODE('Sample BLOB data', 'escape'));
INSERT INTO test_blob (id, data) VALUES (2, DECODE(REPEAT('A', 1049), 'escape'));
SELECT * FROM test_blob WHERE id = 1;
DELETE FROM test_blob WHERE id IS NULL;
