CREATE TABLE t2(id SERIAL PRIMARY KEY, name VARCHAR(50) NOT NULL, age INT CHECK (age >= 0));
CREATE TABLE t3(id VARCHAR(100) NOT NULL, description TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2(id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', NULL);
INSERT INTO t3(id, description) VALUES ('1', 'First entry'), ('2', 'Second entry');
EXPLAIN SELECT * FROM t2 WHERE age IN (SELECT CAST(id AS INTEGER) FROM t3);
