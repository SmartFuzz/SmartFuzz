CREATE TABLE nested_structs (id INTEGER, data JSONB);
INSERT INTO nested_structs VALUES (1, jsonb_build_object('name', 'item1', 'value', 100));
INSERT INTO nested_structs VALUES (2, jsonb_build_object('name', 'item2', 'value', NULL));
SELECT id, data->>'name' AS name, (data->>'value')::INTEGER AS value FROM nested_structs WHERE (data->>'value')::INTEGER IS NOT NULL;
SELECT id, data->>'name' AS name, COALESCE((data->>'value')::INTEGER, 0) AS value FROM nested_structs;
