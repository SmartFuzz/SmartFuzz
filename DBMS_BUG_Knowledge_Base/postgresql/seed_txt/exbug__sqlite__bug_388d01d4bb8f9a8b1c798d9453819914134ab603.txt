CREATE EXTENSION IF NOT EXISTS fts4;
CREATE TABLE t1(x TEXT PRIMARY KEY, y INTEGER NOT NULL DEFAULT 0);
INSERT INTO t1(x, y) VALUES ('hello world', 1);
INSERT INTO fts(content) VALUES ('hello world');
SELECT DISTINCT fts.ctid, t1.x FROM fts JOIN t1 ON fts.content = t1.x WHERE fts.content = 'hello world' ORDER BY t1.y;
