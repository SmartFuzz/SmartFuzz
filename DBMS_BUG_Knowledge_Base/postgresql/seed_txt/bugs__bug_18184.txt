CREATE TABLE table4(table4_id serial PRIMARY KEY, i int NOT NULL, data JSONB);
INSERT INTO table4 (i, data) VALUES (1, '{"key": "value1"}'), (2, '{"key": "value2"}');
CREATE OR REPLACE FUNCTION function2(y table4) RETURNS text LANGUAGE sql IMMUTABLE AS $function$ SELECT CASE WHEN y.i % 2 = 0 THEN 'even' ELSE 'odd' END;
$function$;
SELECT function2(t4) FROM table4 t4 LEFT JOIN LATERAL (SELECT * FROM table3 WHERE a1 IS NOT NULL) AS tx3 ON (true) LIMIT 3;
SELECT * FROM table1 LEFT JOIN table2 ON table1.i = table2.i WHERE table1.i IS NOT NULL;
