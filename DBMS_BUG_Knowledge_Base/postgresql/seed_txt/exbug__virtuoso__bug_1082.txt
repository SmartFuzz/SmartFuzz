CREATE TABLE test_graph (id SERIAL PRIMARY KEY, graph_uri VARCHAR(255) NOT NULL, host VARCHAR(100));
INSERT INTO test_graph (id, graph_uri, host) VALUES (1, 'https://example.com/graph', 'example.com');
INSERT INTO test_graph (id, graph_uri, host) VALUES (2, 'https://example.com/graph', 'example.com');
SELECT * FROM test_graph WHERE graph_uri LIKE 'https://%';
UPDATE test_graph SET graph_uri = NULL WHERE id = 1;
