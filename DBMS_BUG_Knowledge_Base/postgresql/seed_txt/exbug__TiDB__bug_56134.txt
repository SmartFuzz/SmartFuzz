CREATE TABLE tc (id SERIAL PRIMARY KEY, name VARCHAR(64) NOT NULL, age INT DEFAULT 0);
INSERT INTO tc (name, age) VALUES ('Alice', 30), ('Bob', NULL);
CREATE TABLE td (id INT, name VARCHAR(64) NOT NULL, age INT DEFAULT 0);
INSERT INTO td (id, name, age) SELECT id, name, age FROM tc WHERE age IS NOT NULL;
SELECT * FROM tc;
