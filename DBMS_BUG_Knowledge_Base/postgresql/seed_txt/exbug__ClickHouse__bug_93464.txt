CREATE SCHEMA polaris_iceberg;
CREATE TABLE polaris_iceberg.teslasource_kafkav6_source_data (id INTEGER, name TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO polaris_iceberg.teslasource_kafkav6_source_data VALUES (1, 'test1', CURRENT_TIMESTAMP), (2, 'test2', CURRENT_TIMESTAMP);
SELECT * FROM polaris_iceberg.teslasource_kafkav6_source_data WHERE id IS NOT NULL;
SELECT table_name FROM information_schema.tables WHERE table_schema = 'polaris_iceberg';
