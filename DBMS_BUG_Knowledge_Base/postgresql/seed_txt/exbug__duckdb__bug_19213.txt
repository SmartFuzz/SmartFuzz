CREATE TABLE users (user_id INTEGER PRIMARY KEY, age INTEGER, status BOOLEAN);
INSERT INTO users VALUES (1, 25, true), (2, 30, false), (3, NULL, true);
CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DECIMAL(10, 2), FOREIGN KEY (user_id) REFERENCES users(user_id));
INSERT INTO orders VALUES (1, 1, 100.00), (2, 2, 150.50), (3, 3, 200.75);
SELECT COUNT(DISTINCT user_id) FROM orders WHERE user_id IN (SELECT users.user_id FROM users WHERE users.age < 30 AND users.status = true);
