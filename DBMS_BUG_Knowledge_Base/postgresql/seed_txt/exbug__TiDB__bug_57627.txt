CREATE TABLE chunks_2 ( id char(32) NOT NULL, embedding float8[] DEFAULT NULL, PRIMARY KEY (id) );
INSERT INTO chunks_2 (id, embedding) VALUES ('1', ARRAY[0.1, 0.2, 0.3]), ('2', ARRAY[0.4, 0.5, 0.6]), ('3', ARRAY[0.7, 0.8, 0.9]);
SELECT * FROM chunks_2 WHERE embedding IS NOT NULL ORDER BY (SELECT 1 - (embedding <=> ARRAY[0.1, 0.2, 0.3])::float) LIMIT 5;
CREATE TABLE documents ( document_id int NOT NULL, content text DEFAULT NULL, PRIMARY KEY (document_id) );
INSERT INTO documents (document_id, content) VALUES (1, 'Document 1'), (2, 'Document 2');
