CREATE TABLE t1(id SERIAL PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t1(id, name, created_at) VALUES (1, 'Alice', NULL);
BEGIN;
SET LOCAL timezone = 'UTC';
SELECT * FROM t1 WHERE id = (SELECT MAX(id) FROM t1) AND name IS NOT NULL;
