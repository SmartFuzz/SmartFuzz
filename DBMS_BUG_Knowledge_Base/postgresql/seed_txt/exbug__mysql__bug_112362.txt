CREATE TABLE c (c_id SERIAL PRIMARY KEY, value VARCHAR(100) NOT NULL UNIQUE);
CREATE TABLE d (d_id SERIAL PRIMARY KEY, value VARCHAR(100) NOT NULL);
INSERT INTO c (value) SELECT 'value_' || seq FROM generate_series(1, 1000) AS seq;
INSERT INTO d (value) SELECT 'value_' || seq FROM generate_series(1, 1000000) AS seq;
SELECT DISTINCT value FROM d WHERE value NOT IN (SELECT value FROM c);
