CREATE TABLE c(id SERIAL PRIMARY KEY, name VARCHAR(100) NOT NULL, age INT CHECK (age >= 0));
INSERT INTO c (name, age) VALUES('Alice', 30);
INSERT INTO c (name, age) VALUES('Bob', NULL);
CREATE VIEW d AS SELECT ROW_NUMBER() OVER (ORDER BY id) AS rn, name FROM c;
SELECT * FROM d WHERE rn = 1;
