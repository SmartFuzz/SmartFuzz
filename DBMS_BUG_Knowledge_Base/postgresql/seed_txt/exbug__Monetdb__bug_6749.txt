CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(50), age INT CHECK (age >= 0));
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
ALTER TABLE test_table ADD CONSTRAINT unique_name UNIQUE (name);
SELECT * FROM test_table WHERE age IS NULL OR age < 0;
ALTER TABLE test_table DROP COLUMN age;
