CREATE TABLE Users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT UNIQUE);
CREATE TABLE Comments (id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES Users(id) ON DELETE CASCADE, content TEXT);
INSERT INTO Users (name, email) VALUES ('Alice', 'alice@example.com');
INSERT INTO Comments (user_id, content) VALUES (1, 'This is a comment.');
DELETE FROM Users WHERE id = 1;
SELECT * FROM Comments;
