CREATE DATABASE test_db;
CREATE TABLE test_db.joinit (i SERIAL PRIMARY KEY, s VARCHAR(64) DEFAULT NULL, t TIME NOT NULL, g INT NOT NULL);
INSERT INTO test_db.joinit (s, t, g) VALUES (UUID_GENERATE_V4(), CURRENT_TIME, FLOOR(1 + RANDOM() * 60));
INSERT INTO test_db.joinit (s, t, g) SELECT UUID_GENERATE_V4(), CURRENT_TIME, FLOOR(1 + RANDOM() * 60) FROM test_db.joinit WHERE g < 30;
SELECT COUNT(*) FROM test_db.joinit WHERE g IS NULL OR g > 50;
