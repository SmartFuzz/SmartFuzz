CREATE TABLE IF NOT EXISTS employees (id SERIAL PRIMARY KEY, name VARCHAR, department_id INTEGER);
CREATE TABLE IF NOT EXISTS departments (id SERIAL PRIMARY KEY, department_name VARCHAR);
INSERT INTO departments (id, department_name) VALUES (1, 'HR'), (2, 'Engineering'), (3, 'Sales');
INSERT INTO employees (id, name, department_id) VALUES (1, 'Alice', 1), (2, 'Bob', 2), (3, 'Charlie', 3), (4, 'David', NULL);
SELECT * FROM employees LEFT JOIN departments ON employees.department_id = departments.id AND departments.department_name IS NOT NULL;
