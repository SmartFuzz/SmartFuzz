CREATE TABLE test_papers (id SERIAL PRIMARY KEY, title VARCHAR(255), type VARCHAR(255));
INSERT INTO test_papers (title, type) VALUES ('Paper A', 'http://example.com/test#Paper');
CREATE TABLE test_authors (id SERIAL PRIMARY KEY, name VARCHAR(255), paper_id INT REFERENCES test_papers(id));
INSERT INTO test_authors (name, paper_id) VALUES ('Author A', 1);
SELECT * FROM test_papers p JOIN test_authors a ON p.id = a.paper_id WHERE p.type = 'http://example.com/test#Paper';
