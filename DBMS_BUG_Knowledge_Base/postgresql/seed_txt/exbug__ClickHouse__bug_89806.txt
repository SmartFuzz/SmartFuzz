CREATE TABLE n4 (key BIGINT, value DOUBLE PRECISION);
INSERT INTO n4 SELECT generate_series(0, 9) AS number, number / 2.0 FROM generate_series(0, 9);
CREATE MATERIALIZED VIEW mv2 AS SELECT n1.key AS k, n1.value AS v1, n4.value AS v2 FROM n1 JOIN n4 ON n1.key = n4.key;
INSERT INTO n1 SELECT generate_series(0, 9) AS key, (generate_series(0, 9) + 50)::TEXT FROM generate_series(0, 9);
SELECT * FROM mv2 JOIN n3 ON mv2.k = n3.key WHERE mv2.v2 IS NOT NULL ORDER BY mv2.k, n3.key;
