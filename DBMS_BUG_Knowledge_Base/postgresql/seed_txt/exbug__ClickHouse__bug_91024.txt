CREATE TABLE lc_00906 (id SERIAL PRIMARY KEY, value TEXT);
INSERT INTO lc_00906 (value) SELECT '0123456789' FROM generate_series(1, 100000000);
CREATE TABLE lc_test (id SERIAL PRIMARY KEY, data INT);
INSERT INTO lc_test (data) SELECT CASE WHEN number % 2 = 0 THEN number ELSE NULL END FROM generate_series(1, 100);
SELECT count(*) FROM lc_test WHERE data IS NULL;
