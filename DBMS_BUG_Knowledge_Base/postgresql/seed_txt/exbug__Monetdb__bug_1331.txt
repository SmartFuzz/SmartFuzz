CREATE TABLE users(id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT CHECK (age >= 0));
CREATE TABLE orders(order_id SERIAL PRIMARY KEY, user_id INT REFERENCES users(id), amount DECIMAL(10, 2) NOT NULL);
INSERT INTO users (name, age) VALUES ('Alice', 30);
INSERT INTO orders (user_id, amount) VALUES (1, 99.99);
SELECT * FROM users;
DROP TABLE orders;
