CREATE TABLE test_table (id INTEGER, value INTEGER, z INTEGER);
INSERT INTO test_table VALUES (1, 10, 5), (2, 20, NULL), (3, 30, 15);
CREATE OR REPLACE FUNCTION demo(n INTEGER, z INTEGER) RETURNS TABLE(row INTEGER) AS $$ SELECT generate_series(0, n - 1) AS x, z AS row;
$$ LANGUAGE SQL;
SELECT * FROM demo(3, 0);
SELECT * FROM demo(2, NULL);
