CREATE TABLE test_table (id SERIAL PRIMARY KEY, name TEXT, value INTEGER CHECK (name ILIKE 'A%'));
INSERT INTO test_table (name, value) VALUES ('Alice', 10);
SELECT * FROM test_table WHERE name ILIKE 'A%';
UPDATE test_table SET value = NULL WHERE name ILIKE 'A%';
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL READ ONLY;
