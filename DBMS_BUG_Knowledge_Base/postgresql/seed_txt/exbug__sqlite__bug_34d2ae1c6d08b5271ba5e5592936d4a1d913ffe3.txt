CREATE TABLE fuzz_postgresql_r(a INTEGER, row_id INTEGER);
INSERT INTO fuzz_postgresql_r(a, row_id) VALUES (1, 100);
CREATE OR REPLACE FUNCTION fuzz_postgresql_r_ai() RETURNS TRIGGER AS $$ BEGIN INSERT INTO fuzz_postgresql_r(a, row_id) VALUES (NEW.a + 1, NEW.row_id + 1);
RETURN NEW;
END;
$$ LANGUAGE plpgsql;
