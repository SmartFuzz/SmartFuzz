DROP TABLE IF EXISTS t;
CREATE TABLE t (id SERIAL PRIMARY KEY, name VARCHAR(50), status VARCHAR(20));
INSERT INTO t (name, status) VALUES ('Alice', 'active'), ('Bob', 'inactive'), ('Charlie', 'pending');
SELECT COUNT(*) FROM t WHERE status = 'active';
ALTER TABLE t ALTER COLUMN status TYPE VARCHAR(20);
