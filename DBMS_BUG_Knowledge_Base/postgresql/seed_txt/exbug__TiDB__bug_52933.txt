CREATE TABLE t2 (c1 INT, c2 VARCHAR(50), c3 DECIMAL(10, 2), c4 DATE);
INSERT INTO t2 (c1, c2, c3, c4) VALUES (1, 'test', 10.50, '2024-01-01');
SELECT c1, VAR_SAMP(c3) OVER (PARTITION BY c1) FROM t2;
SELECT c1, c2, VAR_SAMP(c3) OVER (PARTITION BY c2) FROM t2;
SELECT c1, VAR_SAMP(c3) OVER (PARTITION BY c1 ORDER BY c4) FROM t2;
