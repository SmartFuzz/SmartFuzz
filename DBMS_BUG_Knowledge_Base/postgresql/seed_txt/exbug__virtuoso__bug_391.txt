CREATE TABLE vehicles (id SERIAL PRIMARY KEY, name VARCHAR(255), image_url VARCHAR(255));
INSERT INTO vehicles (name, image_url) VALUES ('Fiat 500', NULL), ('Smart Fortwo', NULL);
CREATE TABLE images (vehicle_id INT, image_url VARCHAR(255), FOREIGN KEY (vehicle_id) REFERENCES vehicles(id));
INSERT INTO images (vehicle_id, image_url) VALUES (1, 'http://example.com/fiat500.jpg');
SELECT v.name, i.image_url FROM vehicles v LEFT JOIN images i ON v.id = i.vehicle_id WHERE v.name IN ('Fiat 500', 'Smart Fortwo');
