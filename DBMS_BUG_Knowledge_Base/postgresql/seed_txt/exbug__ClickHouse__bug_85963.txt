CREATE TABLE test_table1 (id SERIAL PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP);
CREATE TABLE test_table2 (user_id SERIAL PRIMARY KEY, score FLOAT8, load_time TIMESTAMP);
INSERT INTO test_table1 (name, created_at) VALUES ('Alice', NOW()), ('Bob', NOW());
INSERT INTO test_table2 (user_id, score, load_time) VALUES (1, 99.5, NOW()), (2, 88.0, NOW());
SELECT t1.name, t2.score FROM test_table1 t1 JOIN test_table2 t2 ON t1.id = t2.user_id WHERE t2.score > 90;
