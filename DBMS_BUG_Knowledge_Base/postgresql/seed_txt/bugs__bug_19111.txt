CREATE TABLE part4 (key integer, val text, created_at timestamp DEFAULT CURRENT_TIMESTAMP);
INSERT INTO part4 VALUES (3, 'initial value');
CREATE TABLE part5 (key integer PRIMARY KEY, val text NOT NULL, updated_at timestamp DEFAULT CURRENT_TIMESTAMP);
INSERT INTO part5 (key, val) VALUES (4, NULL);
MERGE INTO part4 t USING (SELECT 3 as key, 'merge3' as val) s ON s.key = t.key WHEN NOT MATCHED THEN INSERT VALUES (s.key, s.val) WHEN MATCHED THEN UPDATE SET val = COALESCE(t.val, '') || ' updated';
