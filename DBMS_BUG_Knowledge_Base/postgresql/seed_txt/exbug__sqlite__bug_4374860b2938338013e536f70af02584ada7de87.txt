CREATE TABLE t1(a INTEGER, b TEXT, c REAL, d BYTEA);
CREATE TABLE t2(x SERIAL PRIMARY KEY, y TEXT NOT NULL, z REAL DEFAULT 0.0);
INSERT INTO t1(a, b, c, d) VALUES(1, 'test', 3.14, decode('0001', 'hex'));
CREATE VIEW v1 AS SELECT a, coalesce(b, 'default') AS b_default FROM t1 WHERE c > 2.0;
SELECT * FROM v1 JOIN t2 ON v1.a = t2.x WHERE t2.y LIKE 'test%' OR t2.z IS NULL;
