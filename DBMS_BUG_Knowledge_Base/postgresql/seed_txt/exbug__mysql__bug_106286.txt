CREATE TABLE test (id INT PRIMARY KEY, ts TIMESTAMP NULL);
INSERT INTO test (id, ts) VALUES (1, '2023-01-01 00:00:00');
CREATE OR REPLACE FUNCTION check_uniqueness_with_nulls() RETURNS TRIGGER AS $$ BEGIN IF EXISTS ( SELECT 1 FROM test WHERE test.id = NEW.id AND test.ts IS NOT DISTINCT FROM NEW.ts ) THEN RAISE EXCEPTION 'Duplicated values not allowed.';
END IF;
RETURN NEW;
END;
