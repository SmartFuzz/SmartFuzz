CREATE TABLE test_table (id SERIAL PRIMARY KEY, type VARCHAR(255), label VARCHAR(255), oType VARCHAR(255), p VARCHAR(255));
INSERT INTO test_table (type, label, oType, p) VALUES ('foo:Foo', 'example x', 'bar:Bar', 'foo:predicate1');
INSERT INTO test_table (type, label, oType, p) VALUES ('foo:Foo', 'example y', 'bar:Bar', 'foo:predicate2');
SELECT COUNT(DISTINCT id) AS count, type FROM test_table WHERE type = 'foo:Foo' AND (label ILIKE '%x%' OR label ILIKE '%y%') GROUP BY type ORDER BY count DESC;
SELECT COUNT(DISTINCT id) AS count, type FROM test_table WHERE p IN ('foo:predicate1', 'foo:predicate2') AND (oType = 'bar:Bar' OR label ILIKE '%x%' OR label ILIKE '%y%') GROUP BY type ORDER BY count DESC;
