CREATE EXTENSION IF NOT EXISTS postgres_fdw;
CREATE SERVER loopback FOREIGN DATA WRAPPER postgres_fdw OPTIONS (dbname 'current_database()', port '5432', async_capable 'true');
CREATE USER MAPPING FOR current_user SERVER loopback;
CREATE TABLE tab1 (a INT NOT NULL, b TEXT DEFAULT 'default');
INSERT INTO tab1 (a, b) VALUES (1, NULL), (2, 'text'), (3, 'another text');
