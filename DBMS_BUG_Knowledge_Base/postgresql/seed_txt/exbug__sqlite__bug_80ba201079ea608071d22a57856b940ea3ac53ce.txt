CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER, email TEXT UNIQUE);
INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');
INSERT INTO users (name, age, email) VALUES ('Bob', NULL, 'bob@example.com');
SELECT * FROM users WHERE id IN (SELECT id FROM users WHERE age > 25 OR email LIKE '%example.com');
SELECT * FROM users WHERE age IS NULL OR (name = 'Alice' AND id = 1);
