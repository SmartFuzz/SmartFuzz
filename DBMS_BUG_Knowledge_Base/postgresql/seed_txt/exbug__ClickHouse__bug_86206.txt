CREATE TABLE wikistat_iceberg_v2 (time TIMESTAMP, project TEXT, subproject TEXT, path TEXT, hits BIGINT, created_at DATE DEFAULT CURRENT_DATE);
INSERT INTO wikistat_iceberg_v2 (time, project, subproject, path, hits, created_at) VALUES (NOW(), 'project1', 'subproject1', '/path/to/resource', 100, CURRENT_DATE);
CREATE TABLE wikistat_temp (temp_time TIMESTAMP, temp_hits BIGINT);
INSERT INTO wikistat_temp (temp_time, temp_hits) SELECT time, hits FROM wikistat_iceberg_v2 WHERE hits > 50;
SELECT temp_time, SUM(temp_hits) AS total_hits FROM wikistat_temp GROUP BY temp_time HAVING total_hits > 100;
