CREATE TABLE test_rounding (id SERIAL PRIMARY KEY, value NUMERIC(10, 5));
INSERT INTO test_rounding (value) VALUES (123.4);
SELECT ROUND(value::NUMERIC, 5) FROM test_rounding WHERE id = 1;
SELECT ROUND(value::NUMERIC, 0) FROM test_rounding;
SELECT ROUND(NULL::NUMERIC, 5) AS rounded_value;
