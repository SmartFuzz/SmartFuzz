CREATE TABLE lightweight_test (ts TIMESTAMP, value TEXT, key TEXT, status INT) PARTITION BY (DATE_TRUNC('day', ts)) PRIMARY KEY (key);
CREATE TABLE keys (key TEXT, priority INT) PRIMARY KEY (key);
INSERT INTO lightweight_test VALUES (NOW(), 'val', 'key', NULL);
INSERT INTO keys VALUES ('key', 1), ('key2', 2);
UPDATE lightweight_test SET value = 'UPDATED-2' WHERE key IN (SELECT key FROM keys WHERE priority > 1);
SELECT * FROM lightweight_test;
