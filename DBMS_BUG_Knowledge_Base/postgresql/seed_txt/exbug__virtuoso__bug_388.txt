CREATE TABLE test_table (id SERIAL PRIMARY KEY, link VARCHAR(255), term VARCHAR(255));
INSERT INTO test_table (link, term) VALUES ('http://dbpedia.org/resource/Fiat_500', 'term1'), ('http://dbpedia.org/resource/Fiat_500', 'term2'), ('http://dbpedia.org/resource/Fiat_500', NULL);
CREATE TABLE label_table (link VARCHAR(255) PRIMARY KEY, label VARCHAR(255) NOT NULL);
INSERT INTO label_table (link, label) VALUES ('http://dbpedia.org/resource/Fiat_500', 'Fiat 500');
SELECT link, label, COUNT(term) AS count FROM test_table INNER JOIN label_table ON test_table.link = label_table.link GROUP BY link, label HAVING COUNT(term) > 0;
