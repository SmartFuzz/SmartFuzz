CREATE TABLE test_array_distinct (id INTEGER, values TEXT[]);
INSERT INTO test_array_distinct VALUES (1, ARRAY['A', 'A', 'B', NULL, NULL]), (2, ARRAY['B', 'C', 'C', 'A', NULL]);
SELECT ARRAY(SELECT DISTINCT unnest(values) FROM test_array_distinct WHERE id = 1) AS distinct_values;
SELECT ARRAY(SELECT DISTINCT unnest(ARRAY_CAT(values, ARRAY['D', 'D'])) FROM test_array_distinct WHERE id = 2) AS distinct_values;
SELECT ARRAY(SELECT DISTINCT unnest(ARRAY[1, 2, NULL, 2, NULL])) AS distinct_numbers;
