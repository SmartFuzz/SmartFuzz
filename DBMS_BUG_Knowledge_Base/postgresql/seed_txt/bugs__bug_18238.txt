CREATE TABLE t (a int, b text) PARTITION BY LIST (a);
CREATE TABLE tp1 PARTITION OF t FOR VALUES IN (1);
CREATE TABLE tp2 PARTITION OF t FOR VALUES IN (2);
INSERT INTO t VALUES (1, 'one');
MERGE INTO t USING (SELECT 1 AS a) AS st ON t.a = st.a WHEN MATCHED THEN UPDATE SET b = 'updated';
