DROP TABLE IF EXISTS demo_ts;
CREATE TABLE demo_ts (id SERIAL PRIMARY KEY, labels JSONB);
INSERT INTO demo_ts (id, labels) VALUES (1, '{"http.status_code":"200"}'), (2, '{"http.status_code":"503"}'), (3, '{"http.status_code":"404"}'), (4, '{"http.status_code":"208"}');
SELECT count(*) FROM demo_ts WHERE labels ? 'http.status_code' AND (labels->>'http.status_code')::int > 100;
SELECT id, (labels->>'http.status_code') AS sc FROM demo_ts WHERE labels->>'http.status_code' IS NOT NULL ORDER BY sc;
