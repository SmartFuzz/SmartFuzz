CREATE TABLE releases (version REAL PRIMARY KEY, CONSTRAINT version_comment CHECK (version IS NOT NULL)) WITH (OIDS=FALSE);
CREATE TABLE releases_v2 (version FLOAT, CONSTRAINT version_comment CHECK (version IS NOT NULL));
ALTER TABLE releases_v2 ADD CONSTRAINT version_v2_primary_key PRIMARY KEY (version);
CREATE TABLE releases_v3 (id SERIAL PRIMARY KEY, version REAL, description TEXT);
INSERT INTO releases_v3 (version, description) VALUES (1.0, 'Initial release');
SELECT * FROM releases_v3 WHERE version > 0.5;
