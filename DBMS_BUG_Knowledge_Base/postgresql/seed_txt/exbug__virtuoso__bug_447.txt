CREATE TABLE proteins (id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL, sequence TEXT, organism VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO proteins (name, sequence, organism) VALUES ('ProteinA', 'MKTAYIAKQRQISFVKSHFSRQD', 'Homo sapiens');
INSERT INTO proteins (name, sequence, organism) VALUES ('ProteinB', NULL, 'Mus musculus');
SELECT p.name, p.sequence FROM proteins p WHERE p.sequence IS NOT NULL AND LENGTH(p.sequence) > 10;
SELECT p1.name, p2.name FROM proteins p1 JOIN proteins p2 ON p1.id <> p2.id WHERE p1.organism = p2.organism;
