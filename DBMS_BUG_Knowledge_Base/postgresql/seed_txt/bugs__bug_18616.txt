CREATE TABLE t1(id SERIAL PRIMARY KEY, value TEXT NOT NULL);
INSERT INTO t1(value) SELECT 'test' || generate_series(1, 100);
CREATE TABLE t2(a INT, b VARCHAR(255), c TIMESTAMP DEFAULT NOW());
INSERT INTO t2(a, b) SELECT i, NULL FROM generate_series(1, 1000);
CREATE INDEX idx_t2_a ON t2 USING hash (a);
