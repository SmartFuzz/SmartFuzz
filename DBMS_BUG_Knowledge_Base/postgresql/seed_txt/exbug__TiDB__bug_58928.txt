CREATE TABLE t2 (id int, name varchar(50), created_at timestamp);
INSERT INTO t2 VALUES (1, 'test', NOW());
PREPARE stmt1 AS SELECT * FROM t2 WHERE created_at >= $1 OR id < $2;
EXECUTE stmt1 USING '2023-01-01', 2;
EXECUTE stmt1 USING 'abc', 2;
