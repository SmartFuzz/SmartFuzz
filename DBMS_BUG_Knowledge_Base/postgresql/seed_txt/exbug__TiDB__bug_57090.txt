CREATE TABLE user_data ( user_id SERIAL PRIMARY KEY, username VARCHAR(50) NOT NULL, email VARCHAR(100) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, preferences JSON DEFAULT NULL);
INSERT INTO user_data (username, email, preferences) VALUES ('john_doe', 'john@example.com', '{"theme": "dark", "notifications": true}');
CREATE TABLE transaction_log ( transaction_id SERIAL PRIMARY KEY, user_id INT NOT NULL, amount DECIMAL(10,2) NOT NULL, transaction_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES user_data(user_id));
INSERT INTO transaction_log (user_id, amount) VALUES (1, 150.75);
SELECT u.username, t.amount FROM user_data u JOIN transaction_log t ON u.user_id = t.user_id WHERE t.amount > 100.00 ORDER BY t.transaction_date DESC;
