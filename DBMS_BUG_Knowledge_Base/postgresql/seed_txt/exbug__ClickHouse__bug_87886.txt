CREATE TABLE test_improve_prewhere__fuzz_20 (id SERIAL PRIMARY KEY, name VARCHAR, created_at TIMESTAMP NULL, status VARCHAR NULL);
INSERT INTO test_improve_prewhere__fuzz_20 (id, name, created_at, status) SELECT number, CASE (number % 3) WHEN 1 THEN 'active' WHEN 2 THEN 'inactive' ELSE 'pending' END, to_timestamp('2025-08-01 12:00:00') + number * interval '1 second', CASE WHEN number % 2 = 0 THEN 'active' ELSE NULL END FROM generate_series(1, 10) AS number;
CREATE TABLE test_improve_prewhere__fuzz_21 (user_id SERIAL PRIMARY KEY, email VARCHAR, signup_date DATE NULL, last_login TIMESTAMP NULL);
INSERT INTO test_improve_prewhere__fuzz_21 (user_id, email, signup_date, last_login) SELECT number, concat('user', number, '@example.com'), to_date('2025-08-01') + number, to_timestamp('2025-09-01 08:00:00') + number * interval '1 second' FROM generate_series(1, 10) AS number;
SELECT * FROM test_improve_prewhere__fuzz_21 WHERE (signup_date IS NOT NULL) AND (last_login < to_timestamp('2025-09-05 00:00:00')) AND (email LIKE '%@example.com');
