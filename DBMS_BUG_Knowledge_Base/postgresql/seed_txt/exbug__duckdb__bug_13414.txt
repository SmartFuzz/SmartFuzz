CREATE TABLE employee (emp_id SERIAL PRIMARY KEY, emp_name VARCHAR, emp_age SMALLINT, emp_salary DECIMAL(10, 2));
CREATE UNIQUE INDEX emp_idx ON employee (emp_name, emp_age);
INSERT INTO employee (emp_id, emp_name, emp_age, emp_salary) VALUES (1, 'Alice', 30, 70000.00);
SELECT * FROM pg_indexes WHERE tablename = 'employee';
CREATE TABLE department (dept_id SERIAL PRIMARY KEY, dept_name VARCHAR, budget DECIMAL(15, 2));
