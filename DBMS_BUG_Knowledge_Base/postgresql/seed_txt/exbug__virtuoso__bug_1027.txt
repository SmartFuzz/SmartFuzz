CREATE TABLE building_objects (id SERIAL PRIMARY KEY, floor_count INT NOT NULL, description VARCHAR(255));
INSERT INTO building_objects (floor_count, description) VALUES (1, 'Building with one floor');
INSERT INTO building_objects (floor_count, description) VALUES (NULL, 'Building with unknown floors');
SELECT id, COALESCE(floor_count, 0) AS floor_count FROM building_objects WHERE floor_count IS NOT NULL;
SELECT DISTINCT floor_count FROM building_objects WHERE floor_count > 0 ORDER BY floor_count DESC;
