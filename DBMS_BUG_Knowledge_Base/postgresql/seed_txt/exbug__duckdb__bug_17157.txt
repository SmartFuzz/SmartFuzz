CREATE TABLE test_table (id SERIAL PRIMARY KEY, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, value) VALUES (1, 'test1'), (2, 'test2'), (3, NULL);
CREATE OR REPLACE FUNCTION example_macro(x VARCHAR) RETURNS VARCHAR AS $$ SELECT (SELECT value FROM test_table WHERE id = 1) AS result;
$$ LANGUAGE SQL;
SELECT example_macro(value) FROM test_table WHERE id = 2;
SELECT unnest(ARRAY[1, 2, 3]) * 2 AS doubled_values;
