CREATE TABLE test_dates (id SERIAL PRIMARY KEY, event_date INTEGER, description TEXT);
INSERT INTO test_dates (event_date, description) VALUES (2457754, 'Test event with start of day');
SELECT to_timestamp(event_date) AT TIME ZONE 'UTC' AS start_of_day FROM test_dates WHERE id = 1;
SELECT to_timestamp(event_date) AT TIME ZONE 'UTC' AS start_of_day, description FROM test_dates;
SELECT to_timestamp(NULL) AT TIME ZONE 'UTC' AS null_case;
