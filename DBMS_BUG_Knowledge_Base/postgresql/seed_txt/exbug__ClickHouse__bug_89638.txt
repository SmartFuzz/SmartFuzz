CREATE TABLE test_partitioned (id SERIAL PRIMARY KEY, dt TIMESTAMP, value FLOAT8);
INSERT INTO test_partitioned (id, dt, value) SELECT number, '2015-01-01'::DATE + (number % 3650) AS dt, random() FROM generate_series(1, 10000000) AS number;
CREATE TABLE test_non_partitioned (id SERIAL PRIMARY KEY, dt TIMESTAMP, value FLOAT8);
INSERT INTO test_non_partitioned (id, dt, value) SELECT number, '2015-01-01'::DATE + (number % 3650) AS dt, random() FROM generate_series(1, 10000000) AS number;
SELECT dt FROM test_partitioned WHERE value IS NOT NULL ORDER BY dt LIMIT 20;
