DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (id INT PRIMARY KEY, value VARCHAR(255), status BOOLEAN);
INSERT INTO t2 (id, value, status) VALUES (1, 'test', TRUE), (2, 'example', FALSE);
SELECT id FROM t2 WHERE INET_NTOA(CASE WHEN status = TRUE THEN id ELSE NULL END) IS NOT NULL;
SELECT value FROM t2 WHERE INET_NTOA(CASE WHEN id < 3 THEN id ELSE NULL END) IS NOT NULL;
