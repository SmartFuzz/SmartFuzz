CREATE TABLE v1 ( id SERIAL PRIMARY KEY, name VARCHAR(50), age INT CHECK (age >= 0) );
INSERT INTO v1 (name, age) VALUES ('Alice', 30), ('Bob', 25), ('Charlie', NULL);
CREATE TABLE v2 ( group_id INT, description VARCHAR(100), FOREIGN KEY (group_id) REFERENCES v1(id) );
INSERT INTO v2 (group_id, description) VALUES (1, 'Group A'), (2, 'Group B'), (3, 'Group C');
SELECT v1.id, v1.name, LAST_VALUE(v2.description) OVER (PARTITION BY v2.group_id ORDER BY v1.id ROWS BETWEEN CURRENT ROW AND UNBOUNDED FOLLOWING) FROM v1 LEFT JOIN v2 ON v1.id = v2.group_id;
