CREATE TABLE user (id BIGSERIAL PRIMARY KEY, data JSON NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO user (data) VALUES ('{"name": "John", "age": 30}');
INSERT INTO user (data) VALUES (NULL);
SELECT id, data FROM user WHERE data->>'name' = 'John';
UPDATE user SET data = jsonb_set(data::jsonb, '{age}', '31'::jsonb) WHERE id = 1;
