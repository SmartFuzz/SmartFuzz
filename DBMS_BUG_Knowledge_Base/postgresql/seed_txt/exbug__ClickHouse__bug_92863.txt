DROP DATABASE IF EXISTS bug_repro;
CREATE DATABASE bug_repro;
CREATE TABLE IF NOT EXISTS user_data (user_id SERIAL PRIMARY KEY, user_name VARCHAR, user_age SMALLINT, user_email VARCHAR, user_active BOOLEAN);
INSERT INTO user_data (user_id, user_name, user_age, user_email, user_active) VALUES (1, 'Alice', 30, 'alice@example.com', true);
SELECT COUNT(*) FROM user_data WHERE user_age > 25 AND user_active = true;
