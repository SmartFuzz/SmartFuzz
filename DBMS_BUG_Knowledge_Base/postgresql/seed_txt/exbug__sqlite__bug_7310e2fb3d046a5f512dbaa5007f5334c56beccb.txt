CREATE TABLE a(id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK(age >= 0));
CREATE TABLE b(id SERIAL PRIMARY KEY, a_id INTEGER REFERENCES a(id));
CREATE TABLE c(id SERIAL PRIMARY KEY, value REAL DEFAULT 0.0);
INSERT INTO a(name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', -1);
SELECT * FROM a JOIN b ON a.id = b.a_id WHERE (a.id, a.age) IN (SELECT id, age FROM a WHERE age IS NOT NULL);
