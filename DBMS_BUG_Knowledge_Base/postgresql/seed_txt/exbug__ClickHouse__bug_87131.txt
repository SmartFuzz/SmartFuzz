CREATE TABLE test2 (id BIGINT, value INT, arr TEXT[]) WITH (OIDS=FALSE);
INSERT INTO test2 SELECT generate_series(0, 9), CASE WHEN generate_series(0, 9) % 2 = 0 THEN generate_series(0, 9) ELSE NULL END, ARRAY['a', 'b', 'c'];
CREATE TABLE test3 AS SELECT * FROM test2;
SELECT arr, array_length(arr, 1) FROM test2 WHERE value IS NOT NULL ORDER BY value LIMIT 3;
SELECT id, value, arr[1] FROM test3 WHERE array_length(arr, 1) > 0;
