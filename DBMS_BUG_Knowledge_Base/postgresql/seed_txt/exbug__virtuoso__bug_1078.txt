CREATE TABLE test_classes (id SERIAL PRIMARY KEY, class_uri VARCHAR(255) NOT NULL, class_type VARCHAR(50));
INSERT INTO test_classes (class_uri, class_type) VALUES ('http://www.w3.org/2002/07/owl#Class', 'owl:Class');
INSERT INTO test_classes (class_uri, class_type) VALUES ('http://www.w3.org/2002/07/owl#Restriction', 'owl:Restriction');
SELECT DISTINCT class_uri FROM test_classes WHERE class_type = 'owl:Class' AND id IS NOT NULL;
SELECT DISTINCT class_uri FROM test_classes WHERE class_type = 'owl:Restriction' OR class_type IS NULL;
