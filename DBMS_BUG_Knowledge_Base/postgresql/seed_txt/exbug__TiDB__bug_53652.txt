CREATE TABLE t_parent (id SERIAL PRIMARY KEY, name VARCHAR(50) NOT NULL);
CREATE TABLE t_child (id SERIAL PRIMARY KEY, pid INT, age INT CHECK (age >= 0), FOREIGN KEY (pid) REFERENCES t_parent(id) ON DELETE CASCADE ON UPDATE CASCADE);
INSERT INTO t_parent (name) VALUES ('Parent1');
INSERT INTO t_child (pid, age) VALUES (1, 10);
SELECT * FROM t_parent;
SELECT * FROM t_child;
