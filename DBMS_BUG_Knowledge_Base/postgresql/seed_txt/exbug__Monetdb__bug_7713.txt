CREATE TABLE v1 ( id SERIAL PRIMARY KEY, value FLOAT, description TEXT );
INSERT INTO v1 (id, value, description) VALUES (DEFAULT, 1.5, 'test1'), (DEFAULT, NULL, 'test2'), (DEFAULT, 2.5, 'test3');
CREATE TABLE v2 ( category VARCHAR(50), amount FLOAT );
INSERT INTO v2 (category, amount) VALUES ('A', 10.0), ('B', 20.0), ('A', 30.0);
SELECT category, SUM(amount) FROM v2 GROUP BY category, amount + 1;
