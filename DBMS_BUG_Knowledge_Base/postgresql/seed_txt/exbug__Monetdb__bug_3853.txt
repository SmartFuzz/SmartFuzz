CREATE TABLE sales (id SERIAL PRIMARY KEY, product_name VARCHAR(50) NOT NULL, quantity INTEGER CHECK (quantity >= 0), price NUMERIC(10, 2) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO sales (product_name, quantity, price) VALUES ('Widget', 10, 19.99);
INSERT INTO sales (product_name, quantity, price) VALUES ('Gadget', 5, 29.99);
SELECT product_name, SUM(quantity) AS total_quantity FROM sales GROUP BY product_name HAVING SUM(quantity) > 0;
DELETE FROM sales WHERE created_at < NOW() - INTERVAL '30 days';
