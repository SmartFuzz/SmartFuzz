SET SESSION check_function_bodies = 'ON';
CREATE TABLE t2 (id SERIAL PRIMARY KEY, name VARCHAR(50) NOT NULL, age INT CHECK (age >= 0), salary DECIMAL(10,2) CHECK (salary > 0));
SET SESSION check_function_bodies = 'OFF';
ALTER TABLE t2 DROP CONSTRAINT age_check;
INSERT INTO t2 (name, age, salary) VALUES ('Alice', 30, 50000.00);
SELECT * FROM t2;
