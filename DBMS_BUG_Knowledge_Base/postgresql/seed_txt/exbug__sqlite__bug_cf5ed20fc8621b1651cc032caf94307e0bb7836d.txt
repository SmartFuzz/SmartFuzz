CREATE TABLE user ( id SERIAL PRIMARY KEY, active INTEGER NOT NULL, account INTEGER );
CREATE TABLE account ( id SERIAL PRIMARY KEY, name TEXT, balance REAL DEFAULT 0.0 );
INSERT INTO user (active, account) VALUES (1, 1);
INSERT INTO account (name, balance) VALUES ('test', 100.0);
SELECT user.id FROM user INNER JOIN account ON user.account = account.id WHERE user.active = 1;
