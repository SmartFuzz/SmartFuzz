CREATE TABLE t4 (id SERIAL PRIMARY KEY, name VARCHAR(100) NOT NULL, value DECIMAL(10,2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t4 (name, value) VALUES ('test1', 123.45);
INSERT INTO t4 (name, value) VALUES ('test2', NULL);
SELECT id, name, ROW_NUMBER() OVER (ORDER BY created_at DESC) AS row_num FROM t4 WHERE value IS NOT NULL;
SELECT id, name, ROW_NUMBER() OVER (PARTITION BY value ORDER BY created_at ASC) AS row_num FROM t4;
