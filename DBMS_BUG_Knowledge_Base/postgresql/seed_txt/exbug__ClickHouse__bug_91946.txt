CREATE TABLE users_extended (uid INT, name VARCHAR, age SMALLINT, deleted BOOLEAN, version BIGINT, salary DOUBLE PRECISION);
INSERT INTO users_extended VALUES (1, 'John', 33, FALSE, 1, 50000.0);
INSERT INTO users_extended VALUES (2, 'Ksenia', 48, FALSE, 1, 60000.0);
CREATE POLICY only_females ON users_extended FOR SELECT USING (deleted = FALSE AND name NOT LIKE 'J%');
SELECT AVG(salary) FROM users_extended WHERE age > 30 GROUP BY age;
