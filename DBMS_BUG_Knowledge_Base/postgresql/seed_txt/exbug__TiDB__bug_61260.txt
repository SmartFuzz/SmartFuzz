CREATE DATABASE testdb_fuzzing;
CREATE TABLE t (id SERIAL PRIMARY KEY, name VARCHAR(50), value DECIMAL(10,2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t (name, value) VALUES ('test1', 10.50), ('test2', NULL), ('test3', 20.00);
SELECT (name NOT BETWEEN 'a' AND (MD5(CONCAT(name, value)) || (ROW_NUMBER() OVER(ORDER BY id) + COALESCE(value, 0)))) FROM t GROUP BY name WITH ROLLUP;
SELECT version();
