CREATE TABLE tbl ( id SERIAL PRIMARY KEY, user_id INT DEFAULT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP );
CREATE TABLE fp_tbl ( id SERIAL PRIMARY KEY, user_id INT DEFAULT NULL, status VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive')) );
CREATE TABLE fp_pol ( id SERIAL PRIMARY KEY, lead_id INT DEFAULT NULL, amount DECIMAL(10,2) DEFAULT NULL );
INSERT INTO tbl (id, user_id) VALUES (1, 583532949), (2, 1342458479);
INSERT INTO fp_pol (lead_id, amount) VALUES (1, 100.00), (2, 200.00);
SELECT a.id, COUNT(*) AS n FROM tbl AS a LEFT JOIN fp_tbl AS f ON f.id = a.id LEFT JOIN fp_pol AS p ON p.lead_id = f.id WHERE a.id IN (1,2) AND p.id IS NULL GROUP BY a.id;
