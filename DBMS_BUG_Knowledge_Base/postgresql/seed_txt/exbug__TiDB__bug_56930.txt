CREATE TABLE articles (id SERIAL PRIMARY KEY, content TEXT, summary VARCHAR(255), UNIQUE (content, summary));
ALTER TABLE articles ALTER COLUMN content TYPE TEXT USING content::TEXT;
INSERT INTO articles (content, summary) VALUES ('This is a test article with a long content that exceeds normal limits.', 'Short summary');
SELECT * FROM articles WHERE CHAR_LENGTH(content) > 100;
CREATE TABLE comments (id SERIAL PRIMARY KEY, article_id INT, comment TEXT, FOREIGN KEY (article_id) REFERENCES articles(id));
