CREATE TABLE test_table (id SERIAL PRIMARY KEY, value DOUBLE PRECISION, description VARCHAR(255));
INSERT INTO test_table (value, description) VALUES (random(), 'First entry'), (random(), 'Second entry'), (random(), 'Third entry');
CREATE OR REPLACE FUNCTION random_value() RETURNS DOUBLE PRECISION AS $$ BEGIN RETURN random();
END;
$$ LANGUAGE plpgsql;
SELECT random_value();
