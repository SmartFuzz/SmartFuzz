CREATE TABLE test_table (id SERIAL PRIMARY KEY, text_column VARCHAR(255) NOT NULL, num_column INT DEFAULT NULL);
INSERT INTO test_table (text_column, num_column) VALUES ('example', 10), ('text', NULL), ('sample', 20);
SELECT SUM(DISTINCT SUBSTRING(text_column FROM 1 FOR 3)) FROM test_table;
SELECT SUM(SUBSTRING(text_column FROM 1 FOR 3)) FROM test_table;
SELECT SUM(DISTINCT SUBSTRING(text_column FROM 1 FOR 3)) + COALESCE(num_column, 0) FROM test_table;
