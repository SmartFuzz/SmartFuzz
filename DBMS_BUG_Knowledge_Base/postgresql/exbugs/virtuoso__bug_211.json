{
  "url": "https://github.com/openlink/virtuoso-opensource/issues/211",
  "title": "SPARQL abs function bug with variables",
  "description": "Hi,\nI just noticed a bug with the abs() function in Virtuoso 7.1 stable. Test query:\nselect abs(?test) as ?absoluteFromVariable\n       abs(-0.234) as ?absoluteFromValue\n       datatype(?test) as ?datatypeOfValue\nwhere\n{ { select (-0.234 as ?test) {}\n} }\n\nBasically both ?absoluteFromVariable and ?absoluteFromValue should be equal (0.234). They are in Virtuoso 6.1, but ?absoluteFromVariable is 0 in Virtuoso 7.1. I checked the other functions on numerics, they appear to be ok at first sight.",
  "date": "2014-07-03 15:54:49",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "virtuoso",
  "bug_features_seed": [
    "CREATE TABLE test_values (id SERIAL PRIMARY KEY, value FLOAT);",
    "INSERT INTO test_values (id, value) VALUES (1, -0.234), (2, NULL), (3, 0.0);",
    "SELECT ABS(value) AS absoluteValue FROM test_values WHERE value IS NOT NULL;",
    "WITH test AS (SELECT -0.234 AS test_value) SELECT ABS(test_value) AS absoluteFromVariable, ABS(-0.234) AS absoluteFromValue FROM test;",
    "SELECT ABS(COALESCE(value, 0)) AS absoluteValueWithNullHandling FROM test_values;"
  ],
  "key_bug_features": [
    "abs() function with variable returns incorrect result"
  ]
}