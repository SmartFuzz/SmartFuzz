{
  "url": "https://www.sqlite.org/src/tktview/73eba7ba5c",
  "title": "Table-valued function syntax allowed for ordinary tables and views",
  "description": "drh added on 2015-11-21 13:11:54:\nThe parser silently accepts and ignores table-valued function syntax on\nordinary tables and views which are not table-valued functions.  This can\nlead to memory leaks.  An example:\nCREATE TABLE t(x);\nSELECT * FROM t(0);\nWITH x AS (SELECT * FROM t) SELECT * FROM x(0);",
  "date": "2015-11-21 13:11:549.72 years ago",
  "vulnerable_version": "3.9.2",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users(id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK(age >= 0));",
    "INSERT INTO users(name, age) VALUES('Alice', 30);",
    "SELECT * FROM users;",
    "WITH temp AS (SELECT id FROM users WHERE age IS NULL) SELECT * FROM temp;",
    "CREATE TABLE orders(order_id SERIAL PRIMARY KEY, user_id INTEGER, amount REAL DEFAULT 0.0);"
  ],
  "key_bug_features": [
    "Table-valued function syntax on ordinary tables and views"
  ]
}