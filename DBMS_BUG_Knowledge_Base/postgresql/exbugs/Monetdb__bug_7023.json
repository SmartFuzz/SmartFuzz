{
  "url": "https://github.com/MonetDB/MonetDB/issues/7023",
  "title": "CREATE VIEW: SELECT: cannot use non GROUP BY column '%1' in query results without an aggregate function",
  "description": "CREATE VIEW doesn't seem to recognise that a generated column does have an aggregate function:\n```\ncreate table tst (k integer  not null, name char(20)  not null);\n-- this works\nselect min(k) from tst group by name;\n-- but putting it in a VIEW doesn't\ncreate view v1 as select max(k) from tst group by name;\nselect * from v1; -- returns SELECT: cannot use non GROUP BY column '%1' in query results without an aggregate function\n```\n\n**Expected behavior**\nNo error but 0 tuple.\n\n**Software versions**\n - MonetDB version number [v11.39.8 (hg id: d1c774305959)]\n - OS and version: [e.g. Ubuntu 20.04]\n - Mercurial source",
  "date": "2020-12-09T14:19:23Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE tst (k INTEGER NOT NULL, name CHAR(20) NOT NULL, age INTEGER DEFAULT NULL);",
    "INSERT INTO tst (k, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', NULL), (3, 'Charlie', 25);",
    "CREATE VIEW v1 AS SELECT MAX(k) AS max_k FROM tst GROUP BY name;",
    "CREATE VIEW v2 AS SELECT name, COUNT(*) AS count FROM tst GROUP BY name;",
    "SELECT * FROM v1;"
  ],
  "key_bug_features": [
    "CREATE VIEW with non GROUP BY column fails without aggregate"
  ]
}