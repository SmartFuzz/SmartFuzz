{
  "url": "https://github.com/MonetDB/MonetDB/issues/6502",
  "title": "Query with multiple limit clauses does not return anything",
  "description": "",
  "date": "2020-11-30T16:22:58Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE test_table (id SERIAL PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, amount DECIMAL(10, 2), status BOOLEAN);",
    "INSERT INTO test_table (name, amount, status) VALUES ('Test Product', 99.99, TRUE);",
    "INSERT INTO test_table (name, amount, status) VALUES ('Another Product', NULL, FALSE);",
    "SELECT name, amount FROM test_table WHERE amount IS NOT NULL ORDER BY amount DESC LIMIT 5;",
    "SELECT * FROM (SELECT * FROM test_table ORDER BY created_at DESC LIMIT 10) AS subquery LIMIT 3;"
  ],
  "key_bug_features": [
    "Query with multiple limit clauses fails to return results"
  ]
}