{
  "url": "https://github.com/oceanbase/oceanbase/issues/2178",
  "title": "[Bug]: When using the SPACE function, executing the query results in an Internal error.",
  "description": "<!-- A clear and concise description of bug. -->",
  "date": "2024-11-30T14:10:34Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "oceanbase",
  "bug_features_seed": [
    "CREATE TABLE test_space (id SERIAL PRIMARY KEY, name VARCHAR(50), value INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_space (id, name, value) VALUES (1, 'Test1', 10), (2, 'Test2', NULL), (3, 'Test3', 20);",
    "SELECT id, repeat(' ', value) AS space_value FROM test_space WHERE value IS NOT NULL;",
    "SELECT id FROM test_space WHERE CASE WHEN value IS NULL THEN repeat(' ', 1) ELSE repeat(' ', value) END;",
    "SELECT * FROM test_space WHERE name LIKE '%' || repeat(' ', 3) || '%';"
  ],
  "key_bug_features": [
    "Executing query with SPACE function causes internal error"
  ]
}