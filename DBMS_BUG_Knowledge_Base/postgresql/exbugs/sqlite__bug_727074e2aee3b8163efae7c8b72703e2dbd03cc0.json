{
  "url": "https://www.sqlite.org/src/tktview/727074e2ae",
  "title": "DBSTAT query computes incorrect result for aggregate column",
  "description": "mrigger added on 2020-01-04 11:22:43:\nConsider the following test case:\nCREATE VIRTUAL TABLE stat USING dbstat;\nSELECT * FROM stat WHERE stat.aggregate == NULL; -- unexpected: row is fetched\nI would not expect the row to be fetched, since the comparison should evaluate to FALSE:\nSELECT stat.aggregate == NULL FROM stat; -- NULL\nThis affects only the DBSTAT extension.",
  "date": "2020-01-04 11:22:435.60 years ago",
  "vulnerable_version": "3.30.0",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE stat (aggregate INTEGER);",
    "INSERT INTO stat (aggregate) VALUES (NULL);",
    "INSERT INTO stat (aggregate) VALUES (5);",
    "SELECT * FROM stat WHERE stat.aggregate IS NULL;",
    "SELECT stat.aggregate IS NULL FROM stat;"
  ],
  "key_bug_features": [
    "Comparison of aggregate column with NULL returns unexpected row"
  ]
}