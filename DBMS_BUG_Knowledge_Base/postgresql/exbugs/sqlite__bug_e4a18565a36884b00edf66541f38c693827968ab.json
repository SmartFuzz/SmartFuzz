{
  "url": "https://www.sqlite.org/src/tktview/e4a18565a3",
  "title": "Stale KeyInfo object used in shared-cache mode",
  "description": "drh added on 2014-12-05 04:45:14:\nThe sqlite3KeyInfoOfIndex() routine (here)\ncan return a stale KeyInfo object in shared cache mode.\nScenario:\nTwo open database connections (A and B) on the same cache\nThe shared pIdx object is holding a KeyInfo object for A\nConnection A closes.\nConnection C opens, and by (bad) luck is allocated\n     the same database connection pointer as A had.\nConnection C now tries to get a KeyInfo object for index pIdx but\n     is handed the stale one from A.  The KeyInfo object itself is ref-counted\n     and so it is still live.  But the KeyInfo points to CollSeq objects\n     within the old A connection that are now dead and have been freed.\nThis problem appears to have been introduced by check-in [c80e229dd9c1230ab]\non 2013-11-07, which first appeared in SQLite version 3.8.2.",
  "date": "2014-12-05 04:45:1410.68 years ago",
  "vulnerable_version": "3.8.5",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_table (id SERIAL PRIMARY KEY, name TEXT NOT NULL, value REAL DEFAULT 0.0);",
    "INSERT INTO test_table (name, value) VALUES ('Alice', 10.5);",
    "CREATE INDEX idx_value ON test_table(value);",
    "SELECT * FROM test_table WHERE value > (SELECT AVG(value) FROM test_table);",
    "DELETE FROM test_table WHERE id = (SELECT id FROM test_table WHERE name IS NULL LIMIT 1);"
  ],
  "key_bug_features": [
    "Stale KeyInfo object in shared cache mode"
  ]
}