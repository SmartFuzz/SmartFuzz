{
  "url": "https://www.sqlite.org/src/tktview/9f2eb3abac",
  "title": "A lookup on a primary key or unique index with more than 4 fields can cause a crash",
  "description": "dan added on 2013-08-29 08:50:43:\nThe following query causes a buffer overflow in the NGQP module. Any query with a single table in the FROM clause that may use more than four fields of a PRIMARY KEY or UNIQUE index is similarly vulnerable.\nCREATE TABLE t1(a,b,c,d,e, PRIMARY KEY(a,b,c,d,e));\n  SELECT * FROM t1 WHERE a=? AND b=? AND c=? AND d=? AND e=?;\n3.8.0 is the only version with this bug. Earlier versions are Ok.",
  "date": "2013-08-29 08:50:4311.95 years ago",
  "vulnerable_version": "3.8.0",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE t1(a INTEGER, b TEXT, c REAL, d BYTEA, e INTEGER, PRIMARY KEY(a, b, c, d, e));",
    "INSERT INTO t1(a, b, c, d, e) VALUES (1, 'test', 3.14, decode('001122', 'hex'), 5);",
    "SELECT * FROM t1 WHERE a=1 AND b='test' AND c=3.14 AND d=decode('001122', 'hex') AND e=5;",
    "CREATE TABLE t2(x INTEGER, y TEXT, z REAL, PRIMARY KEY(x, y, z));",
    "INSERT INTO t2(x, y, z) VALUES (10, 'example', 2.71);"
  ],
  "key_bug_features": [
    "Lookup on primary key with more than four fields"
  ]
}