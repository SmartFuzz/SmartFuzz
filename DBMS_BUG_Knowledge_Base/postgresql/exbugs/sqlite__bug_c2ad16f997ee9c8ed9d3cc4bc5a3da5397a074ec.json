{
  "url": "https://www.sqlite.org/src/tktview/c2ad16f997",
  "title": "Segfault on query involving deeply nested aggregate views",
  "description": "未找到User Comments标签",
  "date": "2012-05-21 03:21:0513.22 years ago",
  "vulnerable_version": "3.7.12",
  "poc_type": "sql_text",
  "vulnerable_database": "Postgresql",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, age INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "CREATE TABLE comments (id SERIAL PRIMARY KEY, user_id INTEGER, content TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(user_id) REFERENCES users(id));",
    "INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);",
    "INSERT INTO comments (user_id, content) VALUES (1, 'First comment'), (2, 'Second comment'), (1, NULL), (3, 'Third comment');",
    "SELECT user_id, COUNT(content) AS comment_count FROM comments GROUP BY user_id HAVING COUNT(content) > 0 ORDER BY comment_count DESC;"
  ],
  "key_bug_features": [
    "query involving deeply nested aggregate views"
  ]
}