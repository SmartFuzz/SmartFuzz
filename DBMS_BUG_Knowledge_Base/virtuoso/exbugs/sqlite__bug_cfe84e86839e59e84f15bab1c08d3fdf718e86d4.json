{
  "url": "https://www.sqlite.org/src/tktview/cfe84e8683",
  "title": "The \".schema\" command outputs nested VIEWS in the wrong order",
  "description": "未找到User Comments标签",
  "date": "2012-05-21 03:01:3113.22 years ago",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE parent_view (id INTEGER, name NVARCHAR(255) NOT NULL, PRIMARY KEY (id));",
    "CREATE TABLE child_view (id INTEGER, parent_id INTEGER, value INTEGER, PRIMARY KEY (id), FOREIGN KEY (parent_id) REFERENCES parent_view(id));",
    "INSERT INTO parent_view (id, name) VALUES (1, 'Parent 1'), (2, 'Parent 2');",
    "INSERT INTO child_view (id, parent_id, value) VALUES (1, 1, 10), (2, 1, 20), (3, 2, NULL);",
    "CREATE VIEW nested_view AS SELECT p.name, c.value FROM parent_view p LEFT JOIN child_view c ON p.id = c.parent_id ORDER BY p.id, c.value DESC;"
  ],
  "key_bug_features": [
    ".schema command outputs nested VIEWS incorrectly"
  ]
}