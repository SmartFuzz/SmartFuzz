{
  "url": "https://github.com/MonetDB/MonetDB/issues/2807",
  "title": "wrong results for group by queries in the presence of primary keys",
  "description": "",
  "date": "2020-11-30T11:42:17Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "DROP TABLE IF EXISTS facts;",
    "CREATE TABLE facts (a_id bigint, b_id bigint, value double precision);",
    "INSERT INTO facts (a_id, b_id, value) VALUES(1, 1, 10.5);",
    "INSERT INTO facts (a_id, b_id, value) VALUES(2, 1, 20.5);",
    "INSERT INTO facts (a_id, b_id, value) VALUES(3, 2, 30.5);",
    "SELECT b.name, c.id, c.name, SUM(f.value) FROM facts f LEFT JOIN a ON f.a_id = a.id LEFT JOIN b ON f.b_id = b.id LEFT JOIN c ON a.c_id = c.id GROUP BY b.name, c.id, c.name;"
  ],
  "key_bug_features": [
    "group by queries with primary keys in joins"
  ]
}