{
  "url": "https://www.sqlite.org/src/tktview/c997b11c4d",
  "title": "ORDER BY clause ignored in 3-way join query",
  "description": "未找到User Comments标签",
  "date": "2013-01-09 09:39:2212.59 years ago",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users (id INTEGER IDENTITY PRIMARY KEY, name NVARCHAR(255) NOT NULL, age INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "CREATE TABLE posts (id INTEGER IDENTITY PRIMARY KEY, user_id INTEGER, content NVARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(user_id) REFERENCES users(id));",
    "CREATE TABLE comments (id INTEGER IDENTITY PRIMARY KEY, post_id INTEGER, user_id INTEGER, comment NVARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(post_id) REFERENCES posts(id), FOREIGN KEY(user_id) REFERENCES users(id));",
    "INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);",
    "SELECT u.name, COUNT(c.id) AS comment_count FROM users u LEFT JOIN posts p ON u.id = p.user_id LEFT JOIN comments c ON p.id = c.post_id GROUP BY u.id ORDER BY comment_count DESC;"
  ],
  "key_bug_features": [
    "ORDER BY clause in 3-way join query"
  ]
}