{
  "url": "https://github.com/duckdb/duckdb/issues/20306",
  "title": "Potential Unexpected Results of `CONCAT(NULL)`",
  "description": "### What happens?\n\nWhen using `CONCAT(NULL)` on a NULL literal, DuckDB returns a `NULL` value in output, but evaluating `CONCAT(NULL) IS NULL` returns false. This might be related to https://github.com/duckdb/duckdb/issues/19666.\n\n### To Reproduce\n\n```sql\nSELECT CONCAT(NULL); -- NULL\nSELECT CONCAT(NULL) IS NULL; -- false\n```\n\n### OS:\n\nUbuntu 22.04\n\n### DuckDB Version:\n\nDuckDB v1.5.0-dev5021 (Development Version, 041f4cac68)\n\n### DuckDB Client:\n\nCLI\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nSuyang Zhong\n\n### Affiliation:\n\nNUS\n\n### Did you include all relevant configuration (e.g., CPU architecture, Linux distribution) to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all code required to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all relevant data sets for reproducing the issue?\n\nNot applicable - the reproduction does not require a data set",
  "date": "2025-12-25 07:37:26",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER, name VARCHAR, value FLOAT);",
    "INSERT INTO test_table (id, name, value) VALUES (1, 'test', NULL);",
    "SELECT '' || NULL AS concat_result;",
    "SELECT CASE WHEN NULL IS NULL THEN TRUE ELSE FALSE END AS is_null_result;",
    "SELECT id, name FROM test_table WHERE value IS NULL;"
  ],
  "key_bug_features": [
    "CONCAT(NULL) returns NULL but evaluates to false"
  ]
}