{
  "url": "https://www.sqlite.org/src/tktview/bb1977f6e9",
  "title": "Concatenation can create very invalid strings",
  "description": "未找到User Comments标签",
  "date": "2010-04-19 16:58:3215.31 years ago",
  "vulnerable_version": "3.6.23.1",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users (id INTEGER, name VARCHAR(255) NOT NULL, age INTEGER, email VARCHAR(255) UNIQUE, PRIMARY KEY (id));",
    "INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');",
    "INSERT INTO users (name, age, email) VALUES ('Bob', NULL, 'bob@example.com');",
    "SELECT CONCAT(name, ' is ', age, ' years old') AS user_info FROM users WHERE age IS NOT NULL;",
    "SELECT CONCAT(name, ' - ', COALESCE(email, 'no email')) AS user_contact FROM users;"
  ],
  "key_bug_features": [
    "Concatenation of strings leads to invalid outputs"
  ]
}