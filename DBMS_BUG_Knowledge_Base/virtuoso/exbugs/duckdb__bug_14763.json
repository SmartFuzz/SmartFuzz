{
  "url": "https://github.com/duckdb/duckdb/issues/14763",
  "title": "Not working: \"A macro can query a Common Table Expression (CTE) / WITH clause that is in scope during execution\"",
  "description": "### What happens?\n\nAccording to the article at https://duckdb.org/2024/03/01/sql-gymnastics.html:\r\n\r\n\"A macro can query a Common Table Expression (CTE) / WITH clause that\r\nis in scope during execution\"\r\n\r\nThis does not seem to work (any longer) as can be seen by running\r\nthe program below; doing so produces:\r\n```\r\n┌────────────────┐\r\n│  \"version\"()   │\r\n│    varchar     │\r\n├────────────────┤\r\n│ v1.1.4-dev1693 │\r\n└────────────────┘\r\nCatalog Error: Table with name zz does not exist!\r\n```\r\n\n\n### To Reproduce\n\n```\r\nselect version();\r\n\r\n# The i-th row as a list\r\ncreate or replace function ijTable2Row(m) as (\r\n  select array_agg(value order by j)\r\n  from zz\r\n  where i = m\r\n);\r\n\r\n# Pretty-print the mxn matrix stored as an ij-table named zz\r\ncreate or replace function ijTable_pp(m,n) as table (\r\n  select ijTable2Row(i) as rowi\r\n  from range(1, m+1) t(i)\r\n  order by i\r\n);\r\n\r\nwith zz as (select 1 as i, 1 as j, 1 as value)\r\nfrom ijTable_pp(1,1);\r\n```\r\n\n\n### OS:\n\nMacOS\n\n### DuckDB Version:\n\nv1.1.4-dev1693\n\n### DuckDB Client:\n\nCLI\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nPeter Koppstein\n\n### Affiliation:\n\nPrinceton University\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a nightly build\n\n### Did you include all relevant data sets for reproducing the issue?\n\nNot applicable - the reproduction does not require a data set\n\n### Did you include all code required to reproduce the issue?\n\n- [X] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [X] Yes, I have",
  "date": "2024-11-09 03:29:29",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR, value FLOAT, created_at TIMESTAMP DEFAULT NOW());",
    "INSERT INTO test_table (id, name, value) VALUES (1, 'Test A', 10.5), (2, 'Test B', NULL), (3, 'Test C', 20.0);",
    "SELECT COUNT(*) FROM (SELECT id, name, value FROM test_table WHERE value IS NOT NULL) AS cte;",
    "CREATE OR REPLACE FUNCTION get_value(id INTEGER) RETURNS FLOAT AS SELECT value FROM test_table WHERE id = id;",
    "SELECT get_value(2) AS value_result;"
  ],
  "key_bug_features": [
    "Macro querying CTE not functioning during execution"
  ]
}