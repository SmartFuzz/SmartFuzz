{
  "url": "https://github.com/MonetDB/MonetDB/issues/6600",
  "title": "Sqlitelogictest queries fail to execute",
  "description": "",
  "date": "2020-11-30T16:28:17Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, value INTEGER, description VARCHAR(255));",
    "INSERT INTO test_table (id, value, description) VALUES (1, NULL, 'Test entry 1');",
    "INSERT INTO test_table (id, value, description) VALUES (2, 100, 'Test entry 2');",
    "SELECT id, CASE WHEN value IS NULL OR value = 0 THEN NULL ELSE value * 10 END AS adjusted_value FROM test_table WHERE description IS NOT NULL;",
    "SELECT -5 * CASE WHEN (SELECT COUNT(*) FROM test_table) = 0 THEN NULL ELSE (SELECT COUNT(*) FROM test_table) END + 20 AS result;"
  ],
  "key_bug_features": [
    "Queries with NULLIF and complex arithmetic fail"
  ]
}