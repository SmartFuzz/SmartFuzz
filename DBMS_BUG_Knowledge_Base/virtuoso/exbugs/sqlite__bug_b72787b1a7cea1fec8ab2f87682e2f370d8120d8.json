{
  "url": "https://www.sqlite.org/src/tktview/b72787b1a7",
  "title": "Expiry mechanism causes incorrect collateral damage",
  "description": "未找到User Comments标签",
  "date": "2011-02-20 09:28:0814.47 years ago",
  "vulnerable_version": "3.7.5",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users (id INTEGER IDENTITY PRIMARY KEY, username NVARCHAR(255) NOT NULL, expiry_date TIMESTAMP, is_active BOOLEAN DEFAULT TRUE);",
    "INSERT INTO users (username, expiry_date, is_active) VALUES ('user1', TIMESTAMP '2023-10-01 00:00:00', TRUE);",
    "INSERT INTO users (username, expiry_date, is_active) VALUES ('user2', NULL, FALSE);",
    "UPDATE users SET is_active = FALSE WHERE expiry_date < NOW();",
    "SELECT username FROM users WHERE is_active = TRUE OR (expiry_date IS NULL AND is_active = TRUE);"
  ],
  "key_bug_features": [
    "Expiry mechanism causing collateral damage"
  ]
}