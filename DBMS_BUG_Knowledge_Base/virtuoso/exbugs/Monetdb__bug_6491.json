{
  "url": "https://github.com/MonetDB/MonetDB/issues/6491",
  "title": "SELECT IN returns NULL instead of false when not found",
  "description": "",
  "date": "2020-11-30T15:57:22Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE test_table(id INT, name VARCHAR(50), value FLOAT, is_active BOOLEAN);",
    "INSERT INTO test_table (id, name, value, is_active) VALUES (1, 'Alice', 10.5, TRUE), (2, 'Bob', 20.0, FALSE), (3, 'Charlie', 30.0, TRUE);",
    "SELECT CASE WHEN 4 IN (SELECT id FROM test_table WHERE is_active = TRUE) THEN TRUE ELSE FALSE END AS result;",
    "SELECT CASE WHEN 'Alice' IN (SELECT name FROM test_table WHERE value > 15.0) THEN TRUE ELSE FALSE END AS result;",
    "SELECT CASE WHEN NULL IN (SELECT value FROM test_table WHERE id = 2) THEN TRUE ELSE FALSE END AS result;"
  ],
  "key_bug_features": [
    "SELECT IN returns NULL for non-existent values"
  ]
}