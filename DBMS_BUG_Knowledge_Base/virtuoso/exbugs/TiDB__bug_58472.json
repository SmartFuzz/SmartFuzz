{
  "url": "https://github.com/pingcap/tidb/issues/58472",
  "title": "insert `null` into not-null virtual generated column leads to wrong index record",
  "description": "## Bug Report\n\nPlease answer these questions before submitting your issue. Thanks!\n\n### 1. Minimal reproduce step (Required)\n\nThe following problem only exists in **virtual generated** columns.\n\n```sql\ndrop table t3;\ncreate table t3(\n    id int primary key,\n    c1 varchar(16) default null,\n    c2 varchar(16) GENERATED ALWAYS AS (concat(c1, c1)) VIRTUAL NOT NULL,\n    KEY idx (c2)\n);\n\ninsert into t3(id, c1) values(1, \"aaaa\");\ninsert ignore into t3 set id = 1, c1 = \"bbbb\" on duplicate key update id = 2, c1 = null;\ninsert into t3 set id = 2, c1 = \"cccc\" on duplicate key update c1 = \"dddd\";\n```\n\n<!-- a step by step guide for reproducing the bug. -->\n\n### 2. What did you expect to see? (Required)\n\nNo error, or get the error `ERROR 1048 (23000): Column 'c2' cannot be null`.\n\nIf we follow the MySQL implement, the result should be the latter one.\n\n### 3. What did you see instead (Required)\n\nWe get none of the expected results.\n\n```\nERROR 8141 (HY000): assertion failed: key: 7480000000000000715f69800000000000000100038000000000000002, assertion: Exist, start_ts: 454804733357457409, existing start ts: 0, existing commit ts: 0\n```\n\n### 4. What is your TiDB version? (Required)\n\n<!-- Paste the output of SELECT tidb_version() -->\n\n",
  "date": "2024-12-23T08:27:13Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "TiDB",
  "bug_features_seed": [
    "DROP TABLE IF EXISTS t4;",
    "CREATE TABLE t4(id INT PRIMARY KEY, c1 VARCHAR(16) DEFAULT NULL, c2 VARCHAR(16) AS (CONCAT(c1, 'test')) NOT NULL, INDEX idx (c2));",
    "INSERT INTO t4(id, c1) VALUES(1, 'hello');",
    "INSERT INTO t4(id, c1) VALUES(1, NULL) WHERE NOT EXISTS (SELECT 1 FROM t4 WHERE id = 1) OR (UPDATE t4 SET c1 = 'world' WHERE id = 1);",
    "INSERT INTO t4(id, c1) VALUES(2, 'foo') WHERE NOT EXISTS (SELECT 1 FROM t4 WHERE id = 2) OR (UPDATE t4 SET c1 = NULL WHERE id = 2);"
  ],
  "key_bug_features": [
    "insert null into not-null virtual generated column"
  ]
}