{
  "url": "https://www.sqlite.org/src/tktview/edb497982c",
  "title": "FTS n-character prefix query returns incorrect results when using an n+1 character prefix index.",
  "description": "未找到User Comments标签",
  "date": "2012-01-25 19:04:4313.54 years ago",
  "vulnerable_version": "3.7.10",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE fts_table (content NVARCHAR(255));",
    "INSERT INTO fts_table(content) VALUES('apple'), ('application'), ('appetite'), ('banana');",
    "CREATE INDEX idx_content_prefix ON fts_table(content);",
    "SELECT * FROM fts_table WHERE CONTAINS(content, 'app*');",
    "SELECT * FROM fts_table WHERE CONTAINS(content, 'appl*');"
  ],
  "key_bug_features": [
    "n-character prefix query with n+1 character index"
  ]
}