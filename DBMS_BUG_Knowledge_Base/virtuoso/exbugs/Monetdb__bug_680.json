{
  "url": "https://github.com/MonetDB/MonetDB/issues/680",
  "title": "SQL: select interval '1' second * 2",
  "description": "",
  "date": "2020-11-30T08:48:37Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "virtuoso",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE test_intervals (id INTEGER PRIMARY KEY, duration INTERVAL);",
    "INSERT INTO test_intervals (id, duration) VALUES (1, INTERVAL '1' SECOND);",
    "SELECT duration * 2 FROM test_intervals WHERE id = 1;",
    "SELECT INTERVAL '1' SECOND + INTERVAL '1' SECOND AS total_duration;",
    "SELECT (SELECT duration FROM test_intervals WHERE id = 1) * 2 AS multiplied_duration;"
  ],
  "key_bug_features": [
    "Multiplying interval by a numeric value"
  ]
}