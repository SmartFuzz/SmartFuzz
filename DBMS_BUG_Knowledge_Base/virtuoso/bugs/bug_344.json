{
  "url": "https://github.com/openlink/virtuoso-opensource/issues/344",
  "title": "STRBEFORE does not return language tag",
  "vulnerable_database": "Virtuoso",
  "date": "2015-02-13 12:52:42",
  "vulnerable_version": "",
  "description": "Query:\nSELECT (STRBEFORE(?S, \" string\") AS ?B) WHERE\n{\n    BIND(\"test string\"@en AS ?S)\n}\n\nResult:\ntest\n\nBut the correct result should be \"test\"@en",
  "poc": [
    "SELECT (STRBEFORE(?S, \" string\") AS ?B) WHERE\n{\n    BIND(\"test string\"@en AS ?S)\n}"
  ],
  "key_bug_features": [
    "STRBEFORE fails with language-tagged strings"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_strings (id INT PRIMARY KEY, lang_string STRING);",
    "INSERT INTO test_strings (id, lang_string) VALUES (1, 'example string'@en);",
    "INSERT INTO test_strings (id, lang_string) VALUES (2, 'test string'@fr);",
    "SELECT (STRBEFORE(lang_string, ' string') AS result) FROM test_strings WHERE id = 1;",
    "SELECT (STRBEFORE(lang_string, ' string') AS result) FROM test_strings WHERE id = 2;"
  ]
}