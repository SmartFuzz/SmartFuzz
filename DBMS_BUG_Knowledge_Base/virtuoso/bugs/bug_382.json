{
  "url": "https://github.com/openlink/virtuoso-opensource/issues/382",
  "title": "abs() casts results to xsd:integer",
  "vulnerable_database": "Virtuoso",
  "date": "2015-04-28 08:46:22",
  "vulnerable_version": "",
  "description": "The following query is casting xsd:double values to xsd:integer on the http://dbpedia.org/sparql endpoint (Virtuoso 7.20).\nprefix dbo: <http://dbpedia.org/ontology/>\n\nselect ?a ?l (abs(?l) as ?abs) {\n\n  ?a a dbo:Automobile; dbo:length ?l\n\n} limit 10\n\nSame behaviour with xsd:decimal on a private endpoint.\nVery likely related to #211.",
  "poc": [
    "prefix dbo: <http://dbpedia.org/ontology/>\n\nselect ?a ?l (abs(?l) as ?abs) {\n\n  ?a a dbo:Automobile; dbo:length ?l\n\n} limit 10"
  ],
  "key_bug_features": [
    "abs() function casts xsd:double to xsd:integer"
  ],
  "bug_features_seed": [
    "CREATE TABLE automobiles (id xsd:integer PRIMARY KEY, length xsd:double NOT NULL);",
    "INSERT INTO automobiles (id, length) VALUES (1, 4.5), (2, -3.2), (3, NULL), (4, 0.0);",
    "CREATE TABLE manufacturers (id xsd:integer PRIMARY KEY, name xsd:string NOT NULL);",
    "INSERT INTO manufacturers (id, name) VALUES (1, 'Toyota'), (2, 'Ford');",
    "SELECT a.id, a.length, (abs(a.length) AS abs_length) FROM automobiles a JOIN manufacturers m ON a.id = m.id WHERE a.length IS NOT NULL LIMIT 10;"
  ]
}