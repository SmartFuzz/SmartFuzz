CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test_table (name) VALUES ('Test Entry 1');
INSERT INTO test_table (name) VALUES (NULL);
SELECT id, name, STR_TO_DATE(created_at, '%Y-%m-%d %H:%M:%S') AS formatted_date FROM test_table WHERE created_at IS NOT NULL;
UPDATE test_table SET created_at = NOW() WHERE id = 1;
