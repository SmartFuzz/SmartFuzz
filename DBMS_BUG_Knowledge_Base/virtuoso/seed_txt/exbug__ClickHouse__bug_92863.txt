DROP DATABASE IF EXISTS bug_repro;
CREATE DATABASE bug_repro;
CREATE TABLE user_data (user_id INTEGER, user_name VARCHAR(255), user_age INTEGER, user_email VARCHAR(255), user_active BOOLEAN);
INSERT INTO user_data VALUES (1, 'Alice', 30, 'alice@example.com', TRUE);
SELECT COUNT(*) FROM user_data WHERE user_age > 25 AND user_active = TRUE;
