CREATE TABLE test_table (id INTEGER IDENTITY PRIMARY KEY, name NVARCHAR(255), age INTEGER, score FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, age, score) VALUES ('Alice', 30, 85.5);
INSERT INTO test_table (name, age, score) VALUES ('Bob', NULL, 90.0);
INSERT INTO test_table (name, age, score) VALUES ('Charlie', 25, NULL);
SELECT DISTINCT name FROM (SELECT name FROM test_table ORDER BY score DESC) AS subquery;
