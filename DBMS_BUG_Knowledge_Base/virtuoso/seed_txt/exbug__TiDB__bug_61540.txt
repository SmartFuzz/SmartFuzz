CREATE TABLE t2 ( x int, y varchar(50), z datetime, PRIMARY KEY (x, y), INDEX idx_z (z));
INSERT INTO t2 (x, y, z) VALUES (1, 'test', NOW()), (2, 'example', NOW()), (3, 'sample', NOW());
UPDATE t2 SET y = NULL WHERE x = 2;
SELECT * FROM t2 WHERE x = 1 AND z > TIMESTAMP '2023-01-01 00:00:00' ORDER BY y LIMIT 5;
ANALYZE TABLE t2;
