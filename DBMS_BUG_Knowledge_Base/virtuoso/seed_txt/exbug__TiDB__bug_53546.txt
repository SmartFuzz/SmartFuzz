CREATE TABLE t2 (c1 INTEGER, c2 FLOAT, c3 VARCHAR(10));
INSERT INTO t2 VALUES (NULL, 1.5, 'test');
SELECT TRUNCATE(c2, 1) AS truncated_value, CASE WHEN TRUNCATE(c2, 1) IS NULL THEN 0 ELSE 1 END AS is_not_null FROM t2;
CREATE TABLE t3 (c1 INTEGER NOT NULL, c2 INTEGER DEFAULT NULL);
INSERT INTO t3 VALUES (1, NULL);
