DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INTEGER NOT NULL, value DECIMAL(10, 2) NULL, category VARCHAR(50) NOT NULL);
INSERT INTO test_table (id, value, category) VALUES (1, 10.50, 'A'), (2, NULL, 'B'), (3, 20.00, 'A'), (4, 30.00, 'C');
SELECT FIRST(value) AS first_val FROM (SELECT value, category, id FROM test_table ORDER BY category, id) AS subquery GROUP BY category;
SELECT FIRST(value) AS first_val FROM (SELECT value, category, id FROM test_table ORDER BY category, id) AS subquery GROUP BY category;
