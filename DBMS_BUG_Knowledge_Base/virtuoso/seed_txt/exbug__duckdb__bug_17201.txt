CREATE TABLE test_table (id INTEGER PRIMARY KEY, value VARCHAR, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test_table (id, value) VALUES (1, 'test1'), (2, 'test2'), (3, NULL);
SELECT n FROM (SELECT 1 AS n UNION SELECT n + 1 FROM (SELECT 1 AS n UNION SELECT 2 UNION SELECT 3 UNION SELECT 4) AS seq WHERE n < 4) AS seq;
SELECT t1.id, t1.value, t2.value FROM test_table t1 LEFT JOIN test_table t2 ON t1.id = t2.id + 1;
SELECT COALESCE(MAX(value), 'default') FROM test_table;
