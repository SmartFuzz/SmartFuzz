DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INTEGER, name VARCHAR(5), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) SELECT number, SUBSTRING(CONVERT(VARCHAR, NEWID()), 1, 5), RAND() FROM (SELECT ROW_NUMBER() OVER() AS number FROM sysibm.sysdummy1) AS numbers WHERE number <= 100;
SELECT * FROM test_table ORDER BY name, LENGTH(name);
DROP TABLE test_table;
