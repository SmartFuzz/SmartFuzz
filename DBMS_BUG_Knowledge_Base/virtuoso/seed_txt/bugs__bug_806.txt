CREATE TABLE geospatial_data (id INT PRIMARY KEY, geom GEOMETRY NOT NULL, description VARCHAR(255));
INSERT INTO geospatial_data (id, geom, description) VALUES (1, 'POLYGON((21.21482082946 38.604241103633, 21.214753050773 38.603677052906))'::GEOMETRY, 'Sample polygon');
CREATE TABLE measurements (id SERIAL PRIMARY KEY, geom_id INT REFERENCES geospatial_data(id), radius FLOAT CHECK (radius > 0));
INSERT INTO measurements (geom_id, radius) VALUES (1, 1.0);
SELECT g.id, g.geom, m.radius, geof:buffer(g.geom, m.radius, <http://www.opengis.net/def/uom/OGC/1.0/metre>) AS buffered_geom FROM geospatial_data g JOIN measurements m ON g.id = m.geom_id;
