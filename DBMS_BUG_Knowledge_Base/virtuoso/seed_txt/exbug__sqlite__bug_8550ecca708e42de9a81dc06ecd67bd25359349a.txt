CREATE TABLE test_struct (id INTEGER IDENTITY PRIMARY KEY, data BLOB NOT NULL, value INTEGER DEFAULT 0);
INSERT INTO test_struct (data, value) VALUES (CAST(0 AS BLOB), 1);
INSERT INTO test_struct (data, value) VALUES (CAST(0x0102030405060708 AS BLOB), NULL);
SELECT * FROM test_struct WHERE data = CAST(0 AS BLOB);
UPDATE test_struct SET value = 2 WHERE id = 1 AND data IS NOT NULL;
