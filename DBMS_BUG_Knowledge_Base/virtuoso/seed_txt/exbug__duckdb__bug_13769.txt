CREATE TABLE mytable (id INTEGER PRIMARY KEY, j VARCHAR, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO mytable (id, j) VALUES (1, 'a');
CREATE VIEW first_view AS SELECT id, j FROM mytable WHERE j IS NOT NULL;
CREATE VIEW second_view AS SELECT id, j FROM first_view WHERE id > 0;
SPARQL INSERT DATA { GRAPH <demo> { <mytable> <id> 1;
<j> 'a' . } };
