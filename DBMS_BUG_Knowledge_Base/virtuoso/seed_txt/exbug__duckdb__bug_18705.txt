CREATE TABLE test_strings (id INTEGER PRIMARY KEY, str_value VARCHAR(100));
INSERT INTO test_strings (id, str_value) VALUES (1, 'HELLO'), (2, 'world'), (3, 'HELLO world');
SELECT CASE WHEN UPPER(str_value) LIKE UPPER('%el%') THEN 1 ELSE 0 END AS position FROM test_strings WHERE id = 1;
SELECT CASE WHEN UPPER(str_value) LIKE UPPER('%hEllO%') THEN 1 ELSE 0 END AS position FROM test_strings WHERE id = 1;
SELECT CASE WHEN UPPER(str_value) LIKE UPPER('%HELLO%') THEN 1 ELSE 0 END AS contains FROM test_strings WHERE id = 3;
