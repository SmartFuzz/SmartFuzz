CREATE TABLE t02 ( id INTEGER NOT NULL, status VARCHAR(20) NOT NULL DEFAULT 'active', created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id) );
INSERT INTO t02 (status) VALUES ('inactive');
INSERT INTO t02 (status) VALUES ('unknown');
SELECT status FROM t02 WHERE created_at < TIMESTAMP '2023-01-01 00:00:00' OR NOT (status = 'active');
UPDATE t02 SET status = NULL WHERE id = 1;
