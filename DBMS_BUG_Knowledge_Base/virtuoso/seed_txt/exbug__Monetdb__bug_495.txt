CREATE TABLE first (id INTEGER NOT NULL, value VARCHAR(255), PRIMARY KEY (id));
CREATE TABLE second (id INTEGER NOT NULL, ref INTEGER, data FLOAT, PRIMARY KEY (id), FOREIGN KEY (ref) REFERENCES first (id));
INSERT INTO first (id, value) VALUES (1, 'test');
INSERT INTO second (id, ref, data) VALUES (1, NULL, 3.14);
SELECT * FROM second WHERE ref IS NULL;
