CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(50), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'test', NULL);
SELECT * FROM test_table WHERE value IS NULL;
SELECT (CASE WHEN value IS NULL THEN 1 ELSE 0 END) AS is_null FROM test_table;
SELECT id FROM test_table WHERE name IS NULL OR name = 'test';
