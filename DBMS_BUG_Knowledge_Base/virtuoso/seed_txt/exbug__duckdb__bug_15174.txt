CREATE TABLE test_data (id INTEGER PRIMARY KEY, value DOUBLE, name VARCHAR(255), created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test_data SELECT i, RAND(), CONCAT('Name ', CAST(i AS VARCHAR)), NOW() FROM (SELECT ROW_NUMBER() OVER () AS i FROM DB.DUAL CONNECT BY LEVEL <= 200000000);
CREATE TABLE filtered_data AS SELECT * FROM test_data WHERE value IS NOT NULL;
EXPORT AS CSV SELECT * FROM filtered_data ORDER BY value DESC TO '/tmp/filtered_test.csv';
SELECT COUNT(*) FROM filtered_data WHERE value >= 0.0 AND value <= 1.0;
