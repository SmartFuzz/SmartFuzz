CREATE TABLE test_table (id INTEGER PRIMARY KEY, value VARCHAR(255), is_active BOOLEAN);
INSERT INTO test_table (id, value, is_active) VALUES (1, 'test', TRUE), (2, 'example', FALSE);
SELECT (CASE WHEN EXISTS (SELECT 1 FROM test_table WHERE is_active) THEN -10 ELSE 0 END) AS c1, (CASE WHEN TRUE THEN -10 ELSE 0 END) AS c2, (CASE WHEN 1 IN (SELECT id FROM test_table) THEN -10 ELSE 0 END) AS c3;
CREATE TABLE another_table (num DECIMAL(10, 2), description LONG VARCHAR);
INSERT INTO another_table (num, description) VALUES (5.5, 'sample'), (NULL, 'null value');
