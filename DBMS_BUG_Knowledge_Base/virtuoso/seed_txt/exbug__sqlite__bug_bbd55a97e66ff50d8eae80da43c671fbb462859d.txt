CREATE TABLE test_table (id INTEGER, name VARCHAR, value BLOB, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO test_table (name, value) VALUES ('test1', CAST(zeroblob(1024) AS BLOB));
CREATE VIEW test_view AS SELECT name, value FROM test_table WHERE value IS NOT NULL;
SELECT DISTINCT name FROM test_view WHERE LENGTH(value) IN (SELECT LENGTH(CAST(zeroblob(2048) AS BLOB)));
UPDATE test_table SET value = NULL WHERE id = 1;
