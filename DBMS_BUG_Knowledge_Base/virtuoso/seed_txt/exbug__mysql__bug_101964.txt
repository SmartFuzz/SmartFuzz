CREATE DATABASE test_db;
CREATE TABLE test_db.joinit (i INTEGER NOT NULL, s VARCHAR(64) DEFAULT NULL, t TIME NOT NULL, g INTEGER NOT NULL, PRIMARY KEY (i));
INSERT INTO test_db.joinit VALUES (0, UUID(), TIME(NOW()), FLOOR(1 + RAND() * 60));
INSERT INTO test_db.joinit (s, t, g) SELECT UUID(), TIME(NOW()), FLOOR(1 + RAND() * 60) FROM test_db.joinit WHERE g < 30;
SELECT COUNT(*) FROM test_db.joinit WHERE g IS NULL OR g > 50;
