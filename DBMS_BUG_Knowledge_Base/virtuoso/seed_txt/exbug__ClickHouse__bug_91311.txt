CREATE TABLE test1 (n_a ARRAY<TUPLE(aa INTEGER, bb INTEGER)> DEFAULT [TUPLE(0, 0)]);
INSERT INTO test1 VALUES (ARRAY[TUPLE(1, 2), TUPLE(3, 4), TUPLE(5, 6)]);
ALTER TABLE test1 ADD COLUMN n_c ARRAY<TUPLE(cc INTEGER, dd INTEGER)> DEFAULT [TUPLE(0, 0)];
SELECT n_a, n_c FROM test1 WHERE EXISTS (SELECT 1 FROM n_a AS x WHERE x.aa > 2);
CREATE TABLE test2 AS test1;
