CREATE TABLE test_dates (id INTEGER PRIMARY KEY, date_value TIMESTAMP, description VARCHAR(255));
INSERT INTO test_dates (id, date_value, description) VALUES (1, NULL, 'Invalid date format'), (2, NULL, 'Invalid date format');
SELECT id, date_value, description FROM test_dates WHERE date_value IS NULL;
SELECT NULL AS d1, NULL AS d2, NULL AS d3;
SELECT COUNT(*) FROM test_dates WHERE date_value IS NOT NULL;
