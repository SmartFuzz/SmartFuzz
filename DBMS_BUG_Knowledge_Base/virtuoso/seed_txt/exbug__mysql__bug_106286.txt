CREATE TABLE test (id INTEGER PRIMARY KEY, ts TIMESTAMP NULL);
INSERT INTO test (id, ts) VALUES (1, TIMESTAMP '2023-01-01 00:00:00');
CREATE TRIGGER tr_bi_check_uniqueness_with_nulls BEFORE INSERT ON test FOR EACH ROW BEGIN IF EXISTS ( SELECT NULL FROM test WHERE test.id = NEW.id AND test.ts = CAST(NEW.ts AS TIMESTAMP) ) THEN RAISE ERROR 'Duplicated values not allowed.';
END IF;
END;
INSERT INTO test (id, ts) VALUES (1, NULL);
