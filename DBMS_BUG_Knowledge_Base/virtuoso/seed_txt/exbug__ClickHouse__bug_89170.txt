CREATE TABLE test_distinct_oom_v2 ( id INTEGER, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP );
INSERT INTO test_distinct_oom_v2 (id, value) SELECT number, CASE WHEN number % 2 = 0 THEN CAST(number AS VARCHAR) ELSE NULL END FROM (SELECT ROW_NUMBER() OVER () AS number FROM dbms_random.value) LIMIT 100000000;
SELECT DISTINCT value FROM test_distinct_oom_v2;
CREATE TABLE test_distinct_oom_v3 ( id INTEGER, value FLOAT, category VARCHAR DEFAULT 'unknown' );
INSERT INTO test_distinct_oom_v3 (id, value, category) SELECT number, number / 3.14, CAST(number % 10 AS VARCHAR) FROM (SELECT ROW_NUMBER() OVER () AS number FROM dbms_random.value) LIMIT 1000000;
