CREATE TABLE test_table (id INTEGER IDENTITY PRIMARY KEY, name VARCHAR(50) NOT NULL, description LONG VARCHAR);
INSERT INTO test_table (name, description) VALUES ('foo', 'A test entry'), ('BAR', 'Another test entry');
CREATE INDEX test_idx ON test_table USING gist(lower(name)) INCLUDE (description);
SELECT lower(name) FROM test_table WHERE lower(name) LIKE '%o%';
