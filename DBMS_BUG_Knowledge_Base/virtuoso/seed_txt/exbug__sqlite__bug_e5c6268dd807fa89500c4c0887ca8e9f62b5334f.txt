CREATE TABLE test_table (id INTEGER IDENTITY PRIMARY KEY, name NVARCHAR(255), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, value) VALUES ('Alice', 10.5), ('Bob', 20.0), ('Charlie', NULL), ('David', 15.0);
CREATE TABLE another_table (ref_id INTEGER, description NVARCHAR(255), FOREIGN KEY(ref_id) REFERENCES test_table(id));
INSERT INTO another_table (ref_id, description) VALUES (1, 'First'), (2, 'Second'), (3, 'Third'), (NULL, 'Fourth');
SELECT (SELECT 'mmm' UNION SELECT DISTINCT max(name) FROM test_table WHERE value IS NOT NULL) FROM db.dba.v_catalog;
