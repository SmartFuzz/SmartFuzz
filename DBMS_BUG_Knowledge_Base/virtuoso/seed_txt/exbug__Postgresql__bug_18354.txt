SET TEMP_FILE_LIMIT = 100;
CREATE TABLE t (a VARCHAR(255), b INTEGER, c TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t (a, b) VALUES ('test', 1);
INSERT INTO t (a, b) SELECT CONCAT('test_', CAST(VALUES AS INTEGER)) AS a, VALUES AS b FROM (SELECT ROW_NUMBER() OVER () AS VALUES FROM DUAL) AS temp_table LIMIT 20;
SELECT * FROM t WHERE b IS NULL OR c > NOW() - INTERVAL '1 DAY';
