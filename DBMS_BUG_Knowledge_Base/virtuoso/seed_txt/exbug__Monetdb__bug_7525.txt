CREATE TABLE test_vals(grp INTEGER NOT NULL, value INTEGER CHECK(value >= 0));
INSERT INTO test_vals (grp, value) VALUES (1, 100), (2, 200), (1, NULL), (2, 300);
CREATE OR REPLACE AGGREGATE jit_sum_variadic(*, INTEGER) RETURNS BIGINT LANGUAGE C { result->initialize(result, 1);
memset(result->data, 0, result->count * sizeof(result->null_value));
for(size_t i = 0;
i < aggr_group.count;
