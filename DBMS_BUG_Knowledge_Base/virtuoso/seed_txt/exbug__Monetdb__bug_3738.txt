CREATE TABLE bar (id INTEGER IDENTITY PRIMARY KEY, name NVARCHAR(50), created_at TIMESTAMP DEFAULT NOW());
INSERT INTO bar (name) VALUES ('test1'), ('test2'), (NULL);
BEGIN TRANSACTION;
SELECT * FROM bar WHERE created_at IS NOT NULL;
ROLLBACK;
