CREATE TABLE test_table (id INTEGER PRIMARY KEY, value DOUBLE, description VARCHAR(255));
INSERT INTO test_table (id, value, description) VALUES (1, RAND(), 'First entry'), (2, RAND(), 'Second entry'), (3, RAND(), 'Third entry');
CREATE OR REPLACE FUNCTION random_value() AS (SELECT RAND());
SELECT random_value() FROM test_table;
SELECT (SELECT RAND() FROM test_table) AS random_result, id FROM test_table;
