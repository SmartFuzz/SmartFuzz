CREATE GRAPH <financialdata>;
CREATE TABLE <financialdata>.<stocks> (id INTEGER NOT NULL, trade_time TIMESTAMP WITH TIME ZONE NOT NULL, trade_date TIMESTAMP WITH TIME ZONE, ticker VARCHAR(10) NOT NULL, currency VARCHAR(10) NOT NULL, opening_price DOUBLE NOT NULL, highest_price DOUBLE NOT NULL, lowest_price DOUBLE NOT NULL, closing_price DOUBLE NOT NULL, trade_volume DOUBLE, PRIMARY KEY (id));
INSERT INTO <financialdata>.<stocks> (trade_time, trade_date, ticker, currency, opening_price, highest_price, lowest_price, closing_price, trade_volume) VALUES (NOW(), NOW(), 'AAPL', 'USD', 150.0, 155.0, 149.0, 154.0, 10000.0);
SELECT trade_date, opening_price, highest_price, lowest_price, closing_price, trade_volume FROM <financialdata>.<stocks> WHERE ticker='AAPL' AND trade_date IS NOT NULL AND trade_date > CURRENT_DATE - 30 ORDER BY trade_date DESC;
SELECT DISTINCT CAST(trade_date AS DATE) FROM <financialdata>.<stocks> LIMIT 10;
