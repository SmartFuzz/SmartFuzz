CREATE TABLE test_shift (id INTEGER IDENTITY PRIMARY KEY, value BIGINT NOT NULL);
INSERT INTO test_shift (value) VALUES (1), (98), (NULL);
SELECT value * POWER(2, 6) FROM test_shift WHERE value IS NOT NULL;
SELECT CAST(value AS BIGINT) * POWER(2, 8) FROM test_shift;
SELECT CAST(NULL AS BIGINT) * POWER(2, 32) FROM test_shift;
