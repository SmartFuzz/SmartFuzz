CREATE TABLE orders (o_custkey INTEGER, o_orderdate DATE, o_amount DECIMAL(10, 2), o_status VARCHAR(20) DEFAULT 'pending');
INSERT INTO orders (o_custkey, o_orderdate, o_amount, o_status) VALUES (1, '2023-01-01', 100.00, 'completed'), (2, NULL, 200.50, 'pending'), (NULL, '2023-01-03', 150.75, 'shipped');
CREATE TABLE customers (c_custkey INTEGER PRIMARY KEY, c_name VARCHAR(50), c_email VARCHAR(100) UNIQUE);
INSERT INTO customers (c_custkey, c_name, c_email) VALUES (1, 'Alice', 'alice@example.com'), (2, 'Bob', NULL);
SELECT c.c_custkey, COUNT(o.o_custkey) AS order_count FROM customers c LEFT JOIN orders o ON c.c_custkey = o.o_custkey GROUP BY c.c_custkey;
