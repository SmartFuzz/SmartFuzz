CREATE TABLE emp (empno INTEGER, name VARCHAR(50), deptno INTEGER CHECK (deptno >= 0));
INSERT INTO emp (empno, name, deptno) VALUES (0, 'Alex', 0);
INSERT INTO emp (empno, name, deptno) VALUES (10, 'Bob', 1);
CREATE TABLE dept (deptno INTEGER PRIMARY KEY, deptname VARCHAR(50));
INSERT INTO dept (deptno, deptname) VALUES (1, 'Sales');
SELECT e.name FROM emp e INNER JOIN dept d ON e.deptno = d.deptno WHERE (10 / CASE WHEN e.empno = 0 THEN NULL ELSE e.empno END) = 1;
