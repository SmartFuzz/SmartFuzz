DROP TABLE IF EXISTS tt3;
CREATE TABLE tt3 (id INT NOT NULL, listid INT, name VARCHAR(20) NOT NULL DEFAULT 'default_name');
INSERT INTO tt3 VALUES (1, 1, 'Alice'), (2, 2, 'Bob'), (3, 3, 'Charlie');
SELECT COUNT(*) FROM tt3 LEFT JOIN (SELECT listid FROM tt3 WHERE listid = 5) AS subquery ON tt3.listid = subquery.listid;
ANALYZE TABLE tt3;
