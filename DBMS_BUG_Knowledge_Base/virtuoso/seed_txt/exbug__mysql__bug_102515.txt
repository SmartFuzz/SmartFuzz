CREATE TABLE test_replication (id INTEGER PRIMARY KEY, data VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_replication (id, data) VALUES (1, 'Initial data');
UPDATE test_replication SET data = NULL WHERE id = 1;
SELECT * FROM test_replication WHERE data IS NULL;
DELETE FROM test_replication WHERE id = 1;
