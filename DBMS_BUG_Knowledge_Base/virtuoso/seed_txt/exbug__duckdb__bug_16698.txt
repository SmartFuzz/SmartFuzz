CREATE TABLE users (user_id integer PRIMARY KEY, username varchar, age integer);
INSERT INTO users (user_id, username, age) VALUES (1, 'Alice', 30);
INSERT INTO users (user_id) VALUES (1);
IF @@ROWCOUNT = 0 THEN UPDATE users SET username = 'Alice' WHERE user_id = 1;
END IF;
SELECT * FROM users;
