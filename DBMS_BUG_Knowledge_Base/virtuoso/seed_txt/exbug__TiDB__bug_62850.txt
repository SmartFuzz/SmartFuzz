CREATE TABLE parent ( id INTEGER NOT NULL, PRIMARY KEY (id) );
CREATE TABLE child ( v INTEGER DEFAULT NULL, parent_id INTEGER DEFAULT NULL, FOREIGN KEY (parent_id) REFERENCES parent(id) ON DELETE CASCADE ON UPDATE CASCADE );
INSERT INTO parent VALUES (1), (2), (3);
INSERT INTO child VALUES (NULL, 2);
UPDATE parent SET id = 4 WHERE id = 2;
SELECT * FROM child;
