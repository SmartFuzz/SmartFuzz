CREATE TABLE testtable (id INTEGER PRIMARY KEY, textColumn VARCHAR(255), varcharColumn VARCHAR(255), nullableColumn VARCHAR(255));
INSERT INTO testtable (id, textColumn, varcharColumn, nullableColumn) VALUES (1, 'Sample text', 'Sample varchar', NULL);
SELECT c.rdf_type AS relname, a.name AS attname, a.type AS atttypid FROM rdf_class c JOIN rdf_namespace n ON n.id = c.namespace_id JOIN rdf_attribute a ON a.class_id = c.id WHERE a.name = 'textColumn';
CREATE TABLE another_table (ref_id INTEGER, description VARCHAR(255) NOT NULL, FOREIGN KEY (ref_id) REFERENCES testtable(id));
INSERT INTO another_table (ref_id, description) VALUES (1, 'Reference to testtable');
