CREATE USER user03 IDENTIFIED BY 'user03';
GRANT SELECT, INSERT ON test01.c TO user03;
CREATE TABLE tbl03 (id int primary key, b int NOT NULL DEFAULT 0, c varchar(255) DEFAULT NULL);
INSERT INTO tbl03 (id, b) VALUES (1, NULL);
CREATE TRIGGER trg02 BEFORE INSERT ON tbl03 FOR EACH ROW SET NEW.c = (SELECT a FROM test01.c WHERE id = 1);
