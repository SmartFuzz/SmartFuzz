CREATE TABLE t1 ( id INTEGER NOT NULL, name VARCHAR(255) NOT NULL, value DECIMAL(10,2) NULL, PRIMARY KEY (id) );
INSERT INTO t1 (id, name, value) VALUES (1, 'test', 10.50), (2, 'example', NULL), (3, 'sample', 5.75);
SELECT * FROM t1 WHERE value IS NULL OR (value LIKE '%');
CREATE TABLE t2 ( a VARCHAR(100) NOT NULL, b TIMESTAMP NULL, PRIMARY KEY (a) );
SELECT a FROM t2 WHERE (b IS NULL AND (a LIKE '[A-Z]%')) OR (b IS NOT NULL AND (a LIKE '%'));
