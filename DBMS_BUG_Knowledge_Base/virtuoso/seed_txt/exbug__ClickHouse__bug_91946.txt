CREATE TABLE users_extended (uid INTEGER, name VARCHAR(255), age SMALLINT, deleted TINYINT, version BIGINT, salary FLOAT) PRIMARY KEY (uid);
INSERT INTO users_extended (uid, name, age, deleted, version, salary) VALUES (1, 'John', 33, 0, 1, 50000.0);
INSERT INTO users_extended (uid, name, age, deleted, version, salary) VALUES (2, 'Ksenia', 48, 0, 1, 60000.0);
CREATE VIEW only_females AS SELECT * FROM users_extended WHERE deleted = 0 AND name NOT LIKE 'J%';
SELECT AVG(salary) FROM only_females WHERE age > 30 GROUP BY age;
