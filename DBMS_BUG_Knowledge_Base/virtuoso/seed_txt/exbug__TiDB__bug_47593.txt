CREATE TABLE users ( user_id INTEGER NOT NULL, username VARCHAR(50) NOT NULL, email VARCHAR(100) NULL, PRIMARY KEY (user_id), UNIQUE INDEX idx_username (username));
INSERT INTO users (username, email) VALUES ('testuser', 'test@example.com');
CREATE TABLE orders ( order_id INTEGER NOT NULL, user_id INTEGER NOT NULL, amount DECIMAL(10,2) NOT NULL, PRIMARY KEY (order_id), FOREIGN KEY (user_id) REFERENCES users(user_id));
INSERT INTO orders (user_id, amount) VALUES (1, 99.99);
SPARQL INSERT { GRAPH <http://example.org/test> { ?s ?p ?o } } WHERE { BIND(IRI('http://example.org/test') AS ?s) BIND(IRI('http://example.org/username') AS ?p) BIND('testuser' AS ?o) };
