CREATE TABLE v1 ( id INTEGER PRIMARY KEY, value VARCHAR(255) NOT NULL );
INSERT INTO v1 (value) VALUES ('A'), ('B'), ('C');
CREATE TABLE v2 ( id INTEGER PRIMARY KEY, ref_id INTEGER, amount FLOAT );
INSERT INTO v2 (ref_id, amount) VALUES (1, 10.5), (1, 20.0), (2, NULL);
SELECT DISTINCT value, (SELECT value FROM v1 WHERE id > v1.id ORDER BY id LIMIT 1) AS next_value FROM v1 ORDER BY value;
