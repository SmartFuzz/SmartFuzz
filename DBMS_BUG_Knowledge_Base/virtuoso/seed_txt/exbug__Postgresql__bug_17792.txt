CREATE TABLE source (sid integer, sname varchar(255));
INSERT INTO source (sid, sname) VALUES (1, 'Source1');
CREATE TABLE target (tid integer, tval integer DEFAULT 0 CHECK (tval >= 0));
INSERT INTO target (tid, tval) VALUES (1, 1);
INSERT { tval = tval + 1 } WHERE tid IN (SELECT sid FROM source);
