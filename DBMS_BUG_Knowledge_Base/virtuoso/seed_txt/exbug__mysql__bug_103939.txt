CREATE TABLE t4 (id INTEGER NOT NULL, name VARCHAR(50) NOT NULL, age INTEGER, PRIMARY KEY (name, id));
CREATE TABLE t5 (id INTEGER NOT NULL, description LONG VARCHAR, created_at TIMESTAMP DEFAULT NOW(), PRIMARY KEY (created_at, id));
INSERT INTO t4 (name, age) VALUES ('Alice', 30);
INSERT INTO t5 (description) VALUES ('Sample description');
SELECT * FROM t4 WHERE age IS NULL OR age > 25;
