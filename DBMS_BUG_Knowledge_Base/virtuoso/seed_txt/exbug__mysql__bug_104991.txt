CREATE TABLE user ( id INTEGER NOT NULL, data VARCHAR(65535) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO user (id, data) VALUES (1, '{"name": "John", "age": 30}');
INSERT INTO user (id, data) VALUES (2, NULL);
SELECT id, data FROM user WHERE data LIKE '%"name": "John"%';
UPDATE user SET data = CONCAT(data, ', "age": 31') WHERE id = 1;
