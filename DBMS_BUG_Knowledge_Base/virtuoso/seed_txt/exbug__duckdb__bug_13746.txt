CREATE TABLE test_array_distinct (id INTEGER, values VARCHAR[]);
INSERT INTO test_array_distinct (id, values) VALUES (1, ARRAY['A', 'A', 'B', NULL, NULL]), (2, ARRAY['B', 'C', 'C', 'A', NULL]);
SELECT DISTINCT ? AS distinct_values FROM test_array_distinct WHERE id = 1 AND ? IN (SELECT UNNEST(values) WHERE id = 1);
SELECT DISTINCT ? AS distinct_values FROM (SELECT UNNEST(ARRAY_CONCAT(values, ARRAY['D', 'D'])) AS value FROM test_array_distinct WHERE id = 2);
SELECT DISTINCT ? AS distinct_numbers FROM (SELECT UNNEST(ARRAY[1, 2, NULL, 2, NULL]) AS number);
