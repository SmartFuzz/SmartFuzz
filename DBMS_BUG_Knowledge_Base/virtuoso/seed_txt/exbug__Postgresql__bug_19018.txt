CREATE TABLE comment (id INTEGER IDENTITY PRIMARY KEY, path VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO comment (path) VALUES ('0.1.2.3'), ('0.1.2.4'), (NULL);
INSERT INTO comment (path) SELECT '0.' || CAST(ROW_NUMBER() OVER() AS VARCHAR(255)) FROM (SELECT 1 AS dummy FROM DUAL CONNECT BY LEVEL <= 500) AS seq;
CREATE INDEX ON comment (path);
SELECT COUNT(*) FROM comment;
