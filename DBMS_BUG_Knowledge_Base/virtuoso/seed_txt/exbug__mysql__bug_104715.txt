DROP TABLE IF EXISTS vs_server;
CREATE TABLE vs_server (nGroupId INT, nName VARCHAR(50));
INSERT INTO vs_server(nGroupId, nName) VALUES (15, 'Group A'), (21, 'Group B');
SET nVal = NULL;
SELECT nVal = CASE WHEN EXISTS (SELECT * FROM vs_server WHERE nGroupId = 15) THEN 1 ELSE 0 END;
