CREATE TABLE test_table (id INTEGER NOT NULL PRIMARY KEY, name VARCHAR(100), age INTEGER, status VARCHAR(20));
INSERT INTO test_table (id, name, age, status) VALUES (1, 'Alice', 30, 'active');
INSERT INTO test_table (id, name, age, status) VALUES (2, 'Bob', 25, 'inactive');
SELECT * FROM test_table WHERE age > 20 AND status = 'active';
SELECT COUNT(*) FROM test_table WHERE status IS NULL;
