CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, product_name VARCHAR(100), quantity INTEGER CHECK (quantity > 0), order_date TIMESTAMP DEFAULT NOW());
INSERT INTO orders (user_id, product_name, quantity) VALUES (1, 'Product_A', 5), (2, 'Product_B', 10), (3, 'Product_C', 1);
CREATE TABLE payments (payment_id INTEGER PRIMARY KEY, order_id INTEGER, amount DECIMAL(10, 2), payment_date TIMESTAMP DEFAULT NOW(), FOREIGN KEY (order_id) REFERENCES orders(order_id));
INSERT INTO payments (order_id, amount) SELECT order_id, 99.99 FROM orders WHERE quantity > 0;
SELECT o.order_id, p.amount FROM orders o JOIN payments p ON o.order_id = p.order_id WHERE p.amount IS NOT NULL;
