CREATE TABLE users(id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, age INTEGER CHECK(age >= 0));
INSERT INTO users(name, age) VALUES('Alice', 30);
CREATE TABLE temp_users AS SELECT * FROM users WHERE age > 25;
CREATE TABLE orders(order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DECIMAL(10, 2) NOT NULL);
INSERT INTO orders(user_id, amount) VALUES((SELECT id FROM users WHERE name = 'Alice'), 99.99);
