CREATE TABLE tbl ( id INTEGER NOT NULL, user_id INTEGER DEFAULT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id) );
CREATE TABLE fp_tbl ( id INTEGER NOT NULL, user_id INTEGER DEFAULT NULL, status VARCHAR(20) DEFAULT 'active', PRIMARY KEY (id) );
CREATE TABLE fp_pol ( id INTEGER NOT NULL, lead_id INTEGER DEFAULT NULL, amount DECIMAL(10,2) DEFAULT NULL, PRIMARY KEY (id) );
INSERT INTO tbl (id, user_id) VALUES (1, 583532949), (2, 1342458479);
INSERT INTO fp_pol (lead_id, amount) VALUES (1, 100.00), (2, 200.00);
SELECT a.id, COUNT(*) AS n FROM tbl AS a LEFT JOIN fp_tbl AS f ON f.id = a.id LEFT JOIN fp_pol AS p ON p.lead_id = f.id WHERE a.id IN (1,2) AND p.id IS NULL GROUP BY a.id;
