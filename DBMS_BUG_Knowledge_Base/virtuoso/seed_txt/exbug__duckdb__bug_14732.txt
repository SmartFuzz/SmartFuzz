CREATE TABLE test1(id INT, name VARCHAR(50), created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test1(id, name) VALUES (1, 'Alice'), (2, 'Bob');
CREATE SCHEMA risk;
SET SCHEMA risk;
SELECT * FROM test1 WHERE id = 1;
