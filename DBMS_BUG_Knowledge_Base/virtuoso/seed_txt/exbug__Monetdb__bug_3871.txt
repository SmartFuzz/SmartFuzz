CREATE TABLE bar (id INTEGER PRIMARY KEY, name VARCHAR(20), age INTEGER CHECK (age >= 0));
INSERT INTO bar (id, name, age) VALUES (1, 'Alice', 30);
INSERT INTO bar (id, name, age) VALUES (2, 'Bob', NULL);
SELECT * FROM bar WHERE NOT name LIKE 'A%';
SELECT * FROM bar WHERE NOT (age IS NULL OR name LIKE 'B%') AND id < 3;
