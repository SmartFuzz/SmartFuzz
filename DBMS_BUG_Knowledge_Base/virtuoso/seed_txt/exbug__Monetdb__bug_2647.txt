CREATE TABLE customers (id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, email VARCHAR(255) UNIQUE, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO customers (name, email) VALUES ('John Doe', 'john.doe@example.com');
CREATE TABLE orders (id INTEGER PRIMARY KEY, customer_id INTEGER, total_amount DECIMAL(10,2) NOT NULL, created_at TIMESTAMP DEFAULT NOW(), FOREIGN KEY (customer_id) REFERENCES customers(id));
INSERT INTO orders (customer_id, total_amount) VALUES (1, 99.99);
SELECT c.name, o.total_amount FROM customers c JOIN orders o ON c.id = o.customer_id WHERE o.total_amount > 50.00;
