CREATE TABLE test_table (id INTEGER PRIMARY KEY, flag BOOLEAN, description VARCHAR(255));
INSERT INTO test_table (id, flag, description) VALUES (1, TRUE, 'First entry'), (2, FALSE, 'Second entry'), (3, NULL, 'Third entry');
CREATE PROCEDURE evaluate_flag(b BOOLEAN) AS BEGIN IF b THEN RETURN 'true';
ELSE IF NOT b THEN RETURN 'false';
ELSE RETURN 'null';
END IF;
