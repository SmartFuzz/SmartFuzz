CREATE TABLE test_table (id INTEGER PRIMARY KEY, value DOUBLE NOT NULL, name VARCHAR(255));
INSERT INTO test_table (value, name) VALUES (0.5, 'TestValue');
SELECT CASE WHEN value = 1.0 THEN 1 ELSE 0 END AS is_true FROM test_table WHERE id = 1;
SELECT CASE WHEN value = 1.0 THEN 1 ELSE 0 END AS is_true FROM test_table WHERE id = 1;
SELECT CASE WHEN value = 0.0 THEN 1 ELSE 0 END AS is_false FROM test_table WHERE id = 1;
