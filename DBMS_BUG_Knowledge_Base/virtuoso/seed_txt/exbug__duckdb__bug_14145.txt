CREATE TABLE test_json_data (id INTEGER, data VARCHAR);
INSERT INTO test_json_data VALUES (1, '{"a":1}'), (2, '[2]');
SELECT MAX(data) AS max_value FROM test_json_data;
SELECT MAX(d) FROM (SELECT SPLIT(data, ',') AS d FROM test_json_data);
SELECT MAX(STR_TO_JSON('[{"b":2},{"a":1}]')) AS max_json;
