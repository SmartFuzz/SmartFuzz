CREATE TABLE lightweight_test (ts TIMESTAMP, value VARCHAR(255), key VARCHAR(255), status INTEGER) INDEX key_idx ON key;
CREATE TABLE keys (key VARCHAR(255), priority INTEGER) INDEX key_idx ON key;
INSERT INTO lightweight_test (ts, value, key, status) VALUES (CURRENT_TIMESTAMP, 'val', 'key', NULL);
INSERT INTO keys (key, priority) VALUES ('key', 1), ('key2', 2);
UPDATE lightweight_test SET value = 'UPDATED-2' WHERE key IN (SELECT key FROM keys WHERE priority > 1);
SELECT * FROM lightweight_test;
