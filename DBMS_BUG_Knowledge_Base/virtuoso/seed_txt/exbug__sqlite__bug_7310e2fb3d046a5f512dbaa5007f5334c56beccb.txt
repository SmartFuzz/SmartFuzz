CREATE TABLE a(id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, age INTEGER CHECK(age >= 0));
CREATE TABLE b(id INTEGER PRIMARY KEY, a_id INTEGER, FOREIGN KEY(a_id) REFERENCES a(id));
CREATE TABLE c(id INTEGER PRIMARY KEY, value FLOAT DEFAULT 0.0);
INSERT INTO a(name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', -1);
SELECT * FROM a JOIN b ON a.id = b.a_id WHERE EXISTS (SELECT 1 FROM a AS sub WHERE sub.id = a.id AND sub.age IS NOT NULL);
