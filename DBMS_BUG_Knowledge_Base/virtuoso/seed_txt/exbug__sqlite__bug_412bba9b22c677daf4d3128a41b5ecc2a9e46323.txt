CREATE TABLE test_table (id INTEGER PRIMARY KEY, value DECIMAL, description VARCHAR(255));
INSERT INTO test_table (value, description) VALUES (0, 'Test case for dot as numeric');
SELECT -value FROM test_table WHERE id = 1;
SELECT value + 0 FROM test_table WHERE description LIKE 'Test%';
SELECT -CAST('0' AS DECIMAL) AS result;
