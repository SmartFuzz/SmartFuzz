CREATE TABLE t2 ( x int DEFAULT NULL, y varchar(255) DEFAULT NULL, z datetime DEFAULT NULL, UNIQUE (x), UNIQUE (y) );
INSERT INTO t2 (x, y, z) VALUES (5, 'test', NOW());
INSERT INTO t2 (x, y, z) VALUES (15, 'example', NOW());
SELECT * FROM t2 WHERE y = 'example';
SELECT * FROM t2 WHERE x = 5;
