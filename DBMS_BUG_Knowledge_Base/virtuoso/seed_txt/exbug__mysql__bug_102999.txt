CREATE TABLE articles (FTS_DOC_ID BIGINT NOT NULL PRIMARY KEY, id INT NOT NULL, title VARCHAR(200) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FULLTEXT (title));
INSERT INTO articles(id, title) VALUES(1, 'mysql');
INSERT INTO articles(id, title) VALUES(2, 'mysql');
INSERT INTO articles(id, title) VALUES(3, 'mysql');
SELECT * FROM articles WHERE CONTAINS(title, 'mysql');
