CREATE DATABASE reproduction_test;
CREATE TABLE reproduction_test.tree_nodes (date xsd:date, c1 xsd:string, c2 xsd:integer);
INSERT DATA { (reproduction_test:tree_nodes, '2023-01-01', 'node1', 1), (reproduction_test:tree_nodes, '2023-01-01', 'node2', 2), (reproduction_test:tree_nodes, '2023-01-01', 'node3', 3) };
SELECT date, COUNT(DISTINCT c2) AS unique_count FROM reproduction_test.tree_nodes GROUP BY date;
SELECT date, COUNT(DISTINCT c2) AS unique_count FROM reproduction_test.tree_nodes WHERE c2 IS NOT NULL GROUP BY date;
