CREATE TABLE tx(s1 BLOB, s2 BLOB, s3 VARCHAR(255) NOT NULL DEFAULT 'default');
INSERT INTO tx VALUES (ENCRYPT('a', 'b'), ENCRYPT('c', 'd'), NULL);
INSERT INTO tx VALUES (ENCRYPT('e', 'f'), ENCRYPT('g', 'h'), 'test');
SELECT * FROM tx WHERE DECRYPT(s1, 'b') IS NULL OR DECRYPT(s2, 'd') IS NULL;
SELECT * FROM tx WHERE DECRYPT(s1, 'b')='' AND s3='default';
