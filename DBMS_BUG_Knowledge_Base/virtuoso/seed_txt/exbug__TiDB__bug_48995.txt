CREATE TABLE t1 (id INTEGER PRIMARY KEY, name VARCHAR(50), value DECIMAL(10,2));
INSERT INTO t1 (id, name, value) VALUES (1, 'item1', 10.00), (2, 'item2', NULL), (3, 'item3', 20.50);
CREATE VIEW v1 AS SELECT id, CASE WHEN value IS NULL THEN 0 ELSE value END AS value FROM t1;
SELECT DISTINCT value FROM v1 WHERE value > 5.00;
CREATE TABLE t2 (x VARCHAR(3) NOT NULL, y INTEGER DEFAULT 0);
