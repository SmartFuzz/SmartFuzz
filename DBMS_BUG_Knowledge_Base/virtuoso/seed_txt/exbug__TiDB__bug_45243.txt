DROP DATABASE IF EXISTS test_db;
CREATE DATABASE test_db;
CREATE TABLE users(id INTEGER NOT NULL PRIMARY KEY, name VARCHAR(100), age INTEGER, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO users VALUES (1, 'Alice', 30, NULL), (2, 'Bob', NULL, NULL), (3, 'Charlie', 25, NULL);
SELECT * FROM users;
