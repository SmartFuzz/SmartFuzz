CREATE TABLE test_dates (id INTEGER PRIMARY KEY, t_date TIMESTAMP NULL);
INSERT INTO test_dates (t_date) VALUES (CASE WHEN ? = '' THEN NULL ELSE ? END);
SET @empty_string = '';
INSERT INTO test_dates (t_date) VALUES (CASE WHEN @empty_string = '' THEN NULL ELSE @empty_string END);
SELECT * FROM test_dates;
