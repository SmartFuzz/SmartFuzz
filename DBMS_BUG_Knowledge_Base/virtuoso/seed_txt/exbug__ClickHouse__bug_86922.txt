DROP GRAPH IF EXISTS test;
CREATE GRAPH test;
CREATE CLASS mv_src_table SUBCLASS OF rdfs:Resource HAS id xsd:integer, name xsd:string, created_at xsd:dateTime;
INSERT DATA { (mv_src_table:1, 'test1', now()), (mv_src_table:2, 'test2', now()) };
CREATE CLASS mv_refreshable_dst_table SUBCLASS OF rdfs:Resource HAS id xsd:integer, value xsd:double;
CREATE MATERIALIZED VIEW mv_refreshable AS SELECT id, max(id) * 1.0 AS value FROM mv_src_table GROUP BY id;
