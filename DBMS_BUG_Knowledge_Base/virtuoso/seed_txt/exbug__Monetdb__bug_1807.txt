CREATE TABLE B (ID INTEGER PRIMARY KEY, VALUE DECIMAL(5,2));
INSERT INTO B (ID, VALUE) VALUES (1, -15.75);
INSERT INTO B (ID, VALUE) VALUES (2, 20.50);
UPDATE B SET VALUE = CASE WHEN VALUE < 0 THEN -VALUE ELSE VALUE END;
SELECT * FROM B;
