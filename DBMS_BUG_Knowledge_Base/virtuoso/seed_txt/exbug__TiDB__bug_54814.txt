CREATE TABLE item ( id INTEGER NOT NULL, status TINYINT NOT NULL, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id) );
INSERT INTO item(status) VALUES(0);
INSERT INTO item(status) VALUES(1);
UPDATE item SET status = NULL WHERE id = 1;
SELECT * FROM item WHERE updated_at IS NOT NULL;
