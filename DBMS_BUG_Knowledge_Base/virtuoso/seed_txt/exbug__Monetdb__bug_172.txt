CREATE TABLE test_table (id INTEGER PRIMARY KEY, flag BOOLEAN, value INTEGER);
INSERT INTO test_table (id, flag, value) VALUES (1, TRUE, 10);
INSERT INTO test_table (id, flag, value) VALUES (2, FALSE, 20);
SELECT CASE WHEN flag IS NULL THEN 1 ELSE 0 END AS is_null_flag FROM test_table WHERE id = 1;
SELECT (flag IS NOT NULL AND flag = FALSE) AS flag_not_null_false FROM test_table WHERE value > 5;
