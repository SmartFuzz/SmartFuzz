CREATE TABLE tbl1 (id INTEGER IDENTITY PRIMARY KEY, data NVARCHAR(4000), created_at TIMESTAMP DEFAULT NOW());
CREATE TABLE tbl2 (ref_id INTEGER REFERENCES tbl1(id), value INTEGER CHECK (value > 0));
INSERT INTO tbl1 (data) VALUES (REPEAT('A', 300)), (REPEAT('B', 300));
DELETE FROM tbl1 WHERE id = 1;
DB.DROP_TABLE('tbl1');
DB.CREATE_TABLE('tbl1');
