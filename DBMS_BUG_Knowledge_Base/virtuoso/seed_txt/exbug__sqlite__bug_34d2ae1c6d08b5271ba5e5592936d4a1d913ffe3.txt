CREATE TABLE fuzz_sqlite_r(a INTEGER, ROWID INTEGER);
INSERT INTO fuzz_sqlite_r(a, ROWID) VALUES (1, 100);
CREATE PROCEDURE fuzz_sqlite_r_ai() AS BEGIN INSERT INTO fuzz_sqlite_r(a, ROWID) VALUES (SELECT a + 1 FROM fuzz_sqlite_r WHERE ROWID = LAST_INSERT_ID(), SELECT ROWID + 1 FROM fuzz_sqlite_r WHERE ROWID = LAST_INSERT_ID());
END;
INSERT INTO fuzz_sqlite_r(a, ROWID) VALUES (2, 200);
SELECT ROWID, a, ROWID FROM fuzz_sqlite_r;
