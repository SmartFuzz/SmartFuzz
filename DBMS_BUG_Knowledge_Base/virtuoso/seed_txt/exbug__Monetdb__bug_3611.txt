CREATE TABLE test_table ( id INTEGER PRIMARY KEY, value DOUBLE, description VARCHAR(255) );
INSERT INTO test_table (id, value, description) VALUES (1, NULL, 'first'), (2, NULL, 'second'), (3, 1.5, 'third');
CREATE TABLE another_table ( ref_id INTEGER REFERENCES test_table(id), score DOUBLE );
INSERT INTO another_table (ref_id, score) VALUES (1, NULL), (2, 2.5), (3, NULL);
SELECT AVG(value) AS median_value FROM (SELECT value FROM test_table ORDER BY value LIMIT 2 OFFSET (SELECT COUNT(*) FROM test_table) / 2) AS subquery;
