CREATE TABLE users(id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, age INTEGER);
INSERT INTO users(name, age) VALUES('Alice', 30);
UPDATE users SET age = age + 1 WHERE name = 'Alice';
SELECT id, name, age FROM users WHERE name = 'Alice';
CREATE TABLE orders(order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DECIMAL(10, 2));
INSERT INTO orders(user_id, amount) VALUES(1, 99.99);
