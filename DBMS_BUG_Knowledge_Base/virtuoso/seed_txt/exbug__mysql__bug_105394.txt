CREATE TABLE test_strings (id INTEGER PRIMARY KEY, str_col NVARCHAR(255) NOT NULL);
INSERT INTO test_strings (str_col) VALUES ('HELLO'), ('WORLD'), ('MYSQL');
SELECT HEX(str_col), HEX(CONVERT(str_col, 'ucs2')) FROM test_strings;
UPDATE test_strings SET str_col = NULL WHERE id = 1;
SELECT id, STRLEN(str_col) AS length, HEX(CONVERT(str_col, 'ucs2')) FROM test_strings WHERE str_col IS NOT NULL;
