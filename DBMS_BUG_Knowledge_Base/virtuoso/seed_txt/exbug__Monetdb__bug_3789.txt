CREATE TABLE large_strings(id INT, data LONG VARCHAR);
INSERT INTO large_strings VALUES (1, 'a' || RPAD('b', 1000000, 'b'));
SELECT * FROM large_strings WHERE LENGTH(data) > 500000;
CREATE TABLE another_table(id INT, ref_id INT, FOREIGN KEY(ref_id) REFERENCES large_strings(id));
INSERT INTO another_table VALUES (1, 1);
