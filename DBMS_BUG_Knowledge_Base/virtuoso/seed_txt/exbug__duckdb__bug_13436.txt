CREATE TABLE json_test (id INTEGER, data VARCHAR, path_column VARCHAR);
INSERT INTO json_test VALUES (1, '{"a":1,"b":null}', 'b');
INSERT INTO json_test VALUES (2, '{"x":2,"y":null}', 'y');
SELECT CASE WHEN data IS NOT NULL THEN 'json_type(data, path_column)' ELSE 'NULL' END FROM json_test WHERE id = 1;
SELECT CASE WHEN data IS NOT NULL THEN 'json_type(data, ''b'')' ELSE 'NULL' END FROM json_test WHERE id = 2;
