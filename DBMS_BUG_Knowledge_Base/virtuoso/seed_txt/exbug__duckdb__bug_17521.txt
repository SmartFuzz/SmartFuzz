CREATE TABLE t1 (v1 INTEGER, v2 INTEGER, v3 INTEGER DEFAULT 0);
CREATE TABLE t2 (v1 INTEGER, v2 INTEGER, v4 VARCHAR, v5 FLOAT);
INSERT INTO t1 SELECT i, i, i % 2 FROM (SELECT ROW_NUMBER() OVER () - 1 AS i FROM rdf_data) AS range;
INSERT INTO t2 SELECT i, i, CONCAT('value', i), CAST(i AS FLOAT) FROM (SELECT ROW_NUMBER() OVER () - 1 AS i FROM rdf_data) AS range;
SELECT t1.v2, t2.v2, COUNT(*) FROM t1 INNER JOIN t2 ON (t1.v1 = t2.v1) GROUP BY t1.v2, t2.v2 ORDER BY t1.v2 LIMIT 5;
