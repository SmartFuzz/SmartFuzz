CREATE TABLE a (x VARCHAR(10) NOT NULL, y VARCHAR(10));
INSERT INTO a (x, y) VALUES ('aaa', 'AAA');
CREATE TABLE b (x VARCHAR(10) UNIQUE, y VARCHAR(10));
INSERT INTO b (x, y) VALUES ('aaa', 'AAA'), ('aAa', 'aAa'), ('aA', 'aA');
SELECT a.x, b.x FROM a JOIN b ON UPPER(a.y) = UPPER(b.y) WHERE UPPER(a.x) = UPPER(b.x);
