CREATE TABLE t1 (id INTEGER, name VARCHAR, age TINYINT, salary FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t1 (id, name, age, salary) VALUES (1, 'Alice', 30, 70000.50);
INSERT INTO t1 (id, name, age, salary) SELECT number, CONCAT('User', number), number % 100, number * 1000.0 FROM (SELECT ROW_NUMBER() OVER () AS number FROM DUAL LIMIT 10);
CREATE TABLE t2 (product_id INTEGER, product_name VARCHAR, price FLOAT, stock INTEGER NULL);
INSERT INTO t2 (product_id, product_name, price, stock) SELECT id, name, price, NULL FROM (SELECT number AS id, CONCAT('Product', number) AS name, number * 10.5 AS price FROM (SELECT ROW_NUMBER() OVER () AS number FROM DUAL LIMIT 5));
