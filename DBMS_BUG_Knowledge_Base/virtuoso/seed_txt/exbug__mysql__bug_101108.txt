CREATE TABLE test_table (id INTEGER PRIMARY KEY, value FLOAT, status VARCHAR(10) DEFAULT 'active');
INSERT INTO test_table (id, value) VALUES (1, NULL), (2, RAND() * 10000), (3, RAND() * 10000), (4, NULL), (5, RAND() * 10000);
SELECT id, CASE WHEN RAND() < 0.5 THEN value ELSE NULL END AS computed_value FROM test_table WHERE computed_value IS NOT NULL;
SELECT id, value FROM test_table WHERE value IS NOT NULL;
SET SESSION optimizer_switch = 'derived_merge=off';
