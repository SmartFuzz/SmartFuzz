CREATE TABLE test_table1 (id INTEGER, name VARCHAR(255), created_at TIMESTAMP) AS SELECT * FROM <your_source_table> WHERE 0;
CREATE TABLE test_table2 (user_id INTEGER, score FLOAT, load_time TIMESTAMP) AS SELECT * FROM <your_source_table> WHERE 0;
INSERT INTO test_table1 VALUES (1, 'Alice', NOW()), (2, 'Bob', NOW());
INSERT INTO test_table2 VALUES (1, 99.5, NOW()), (2, 88.0, NOW());
SELECT t1.name, t2.score FROM test_table1 t1 JOIN test_table2 t2 ON t1.id = t2.user_id WHERE t2.score > 90;
