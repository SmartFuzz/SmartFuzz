CREATE DATABASE sbtest;
CREATE TABLE ARRAY (id INTEGER NOT NULL, value VARCHAR(255) NULL, PRIMARY KEY (id));
CREATE TABLE FULL (id INTEGER NOT NULL, description LONG VARCHAR NULL, created_at TIMESTAMP DEFAULT NOW(), PRIMARY KEY (id));
CREATE TABLE INTERSECT (id INTEGER NOT NULL, amount DECIMAL(10, 2) CHECK (amount >= 0), PRIMARY KEY (id));
INSERT INTO FULL (id, description) VALUES (1, 'Test entry for FULL');
SELECT * FROM sbtest.FULL;
