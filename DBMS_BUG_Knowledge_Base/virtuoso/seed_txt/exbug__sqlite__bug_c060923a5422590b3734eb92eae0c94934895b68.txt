CREATE TABLE test_files (id INTEGER PRIMARY KEY, filename VARCHAR(255) NOT NULL, size INTEGER CHECK(size >= 0));
INSERT INTO test_files (filename, size) VALUES ('file1.txt', 100);
INSERT INTO test_files (filename, size) VALUES (NULL, 200);
UPDATE test_files SET filename = 'new_file_name_that_exceeds_the_limit' WHERE id = 1;
SELECT * FROM test_files WHERE size < 150 OR filename IS NULL;
