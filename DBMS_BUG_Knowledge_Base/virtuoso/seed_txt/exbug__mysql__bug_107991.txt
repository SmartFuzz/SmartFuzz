CREATE TABLE users (id INTEGER NOT NULL, username VARCHAR(50) NOT NULL UNIQUE, email VARCHAR(100) NOT NULL, created_at TIMESTAMP DEFAULT NOW(), PRIMARY KEY (id));
INSERT INTO users (id, username, email) VALUES (1, 'user1', 'user1@example.com');
CREATE TABLE orders (order_id INTEGER NOT NULL, user_id INTEGER NOT NULL, amount DECIMAL(10, 2) NOT NULL, order_date TIMESTAMP DEFAULT NOW(), PRIMARY KEY (order_id), FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO orders (order_id, user_id, amount) VALUES (1, 1, 99.99);
SELECT u.username, o.amount FROM users u JOIN orders o ON u.id = o.user_id WHERE o.amount > 50;
