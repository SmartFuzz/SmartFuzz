DROP SCHEMA IF EXISTS myschema;
CREATE SCHEMA myschema;
CREATE TABLE myschema.pktable (id INTEGER PRIMARY KEY, name VARCHAR(100) NOT NULL, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO myschema.pktable (id, name) VALUES (1, 'Test Name');
DELETE FROM myschema.pktable WHERE id = 1;
