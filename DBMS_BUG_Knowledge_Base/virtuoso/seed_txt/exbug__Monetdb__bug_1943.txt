CREATE TABLE test_div0 ( a INTEGER, b DOUBLE, c VARCHAR(50) );
INSERT INTO test_div0 (a, b, c) VALUES (1, 0.0, 'zero');
INSERT INTO test_div0 (a, b, c) VALUES (2, 1.0, 'one');
INSERT INTO test_div0 (a, b, c) VALUES (3, NULL, 'null_value');
SELECT MIN(a2) AS a3, SUM(b2) AS b3 FROM ( SELECT a AS a2, SUM(b) AS b2 FROM test_div0 GROUP BY a HAVING SUM(b) > 0 ) AS Sub WHERE b2 / CASE WHEN b2 = 0 THEN NULL ELSE b2 END < 0 GROUP BY a2;
