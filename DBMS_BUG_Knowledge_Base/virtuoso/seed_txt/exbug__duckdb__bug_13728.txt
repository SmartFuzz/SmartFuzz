CREATE TABLE t2 (a INTEGER, b VARCHAR, c DOUBLE, d BOOLEAN);
INSERT INTO t2 VALUES (1, 'test', 3.14, TRUE), (2, 'example', NULL, FALSE);
CREATE OR REPLACE FUNCTION unpivot_test() AS TABLE (SELECT a, b FROM t2);
SELECT * FROM unpivot_test();
CREATE OR REPLACE FUNCTION unpivot_with_null() AS TABLE (SELECT c, d FROM t2);
