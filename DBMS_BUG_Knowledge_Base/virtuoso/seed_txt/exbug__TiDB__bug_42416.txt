CREATE TABLE test_dates (id INTEGER PRIMARY KEY, event_date TIMESTAMP NOT NULL);
INSERT INTO test_dates (id, event_date) VALUES (1, TIMESTAMP '2023-03-21 12:00:00');
SELECT event_date + (3 * 86400) AS new_date FROM test_dates WHERE id = 1;
SELECT event_date + (3 * 86400) AS new_date FROM test_dates WHERE id = 1;
SELECT event_date + (DATEDIFF(NOW(), '1970-01-01') + 3) * 86400 AS new_date FROM test_dates WHERE id = 1;
