CREATE TYPE direction_state AS ENUM('UP', 'DOWN');
CREATE TABLE example (id INTEGER IDENTITY PRIMARY KEY, direction direction_state NOT NULL, below BOOLEAN NOT NULL);
ALTER TABLE example ADD CONSTRAINT ck_example_direction CHECK ((direction = 'DOWN' AND below) OR (NOT below));
ALTER TABLE example DROP CONSTRAINT ck_example_direction;
CREATE TYPE direction_state AS ENUM('UP', 'DOWN', 'UNKNOWN');
