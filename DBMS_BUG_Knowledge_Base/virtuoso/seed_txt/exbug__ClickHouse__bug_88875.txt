CREATE TABLE users (uid SMALLINT, name VARCHAR(255), age SMALLINT, email VARCHAR(255));
INSERT INTO users (uid, name, age, email) VALUES (1231, 'John', 33, NULL);
INSERT INTO users (uid, name, age, email) VALUES (6666, 'Ksenia', 48, 'ksenia@example.com');
CREATE VIEW v_users AS SELECT uid AS id, email FROM users WHERE age > 30;
SELECT id, email FROM v_users;
