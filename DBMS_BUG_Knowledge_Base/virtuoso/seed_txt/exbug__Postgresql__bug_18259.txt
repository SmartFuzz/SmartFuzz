CREATE TABLE test_table(id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test_table(name) VALUES ('test1'), ('test2'), (NULL), ('test3');
CREATE TABLE filler(a INTEGER, b VARCHAR(1000));
INSERT INTO filler SELECT g, REPEAT('x', 1000) FROM (SELECT ROW_NUMBER() OVER () AS g FROM DUAL) AS series WHERE g <= 50000;
DROP TABLE IF EXISTS filler;
