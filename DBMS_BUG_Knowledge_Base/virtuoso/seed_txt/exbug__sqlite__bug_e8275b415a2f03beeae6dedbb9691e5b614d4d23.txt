CREATE TABLE test_table (id INTEGER PRIMARY KEY, parent INTEGER, value VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, parent, value) VALUES (1, 2, 'test1'), (2, NULL, 'test2'), (3, 1, 'test3');
WITH RECURSIVE t(id, parent) AS (SELECT id, parent FROM test_table WHERE parent IS NOT NULL), q(id, parent, rn) AS (SELECT id, parent, ROW_NUMBER() OVER (ORDER BY id) AS rn FROM test_table WHERE parent IS NOT NULL UNION ALL SELECT t.id, t.parent, ROW_NUMBER() OVER (ORDER BY t.id) FROM q JOIN t ON t.parent = q.id) SELECT * FROM q;
SELECT COUNT(*) FROM test_table WHERE value IS NULL;
UPDATE test_table SET value = 'updated' WHERE id = (SELECT TOP 1 id FROM test_table WHERE value = 'test1');
