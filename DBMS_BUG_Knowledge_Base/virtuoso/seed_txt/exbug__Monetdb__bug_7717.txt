CREATE TABLE test_table ( id INT PRIMARY KEY, name VARCHAR(50) NOT NULL, age INT CHECK (age >= 0) );
INSERT INTO test_table (id, name, age) VALUES (1, 'Alice', 30);
CREATE TABLE another_table ( ref_id INT, score FLOAT CHECK (score >= 0 AND score <= 100), FOREIGN KEY (ref_id) REFERENCES test_table(id) );
INSERT INTO another_table (ref_id, score) VALUES (1, 95.5);
SELECT * FROM test_table WHERE age IS NOT NULL AND id IN (SELECT ref_id FROM another_table WHERE score > 90);
