CREATE DATABASE test_db;
CREATE TABLE users (id INTEGER PRIMARY KEY, username VARCHAR(50) NOT NULL, email VARCHAR(100) UNIQUE, created_at TIMESTAMP DEFAULT NOW());
CREATE VIEW active_users AS SELECT id, username FROM users WHERE created_at > TIMESTAMPADD(DAY, -30, NOW());
SELECT * FROM active_users;
DROP VIEW active_users;
