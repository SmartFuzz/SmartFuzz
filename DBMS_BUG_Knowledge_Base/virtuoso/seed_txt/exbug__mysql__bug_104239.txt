CREATE TABLE test_table (id INTEGER PRIMARY KEY, text_column VARCHAR(255) NOT NULL);
INSERT INTO test_table (id, text_column) VALUES (1, 'Find this pattern'), (2, NULL), (3, 'Another pattern');
CREATE FUNCTION func(input_text VARCHAR(255)) RETURNS INTEGER AS BEGIN RETURN CASE WHEN input_text IS NULL THEN NULL ELSE INSTR(input_text, 'pattern');
END;
END;
SELECT func(text_column) FROM test_table WHERE id = 1;
