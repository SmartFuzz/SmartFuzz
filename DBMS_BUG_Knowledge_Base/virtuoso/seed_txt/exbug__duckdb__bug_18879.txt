CREATE TABLE c (id INT, value VARCHAR);
INSERT INTO c (id, value) VALUES (1, 'one'), (2, 'two'), (3, 'three');
CREATE TABLE d (id INT, description VARCHAR);
INSERT INTO d (id, description) VALUES (2, 'two'), (3, 'three'), (4, 'four');
SELECT c.id, COUNT(*) AS total_count, COUNT(c.id) AS c_star, COUNT(d.id) AS d_star, COUNT(d.id) AS d_id FROM c LEFT JOIN d ON c.id = d.id GROUP BY c.id;
