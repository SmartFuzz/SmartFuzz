DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (c1 INTEGER, c2 FLOAT, c3 VARCHAR(20));
INSERT INTO t2 VALUES (1, 0.7, 'test');
SELECT c1 FROM t2 WHERE (CASE WHEN c2 > 0 THEN c1 ELSE NULL END) IS NOT NULL AND (CASE WHEN c2 > 0 THEN c1 ELSE NULL END) ^ c1;
SELECT c1 FROM t2 WHERE c1 ^ c1;
