CREATE TABLE users(id INTEGER PRIMARY KEY, name VARCHAR(100), age INTEGER, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO users VALUES (1, 'Alice', 30), (2, 'Bob', NULL), (3, 'Charlie', 25);
CREATE TABLE orders(order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DECIMAL(10, 2), FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO orders VALUES (1, 1, 100.50), (2, 2, 200.75), (3, 3, NULL);
SELECT u.name, o.amount, DB.DBA.sleep(0.1) FROM users u LEFT JOIN orders o ON u.id = o.user_id WHERE u.age IS NOT NULL;
