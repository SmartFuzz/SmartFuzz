CREATE TABLE employee_test (id INTEGER NOT NULL, name NVARCHAR(255) DEFAULT NULL, surname NVARCHAR(255) DEFAULT NULL, retired BOOLEAN DEFAULT NULL, email NVARCHAR(255) DEFAULT NULL, PRIMARY KEY(id));
INSERT INTO employee_test (id, name, surname, retired, email) VALUES (1, 'Alice', 'Johnson', FALSE, 'alice@example.com');
INSERT INTO employee_test (id, name, surname, retired, email) VALUES (2, 'Bob', 'Brown', TRUE, NULL);
SELECT name, surname FROM employee_test WHERE retired IS NULL OR retired = FALSE;
SELECT * FROM (SELECT 'žlutý_😀' AS ❤) AS 🚀 WHERE ❤ = 'žlutý_😀' GROUP BY 🚀.❤;
