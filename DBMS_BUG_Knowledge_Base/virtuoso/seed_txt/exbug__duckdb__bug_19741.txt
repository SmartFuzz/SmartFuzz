CREATE TABLE three (x INTEGER, y VARCHAR, z FLOAT, w BOOLEAN);
INSERT INTO three VALUES (1, 'test', 1.1, TRUE), (2, 'example', 2.2, FALSE);
EXPORT TO 'three.parquet' FROM three;
DROP TABLE three;
CREATE VIEW three AS SELECT * FROM DB.DBA.load_table('three.parquet');
SELECT * FROM three t1 JOIN three t2 ON t1.x = t2.x;
