CREATE TABLE messages ( id bigint NOT NULL, muid varchar(100) DEFAULT NULL, conversationId varchar(255) DEFAULT NULL, sender varchar(100) NOT NULL, receiverType varchar(10) DEFAULT 'user', receiver varchar(100) NOT NULL, PRIMARY KEY (id));
INSERT INTO messages (id, muid, conversationId, sender, receiverType, receiver) VALUES (730234878, 'msg1', 'conv1', 'user1', 'user', 'user2');
INSERT INTO messages (id, muid, conversationId, sender, receiverType, receiver) VALUES (730234879, 'msg2', 'conv2', 'user2', 'group', 'group1');
SELECT id, updatedAt FROM messages WHERE id >= 14609942 ORDER BY id LIMIT 10;
UPDATE messages SET updatedAt = NULL WHERE id = 730170668;
