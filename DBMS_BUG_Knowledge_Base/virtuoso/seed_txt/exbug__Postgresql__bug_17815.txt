CREATE TABLE test_gist (p POINT NOT NULL, t VARCHAR(255) DEFAULT 'default');
CREATE INDEX test_gist_idx ON test_gist (p);
INSERT INTO test_gist (p, t) VALUES (POINT(1, 2), NULL), (POINT(3, 4), 'text'), (POINT(5, 6), '');
SELECT * FROM test_gist WHERE t IS NOT NULL AND LENGTH(t) > 0;
DROP TABLE test_gist;
