CREATE TABLE <uitspraken> (id INTEGER NOT NULL, ljn CHAR(6) NOT NULL, gepubliceerd BOOLEAN NOT NULL, instantie VARCHAR(63) NOT NULL, datum DATE NOT NULL, publicatie DATE, zaaknummers VARCHAR(100) NOT NULL, uitspraak LONG VARBINARY, conclusie LONG VARBINARY, zittingsplaats VARCHAR(16), rechtsgebied VARCHAR(24), sector VARCHAR(20), soort VARCHAR(32), indicatie LONG VARBINARY, kop LONG VARBINARY, PRIMARY KEY (id), UNIQUE (ljn));
INSERT INTO <uitspraken> (id, ljn, gepubliceerd, instantie, datum, zaaknummers) VALUES (1, 'ABC123', TRUE, 'Instance1', DATE '2023-01-01', 'ZN12345');
INSERT INTO <uitspraken> (id, ljn, gepubliceerd, instantie, datum, zaaknummers) VALUES (2, 'DEF456', FALSE, 'Instance2', DATE '2023-01-02', 'ZN67890');
SELECT * FROM <uitspraken> WHERE gepubliceerd IS NULL;
UPDATE <uitspraken> SET datum = NULL WHERE id = 1;
