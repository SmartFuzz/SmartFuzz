CREATE TABLE user_card_info_hash_4 (id INTEGER PRIMARY KEY, name NVARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT NOW(), amount DECIMAL(10, 2) CHECK (amount >= 0));
INSERT INTO user_card_info_hash_4 (id, name, amount) SELECT ROW_NUMBER() OVER () AS id, CONCAT('User', ROW_NUMBER() OVER ()) AS name, ROUND(RAND() * 1000, 2) AS amount FROM (SELECT 1 AS dummy UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) AS numbers LIMIT 10000000;
SELECT COUNT(*) FROM user_card_info_hash_4;
COMMIT;
