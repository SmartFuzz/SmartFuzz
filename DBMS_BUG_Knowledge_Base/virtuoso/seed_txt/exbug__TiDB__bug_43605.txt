CREATE TABLE test.c (id INTEGER PRIMARY KEY, value VARCHAR(64) NOT NULL, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test.c (id, value) VALUES (1, 'x'), (2, 'y'), (3, NULL), (4, 'z');
CREATE TABLE test.d (id INTEGER, value VARCHAR(64), FOREIGN KEY (id) REFERENCES test.c(id));
INSERT INTO test.d (id, value) VALUES (1, 'a'), (2, 'b'), (3, 'c');
SELECT * FROM test.c AS t1 JOIN test.d AS t2 ON t1.id = t2.id WHERE t1.value IS NOT NULL;
