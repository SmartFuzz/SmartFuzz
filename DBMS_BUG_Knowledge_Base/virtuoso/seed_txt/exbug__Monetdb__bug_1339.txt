CREATE TABLE users (id INTEGER PRIMARY KEY, name VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, NULL);
CREATE TABLE orders AS SELECT id, name FROM users WHERE id IS NOT NULL ORDER BY id DESC;
SELECT * FROM orders;
CREATE TABLE products (product_id INTEGER, price DECIMAL(10, 2), stock INTEGER CHECK (stock >= 0));
