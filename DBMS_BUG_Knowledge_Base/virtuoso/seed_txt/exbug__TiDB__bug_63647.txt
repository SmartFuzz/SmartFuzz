CREATE TABLE t1(id INTEGER PRIMARY KEY, name VARCHAR(50), active BOOLEAN);
INSERT INTO t1 VALUES (1, 'Alice', TRUE), (2, 'Bob', FALSE);
SET x = NULL;
SET y = 1;
SELECT (CASE WHEN x IS NULL THEN NULL ELSE (CASE WHEN x = t1.id THEN NULL ELSE t1.active END) END) FROM t1 WHERE t1.id = y;
