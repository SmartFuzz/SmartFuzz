CREATE TABLE p (id INTEGER PRIMARY KEY, uid VARCHAR(36), t INTEGER DEFAULT 0);
CREATE TABLE pa (id INTEGER PRIMARY KEY, a_uid VARCHAR(36) UNIQUE);
CREATE TABLE a (id INTEGER PRIMARY KEY, uid VARCHAR(36), t INTEGER CHECK(t >= 0));
INSERT INTO p (uid, t) VALUES ('1234', 1), ('5678', 2);
INSERT INTO pa (a_uid) VALUES ('abcd-1234'), ('efgh-5678');
SELECT DISTINCT * FROM pa LEFT JOIN p ON p.uid='1234' LEFT JOIN a ON a.uid=pa.a_uid WHERE a.t IS NULL OR a.t=p.t;
