CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR, value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.5), (2, 'test2', NULL), (3, 'test3', 20.0);
SELECT DISTINCT name, (CASE WHEN (SELECT COUNT(*) FROM test_table WHERE value IS NOT NULL AND created_at > '2023-01-01') > 0 THEN 1 ELSE 0 END) AS exists_flag FROM test_table;
CREATE TABLE nested_table (ref_id INTEGER, description VARCHAR);
INSERT INTO nested_table (ref_id, description) VALUES (1, 'nested1'), (2, 'nested2');
