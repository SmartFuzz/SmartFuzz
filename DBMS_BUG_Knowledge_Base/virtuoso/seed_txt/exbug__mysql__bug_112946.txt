CREATE TABLE test.user_table_v2 (id INTEGER PRIMARY KEY, insert_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP, user VARCHAR(50) NOT NULL, action VARCHAR(20) NOT NULL, status VARCHAR(8) DEFAULT 'active');
CREATE TABLE mysql.user_table_v2 (id INTEGER PRIMARY KEY, insert_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP, user VARCHAR(50) NOT NULL, action VARCHAR(20) NOT NULL, status VARCHAR(8) DEFAULT 'active');
INSERT INTO test.user_table_v2 (user, action, status) VALUES (current_user(), 'logged in', 'active');
INSERT INTO mysql.user_table_v2 (user, action, status) VALUES (current_user(), 'logged in', 'active');
ALTER TABLE test.user_table_v2 ADD COLUMN uh_oh INTEGER DEFAULT NULL;
