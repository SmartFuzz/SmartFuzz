CREATE TABLE test_blob (id INTEGER PRIMARY KEY, data BLOB NOT NULL, description VARCHAR(255));
INSERT INTO test_blob (id, data, description) VALUES (1, CAST('Sample Blob Data 1' AS BLOB), 'First blob entry');
INSERT INTO test_blob (id, data, description) VALUES (2, CAST('Sample Blob Data 2' AS BLOB), 'Second blob entry');
SELECT data FROM test_blob ORDER BY data DESC;
SELECT id, data FROM test_blob WHERE description IS NOT NULL ORDER BY description DESC;
