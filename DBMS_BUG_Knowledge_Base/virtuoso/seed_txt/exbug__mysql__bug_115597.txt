CREATE TABLE t2 (c1 INTEGER NOT NULL, c2 INTEGER DEFAULT 0);
INSERT INTO t2 (c1, c2) VALUES (1, 1), (0, 2), (1, 3);
SELECT c1, ':', (SELECT c1 FROM t2 t2_sub WHERE t2_sub.c2 < t2.c2 ORDER BY t2_sub.c2 DESC LIMIT 1) AS lag_value FROM t2;
SELECT c1, ':', (SELECT MAX(c1) FROM t2) AS max_value FROM t2;
CREATE TABLE t3 AS SELECT c1 FROM (SELECT NULLIF(c1, 0) AS c1 FROM t2) AS subquery ORDER BY c1;
