CREATE TABLE test_table(id INTEGER, value BIGINT, flag BINARY(8), description VARCHAR(255));
INSERT INTO test_table VALUES(1, -1, 1, 'Negative one'), (2, 18446744073709551615, 1, 'Max unsigned bigint');
SELECT id, value = CAST(flag AS BIGINT), value = CAST(flag AS BIGINT) FROM test_table;
SELECT t1.id, t2.flag FROM test_table t1 JOIN test_table t2 ON t1.value = CAST(t2.flag AS BIGINT);
SELECT * FROM test_table WHERE flag IS NULL OR value IS NOT NULL;
