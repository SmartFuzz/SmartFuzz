CREATE TABLE bar (id INTEGER, name VARCHAR(50), age INTEGER CHECK (age >= 0));
INSERT INTO bar (id, name, age) VALUES (1, 'Alice', NULL);
DELETE FROM bar WHERE age IS NULL;
ALTER TABLE bar ADD CONSTRAINT age_not_null CHECK (age IS NOT NULL);
INSERT INTO bar (id, name, age) VALUES (2, 'Bob', 25);
SELECT * FROM bar;
