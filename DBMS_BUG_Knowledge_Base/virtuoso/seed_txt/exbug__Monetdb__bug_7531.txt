CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test_table (id, name) SELECT x, CONCAT('Name ', x) FROM (SELECT ROW_NUMBER() OVER () - 1 AS x FROM DB.DUAL) AS seq WHERE x <= 2500000000;
CREATE TABLE another_table (value INTEGER, description LONG VARCHAR, is_active BOOLEAN DEFAULT TRUE);
INSERT INTO another_table (value, description) SELECT id, CONCAT('Description ', id) FROM test_table WHERE id < 2147483647;
SELECT COUNT(*) FROM another_table WHERE value IS NOT NULL;
