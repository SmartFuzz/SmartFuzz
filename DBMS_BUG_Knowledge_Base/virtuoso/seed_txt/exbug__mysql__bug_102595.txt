CREATE DATABASE bug;
USE bug;
CREATE TABLE test_table (id INTEGER PRIMARY KEY, data LONG VARCHAR, name VARCHAR(255) NOT NULL);
INSERT INTO test_table (id, data, name) VALUES (1, '{"x": NULL}', 'test_name');
SELECT CASE WHEN data IS NOT NULL THEN data ELSE 'NULL' END AS data_value FROM test_table WHERE id = 1;
