CREATE TABLE nested (date TIMESTAMP, timezone VARCHAR);
INSERT INTO nested (date, timezone) VALUES (TIMESTAMP '2023-10-01 12:00:00', '-0400');
SELECT date + (CAST(SUBSTR(timezone, 1, 3) AS INTEGER) * INTERVAL '1' HOUR) FROM nested;
CREATE TABLE test_dates (event_date TIMESTAMP NOT NULL, event_timezone VARCHAR NOT NULL DEFAULT '+0000');
INSERT INTO test_dates (event_date, event_timezone) VALUES (TIMESTAMP '2023-10-01 15:00:00', '-0500');
