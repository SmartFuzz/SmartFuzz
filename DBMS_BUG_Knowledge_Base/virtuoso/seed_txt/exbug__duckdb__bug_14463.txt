CREATE TABLE test_table (id INTEGER PRIMARY KEY, arr VARCHAR(255) ARRAY NOT NULL);
INSERT INTO test_table (id, arr) VALUES (1, ARRAY['a,c', 'b']);
SELECT arr, COUNT(arr) AS arr_length FROM test_table WHERE id = 1 GROUP BY arr;
SELECT '''' || STRING_AGG(arr, ''', ''') || '''' AS escaped_arr FROM test_table;
SELECT LENGTH(ARRAY['hello, world']) AS before, LENGTH(ARRAY['hello, world']) AS after;
