CREATE TABLE t1(id INTEGER PRIMARY KEY, name VARCHAR(50) NOT NULL, age INTEGER CHECK (age >= 0), salary DECIMAL(10,2) DEFAULT 0.00);
INSERT INTO t1(name, age, salary) VALUES ('Alice', 30, 50000.00), ('Bob', NULL, 60000.00), ('Charlie', 25, NULL);
CREATE TABLE t2(department_id INTEGER, department_name VARCHAR(50), FOREIGN KEY (department_id) REFERENCES t1(id));
INSERT INTO t2(department_id, department_name) VALUES (1, 'HR'), (2, 'Engineering'), (NULL, 'Marketing');
SELECT t1.name, t1.salary FROM t1 LEFT JOIN t2 ON t1.id = t2.department_id WHERE t1.age IS NOT NULL AND (t1.salary > 40000 OR t2.department_name IS NULL);
