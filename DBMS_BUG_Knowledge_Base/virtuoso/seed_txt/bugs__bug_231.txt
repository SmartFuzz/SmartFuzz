CREATE TABLE proteins (id INT PRIMARY KEY, featureId VARCHAR(255), sequence TEXT, annotation IRI);
INSERT INTO proteins (id, featureId, sequence, annotation) VALUES (1, 'PRO_0000024369', 'MKTAYIAKQRQISFVKSHFSRQD', IRI('http://purl.uniprot.org/annotation/PRO_0000024369'));
CREATE TABLE ranges (id INT PRIMARY KEY, begin INT, end INT, protein_id INT, FOREIGN KEY (protein_id) REFERENCES proteins(id));
INSERT INTO ranges (id, begin, end, protein_id) VALUES (1, 1, 10, 1);
SELECT ?fasta WHERE { BIND(IRI(concat('http://purl.uniprot.org/annotation/', 'PRO_0000024369')) AS ?featureIRI) { ?protein up:annotation ?featureIRI;
up:sequence/rdf:value ?sequence. ?featureIRI up:range ?range. ?range up:begin ?begin;
up:end ?end. BIND(substr(?sequence, ?begin, ?end - ?begin + 1) AS ?seq). BIND(substr(str(?protein), 33) AS ?primAcc). BIND(concat('>', ?primAcc, '#', 'PRO_0000024369', '\\n', ?seq) AS ?fasta) } } LIMIT 10 OFFSET 0;
