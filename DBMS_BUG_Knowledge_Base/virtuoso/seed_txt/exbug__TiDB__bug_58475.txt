DROP TABLE IF EXISTS tp;
CREATE TABLE tp (id INTEGER, c1 VARCHAR(255), c2 VARCHAR(255), c3 INTEGER DEFAULT 0, PRIMARY KEY (id));
INSERT INTO tp (id, c1, c3) VALUES (1, '[1]', 10);
INSERT INTO tp (id, c1) VALUES (0, '[2]');
SELECT id FROM tp WHERE CONTAINS(c2, '1') OR c3 IS NOT NULL GROUP BY id HAVING id IN (0, 1);
