DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INTEGER, name VARCHAR, age INTEGER, score FLOAT, created_at TIMESTAMP);
INSERT INTO test_table (id, name, age, score, created_at) VALUES (1, 'Alice', 25, 88.5, CURRENT_TIMESTAMP), (2, 'Bob', 30, 92.0, CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, age, score, created_at) SELECT number, CAST(number AS VARCHAR), number % 100, number / 10.0, CURRENT_TIMESTAMP FROM (SELECT ROW_NUMBER() OVER () AS number FROM DUAL LIMIT 5);
SELECT id FROM test_table WHERE name = 'Bob' AND age < 30;
