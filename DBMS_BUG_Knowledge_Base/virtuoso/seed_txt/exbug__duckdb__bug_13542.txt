CREATE TABLE items (id INTEGER PRIMARY KEY, name VARCHAR, quantity INTEGER, created_at TIMESTAMP);
INSERT INTO items (id, name, quantity, created_at) VALUES (1, 'item1', 10, TIMESTAMP '2024-01-01 10:00:00');
INSERT INTO items (id, name, quantity, created_at) VALUES (2, 'item2', 5, TIMESTAMP '2024-01-01 10:05:00');
SELECT id, (CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END) AS has_stock FROM items WHERE quantity > 0 GROUP BY id HAVING has_stock;
SELECT id, (CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END) AS has_stock FROM items WHERE quantity > 0 GROUP BY id HAVING NOT has_stock;
