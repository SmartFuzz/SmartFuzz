CREATE TABLE t2 (id INTEGER PRIMARY KEY, name VARCHAR(100) NOT NULL, age INTEGER, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO t2 (name, age) VALUES ('Alice', 30);
INSERT INTO t2 (name, age) VALUES ('Bob', NULL);
SELECT name, age FROM t2 WHERE age IS NULL OR age < 25;
SELECT COUNT(*) FROM t2 WHERE age >= 20 AND age <= 40;
