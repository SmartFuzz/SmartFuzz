CREATE TABLE a(id INTEGER, new_name VARCHAR(255) NOT NULL, value DOUBLE DEFAULT 0.0);
CREATE TABLE b(id INTEGER, a_id INTEGER, FOREIGN KEY(a_id) REFERENCES a(id));
INSERT INTO a(new_name, value) VALUES('test', NULL);
CREATE VIEW c AS SELECT a.id, b.id AS b_id FROM a LEFT JOIN b ON a.id = b.a_id;
ALTER TABLE a RENAME new_name TO name;
