CREATE TABLE test_table (id INTEGER IDENTITY, value VARCHAR(255));
INSERT INTO test_table (value) VALUES (''), (''), (NULL);
SELECT CASE WHEN COUNT(value) = 0 THEN '' ELSE CAST(STRING_AGG(value, '') AS VARCHAR) END AS concatenated FROM test_table;
INSERT INTO test_table (value) VALUES ('a'), ('b'), (NULL);
SELECT CASE WHEN COUNT(value) = 0 THEN '' ELSE CAST(STRING_AGG(value, '') AS VARCHAR) END AS concatenated FROM test_table WHERE value IS NOT NULL;
