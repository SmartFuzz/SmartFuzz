CREATE GRAPH <test>;
CREATE TABLE <test>.<foo> (key_var int NOT NULL, value_var2 float);
CREATE TABLE <test>.<bar> (key_var int NOT NULL, value_var1 varchar(255));
INSERT INTO <test>.<foo> VALUES (1, 630.5);
INSERT INTO <test>.<bar> VALUES (1, 'eleven');
SELECT * FROM <test>.<foo> INNER JOIN <test>.<bar> ON <foo>.key_var = <bar>.key_var;
