CREATE TABLE election_results (id INT PRIMARY KEY, refPeriod xsd:gYear, party_name VARCHAR(255), votes INT);
INSERT INTO election_results (id, refPeriod, party_name, votes) VALUES (1, '2006'^^xsd:gYear, 'Party A', 1500);
INSERT INTO election_results (id, refPeriod, party_name, votes) VALUES (2, '2007'^^xsd:gYear, 'Party B', 2000);
SELECT id, refPeriod, party_name FROM election_results WHERE refPeriod = '2006'^^xsd:gYear;
SELECT COUNT(*) FROM election_results WHERE votes IS NULL OR votes < 0;
