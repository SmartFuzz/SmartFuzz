CREATE VIRTUAL TABLE test_table USING fts5(content);
INSERT DATA { (graph:orders) { (graph:orders) <product> 'apple';
<quantity> 10 . } (graph:orders) <product> 'banana';
<quantity> 5 . } (graph:orders) <product> 'cherry';
<quantity> 15 . };
CREATE TABLE orders(id INTEGER IDENTITY PRIMARY KEY, product VARCHAR(255) NOT NULL, quantity INTEGER DEFAULT 0);
