CREATE TABLE test_table_v2 (id INTEGER PRIMARY KEY, name VARCHAR(50) NOT NULL, age INTEGER CHECK (age >= 0), created_at TIMESTAMP DEFAULT NOW());
INSERT INTO test_table_v2 (name, age) VALUES ('Alice', 25);
INSERT INTO test_table_v2 (name, age) VALUES ('Bob', NULL);
CREATE INDEX idx_age_v2 ON test_table_v2(age);
SELECT COUNT(*) FROM test_table_v2 WHERE age IS NOT NULL;
