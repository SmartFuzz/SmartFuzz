CREATE TABLE test_table (id INTEGER IDENTITY PRIMARY KEY, name NVARCHAR(255) NOT NULL, age INTEGER CHECK(age >= 0));
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
INSERT INTO test_table (name, age) VALUES ('Bob', NULL);
SELECT * FROM test_table WHERE age IS NULL;
CREATE PROCEDURE invalid_procedure AS BEGIN SELECT * FROM non_existent_table;
END;
