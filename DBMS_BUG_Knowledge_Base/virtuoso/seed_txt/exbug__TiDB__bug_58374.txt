CREATE TABLE testPartition2 ( ID INTEGER PRIMARY KEY, OrderDate TIMESTAMP, Status VARCHAR(10) DEFAULT 'Pending' );
INSERT INTO testPartition2 (ID, OrderDate) VALUES (1, NULL);
INSERT INTO testPartition2 (ID, OrderDate, Status) VALUES (2, TIMESTAMP '2024-01-01 00:00:00', 'Completed');
SELECT * FROM testPartition2 WHERE OrderDate IS NULL;
SELECT COUNT(*) FROM testPartition2 WHERE Status = 'Pending' OR OrderDate IS NULL;
