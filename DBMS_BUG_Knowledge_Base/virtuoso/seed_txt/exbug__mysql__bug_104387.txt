CREATE TABLE colltest2 ( id INTEGER NOT NULL PRIMARY KEY, cbin VARBINARY(140), ctxt TEXT, cstr VARCHAR(255), cnum INTEGER DEFAULT NULL);
INSERT INTO colltest2 (cbin, ctxt, cstr, cnum) VALUES (CAST('asdf' AS VARBINARY), 'asdf', 'asdf', NULL);
SELECT * FROM colltest2 WHERE REGEXP(cbin, cstr);
SELECT * FROM colltest2 WHERE REGEXP(cbin, ctxt);
SELECT * FROM colltest2 WHERE cnum IS NULL;
