CREATE TABLE Bar (id INTEGER PRIMARY KEY, description VARCHAR, created_at TIMESTAMP);
INSERT INTO Bar (id, description, created_at) VALUES (1, 'First entry', NOW());
CREATE TABLE Baz (id INTEGER PRIMARY KEY, value FLOAT, bar_id INTEGER, FOREIGN KEY (bar_id) REFERENCES Bar(id));
INSERT INTO Baz (id, value, bar_id) VALUES (1, 3.14, 1);
SELECT b.id, b.description, COUNT(z.id) FROM Bar b LEFT JOIN Baz z ON b.id = z.bar_id GROUP BY b.id, b.description;
