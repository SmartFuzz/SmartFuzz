CREATE TABLE test1 (a ARRAY<Integer>, b Integer);
INSERT INTO test1 SELECT number % 10, number FROM (SELECT ROW_NUMBER() OVER () AS number FROM <your_data_source> LIMIT 100);
CREATE TABLE test2 (x FLOAT, y VARCHAR);
INSERT INTO test2 SELECT CAST(number AS FLOAT), CAST(number AS VARCHAR) FROM (SELECT ROW_NUMBER() OVER () AS number FROM <your_data_source> LIMIT 50);
CREATE TABLE test3 (id Integer, data ARRAY<VARCHAR>);
