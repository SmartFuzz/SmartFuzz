CREATE TABLE t (a integer, b varchar(255));
CREATE TABLE tp (a integer PRIMARY KEY, c boolean NOT NULL DEFAULT true);
INSERT INTO t (a, b) VALUES (0, 'example');
CREATE UNIQUE INDEX t_a_idx ON t (a, b);
SELECT * FROM t WHERE a BETWEEN 0 AND 1000;
