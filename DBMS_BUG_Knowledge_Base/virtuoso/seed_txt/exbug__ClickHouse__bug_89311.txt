CREATE TABLE default.test_v2 (list_id VARCHAR(36), item VARCHAR(255), created_at TIMESTAMP, price FLOAT, is_active BOOLEAN);
INSERT INTO default.test_v2 (list_id, item, created_at, price, is_active) SELECT UUID(), 'item_' || CAST(ROW_NUMBER() OVER() AS VARCHAR), CURRENT_TIMESTAMP, RANDOM() * 100, TRUE FROM (SELECT ROW_NUMBER() OVER() AS rn FROM rdf::get_all()) LIMIT 100000;
CREATE INDEX prj_list_id_created_at ON default.test_v2 (list_id, created_at);
SELECT * FROM default.test_v2 WHERE list_id = '019a369d-0a96-7fbd-853c-a4335fd2ee9f' ORDER BY created_at LIMIT 100 OFFSET 6000;
