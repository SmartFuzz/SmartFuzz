CREATE TABLE test_json (id INTEGER PRIMARY KEY, a VARCHAR(4000), b VARCHAR(255) NOT NULL);
INSERT INTO test_json (a, b) VALUES (CAST('{"key1": "base64:type15:VQ=="}' AS VARCHAR(4000)), 'test_value');
SELECT CASE WHEN a IS NOT NULL THEN 'STRING' ELSE 'NULL' END AS json_type FROM test_json WHERE b = 'test_value';
SELECT json_extract(a, '$.key1') AS extracted_value, extracted_value = 'base64:type15:VQ==' FROM test_json;
SELECT * FROM test_json WHERE a IS NULL;
