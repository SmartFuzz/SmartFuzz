CREATE TABLE s1 (id INTEGER PRIMARY KEY, name VARCHAR(50));
CREATE TABLE s2 (id INTEGER PRIMARY KEY, description LONG VARCHAR, created_at TIMESTAMP DEFAULT NOW());
CREATE TABLE s3 (x INTEGER CHECK (x > 0), y FLOAT DEFAULT NULL);
INSERT INTO s1 (id, name) VALUES (1, 'Alice'), (2, 'Bob');
SELECT * FROM s3 WHERE x IN (SELECT id FROM s1 WHERE name IS NOT NULL);
