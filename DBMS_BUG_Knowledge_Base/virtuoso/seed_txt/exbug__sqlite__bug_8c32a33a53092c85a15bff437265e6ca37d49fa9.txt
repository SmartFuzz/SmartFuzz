CREATE TABLE test_table (id INTEGER PRIMARY KEY, value VARCHAR(255) NOT NULL, flag BOOLEAN DEFAULT 0);
INSERT INTO test_table (value, flag) VALUES ('test', TRUE);
SELECT CASE WHEN value IS NULL THEN TRUE ELSE FALSE END AS is_null FROM test_table WHERE id = 1;
SELECT CASE WHEN 1 IS NULL THEN TRUE ELSE FALSE END AS is_null;
SELECT value FROM test_table WHERE flag = TRUE AND value IS NOT NULL;
