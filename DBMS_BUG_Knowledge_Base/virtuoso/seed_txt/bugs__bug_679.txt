CREATE TABLE test_images (id INT PRIMARY KEY, image_data BLOB, description VARCHAR(255));
INSERT INTO test_images (id, image_data, description) VALUES (1, NULL, 'Test image 1');
INSERT INTO test_images (id, image_data, description) VALUES (2, CAST('image_data_2' AS BLOB), 'Test image 2');
SELECT "IM XYtoMorton"(id, 1) FROM test_images WHERE image_data IS NOT NULL;
SELECT "IM XYtoMorton"(1, 1) FROM test_images WHERE id = 1;
