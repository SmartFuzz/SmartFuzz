CREATE TABLE tc (id INTEGER PRIMARY KEY, name VARCHAR(64) NOT NULL, age INTEGER DEFAULT 0);
INSERT INTO tc (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', NULL);
CREATE TABLE td (id INTEGER, name VARCHAR(64), age INTEGER);
INSERT INTO td (id, name, age) SELECT id, name, age FROM tc WHERE age IS NOT NULL;
SELECT * FROM td;
