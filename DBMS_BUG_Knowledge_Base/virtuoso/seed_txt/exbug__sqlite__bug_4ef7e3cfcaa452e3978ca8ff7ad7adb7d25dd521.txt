CREATE TABLE a_table(id INTEGER, value VARCHAR(255) NOT NULL, PRIMARY KEY(id));
CREATE TABLE b_table(id INTEGER, ref_id INTEGER, FOREIGN KEY(ref_id) REFERENCES a_table(id), PRIMARY KEY(id));
CREATE TRIGGER after_insert_a AFTER INSERT ON a_table INSERT INTO b_table(ref_id) SELECT id FROM a_table WHERE value IS NULL;
INSERT INTO a_table(value) VALUES('test');
INSERT INTO a_table(value) VALUES(NULL);
SELECT ref_id, typeof(ref_id) FROM b_table;
