CREATE DATABASE test_db;
CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(50), age INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', NULL), (3, 'Charlie', 25);
SELECT id, name, age FROM test_table ORDER BY age;
SELECT * FROM test_table;
