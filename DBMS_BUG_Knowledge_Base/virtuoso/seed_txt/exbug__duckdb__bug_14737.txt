CREATE TABLE test_table (id INTEGER PRIMARY KEY, value INTEGER NOT NULL);
INSERT INTO test_table (id, value) VALUES (1, 10), (2, 20), (3, 10), (4, 30);
SELECT DISTINCT value AS distinct_values FROM test_table ORDER BY value;
SELECT DISTINCT value % 3 AS mod_values FROM test_table ORDER BY id;
SELECT DISTINCT CASE WHEN value > 15 THEN 'high' ELSE 'low' END AS categories FROM test_table ORDER BY value;
