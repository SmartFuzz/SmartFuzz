CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, age INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
INSERT INTO test_table (name, age) VALUES (NULL, 25);
SELECT * FROM test_table WHERE age > (SELECT AVG(age) FROM test_table);
UPDATE test_table SET name = 'Bob' WHERE id = (SELECT id FROM test_table WHERE name IS NULL);
