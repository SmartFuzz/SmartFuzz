CREATE TABLE users (id INTEGER PRIMARY KEY, username NVARCHAR(255) NOT NULL, email NVARCHAR(255) UNIQUE);
INSERT INTO users (username, email) VALUES ('testUser', 'test@example.com');
CREATE TABLE comments (id INTEGER PRIMARY KEY, user_id INTEGER, comment NVARCHAR(255), FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO comments (user_id, comment) VALUES (1, 'This is a comment');
SELECT * FROM users WHERE UPPER(username) = UPPER('TESTuser');
