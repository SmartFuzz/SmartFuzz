CREATE TABLE equipment (EquipmentName VARCHAR(255), FailureReason VARCHAR(255));
INSERT INTO equipment (EquipmentName, FailureReason) VALUES ('Excavator', 'Hydraulic Failure'), ('Bulldozer', 'Engine Overheat');
CREATE VIEW v_resolve_maintenance_action AS SELECT * FROM equipment WHERE EXISTS (SELECT 1 FROM equipment e WHERE e.EquipmentName = equipment.EquipmentName AND e.FailureReason = equipment.FailureReason);
SELECT * FROM equipment e LEFT JOIN v_resolve_maintenance_action ON e.EquipmentName = v_resolve_maintenance_action.EquipmentName AND e.FailureReason = v_resolve_maintenance_action.FailureReason;
CREATE TABLE maintenance_actions (ActionId INT, ActionDescription VARCHAR(255));
