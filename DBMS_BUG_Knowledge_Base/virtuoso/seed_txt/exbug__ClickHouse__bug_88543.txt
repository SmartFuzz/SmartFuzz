CREATE TABLE test_vectors (id INTEGER, text VARCHAR, embeddings BLOB, attributes VARCHAR);
INSERT INTO test_vectors (id, text, embeddings, attributes) VALUES (1, 'vector1', CAST(ARRAY[0.1, 0.2, 0.3] AS BLOB), 'attr1');
INSERT INTO test_vectors (id, text, embeddings) VALUES (2, 'vector2', CAST(ARRAY[0.4, 0.5, 0.6] AS BLOB));
SELECT * FROM test_vectors WHERE attributes IS NOT NULL;
SELECT text, UNNEST(embeddings) AS embedding_value FROM test_vectors WHERE id = 1;
