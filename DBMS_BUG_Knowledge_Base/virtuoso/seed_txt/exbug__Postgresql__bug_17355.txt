CREATE TABLE async_pt (a int, b int, c text DEFAULT 'default' NOT NULL);
CREATE TABLE base_tbl2 (a int, b int, c varchar(255) UNIQUE);
CREATE TABLE async_p2 AS SELECT * FROM base_tbl2 WHERE a BETWEEN 2000 AND 3000;
INSERT INTO async_pt SELECT i, CAST(i AS text) FROM (SELECT 3000 + ROW_NUMBER() OVER () - 1 AS i FROM db.dummy_table) AS seq LIMIT 101;
DELETE FROM async_pt WHERE a IS NOT NULL;
