CREATE TABLE test_uuid (id INT PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_uuid (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
SELECT id, name, (UUID() AS uuid) FROM test_uuid WHERE id IN (SELECT id FROM test_uuid WHERE name LIKE 'A%');
SELECT DISTINCT (STRUUID() AS str_uuid), name FROM test_uuid WHERE created_at > NOW() - INTERVAL '1 DAY';
SELECT id, (STRUUID() AS uuid) FROM test_uuid WHERE id = (SELECT MAX(id) FROM test_uuid);
