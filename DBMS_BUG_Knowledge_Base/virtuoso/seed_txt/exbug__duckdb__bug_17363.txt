CREATE TABLE test_map(id int, attr VARCHAR);
INSERT INTO test_map VALUES (1, 'key1:value1'), (2, 'key2:value2');
SELECT id, CASE WHEN CONTAINS(attr, 'key3') THEN SPLIT(attr, ':')[1] ELSE NULL END AS value FROM test_map;
SELECT id, CASE WHEN CONTAINS(attr, 'key3') THEN SPLIT(attr, ':')[1] ELSE NULL END AS value FROM test_map;
CREATE TABLE test_union(id int, value VARCHAR);
