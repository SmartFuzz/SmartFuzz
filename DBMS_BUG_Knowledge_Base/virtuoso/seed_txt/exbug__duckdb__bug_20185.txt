CREATE TABLE bar (id INTEGER PRIMARY KEY, name VARCHAR, value FLOAT, created_at TIMESTAMP, is_active BOOLEAN);
INSERT INTO bar (id, name, value, created_at, is_active) VALUES (1, 'Event A', 10.5, TIMESTAMP '2023-01-01 10:00:00', TRUE);
INSERT INTO bar (id, name, value, created_at, is_active) VALUES (2, 'Event B', 20.0, TIMESTAMP '2023-01-02 11:00:00', FALSE);
SELECT name, value FROM (SELECT ? AS name, ? AS value FROM (SELECT id, name, value, created_at FROM bar WHERE is_active = TRUE ORDER BY created_at DESC) AS t GROUP BY name LIMIT 1);
SELECT name, value FROM (SELECT ? AS name, ? AS value FROM (SELECT id, name, value FROM bar) AS t GROUP BY name ORDER BY value DESC LIMIT 1);
