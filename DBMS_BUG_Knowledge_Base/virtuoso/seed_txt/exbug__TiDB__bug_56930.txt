CREATE TABLE articles (id INTEGER PRIMARY KEY, content LONG VARCHAR, summary VARCHAR(255), UNIQUE (content, summary));
ALTER TABLE articles SET CHARACTER SET utf8;
INSERT INTO articles (content, summary) VALUES ('This is a test article with a long content that exceeds normal limits.', 'Short summary');
SELECT * FROM articles WHERE LENGTH(content) > 100;
CREATE TABLE comments (id INTEGER PRIMARY KEY, article_id INTEGER, comment LONG VARCHAR, FOREIGN KEY (article_id) REFERENCES articles(id));
