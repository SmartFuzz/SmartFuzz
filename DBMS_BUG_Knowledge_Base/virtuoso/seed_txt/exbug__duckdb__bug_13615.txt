CREATE TABLE tab2 (x int, y float, z varchar(10));
INSERT INTO tab2 (x, y, z) VALUES (3, 2.5, 'test1');
INSERT INTO tab2 (x, y, z) VALUES (7, NULL, 'test2');
CREATE PROCEDURE s3(x int) AS BEGIN SELECT SUM(y) OVER() FROM (SELECT x AS y) END;
SELECT s3(x), SUM(x) OVER() FROM tab2;
