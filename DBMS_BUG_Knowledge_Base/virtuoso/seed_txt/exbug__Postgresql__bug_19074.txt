CREATE TABLE p (a integer, b text);
CREATE TABLE c (a integer, b text) AS SELECT * FROM p WHERE 1=0;
INSERT INTO p (a, b) VALUES (1, 'test');
INSERT INTO c (a, b) VALUES (1, 'test');
SELECT * FROM c WHERE a IS NOT NULL;
