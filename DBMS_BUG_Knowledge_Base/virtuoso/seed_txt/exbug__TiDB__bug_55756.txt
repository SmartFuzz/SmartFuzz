CREATE TABLE test_time (time_col TIME, id INTEGER IDENTITY(1,1) PRIMARY KEY);
INSERT INTO test_time (time_col) VALUES ('10:30:00'), ('15:45:00'), ('20:15:00'), (NULL), ('23:59:59');
CREATE TABLE test_subquery (sub_time TIME, sub_id INTEGER);
INSERT INTO test_subquery (sub_time, sub_id) SELECT time_col, id FROM test_time WHERE time_col IS NOT NULL;
SELECT sub_id FROM test_subquery WHERE sub_time IN (SELECT time_col FROM test_time WHERE time_col < '12:00:00');
