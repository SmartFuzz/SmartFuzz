CREATE TABLE test_json (id INTEGER PRIMARY KEY, data NVARCHAR(MAX));
INSERT INTO test_json (id, data) VALUES (1, '"a":1,"b":null');
INSERT INTO test_json (id, data) VALUES (2, '[1, 2, null]');
SELECT data AS extracted_b FROM test_json WHERE id = 1;
SELECT CASE WHEN data LIKE '%"b":null%' THEN 'null' ELSE data END AS coalesced_b FROM test_json WHERE id = 1;
