CREATE TABLE t_proj_test1 (id xsd:integer, name xsd:string, value xsd:double, created_at xsd:dateTime);
INSERT INTO t_proj_test1 SELECT number, CONCAT('Name', CAST(number AS xsd:string)), number * 1.5, NOW() FROM (SELECT ROW_NUMBER() OVER () AS number FROM DB.DUAL) WHERE number <= 10000;
CREATE TABLE t_proj_test2 (user_id xsd:integer, score xsd:integer, timestamp xsd:date);
INSERT INTO t_proj_test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, CURRENT_DATE FROM (SELECT ROW_NUMBER() OVER () AS number FROM DB.DUAL) WHERE number <= 5000;
SELECT user_id, SUM(score) AS total_score FROM t_proj_test2 GROUP BY user_id ORDER BY total_score DESC;
