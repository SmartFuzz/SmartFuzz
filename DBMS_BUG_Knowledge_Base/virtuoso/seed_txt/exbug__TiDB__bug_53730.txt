CREATE TABLE users (id INTEGER PRIMARY KEY, username VARCHAR(30), email VARCHAR(50) NOT NULL);
INSERT INTO users (id, username, email) VALUES (1, 'user1', 'user1@example.com'), (2, 'user2 ', 'user2@example.com'), (3, 'user3', 'user3@example.com');
CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DECIMAL(10, 2), FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO orders (order_id, user_id, amount) VALUES (101, 1, 99.99), (102, 2, 150.00), (103, 1, 200.50);
SELECT u.id, u.username FROM users u WHERE TRIM(u.username) = 'user1' AND LENGTH(u.username) = 5;
