CREATE TABLE test_events (id INTEGER PRIMARY KEY, event_name VARCHAR(255) NOT NULL, event_time TIMESTAMP NOT NULL, is_active BOOLEAN DEFAULT TRUE);
INSERT INTO test_events (event_name, event_time) VALUES ('Test Event 1', TIMESTAMPADD(SECOND, 1, NOW()));
INSERT INTO test_events (event_name, event_time) VALUES ('Scheduled Event', NOW());
SELECT * FROM test_events WHERE is_active = TRUE;
DELETE FROM test_events WHERE event_time < NOW();
