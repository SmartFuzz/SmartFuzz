CREATE TABLE test.data_types (id INTEGER PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, amount DECIMAL(10, 2) NOT NULL);
INSERT INTO test.data_types (id, name, amount) VALUES (1, 'Test', 100.50);
CREATE PROCEDURE test.exampleProcedure(a INTEGER, b VARCHAR(50), c DECIMAL(10, 2), d TIMESTAMP) { SELECT a, b, c, d;
};
CALL test.exampleProcedure(1, 'Sample', NULL, CURRENT_TIMESTAMP);
SELECT * FROM test.data_types WHERE amount > (SELECT AVG(amount) FROM test.data_types);
