CREATE TABLE nested_structs (id INTEGER, data VARCHAR);
INSERT INTO nested_structs VALUES (1, 'item1,100');
INSERT INTO nested_structs VALUES (2, 'item2,NULL');
SELECT id, SPLIT(data, ',')[0] AS name, SPLIT(data, ',')[1] AS value FROM nested_structs WHERE SPLIT(data, ',')[1] IS NOT NULL;
SELECT id, SPLIT(data, ',')[0] AS name, COALESCE(CAST(SPLIT(data, ',')[1] AS INTEGER), 0) AS value FROM nested_structs;
