DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (id INTEGER, name VARCHAR(255), description LONG VARCHAR, created_at TIMESTAMP DEFAULT NOW());
INSERT INTO t2 (id, name, description) VALUES (1, 'Test', REPEAT('x', 600)), (2, 'Sample', REPEAT('y', 700));
SELECT id, STR_AGG(name) AS concatenated_names FROM t2 GROUP BY id;
SELECT * FROM (SELECT id, STR_AGG(description) AS concatenated_descriptions FROM t2 GROUP BY id) AS a1 UNION SELECT 3, 'Extra data';
