DROP TABLE IF EXISTS job_sample;
CREATE TABLE job_sample (id INTEGER, posted_at DATE, discovered_at TIMESTAMP WITH TIME ZONE, url VARCHAR, title VARCHAR, company_name VARCHAR, description VARCHAR);
INSERT INTO job_sample SELECT * FROM <your_s3_source>;
ALTER TABLE job_sample ADD COLUMN description_matches_python INTEGER DEFAULT 0;
UPDATE job_sample SET description_matches_python = CASE WHEN description LIKE '%Python%' THEN 1 ELSE 0 END;
SELECT company_name, COUNT(*) AS initial_matches FROM job_sample WHERE description_matches_python = 1 GROUP BY company_name ORDER BY initial_matches DESC LIMIT 10;
