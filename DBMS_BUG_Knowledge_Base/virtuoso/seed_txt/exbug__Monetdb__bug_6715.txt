CREATE TABLE ProductTarget (ProductID INTEGER IDENTITY PRIMARY KEY, Name VARCHAR(100) NOT NULL, ProductNumber BIGINT, Color VARCHAR(30) NOT NULL);
CREATE TABLE ProductSource (ProductID INTEGER IDENTITY PRIMARY KEY, Name VARCHAR(100) NOT NULL, ProductNumber BIGINT, Color VARCHAR(30) NOT NULL);
INSERT INTO ProductSource (Name, ProductNumber, Color) VALUES ('Widget', 1001, 'Red');
INSERT INTO ProductTarget (Name, ProductNumber, Color) VALUES ('Gadget', NULL, 'Blue');
UPDATE ProductTarget SET Name = (SELECT S.Name FROM ProductSource S WHERE S.ProductID = ProductTarget.ProductID), Color = COALESCE((SELECT S.Color FROM ProductSource S WHERE S.ProductID = ProductTarget.ProductID), 'Unknown') WHERE EXISTS (SELECT 1 FROM ProductSource S WHERE S.ProductID = ProductTarget.ProductID);
