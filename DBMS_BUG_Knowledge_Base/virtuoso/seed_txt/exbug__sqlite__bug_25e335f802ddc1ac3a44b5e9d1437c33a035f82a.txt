CREATE TABLE users (id INTEGER, name VARCHAR(255) NOT NULL, PRIMARY KEY (id));
INSERT INTO users (name) VALUES ('Alice');
INSERT INTO users (name) VALUES ('Bob');
CREATE TABLE orders (order_id INTEGER, user_id INTEGER, amount FLOAT, PRIMARY KEY (order_id), FOREIGN KEY (user_id) REFERENCES users(id));
SELECT * FROM users LEFT JOIN orders ON users.id = orders.user_id WHERE orders.amount IS NULL;
