CREATE TABLE test_data (id INTEGER PRIMARY KEY, name VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, name) VALUES (1, 'Sample Data');
SELECT * FROM test_data WHERE id = 1;
CREATE SECRET my_test_secret (TYPE 'postgres', HOST 'localhost', PORT 5432, DATABASE 'test_db', USER 'user', PASSWORD 'password');
SELECT * FROM DB.DBA.SECRETS;
