CREATE TABLE test_table (id INT, status BOOLEAN, description VARCHAR);
INSERT INTO test_table VALUES (1, true, 'active'), (2, false, 'inactive'), (3, true, 'running');
SELECT CASE WHEN status THEN 'Status is active' ELSE CAST(0 AS VARCHAR) END FROM test_table;
SELECT CASE WHEN status THEN 'Active' ELSE NULL END AS result FROM test_table;
SELECT CASE WHEN id > 1 THEN 'Greater than one' ELSE 'One or less' END FROM test_table;
