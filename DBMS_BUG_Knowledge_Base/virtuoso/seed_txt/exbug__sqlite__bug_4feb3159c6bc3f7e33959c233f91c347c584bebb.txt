CREATE TABLE test_table (id INTEGER IDENTITY PRIMARY KEY, value INTEGER, category VARCHAR(255));
INSERT INTO test_table (value, category) VALUES (1, 'A'), (2, 'B'), (3, 'A'), (NULL, 'C');
SELECT value, SUM(value) AS category_sum FROM test_table GROUP BY category ORDER BY category_sum;
SELECT SUM(value) AS total_sum FROM test_table WHERE value IS NOT NULL;
SELECT SUM(value) AS total_sum FROM test_table ORDER BY total_sum;
