{
  "url": "https://github.com/ClickHouse/ClickHouse/issues/86227",
  "title": "Alias, Order by and Distributed tables does not work properly together",
  "vulnerable_database": "ClickHouse",
  "date": "2025-08-26T16:43:08Z",
  "vulnerable_version": "",
  "description": "### Describe what's wrong\nOrder by used on a distributed table with an alias seems to shuffle the columns.\n### Does it reproduce on the most recent release?\nYes -  v25.7.2\n### How to reproduce\nTable setup\n```sql\nCREATE TABLE shard_Test ON CLUSTER `{cluster}`\n(\n`a` String,\n`b` Float64,\n`c` Float64,\n`d` Float64,\n`e` Float64 ALIAS b + c,\n`f` Float64 ALIAS c + d\n)\nENGINE = MergeTree\nORDER BY a;\nCREATE TABLE Test ON CLUSTER `{cluster}`\n(\n`a` String,\n`b` Float64,\n`c` Float64,\n`d` Float64,\n`e` Float64 ALIAS b + c,\n`f` Float64 ALIAS c + d\n)\nENGINE =  Distributed('{cluster}', 'default', 'shard_Test', rand());\nINSERT INTO Test VALUES('a', 1,2,3);\n```\nSimple query\n```sql\nSELECT\ne,\nf\nFROM Test\n┌─e─┬─f─┐\n1. │ 3 │ 5 │\n└───┴───┘\n```\nQuery with order by\n```sql\nSELECT\ne,\nf\nFROM Test\nORDER BY a ASC\n┌─e─┬─f─┐\n1. │ 5 │ 3 │\n└───┴───┘",
  "poc": [
    "CREATE TABLE shard_Test ON CLUSTER `{cluster}`\n(\n    `a` String,\n    `b` Float64,\n    `c` Float64,\n    `d` Float64,\n    `e` Float64 ALIAS b + c,\n    `f` Float64 ALIAS c + d\n)\nENGINE = MergeTree\nORDER BY a;",
    "CREATE TABLE Test ON CLUSTER `{cluster}`\n(\n    `a` String,\n    `b` Float64,\n    `c` Float64,\n    `d` Float64,\n    `e` Float64 ALIAS b + c,\n    `f` Float64 ALIAS c + d\n)\nENGINE =  Distributed('{cluster}', 'default', 'shard_Test', rand());",
    "INSERT INTO Test VALUES('a', 1,2,3);"
  ],
  "key_bug_features": [
    "Order by on distributed table with alias shuffles columns"
  ],
  "bug_features_seed": [
    "CREATE TABLE shard_Test ON CLUSTER `{cluster}` ( `a` String, `b` Float64, `c` Float64, `d` Float64, `e` Float64 ALIAS b + c, `f` Float64 ALIAS c + d, `g` Nullable(Int32) ) ENGINE = MergeTree ORDER BY a;",
    "CREATE TABLE Test ON CLUSTER `{cluster}` ( `a` String, `b` Float64, `c` Float64, `d` Float64, `e` Float64 ALIAS b + c, `f` Float64 ALIAS c + d, `g` Nullable(Int32) ) ENGINE = Distributed('{cluster}', 'default', 'shard_Test', rand());",
    "INSERT INTO Test VALUES('a', 1, 2, 3, NULL), ('b', 4, 5, 6, 10), ('c', 7, 8, 9, 15);",
    "SELECT e, f FROM Test WHERE g IS NULL ORDER BY a ASC;",
    "SELECT e, f FROM Test ORDER BY e DESC;"
  ]
}