{
  "url": "https://www.sqlite.org/src/tktview/b47e3627ec",
  "title": "Comparison of row values results in incorrect result",
  "description": "mrigger added on 2019-10-21 14:45:34:\nConsider the following test case:\nCREATE TABLE t0(c0 COLLATE NOCASE, c1);\nINSERT INTO t0 VALUES('a', 'A');\nSELECT * FROM t0 WHERE (+ t0.c1, 1) >= (t0.c0, 1); -- expected: row is not fetched, actual: row is fetched\nI would not expect the row to be fetched, because the expression should evaluate to TRUE:\nSELECT (+ t0.c1, 1) >= (t0.c0, 1) FROM t0; -- 0\nmrigger added on 2019-10-22 10:43:16:\nIt seems that the check-in did not fix this issue-I detected the same bug again, and can still reproduce the issue on the current trunk.",
  "date": "2019-10-21 14:45:345.81 years ago",
  "vulnerable_version": "3.30.0",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE t1(c0 String, c1 Int32, c2 Float64);",
    "INSERT INTO t1 VALUES('a', 1, 1.0);",
    "INSERT INTO t1 VALUES('A', 2, 2.0);",
    "SELECT * FROM t1 WHERE (toInt32(t1.c1), 1) >= (toString(t1.c0), 1);",
    "SELECT (toInt32(t1.c1), 1) >= (toString(t1.c0), 1) FROM t1;"
  ],
  "key_bug_features": [
    "Comparison of row values using collations"
  ]
}