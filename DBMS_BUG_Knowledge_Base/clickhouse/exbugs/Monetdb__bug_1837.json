{
  "url": "https://github.com/MonetDB/MonetDB/issues/1837",
  "title": "SQL: Aborting process",
  "description": "",
  "date": "2020-11-30T10:03:18Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE scene (id Int32, victim_id Int32 NOT NULL, description String) ENGINE = MergeTree() ORDER BY id;",
    "CREATE TABLE victim (id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;",
    "CREATE TABLE inspector (id Int32, name String, experience_years Int32 DEFAULT 0) ENGINE = MergeTree() ORDER BY id;",
    "INSERT INTO scene (id, victim_id, description) VALUES (1, 1, 'Crime Scene A');",
    "SELECT i.name, v1.name, v2.name FROM scene s JOIN scene b ON s.id <> b.id JOIN victim v1 ON v1.id = s.victim_id JOIN victim v2 ON v2.id = b.victim_id JOIN inspector i ON 1=1;"
  ],
  "key_bug_features": [
    "Invalid SQL join condition causing process abortion"
  ]
}