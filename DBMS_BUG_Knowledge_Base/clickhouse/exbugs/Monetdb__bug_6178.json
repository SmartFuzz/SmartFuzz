{
  "url": "https://github.com/MonetDB/MonetDB/issues/6178",
  "title": "AVG + GROUP BY returns NULL for some records that should have results",
  "description": "",
  "date": "2020-11-30T15:30:15Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE flights (tailnum String, arr_delay Int32, flight_date Date);",
    "INSERT INTO flights SELECT * FROM file('c:\\Users\\anthonyd\\Desktop\\flights.csv', 'CSV', 'tailnum String, arr_delay Int32, flight_date Date', 'NULL');",
    "SELECT tailnum, AVG(arr_delay) FROM flights WHERE arr_delay IS NOT NULL GROUP BY tailnum;",
    "SELECT tailnum, AVG(arr_delay) FROM flights GROUP BY tailnum HAVING AVG(arr_delay) IS NOT NULL;",
    "SELECT * FROM (SELECT tailnum, AVG(arr_delay) AS avg_delay FROM flights GROUP BY tailnum) AS avg_flights WHERE avg_delay > 0;"
  ],
  "key_bug_features": [
    "AVG with GROUP BY returns NULL for valid records"
  ]
}