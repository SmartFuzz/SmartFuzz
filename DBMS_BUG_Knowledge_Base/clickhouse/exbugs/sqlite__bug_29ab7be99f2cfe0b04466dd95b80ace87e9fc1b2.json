{
  "url": "https://www.sqlite.org/src/tktview/29ab7be99f",
  "title": "Hitting NOT NULL constraint does not roll back statement transaction",
  "description": "未找到User Comments标签",
  "date": "2009-09-09 10:31:2715.92 years ago",
  "vulnerable_version": "3.6.17",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users (id UInt32, name String NOT NULL, age Nullable(UInt32), email String) ENGINE = MergeTree() ORDER BY id;",
    "INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');",
    "INSERT INTO users (name, age) VALUES ('Bob', NULL);",
    "ALTER TABLE users UPDATE email = NULL WHERE id = 1;",
    "SELECT * FROM users WHERE age > (SELECT AVG(age) FROM users WHERE email IS NOT NULL);"
  ],
  "key_bug_features": [
    "NOT NULL constraint fails during transaction execution"
  ]
}