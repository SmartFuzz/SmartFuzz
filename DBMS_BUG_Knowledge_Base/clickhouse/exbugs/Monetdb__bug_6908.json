{
  "url": "https://github.com/MonetDB/MonetDB/issues/6908",
  "title": "SQLancer inputs not the same size",
  "description": "",
  "date": "2020-11-30T16:58:30Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE t1(id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;",
    "INSERT INTO t1 VALUES (1, 'Alice', 30), (2, 'Bob', NULL), (3, 'Charlie', -5);",
    "CREATE VIEW v1 AS SELECT id, name FROM t1 WHERE age IS NOT NULL;",
    "SELECT v1.name, (SELECT COUNT(*) FROM t1 WHERE age < 0) AS negative_age_count FROM v1;",
    "SELECT * FROM t1 WHERE name LIKE '%' || CAST(NULL AS String) || '%';"
  ],
  "key_bug_features": [
    "Mismatched input sizes in SQLancer queries"
  ]
}