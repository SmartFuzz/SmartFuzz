{
  "url": "https://github.com/MonetDB/MonetDB/issues/3629",
  "title": "IF THEN ELSEIF always evaluates the first test as true",
  "description": "",
  "date": "2020-11-30T12:57:47Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE test_table (id Int32, flag UInt8, description String);",
    "INSERT INTO test_table (id, flag, description) VALUES (1, 1, 'First entry'), (2, 0, 'Second entry'), (3, NULL, 'Third entry');",
    "CREATE FUNCTION evaluate_flag(b UInt8) RETURNS String AS (CASE WHEN b = 1 THEN 'true' WHEN b = 0 THEN 'false' ELSE 'null' END);",
    "SELECT id, evaluate_flag(flag) AS evaluation FROM test_table;",
    "SELECT evaluate_flag(NULLIF(flag, 0)) AS adjusted_evaluation FROM test_table;"
  ],
  "key_bug_features": [
    "IF THEN ELSEIF evaluates first condition as true always"
  ]
}