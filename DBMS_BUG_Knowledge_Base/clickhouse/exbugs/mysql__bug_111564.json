{
  "url": "https://bugs.mysql.com/bug.php?id=111564",
  "title": "Contribution by Tencent: explain format=tree lost the subquery in the hash join",
  "description": "Plan of subquery in hash join condition is missing.\nHow to repeat:\ncreate table t1 (a int not null, b int not null);\r\ncreate table t2 (a decimal(15,2) not null, b int not null);\r\n\r\nexplain format=tree\r\nselect * from t1, t2 where t2.a = (SELECT MIN(t2.a) FROM t2 WHERE t1.a = t2.a);\r\n\r\nexplain format=tree\r\nselect * from t1 left join t2 on t1.b = t2.b AND t2.a < (SELECT MIN(t2.a) FROM t2 WHERE t1.a = t2.a);",
  "date": "2023-06-26 12:33:00",
  "vulnerable_version": "8.0",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "mysql",
  "bug_features_seed": [
    "CREATE TABLE t3 (x Int32 NOT NULL, y String NOT NULL, z Date);",
    "CREATE TABLE t4 (m Decimal(10,2) NOT NULL, n Int32 DEFAULT 0, o DateTime NOT NULL DEFAULT now());",
    "INSERT INTO t3 (x, y, z) VALUES (1, 'test', '2023-01-01');",
    "INSERT INTO t4 (m, n) VALUES (100.50, NULL);",
    "SELECT * FROM t3 JOIN t4 ON t3.x = (SELECT MAX(m) FROM t4 WHERE n IS NULL) WHERE t3.z < today();"
  ],
  "key_bug_features": [
    "explain format=tree loses subquery in hash join"
  ]
}