{
  "url": "https://www.sqlite.org/src/tktview/11d5aa455e",
  "title": "sqlite3_trace() sometimes does not invoke its callback",
  "description": "drh added on 2014-08-19 19:57:38:\nThe callback to sqlite3_trace() is suppose to be invoked for every SQL statement\nthat is run by the VDBE.  But sometimes the callback will be suppressed.  If\nthe prepared statement is marked as expired (which can happen, for example,\nif the RHS of an expression like \"x GLOB ?1\" is rebound and X is an indexed\ntext column) then the callback is not invoked.  This can be seen in the shell\nby running:\n.trace stderr\n.tables\nThe SELECT statement that implements the \".tables\" command is not traced.\nThis problem appears to have existed since 2012-10-03, version 3.7.15 and\ncheck-in [39f763bfc04174ee0fe2cdf6a92].  The problem was discovered during\ninternal testing and has not been observed in the wild.",
  "date": "2014-08-19 19:57:3810.98 years ago",
  "vulnerable_version": "3.8.6",
  "poc_type": "sql_text",
  "vulnerable_database": "ClickHouse",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_table (id UInt32, name String, value Float64, created_at DateTime DEFAULT now());",
    "INSERT INTO test_table (name, value) VALUES ('example', 10.5);",
    "ALTER TABLE test_table UPDATE value = NULL WHERE id = 1;",
    "SELECT * FROM test_table WHERE name LIKE 'example';",
    "SELECT * FROM test_table WHERE value > (SELECT avg(value) FROM test_table);"
  ],
  "key_bug_features": [
    "Callback suppression when prepared statement is marked as expired"
  ]
}