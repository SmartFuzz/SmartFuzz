CREATE TABLE t_tmp (a Int32, b String, c DateTime DEFAULT now());
CREATE INDEX idx_a ON t_tmp (a);
CREATE TEMPORARY TABLE t_tmp_temp AS SELECT * FROM t_tmp;
INSERT INTO t_tmp_temp (a, b) VALUES (1, 'test'), (2, NULL), (3, 'example');
SELECT * FROM system.parts WHERE table = 't_tmp_temp';
