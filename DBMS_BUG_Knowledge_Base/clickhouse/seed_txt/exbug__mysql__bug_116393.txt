CREATE TABLE test_hashing (id UInt32, data String, created_at DateTime DEFAULT now());
INSERT INTO test_hashing (id, data) VALUES (1, CONCAT('Test', toString(cityHash64('Sample Data')))), (2, NULL), (3, CONCAT('Boundary', toString(cityHash64('Edge Case'))));
SELECT id, toString(cityHash64(data)) AS md5_hash FROM test_hashing WHERE data IS NOT NULL;
SELECT COUNT(*) FROM test_hashing WHERE data IS NOT NULL;
SELECT count() FROM test_hashing WHERE data IS NOT NULL;
