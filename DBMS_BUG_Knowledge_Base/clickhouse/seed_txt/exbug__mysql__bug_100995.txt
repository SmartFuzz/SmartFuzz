CREATE TABLE t3 (a Int32 NOT NULL, b Float64 NOT NULL, c String DEFAULT NULL) ENGINE = MergeTree() ORDER BY a;
INSERT INTO t3 VALUES (1, 1.5, 'test1');
INSERT INTO t3 VALUES (2, 2.5, 'test2');
SELECT * FROM t3 WHERE (a NOT IN (1.1, -1.1));
SELECT * FROM t3 WHERE (a NOT IN (2.0, -2.0));
