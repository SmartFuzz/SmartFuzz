CREATE TABLE t1 ( a Int32, b String, c Date DEFAULT NULL );
INSERT INTO t1 VALUES (1, 'X', '2023-01-01'), (2, 'Y', NULL), (3, 'Z', '2023-01-03');
CREATE TABLE t2 ( a Int32, d Float64 DEFAULT 0.0 ) ENGINE = MergeTree() ORDER BY a;
INSERT INTO t2 VALUES (1, 1.5), (2, 2.5), (3, NULL);
SELECT t2.a, SUM(COALESCE(t1.b, '')) AS total FROM t1 JOIN t2 ON t1.a = t2.a GROUP BY t2.a HAVING t2.a IS NOT NULL OR total < 5;
