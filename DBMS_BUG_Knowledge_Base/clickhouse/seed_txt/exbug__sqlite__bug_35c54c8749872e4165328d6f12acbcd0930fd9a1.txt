CREATE TABLE test_table (id UInt32, name String NOT NULL, age Nullable(UInt32) CHECK(age >= 0), created_at DateTime DEFAULT now());
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
INSERT INTO test_table (name, age) VALUES ('Bob', NULL);
SELECT * FROM test_table WHERE age IS NULL OR age < 18;
ALTER TABLE test_table UPDATE age = age + 1 WHERE name = 'Alice';
