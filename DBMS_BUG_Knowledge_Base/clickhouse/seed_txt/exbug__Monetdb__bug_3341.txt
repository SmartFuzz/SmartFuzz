CREATE TABLE orders (o_custkey Int32, o_orderdate Date, o_amount Decimal(10, 2), o_status String DEFAULT 'pending');
INSERT INTO orders (o_custkey, o_orderdate, o_amount, o_status) VALUES (1, '2023-01-01', 100.00, 'completed'), (2, NULL, 200.50, 'pending'), (NULL, '2023-01-03', 150.75, 'shipped');
CREATE TABLE customers (c_custkey Int32, c_name String, c_email String) ENGINE = MergeTree() ORDER BY c_custkey;
INSERT INTO customers (c_custkey, c_name, c_email) VALUES (1, 'Alice', 'alice@example.com'), (2, 'Bob', NULL);
SELECT o_custkey, o_amount, rank() OVER (PARTITION BY o_custkey ORDER BY o_amount DESC) AS rank FROM orders;
