CREATE TABLE test_data (id UInt32, url String, format Enum('JSON', 'CSV'));
INSERT INTO test_data (id, url, format) VALUES (1, 'http://example.com/data.json', 'JSON');
INSERT INTO test_data (id, url, format) VALUES (2, 'http://example.com/data.csv', 'CSV');
SELECT id, url, format, if(format = 'JSON', readJSON(url), readCSV(url)) AS data FROM test_data;
CREATE TABLE processed_data AS SELECT id, url, format FROM test_data WHERE url IS NOT NULL;
