CREATE TABLE authors (id UInt32, name String NOT NULL, email String NOT NULL) ENGINE = MergeTree() ORDER BY id;
ALTER TABLE authors ADD CONSTRAINT unique_email UNIQUE (email);
CREATE TABLE books (id UInt32, title String NOT NULL, publication_date Date NOT NULL, author_id UInt32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO authors (name, email) VALUES ('John Doe', 'john@example.com');
INSERT INTO books (title, publication_date, author_id) VALUES ('Sample Book', '2023-01-01', 1);
SELECT * FROM books WHERE author_id IS NULL;
