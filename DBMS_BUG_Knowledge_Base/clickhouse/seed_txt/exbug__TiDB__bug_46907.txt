DROP TABLE IF EXISTS test_complex;
CREATE TABLE test_complex (id Int32, name String, created_at DateTime) ENGINE = MergeTree() ORDER BY id PARTITION BY intDiv(id, 100);
ALTER TABLE test_complex ADD INDEX idx_name (name) TYPE minmax;
INSERT INTO test_complex VALUES (1, 'Alice', now()), (2, 'Bob', now()), (150, 'Charlie', now());
ALTER TABLE test_complex DROP PARTITION 0, 1;
