CREATE TABLE Employees (ID UInt32, Name String NOT NULL, Age UInt32 CHECK(Age >= 18), Salary Float64 DEFAULT 0.0) ENGINE = MergeTree() ORDER BY ID;
INSERT INTO Employees VALUES (1, 'Alice', 30, 50000.0);
INSERT INTO Employees VALUES (2, 'Bob', 25, NULL);
UPDATE Employees SET Salary = Salary * 1.1 WHERE Age > 28;
SELECT Name, Salary FROM Employees WHERE Salary IS NOT NULL ORDER BY Salary DESC;
