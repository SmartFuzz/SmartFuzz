CREATE TABLE json_test (id UInt32, data String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO json_test (id, data) VALUES (1, '{"const": false}');
INSERT INTO json_test (id, data) VALUES (2, '{"$ref": "#/$defs/test"}');
SELECT if(JSONHas(data, 'const') AND JSONExtractBool(data, 'const') = 0, 1, 0) FROM json_test WHERE id = 1;
SELECT if(JSONHas(data, '$ref'), 1, 0) FROM json_test WHERE id = 2;
