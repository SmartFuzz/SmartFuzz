CREATE TABLE departments (id Int32 NOT NULL, name String NOT NULL, budget Decimal(10,2) NOT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO departments (id, name, budget) VALUES (1, 'HR', 100000.00), (2, 'Engineering', 250000.00), (3, 'Sales', 150000.00);
CREATE TABLE employees (id Int32 NOT NULL, fname String NOT NULL, lname String NOT NULL, store_id Int32 NOT NULL, department_id Int32 NOT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO employees (fname, lname, store_id, department_id) VALUES ('Alice', 'Smith', 1, 1), ('Bob', 'Johnson', 2, 2), ('Charlie', 'Brown', 3, 3);
SELECT * FROM employees;
