CREATE TABLE test_table (id UInt32, name String NOT NULL, age Nullable(UInt32) DEFAULT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
INSERT INTO test_table (name, age) VALUES ('Bob', NULL);
DROP TABLE test_table;
SELECT * FROM test_table WHERE age IS NOT NULL;
