CREATE TABLE tab1 (a Int32 NOT NULL, b String DEFAULT 'default');
INSERT INTO tab1 (a, b) VALUES (1, NULL), (2, 'text'), (3, 'another text');
SELECT a FROM tab1 WHERE a IN (SELECT a FROM tab1);
CREATE TABLE ftab1 ENGINE = MergeTree() ORDER BY a AS SELECT a FROM tab1;
CREATE TABLE ftab2 ENGINE = MergeTree() ORDER BY a AS SELECT * FROM ftab1;
