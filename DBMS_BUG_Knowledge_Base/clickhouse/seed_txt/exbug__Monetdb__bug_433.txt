CREATE TABLE a (id Int32 NOT NULL, val Int32, unique_val Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO a (id, val, unique_val) VALUES (1, 10, 100);
INSERT INTO a (id, val, unique_val) VALUES (2, 20, 200);
ALTER TABLE a ADD CONSTRAINT unique_val_unique UNIQUE (unique_val);
UPDATE a SET unique_val = 100 WHERE id = 2;
SELECT * FROM a;
