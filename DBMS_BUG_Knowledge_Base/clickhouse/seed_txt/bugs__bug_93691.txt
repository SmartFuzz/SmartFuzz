CREATE TABLE test_enum1 (status Enum('active', 'inactive', 'pending'=1, 'suspended'=2) ENGINE=MergeTree() ORDER BY tuple();
CREATE TABLE test_enum2 (level Enum('low', 'medium'=10, 'high'=20) ENGINE=MergeTree() ORDER BY tuple();
INSERT INTO test_enum1 (status) VALUES ('active'), ('inactive'), ('pending');
INSERT INTO test_enum2 (level) VALUES ('low'), ('medium'), ('high');
SELECT * FROM test_enum1 WHERE status = 'pending';
