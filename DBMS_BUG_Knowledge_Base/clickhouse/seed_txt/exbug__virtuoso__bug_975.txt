CREATE TABLE nextprot.isoform_bases (id String NOT NULL, entry String NOT NULL, PRIMARY KEY(id));
CREATE TABLE nextprot.isoform_cellular_components (isoform String NOT NULL, annotation String, PRIMARY KEY(isoform, annotation));
INSERT INTO nextprot.isoform_bases (id, entry) VALUES ('isoform1', 'entry1'), ('isoform2', 'entry2');
INSERT INTO nextprot.isoform_cellular_components (isoform, annotation) VALUES ('isoform1', 'annotation1'), ('isoform2', NULL);
SELECT * FROM nextprot.isoform_bases b JOIN nextprot.isoform_cellular_components c ON b.id = c.isoform WHERE c.annotation IS NOT NULL;
