CREATE TABLE t2 (id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE t3 (id Int32, description String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
ALTER TABLE t2 ADD INDEX idx_name (name) TYPE minmax;
ALTER TABLE t3 ADD INDEX idx_description (substring(description, 1, 5)) TYPE minmax;
INSERT INTO t2 (id, name, age) VALUES (1, 'Alice', 30);
SELECT * FROM t2 WHERE age IS NULL OR age > 25;
