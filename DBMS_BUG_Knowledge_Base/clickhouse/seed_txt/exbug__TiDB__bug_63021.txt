CREATE TABLE products (id UInt64 NOT NULL, name String NOT NULL, price Decimal(10, 2) NOT NULL) ENGINE = MergeTree() ORDER BY id;
ALTER TABLE products ADD COLUMN category_id UInt64 NOT NULL;
CREATE INDEX idx_category_id ON products (category_id);
INSERT INTO products (name, price, category_id) VALUES ('Product A', 19.99, 1);
ALTER TABLE products UPDATE price = NULL WHERE id = 1;
SELECT * FROM products WHERE category_id IS NOT NULL;
