CREATE TABLE nested_structs (id UInt32, data Nested(name String, value Nullable(Int32)));
INSERT INTO nested_structs VALUES (1, ('item1', 100));
INSERT INTO nested_structs VALUES (2, ('item2', NULL));
SELECT id, data.name, data.value FROM nested_structs WHERE data.value IS NOT NULL;
SELECT id, data.name, COALESCE(data.value, 0) AS value FROM nested_structs;
