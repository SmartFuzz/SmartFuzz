DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data (id UInt64, value String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_data SELECT number, toString(number) FROM numbers(100);
SELECT id, value FROM test_data WHERE id % 2 = 0;
ALTER TABLE test_data ADD COLUMN updated_at DateTime DEFAULT now();
