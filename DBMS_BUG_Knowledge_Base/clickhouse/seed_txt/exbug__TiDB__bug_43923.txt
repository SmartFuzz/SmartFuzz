CREATE TABLE test.t2(c1 Int32, c2 String, c3 Date, c4 Float32 DEFAULT NULL);
INSERT INTO test.t2 VALUES(1, 'A', '2023-01-01', 1.1), (2, 'B', NULL, 2.2), (3, 'C', '2023-03-03', 3.3), (4, 'D', '2023-04-04', NULL);
CREATE TABLE test.t3(c1 Int32, c2 Int32) ENGINE = MergeTree() ORDER BY c1;
INSERT INTO test.t3 SELECT c1, COUNT(*) FROM test.t2 GROUP BY c1;
SELECT c1, rowNumberInAllBlocks() OVER(PARTITION BY c2 ORDER BY c1) FROM (SELECT c1, c2 FROM test.t2 WHERE c3 IS NOT NULL) AS subquery;
