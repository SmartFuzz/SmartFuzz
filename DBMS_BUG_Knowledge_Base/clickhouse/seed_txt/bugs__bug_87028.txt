CREATE TABLE test.user_table (id Int64, name String);
CREATE TABLE datamart.some_table (id Nullable(Int64), description String);
CREATE MATERIALIZED VIEW test.user_mv TO test.user_table AS SELECT id, description FROM datamart.some_table WHERE id IS NOT NULL;
INSERT INTO datamart.some_table (id, description) VALUES (1, 'First entry'), (NULL, 'Second entry');
INSERT INTO test.user_table (id, name) VALUES (1, 'User One');
