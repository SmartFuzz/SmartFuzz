CREATE TABLE employees (id UInt32 NOT NULL, name String NOT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO employees (id, name) VALUES (1, 'Alice');
INSERT INTO employees (id, name) VALUES (8192, 'Bob');
SELECT COUNT(*) FROM employees WHERE id IS NULL;
CREATE TABLE departments (dept_id UInt32 NOT NULL, dept_name String NOT NULL) ENGINE = MergeTree() ORDER BY dept_id;
