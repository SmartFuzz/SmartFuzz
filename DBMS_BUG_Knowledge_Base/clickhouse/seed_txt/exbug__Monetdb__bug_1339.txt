CREATE TABLE users (id Int32, name String, created_at DateTime DEFAULT now());
INSERT INTO users (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, NULL);
CREATE TABLE orders AS SELECT id, name FROM users WHERE id IS NOT NULL ORDER BY id DESC;
SELECT * FROM orders;
CREATE TABLE products (product_id Int32, price Decimal(10, 2), stock Int32) ENGINE = MergeTree() ORDER BY product_id;
