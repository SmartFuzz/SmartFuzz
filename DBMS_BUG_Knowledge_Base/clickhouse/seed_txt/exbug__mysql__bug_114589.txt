CREATE TABLE tt2 (id UInt32 NOT NULL, name String NOT NULL, time DateTime DEFAULT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO tt2 (name, time) VALUES ('aa', '2023-03-01 00:00:00'), ('bb', '2023-03-02 00:00:00'), ('cc', NULL), ('aa', '2023-03-03 00:00:00'), ('bb', '2023-03-04 00:00:00');
SELECT DISTINCT t1.name, toString(toDate(t1.time)) FROM (SELECT * FROM tt2) t1 WHERE t1.time IS NOT NULL ORDER BY t1.name ASC, toString(toDate(t1.time)) ASC;
SELECT DISTINCT t1.name, toString(toDate(t1.time)) AS formatted_time FROM (SELECT * FROM tt2) t1 ORDER BY t1.name ASC, formatted_time ASC;
SELECT DISTINCT name, toString(toDate(time)) FROM tt2 WHERE time IS NOT NULL ORDER BY name ASC, toString(toDate(time)) ASC;
