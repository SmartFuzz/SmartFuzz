CREATE TABLE test_table (id UInt32, name String, value Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (name, value) VALUES ('A', 10), ('B', NULL), ('C', -5);
CREATE VIEW test_view AS SELECT name, value FROM test_table WHERE value IS NOT NULL;
SELECT name, value FROM test_view GROUP BY name, value WITH CUBE;
SELECT * FROM test_view WHERE value IS NOT NULL OR name IS NULL;
