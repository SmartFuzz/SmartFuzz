CREATE TABLE t3(id Int32 PRIMARY KEY, name String NOT NULL, age Int32 CHECK (age >= 0));
CREATE TABLE t4(id Int32, name String, salary Decimal(10,2)) ENGINE = MergeTree() PARTITION BY salary < 3000 ORDER BY id;
ALTER TABLE t3 ADD CONSTRAINT chk_age CHECK (age <= 100);
INSERT INTO t4(id, name, salary) VALUES (1, 'Alice', 2500.00), (2, 'Bob', 5500.50);
SELECT name, salary FROM t4 WHERE salary > 3000;
