CREATE TABLE orders ( order_id UInt32, product_id UInt32, client_id UInt32, order_date DateTime, PRIMARY KEY (order_id), FOREIGN KEY (product_id) REFERENCES products(product_id) ON DELETE CASCADE ) ENGINE = MergeTree() ORDER BY order_id;
INSERT INTO orders (product_id, client_id, order_date) VALUES (1, 4, now());
INSERT INTO orders (product_id, client_id, order_date) SELECT product_id, client_id, now() FROM products WHERE client_id IS NOT NULL;
SELECT o.order_id, p.payload1 FROM orders o JOIN products p ON o.product_id = p.product_id WHERE o.client_id = 4;
ALTER TABLE products UPDATE payload1 = NULL WHERE client_id = 4;
