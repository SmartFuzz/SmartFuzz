DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (id Int32, name String, description String, created_at DateTime DEFAULT now());
INSERT INTO t2 (id, name, description) VALUES (1, 'Test', repeat('x', 600)), (2, 'Sample', repeat('y', 700));
SELECT id, arrayStringConcat(groupArray(name)) AS concatenated_names FROM t2 GROUP BY id;
SELECT id, arrayStringConcat(groupArray(description)) AS concatenated_descriptions FROM t2 GROUP BY id UNION ALL SELECT 3, 'Extra data';
