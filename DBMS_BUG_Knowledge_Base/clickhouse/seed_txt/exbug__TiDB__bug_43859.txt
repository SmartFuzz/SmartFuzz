DROP TABLE IF EXISTS t2;
CREATE TABLE t2(id Int32, name String, age Int32) ENGINE = MergeTree() PRIMARY KEY id;
INSERT INTO t2 (id, name, age) SELECT * FROM file('./a.dat', 'CSVWithNames');
INSERT INTO t2 (id, name, age) SELECT * FROM file('./b.dat', 'CSVWithNames');
SELECT * FROM t2 WHERE age IS NULL;
