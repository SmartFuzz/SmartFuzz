CREATE TABLE users (id UInt32, name String, age UInt32, email String) ENGINE = MergeTree() ORDER BY id;
CREATE INDEX idx_users_name ON users(name) TYPE minmax;
INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');
INSERT INTO users (name, age, email) VALUES ('Bob', 25, 'bob@example.com');
SELECT DISTINCT name FROM users WHERE id IN (1, 2);
