CREATE TABLE test1(id Int32, name String, created_at DateTime DEFAULT now());
INSERT INTO test1(id, name) VALUES (1, 'Alice'), (2, 'Bob');
CREATE DATABASE IF NOT EXISTS risk;
USE risk;
SELECT * FROM test1 WHERE id = 1;
