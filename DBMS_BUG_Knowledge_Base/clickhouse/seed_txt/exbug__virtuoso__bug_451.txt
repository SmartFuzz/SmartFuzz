CREATE TABLE graph1 (subject String, predicate String, object String) ENGINE = MergeTree() ORDER BY subject;
CREATE TABLE graph2 (subject String, predicate String, object String) ENGINE = MergeTree() ORDER BY subject;
INSERT INTO graph1 VALUES ('<urn:subject1>', 'rdfs:label', 'Subject one');
INSERT INTO graph2 VALUES ('<urn:subject2>', 'rdfs:label', 'Subject two');
SELECT DISTINCT subject FROM graph1;
