CREATE TABLE test_table (id UInt32 NOT NULL, name String NOT NULL, age Nullable(UInt32), created_at DateTime DEFAULT now(), PRIMARY KEY (id));
INSERT INTO test_table (id, name, age) VALUES (1, 'Alice', 30);
INSERT INTO test_table (id, name, age) VALUES (2, 'Bob', NULL);
UPDATE test_table SET age = age + 1 WHERE name = 'Alice';
SELECT * FROM test_table WHERE age IS NULL OR age > 25;
