CREATE TABLE products (id Int32, name String, price Float64, created_at DateTime) ENGINE = MergeTree() ORDER BY id;
INSERT INTO products VALUES (1, 'Laptop', 999.99, now());
INSERT INTO products VALUES (2, 'Smartphone', 499.99, now());
SELECT id, avg(price),  -- trailing comma
FROM products
GROUP BY id,      -- trailing comma
;
SELECT id, count(*)
FROM products
GROUP BY id,  -- trailing comma
created_at;
