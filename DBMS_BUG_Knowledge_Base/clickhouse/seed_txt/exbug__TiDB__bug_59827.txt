DROP TABLE IF EXISTS t;
CREATE TABLE t (a String NOT NULL, b String NOT NULL, c Int32 NOT NULL DEFAULT 0, d DateTime DEFAULT NULL) ENGINE = MergeTree() ORDER BY (a, b) PARTITION BY b;
INSERT INTO t (a, b, c, d) VALUES ('b', '2', 1, now());
INSERT INTO t (a, b, c, d) VALUES ('b', '1', 2, NULL);
EXPLAIN SELECT * FROM t WHERE a = 'b' AND (b = '2');
