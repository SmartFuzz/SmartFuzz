CREATE TABLE test_enum ( id Int32 NOT NULL, status Enum8('active' = 1, 'inactive' = 2, 'pending' = 3) NOT NULL, PRIMARY KEY (id) ) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_enum (status) VALUES ('active'), ('inactive'), ('pending');
UPDATE test_enum SET status = 'unknown' WHERE id = 1;
SELECT * FROM test_enum WHERE status = (SELECT status FROM test_enum WHERE id = 2);
CREATE TABLE test_null ( id Int32 NOT NULL, value Nullable(String), PRIMARY KEY (id) ) ENGINE = MergeTree() ORDER BY id;
