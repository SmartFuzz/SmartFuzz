CREATE TABLE Albums (id UInt32, artist String, releaseDate Date, recordLabel String);
INSERT INTO Albums (id, artist, releaseDate, recordLabel) VALUES (1, 'Elvis Presley', '1956-01-01', 'RCA'), (2, 'Elvis Presley', '1957-01-01', 'RCA'), (3, 'Elvis Presley', NULL, 'RCA');
SELECT recordLabel FROM (SELECT DISTINCT recordLabel, releaseDate FROM Albums WHERE artist = 'Elvis Presley' AND releaseDate IS NOT NULL) ORDER BY releaseDate ASC LIMIT 1;
SELECT recordLabel FROM (SELECT DISTINCT recordLabel, releaseDate FROM Albums WHERE artist = 'Elvis Presley') ORDER BY releaseDate ASC LIMIT 1;
SELECT recordLabel FROM (SELECT DISTINCT recordLabel, releaseDate FROM Albums WHERE artist = 'Elvis Presley' AND (releaseDate IS NULL OR releaseDate < '1957-01-01')) ORDER BY releaseDate ASC LIMIT 1;
