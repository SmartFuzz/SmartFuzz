CREATE TABLE v1 ( id Int32, value String NOT NULL ) ENGINE = MergeTree() ORDER BY id;
INSERT INTO v1 (id, value) VALUES (1, 'test'), (2, NULL), (3, 'example');
CREATE TABLE v2 ( a Float64, b Int32 DEFAULT 0, c Date );
INSERT INTO v2 (a, b, c) VALUES (NULL, 1, '2023-01-01'), (5.5, 2, NULL), (3.14, NULL, '2023-01-03');
DELETE FROM v2 WHERE ( a IS NULL OR ( SELECT COUNT() FROM ( SELECT a, DENSE_RANK() OVER ( PARTITION BY b ORDER BY a ) AS rank FROM v2 ) WHERE rank < 2 ) > 0 );
