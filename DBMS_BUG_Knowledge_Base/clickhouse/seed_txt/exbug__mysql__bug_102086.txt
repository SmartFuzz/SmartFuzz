CREATE TABLE t(a String, b Int32 DEFAULT NULL);
INSERT INTO t VALUES ('+123', 1), ('-123', 2), (NULL, 3);
SELECT * FROM t WHERE b IS NOT NULL ORDER BY CAST(a AS String);
SELECT * FROM t ORDER BY a DESC;
SELECT CAST(a AS String) AS casted_value, b FROM t ORDER BY casted_value;
