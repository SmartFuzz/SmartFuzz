CREATE TABLE t1 (id UInt32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE t2 (description String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY created_at;
INSERT INTO t1 (name, age) VALUES ('Alice', 30), ('Bob', 25), ('Charlie', -1);
INSERT INTO t2 (description) VALUES ('Sample entry'), (NULL), ('Another entry with a long description that exceeds the normal length for testing purposes.');
SELECT * FROM t1 WHERE id IN (SELECT id FROM t1 WHERE age IS NOT NULL AND age < 100);
