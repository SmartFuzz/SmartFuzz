CREATE TABLE Bar (id Int32 PRIMARY KEY, description String, created_at DateTime);
INSERT INTO Bar VALUES (1, 'First entry', now());
CREATE TABLE Baz (id Int32 PRIMARY KEY, value Float32, bar_id Int32, CONSTRAINT fk_bar FOREIGN KEY (bar_id) REFERENCES Bar(id));
INSERT INTO Baz VALUES (1, 3.14, 1);
SELECT b.id, b.description, COUNT(z.id) FROM Bar b LEFT JOIN Baz z ON b.id = z.bar_id GROUP BY b.id, b.description;
