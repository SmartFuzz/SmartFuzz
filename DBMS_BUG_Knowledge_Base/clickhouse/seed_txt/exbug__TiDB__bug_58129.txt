CREATE TABLE b (id Int32 NOT NULL, name String, created_at DateTime DEFAULT now(), PRIMARY KEY (id));
INSERT INTO b VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie'), (4, NULL);
CREATE TABLE c (id Int32 NOT NULL, score Float32, FOREIGN KEY (id) REFERENCES b(id));
INSERT INTO c VALUES (1, 95.5), (2, 88.0), (3, NULL), (4, 76.5);
SELECT b.id, b.name, row_number() OVER (PARTITION BY b.id ORDER BY b.id) AS rn FROM b ORDER BY b.id;
