CREATE TABLE t1 (id Int32, name String, created_at DateTime DEFAULT now()) ENGINE = DeltaLakeLocal(local, format = 'Parquet', path = '/var/lib/clickhouse/user_files/lakehouses/spark_catalog/test/t1');
INSERT INTO t1 VALUES (1, 'Alice', now()), (2, 'Bob', now());
SELECT id, name FROM t1 WHERE id IN (SELECT id FROM t1 WHERE name LIKE 'A%');
SELECT id, name FROM t1 SETTINGS delta_lake_snapshot_version = 1;
SELECT id, name FROM t1 WHERE created_at IS NOT NULL;
