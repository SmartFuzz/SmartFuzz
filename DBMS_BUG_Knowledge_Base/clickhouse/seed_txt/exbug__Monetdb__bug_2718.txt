CREATE TABLE bug (time DateTime, val Int32, description String) ENGINE = MergeTree() ORDER BY time;
INSERT INTO bug VALUES (now(), 1, 'First entry');
INSERT INTO bug VALUES (now() + INTERVAL 2 SECOND, 2, 'Second entry');
INSERT INTO bug VALUES (now() - INTERVAL 3 SECOND, 3, 'Third entry');
SELECT t1.time, COUNT(t2.val) FROM bug t1 JOIN bug t2 ON t2.time BETWEEN (t1.time - INTERVAL 1 SECOND) AND (t1.time + INTERVAL 1 SECOND) GROUP BY t1.time ORDER BY t1.time;
