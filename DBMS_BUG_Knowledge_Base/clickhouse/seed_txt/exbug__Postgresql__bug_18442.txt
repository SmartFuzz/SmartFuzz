CREATE TABLE t1 (a Int32, b Int32, c String DEFAULT 'default');
INSERT INTO t1 SELECT NULL, i, 'value' FROM range(1, 101);
CREATE INDEX idx_t1_a_b ON t1 (a, b) TYPE minmax;
EXPLAIN SELECT * FROM t1 WHERE a IS NULL ORDER BY b;
EXPLAIN SELECT * FROM t1 WHERE a = 1 OR a IS NULL ORDER BY c;
