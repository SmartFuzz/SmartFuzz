CREATE TABLE users (id Int32 DEFAULT 1, name String NOT NULL, PRIMARY KEY (id));
INSERT INTO users (name) VALUES ('Alice');
SELECT * FROM users WHERE id IS NULL;
CREATE TABLE orders (order_id Int32 DEFAULT 0, user_id Int32, amount Decimal(10, 2) DEFAULT 0.00, PRIMARY KEY (order_id));
INSERT INTO orders (user_id, amount) VALUES (1, 10.00);
