CREATE TABLE users (id UInt64 NOT NULL, name String NOT NULL, age Int32 DEFAULT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users(name, age) VALUES ('Alice', 30);
CREATE TABLE orders (order_id UInt64 NOT NULL, user_id UInt64 NOT NULL, amount Decimal(10, 2) NOT NULL) ENGINE = MergeTree() ORDER BY (order_id, user_id);
INSERT INTO orders(order_id, user_id, amount) VALUES (1, 1, 99.99);
DELETE FROM orders WHERE (order_id, user_id) IN ((1, 1));
