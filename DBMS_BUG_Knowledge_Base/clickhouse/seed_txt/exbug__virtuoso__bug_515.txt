CREATE TABLE test_uuid (id Int32, name String, created_at DateTime DEFAULT now());
INSERT INTO test_uuid (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
SELECT id, name, generateUUIDv4() AS uuid FROM test_uuid WHERE id IN (SELECT id FROM test_uuid WHERE name LIKE 'A%');
SELECT DISTINCT generateUUIDv4() AS str_uuid, name FROM test_uuid WHERE created_at > now() - INTERVAL 1 DAY;
SELECT id, generateUUIDv4() AS uuid FROM test_uuid WHERE id = (SELECT MAX(id) FROM test_uuid);
