CREATE TABLE users (user_id UInt32 NOT NULL, username String NOT NULL, email String DEFAULT NULL, PRIMARY KEY (user_id));
INSERT INTO users (username, email) VALUES ('testuser', 'test@example.com');
CREATE TABLE orders (order_id UInt32 NOT NULL, user_id UInt32 NOT NULL, amount Decimal(10, 2) NOT NULL, PRIMARY KEY (order_id), FOREIGN KEY (user_id) REFERENCES users(user_id));
INSERT INTO orders (user_id, amount) VALUES (1, 99.99);
SELECT * FROM users;
