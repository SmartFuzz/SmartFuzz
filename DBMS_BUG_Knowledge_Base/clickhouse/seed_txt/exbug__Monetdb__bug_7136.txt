DROP TABLE IF EXISTS sys.v;
CREATE TABLE sys.v (id Int32, version Int32, value Float64, status String);
INSERT INTO sys.v VALUES (1, 1622470128, 1.0, 'active');
INSERT INTO sys.v VALUES (2, 1622470128, 2.0, 'inactive');
DELETE FROM sys.v WHERE (id, version) IN (SELECT id, version FROM (SELECT id, version, DENSE_RANK() OVER (PARTITION BY id ORDER BY id, version DESC) AS rn FROM sys.v) t WHERE rn > 1);
CREATE TABLE sys.v_temp (id Int32, version Int32, value Float64, created_at DateTime DEFAULT now());
