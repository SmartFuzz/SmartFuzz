CREATE TABLE t2 (id Int32, name String, created_at DateTime);
INSERT INTO t2 VALUES (1, 'test', now());
SELECT * FROM t2 WHERE created_at >= toDateTime('2023-01-01') OR id < 2;
SELECT * FROM t2 WHERE created_at >= toDateTime('abc') OR id < 2;
