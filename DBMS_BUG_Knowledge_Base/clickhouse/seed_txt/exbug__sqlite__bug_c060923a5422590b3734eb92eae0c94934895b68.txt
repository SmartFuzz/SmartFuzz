CREATE TABLE test_files (id UInt32, filename String, size UInt32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_files (filename, size) VALUES ('file1.txt', 100);
INSERT INTO test_files (filename, size) VALUES (NULL, 200);
ALTER TABLE test_files UPDATE filename = 'new_file_name_that_exceeds_the_limit' WHERE id = 1;
SELECT * FROM test_files WHERE size < 150 OR filename IS NULL;
