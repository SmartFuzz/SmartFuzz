CREATE TABLE t1 (id Int32, name String) ENGINE = MergeTree() ORDER BY id PARTITION BY (id % 2) SETTINGS partitioned_by = 'id';
CREATE TABLE t2 (value Decimal(10, 2), description String) ENGINE = MergeTree() ORDER BY value PARTITION BY (value < 100);
INSERT INTO t1 (id, name) VALUES (1, 'Alice');
INSERT INTO t2 (value, description) VALUES (150.00, 'High value');
SELECT * FROM t1 WHERE id IN (SELECT id FROM t1 WHERE name LIKE 'A%');
