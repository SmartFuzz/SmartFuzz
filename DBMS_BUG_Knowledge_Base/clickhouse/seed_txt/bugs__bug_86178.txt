CREATE TABLE pg_test.interest (id Int32, date Date, comments String, start_time DateTime, end_time DateTime, updated_at DateTime) ENGINE = MergeTree() ORDER BY id;
INSERT INTO pg_test.interest VALUES (15001, '2023-01-01', 'Comment 1', '2023-01-01 10:00:00', '2023-01-01 11:00:00', now()), (15002, '2023-01-02', 'Comment 2', '2023-01-02 10:00:00', '2023-01-02 11:00:00', now());
CREATE VIEW pg_test.all_view SQL SECURITY DEFINER AS SELECT * FROM postgresql(credentials, table='interest', schema='pg');
SELECT * FROM pg_test.all_view WHERE id > 15000;
SELECT * FROM postgresql(credentials, table='interest', schema='pg') WHERE id IN (15001, 15002);
