CREATE TABLE test_table1 (id UInt64, name String) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE test_table2 (value UInt64, description String) ENGINE = MergeTree() ORDER BY value;
INSERT INTO test_table1 (name) VALUES ('Alice'), ('Bob'), (NULL);
INSERT INTO test_table2 (value, description) VALUES (1, 'One'), (2, 'Two'), (NULL, 'Null Value');
SELECT description FROM test_table2 WHERE value IN (SELECT value FROM test_table2 WHERE value IS NOT NULL AND value LIKE '2%');
