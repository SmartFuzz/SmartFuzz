CREATE TABLE t1 ON CLUSTER 'cluster0' (id Int32, name String, created_at DateTime) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE t2 ON CLUSTER 'cluster0' (user_id Int32, score Float64, status Nullable(String)) ENGINE = MergeTree() ORDER BY user_id;
INSERT INTO t1 VALUES (1, 'Alice', now()), (2, 'Bob', now());
INSERT INTO t2 VALUES (1, 95.5, 'active'), (2, 88.0, NULL);
SELECT t1.name, t2.score FROM t1 JOIN t2 ON t1.id = t2.user_id WHERE t2.status IS NOT NULL;
