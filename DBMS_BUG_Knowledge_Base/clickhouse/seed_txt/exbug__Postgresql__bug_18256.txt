CREATE TABLE partitioned_table (id UInt64, value Int32, description String) ENGINE = MergeTree() ORDER BY id PARTITION BY value;
CREATE TABLE partitioned_table_1 AS partitioned_table ENGINE = MergeTree() ORDER BY id PARTITION BY value WHERE value >= 1 AND value < 10;
INSERT INTO partitioned_table (value, description) VALUES (5, NULL), (NULL, 'Test'), (15, 'Boundary Test');
SELECT * FROM partitioned_table WHERE value IS NULL OR value < 10;
