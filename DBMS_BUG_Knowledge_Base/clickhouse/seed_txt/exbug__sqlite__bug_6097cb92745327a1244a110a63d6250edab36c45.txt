CREATE TABLE test_dates (id UInt32, event_date Int32, description String);
INSERT INTO test_dates (event_date, description) VALUES (2457754, 'Test event with start of day');
SELECT toStartOfDay(toDateTime(event_date * 86400)) FROM test_dates WHERE id = 1;
SELECT toStartOfDay(toDateTime(event_date * 86400)) AS start_of_day, description FROM test_dates;
SELECT toStartOfDay(toDateTime(NULL)) AS null_case;
