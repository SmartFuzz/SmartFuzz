CREATE TABLE users (id Int32, name String, created_at DateTime DEFAULT now());
INSERT INTO users (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, NULL);
CREATE TABLE orders (order_id Int32, user_id Int32, amount Decimal(10, 2));
INSERT INTO orders (order_id, user_id, amount) VALUES (101, 1, 150.00), (102, 2, 200.50), (103, 1, NULL);
SELECT * FROM (SELECT user_id, SUM(amount) AS total_amount FROM orders GROUP BY user_id) AS recent_orders WHERE total_amount IS NOT NULL;
