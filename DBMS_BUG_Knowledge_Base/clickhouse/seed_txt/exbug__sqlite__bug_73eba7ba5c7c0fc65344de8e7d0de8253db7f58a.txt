CREATE TABLE users(id UInt32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users(name, age) VALUES('Alice', 30);
SELECT * FROM users;
WITH temp AS (SELECT id FROM users WHERE age IS NULL) SELECT * FROM temp;
CREATE TABLE orders(order_id UInt32, user_id UInt32, amount Float64 DEFAULT 0.0) ENGINE = MergeTree() ORDER BY order_id;
