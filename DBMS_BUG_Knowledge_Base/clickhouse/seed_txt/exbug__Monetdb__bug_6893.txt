CREATE TABLE tbl_Orders (OrderID Int32, CustomerID Int32, OrderDate Date, TotalAmount Decimal(10,2)) ENGINE = MergeTree() ORDER BY OrderID;
INSERT INTO tbl_Orders VALUES (1, 101, '2023-01-15', 150.75), (2, 102, '2023-01-16', NULL), (3, 103, '2023-01-17', 200.00);
CREATE TABLE tbl_Customers (CustomerID Int32, CustomerName String, Email String) ENGINE = MergeTree() ORDER BY CustomerID;
INSERT INTO tbl_Customers VALUES (101, 'Alice', 'alice@example.com'), (102, 'Bob', NULL), (103, 'Charlie', 'charlie@example.com');
SELECT o.OrderID, c.CustomerName FROM tbl_Orders AS o FULL OUTER JOIN tbl_Customers AS c ON o.CustomerID = c.CustomerID WHERE o.TotalAmount IS NULL OR c.Email IS NULL;
