CREATE TABLE test3 (date_received UInt32, pid UInt32, status Enum8('active' = 1, 'inactive' = 2) DEFAULT 'active') ENGINE = MergeTree() ORDER BY date_received;
INSERT INTO test3 VALUES (1596286909, 1, 'active'), (1596286908, 2, 'inactive'), (1596286907, 3, 'active'), (1596286906, 4, 'inactive'), (1596286905, 5, 'active');
SELECT toDate(toUnixTimestamp(date_received)) AS dt, COUNT(DISTINCT pid) FROM test3 GROUP BY dt;
SELECT toDate(toUnixTimestamp(date_received)) AS dt, COUNT(DISTINCT pid) FROM test3 GROUP BY dt ORDER BY dt;
