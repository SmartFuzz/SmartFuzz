CREATE TABLE t2(a Int32 NOT NULL DEFAULT 5, b String, c Float64, PRIMARY KEY(a, b)) ENGINE = MergeTree() ORDER BY (a, b);
INSERT INTO t2(a, b, c) VALUES (2, 'one', 1.1), (NULL, 'two', 2.2), (3, 'three', 3.3);
INSERT INTO t2(a, b, c) VALUES (2, 'one', 1.1), (5, 'four', 4.4);
SELECT * FROM t2;
CREATE TABLE t3(x String, y Int32, z String DEFAULT 'default', PRIMARY KEY(x, y)) ENGINE = MergeTree() ORDER BY (x, y);
