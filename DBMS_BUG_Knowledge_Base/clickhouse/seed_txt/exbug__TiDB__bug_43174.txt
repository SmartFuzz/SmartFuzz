CREATE TABLE tp2 (id Int32, name String, age Int32, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id PARTITION BY (id % 2);
INSERT INTO tp2 (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', 35);
SELECT * FROM tp2;
ALTER TABLE tp2 DROP PARTITION 0;
