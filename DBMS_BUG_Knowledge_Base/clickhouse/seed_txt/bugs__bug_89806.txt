CREATE TABLE n4 (key UInt64, value Float64) ENGINE = MergeTree ORDER BY key SETTINGS index_granularity=1;
INSERT INTO n4 SELECT number, number / 2.0 FROM numbers(10);
CREATE MATERIALIZED VIEW mv2 TO n1_n2_join AS SELECT n1.key as k, n1.value as v1, n4.value as v2 FROM n1 JOIN n4 ON n1.key = n4.key ORDER BY n1.key;
INSERT INTO n1 SELECT number as key, toString(number + 50) FROM numbers(10);
SELECT * FROM mv2 JOIN n3 ON mv2.k = n3.key WHERE mv2.v2 IS NOT NULL ORDER BY mv2.k, n3.key;
