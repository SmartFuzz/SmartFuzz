CREATE TABLE t2 (x Int32, y String, z DateTime, w Float64, v UInt8);
INSERT INTO t2 (x, y, z, w, v) VALUES (1, 'test', '2023-01-01 00:00:00', 3.14, 1);
SELECT name AS column_name, position AS ordinal_position FROM system.columns WHERE table = 't2' AND database = currentDatabase() AND name = 'y';
SELECT name AS column_name, position AS ordinal_position FROM system.columns WHERE table = 't2' AND database = currentDatabase() AND position > 2;
SELECT name AS column_name, position AS ordinal_position FROM system.columns WHERE table = 't2' AND database = currentDatabase() AND name IS NULL;
