CREATE TABLE IF NOT EXISTS mask_test (flags UInt8, id UInt32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO mask_test (flags, id) VALUES (1, 1);
INSERT INTO mask_test (flags, id) VALUES (3, 2);
SELECT * FROM mask_test WHERE (flags & 1) = 1;
SELECT (flags & 1) = 1 AS comparison_result FROM mask_test;
