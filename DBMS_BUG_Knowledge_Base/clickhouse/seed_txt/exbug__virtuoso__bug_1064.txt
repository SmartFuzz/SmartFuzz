CREATE TABLE test_genes (id String NOT NULL, chromosome String NOT NULL) ENGINE = MergeTree() PRIMARY KEY id;
INSERT INTO test_genes (id, chromosome) VALUES ('GENE1', '12');
CREATE TABLE test_isoforms (id String NOT NULL, entry String NOT NULL) ENGINE = MergeTree() PRIMARY KEY id;
INSERT INTO test_isoforms (id, entry) VALUES ('ISOFORM1', 'GENE1');
SELECT * FROM test_genes WHERE chromosome = '12' AND id = 'GENE1' AND id IS NOT NULL;
