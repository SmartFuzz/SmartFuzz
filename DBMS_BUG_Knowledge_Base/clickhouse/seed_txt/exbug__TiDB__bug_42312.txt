DROP TABLE IF EXISTS test_partitioned;
CREATE TABLE test_partitioned (id Int32, name String, value Decimal(10, 2)) ENGINE = MergeTree() ORDER BY id PARTITION BY intDiv(id, 10);
ALTER TABLE test_partitioned ADD CONSTRAINT idx_name UNIQUE(name);
INSERT INTO test_partitioned VALUES (1, 'A', 10.00), (5, 'B', 20.00), (15, 'C', 30.00), (25, 'D', 40.00);
SELECT * FROM test_partitioned;
