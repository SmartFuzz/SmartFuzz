CREATE DATABASE IF NOT EXISTS test_db;
CREATE TABLE users (id UInt32, username String NOT NULL, email String, created_at DateTime DEFAULT now());
INSERT INTO users (username, email) VALUES ('test_user', 'test@example.com');
CREATE TABLE orders (order_id UInt32, user_id UInt32, amount Decimal(10, 2) NOT NULL, PRIMARY KEY (order_id));
INSERT INTO orders (user_id, amount) VALUES (1, 99.99);
