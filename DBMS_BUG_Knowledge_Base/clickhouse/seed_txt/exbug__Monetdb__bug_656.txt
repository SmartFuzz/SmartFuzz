CREATE TABLE test_table (id Int32, event_time Time, description String);
INSERT INTO test_table VALUES (1, '12:34:56', 'Event A');
INSERT INTO test_table VALUES (2, '12:34:00', 'Event B');
SELECT event_time FROM test_table WHERE toHour(event_time) = 12 AND toMinute(event_time) = 34;
SELECT event_time FROM test_table WHERE toHour(event_time) IS NULL OR toMinute(event_time) IS NULL;
