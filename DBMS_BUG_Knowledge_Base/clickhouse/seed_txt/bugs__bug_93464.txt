CREATE DATABASE polaris_iceberg ENGINE = DataLakeCatalog('https://example.snowflakecomputing.com/polaris/api/catalog') SETTINGS catalog_type = 'rest', warehouse = 'my-warehouse', auth_scope = 'PRINCIPAL_ROLE:ALL', oauth_server_uri = 'https://example.snowflakecomputing.com/polaris/api/catalog/v1/oauth/tokens', catalog_credential = 'client_id:client_secret';
CREATE TABLE polaris_iceberg.`teslasource.kafkav6.source-data` (id UInt32, name String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO polaris_iceberg.`teslasource.kafkav6.source-data` VALUES (1, 'test1', now()), (2, 'test2', now());
SELECT * FROM polaris_iceberg.`teslasource.kafkav6.source-data` WHERE id IS NOT NULL;
SHOW TABLES FROM polaris_iceberg;
