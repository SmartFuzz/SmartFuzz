CREATE TABLE test2 (id1 UInt32 NOT NULL, id2 UInt32 NOT NULL, id3 Nullable(UInt32), name String, name2 String NOT NULL) ENGINE = MergeTree() ORDER BY id1;
INSERT INTO test2 (id1, id2, id3, name, name2) VALUES (1, 2, NULL, 'example1', 'example1');
INSERT INTO test2 (id1, id2, id3, name, name2) VALUES (3, 4, 5, 'example2', 'example2');
SELECT count() OVER(), q.* FROM (SELECT 1) d LEFT JOIN (SELECT * FROM test2) q ON d.1 = 1 ORDER BY name;
SELECT count() OVER(), q.* FROM (SELECT 1) d LEFT JOIN (SELECT * FROM test2 WHERE id3 IS NOT NULL) q ON d.1 = 1 ORDER BY name2;
