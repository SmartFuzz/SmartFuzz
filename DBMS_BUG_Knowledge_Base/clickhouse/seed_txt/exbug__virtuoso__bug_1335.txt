CREATE TABLE OperationalPoint (id Int32, country String, value Int32);
INSERT INTO OperationalPoint (id, country, value) VALUES (1, 'CountryA', 10), (2, 'CountryB', 20), (3, 'CountryA', 15);
CREATE TABLE CountryStats (country String, total Int32) ENGINE = MergeTree() ORDER BY country;
INSERT INTO CountryStats (country, total) SELECT country, COUNT(*) FROM OperationalPoint GROUP BY country HAVING COUNT(*) > 1;
SELECT country, (SELECT COUNT(*) FROM OperationalPoint WHERE country = CountryStats.country) AS country_count FROM CountryStats HAVING country_count > 1;
