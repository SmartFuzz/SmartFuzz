CREATE TABLE t2 (a Int32, b String, c Float64, d UInt8);
INSERT INTO t2 VALUES (1, 'test', 3.14, 1), (2, 'example', NULL, 0);
CREATE FUNCTION unpivot_test() RETURNS Array(Tuple(a Int32, b String)) AS (SELECT a, b FROM t2);
SELECT * FROM unpivot_test();
CREATE FUNCTION unpivot_with_null() RETURNS Array(Tuple(c Float64, d UInt8)) AS (SELECT c, d FROM t2);
