CREATE TABLE range_parted(a Int32, b String, c Date DEFAULT today()) ENGINE = MergeTree() ORDER BY a;
CREATE TABLE range_parted_1 ENGINE = MergeTree() ORDER BY a AS SELECT * FROM range_parted WHERE a >= 0 AND a < 10;
INSERT INTO range_parted (a, b) VALUES (5, 'test'), (15, 'example');
SELECT * FROM range_parted;
