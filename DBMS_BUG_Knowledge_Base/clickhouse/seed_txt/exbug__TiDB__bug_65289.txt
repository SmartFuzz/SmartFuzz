DROP TABLE IF EXISTS t, tp;
CREATE TABLE t (a Int32, b Int32, dt Date) ENGINE = MergeTree() PRIMARY KEY a;
CREATE TABLE tp (a Int32, b Int32, dt Date) ENGINE = MergeTree() PRIMARY KEY a PARTITION BY (a % 5);
INSERT INTO tp (a, b, dt) VALUES (2, 2, '2023-01-01'), (4, 4, '2023-01-02'), (6, 6, '2023-01-03');
ALTER TABLE tp ADD COLUMN dt Date DEFAULT NULL;
