CREATE TABLE t2 (id Int32 NOT NULL, name String, value Decimal(10, 2)) ENGINE = MergeTree() ORDER BY id PARTITION BY intDiv(id, 10);
INSERT INTO t2 (id, name, value) VALUES (1, 'test1', 10.50);
SELECT * FROM t2 WHERE id = 1;
CREATE TABLE t3 (x Int32, y String) ENGINE = MergeTree() ORDER BY (x, y) PARTITION BY (x);
SELECT * FROM t3 WHERE x = 2;
