CREATE TABLE users (id Int32, username String, email String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users VALUES (1, 'user1'), (2, 'user2 '), (3, 'user3');
CREATE TABLE orders (order_id Int32, user_id Int32, amount Decimal(10, 2)) ENGINE = MergeTree() ORDER BY order_id;
INSERT INTO orders VALUES (101, 1, 99.99), (102, 2, 150.00), (103, 1, 200.50);
SELECT u.id, u.username FROM users u WHERE trim(u.username) = 'user1' AND length(u.username) = 5;
