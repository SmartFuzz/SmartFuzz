CREATE TABLE t1(a Int32, b Int32, c String DEFAULT 'default', PRIMARY KEY(a)) ENGINE = MergeTree() ORDER BY a;
INSERT INTO t1 VALUES(888, 9999, NULL);
CREATE TABLE t2(x Int32, y Int32, z Float64 CHECK(z > 0), PRIMARY KEY(x)) ENGINE = MergeTree() ORDER BY x;
INSERT INTO t2 SELECT a, b, 1.0 FROM t1 WHERE b IS NOT NULL;
SELECT * FROM t1;
