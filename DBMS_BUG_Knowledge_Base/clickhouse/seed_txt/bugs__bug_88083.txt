CREATE TABLE test_table1 (id UInt32, name String, created_at DateTime) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE test_table2 (id UInt32, value Float64, description Nullable(String)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table1 VALUES (1, 'Alice', now()), (2, 'Bob', now());
INSERT INTO test_table2 VALUES (1, 3.14, 'Pi'), (2, NULL, 'Null Value');
SELECT name, value FROM test_table1 JOIN test_table2 ON test_table1.id = test_table2.id WHERE test_table2.value IS NOT NULL;
