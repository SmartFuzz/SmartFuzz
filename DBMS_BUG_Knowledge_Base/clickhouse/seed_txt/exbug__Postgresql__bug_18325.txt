DROP TABLE IF EXISTS xyz;
CREATE TABLE xyz (id UInt32, name String, age Nullable(UInt32), created_at DateTime DEFAULT now());
INSERT INTO xyz (name, age) VALUES ('Alice', 30), ('Bob', NULL);
CREATE OR REPLACE FUNCTION get_age() RETURNS UInt32 AS (SELECT age FROM xyz WHERE age IS NOT NULL LIMIT 1);
ALTER TABLE xyz DROP COLUMN age;
