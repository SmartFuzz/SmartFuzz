CREATE TABLE test_table (id Int32, value String, created_at DateTime DEFAULT now());
INSERT INTO test_table (id, value) VALUES (1, 'first'), (2, 'second'), (3, 'third');
CREATE TABLE ordered_table AS SELECT * FROM test_table ORDER BY created_at;
SELECT arrayJoin([test_table.value]) AS original_array, arrayJoin([ordered_table.value]) AS ordered_array;
SELECT arrayJoin((SELECT value FROM test_table WHERE id IS NOT NULL)) AS non_null_array;
