CREATE DATABASE d1 ENGINE = Replicated('/clickhouse/path/d1', '{shard}', '{replica}');
CREATE TABLE d1.t1 (id UInt32, name String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO d1.t1 (id, name) VALUES (1, 'test1'), (2, 'test2');
CREATE VIEW d1.v1 AS SELECT id, name FROM d1.t1 WHERE id > 0;
DETACH TABLE d1.v1 PERMANENTLY;
