CREATE TABLE default.ye3_local_test ON CLUSTER cluster_2S_1R
(
    `dt` DateTime,
    `event` LowCardinality(String),
    `user_id` UInt64,
    `context` Nullable(String)
)
ENGINE = MergeTree
PARTITION BY event
ORDER BY (user_id);
INSERT INTO default.ye3_local_test VALUES (now(), 'event1', 1, 'context1'), (now(), 'event2', 2, NULL), (now(), 'event1', 3, 'context3');
CREATE TABLE ye3_tmp ON CLUSTER 'cluster_2S_1R'
(
    `user_id` UInt64,
    `extra_info` String
)
ENGINE = MergeTree
ORDER BY (user_id);
INSERT INTO ye3_tmp VALUES (1, 'info1'), (2, 'info2');
DELETE FROM default.ye3_local_test ON CLUSTER 'cluster_2S_1R' WHERE user_id IN (SELECT user_id FROM ye3_tmp);
