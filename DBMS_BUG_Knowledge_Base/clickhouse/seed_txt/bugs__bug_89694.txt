DROP TABLE IF EXISTS client;
CREATE TABLE client (code String, name String, email String, contact_number String, last_reviewed_at DateTime64(3, 'UTC')) ENGINE = CoalescingMergeTree() ORDER BY code;
INSERT INTO client (code, name, email, contact_number, last_reviewed_at) VALUES ('A002', 'Jane Smith', 'JANE@EXAMPLE.COM', '0123456789', now());
INSERT INTO client (name, code, email, contact_number, last_reviewed_at) VALUES ('A' || repeat('B', 10000), 'A003', 'TEST@EXAMPLE.COM', '0198765432', now());
SELECT * FROM client WHERE length(name) > 50;
