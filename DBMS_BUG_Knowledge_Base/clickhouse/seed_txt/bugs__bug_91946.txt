CREATE TABLE users_extended (uid Int32, name String, age Int16, deleted UInt8, version UInt64, salary Float64) ENGINE=ReplacingMergeTree(version, deleted) PRIMARY KEY uid ORDER BY uid;
INSERT INTO users_extended VALUES (1, 'John', 33, 0, 1, 50000.0);
INSERT INTO users_extended VALUES (2, 'Ksenia', 48, 0, 1, 60000.0);
CREATE ROW POLICY only_females ON users_extended USING deleted = 0 AND name NOT LIKE 'J%' TO ALL;
SELECT AVG(salary) FROM users_extended FINAL WHERE age > 30 GROUP BY age;
