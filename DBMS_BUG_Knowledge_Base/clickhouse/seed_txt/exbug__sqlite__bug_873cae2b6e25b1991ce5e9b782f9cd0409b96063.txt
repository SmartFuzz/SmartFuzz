CREATE TABLE test_table (id UInt64, name String, value Float64 DEFAULT 0.0);
INSERT INTO test_table (id, name, value) VALUES (1, 'example', 10.0), (2, 'test', 20.0);
CREATE TABLE temp_table AS SELECT * FROM test_table WHERE value > (SELECT AVG(value) FROM test_table);
CREATE TABLE faulty_table AS SELECT arrayJoin([0]) AS dummy;
-- using arrayJoin to simulate zeroblob;
SELECT count() FROM test_table;
