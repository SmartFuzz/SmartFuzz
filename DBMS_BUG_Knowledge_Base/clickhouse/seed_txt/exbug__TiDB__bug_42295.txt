DROP TABLE IF EXISTS t2;
CREATE TABLE t2(id1 Int32, id2 String NOT NULL, id3 Float32 DEFAULT 0.0) ENGINE = MergeTree() ORDER BY id1;
INSERT INTO t2 VALUES(1, 'test', 1.1), (2, NULL, 2.2), (3, 'example', NULL), (4, 'boundary', 0.0);
EXPLAIN SELECT id1, id2 FROM t2 WHERE id3 IS NOT NULL;
EXPLAIN SELECT id1, id2 FROM t2 WHERE id2 LIKE 't%';
