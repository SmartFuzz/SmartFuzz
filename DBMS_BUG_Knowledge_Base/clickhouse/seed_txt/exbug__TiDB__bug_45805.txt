CREATE TABLE signed_estimate_volume (id UInt64, estimate_id Int64 DEFAULT 0, dealer_id String DEFAULT '', broker_id String DEFAULT '', real_t0_volume Int64 DEFAULT 0, real_t1_volume Int64 DEFAULT 0, manager_yid String DEFAULT '', estimate_volume Int64 DEFAULT 0, estimate_year_month String DEFAULT '', is_dropped UInt8 DEFAULT 0) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE unsigned_estimate_volume (id UInt64, estimate_id Int64 DEFAULT 0, estimate_year_month String DEFAULT '', business_id Int64 DEFAULT 0, broker_id String DEFAULT '', estimate_volume Int64 DEFAULT 0, manager_yid String DEFAULT '') ENGINE = MergeTree() ORDER BY id;
INSERT INTO signed_estimate_volume (id, estimate_volume, real_t1_volume) VALUES (1, 100, 200);
INSERT INTO unsigned_estimate_volume (id, estimate_id, estimate_year_month, business_id, broker_id, estimate_volume, manager_yid) VALUES (1, 1, '202310', 1, 'broker1', 150, 'manager1');
SELECT estimate_year_month, manager_yid, SUM(real_t1_volume) AS real_t1_volume, SUM(real_t0_volume) AS real_t0_volume, SUM(estimate_volume) AS estimate_volume FROM signed_estimate_volume GROUP BY manager_yid;
