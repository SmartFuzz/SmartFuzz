CREATE TABLE chunks_2 (id String NOT NULL, embedding Array(Float32) DEFAULT NULL) ENGINE = MergeTree() PRIMARY KEY id;
INSERT INTO chunks_2 (id, embedding) VALUES ('1', [0.1, 0.2, 0.3]), ('2', [0.4, 0.5, 0.6]), ('3', [0.7, 0.8, 0.9]);
SELECT * FROM chunks_2 WHERE embedding IS NOT NULL ORDER BY arrayJoin(embedding) LIMIT 5;
CREATE TABLE documents (document_id Int32 NOT NULL, content String DEFAULT NULL) ENGINE = MergeTree() PRIMARY KEY document_id;
INSERT INTO documents (document_id, content) VALUES (1, 'Document 1'), (2, 'Document 2');
