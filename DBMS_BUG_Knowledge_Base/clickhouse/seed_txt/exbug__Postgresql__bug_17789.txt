CREATE TABLE t1 (a Int32 NOT NULL, b String DEFAULT 'default', c DateTime DEFAULT now());
INSERT INTO t1 SELECT number, 'default', now() FROM system.numbers LIMIT 66;
CREATE TABLE ft1 (b Int32) ENGINE = MergeTree() ORDER BY b;
INSERT INTO ft1 SELECT a FROM t1;
SELECT sleep(60);
