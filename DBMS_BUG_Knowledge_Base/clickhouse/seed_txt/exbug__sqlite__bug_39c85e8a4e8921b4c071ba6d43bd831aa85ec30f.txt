CREATE TABLE users (id UInt32, name String NOT NULL, age Nullable(UInt32) CHECK(age >= 0), email String UNIQUE);
INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');
INSERT INTO users (name, age, email) VALUES ('Bob', NULL, 'bob@example.com');
SELECT * FROM users WHERE age IS NULL OR age < 18;
ALTER TABLE users UPDATE age = age + 1 WHERE id = 1;
