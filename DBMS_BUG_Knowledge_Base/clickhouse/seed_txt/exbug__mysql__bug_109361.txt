CREATE TABLE users(id Int32 NOT NULL, name String, age Int32, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users(name, age) VALUES ('Alice', 30), ('Bob', 25), (NULL, 40), ('Charlie', NULL);
CREATE TABLE orders(order_id Int32 NOT NULL, user_id Int32, amount Decimal(10, 2), PRIMARY KEY (order_id)) ENGINE = MergeTree() ORDER BY order_id;
INSERT INTO orders(user_id, amount) VALUES (1, 100.50), (2, 200.75), (NULL, 150.00), (3, NULL);
SELECT u.name, COUNT(o.order_id) AS order_count FROM users u LEFT JOIN orders o ON u.id = o.user_id GROUP BY u.id;
