CREATE TABLE large_strings(id Int32, data String);
INSERT INTO large_strings VALUES (1, concat('a', repeat('b', 1000000)));
SELECT * FROM large_strings WHERE length(data) > 500000;
CREATE TABLE another_table(id Int32, ref_id Int32, FOREIGN KEY (ref_id) REFERENCES large_strings(id));
INSERT INTO another_table VALUES (1, 1);
