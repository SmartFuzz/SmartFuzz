CREATE DATABASE sbtest;
CREATE TABLE ARRAY (id Int32 NOT NULL, value String DEFAULT NULL) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE FULL (id Int32 NOT NULL, description String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE INTERSECT (id Int32 NOT NULL, amount Decimal(10, 2) CHECK (amount >= 0)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO FULL (id, description) VALUES (1, 'Test entry for FULL');
SELECT * FROM FULL;
