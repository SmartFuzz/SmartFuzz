CREATE TABLE test_table (id UInt32, value Nullable(Int32), category String);
INSERT INTO test_table (value, category) VALUES (1, 'A'), (2, 'B'), (3, 'A'), (NULL, 'C');
SELECT value, SUMIf(value, value IS NOT NULL) OVER (PARTITION BY category) AS category_sum FROM test_table ORDER BY category_sum;
SELECT SUMIf(value, value IS NOT NULL) OVER () AS total_sum FROM test_table;
SELECT SUMIf(value, value IS NOT NULL) OVER () ORDER BY SUMIf(value, value IS NOT NULL) OVER ();
