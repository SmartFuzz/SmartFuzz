CREATE TABLE test_dates (id Int32, event_date Nullable(DateTime), description String);
INSERT INTO test_dates VALUES (1, toDateTime('infinity'), 'Event with infinite date');
INSERT INTO test_dates VALUES (2, toDateTime('2025-10-02 00:00:00'), 'Regular event date');
SELECT if(event_date IS NULL, 'infinity', toYear(event_date)) AS year FROM test_dates WHERE id = 1;
SELECT toYear(event_date) AS year FROM test_dates WHERE id = 2;
