CREATE TABLE test_intervals (id UInt32, duration Interval SECOND) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_intervals (id, duration) VALUES (1, INTERVAL 60 SECOND);
SELECT duration, toTime(duration) AS time_value FROM test_intervals;
SELECT id, toTime(duration) AS time_value FROM test_intervals WHERE duration IS NOT NULL;
SELECT id, toTime(INTERVAL 120 SECOND) AS time_value FROM test_intervals;
