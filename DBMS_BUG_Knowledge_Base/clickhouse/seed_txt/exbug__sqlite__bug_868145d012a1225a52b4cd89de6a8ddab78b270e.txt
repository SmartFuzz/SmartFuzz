CREATE TABLE p (id UInt64, uid String, t Int32 DEFAULT 0);
CREATE TABLE pa (id UInt64, a_uid String UNIQUE);
CREATE TABLE a (id UInt64, uid String, t Int32 CHECK(t >= 0));
INSERT INTO p (uid, t) VALUES ('1234', 1), ('5678', 2);
SELECT DISTINCT * FROM pa LEFT JOIN p ON p.uid='1234' LEFT JOIN a ON a.uid=pa.a_uid WHERE a.t IS NULL OR a.t=p.t;
