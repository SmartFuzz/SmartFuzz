DROP TABLE IF EXISTS t;
CREATE TABLE t (id Int32, Uid UInt64 NOT NULL, bid String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY Uid PARTITION BY Uid % 5;
INSERT INTO t (id, Uid, bid) VALUES (1, 1, 'A'), (2, 2, 'B'), (3, 3, 'C'), (4, 4, 'D'), (5, 5, 'E');
SELECT * FROM t WHERE bid IS NULL LIMIT 1;
