CREATE TABLE testb (a Int32, b Float64, c String) ENGINE = MergeTree() ORDER BY a;
INSERT INTO testb (a, b, c) VALUES (1, 1.1, 'test1'), (2, 2.2, 'test2'), (3, 3.3, 'test3');
ALTER TABLE testb ADD COLUMN d DateTime DEFAULT now() AFTER c;
UPDATE testb SET d = now() WHERE a < 3;
SELECT AVG(b) AS avg_b, COUNT(*) AS total FROM testb GROUP BY d ORDER BY total DESC LIMIT 5;
