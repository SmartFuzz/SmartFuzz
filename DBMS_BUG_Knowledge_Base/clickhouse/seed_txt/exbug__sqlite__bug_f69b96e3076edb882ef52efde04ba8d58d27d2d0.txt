CREATE TABLE test_table (content String) ENGINE = MergeTree() ORDER BY content;
INSERT INTO test_table(content) VALUES('apple'), ('banana'), ('cherry'), ('date');
CREATE TABLE orders (id UInt32, product String, quantity UInt32 DEFAULT 0) ENGINE = MergeTree() ORDER BY id;
INSERT INTO orders(id, product, quantity) VALUES(1, 'apple', 10), (2, 'banana', 5), (3, 'cherry', 15);
SELECT * FROM orders WHERE product IN (SELECT content FROM test_table) ORDER BY quantity;
