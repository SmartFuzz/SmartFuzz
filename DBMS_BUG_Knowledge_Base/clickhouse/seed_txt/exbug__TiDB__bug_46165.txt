CREATE TABLE nt ( a Int32, b String, UNIQUE INDEX u_idx (b));
CREATE TABLE pt ( a Int32, b Int32, UNIQUE INDEX u_idx (b)) ENGINE = MergeTree() PARTITION BY a ORDER BY a;
INSERT INTO nt (a, b) VALUES (1, 'test1');
INSERT INTO pt (a, b) VALUES (5, 100), (15, 200);
SELECT * FROM pt WHERE a < 10;
