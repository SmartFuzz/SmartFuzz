CREATE TABLE test_data (id Int32, value Float64, category String);
INSERT INTO test_data VALUES (1, 50.0, 'A'), (2, 50.0, 'B'), (3, NULL, 'A'), (4, 100.0, 'B');
CREATE TABLE union_data AS SELECT value FROM test_data WHERE category = 'A' UNION ALL SELECT value FROM test_data WHERE category = 'B';
SELECT value, SUM(value) OVER (ORDER BY value ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS cumulative_sum FROM union_data;
CREATE TABLE boundary_test (num Int32 NOT NULL) ENGINE = MergeTree() ORDER BY num;
