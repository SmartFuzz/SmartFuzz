DROP TABLE IF EXISTS t1;
CREATE TABLE t1 (c0 Int32, c1 String, c2 Date, c3 Float32, c4 UInt8);
INSERT INTO t1 (c0, c1, c2, c3, c4) VALUES (1, 'test', '2023-01-01', 1.5, 1);
CREATE VIEW v1 AS SELECT c1, COUNT(*) AS count FROM t1 GROUP BY (CASE WHEN c4 = 1 THEN c1 ELSE NULL END);
CREATE VIEW v2 AS SELECT c2, AVG(c3) AS avg_c3 FROM t1 GROUP BY (CASE WHEN c0 = 1 THEN c2 ELSE NULL END);
