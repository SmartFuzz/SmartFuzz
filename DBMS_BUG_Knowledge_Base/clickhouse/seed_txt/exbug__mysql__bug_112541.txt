CREATE TABLE t2 (id UInt32, name String, age Nullable(UInt32), created_at DateTime DEFAULT now());
INSERT INTO t2 (name, age) VALUES ('Alice', 30);
INSERT INTO t2 (name, age) VALUES ('Bob', NULL);
SELECT name, age FROM t2 WHERE age IS NULL OR age < 25;
SELECT COUNT(*) FROM t2 WHERE age BETWEEN 20 AND 40;
