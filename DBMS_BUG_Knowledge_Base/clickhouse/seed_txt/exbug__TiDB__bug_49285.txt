CREATE TABLE t4(id Int32, sala FixedString(10), name String, age Int32) ENGINE = MergeTree() ORDER BY (id, sala) PARTITION BY age;
INSERT INTO t4 VALUES (1, 'a', 'Alice', 25), (2, 'b', 'Bob', 35), (3, 'a', 'Charlie', 45);
SELECT * FROM t4 SETTINGS max_partitions_per_insert_block = 1;
EXPLAIN SELECT * FROM t4 WHERE sala='a' AND age > 20;
SELECT COUNT(*) FROM t4 WHERE sala IS NULL OR sala = 'b';
