CREATE DATABASE d1 ENGINE = Atomic;
CREATE TABLE d1.t1 (c1 Int, c2 String, c3 Float64 DEFAULT 0.0) ENGINE = MergeTree() ORDER BY c1;
INSERT INTO d1.t1 (c1, c2) VALUES (1, 'test'), (2, 'example');
CREATE DICTIONARY d2 (c1 Int DEFAULT 1, c2 String) PRIMARY KEY (c1) SOURCE(CLICKHOUSE(DB 'd1' TABLE 't1')) LAYOUT(FLAT()) LIFETIME(10);
RENAME DATABASE d1 TO d3;
