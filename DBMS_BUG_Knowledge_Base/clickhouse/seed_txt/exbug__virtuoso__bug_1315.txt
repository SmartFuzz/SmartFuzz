CREATE TABLE test_files (id Int32, name String, created_at DateTime DEFAULT now());
INSERT INTO test_files (id, name) VALUES (1, 'File A'), (2, 'File B'), (3, 'File C'), (4, 'File D'), (5, 'File E');
CREATE TABLE test_people (id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_people (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', NULL);
SELECT DISTINCT * FROM test_files WHERE id IN (SELECT id FROM test_people) LIMIT 1;
