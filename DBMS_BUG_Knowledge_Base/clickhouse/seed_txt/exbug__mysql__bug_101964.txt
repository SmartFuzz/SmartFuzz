CREATE DATABASE test_db;
CREATE TABLE test_db.joinit (i UInt32, s String, t Time, g UInt32) ENGINE = MergeTree() ORDER BY i;
INSERT INTO test_db.joinit VALUES (NULL, generateUUIDv4(), now(), floor(1 + rand() * 60));
INSERT INTO test_db.joinit (s, t, g) SELECT generateUUIDv4(), now(), floor(1 + rand() * 60) FROM test_db.joinit WHERE g < 30;
SELECT COUNT(*) FROM test_db.joinit WHERE g IS NULL OR g > 50;
