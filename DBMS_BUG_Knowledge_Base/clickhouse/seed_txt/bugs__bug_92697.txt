CREATE TABLE test_table (id UInt32, name String, created_at DateTime DEFAULT now(), value Nullable(Float64)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.5), (2, 'test2', NULL), (3, 'test3', 20.0);
CREATE TABLE another_table (ref_id UInt32, description String, amount Int32) ENGINE = MergeTree() ORDER BY ref_id;
INSERT INTO another_table (ref_id, description, amount) SELECT id, concat(name, ' description'), CAST(value AS Int32) FROM test_table WHERE value IS NOT NULL;
SELECT * FROM postgresql('localhost:5432', 'testdb', 'infinite_counter', 'admin', 'admin123') WHERE counter < 100;
