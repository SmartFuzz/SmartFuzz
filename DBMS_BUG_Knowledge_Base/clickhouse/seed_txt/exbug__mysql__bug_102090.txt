CREATE DATABASE test_db;
CREATE TABLE test_table1 (id Int32, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id PARTITION BY toYYYYMM(created_at);
CREATE TABLE test_table2 (id Int32, updated_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id PARTITION BY intDiv(toUnixTimestamp(updated_at), 2592000);
CREATE TABLE test_table3 (id Int32, event_date Date NOT NULL, description String) ENGINE = MergeTree() ORDER BY id PARTITION BY event_date;
SELECT * FROM test_table1;
