CREATE TABLE p ( groupid Int64 DEFAULT NULL, PRIMARY KEY (groupid));
CREATE TABLE g ( groupid Int64 DEFAULT NULL, parentid Int64 NOT NULL, PRIMARY KEY (groupid, parentid));
INSERT INTO p (groupid) VALUES (1), (2), (3);
INSERT INTO g (groupid, parentid) VALUES (1, 0), (2, 1), (3, 1);
WITH w AS ( SELECT groupid FROM p UNION ALL SELECT g.groupid FROM g JOIN w ON g.parentid = w.gid ) SELECT 1 FROM g WHERE g.groupid IN (SELECT gid FROM w);
