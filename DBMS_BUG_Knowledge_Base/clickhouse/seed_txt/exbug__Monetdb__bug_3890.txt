CREATE TABLE test_table(id Int32, name String, value Float32, created_at DateTime);
INSERT INTO test_table(id, name, value, created_at) VALUES (1, 'Alice', 10.5, now()), (2, 'Bob', 20.0, now()), (3, 'Charlie', NULL, now());
SELECT rowNumberInAllBlocks() OVER(ORDER BY name) AS row_num, name FROM (SELECT name FROM test_table WHERE value IS NOT NULL GROUP BY name) AS subquery;
CREATE TABLE another_table(ref_id Int32, description String);
INSERT INTO another_table(ref_id, description) VALUES (1, 'Test 1'), (2, 'Test 2'), (3, NULL);
