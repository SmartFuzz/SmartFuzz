CREATE TABLE t2 (id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t2 VALUES (1, 'Alice', 30), (2, 'Bob', 25);
DELETE FROM t2 WHERE id = (SELECT MAX(id) FROM (SELECT id FROM t2 WHERE age < 35) AS subquery);
CREATE TABLE t3 (id Int32, value Decimal(10, 2) DEFAULT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t3 VALUES (1, NULL), (2, 10.50);
