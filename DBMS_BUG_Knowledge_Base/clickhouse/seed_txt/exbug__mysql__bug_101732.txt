CREATE TABLE sbtest1 (id UInt32, k Int32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE sbtest2 (id UInt32, a Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO sbtest1 (id, k) SELECT seq, seq * 2 FROM (SELECT number AS seq FROM system.numbers LIMIT 10000000);
INSERT INTO sbtest2 (id) SELECT seq FROM (SELECT number AS seq FROM system.numbers LIMIT 10000000);
SELECT COUNT(*) FROM sbtest1 WHERE k IN (SELECT a FROM sbtest2 WHERE a IS NOT NULL);
