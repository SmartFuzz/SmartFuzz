CREATE TABLE point_tbl(f1 Tuple(float64, float64) NOT NULL, f2 Int32 CHECK (f2 > 0));
INSERT INTO point_tbl SELECT (0, 0), number FROM numbers(185);
INSERT INTO point_tbl VALUES ((0, NaN), 1);
SELECT f1, f2, sqrt(pow(f1.1 - 0, 2) + pow(f1.2 - 0, 2)) AS dist FROM point_tbl WHERE f2 IS NOT NULL ORDER BY dist;
