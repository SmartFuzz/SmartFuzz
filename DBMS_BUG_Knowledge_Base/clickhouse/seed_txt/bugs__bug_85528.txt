CREATE TABLE t1 (c0 Int, c1 String, c2 Float64) ENGINE = S3(s3, partition_strategy = 'hive', format = 'Parquet') PARTITION BY (c0);
CREATE TABLE t2 (c0 Int, c1 Nullable(String), c2 DateTime) ENGINE = MergeTree() ORDER BY c0;
INSERT INTO t1 VALUES (1, 'test', 1.5), (2, NULL, 2.5);
INSERT INTO t2 SELECT c0, c1, now() FROM t1 WHERE c0 > 0;
SELECT c0, count(*) FROM t2 GROUP BY c0 HAVING count(*) > 1;
