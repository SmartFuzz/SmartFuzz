CREATE TABLE test_table (id Int32, name String, description String, created_at DateTime DEFAULT now());
INSERT INTO test_table (id, name, description) VALUES (1, 'Test Entry', 'This is a test description for MARTIN AND LUTHER AND PRAISED');
SELECT * FROM test_table WHERE position(description, 'MARTIN AND LUTHER AND PRAISED') > 0;
ALTER TABLE test_table UPDATE description = NULL WHERE id = 1;
SELECT COUNT(*) FROM test_table WHERE description IS NULL;
