CREATE TABLE test_table1 (id Int32, name String, created_at DateTime) ENGINE = MergeTree() ORDER BY id;
GRANT TABLE ENGINE ON NonExistentEngine TO user1 WITH GRANT OPTION;
CREATE TABLE test_table2 (value Nullable(Float64), description String) ENGINE = Log;
GRANT TABLE ENGINE ON test_table2 TO user2;
SELECT * FROM test_table1 WHERE id = (SELECT MAX(id) FROM test_table1);
