CREATE TABLE test (s String, json JSON materialized s, id UInt32) ENGINE=MergeTree() ORDER BY id;
INSERT INTO test (s, id) VALUES ('example', 1);
ALTER TABLE test UPDATE s = 'updated' WHERE json.s = 'example';
DELETE FROM test WHERE json.s = 'updated';
SELECT * FROM test WHERE json.s IS NOT NULL;
