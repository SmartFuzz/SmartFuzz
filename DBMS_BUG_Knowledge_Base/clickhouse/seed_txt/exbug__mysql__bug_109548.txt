CREATE TABLE product_v2 (value Int32 NOT NULL, code String NOT NULL, name String DEFAULT NULL, comment String DEFAULT NULL, platform String DEFAULT NULL) ENGINE = MergeTree() ORDER BY value;
CREATE TABLE temp_product (temp_id Int32 NOT NULL, temp_code String, temp_name String) ENGINE = MergeTree() ORDER BY temp_id;
INSERT INTO product_v2 (code, name) VALUES ('test1', 'Test Product 1');
INSERT INTO temp_product (temp_id, temp_code, temp_name) SELECT value, code, name FROM product_v2 WHERE code IS NOT NULL;
SELECT COUNT(*) FROM (SELECT value FROM product_v2 GROUP BY code, name, comment, platform) AS derived;
