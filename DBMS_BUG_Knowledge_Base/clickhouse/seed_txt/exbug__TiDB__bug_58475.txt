DROP TABLE IF EXISTS tp;
CREATE TABLE tp (id Int32, c1 String, c2 String MATERIALIZED c1, c3 Int32 DEFAULT 0) ENGINE = MergeTree() PARTITION BY id % 1000 ORDER BY id;
INSERT INTO tp (id, c1, c3) VALUES (1, '[1]', 10);
INSERT INTO tp (id, c1) VALUES (0, '[2]');
SELECT id FROM tp WHERE position(c2, '1') > 0 OR c3 IS NOT NULL GROUP BY id HAVING id IN (0, 1);
