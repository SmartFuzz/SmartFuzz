CREATE DATABASE catalog_demo ENGINE = DataLakeCatalog SETTINGS catalog_type = 'glue', region = 'eu-west-1', aws_access_key_id = 'key', aws_secret_access_key = 'passwd';
CREATE TABLE catalog_demo.test (id UInt32, name String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO catalog_demo.test (id, name) VALUES (1, 'test1'), (2, 'test2');
SHOW TABLES FROM catalog_demo;
USE catalog_demo;
SHOW TABLES;
