CREATE TABLE t1 (id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t1(id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', 35);
CREATE TABLE t2 (ref_id Int32, description String) ENGINE = MergeTree() ORDER BY ref_id;
ALTER TABLE t2 ADD CONSTRAINT unique_ref_id_description UNIQUE(ref_id, description);
SELECT * FROM t1 WHERE age >= 0;
