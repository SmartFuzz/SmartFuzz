CREATE DATABASE d2;
CREATE TABLE t2 (id UInt32, name String, created_at DateTime DEFAULT now());
INSERT INTO t2 (id, name) VALUES (1, 'Test Name');
SELECT * FROM t2 WHERE id = (SELECT max(id) FROM t2);
SELECT '172.17.0.2:3306' AS clone_valid_donor_list;
