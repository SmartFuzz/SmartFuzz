CREATE TABLE v1 ( id UInt64, value String ) ENGINE = MergeTree() ORDER BY id;
INSERT INTO v1 (id, value) VALUES (1, 'A'), (2, 'B'), (3, 'C');
CREATE TABLE v2 ( id UInt64, ref_id UInt64, amount Float64 ) ENGINE = MergeTree() ORDER BY id;
INSERT INTO v2 (ref_id, amount) VALUES (1, 10.5), (1, 20.0), (2, NULL);
SELECT DISTINCT value, lead(value, 1, NULL) OVER() AS next_value FROM v1 ORDER BY value;
