CREATE TABLE t2(x Int32, y Float64, z String, w Date, v UInt8);
INSERT INTO t2(x, y, z, w, v) VALUES(1, 2.5, 'test', '2023-01-01', 1);
SELECT CASE WHEN y > (SELECT avg(y) FROM t2) THEN x * 3 ELSE CAST(z AS Int32) END FROM t2;
SELECT CASE WHEN z IS NULL THEN 0 ELSE length(z) END, CASE WHEN y < 0 THEN NULL ELSE y + 1 END FROM t2;
SELECT x, CASE WHEN y > (SELECT avg(y) FROM t2) THEN x * 3 ELSE CAST(z AS Int32) END, COALESCE(w, today()) FROM t2;
