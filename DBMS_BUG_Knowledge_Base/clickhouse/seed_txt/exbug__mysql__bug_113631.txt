CREATE TABLE t(a Int32, b String, c Float32, d Date);
INSERT INTO t VALUES (NULL, 'test1', 1.1, '2023-01-01'), (1, 'test2', 2.2, '2023-01-02'), (2, 'test3', NULL, '2023-01-03');
SELECT a, MIN(a) OVER (ORDER BY a ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS min_a FROM t;
CREATE TABLE u(x Int32 NOT NULL, y Int32 DEFAULT 0);
INSERT INTO u SELECT a, MIN(a) FROM t GROUP BY a;
