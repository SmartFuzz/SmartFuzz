CREATE TABLE test_insert_1 (`logged_at` DateTime64(6), `id` String, `is_pk` UInt8) ENGINE = ReplicatedMergeTree('/clickhouse/tables/{uuid}/{shard}', '{replica}') ORDER BY tuple() SETTINGS index_granularity = 8192;
INSERT INTO test_insert_1 SELECT now64(6), 'test1', 1;
CREATE TABLE test_insert_2 (`logged_at` DateTime64(6), `id` String, `is_pk` UInt8, `value` Float64) ENGINE = ReplicatedMergeTree('/clickhouse/tables/{uuid}/{shard}', '{replica}') ORDER BY tuple() SETTINGS index_granularity = 8192;
INSERT INTO test_insert_2 WITH cte_test AS (SELECT now64(6), 'test2', 1, CAST(3.14 AS Float64)) SELECT * FROM cte_test;
CREATE TABLE test_insert_3 (`logged_at` DateTime64(6), `id` String, `is_pk` UInt8, `nullable_value` Nullable(Int32)) ENGINE = ReplicatedMergeTree('/clickhouse/tables/{uuid}/{shard}', '{replica}') ORDER BY tuple() SETTINGS index_granularity = 8192;
