CREATE TABLE users (user_id Int32, username String) ENGINE = MergeTree() ORDER BY user_id;
CREATE TABLE orders (order_id Int32, user_id Int32) ENGINE = MergeTree() ORDER BY order_id;
INSERT INTO users (user_id, username) VALUES (1, 'alice');
INSERT INTO orders (order_id, user_id) VALUES (101, 1);
SELECT * FROM users;
DELETE FROM users WHERE user_id = 1;
