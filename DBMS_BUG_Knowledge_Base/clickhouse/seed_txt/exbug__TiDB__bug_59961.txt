CREATE TABLE table2 (id Int32, name String NOT NULL, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO table2 VALUES (1, 'Alice', 30);
INSERT INTO table2 VALUES (2, 'Bob', NULL);
ALTER TABLE table2 ADD INDEX idx_name(name) TYPE minmax GRANULARITY 1;
SELECT * FROM table2 WHERE age IS NULL;
