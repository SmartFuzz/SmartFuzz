CREATE TABLE test_strings (id Int32, title String NOT NULL, lang_title String);
INSERT INTO test_strings (id, title, lang_title) VALUES (1, 'Sample Title', 'Sample Title');
SELECT id, title, toString(lang_title) AS lang FROM test_strings WHERE lang_title IS NOT NULL;
SELECT 'rdf:langString' AS datatype WHERE id = 1;
SELECT lang_title FROM test_strings WHERE id = 1;
