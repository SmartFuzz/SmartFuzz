CREATE TABLE test_table (id Int32, name String, created_at DateTime DEFAULT now(), amount Decimal(10, 2), status UInt8);
INSERT INTO test_table (id, name, amount, status) VALUES (1, 'Test Product', 99.99, 1);
INSERT INTO test_table (id, name, amount, status) VALUES (2, 'Another Product', NULL, 0);
SELECT name, amount FROM test_table WHERE amount IS NOT NULL ORDER BY amount DESC LIMIT 5;
SELECT * FROM (SELECT * FROM test_table ORDER BY created_at DESC LIMIT 10) LIMIT 3;
