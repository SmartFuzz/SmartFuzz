CREATE TABLE test_labels (id Int32, label String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_labels (id, label) VALUES (1, 'leden'), (2, 'únor'), (3, 'Leden'), (4, 'Únor');
SELECT label FROM test_labels WHERE label LIKE 'l%' OR label LIKE 'ú%';
SELECT (label LIKE 'L%' OR label LIKE 'Ú%') AS matches FROM test_labels WHERE label IS NOT NULL;
SELECT COUNT(*) FROM test_labels WHERE (label LIKE 'l%' OR label LIKE 'ú%') OR label IS NULL;
