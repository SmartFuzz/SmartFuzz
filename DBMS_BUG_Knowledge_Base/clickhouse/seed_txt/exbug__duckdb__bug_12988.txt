CREATE TABLE employees (id Int32, name String, salary Decimal(10, 2), department_id Int32);
CREATE TABLE departments (id Int32, name String);
INSERT INTO employees VALUES (1, 'John Doe', 50000.00, 1), (2, 'Jane Smith', 60000.00, 2), (3, 'Alice Johnson', 55000.00, 1);
INSERT INTO departments VALUES (1, 'Engineering'), (2, 'Marketing');
WITH ranked_employees AS (SELECT *, rowNumberInAllBlocks() OVER (ORDER BY salary) AS salary_rank FROM employees) SELECT * FROM departments JOIN ranked_employees ON departments.id = ranked_employees.department_id;
