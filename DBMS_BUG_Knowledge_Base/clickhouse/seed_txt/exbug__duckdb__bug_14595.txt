CREATE TABLE test_data (id Int32, category String, value Int32);
INSERT INTO test_data VALUES (1, 'X', 150), (2, 'Y', 250), (3, 'X', 350), (4, 'Y', 450), (5, NULL, 550);
SELECT category, value, COUNT(*) AS count_per_category FROM test_data GROUP BY category, value;
CREATE TABLE boundary_data (id Int32, amount Decimal(10, 2) NOT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO boundary_data VALUES (1, 0.00), (2, 9999999999.99);
SELECT amount, ROW_NUMBER() OVER (ORDER BY amount) AS row_num FROM boundary_data;
