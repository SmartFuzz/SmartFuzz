CREATE TABLE x (y String, z String) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO x VALUES('aaa bbb ccc ddd', 'bbb ddd aaa');
CREATE TABLE y (a String, b String) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO y VALUES('aaa', NULL), ('bbb', 'ccc');
SELECT * FROM x WHERE position(y, 'bbb') > 0 AND position(y, 'aaa') > 0 AND abs(position(y, 'bbb') - position(y, 'aaa')) <= 6;
