CREATE TABLE test_single_file (ts DateTime64(9, 'UTC')) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO test_single_file (ts) SELECT ts FROM s3('http://minio:9000/test/output.parquet', 'minio', 'minio123');
CREATE TABLE test_brace_expression (ts DateTime64(9, 'UTC')) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO test_brace_expression (ts) SELECT ts FROM s3('http://minio:9000/test/output.parquet', 'minio', 'minio123') UNION ALL SELECT ts FROM s3('http://minio:9000/test/output.parquet', 'minio', 'minio123');
CREATE TABLE test_nullable (ts Nullable(DateTime64(9, 'UTC'))) ENGINE = MergeTree() ORDER BY tuple();
