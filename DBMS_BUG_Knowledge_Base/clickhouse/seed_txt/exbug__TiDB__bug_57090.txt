CREATE TABLE user_data (user_id UInt32 NOT NULL, username String NOT NULL, email String NOT NULL, created_at DateTime DEFAULT now(), preferences JSON DEFAULT NULL, PRIMARY KEY (user_id));
INSERT INTO user_data (username, email, preferences) VALUES ('john_doe', 'john@example.com', '{"theme": "dark", "notifications": true}');
CREATE TABLE transaction_log (transaction_id UInt32 NOT NULL, user_id UInt32 NOT NULL, amount Decimal(10, 2) NOT NULL, transaction_date DateTime DEFAULT now(), PRIMARY KEY (transaction_id), FOREIGN KEY (user_id) REFERENCES user_data(user_id));
INSERT INTO transaction_log (user_id, amount) VALUES (1, 150.75);
SELECT u.username, t.amount FROM user_data u JOIN transaction_log t ON u.user_id = t.user_id WHERE t.amount > 100.00 ORDER BY t.transaction_date DESC;
