DROP DATABASE IF EXISTS test;
CREATE DATABASE test;
CREATE TABLE products (id UInt32 NOT NULL, name String NOT NULL, price Decimal(10,2) NOT NULL, created_at DateTime DEFAULT now(), PRIMARY KEY (id));
INSERT INTO products (id, name, price) VALUES (1, 'Product A', 10.50), (2, 'Product B', 20.75), (3, 'Product C', 5.25);
SELECT MAX(price) AS max_price, COUNT(*) AS product_count FROM products GROUP BY name HAVING (MAX(price) < 15);
