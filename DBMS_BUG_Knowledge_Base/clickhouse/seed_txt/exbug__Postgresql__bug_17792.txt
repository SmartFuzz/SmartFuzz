CREATE TABLE source (sid Int32, sname String) ENGINE = MergeTree() ORDER BY sid;
INSERT INTO source (sid, sname) VALUES (1, 'Source1');
CREATE TABLE target (tid Int32, tval Int32 DEFAULT 0) ENGINE = MergeTree() ORDER BY tid;
INSERT INTO target (tid, tval) VALUES (1, 1);
INSERT INTO target (tid, tval) SELECT s.sid, tval + 1 FROM target t JOIN source s ON t.tid = s.sid;
