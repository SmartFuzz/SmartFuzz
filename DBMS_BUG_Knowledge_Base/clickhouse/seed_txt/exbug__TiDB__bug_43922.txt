CREATE TABLE test.t2(c1 Int32, c2 String, c3 Date, c4 Float32 DEFAULT 0.0);
INSERT INTO test.t2 VALUES(1, 'a', '2023-01-01', 1.1), (2, 'b', NULL, 2.2), (3, 'c', '2023-01-03', NULL), (4, 'd', '2023-01-04', 4.4);
CREATE TABLE test.t3(c1 Int32, c2 Int32) ENGINE = MergeTree() ORDER BY c1;
INSERT INTO test.t3 SELECT c1, COUNT(*) FROM test.t2 GROUP BY c1;
EXPLAIN SELECT c1, row_number() OVER() FROM (SELECT c1 FROM test.t3 WHERE c2 IS NOT NULL) t;
