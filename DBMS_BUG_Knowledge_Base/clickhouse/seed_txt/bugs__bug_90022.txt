CREATE TABLE IF NOT EXISTS default.http_log (timestamp DateTime64(9,'UTC'), sip IPv6, sport UInt16, dip IPv6, dport UInt16, host String) ENGINE = MergeTree PARTITION BY toYYYYMMDD(timestamp) ORDER BY (timestamp);
INSERT INTO http_log VALUES (now(),'1:1:1:1',40001,'2:2:2:2',8080,'api.example.com'), (now(),'1:1:1:1',40001,'2:2:2:2',8080,'blog.example.com'), (now(),'1:1:1:1',40001,'2:2:2:2',8080,'x.com'), (now(),'1:1:1:1',40001,'2:2:2:2',8080,'myownck.com');
CREATE TABLE IF NOT EXISTS default.domain_whitelist (domain String) ENGINE = MergeTree ORDER BY (domain);
INSERT INTO domain_whitelist VALUES ('example.com'), ('x.com'), ('test.com');
SELECT host FROM http_log a WHERE NOT EXISTS (SELECT 1 FROM domain_whitelist WHERE endsWith(a.host, domain));
