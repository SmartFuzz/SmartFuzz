CREATE TABLE t2 (id UInt32, name String, age UInt32, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t2 (name, age) VALUES ('Alice', 30), ('Bob', 25), ('Charlie', 35), ('David', 40);
SELECT * FROM t2 WHERE age > 30 ORDER BY created_at LIMIT 2 OFFSET 1;
CREATE TABLE t3 (id UInt32, description String NOT NULL, price Decimal(10, 2) CHECK (price >= 0)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t3 (description, price) VALUES ('Item A', 10.99), ('Item B', 20.50), ('Item C', 15.00);
