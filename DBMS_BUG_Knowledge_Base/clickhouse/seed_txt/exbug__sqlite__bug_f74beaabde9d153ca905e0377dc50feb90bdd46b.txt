CREATE TABLE users (id UInt32, name String, age Nullable(UInt32));
CREATE TABLE posts (id UInt32, user_id UInt32, content String);
CREATE TABLE comments (id UInt32, post_id UInt32, user_id UInt32, comment String);
INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', 25), ('Charlie', NULL);
INSERT INTO posts (user_id, content) VALUES (1, 'Post 1'), (2, 'Post 2');
SELECT u.name, p.content, c.comment FROM users u JOIN posts p ON u.id = p.user_id JOIN comments c ON p.id = c.post_id WHERE u.age IS NOT NULL;
