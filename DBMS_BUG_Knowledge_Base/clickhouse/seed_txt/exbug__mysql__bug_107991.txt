CREATE TABLE users (id UInt32 NOT NULL, username String NOT NULL, email String NOT NULL, created_at DateTime DEFAULT now(), PRIMARY KEY (id)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users (id, username, email) VALUES (1, 'user1', 'user1@example.com');
CREATE TABLE orders (order_id UInt32 NOT NULL, user_id UInt32 NOT NULL, amount Decimal(10, 2) NOT NULL, order_date DateTime DEFAULT now(), PRIMARY KEY (order_id)) ENGINE = MergeTree() ORDER BY order_id;
INSERT INTO orders (order_id, user_id, amount) VALUES (1, 1, 99.99);
SELECT u.username, o.amount FROM users AS u JOIN orders AS o ON u.id = o.user_id WHERE o.amount > 50;
