CREATE TABLE t1 (id Int32, name String, age UInt8, salary Float64, created_at DateTime DEFAULT now()) ENGINE = IcebergLocal(local, path = '/var/lib/clickhouse/user_files/lakehouse/t1/', format = 'ORC');
INSERT INTO t1 (id, name, age, salary) VALUES (1, 'Alice', 30, 70000.50);
INSERT INTO t1 (id, name, age, salary) SELECT number, concat('User', number), number % 100, number * 1000.0 FROM numbers(10);
CREATE TABLE t2 (product_id Int32, product_name String, price Float32, stock Nullable(Int32)) ENGINE = IcebergLocal(local, path = '/var/lib/clickhouse/user_files/lakehouse/t2/', format = 'ORC');
INSERT INTO t2 (product_id, product_name, price, stock) SELECT id, name, price, NULL FROM (SELECT number AS id, concat('Product', number) AS name, number * 10.5 AS price FROM numbers(5));
