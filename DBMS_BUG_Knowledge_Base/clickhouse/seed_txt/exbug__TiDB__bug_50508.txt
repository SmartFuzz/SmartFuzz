CREATE TABLE users(id Int32, name String, age Int32, created_at DateTime DEFAULT now());
INSERT INTO users VALUES (1, 'Alice', 30), (2, 'Bob', NULL), (3, 'Charlie', 25);
CREATE TABLE orders(order_id Int32, user_id Int32, amount Decimal(10, 2), PRIMARY KEY (order_id), FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO orders VALUES (1, 1, 100.50), (2, 2, 200.75), (3, 3, NULL);
SELECT u.name, o.amount, sleep(0.1) FROM users u LEFT JOIN orders o ON u.id = o.user_id WHERE u.age IS NOT NULL;
