CREATE TABLE t1(id Int32, name String, active UInt8);
INSERT INTO t1 VALUES (1, 'Alice', 1), (2, 'Bob', 0);
SELECT NULL AS x, 1 AS y;
SELECT (if(isNull(x), t1.id, x) OR t1.active) FROM t1 WHERE t1.id = 1;
