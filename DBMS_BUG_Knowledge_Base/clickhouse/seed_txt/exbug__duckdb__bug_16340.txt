CREATE TABLE test_table (id UInt32, name String, description String);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description.');
SELECT replace(description, '*', 'X') FROM test_table WHERE id = 1;
SELECT arrayJoin(regexpExtractAll(name, '[*]')) FROM test_table WHERE id = 1;
SELECT replace(NULL, '*', 'X') AS result;
