CREATE TABLE t4 (id UInt32 NOT NULL, name String NOT NULL, age Nullable(Int32), PRIMARY KEY (name, id)) ENGINE = MergeTree() ORDER BY (name, id);
CREATE TABLE t5 (id UInt32 NOT NULL, description String, created_at DateTime DEFAULT now(), PRIMARY KEY (created_at, id)) ENGINE = MergeTree() ORDER BY (created_at, id);
INSERT INTO t4 (name, age) VALUES ('Alice', 30);
INSERT INTO t5 (description) VALUES ('Sample description');
SELECT * FROM t4 WHERE age IS NULL OR age > 25;
