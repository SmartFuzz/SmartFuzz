CREATE TABLE test (id Int32, value String NOT NULL, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test (id, value, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 3, NULL);
CREATE TABLE additional_info (info_id Int32, test_id Int32, description String) ENGINE = MergeTree() ORDER BY info_id;
INSERT INTO additional_info (info_id, test_id, description) VALUES (1, 1, 'Engineer'), (2, 2, 'Designer');
SELECT * FROM test WHERE value != 'Bob';
