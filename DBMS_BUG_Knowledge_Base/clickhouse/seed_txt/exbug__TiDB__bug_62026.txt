DROP TABLE IF EXISTS t2;
CREATE TABLE t2(id Int32, c1 String, c2 Float64, c3 Date);
INSERT INTO t2(c1, c2, c3) VALUES ('test1', 1.5, '2023-01-01'), ('test2', NULL, '2023-02-01'), ('test3', 2.5, NULL);
SELECT any(c1) OVER (ORDER BY id) AS last_value FROM t2 CROSS JOIN t0;
SELECT any(c2) OVER (ORDER BY id) AS last_value FROM t2 CROSS JOIN t1;
