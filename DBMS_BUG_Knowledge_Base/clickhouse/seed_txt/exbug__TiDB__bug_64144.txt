CREATE TABLE test.b (id UInt64, description String NOT NULL, age Int32) ENGINE = MergeTree() PRIMARY KEY id;
INSERT INTO test.b VALUES (1, 'Sample�Text', 25);
SELECT hex(description) FROM test.b WHERE id = 1;
SELECT * FROM test.b WHERE description = (SELECT description FROM test.b WHERE id = 1);
ALTER TABLE test.b DELETE WHERE id = 1;
