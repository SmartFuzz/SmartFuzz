DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id UInt32, name String, INDEX `invalid_index_\x80\xFF` name TYPE minmax() GRANULARITY 1) ENGINE = MergeTree ORDER BY id;
INSERT INTO test_table VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie'), (4, 'Дмитрий'), (5, '测试');
SELECT count() FROM test_table;
DETACH TABLE test_table;
