CREATE TABLE sbtest2 ( id Int32 NOT NULL, k Int32 NOT NULL DEFAULT 0, c FixedString(120) NOT NULL DEFAULT '', pad FixedString(60) NOT NULL DEFAULT '', ec1 Nullable(String), PRIMARY KEY (id), INDEX k_2 (k) ) ENGINE = MergeTree() ORDER BY id;
INSERT INTO sbtest2 (k, c, pad, ec1) VALUES (1, 'test', 'padding', NULL);
UPDATE sbtest2 SET ec1 = 'updated' WHERE id = 1;
SELECT * FROM sbtest2 WHERE k IN (SELECT k FROM sbtest2 WHERE ec1 IS NOT NULL);
SYSTEM RELOAD DICTIONARY sbtest2;
