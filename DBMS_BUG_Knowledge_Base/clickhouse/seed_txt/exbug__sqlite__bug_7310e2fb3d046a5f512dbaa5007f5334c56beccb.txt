CREATE TABLE a(id UInt32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE b(id UInt32, a_id UInt32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE c(id UInt32, value Float64 DEFAULT 0.0) ENGINE = MergeTree() ORDER BY id;
INSERT INTO a(name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', -1);
SELECT * FROM a JOIN b ON a.id = b.a_id WHERE (a.id, a.age) IN (SELECT id, age FROM a WHERE age IS NOT NULL);
