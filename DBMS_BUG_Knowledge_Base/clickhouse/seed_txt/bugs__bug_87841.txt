CREATE TABLE table_src (order_id Int64, random_date Date, amount Float64) ENGINE = MergeTree() ORDER BY order_id;
CREATE TABLE table_dst (order_id Int64, random_date Date, amount Float64) ENGINE = MergeTree() PARTITION BY toStartOfMonth(random_date) ORDER BY tuple();
INSERT INTO table_src SELECT number AS order_id, toDate(rand() % 3650) AS random_date, rand() * 100 AS amount FROM numbers(1000000);
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src WHERE amount > 50.0;
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src ORDER BY toStartOfMonth(random_date) SETTINGS min_insert_block_size_rows = 500000;
