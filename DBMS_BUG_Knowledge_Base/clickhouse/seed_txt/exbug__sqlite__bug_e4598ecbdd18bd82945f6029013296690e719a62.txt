CREATE TABLE t2(b Int32, c String) ENGINE = MergeTree() ORDER BY b;
INSERT INTO system.parts (table, name, rows, bytes) VALUES ('t2', 'dummy', 0, 0);
SELECT COUNT(*) FROM t2 WHERE b IN (SELECT b FROM t2 WHERE c IS NULL);
CREATE TABLE t3(d Float64, e Int32 DEFAULT 0) ENGINE = MergeTree() ORDER BY d;
