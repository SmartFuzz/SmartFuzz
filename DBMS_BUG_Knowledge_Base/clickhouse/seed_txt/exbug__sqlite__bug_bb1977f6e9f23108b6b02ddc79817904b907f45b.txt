CREATE TABLE users (id UInt32, name String, age Nullable(UInt32), email String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');
INSERT INTO users (name, age, email) VALUES ('Bob', NULL, 'bob@example.com');
SELECT concat(name, ' is ', toString(age), ' years old') AS user_info FROM users WHERE age IS NOT NULL;
SELECT concat(name, ' - ', if(email IS NULL, 'no email', email)) AS user_contact FROM users;
