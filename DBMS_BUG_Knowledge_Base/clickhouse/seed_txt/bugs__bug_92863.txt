DROP DATABASE IF EXISTS bug_repro;
CREATE DATABASE bug_repro ENGINE = Memory;
CREATE TABLE IF NOT EXISTS user_data (user_id UInt32, user_name String, user_age UInt8, user_email String, user_active Boolean) ENGINE = MergeTree() ORDER BY user_id;
INSERT INTO user_data VALUES (1, 'Alice', 30, 'alice@example.com', true);
SELECT COUNT(*) FROM user_data WHERE user_age > 25 AND user_active = true;
