CREATE DATABASE test_db ON CLUSTER 'test_cluster';
CREATE TABLE test_db.test_table ON CLUSTER 'test_cluster' (id UInt32, name String, revenue Float64, created_at DateTime DEFAULT now()) ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/test_db/test_table', '{replica}') ORDER BY id;
INSERT INTO test_db.test_table (id, name, revenue) VALUES (1, 'Alice', 1000.50), (2, 'Bob', 2000.75), (3, 'Charlie', NULL);
CREATE DICTIONARY test_db.test_dictionary ON CLUSTER 'test_cluster' (id UInt32, name String, is_high_revenue Boolean) PRIMARY KEY id LAYOUT(COMPLEX_KEY_HASHED()) SOURCE (CLICKHOUSE(DB 'test_db' QUERY 'SELECT id, name, revenue > 1500 AS is_high_revenue FROM test_db.test_table WHERE revenue IS NOT NULL')) LIFETIME(MIN 0 MAX 0);
SELECT id, name FROM clusterAllReplicas('test_cluster', test_db.test_dictionary) WHERE is_high_revenue = 1;
