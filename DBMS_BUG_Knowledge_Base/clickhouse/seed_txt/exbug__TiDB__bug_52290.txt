CREATE TABLE table_4_mixed_types (id UInt32, name String, score Decimal(10, 2), created_at DateTime DEFAULT now());
INSERT INTO table_4_mixed_types (id, name, score) VALUES (1, 'Alice', 85.50), (2, 'Bob', 90.00), (3, 'Charlie', NULL);
SELECT name FROM table_4_mixed_types WHERE score IS NOT NULL HAVING score >= 80.00;
SELECT name FROM table_4_mixed_types WHERE score IS NOT NULL HAVING score >= (SELECT AVG(score) FROM table_4_mixed_types WHERE score IS NOT NULL);
SELECT COUNT() AS total FROM table_4_mixed_types HAVING total > 0;
