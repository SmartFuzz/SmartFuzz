CREATE TABLE users(id Int32, name String, created_at DateTime DEFAULT now());
ALTER TABLE users ADD COLUMN email String DEFAULT 'example@example.com';
INSERT INTO users(id, name) VALUES(1, 'Alice');
SELECT * FROM users WHERE created_at IS NOT NULL;
ALTER TABLE users ADD COLUMN age Int32 DEFAULT 18;
