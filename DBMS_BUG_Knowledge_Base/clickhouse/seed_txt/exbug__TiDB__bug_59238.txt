CREATE TABLE t2 ( id Int32, name String, created_at DateTime DEFAULT now() ) ENGINE = MergeTree() ORDER BY id PARTITION BY (id % 100);
INSERT INTO t2 (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (51, 'Charlie');
SELECT id, name, created_at FROM t2 WHERE created_at IS NOT NULL;
ALTER TABLE t2 DROP PARTITION 0;
-- Simulating TRUNCATE by dropping the partition;
SELECT table, partition, create_time FROM system.parts WHERE table = 't2';
