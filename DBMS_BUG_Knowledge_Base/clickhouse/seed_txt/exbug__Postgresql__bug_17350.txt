CREATE TABLE test_table (id UInt32, name String, description String);
INSERT INTO test_table (id, name, description) VALUES (1, 'foo', 'A test entry'), (2, 'BAR', 'Another test entry');
CREATE TABLE test_idx AS SELECT lower(name) AS lower_name, description FROM test_table;
SELECT lower_name FROM test_idx WHERE lower_name LIKE '%o%';
