CREATE TABLE test_dates (id UInt32, event_date Date) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_dates (id, event_date) VALUES (1, '2021-05-22');
SELECT toDate(event_date + INTERVAL 1 DAY) AS next_day FROM test_dates WHERE id = 1;
SELECT * FROM test_dates;
