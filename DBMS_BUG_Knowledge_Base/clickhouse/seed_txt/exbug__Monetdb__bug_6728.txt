CREATE TABLE test_table(id Int32, name String, value Float32);
INSERT INTO test_table VALUES (1, 'apple', 1.1), (2, 'banana', 2.2), (3, 'apple', 1.1), (4, 'banana', 2.3);
SELECT DISTINCT name, value FROM test_table;
SELECT DISTINCT CASE WHEN value > 2.0 THEN name ELSE NULL END AS filtered_name FROM test_table;
SELECT name, COUNT(*) AS count FROM test_table GROUP BY name HAVING count > 1;
