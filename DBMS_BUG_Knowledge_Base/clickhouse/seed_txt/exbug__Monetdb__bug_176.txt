CREATE TABLE test_table (id UInt32, name String NOT NULL, age Int32) ENGINE = MergeTree() ORDER BY id;
ALTER TABLE test_table ADD COLUMN id UInt32 DEFAULT generateUUIDv4();
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
INSERT INTO test_table (name, age) VALUES ('Bob', NULL);
SELECT name FROM test_table ORDER BY age;
SELECT id, name FROM test_table WHERE age IS NOT NULL ORDER BY id DESC;
