CREATE TABLE t(p UInt8, q Int32, r String DEFAULT 'default');
INSERT INTO t(p, q) VALUES (NULL, 1);
SELECT lockTable(2);
WITH lock_t AS (SELECT * FROM t WHERE p IS NULL LIMIT 1) UPDATE t SET q = q + 1 WHERE (p IS NULL) AND (q = (SELECT q FROM lock_t)) RETURNING p, q;
WITH lock_t AS (SELECT * FROM t LIMIT 1) UPDATE t SET r = 'updated' WHERE (p IS NULL) AND (q = (SELECT q FROM lock_t)) RETURNING r;
