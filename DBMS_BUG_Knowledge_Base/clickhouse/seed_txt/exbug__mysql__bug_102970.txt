CREATE TABLE t2 (id UInt32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t2 VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', 35);
INSERT INTO t2 VALUES (2, 'David', 40) ON DUPLICATE KEY UPDATE age = age;
SELECT * FROM t2 WHERE age IS NULL;
SELECT * FROM t2;
