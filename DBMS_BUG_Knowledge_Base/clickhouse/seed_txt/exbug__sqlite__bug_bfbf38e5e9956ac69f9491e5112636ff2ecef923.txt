CREATE TABLE users (id UInt32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE posts (id UInt32, user_id UInt32, content String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', -1);
INSERT INTO posts (user_id, content) VALUES (1, 'Hello World'), (2, 'Another Post'), (3, NULL);
SELECT u.name, (SELECT COUNT(*) FROM posts p WHERE p.user_id = u.id) AS post_count FROM users u WHERE u.age IS NOT NULL;
