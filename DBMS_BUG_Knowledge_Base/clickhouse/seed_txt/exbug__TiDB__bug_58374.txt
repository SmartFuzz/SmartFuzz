CREATE TABLE IF NOT EXISTS testPartition2 ( ID Int32, OrderDate DateTime, Status String DEFAULT 'Pending' ) ENGINE = MergeTree() ORDER BY ID;
INSERT INTO testPartition2 (ID, OrderDate) VALUES (1, NULL);
INSERT INTO testPartition2 (ID, OrderDate, Status) VALUES (2, '2024-01-01 00:00:00', 'Completed');
SELECT * FROM testPartition2 WHERE OrderDate IS NULL;
SELECT COUNT(*) FROM testPartition2 WHERE Status = 'Pending' OR OrderDate IS NULL;
