CREATE TABLE t_partition_test (id Int32 NOT NULL, value String NOT NULL, created_at DateTime DEFAULT now(), PRIMARY KEY (id)) ENGINE = MergeTree() ORDER BY id PARTITION BY (value) SETTINGS partition_by = 'value';
INSERT INTO t_partition_test (value) VALUES ('apple'), ('banana'), ('cherry'), ('date'), ('fig');
SELECT * FROM t_partition_test WHERE value BETWEEN 'a' AND 'z' OR value IN ('banana', 'date') GROUP BY value HAVING COUNT(id) > 0;
CREATE TABLE t_subquery_test (id Int32 NOT NULL, ref_id Int32, PRIMARY KEY (id)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t_subquery_test (id, ref_id) VALUES (1, 1), (2, 2), (3, NULL);
