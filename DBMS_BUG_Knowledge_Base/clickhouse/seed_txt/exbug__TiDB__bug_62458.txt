CREATE TABLE t1 (id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id PARTITION BY (age < 18) AS (SELECT * FROM t1 WHERE age < 18);
INSERT INTO t1 (id, name, age) VALUES (1, 'Alice', 25), (2, 'Bob', 17), (3, 'Charlie', 35);
ALTER TABLE t1 DELETE WHERE age = 40;
CREATE TABLE t2 (c1 Int32, c2 String, c3 Date DEFAULT NULL) ENGINE = MergeTree() ORDER BY c1 PARTITION BY (c1 IN (1, 2));
ALTER TABLE t2 DELETE WHERE c1 IS NULL;
