CREATE TABLE test_table (id UInt32, name String, value Float64, created_at DateTime) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table VALUES (1, 'test1', 1.23, now()), (2, 'test2', NULL, now()), (3, NULL, 3.45, now());
CREATE TABLE string_or_string (string1 String, string2 String) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO string_or_string VALUES ('str1', ''), ('', 'str2'), (NULL, 'str3');
SELECT *, if(string1 IS NOT NULL, 'string1', 'string2') AS string_oneof_present FROM string_or_string;
