CREATE TABLE test_table ( id UInt32 NOT NULL, data JSON NOT NULL, created_at DateTime DEFAULT now(), PRIMARY KEY (id)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (data) VALUES (JSON_ARRAY(1, 2, 3, 4, 5));
INSERT INTO test_table (data) VALUES (NULL);
SELECT * FROM test_table WHERE JSON_CONTAINS(data, '2') OR created_at IS NULL;
CREATE TABLE another_table ( value String NOT NULL, amount Decimal(10, 2) NOT NULL, UNIQUE INDEX unique_value (value)) ENGINE = MergeTree() ORDER BY value;
