CREATE TABLE users (id UInt32, name String NOT NULL, age Nullable(UInt32), email String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');
INSERT INTO users (name, age) VALUES ('Bob', NULL);
ALTER TABLE users UPDATE email = NULL WHERE id = 1;
SELECT * FROM users WHERE age > (SELECT AVG(age) FROM users WHERE email IS NOT NULL);
