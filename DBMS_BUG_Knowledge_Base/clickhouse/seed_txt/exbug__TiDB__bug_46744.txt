CREATE TABLE comments ( id UInt64 NOT NULL, post_id UInt64 NOT NULL, content String NOT NULL, created_at DateTime DEFAULT now(), PRIMARY KEY (id), FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE );
INSERT INTO comments (post_id, content) VALUES (1, 'Great post!'), (2, 'Interesting thoughts!'), (3, 'I disagree with this.'), (1, NULL);
SELECT upper(title) AS upper_title FROM posts WHERE author_id IS NULL LIMIT 1;
SELECT COUNT(*) FROM comments WHERE content IS NULL;
SELECT posts.title, (SELECT COUNT(*) FROM comments WHERE post_id = posts.id) AS comment_count FROM posts WHERE title LIKE '%weblog%';
