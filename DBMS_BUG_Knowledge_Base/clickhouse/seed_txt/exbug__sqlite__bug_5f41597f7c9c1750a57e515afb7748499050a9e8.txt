CREATE TABLE test_table (id UInt32, name String, is_active UInt8 DEFAULT 1);
INSERT INTO test_table (name, is_active) VALUES ('Alice', NULL);
INSERT INTO test_table (name, is_active) VALUES ('Bob', 0);
SELECT name, is_active FROM test_table WHERE is_active IS NOT NULL;
ALTER TABLE test_table UPDATE is_active = 1 WHERE id = 1;
