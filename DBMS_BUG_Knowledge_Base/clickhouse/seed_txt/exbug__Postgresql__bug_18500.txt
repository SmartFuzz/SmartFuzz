CREATE TABLE t (a Int32, b String) ENGINE = MergeTree() ORDER BY a;
CREATE TABLE tp (a Int32, c UInt8 DEFAULT 1) ENGINE = MergeTree() ORDER BY a;
ALTER TABLE t ADD PARTITION '0-1000' PARTITION BY range(a) VALUES (0, 1000);
CREATE UNIQUE INDEX t_a_idx ON t (a, b);
ALTER TABLE t ATTACH PARTITION '0-1000' FROM tp;
