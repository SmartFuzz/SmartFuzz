CREATE TABLE x (id Int32, value String);
CREATE TABLE y (id Int32, value String);
INSERT INTO x VALUES (1, 'test1'), (2, 'test2');
INSERT INTO y VALUES (1, 'test3'), (2, 'test4');
CREATE TABLE xy AS SELECT x.id, x.value AS x_value, y.value AS y_value FROM x JOIN y ON x.id = y.id;
