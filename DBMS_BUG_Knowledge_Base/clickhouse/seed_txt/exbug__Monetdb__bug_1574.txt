CREATE TABLE test2 (id Int32 NOT NULL, value String, created_at Date) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test2 VALUES (1, 'First', '2023-01-01');
INSERT INTO test2 VALUES (2, 'Second', '2023-01-02');
SELECT id, (SELECT value FROM test2 WHERE created_at > t2.created_at LIMIT 1) AS next_value FROM test2 t2;
SELECT created_at, COUNTIf(created_at <= t3.created_at) AS count_before FROM test2 t3 GROUP BY created_at;
