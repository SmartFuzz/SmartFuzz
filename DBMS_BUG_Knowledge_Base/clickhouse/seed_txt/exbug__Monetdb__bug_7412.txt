CREATE TABLE test_intervals (id Int32 PRIMARY KEY, interval_value Interval);
INSERT INTO test_intervals (id, interval_value) VALUES (1, toInterval('0:0:0'));
INSERT INTO test_intervals (id, interval_value) VALUES (2, toInterval('1:30:00'));
SELECT id, interval_value FROM test_intervals WHERE interval_value IS NOT NULL;
SELECT id FROM test_intervals WHERE interval_value > toInterval('0:0:0');
