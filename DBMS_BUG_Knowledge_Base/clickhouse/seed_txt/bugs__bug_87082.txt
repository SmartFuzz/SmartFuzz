CREATE DATABASE db2;
CREATE TABLE tbl2 (id UInt32, name String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO tbl2 (id, name) VALUES (1, 'db2 data');
CREATE VIEW v2 AS SELECT id, name, {p:String} AS p FROM tbl2;
CREATE MATERIALIZED VIEW mv3 REFRESH EVERY 2 MINUTE ORDER BY id AS SELECT id, name, p FROM v2(p='db2 mv3');
