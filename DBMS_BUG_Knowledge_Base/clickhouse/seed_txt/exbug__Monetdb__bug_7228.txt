CREATE TABLE c(x Int32, y Float32, z String) ENGINE = MergeTree() ORDER BY x;
INSERT INTO c VALUES (1, 1.1, 'test1');
CREATE TABLE d(m Int32, n Int64) ENGINE = MergeTree() ORDER BY m;
ALTER TABLE d ADD CONSTRAINT unique_mn UNIQUE (m, n);
INSERT INTO d VALUES (1, 10000000000);
UPDATE c SET y = NULL WHERE x = 1;
