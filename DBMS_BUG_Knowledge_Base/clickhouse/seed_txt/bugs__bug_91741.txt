CREATE TABLE test_parallel_replicas (id UInt32, value String, timestamp DateTime) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_parallel_replicas SELECT number, toString(number) AS value, now() FROM system.numbers LIMIT 100;
CREATE TABLE test_parallel_replicas_2 (id UInt32, value Nullable(String), timestamp DateTime) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_parallel_replicas_2 SELECT number, if(number % 2 = 0, toString(number), NULL) AS value, now() FROM system.numbers LIMIT 100;
SELECT count() FROM remote('127.0.0.{2|3}', currentDatabase(), test_parallel_replicas) WHERE value IS NOT NULL AND timestamp > now() - INTERVAL 1 DAY;
