CREATE TABLE default.t_proj_test1
(
    `id` UInt32,
    `name` String,
    `value` Float64,
    `created_at` DateTime DEFAULT now()
)
ENGINE = MergeTree
ORDER BY id;
INSERT INTO default.t_proj_test1 SELECT number, concat('Name', toString(number)), number * 1.5, now() FROM numbers(10000);
CREATE TABLE default.t_proj_test2
(
    `user_id` UInt32,
    `score` Nullable(Int32),
    `timestamp` Date DEFAULT today()
)
ENGINE = MergeTree
ORDER BY user_id;
INSERT INTO default.t_proj_test2 SELECT number, if(number % 2 = 0, number, NULL), toDate(now()) FROM numbers(5000);
SELECT user_id, sum(score) AS total_score FROM default.t_proj_test2 GROUP BY user_id ORDER BY total_score DESC SETTINGS max_bytes_before_external_group_by = 1;
