CREATE TABLE t (id Int32, name String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE t0 ENGINE = MergeTree() ORDER BY id AS SELECT * FROM t WHERE id >= 0 AND id < 1;
CREATE TABLE t1 ENGINE = MergeTree() ORDER BY id AS SELECT * FROM t WHERE id >= 1 AND id < 2;
INSERT INTO t0 (id, name) VALUES (0, 'test0'), (1, 'test1');
SELECT 1 FROM (SELECT EXISTS (SELECT 1 FROM t0 WHERE id = t00.id) AS b FROM t0 t00) r, t WHERE t.id > CASE WHEN b IS NULL THEN 1 ELSE 1 END;
