CREATE TABLE test_table (id Int32, name String, description String, created_at DateTime DEFAULT now());
INSERT INTO test_table (id, name, description) VALUES (1, 'example label', 'This is a test description.');
SELECT DISTINCT name FROM test_table WHERE description IS NOT NULL AND name LIKE '%example%';
ALTER TABLE test_table UPDATE description = NULL WHERE id = 1;
SELECT COUNT(*) FROM test_table WHERE created_at < now() - INTERVAL 1 DAY;
