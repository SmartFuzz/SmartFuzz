CREATE TABLE bar (id UInt64, name String, created_at DateTime DEFAULT now());
INSERT INTO bar (name) VALUES ('test1'), ('test2'), (NULL);
CREATE VIEW bar_view AS SELECT id, name FROM bar WHERE name IS NOT NULL;
SELECT * FROM bar_view;
CREATE TABLE merge_bar AS SELECT * FROM bar_view ENGINE = MergeTree() ORDER BY id;
