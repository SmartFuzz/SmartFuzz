CREATE TABLE t2 (c1 Int32, c2 String, c3 Decimal(10, 2), c4 Date);
INSERT INTO t2 (c1, c2, c3, c4) VALUES (1, 'test', 10.50, '2024-01-01');
SELECT c1, varSamp(c3) OVER (PARTITION BY c1) AS variance FROM t2;
SELECT c1, c2, varSamp(c3) OVER (PARTITION BY c2) AS variance FROM t2;
SELECT c1, varSamp(c3) OVER (PARTITION BY c1 ORDER BY c4) AS variance FROM t2;
