CREATE TABLE reference (reference_id Int64, description String) ENGINE = MergeTree() ORDER BY reference_id;
INSERT INTO reference VALUES (1, 'First'), (2, 'Second'), (3, 'Third');
CREATE TABLE data (data_id Int32, reference_id Int64, value Float64, PRIMARY KEY (data_id)) ENGINE = MergeTree() ORDER BY data_id;
CREATE TABLE temp_data AS SELECT * FROM (SELECT 1 AS data_id, 1 AS reference_id, 10.5 AS value UNION ALL SELECT 2, NULL, 20.0 UNION ALL SELECT 3, 4, 30.0);
INSERT INTO data (data_id, reference_id, value) SELECT data_id, reference_id, value FROM temp_data;
