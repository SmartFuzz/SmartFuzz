CREATE TABLE t2 ( x Int32, y String, z DateTime, PRIMARY KEY (x, y), INDEX idx_z (z));
INSERT INTO t2 (x, y, z) VALUES (1, 'test', now()), (2, 'example', now()), (3, 'sample', now());
UPDATE t2 SET y = NULL WHERE x = 2;
SELECT * FROM t2 WHERE x = 1 AND z > '2023-01-01 00:00:00' ORDER BY y LIMIT 5;
SYSTEM RELOAD DICTIONARY;
