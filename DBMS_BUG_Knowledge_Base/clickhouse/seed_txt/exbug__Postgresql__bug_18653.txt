CREATE TABLE p (a Int32, b String, c DateTime DEFAULT now());
INSERT INTO p VALUES(1, 'row1', '2023-01-01 10:00:00');
CREATE TABLE c1 AS p ENGINE = MergeTree() ORDER BY a;
INSERT INTO c1 VALUES(10, 'row2', NULL);
SELECT a, b FROM p WHERE a IN (SELECT a FROM c1) UNION ALL SELECT a, b FROM p WHERE b IS NOT NULL;
