CREATE TABLE test_table (id UInt32 NOT NULL, name String NOT NULL, age Nullable(UInt32) CHECK (age >= 0), created_at DateTime DEFAULT now(), updated_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
INSERT INTO test_table (name, age) VALUES ('Bob', NULL);
SELECT * FROM test_table WHERE age IS NULL OR age < 25;
UPDATE test_table SET age = age + 1 WHERE name = 'Alice';
