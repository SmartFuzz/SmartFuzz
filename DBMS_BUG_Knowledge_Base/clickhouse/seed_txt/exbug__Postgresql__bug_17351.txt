CREATE TABLE pt (a Tuple(a Int32, b Int32), b Int32) ENGINE = MergeTree() ORDER BY b;
CREATE TABLE t(a Tuple(a Int32, b Int32)) ENGINE = MergeTree() ORDER BY a;
ALTER TABLE pt MODIFY COLUMN a Tuple(a Int32, b Int32);
INSERT INTO pt (a, b) VALUES (tuple(1, 2), 2);
SELECT * FROM t WHERE a IS NOT NULL;
