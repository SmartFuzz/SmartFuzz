DROP TABLE IF EXISTS t2;
CREATE TABLE t2(c0 Int32, c1 String DEFAULT NULL, c2 Date);
INSERT INTO t2 VALUES (1, 'test', NULL), (2, NULL, '2023-01-01'), (3, 'example', '2023-01-02');
SELECT IF(c1 IS NULL, 'NULL', c1) AS result FROM t2 WHERE c0 = 1;
SELECT TRUE FROM t2 LEFT JOIN (SELECT NULL AS c0) AS subquery ON 1=1 WHERE (CASE c0 WHEN 1 THEN NULL ELSE c1 END) IS NOT NULL;
