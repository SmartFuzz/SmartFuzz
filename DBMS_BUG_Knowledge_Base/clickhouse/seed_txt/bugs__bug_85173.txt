CREATE TABLE delta_table (id UInt8, val String) ENGINE = Delta;
INSERT INTO delta_table VALUES (1, 'X'), (2, 'Y'), (3, 'Z');
CREATE TABLE distributed_table ON CLUSTER default (id UInt8, val String) ENGINE = Distributed(default, default, delta_table, rand());
SELECT a.val, b.val FROM distributed_table AS a JOIN delta_table AS b ON a.id = b.id;
SELECT a.val, b.val FROM delta_table AS b JOIN distributed_table AS a ON a.id = b.id;
