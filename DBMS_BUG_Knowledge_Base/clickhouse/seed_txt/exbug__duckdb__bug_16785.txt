CREATE TABLE IF NOT EXISTS users (id Int32 PRIMARY KEY, username String NOT NULL UNIQUE);
CREATE TABLE IF NOT EXISTS orders (order_id Int32 PRIMARY KEY, user_id Int32, amount Float64 NOT NULL, FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO users (id, username) VALUES (1, 'user1'), (2, 'user2');
INSERT INTO orders (order_id, user_id, amount) VALUES (101, 1, 50.0), (102, 2, 75.0);
SELECT * FROM users;
