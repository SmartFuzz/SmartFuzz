DROP TABLE IF EXISTS vs_server;
CREATE TABLE vs_server (nGroupId Int32, nName String);
INSERT INTO vs_server(nGroupId, nName) VALUES (15, 'Group A'), (21, 'Group B');
SELECT 0 AS arg INTO tmp;
SELECT arg FROM tmp;
SELECT EXISTS (SELECT 1 FROM vs_server INNER JOIN (SELECT 199 AS group_id) AS tmp_table ON vs_server.nGroupId = tmp_table.group_id) AS result;
