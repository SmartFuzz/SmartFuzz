CREATE TABLE test_table (id Int32, data Array(Int32));
INSERT INTO test_table VALUES (1, [1, 2, 3]), (2, [NULL]);
SELECT CASE WHEN data IS NOT NULL THEN arrayJoin(data) ELSE 0 END AS x FROM test_table WHERE id = 1;
SELECT CASE WHEN id = 2 THEN arrayJoin(data) ELSE 'No List' END AS x FROM test_table;
SELECT CASE WHEN id < 3 THEN arrayJoin(data) ELSE NULL END AS x FROM test_table;
