CREATE TABLE c (id Int32 NOT NULL, val String DEFAULT NULL) ENGINE = MergeTree() PRIMARY KEY (id);
CREATE TABLE d (id Int32 NOT NULL, val Decimal(10,2) DEFAULT NULL) ENGINE = MergeTree() PRIMARY KEY (id) ORDER BY (id);
INSERT INTO c (id, val) VALUES (1, '100.00'), (2, '200.50'), (3, NULL);
INSERT INTO d (id, val) VALUES (1, 100.00), (2, 200.50), (3, NULL);
UPDATE c SET val = (SELECT CAST(val AS String) FROM d WHERE c.id = d.id) WHERE id IN (SELECT id FROM d);
