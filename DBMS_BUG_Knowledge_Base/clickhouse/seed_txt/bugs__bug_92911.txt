DROP TABLE IF EXISTS employee_data;
CREATE TABLE employee_data (id UInt32, name String, salary Nullable(Float64)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO employee_data VALUES (1, 'Alice', 50000.0), (2, 'Bob', NULL), (3, 'Charlie', 60000.5);
ALTER TABLE employee_data ADD COLUMN IF NOT EXISTS bonus Nullable(Float64);
ALTER TABLE employee_data UPDATE bonus = multiIf(salary IS NULL, NULL, salary * 0.1) WHERE id = 2 SETTINGS validate_mutation_query = 1;
