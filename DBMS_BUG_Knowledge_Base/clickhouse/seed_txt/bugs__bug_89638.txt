CREATE TABLE test_partitioned (id UInt32, dt DateTime, value Float64) ENGINE = MergeTree PARTITION BY toYYYYMM(dt) ORDER BY (dt);
INSERT INTO test_partitioned SELECT number, toDate('2015-01-01') + (number % 3650) AS dt, rand() FROM numbers(10000000);
CREATE TABLE test_non_partitioned (id UInt32, dt DateTime, value Float64) ENGINE = MergeTree ORDER BY (dt);
INSERT INTO test_non_partitioned SELECT number, toDate('2015-01-01') + (number % 3650) AS dt, rand() FROM numbers(10000000);
SELECT dt FROM test_partitioned WHERE value IS NOT NULL ORDER BY dt LIMIT 20;
