CREATE TABLE bar (id UInt64, name String, value Decimal(10, 2) DEFAULT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO bar (name, value) VALUES ('test1', 10.50);
CREATE TABLE baz (ref_id UInt64, description String) ENGINE = MergeTree() ORDER BY ref_id;
INSERT INTO baz (ref_id, description) VALUES (1, 'description1');
SELECT * FROM baz WHERE ref_id IS NOT NULL;
