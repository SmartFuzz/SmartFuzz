CREATE TABLE way_tags (way Int32 NOT NULL, k String NOT NULL) ENGINE = MergeTree() ORDER BY (way, k);
INSERT INTO way_tags (way, k) VALUES (1, 'bridge');
INSERT INTO way_tags (way, k) VALUES (2, 'tunnel');
SELECT * FROM way_tags WHERE way IN (SELECT way FROM way_tags WHERE k = 'bridge');
SELECT * FROM way_tags WHERE way IN (SELECT way FROM way_tags WHERE k IS NULL);
