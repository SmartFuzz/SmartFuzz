CREATE DATABASE IF NOT EXISTS window_lag_bug;
CREATE TABLE IF NOT EXISTS data_table (uid Int32) ENGINE = MergeTree() ORDER BY uid;
INSERT INTO data_table (uid) VALUES (100), (101);
SELECT (LAST_VALUE(a.uid) OVER (ORDER BY a.uid)) = a.uid AS result FROM (SELECT LAG(uid) OVER (ORDER BY uid) AS uid_lag, uid FROM data_table) AS a;
SELECT version();
DROP DATABASE window_lag_bug;
