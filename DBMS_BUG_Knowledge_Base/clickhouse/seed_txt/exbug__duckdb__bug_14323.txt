CREATE TABLE random_numbers AS SELECT number FROM range(1, 101);
SELECT rand() % 100 / 100 AS random_value;
SELECT * FROM random_numbers ORDER BY rand() LIMIT 10;
CREATE TABLE mixed_types (id UInt32, description String, optional_value Nullable(String));
INSERT INTO mixed_types VALUES (1, 'text', NULL);
SELECT * FROM mixed_types WHERE optional_value IS NULL;
