CREATE TABLE orders (order_id UInt32, user_id UInt32, product_name String, quantity UInt32, order_date DateTime DEFAULT now());
INSERT INTO orders (user_id, product_name, quantity) VALUES (1, 'Product_A', 5), (2, 'Product_B', 10), (3, 'Product_C', 0);
CREATE TABLE payments (payment_id UInt32, order_id UInt32, amount Decimal(10, 2), payment_date DateTime DEFAULT now());
INSERT INTO payments (order_id, amount) SELECT order_id, 99.99 FROM orders WHERE quantity > 0;
SELECT o.order_id, p.amount FROM orders o JOIN payments p ON o.order_id = p.order_id WHERE p.amount IS NOT NULL;
