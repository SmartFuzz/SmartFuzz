CREATE TABLE messages (id UInt64 NOT NULL, muid String, conversationId String, sender String NOT NULL, receiverType Enum8('group' = 1, 'user' = 2) NOT NULL DEFAULT 'user', receiver String NOT NULL, updatedAt UInt32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO messages (muid, conversationId, sender, receiverType, receiver) VALUES ('msg1', 'conv1', 'user1', 'user', 'user2');
INSERT INTO messages (muid, conversationId, sender, receiverType, receiver) VALUES ('msg2', 'conv2', 'user2', 'group', 'group1');
SELECT id, updatedAt FROM messages WHERE id >= 14609942 ORDER BY id LIMIT 10;
ALTER TABLE messages UPDATE updatedAt = NULL WHERE id = 730170668;
