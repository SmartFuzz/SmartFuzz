CREATE TABLE cache_test (id Int32, name String, age Nullable(Int32));
INSERT INTO cache_test (id, name) VALUES (1, 'Alice');
ALTER TABLE cache_test ADD COLUMN score Int32 DEFAULT 0, ADD COLUMN status String DEFAULT 'active';
SELECT id, name, COALESCE(age, 0) AS age, score, status FROM cache_test;
