CREATE TABLE n3 (key UInt64, value Float64) ENGINE = MergeTree ORDER BY key SETTINGS index_granularity=1;
CREATE MATERIALIZED VIEW mv2 TO n1_n2_join AS SELECT n1.key as k, n1.value as v1, n3.value as v2 FROM n1 JOIN n3 ON n1.key = n3.key ORDER BY n1.key;
INSERT INTO n3 SELECT number, number / 2.0 FROM numbers(10);
INSERT INTO n1 SELECT number as key, toString(number) FROM numbers(10);
INSERT INTO n2 SELECT number, number * 10 FROM numbers(5);
