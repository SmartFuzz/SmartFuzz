CREATE TABLE emp (empno Int32 NOT NULL, name String, deptno Int32);
INSERT INTO emp VALUES (0, 'Alex', 0);
INSERT INTO emp VALUES (10, 'Bob', 1);
CREATE TABLE dept (deptno Int32, deptname String);
INSERT INTO dept VALUES (1, 'Sales');
SELECT e.name FROM emp e INNER JOIN dept d ON e.deptno = d.deptno WHERE (10 / NULLIF(e.empno, 0)) = 1;
