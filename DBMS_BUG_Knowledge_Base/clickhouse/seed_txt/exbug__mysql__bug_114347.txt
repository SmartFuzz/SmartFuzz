CREATE TABLE t2 (id Int32 NOT NULL, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
CREATE VIEW v2 AS SELECT id, created_at FROM t2 WHERE created_at IS NOT NULL;
INSERT INTO t2 (id) VALUES (1);
SELECT * FROM v2 WHERE created_at = toDateTime('0000-00-00 00:00:00');
CREATE TABLE t3 (event_time DateTime DEFAULT now(), description String) ENGINE = MergeTree() ORDER BY event_time;
