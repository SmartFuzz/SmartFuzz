CREATE TABLE test2 (id String NOT NULL, pos_start Int32, pos_end Int32, status UInt8, PRIMARY KEY(id));
INSERT INTO test2 VALUES ('ID3', NULL, NULL, 1);
INSERT INTO test2 VALUES ('ID4', 5, 15, 0);
SELECT id FROM test2 WHERE (pos_end - pos_start) IS NOT NULL AND (pos_end - pos_start) > 0;
SELECT id FROM test2 WHERE (pos_end - pos_start) < 10 OR status IS NULL;
