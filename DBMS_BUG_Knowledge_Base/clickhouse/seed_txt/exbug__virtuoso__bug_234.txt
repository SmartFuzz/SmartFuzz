CREATE TABLE countries (id Int32, name String, population Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO countries (id, name, population) VALUES (1, 'Germany', 83783942), (2, 'United States', 331002651);
CREATE TABLE synonyms (country_id Int32, synonym String) ENGINE = MergeTree() ORDER BY country_id;
INSERT INTO synonyms (country_id, synonym) VALUES (1, 'Deutschland'), (2, 'USA');
SELECT DISTINCT c.name, s.synonym FROM countries c JOIN synonyms s ON c.id = s.country_id WHERE c.name IN ('Germany', 'United States');
