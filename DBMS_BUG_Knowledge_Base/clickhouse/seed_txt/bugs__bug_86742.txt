DROP TABLE IF EXISTS job_sample;
CREATE TABLE job_sample (id UInt32, posted_at Date, discovered_at DateTime64(3, 'UTC'), url String, title String, company_name String, description String) ENGINE = MergeTree() ORDER BY (posted_at, discovered_at) PARTITION BY toYYYYMM(posted_at);
INSERT INTO job_sample SELECT * FROM s3('https://media.theirstack.com/jobs/jan-2025-sample.parquet', 'Parquet');
ALTER TABLE job_sample ADD COLUMN description_matches_python UInt8 MATERIALIZED match(description, 'Python') SETTINGS mutations_sync = 2, alter_sync = 2;
ALTER TABLE job_sample MODIFY COLUMN description_matches_python UInt8 MATERIALIZED match(description, '(?i)Python') SETTINGS mutations_sync = 2, alter_sync = 2;
