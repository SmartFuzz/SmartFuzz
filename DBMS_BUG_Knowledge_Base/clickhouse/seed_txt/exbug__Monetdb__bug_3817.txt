CREATE TABLE users (user_id Int32, username String, bio String);
INSERT INTO users VALUES (1, 'alice', 'Loves programming and databases.');
INSERT INTO users VALUES (2, 'bob', 'Enjoys hiking and outdoor activities.');
SELECT user_id, bio FROM users;
SELECT user_id, bio FROM users WHERE length(bio) > 10;
