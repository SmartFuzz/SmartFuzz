CREATE TABLE users(id UInt32, name String, age Nullable(UInt32));
CREATE TABLE orders(order_id UInt32, user_id UInt32, amount Nullable(Float64), PRIMARY KEY(order_id), INDEX idx_user_id(user_id) TYPE minmax);
INSERT INTO users(name, age) VALUES('Alice', 30), ('Bob', NULL), ('Charlie', 25);
INSERT INTO orders(user_id, amount) VALUES(1, 100.50), (2, 200.75), (1, NULL);
SELECT u.name FROM users u WHERE NOT EXISTS(SELECT 1 FROM orders o WHERE o.user_id = u.id AND o.amount IS NOT NULL);
