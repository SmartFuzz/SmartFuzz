CREATE TABLE lightweight_test ON CLUSTER '{cluster}' (ts DateTime, value String, key String, status Nullable(Int32)) ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/lightweight_test','{replica}') PARTITION BY toYYYYMMDD(ts) PRIMARY KEY (key) ORDER BY (key) SETTINGS enable_block_number_column = 1, enable_block_offset_column = 1;
CREATE TABLE keys ON CLUSTER '{cluster}' (key String, priority Int32) ENGINE = ReplicatedMergeTree('/clickhouse/tables/{shard}/keys','{replica}') PRIMARY KEY (key) ORDER BY (key);
INSERT INTO lightweight_test VALUES (now(), 'val', 'key', NULL);
INSERT INTO keys VALUES ('key', 1), ('key2', 2);
UPDATE lightweight_test SET value = 'UPDATED-2' WHERE key IN (SELECT key FROM keys WHERE priority > 1);
