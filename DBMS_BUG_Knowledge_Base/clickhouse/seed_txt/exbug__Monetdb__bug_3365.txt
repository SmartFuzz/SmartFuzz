CREATE TABLE departments (dept_id Int64, dept_name String) ENGINE = MergeTree() ORDER BY dept_id;
INSERT INTO departments (dept_id, dept_name) VALUES (1, 'HR');
CREATE TABLE employees (id Int64, name String, dept_id Int64) ENGINE = MergeTree() ORDER BY id;
INSERT INTO employees (id, name, dept_id) VALUES (1, 'Clark', 1);
SELECT * FROM employees;
