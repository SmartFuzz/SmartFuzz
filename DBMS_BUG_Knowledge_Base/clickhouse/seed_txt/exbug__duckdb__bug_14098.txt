CREATE TABLE nested (date DateTime, timezone String);
INSERT INTO nested (date, timezone) VALUES (toDateTime('2023-10-01 12:00:00'), '-0400');
SELECT date + toIntervalHour(toInt(substring(timezone, 2, 3))) AS adjusted_date FROM nested;
CREATE TABLE test_dates (event_date DateTime NOT NULL, event_timezone String NOT NULL DEFAULT '+0000');
INSERT INTO test_dates (event_date, event_timezone) VALUES (toDateTime('2023-10-01 15:00:00'), '-0500');
