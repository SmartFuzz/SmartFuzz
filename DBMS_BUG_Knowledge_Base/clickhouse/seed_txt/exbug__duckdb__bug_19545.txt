CREATE TABLE test_table (id Int32, text_column String, num_column Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (text_column, num_column) VALUES ('abc', 1), ('d"f', 2), ('g''h', 3);
SELECT text_column, length(text_column) AS length_blob, text_column AS converted_text, length(text_column) AS length_converted FROM test_table;
SELECT id, text_column, text_column AS converted_text, substring(converted_text, 1, 1) AS first_char, substring(converted_text, 2, 1) AS second_char FROM test_table;
SELECT a.text_column, b.text_column AS converted_text FROM (SELECT text_column FROM test_table) AS a JOIN (SELECT text_column FROM test_table) AS b ON a.text_column = b.text_column;
