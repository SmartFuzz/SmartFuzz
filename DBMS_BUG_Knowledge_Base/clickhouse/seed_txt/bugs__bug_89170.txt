CREATE TABLE test_distinct_oom_v2 ( id UInt64, value Nullable(String), created_at DateTime DEFAULT now() ) ENGINE = MergeTree ORDER BY id;
INSERT INTO test_distinct_oom_v2 SELECT number, if(number % 2 = 0, toString(number), NULL) FROM numbers(100000000);
SELECT DISTINCT value FROM test_distinct_oom_v2 SETTINGS max_bytes_in_distinct = 4_000_000_000, distinct_overflow_mode='break';
CREATE TABLE test_distinct_oom_v3 ( id Int32, value Float64, category String DEFAULT 'unknown' ) ENGINE = MergeTree ORDER BY id;
INSERT INTO test_distinct_oom_v3 SELECT number, toFloat64(number) / 3.14, toString(number % 10) FROM numbers(1000000);
