CREATE TABLE table8 (`x.y` ARRAY<STRING>, `z` INT) ENGINE = MergeTree() ORDER BY z;
INSERT INTO table8 VALUES (ARRAY('d','e','f'), 1);
SELECT `x.y`, z FROM table8 WHERE z = 1;
CREATE TABLE table9 (`name` String, `age` UInt8) ENGINE = MergeTree() ORDER BY age;
INSERT INTO table9 VALUES ('Alice', 30), ('Bob', 25);
