DROP TABLE IF EXISTS test_logs;
CREATE TABLE test_logs (id UInt32, message String, created_at DateTime DEFAULT now(), INDEX ix_text_message message TYPE text(tokenizer='sparseGrams')) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_logs (id, message) VALUES (1, 'zymosimeter'), (2, 'example'), (3, NULL);
SELECT * FROM test_logs WHERE hasAllTokens(message, sparseGrams('zymosimeter')) SETTINGS query_plan_direct_read_from_text_index=0;
SELECT COUNT(*) FROM test_logs WHERE created_at > now() - INTERVAL 1 DAY;
