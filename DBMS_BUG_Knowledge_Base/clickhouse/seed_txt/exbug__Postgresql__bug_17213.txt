CREATE TABLE projects (id UInt32, name String, created_at DateTime DEFAULT now());
INSERT INTO projects (name) VALUES ('Project A');
CREATE TABLE tasks (id UInt32, project_id UInt32, description String, due_date Date);
INSERT INTO tasks (project_id, description, due_date) VALUES (1, 'Task 1 for Project A', '2023-12-31');
SELECT p.id, p.name FROM projects p WHERE EXISTS (SELECT 1 FROM tasks t WHERE t.project_id = p.id AND t.due_date IS NOT NULL);
