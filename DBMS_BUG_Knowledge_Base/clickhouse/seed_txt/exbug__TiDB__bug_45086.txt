CREATE TABLE test_table (id UInt32 NOT NULL, name String DEFAULT NULL, created_at DateTime DEFAULT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (name, created_at) VALUES ('Test', '2023-06-30 12:00:00');
SELECT * FROM test_table WHERE created_at = toDate(now());
SELECT * FROM test_table WHERE created_at IS NULL;
