CREATE TABLE fuzz_illegal_tbl() ENGINE = MergeTree() ORDER BY tuple();
CREATE TABLE fuzz_pt(i Int32) ENGINE = MergeTree() ORDER BY i PARTITION BY intHash32(i);
CREATE TABLE fuzz_p1 ENGINE = MergeTree() ORDER BY i AS SELECT * FROM fuzz_pt WHERE i % 4 = 0;
INSERT INTO fuzz_pt VALUES (1);
INSERT INTO fuzz_pt VALUES (1);
SELECT * FROM fuzz_pt;
