CREATE TABLE test_intervals (id UInt32, interval_col IntervalYearMonth, description String);
INSERT INTO test_intervals (interval_col, description) VALUES (INTERVAL 1 MONTH * 2, 'Valid interval');
INSERT INTO test_intervals (interval_col, description) VALUES (NULL, 'NULL interval');
SELECT id, interval_col FROM test_intervals WHERE interval_col IS NOT NULL;
SELECT id, INTERVAL 2 MONTH * 3 AS new_interval FROM test_intervals;
