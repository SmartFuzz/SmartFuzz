CREATE TABLE geo_values (id Int32 NOT NULL, strdfgeo String);
CREATE TABLE label_values (id Int32 NOT NULL, value String NOT NULL);
INSERT INTO geo_values (id, strdfgeo) VALUES (1, 'POINT(1 1)');
INSERT INTO label_values (id, value) VALUES (1, 'Label 1');
SELECT DISTINCT h3.obj, IF(u.value IS NOT NULL, u.value, l.value) FROM triples t0 INNER JOIN triples h1 ON (h1.pred = 21 AND h1.obj = 42 AND h1.subj = t0.subj) INNER JOIN label_values l ON (l.id = h1.obj) INNER JOIN triples h3 ON (h3.pred = 20 AND h3.subj = t0.subj);
