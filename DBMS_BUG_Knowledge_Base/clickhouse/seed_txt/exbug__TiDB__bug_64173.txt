CREATE TABLE test.departments (dept_id Int32 NOT NULL, dept_name String NOT NULL) ENGINE = MergeTree() PRIMARY KEY (dept_id);
ALTER TABLE test.employees1 ADD COLUMN dept_id Int32 DEFAULT NULL;
INSERT INTO test.departments (dept_id, dept_name) VALUES (1, 'HR'), (2, 'Engineering');
ALTER TABLE test.employees1 UPDATE dept_id = 1 WHERE id IN (1, 2);
SELECT e.fname, e.lname, d.dept_name FROM test.employees1 e LEFT JOIN test.departments d ON e.dept_id = d.dept_id WHERE d.dept_name IS NOT NULL;
