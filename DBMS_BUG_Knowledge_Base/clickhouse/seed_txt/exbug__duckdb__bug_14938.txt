CREATE TABLE t2 (id Int32, status Enum8('a' = 1, 'b' = 2, 'c' = 3) NOT NULL) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t2 VALUES (1, 'a');
INSERT INTO t2 VALUES (2, 'b');
SELECT CASE WHEN COUNT(DISTINCT status) = 1 THEN any(status) END AS result FROM t2;
