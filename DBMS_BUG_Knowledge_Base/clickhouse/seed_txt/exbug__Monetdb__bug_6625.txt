CREATE TABLE users (id Int32, name String, age Int32);
CREATE TABLE orders (order_id Int32, user_id Int32, amount Decimal(10, 2), status String);
INSERT INTO users (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25);
INSERT INTO orders (order_id, user_id, amount, status) VALUES (1, 1, 100.00, 'completed'), (2, 2, 150.50, 'pending');
SELECT COUNT(*) FROM orders o JOIN users u ON o.user_id = u.id WHERE o.status = 'completed' OR o.amount > 100;
