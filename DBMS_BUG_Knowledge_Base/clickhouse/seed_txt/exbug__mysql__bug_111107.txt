CREATE TABLE test.data_types (id Int32, name String, created_at DateTime DEFAULT now(), amount Decimal(10, 2) NOT NULL);
INSERT INTO test.data_types (id, name, amount) VALUES (1, 'Test', 100.50);
CREATE PROCEDURE test.exampleProcedure(a Int32, b String, c Decimal(10, 2), d DateTime) AS SELECT a, b, c, d;
CALL test.exampleProcedure(1, 'Sample', NULL, now());
SELECT * FROM test.data_types WHERE amount > (SELECT AVG(amount) FROM test.data_types);
