CREATE TABLE test_namespace (id Int32, name String, value Int32, created_at DateTime DEFAULT now());
INSERT INTO test_namespace (id, name, value) VALUES (1, 'test1', 12), (2, 'test2', 23);
ALTER TABLE test_namespace UPDATE value = NULL WHERE id = 1;
SELECT * FROM test_namespace WHERE value IS NOT NULL;
SELECT name, (SELECT COUNT() FROM test_namespace WHERE value > 10) AS count_above_ten FROM test_namespace;
