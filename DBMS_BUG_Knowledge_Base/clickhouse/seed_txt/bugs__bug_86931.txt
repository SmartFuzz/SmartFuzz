CREATE DATABASE reproduction_test ENGINE = Atomic;
CREATE TABLE reproduction_test.tree_nodes (date Date, c1 LowCardinality(String), c2 UInt32) ENGINE = MergeTree PARTITION BY toYYYYMM(date) ORDER BY (date, c1, c2) SETTINGS index_granularity = 8192;
INSERT INTO reproduction_test.tree_nodes VALUES ('2023-01-01', 'node1', 1), ('2023-01-01', 'node2', 2), ('2023-01-01', 'node3', 3);
SELECT date, uniqExact(c2) AS unique_count FROM reproduction_test.tree_nodes GROUP BY date WITH ROLLUP;
SELECT date, uniqExact(c2) AS unique_count FROM reproduction_test.tree_nodes WHERE c2 IS NOT NULL GROUP BY date WITH ROLLUP;
