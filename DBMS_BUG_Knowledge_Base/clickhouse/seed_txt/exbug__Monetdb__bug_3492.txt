CREATE TABLE g (geom String, box String);
INSERT INTO g (geom, box) VALUES ('POINT(0 0)', NULL);
SELECT * FROM g;
ALTER TABLE g UPDATE box = geom WHERE geom IS NOT NULL;
INSERT INTO g (geom, box) VALUES (NULL, NULL);
