CREATE TABLE t2(id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE t3(id String, description String, created_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t2(id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, (NULL));
INSERT INTO t3(id, description) VALUES ('1', 'First entry'), ('2', 'Second entry');
EXPLAIN SELECT * FROM t2 WHERE age IN (SELECT CAST(id AS Int32) FROM t3);
