CREATE TABLE async_pt (a Int32, b Int32, c String DEFAULT 'default') ENGINE = MergeTree() ORDER BY a PARTITION BY a;
CREATE TABLE base_tbl2 (a Int32, b String) ENGINE = MergeTree() ORDER BY a;
CREATE TABLE async_p2 AS SELECT * FROM base_tbl2 WHERE 0;
-- Placeholder for foreign table partition;
INSERT INTO async_pt SELECT i, toString(i) FROM range(3000, 3101);
DELETE FROM async_pt WHERE a IS NOT NULL;
