CREATE TABLE test_improve_prewhere__fuzz_20 (`id` UInt32, `name` LowCardinality(String), `created_at` Nullable(DateTime), `status` LowCardinality(Nullable(String)) STATISTICS(CountMin)) ENGINE = MergeTree ORDER BY id;
INSERT INTO test_improve_prewhere__fuzz_20 SELECT number, ['active', 'inactive', 'pending'][(number % 3) + 1], toDateTime('2025-08-01 12:00:00') + number, CASE WHEN number % 2 = 0 THEN 'active' ELSE NULL END FROM numbers(10);
CREATE TABLE test_improve_prewhere__fuzz_21 (`user_id` UInt32, `email` String, `signup_date` LowCardinality(Nullable(Date)) STATISTICS(CountMin), `last_login` Nullable(DateTime)) ENGINE = MergeTree ORDER BY user_id;
INSERT INTO test_improve_prewhere__fuzz_21 SELECT number, concat('user', number, '@example.com'), toDate('2025-08-01') + number, toDateTime('2025-09-01 08:00:00') + number FROM numbers(10);
SELECT * FROM test_improve_prewhere__fuzz_21 WHERE (signup_date IS NOT NULL) AND (last_login < toDateTime('2025-09-05 00:00:00')) AND (email LIKE '%@example.com');
