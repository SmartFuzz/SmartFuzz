CREATE TABLE test (v Int32, a Int32 DEFAULT 0, b Float32, c String);
INSERT INTO test (v, b, c) VALUES (23, 1.5, 'foo'), (34, NULL, 'bar'), (12, 2.5, 'baz'), (54, 3.5, 'qux');
UPDATE test SET a = intDiv(toUInt32(rand() % 32), 1) WHERE b IS NOT NULL;
CREATE TABLE test2 (id Int32, value Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test2 (id, value) SELECT v, a FROM test WHERE a IS NOT NULL;
