CREATE TABLE tbl (id UInt32, user_id Int32, created_at DateTime DEFAULT now(), PRIMARY KEY (id));
CREATE TABLE fp_tbl (id UInt32, user_id Int32, status Enum8('active' = 1, 'inactive' = 2) DEFAULT 'active', PRIMARY KEY (id));
CREATE TABLE fp_pol (id UInt32, lead_id UInt32, amount Decimal(10,2) DEFAULT NULL, PRIMARY KEY (id));
INSERT INTO tbl (id, user_id) VALUES (1, 583532949), (2, 1342458479);
INSERT INTO fp_pol (lead_id, amount) VALUES (1, 100.00), (2, 200.00);
SELECT a.id, COUNT(*) AS n FROM tbl AS a LEFT JOIN fp_tbl AS f ON f.id = a.id LEFT JOIN fp_pol AS p ON p.lead_id = f.id WHERE a.id IN (1, 2) AND p.id IS NULL GROUP BY a.id;
