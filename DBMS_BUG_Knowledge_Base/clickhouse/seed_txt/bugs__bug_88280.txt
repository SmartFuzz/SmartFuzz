CREATE TABLE t1 (id UInt32, name String, age Nullable(UInt8), created_at DateTime) ENGINE = IcebergLocal(local, path = '/var/lib/clickhouse/user_files/lakehouses/t1/test/t1', format = 'Parquet') SETTINGS iceberg_use_version_hint = 1;
INSERT INTO t1 VALUES (1, 'Alice', 30, now()), (2, 'Bob', NULL, now());
CREATE TABLE t2 (id UInt32, description String, amount Float64) ENGINE = IcebergLocal(local, path = '/var/lib/clickhouse/user_files/lakehouses/t2/test/t2', format = 'Parquet') SETTINGS iceberg_use_version_hint = 1;
INSERT INTO t2 SELECT id, concat(name, ' - ', toString(age)) AS description, age * 1.5 FROM t1 WHERE age IS NOT NULL;
ALTER TABLE t1 DROP COLUMN age;
