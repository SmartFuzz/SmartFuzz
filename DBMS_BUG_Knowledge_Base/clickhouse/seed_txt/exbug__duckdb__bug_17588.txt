CREATE TABLE test_table (id Int32, status UInt8, description String);
INSERT INTO test_table VALUES (1, 1, 'active'), (2, 0, 'inactive'), (3, 1, 'running');
SELECT IF(status = 1, 'Status is active', toString(0)) FROM test_table;
SELECT IF(status = 1, 'Active', toString(NULL)) AS result FROM test_table;
SELECT IF(id > 1, 'Greater than one', 'One or less') FROM test_table;
