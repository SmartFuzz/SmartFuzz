CREATE TABLE sbtest2 (id Int32 NOT NULL, name String NOT NULL, age Int32 DEFAULT NULL, created_at DateTime DEFAULT now(), PRIMARY KEY (id)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO sbtest2 (id, name, age) VALUES (1, 'Alice', 30);
INSERT INTO sbtest2 (id, name, age) VALUES (2, 'Bob', NULL);
ALTER TABLE sbtest2 ADD INDEX idx_age (age) TYPE minmax, ADD INDEX idx_name_age (name, age) TYPE minmax;
SELECT * FROM sbtest2 WHERE age IS NULL OR name = 'Alice';
