CREATE TABLE t4 (id UInt32 NOT NULL, name String NOT NULL, value Decimal(10,2) NULL, created_at DateTime DEFAULT now(), PRIMARY KEY (id));
INSERT INTO t4 (id, name, value) VALUES (1, 'test1', 123.45);
INSERT INTO t4 (id, name, value) VALUES (2, 'test2', NULL);
SELECT id, name, row_number() OVER (ORDER BY created_at DESC) AS row_num FROM t4 WHERE value IS NOT NULL;
SELECT id, name, row_number() OVER (PARTITION BY value ORDER BY created_at ASC) AS row_num FROM t4;
