CREATE TABLE test_table (id UInt32, data Array(String), INDEX idx_data data TYPE text(tokenizer=array)) ENGINE=MergeTree() ORDER BY id;
INSERT INTO test_table VALUES (1, []);
INSERT INTO test_table VALUES (2, ['abc', 'def']);
SELECT * FROM test_table WHERE hasAllTokens(data, 'abc') FORMAT Null;
SELECT * FROM test_table WHERE id = 1 OR data = [] FORMAT Null;
