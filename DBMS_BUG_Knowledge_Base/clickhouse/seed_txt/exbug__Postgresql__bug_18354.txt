CREATE TABLE t (a String, b Int32, c DateTime DEFAULT now());
INSERT INTO t (a, b) VALUES ('test', 1);
INSERT INTO t (a, b) SELECT concat('test_', toString(number)), number FROM numbers(20);
SELECT * FROM t WHERE b IS NULL OR c > now() - INTERVAL 1 DAY;
