CREATE TABLE authors (id Int32 PRIMARY KEY, name String NOT NULL, email String NOT NULL) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE books (id Int32 PRIMARY KEY, title String NOT NULL, publication_date Date NOT NULL, author_id Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO authors (id, name, email) VALUES (1, 'John Doe', 'john@example.com');
INSERT INTO books (id, title, publication_date, author_id) VALUES (1, 'Sample Book', '2023-01-01', 1);
SELECT * FROM books WHERE author_id IS NULL;
