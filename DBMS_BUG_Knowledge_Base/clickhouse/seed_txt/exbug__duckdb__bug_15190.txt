CREATE TABLE mixed_types (id Int32, name String, created_at DateTime, is_active UInt8);
INSERT INTO mixed_types (id, name, created_at, is_active) VALUES (1, 'Test User', '2023-10-01 10:00:00', 1);
CREATE TABLE integers (i Int32, j Int32) ENGINE = MergeTree() ORDER BY i;
SELECT * FROM mixed_types WHERE is_active IS NOT NULL;
SELECT COUNT(*) FROM integers;
