CREATE TABLE test (foo Int32, bar Int32, baz String);
ALTER TABLE test ADD COLUMN baz String;
INSERT INTO test (foo, bar, baz) VALUES (1, 2, 'example');
ALTER TABLE test DROP COLUMN bar;
CREATE TABLE orders (order_id Int32, order_date Date, customer_id Int32) ENGINE = MergeTree() ORDER BY order_id;
