CREATE TABLE rt1 (id UInt32, minX Float64, minY Float64) ENGINE = MergeTree() ORDER BY id;
CREATE VIEW v1 AS SELECT 2 AS id;
INSERT INTO rt1 (minX, minY) VALUES (1, 1);
SELECT * FROM v1 CROSS JOIN rt1 WHERE rt1.minX IN (2) AND rt1.minY > 0;
INSERT INTO rt1 (minX, minY) VALUES (NULL, 2);
