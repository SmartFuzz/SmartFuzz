CREATE TABLE t2 (x Int32, y String, z Float32, PRIMARY KEY (x));
INSERT INTO t2 VALUES (1, 'test', 1.5), (2, 'example', 2.5), (3, 'sample', 3.5);
SELECT * FROM t2 WHERE x = toUInt32(5.0) / 2;
CREATE INDEX idx_y ON t2(y) TYPE minmax;
SELECT * FROM t2 WHERE y = 'example' AND z > 2.0;
