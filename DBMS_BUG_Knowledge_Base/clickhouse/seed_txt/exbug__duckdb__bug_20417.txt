CREATE TABLE users(id Int32, username String NOT NULL, email String UNIQUE);
INSERT INTO users VALUES (1, 'user1', 'user1@example.com'), (2, 'user2', 'user2@example.com');
CREATE TABLE orders(order_id Int32, user_id Int32, amount Decimal(10, 2));
INSERT INTO orders VALUES (101, 1, 99.99), (102, 2, 149.49);
SELECT u.username, COUNT(o.order_id) FROM users u LEFT JOIN orders o ON u.id = o.user_id GROUP BY u.username HAVING COUNT(o.order_id) > 0;
