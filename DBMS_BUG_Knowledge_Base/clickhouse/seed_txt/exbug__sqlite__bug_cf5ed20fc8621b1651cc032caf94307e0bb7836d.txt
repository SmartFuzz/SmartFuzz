CREATE TABLE user ( id UInt32 NOT NULL, active UInt8 NOT NULL, account UInt32, PRIMARY KEY(id) );
CREATE TABLE account ( id UInt32 NOT NULL, name String, balance Float64 DEFAULT 0.0, PRIMARY KEY(id) );
INSERT INTO user VALUES (1, 1, 1);
INSERT INTO account VALUES (1, 'test', 100.0);
SELECT user.id FROM user INNER JOIN account ON user.account = account.id WHERE user.active = 1;
