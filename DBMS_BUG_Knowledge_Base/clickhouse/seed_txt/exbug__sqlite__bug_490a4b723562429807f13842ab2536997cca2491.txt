CREATE TABLE abc(a Int32, b String, c Float64) ENGINE = MergeTree() ORDER BY a;
INSERT INTO abc VALUES(1, 'test', 3.14);
CREATE TABLE def(d Int32, e String DEFAULT 'default', f String) ENGINE = MergeTree() ORDER BY d;
INSERT INTO def VALUES(NULL, 'example', '\x01\x02\x03');
SELECT * FROM abc WHERE 1 UNION ALL SELECT * FROM def WHERE e IS NOT NULL;
