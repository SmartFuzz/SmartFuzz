CREATE TABLE t2 ( id Int32, name String, created_at DateTime DEFAULT now(), score Float32 CHECK (score >= 0));
INSERT INTO t2(id, name, score) VALUES (1, 'Alice', 95.5), (2, 'Bob', NULL), (3, 'Charlie', -10);
ALTER TABLE t2 ADD CONSTRAINT idx_name UNIQUE(name);
SELECT * FROM t2 WHERE score IS NULL OR score > 90;
