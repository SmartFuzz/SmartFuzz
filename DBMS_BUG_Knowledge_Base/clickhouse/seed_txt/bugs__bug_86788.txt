CREATE DATABASE iceberg_dev ENGINE = DataLakeCatalog('https://my-rest-catalog') SETTINGS catalog_type = 'rest', storage_endpoint = 'http://my_s3_endpoint:port', warehouse = 's3://iceberg-warehouse/', vended_credentials = 0, aws_access_key_id = 's3_key', aws_secret_access_key = 's3_secret';
CREATE TABLE iceberg_dev.test_foo (id UInt32, name String, created_at DateTime DEFAULT now(), value Float64) ENGINE = MergeTree() ORDER BY id;
INSERT INTO iceberg_dev.test_foo (id, name, value) VALUES (1, 'test1', 10.5), (2, 'test2', NULL), (3, 'test3', 20.0);
SELECT id, name, value, created_at FROM iceberg_dev.test_foo WHERE value IS NOT NULL;
SELECT COUNT(*) FROM iceberg_dev.test_foo WHERE created_at > now() - INTERVAL 1 DAY;
