CREATE TABLE t2(c1 Int32, c2 String) ENGINE = MergeTree() PARTITION BY intDiv(c1, 50) ORDER BY c1;
INSERT INTO t2 VALUES (10, 'A'), (20, 'B'), (30, 'C'), (60, 'D');
CREATE TABLE t3(c1 Int32, c2 Int32 NOT NULL, c3 DateTime DEFAULT now()) ENGINE = MergeTree() PARTITION BY (c1 % 4) ORDER BY c1;
INSERT INTO t3 SELECT c1, COUNT(*) FROM t2 GROUP BY c1;
SELECT * FROM t3;
