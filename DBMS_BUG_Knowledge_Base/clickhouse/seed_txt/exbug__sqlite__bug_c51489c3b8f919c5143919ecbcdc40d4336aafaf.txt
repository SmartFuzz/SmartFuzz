CREATE TABLE t (label String, step Int32, value Float64 DEFAULT 0.0);
INSERT INTO t VALUES('a', 1, 1.1);
INSERT INTO t VALUES('a', 1, 1.1);
INSERT INTO t VALUES('b', 1, 2.2);
WITH cte AS (SELECT DISTINCT label, step FROM t) SELECT label, step FROM cte UNION ALL SELECT label, step + 1 FROM cte WHERE step < 3;
