CREATE TABLE ft1 (x String, y Int32) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO ft1 (x, y) VALUES ('test', 1);
CREATE TABLE ft2 (a String, b Float64) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO ft2 (a, b) VALUES (NULL, 3.14);
SELECT * FROM ft1 WHERE isNull(x);
