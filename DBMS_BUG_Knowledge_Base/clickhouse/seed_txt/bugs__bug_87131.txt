CREATE TABLE test2 (id UInt64, value Nullable(Int32), arr Array(String)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test2 SELECT number, if(number % 2 = 0, number, NULL), ['a', 'b', 'c'] FROM numbers(10);
CREATE TABLE test3 AS test2 ENGINE = MergeTree() ORDER BY id;
SELECT arr, arr.size0 FROM test2 WHERE value IS NOT NULL ORDER BY value LIMIT 3;
SELECT id, value, arr[1] FROM test3 WHERE arr.size0 > 0;
