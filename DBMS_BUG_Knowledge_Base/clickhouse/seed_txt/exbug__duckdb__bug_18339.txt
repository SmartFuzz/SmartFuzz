CREATE TABLE C (x Int32, y String, z Float32);
INSERT INTO C VALUES (1, 'a', 1.1), (NULL, 'b', NULL), (3, 'c', 3.3), (NULL, NULL, 4.4);
CREATE TABLE D (x Int32, y String);
INSERT INTO D VALUES (3, 'c'), (NULL, 'd'), (1, NULL);
SELECT * FROM C WHERE (x, y) NOT IN (SELECT x, y FROM D) OR (x IS NULL AND y IS NULL);
