CREATE TABLE test (id Int32, name String, age Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test (id, name, age) VALUES (1, 'Alice', 30);
INSERT INTO test (id, name, age) VALUES (2, 'Bob', NULL);
ALTER TABLE test DROP COLUMN name;
SELECT * FROM test;
