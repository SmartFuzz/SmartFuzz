CREATE TABLE a (x String NOT NULL, y String);
INSERT INTO a VALUES ('aaa', 'AAA');
CREATE TABLE b (x String, y String);
INSERT INTO b VALUES ('aaa', 'AAA'), ('aAa', 'aAa'), ('aA', 'aA');
SELECT a.x, b.x FROM a JOIN b ON lower(a.y) LIKE lower(b.y) WHERE a.x = b.x;
