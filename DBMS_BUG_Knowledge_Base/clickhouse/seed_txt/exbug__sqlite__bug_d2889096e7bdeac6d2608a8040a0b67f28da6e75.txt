CREATE TABLE t4 (Pid UInt32, xmin UInt32, xmax UInt32) ENGINE = MergeTree() ORDER BY Pid;
INSERT INTO t4 VALUES(1, 0, 0);
INSERT INTO t4 VALUES(2, 0, 0);
SELECT * FROM t4 WHERE Pid IN (1, 2);
WITH r AS (SELECT 1 AS x UNION ALL SELECT 2 UNION ALL SELECT NULL) SELECT * FROM r CROSS JOIN t4 WHERE Pid = x;
