CREATE TABLE t2 (id Int32 NOT NULL, value String NOT NULL, created_at DateTime DEFAULT now(), is_active UInt8 DEFAULT 1, PRIMARY KEY (id)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t2 (value, is_active) VALUES ('test1', 1), ('test2', NULL), ('test3', 0);
SELECT * FROM (SELECT id, value FROM t2 WHERE is_active IS NOT NULL) AS active_values;
ALTER TABLE t2 UPDATE value = concat(value, '_updated') WHERE is_active = 1;
SELECT COUNT(*) FROM (SELECT DISTINCT value FROM t2) AS distinct_values;
