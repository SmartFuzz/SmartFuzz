CREATE ROLE user_c;
CREATE DATABASE c;
CREATE TABLE c.test(data Int32, description String, created_at DateTime DEFAULT now());
INSERT INTO c.test(data, description) VALUES (1, 'First entry'), (NULL, 'Second entry'), (2, 'Third entry');
CREATE INDEX description_index ON c.test (description);
