CREATE TABLE comment (id UInt64, path String NOT NULL, created_at DateTime DEFAULT now());
INSERT INTO comment (path) VALUES ('0.1.2.3'), ('0.1.2.4'), (NULL);
INSERT INTO comment (path) SELECT '0.' || toString(number) FROM numbers(500);
SELECT * FROM comment;
ALTER TABLE comment ADD COLUMN path_ltree String;
