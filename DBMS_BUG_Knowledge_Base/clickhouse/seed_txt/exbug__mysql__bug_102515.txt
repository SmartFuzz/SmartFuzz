CREATE TABLE test_replication (id UInt32, data String, created_at DateTime DEFAULT now());
INSERT INTO test_replication (id, data) VALUES (1, 'Initial data');
ALTER TABLE test_replication UPDATE data = NULL WHERE id = 1;
SELECT * FROM test_replication WHERE data IS NULL;
ALTER TABLE test_replication DELETE WHERE id = 1;
