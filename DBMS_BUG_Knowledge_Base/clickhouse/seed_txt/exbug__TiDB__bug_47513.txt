CREATE TABLE t2 ( id Int32, name String, created_at DateTime DEFAULT now() );
INSERT INTO t2 (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
CREATE TABLE t3 ( x Int32, y Int32, z Int32 DEFAULT 0, UNIQUE INDEX idx_y(y) ) ENGINE = MergeTree() ORDER BY (y);
INSERT INTO t3 (x, y) VALUES (5, 10), (15, 20), (25, 30);
SELECT * FROM t3 WHERE y IN (SELECT y FROM t3 WHERE x < 20) ORDER BY z DESC;
