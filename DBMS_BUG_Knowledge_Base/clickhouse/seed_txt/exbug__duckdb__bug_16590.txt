CREATE TABLE test_json (id UInt32, data String, is_active UInt8, created_at DateTime DEFAULT now());
INSERT INTO test_json (id, data, is_active) VALUES (1, '{"key": "value"}', 1);
SELECT JSONExtractString(data, 'key') FROM test_json WHERE is_active = 1;
SELECT JSONExtractString(data, 'key') FROM test_json WHERE id = 1;
SELECT JSONExtractString(data, 'key') FROM test_json WHERE created_at < now();
