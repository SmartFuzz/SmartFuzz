CREATE TABLE test_table (id Int32, name String, created_at DateTime DEFAULT now());
INSERT INTO test_table VALUES (1, 'Alice', NULL);
CREATE VIEW test_view AS SELECT rowNumberInAllBlocks() AS view_rowid, id, name FROM test_table WHERE id IS NOT NULL;
SELECT * FROM test_view;
DESCRIBE TABLE test_view;
