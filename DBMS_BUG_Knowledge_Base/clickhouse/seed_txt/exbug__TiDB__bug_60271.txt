CREATE TABLE users(id Int32, name String, age Int32, created_at DateTime DEFAULT now());
INSERT INTO users VALUES(1, 'Alice', 30), (2, 'Bob', 25), (3, NULL, 40);
CREATE TABLE orders(order_id Int32, user_id Int32, amount Decimal(10, 2), PRIMARY KEY(order_id));
INSERT INTO orders VALUES(101, 1, 99.99), (102, 2, 49.99), (103, 3, 19.99);
SELECT u.name, o.amount FROM users u JOIN orders o ON u.id = o.user_id WHERE u.age IS NOT NULL AND o.amount > 20.00;
