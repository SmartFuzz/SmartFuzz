CREATE TABLE IF NOT EXISTS kafka_topic_queue (operationType String, fullDocument String) ENGINE = Kafka() SETTINGS kafka_broker_list = 'k01.local:9093', kafka_topic_list = 'xxx', kafka_group_name = 'clickhouse', kafka_format = 'BSONEachRow';
INSERT INTO kafka_topic_queue (operationType, fullDocument) VALUES ('insert', '{"nestedField": {"subField": "value"}}');
CREATE TABLE IF NOT EXISTS kafka_topic_queue_test (id UInt32, data String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO kafka_topic_queue_test (id, data) SELECT number, fullDocument FROM kafka_topic_queue WHERE operationType = 'insert';
SELECT id, data FROM kafka_topic_queue_test WHERE data LIKE '%nestedField%';
