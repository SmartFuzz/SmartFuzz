CREATE TABLE t2(a String NOT NULL, b Int32 DEFAULT 0);
INSERT INTO t2 VALUES('123-456', 1);
CREATE TABLE t3(a String, b Float64);
INSERT INTO t3 VALUES('789.01', NULL);
WITH test AS (SELECT CASE WHEN match(a, '^[0-9]+\.*[0-9]+$') THEN CASE WHEN toUInt32(a) < 800 THEN 'valid' END END flg FROM t2) SELECT MAX(flg) FROM test;
