CREATE DATABASE IF NOT EXISTS testdb;
USE testdb;
CREATE TABLE IF NOT EXISTS products (product_id UInt32 NOT NULL, product_name String NOT NULL, price Decimal(10, 2) NOT NULL, PRIMARY KEY (product_id));
CREATE TABLE IF NOT EXISTS orders (order_id UInt32 NOT NULL, product_id UInt32 NOT NULL, quantity UInt32 NOT NULL, PRIMARY KEY (order_id), FOREIGN KEY (product_id) REFERENCES products(product_id));
INSERT INTO products (product_name, price) VALUES ('Widget', 19.99), ('Gadget', 29.99);
SELECT * FROM orders;
