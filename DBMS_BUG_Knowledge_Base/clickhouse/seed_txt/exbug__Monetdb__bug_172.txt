CREATE TABLE test_table (id Int32, flag UInt8, value Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_table (id, flag, value) VALUES (1, 1, 10);
INSERT INTO test_table (id, flag, value) VALUES (2, 0, 20);
SELECT isNull(flag) FROM test_table WHERE id = 1;
SELECT (isNull(flag) = 0) FROM test_table WHERE value > 5;
