CREATE TABLE tt3 (id1 Int32 NOT NULL, id2 Int32 NOT NULL, value1 Int32 CHECK (value1 >= 0), value2 Int32 DEFAULT 0, name String, PRIMARY KEY (id1, id2));
INSERT INTO tt3 VALUES (1, 1, 10, 100, 'A'), (1, 2, 20, 90, 'B'), (2, 1, 30, 80, 'C'), (2, 2, 40, 70, 'D'), (3, 1, 50, 60, 'E');
SELECT id1, id2, SUM(value1) AS total_value, MAX(value2) AS max_value, arrayJoin(splitByString(',', groupArray(name ORDER BY value2))) AS first_name, row_number() OVER (ORDER BY SUM(value1) DESC, MAX(value2) ASC) AS rank FROM tt3 GROUP BY id1, id2 ORDER BY total_value DESC, max_value ASC;
CREATE TABLE tt4 (id UInt32 DEFAULT 0, description String, created_at DateTime DEFAULT now());
INSERT INTO tt4 (description) VALUES (NULL), ('Test entry 1'), ('Test entry 2');
