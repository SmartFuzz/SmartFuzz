CREATE TABLE users(id UInt32, name String, age UInt32) ENGINE = MergeTree() ORDER BY id;
CREATE TABLE orders(order_id UInt32, user_id UInt32, amount Float64) ENGINE = MergeTree() ORDER BY order_id;
INSERT INTO users(name, age) VALUES('Alice', 30);
INSERT INTO orders(user_id, amount) VALUES(1, 100.50);
RENAME TABLE orders TO purchases;
