CREATE TABLE t2(id Int32, value String, amount Decimal(10, 2));
CREATE TABLE t3(id Int32, ref_id Int32, score Float32 DEFAULT 0.0);
INSERT INTO t2(id, value, amount) VALUES (1, 'test', 100.50), (2, 'example', 200.75);
INSERT INTO t3(id, ref_id, score) VALUES (1, 1, 10.5), (2, 2, NULL);
SELECT t2.value FROM t2 JOIN t3 ON t2.id = t3.ref_id WHERE t3.score IS NOT NULL ORDER BY (CASE WHEN t3.score IS NULL THEN t2.amount ELSE t3.score END);
