CREATE TABLE t2 (id Int32, value String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO t2 VALUES (1, 'test'), (2, NULL), (3, 'example');
CREATE TABLE t3 (c0 Float32, c1 Int32 DEFAULT 0) ENGINE = MergeTree() ORDER BY tuple();
INSERT INTO t3 VALUES (NULL, 1), (1.5, 2), (2.5, NULL);
SELECT t2.value FROM t2 LEFT JOIN t3 ON 1 GROUP BY t2.value;
