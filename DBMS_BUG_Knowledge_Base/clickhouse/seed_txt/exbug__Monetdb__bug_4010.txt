CREATE TABLE test (a Int32, b String, c Date DEFAULT NULL);
ALTER TABLE test ADD COLUMN d Float64;
INSERT INTO test (a, b) VALUES (1, 'test1'), (2, 'test2');
ALTER TABLE test UPDATE c = today() WHERE a = 1;
SELECT a, b, COALESCE(c, 'No Date') AS date_info FROM test;
