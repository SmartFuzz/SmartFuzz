CREATE TABLE OpenMedic (id Int32, annee String) ENGINE = MergeTree() ORDER BY id;
INSERT INTO OpenMedic (id, annee) VALUES (1, '2014'), (2, '2015'), (3, '2016'), (4, '2017'), (5, '2018');
CREATE TABLE OpenMedicResults (id Int32, s Int32, t Int32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO OpenMedicResults (id, s, t) SELECT id, IF(match(annee, '2015'), 1, 0), IF(match(annee, '2015'), 1, 0) FROM OpenMedic;
SELECT DISTINCT annee, s, t FROM OpenMedicResults WHERE s = 1;
