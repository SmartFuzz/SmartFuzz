CREATE TABLE t4 (id Int32, c1 String, c2 String MATERIALIZED concat(c1, ' - ', c1), c3 Int32 DEFAULT NULL) ENGINE = MergeTree() PRIMARY KEY id;
INSERT INTO t4(id, c1) VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
SELECT id FROM t4 WHERE c2 IS NOT NULL AND c3 BETWEEN 1 AND 3 GROUP BY id HAVING id < 5;
CREATE TABLE t5 (id Int32, c1 String NOT NULL, c2 Int32 DEFAULT 0, c3 String MATERIALIZED IFNULL(c1, '') || toString(c2)) ENGINE = MergeTree() PRIMARY KEY id;
INSERT INTO t5(id, c1, c2) VALUES (1, 'data', 1), (2, NULL, 2);
