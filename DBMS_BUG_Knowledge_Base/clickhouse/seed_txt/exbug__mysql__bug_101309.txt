CREATE TABLE test_partitioned_table (t_date String, o_no String, c_code String, id UInt32) ENGINE = MergeTree() ORDER BY (id, t_date) PARTITION BY toYYYYMMDD(toDate(t_date));
INSERT INTO test_partitioned_table (t_date, o_no, c_code, id) VALUES ('20181219', 'order1', 'code1', 1);
INSERT INTO test_partitioned_table (t_date, o_no, c_code, id) VALUES ('20181220', 'order2', 'code2', 2);
SELECT COUNT(*) AS total_rows, MIN(id) AS min_id, MAX(id) AS max_id FROM test_partitioned_table;
ALTER TABLE test_partitioned_table ADD COLUMN new_col String DEFAULT NULL;
