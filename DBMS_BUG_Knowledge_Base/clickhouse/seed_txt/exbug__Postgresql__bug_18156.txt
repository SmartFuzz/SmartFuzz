CREATE TABLE y (id UInt32, ref_id UInt32, PRIMARY KEY (id)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO y (ref_id) VALUES (NULL), (1), (2);
DELETE FROM y WHERE id = 1;
SELECT * FROM y;
CREATE TABLE z (a UInt32 NOT NULL, b UInt32, c UInt32) ENGINE = MergeTree() ORDER BY a PARTITION BY a;
