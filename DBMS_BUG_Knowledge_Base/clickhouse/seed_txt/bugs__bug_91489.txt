CREATE TABLE ref_shapes (`id` UInt64, `shape` String, `color_id` LowCardinality(Nullable(UInt64)) DEFAULT NULL) ENGINE = MergeTree ORDER BY id;
INSERT INTO ref_shapes (id, shape, color_id) VALUES (1, 'circle', NULL), (2, 'square', 1), (3, 'triangle', 2);
CREATE TABLE t__fuzz_2 (`shape_id` LowCardinality(Nullable(UInt64)), `description` LowCardinality(String)) ENGINE = MergeTree ORDER BY shape_id SETTINGS allow_nullable_key=1;
INSERT INTO t__fuzz_2 (shape_id, description) VALUES (1, 'A round shape'), (2, 'A four-sided shape'), (NULL, 'No shape');
SELECT DISTINCT shape_id, dictGetUInt64('colors', 'n', shape_id) FROM t__fuzz_2 WHERE shape_id IS NOT NULL ORDER BY shape_id;
