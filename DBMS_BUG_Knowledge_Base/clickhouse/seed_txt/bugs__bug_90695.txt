CREATE TABLE t1 (a Int32, b Nullable(String), c Float64) ENGINE = MergeTree() ORDER BY a;
INSERT INTO t1 VALUES (1, 'test', 1.5), (2, NULL, 2.5), (3, 'example', NULL);
CREATE TABLE t2 AS t1 ENGINE = MergeTree() ORDER BY a;
SELECT a, median(c) FROM t1 WHERE b IS NOT NULL GROUP BY a;
SELECT a, count(*) FROM t2 WHERE c <=> NULL GROUP BY a HAVING count(*) > 1;
