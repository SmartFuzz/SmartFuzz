CREATE TABLE fuzztest (id UInt64 NOT NULL, username String NOT NULL, email Nullable(String), score Nullable(Float32) DEFAULT 0.0, created_at DateTime DEFAULT now(), updated_at DateTime DEFAULT now()) ENGINE = MergeTree() ORDER BY id;
INSERT INTO fuzztest (username, email) VALUES ('user1', 'user1@example.com');
ALTER TABLE fuzztest UPDATE email = 'user1_updated@example.com' WHERE id = 1;
ALTER TABLE fuzztest UPDATE score = NULL WHERE id = 1;
SELECT * FROM fuzztest WHERE score IS NULL;
