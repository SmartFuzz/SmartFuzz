CREATE TABLE v1 ( id Int32, name String, age Int32 ) ENGINE = MergeTree() ORDER BY id;
INSERT INTO v1 (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', NULL);
CREATE TABLE v2 ( group_id Int32, description String ) ENGINE = MergeTree() ORDER BY group_id;
INSERT INTO v2 (group_id, description) VALUES (1, 'Group A'), (2, 'Group B'), (3, 'Group C');
SELECT id, name, (SELECT description FROM v2 WHERE group_id = v1.id ORDER BY group_id LIMIT 1) AS last_description FROM v1;
