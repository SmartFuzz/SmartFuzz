CREATE TABLE test1 (id UInt64, name String, age Int32, created_at DateTime DEFAULT now());
CREATE TABLE test2 (x Int32, y String, z UInt8 DEFAULT 1);
INSERT INTO test2 (y) SELECT name FROM test1 WHERE age IS NOT NULL;
INSERT INTO test2 (x, y) SELECT age, name FROM test1 WHERE age < 30;
INSERT INTO test2 (y) SELECT if(name IS NULL, 'Unknown', name) FROM test1;
