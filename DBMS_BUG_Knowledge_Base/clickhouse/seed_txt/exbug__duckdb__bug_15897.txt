CREATE TABLE courses (course_id Int32, course_name String, credits Int32) ENGINE = MergeTree() PRIMARY KEY course_id;
ALTER TABLE courses ADD CONSTRAINT credits_check CHECK (credits > 0);
CREATE TABLE enrollments (enrollment_id Int32, student_id Int32, course_id Int32, grade Int32) ENGINE = MergeTree() PRIMARY KEY enrollment_id;
ALTER TABLE enrollments ADD CONSTRAINT student_fk FOREIGN KEY (student_id) REFERENCES students(id);
ALTER TABLE enrollments ADD CONSTRAINT course_fk FOREIGN KEY (course_id) REFERENCES courses(course_id);
INSERT INTO courses VALUES (1, 'Mathematics', 3);
