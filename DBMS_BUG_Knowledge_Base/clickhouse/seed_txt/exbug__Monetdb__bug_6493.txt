CREATE TABLE test_table (pk Int32, col_a Int32, col_b Float32, col_c String, col_d Int32, col_e Float32, col_f String) ENGINE = MergeTree() ORDER BY pk;
INSERT INTO test_table (col_a, col_b, col_c, col_d, col_e, col_f) VALUES (NULL, NULL, NULL, NULL, NULL, NULL);
SELECT MIN(DISTINCT col_b) FROM test_table WHERE col_a IS NULL;
CREATE TABLE another_table (id Int32, value Float32, description String) ENGINE = MergeTree() ORDER BY id;
SELECT COUNT(DISTINCT value) FROM another_table WHERE value IS NOT NULL;
