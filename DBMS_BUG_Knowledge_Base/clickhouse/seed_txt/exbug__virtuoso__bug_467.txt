CREATE TABLE events (event_id Int32, event_name String, event_date DateTime, location String, attendees Nullable(Int32) DEFAULT 0);
INSERT INTO events (event_id, event_name, event_date, location, attendees) VALUES (1, 'Concert', '2023-10-01 20:00:00', 'Stadium', 100);
INSERT INTO events (event_id, event_name, event_date, location, attendees) VALUES (2, 'Conference', '2023-10-05 09:00:00', 'Convention Center', NULL);
SELECT event_name, event_date, attendees, IF(attendees IS NULL, 'No attendees', toString(attendees)) AS attendee_status FROM events WHERE event_date > now();
UPDATE events SET attendees = ifNull(attendees, 0) + 1 WHERE event_id = 1;
