CREATE TABLE student_scores (student_id Int32, subject String, score Float32) ENGINE = MergeTree() ORDER BY student_id;
INSERT INTO student_scores VALUES (1, 'math', 85.5);
INSERT INTO student_scores VALUES (2, 'science', 90.0);
INSERT INTO student_scores VALUES (1, 'science', NULL);
SELECT student_id, SUM(score) AS total_score, ROW_NUMBER() OVER (ORDER BY SUM(score) DESC) AS rank FROM student_scores GROUP BY student_id;
