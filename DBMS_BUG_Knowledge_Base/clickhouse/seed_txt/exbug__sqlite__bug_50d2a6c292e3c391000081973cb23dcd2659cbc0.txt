SET allow_experimental_object_type = 1;
CREATE TABLE t2(a Int32, b String, c String, d String, e String, PRIMARY KEY(a), INDEX idx (d, e) TYPE minmax) ENGINE = MergeTree() ORDER BY a;
INSERT INTO t2 VALUES(1, 'one', 'one', 'one', 'one');
INSERT INTO t2 VALUES(2, 'two', 'two', 'one', NULL);
ALTER TABLE t2 DELETE WHERE a = 1;
