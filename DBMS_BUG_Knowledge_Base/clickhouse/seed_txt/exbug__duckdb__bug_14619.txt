CREATE TABLE test_table (id Int32, values Array(Int32));
INSERT INTO test_table VALUES (1, [10, 20, 30]), (2, [NULL, 40, 50]), (3, [60, 70, NULL]);
SELECT id, arrayMap((x, ix) -> ix + 1, values, arrayEnumerate(values)) AS transformed_ix FROM test_table;
SELECT id, arrayReduce((acc, x, ix) -> ix, values) AS reduced_ix FROM test_table;
SELECT id, arrayMap((x) -> x + 1, values) AS incremented_values FROM test_table WHERE values IS NOT NULL;
