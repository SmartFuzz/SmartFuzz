{
  "url": "https://bugs.mysql.com/bug.php?id=101575",
  "title": "\"Illegal mix of collations\" against a string",
  "description": "After upgrade to 8.0.22, we are getting the error\r\n\"ERROR 1267 (HY000): Illegal mix of collations (latin1_swedish_ci,IMPLICIT) and (utf8mb4_0900_ai_ci,COERCIBLE) for operation 'like'\" when comparing a string against a \"latin1_swedish_ci\" column for prepared statements.\r\n\r\nVersions prior to 8.0.22 have no problems.\r\n\r\nThe problem occurs when using:\r\n- mysql client Ver 8.0.22-0ubuntu0.20.04.2 for Linux on x86_64 ((Ubuntu));\r\n- phpmyadmin v4.9.5;\r\n- PHP v7.4, when \"PDO::ATTR_EMULATE_PREPARES\" is \"false\" when establishing a database connection using PDO.\nHow to repeat:\nCREATE DATABASE db COLLATE latin1_swedish_ci;\r\nUSE db;\r\nCREATE TABLE `tb` (\r\n  `id` int UNSIGNED NOT NULL,\r\n  `col1` varchar(100) NOT NULL,\r\n  `col2` varchar(200) NOT NULL COLLATE utf8_general_ci\r\n) ENGINE=InnoDB DEFAULT CHARSET=latin1;\r\nPREPARE stmt1 FROM 'SELECT * from `tb` WHERE `col1` like ? OR `col2` LIKE ?';",
  "date": "2020-11-12 08:03:00",
  "vulnerable_version": "8.0.22",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "mysql",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER NOT NULL PRIMARY KEY, name TEXT NOT NULL, description TEXT NOT NULL);",
    "INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description');",
    "SELECT * FROM test_table WHERE name LIKE ? OR description LIKE ?;",
    "SELECT * FROM test_table WHERE name LIKE 'T%' OR description LIKE 'This%';"
  ],
  "key_bug_features": [
    "Illegal mix of collations in prepared statements"
  ]
}