{
  "url": "https://github.com/openlink/virtuoso-opensource/issues/401",
  "title": "Substract duration from datetime",
  "description": "I want to get the date of yesterday (actually for filtering) and identified a way to compute the date of yesterday in Virtuoso by subtracting an integer of seconds from the current timestamp:\nSELECT DISTINCT (xsd:date(NOW()) AS ?today) (xsd:date(NOW()-(3600*24)) AS ?yesterday)\nWHERE {?s a ?o.}\n\nApache Jena does not support subtracting integers from xsd:date/xsd:datetime, it demands a time period as xsd:duration, which is much more intuitive, e.g.\nSELECT DISTINCT (xsd:date(NOW()) AS ?today) (xsd:date(NOW()-\"P1D\"^^xsd:duration) AS ?yesterday)\nWHERE {?s a ?o.}\n\nThat again is not supported by Virtuoso. Would be great to have compatible queries for this.",
  "date": "2015-05-08 11:50:27",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "virtuoso",
  "bug_features_seed": [
    "CREATE TABLE test_dates (id INTEGER PRIMARY KEY, event_date DATE NOT NULL);",
    "INSERT INTO test_dates (id, event_date) VALUES (1, DATE('now')), (2, DATE('now', '-1 day')), (3, DATE('now', '-2 days'));",
    "SELECT id, event_date, DATE('now', '-1 day') AS yesterday FROM test_dates WHERE event_date < DATE('now');",
    "SELECT id, event_date FROM test_dates WHERE event_date = DATE('now', '-1 day') OR event_date IS NULL;",
    "SELECT DISTINCT DATE('now') AS today, DATE('now', '-1 day') AS yesterday;"
  ],
  "key_bug_features": [
    "Subtracting integers from xsd:date/xsd:datetime"
  ]
}