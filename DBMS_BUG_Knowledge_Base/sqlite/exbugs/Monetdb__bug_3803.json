{
  "url": "https://github.com/MonetDB/MonetDB/issues/3803",
  "title": "SQL query parser fails to parse valid SELECT query with a CASE .. END in it. It fails with parser error: identifier 'x' ambiguous",
  "description": "",
  "date": "2020-11-30T13:17:02Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE t2(x INTEGER, y TEXT, z DATE, w REAL, v INTEGER);",
    "INSERT INTO t2(x, y, z, w, v) VALUES(1, 'test', '2023-01-01', 1.1, 1);",
    "INSERT INTO t2(x, y, z, w, v) VALUES(2, 'example', '2023-02-01', 2.2, 0);",
    "SELECT CASE WHEN x > 1 THEN 'greater' ELSE 'lesser' END AS comparison, y, w + 1.0, (SELECT COUNT(*) FROM t2 WHERE x < t2.x) FROM t2 WHERE v IS NOT NULL ORDER BY 1, 2;",
    "SELECT x, y, CASE WHEN w < 2.0 THEN 'low' ELSE 'high' END AS level FROM t2 WHERE EXISTS(SELECT 1 FROM t2 WHERE y = t2.y) ORDER BY 1;"
  ],
  "key_bug_features": [
    "SQL parser fails with CASE statement in SELECT query"
  ]
}