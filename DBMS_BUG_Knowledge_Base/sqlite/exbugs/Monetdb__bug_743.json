{
  "url": "https://github.com/MonetDB/MonetDB/issues/743",
  "title": "combination of subquery and intersect fails",
  "description": "",
  "date": "2020-11-30T08:51:52Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE urls(urlid INTEGER PRIMARY KEY, c INTEGER, d INTEGER, url_text TEXT);",
    "CREATE TABLE keys(urlid INTEGER, a INTEGER, b INTEGER, FOREIGN KEY(urlid) REFERENCES urls(urlid), created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO urls(urlid, c, d, url_text) VALUES (1, 10, 20, 'http://example.com/1');",
    "INSERT INTO keys(urlid, a, b) VALUES (1, 50, 150), (1, 50, 250), (2, 60, 180);",
    "SELECT * FROM urls WHERE urlid IN (SELECT urlid FROM keys WHERE a < 100 AND b < 200 INTERSECT SELECT urlid FROM keys WHERE a < 100 AND b < 300);"
  ],
  "key_bug_features": [
    "subquery with intersect in URL selection fails"
  ]
}