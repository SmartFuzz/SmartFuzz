{
  "url": "https://bugs.mysql.com/bug.php?id=102843",
  "title": "CSV Engine (tina) does not handle json type",
  "description": "CSV Engine (tina) allows creating json-type field and writing a row, however, it does not read it.\nHow to repeat:\nmysql> create table t1 (c json not null) ENGINE CSV;\r\nQuery OK, 0 rows affected (0.08 sec)\r\n\r\nmysql> insert t1 values ('{\"a\":1}');\r\nQuery OK, 1 row affected (0.01 sec)\r\n\r\nmysql> select * from t1;\r\nERROR 3144 (22032): Cannot create a JSON value from a string with CHARACTER SET 'binary'.",
  "date": "2021-03-07 04:02:00",
  "vulnerable_version": "8.0.23",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "mysql",
  "bug_features_seed": [
    "CREATE TABLE t2 (id INTEGER PRIMARY KEY, data TEXT NOT NULL);",
    "INSERT INTO t2 VALUES (1, '{\"key\":\"value\"}');",
    "CREATE TABLE t3 (ref_id INTEGER, description TEXT, FOREIGN KEY (ref_id) REFERENCES t2(id));",
    "INSERT INTO t3 VALUES (1, 'Test entry');",
    "SELECT t2.data, t3.description FROM t2 JOIN t3 ON t2.id = t3.ref_id;"
  ],
  "key_bug_features": [
    "CSV Engine does not read json-type fields"
  ]
}