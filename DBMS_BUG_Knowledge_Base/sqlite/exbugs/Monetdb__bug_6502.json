{
  "url": "https://github.com/MonetDB/MonetDB/issues/6502",
  "title": "Query with multiple limit clauses does not return anything",
  "description": "",
  "date": "2020-11-30T16:22:58Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, amount REAL, status BOOLEAN);",
    "INSERT INTO test_table (id, name, amount, status) VALUES (1, 'Test Product', 99.99, 1);",
    "INSERT INTO test_table (id, name, amount, status) VALUES (2, 'Another Product', NULL, 0);",
    "SELECT name, amount FROM test_table WHERE amount IS NOT NULL ORDER BY amount DESC LIMIT 5;",
    "SELECT * FROM (SELECT * FROM test_table ORDER BY created_at DESC LIMIT 10) LIMIT 3;"
  ],
  "key_bug_features": [
    "Query with multiple limit clauses fails to return results"
  ]
}