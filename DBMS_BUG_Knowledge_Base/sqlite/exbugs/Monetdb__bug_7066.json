{
  "url": "https://github.com/MonetDB/MonetDB/issues/7066",
  "title": "percent_rank function with wrong results ",
  "description": "The percent_rank window function computes the percentages based on the total number of rows instead of the partitions' when that's the case.\n\n**To Reproduce**\nSELECT PERCENT_RANK() OVER ( PARTITION BY col1 ORDER BY col2 ) FROM tbl;\n\n**Expected behavior**\nThe percentages should be computed based on the partition count.\n\n**Software versions**\n - MonetDB Oct2020\n - OS and version: Fedora 35\n - ISself-installed and compiled",
  "date": "2021-02-23T08:16:39Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE tbl (col1 INTEGER, col2 INTEGER, col3 TEXT NOT NULL);",
    "INSERT INTO tbl (col1, col2, col3) VALUES (1, 10, 'A'), (1, 20, 'B'), (2, 30, 'C'), (2, 40, 'D'), (NULL, 50, 'E');",
    "CREATE TABLE tbl2 (col4 INTEGER PRIMARY KEY, col5 REAL);",
    "INSERT INTO tbl2 (col4, col5) VALUES (1, 0.1), (2, 0.2), (3, 0.3);",
    "SELECT col1, col2, (ROW_NUMBER() OVER (PARTITION BY col1 ORDER BY col2) - 1) * 1.0 / COUNT(*) OVER (PARTITION BY col1) AS percent_rank FROM tbl;"
  ],
  "key_bug_features": [
    "percent_rank function miscalculates percentages in partitions"
  ]
}