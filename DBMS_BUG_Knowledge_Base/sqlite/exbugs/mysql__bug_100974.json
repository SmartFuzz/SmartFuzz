{
  "url": "https://bugs.mysql.com/bug.php?id=100974",
  "title": "JSON_OBJECT() performs key sort on output",
  "description": "Suggested fix:\nJSON_OBJECT function should maintain the column order",
  "date": "2020-09-28 13:52:00",
  "vulnerable_version": "8.0",
  "poc_type": "sql_text",
  "vulnerable_database": "sqlite",
  "source_database": "mysql",
  "bug_features_seed": [
    "CREATE TABLE users (user_id INTEGER PRIMARY KEY, name TEXT, dob TEXT, email TEXT NOT NULL);",
    "INSERT INTO users (user_id, name, dob, email) VALUES (1, 'Ganesh', '1980-04-27', 'ganesh@example.com');",
    "SELECT json_object('user_id', user_id, 'name', name, 'dob', dob) AS output FROM users WHERE user_id = 1;",
    "SELECT json_object('email', email, 'dob', dob, 'name', name) AS output FROM users WHERE user_id = 1;",
    "SELECT json_object('user_id', user_id, 'name', name, 'dob', dob) AS output FROM (SELECT * FROM users) WHERE user_id = 1;"
  ],
  "key_bug_features": [
    "JSON_OBJECT function key order not preserved"
  ]
}