CREATE TABLE test_users (id INTEGER PRIMARY KEY, username TEXT NOT NULL, password TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_users (id, username, password) VALUES (1, 'test_user', 'Test-1234');
SELECT username FROM test_users WHERE id = 1;
SELECT * FROM sqlite_master WHERE type='table' AND name='test_users';
