CREATE TABLE ref_shapes (id INTEGER PRIMARY KEY, shape TEXT, color_id INTEGER);
INSERT INTO ref_shapes (id, shape, color_id) VALUES (1, 'circle', NULL), (2, 'square', 1), (3, 'triangle', 2);
CREATE TABLE t__fuzz_2 (shape_id INTEGER, description TEXT);
INSERT INTO t__fuzz_2 (shape_id, description) VALUES (1, 'A round shape'), (2, 'A four-sided shape'), (NULL, 'No shape');
SELECT DISTINCT shape_id, (SELECT n FROM colors WHERE id = shape_id) AS n FROM t__fuzz_2 WHERE shape_id IS NOT NULL ORDER BY shape_id;
