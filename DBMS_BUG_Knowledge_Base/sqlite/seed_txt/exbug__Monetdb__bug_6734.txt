CREATE TABLE IF NOT EXISTS test_table (id INTEGER PRIMARY KEY, value REAL NOT NULL, description TEXT);
INSERT INTO test_table (id, value, description) VALUES (1, 10.5, 'First entry'), (2, NULL, 'Second entry'), (3, 20.0, 'Third entry');
SELECT SUM(value) FROM test_table WHERE value IS NOT NULL;
SELECT AVG(value) FROM test_table;
SELECT id, value, description FROM test_table WHERE value IS NOT NULL;
