CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT, value REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.50), (2, 'test2', NULL), (3, 'test3', 20.00);
SELECT substr(name, instr(name, ',') + 1, instr(name || ',', ',') - instr(name, ',') - 1) AS extracted FROM test_table WHERE value IS NOT NULL;
SELECT substr('a,b,c', 1, instr('a,b,c', ',') - 1) AS test_case;
WITH RECURSIVE cte AS (SELECT 1 AS a UNION ALL SELECT a + 1 FROM cte WHERE a < 100) SELECT DISTINCT substr('example1,example2,example3', 1, instr('example1,example2,example3', ',') - 1) AS result FROM cte;
