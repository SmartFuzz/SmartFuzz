CREATE TABLE test_table (id INTEGER NOT NULL PRIMARY KEY, name TEXT NOT NULL, description TEXT NOT NULL);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description');
SELECT * FROM test_table WHERE name LIKE ? OR description LIKE ?;
SELECT * FROM test_table WHERE name LIKE 'T%' OR description LIKE 'This%';
