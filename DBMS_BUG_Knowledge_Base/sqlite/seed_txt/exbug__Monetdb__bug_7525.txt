CREATE TABLE test_vals(grp INTEGER NOT NULL, value INTEGER CHECK(value >= 0));
INSERT INTO test_vals VALUES (1, 100), (2, 200), (1, NULL), (2, 300);
CREATE TEMPORARY TABLE jit_sum_variadic AS SELECT grp, SUM(value) AS total FROM test_vals GROUP BY grp;
SELECT grp, total FROM jit_sum_variadic;
SELECT grp, SUM(value) FROM test_vals WHERE value IS NOT NULL GROUP BY grp;
