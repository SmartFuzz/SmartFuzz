CREATE TABLE t1 ( id INTEGER NOT NULL, name TEXT NOT NULL, value REAL DEFAULT NULL, PRIMARY KEY (id) );
INSERT INTO t1 (id, name, value) VALUES (1, 'test', 10.50), (2, 'example', NULL), (3, 'sample', 5.75);
SELECT * FROM t1 WHERE value IS NULL OR (value GLOB '[0-9]*');
CREATE TABLE t2 ( a TEXT NOT NULL, b DATETIME DEFAULT NULL, PRIMARY KEY (a) );
SELECT a FROM t2 WHERE (b IS NULL AND (a GLOB '[A-Z]*')) OR (b IS NOT NULL AND (a GLOB '.*'));
