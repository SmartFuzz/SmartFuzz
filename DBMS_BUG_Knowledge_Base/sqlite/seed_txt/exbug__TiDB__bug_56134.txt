CREATE TABLE tc (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER DEFAULT 0);
INSERT INTO tc VALUES (1, 'Alice', 30), (2, 'Bob', NULL);
CREATE TABLE td (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER);
INSERT INTO td SELECT id, name, age FROM tc WHERE age IS NOT NULL;
SELECT * FROM td;
