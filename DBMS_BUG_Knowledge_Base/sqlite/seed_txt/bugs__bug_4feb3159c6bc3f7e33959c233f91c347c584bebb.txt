CREATE TABLE test_table (id INTEGER PRIMARY KEY, value INTEGER, category TEXT);
INSERT INTO test_table (value, category) VALUES (1, 'A'), (2, 'B'), (3, 'A'), (NULL, 'C');
SELECT value, SUM(value) OVER (PARTITION BY category) AS category_sum FROM test_table ORDER BY category_sum;
SELECT +SUM(value) OVER () AS total_sum FROM test_table WHERE value IS NOT NULL;
SELECT +SUM(value) OVER () ORDER BY +SUM(value) OVER ();
