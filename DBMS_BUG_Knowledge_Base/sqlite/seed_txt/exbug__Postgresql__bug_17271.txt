CREATE TABLE example (id INTEGER PRIMARY KEY AUTOINCREMENT, direction TEXT NOT NULL, below BOOLEAN NOT NULL);
INSERT INTO example (direction, below) VALUES ('UP', 0), ('DOWN', 1);
CREATE TEMP TABLE example_temp AS SELECT id, direction, below FROM example;
DELETE FROM example;
INSERT INTO example (id, direction, below) SELECT id, direction, below FROM example_temp;
DROP TABLE example_temp;
