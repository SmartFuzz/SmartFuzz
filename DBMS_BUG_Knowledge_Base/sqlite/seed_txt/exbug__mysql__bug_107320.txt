CREATE TABLE spatial_test_v2 (id INTEGER PRIMARY KEY AUTOINCREMENT, latitude REAL NOT NULL, longitude REAL NOT NULL, geo_point TEXT NOT NULL);
INSERT INTO spatial_test_v2 (latitude, longitude) VALUES (NULL, -75.417531);
INSERT INTO spatial_test_v2 (latitude, longitude) VALUES (42.062961, NULL);
SELECT * FROM spatial_test_v2 WHERE geo_point IS NOT NULL AND latitude > 40.0;
SELECT * FROM spatial_test_v2 WHERE latitude IS NOT NULL AND longitude IS NOT NULL;
