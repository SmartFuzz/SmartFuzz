CREATE TABLE three (x INTEGER, y TEXT, z REAL, w INTEGER);
INSERT INTO three VALUES (1, 'test', 1.1, 1), (2, 'example', 2.2, 0);
SELECT * FROM three;
DROP TABLE three;
CREATE VIEW three AS SELECT * FROM three;
