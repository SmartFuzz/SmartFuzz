CREATE TABLE test1 (id INTEGER, value INTEGER, description TEXT);
INSERT INTO test1 (id, value, description) VALUES (1, 10, 'first'), (2, NULL, 'second'), (3, 20, 'third');
CREATE TABLE test2 (ref_id INTEGER, amount REAL, status INTEGER);
INSERT INTO test2 (ref_id, amount, status) VALUES (1, 15.5, 1), (2, NULL, 0), (3, 25.0, NULL);
SELECT MIN(value) AS min_value, MAX(value) AS max_value, SUM(value) AS sum_value FROM test1 UNION SELECT MIN(amount) AS min_amount, MAX(amount) AS max_amount, SUM(amount) AS sum_amount FROM test2;
