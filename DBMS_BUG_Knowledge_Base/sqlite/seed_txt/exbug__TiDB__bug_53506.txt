CREATE TABLE t1(c1 INTEGER NOT NULL, c2 TEXT);
INSERT INTO t1 (c1, c2) VALUES (1, 'test'), (2, 'example');
CREATE VIEW v1 AS SELECT c1, LENGTH(c2) AS len FROM t1;
SELECT * FROM v1 LEFT JOIN t1 ON v1.c1 = t1.c1;
SELECT ((~(v1.len & 0)) - (CASE WHEN t1.c1 = 1 THEN 0 ELSE 1 END)) FROM v1 LEFT JOIN t1 ON v1.c1 = t1.c1;
