CREATE TABLE table1 (a INTEGER, b TEXT, c DATE, d REAL, PRIMARY KEY (a, b));
INSERT INTO table1 (a, b, c, d) VALUES (1, 'value1', '2023-01-01', 1.1), (1, 'value2', '2023-01-02', 1.2), (2, 'value1', '2023-01-01', 2.1), (2, NULL, '2023-01-03', 2.2);
SELECT a, COUNT(DISTINCT b) FROM table1 GROUP BY a;
SELECT a, COUNT(b) FROM (SELECT DISTINCT a, b FROM table1 WHERE c IS NOT NULL) AS t GROUP BY a;
SELECT a, COUNT(DISTINCT b) FROM (SELECT DISTINCT a, b FROM table1 WHERE d > 1.0) AS t GROUP BY a;
