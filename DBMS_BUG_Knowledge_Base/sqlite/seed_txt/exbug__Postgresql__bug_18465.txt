CREATE TABLE a (col_int INTEGER, col_text TEXT);
INSERT INTO a VALUES (1, 'one'), (2, 'two'), (NULL, 'three');
CREATE TABLE b (col_int INTEGER, col_float REAL);
INSERT INTO b VALUES (1, 1.1), (2, 2.2), (3, NULL);
SELECT (SELECT MIN(col_int) FROM b WHERE b.col_int = a.col_int), a.col_int FROM a;
