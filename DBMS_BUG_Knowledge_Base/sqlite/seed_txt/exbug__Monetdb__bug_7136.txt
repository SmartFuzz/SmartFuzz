DROP TABLE IF EXISTS sys.v;
CREATE TABLE sys.v (id INTEGER, version INTEGER, value DOUBLE, status TEXT);
INSERT INTO sys.v VALUES (1, 1622470128, 1.0, 'active');
INSERT INTO sys.v VALUES (2, 1622470128, 2.0, 'inactive');
DELETE FROM sys.v WHERE (id, version) IN (SELECT id, version FROM (SELECT id, version, ROW_NUMBER() OVER (PARTITION BY id ORDER BY version DESC) AS rn FROM sys.v) t WHERE rn > 1);
