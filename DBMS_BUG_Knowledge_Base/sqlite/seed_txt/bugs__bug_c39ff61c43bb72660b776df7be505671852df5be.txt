CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK(age >= 0));
CREATE TABLE comments (id INTEGER PRIMARY KEY, user_id INTEGER, content TEXT NOT NULL, FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE);
INSERT INTO users (name, age) VALUES ('Alice', 30);
INSERT INTO comments (user_id, content) VALUES (1, 'This is a comment.');
SELECT * FROM comments WHERE user_id = (SELECT id FROM users WHERE name = 'Alice');
