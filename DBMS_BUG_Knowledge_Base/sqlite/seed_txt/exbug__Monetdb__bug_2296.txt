CREATE TABLE student_scores (student_id INTEGER PRIMARY KEY, subject TEXT, score REAL CHECK (score >= 0));
INSERT INTO student_scores (student_id, subject, score) VALUES (1, 'math', 85.5);
INSERT INTO student_scores (student_id, subject, score) VALUES (2, 'science', 90.0);
INSERT INTO student_scores (student_id, subject, score) VALUES (1, 'science', NULL);
SELECT student_id, SUM(score) AS total_score FROM student_scores GROUP BY student_id;
SELECT student_id, total_score, RANK() OVER (ORDER BY total_score DESC) FROM (SELECT student_id, SUM(score) AS total_score FROM student_scores GROUP BY student_id);
