CREATE TABLE test_table (id INTEGER PRIMARY KEY NOT NULL, name TEXT NOT NULL, age INTEGER DEFAULT 0);
INSERT INTO test_table (id, name, age) VALUES (1, 'Alice', 0);
CREATE TABLE another_table (ref_id INTEGER, description TEXT NOT NULL);
INSERT INTO another_table (ref_id, description) VALUES (1, '');
SELECT * FROM test_table WHERE age IS NULL;
