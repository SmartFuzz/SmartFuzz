CREATE TABLE test_table (id INTEGER PRIMARY KEY, arr TEXT NOT NULL);
INSERT INTO test_table (id, arr) VALUES (1, 'a,c,b');
SELECT arr, LENGTH(arr) AS arr_length FROM test_table WHERE id = 1;
SELECT '''' || REPLACE(arr, ',', ''', ''') || '''' AS escaped_arr FROM test_table;
SELECT LENGTH('hello, world') AS before, LENGTH('hello, world') AS after;
