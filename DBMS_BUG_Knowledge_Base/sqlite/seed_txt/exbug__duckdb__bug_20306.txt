CREATE TABLE test_table (id INTEGER, name TEXT, value REAL);
INSERT INTO test_table (id, name, value) VALUES (1, 'test', NULL);
SELECT '' AS concat_result;
SELECT CASE WHEN NULL IS NULL THEN 1 ELSE 0 END AS is_null_result;
SELECT id, name FROM test_table WHERE value IS NULL;
