DROP TABLE IF EXISTS vs_server;
CREATE TABLE vs_server (nGroupId INTEGER NOT NULL, nName TEXT);
INSERT INTO vs_server(nGroupId, nName) VALUES (15, 'Group A'), (21, 'Group B');
CREATE TEMP TABLE tmp_table (group_id INTEGER);
INSERT INTO tmp_table (group_id) VALUES (199);
SELECT CASE WHEN EXISTS (SELECT 1 FROM vs_server INNER JOIN tmp_table ON vs_server.nGroupId = tmp_table.group_id) THEN 1 ELSE 0 END AS nVal;
