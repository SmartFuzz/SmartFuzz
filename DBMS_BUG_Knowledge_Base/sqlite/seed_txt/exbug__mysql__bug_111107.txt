CREATE TABLE test.data_types (id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, amount REAL NOT NULL);
INSERT INTO test.data_types (id, name, amount) VALUES (1, 'Test', 100.50);
CREATE TABLE test.exampleProcedure(a INTEGER, b TEXT, c REAL, d DATETIME);
INSERT INTO test.exampleProcedure(a, b, c, d) VALUES (1, 'Sample', NULL, CURRENT_TIMESTAMP);
SELECT * FROM test.data_types WHERE amount > (SELECT AVG(amount) FROM test.data_types);
