CREATE TABLE g(id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, age INTEGER CHECK (age >= 0));
CREATE TABLE h(id INTEGER PRIMARY KEY AUTOINCREMENT, description TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO g(name, age) VALUES('Alice', 30), ('Bob', -5), (NULL, 25);
INSERT INTO h(description) VALUES('Test entry'), (NULL), ('Another entry');
SELECT * FROM g LEFT JOIN h ON g.id = h.id WHERE g.age IS NOT NULL AND h.description IS NOT NULL;
