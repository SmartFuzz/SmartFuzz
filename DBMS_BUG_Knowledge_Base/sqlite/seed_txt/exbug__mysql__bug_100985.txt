CREATE TABLE t2 (x REAL, y INTEGER, z REAL NOT NULL DEFAULT 0.00001);
INSERT INTO t2 VALUES (0.001, 1, NULL), (1.0, 2, 0.00001), (NULL, -1, 0.001);
SELECT SUM(x) AS total_x, SUM(z) AS total_z FROM t2 WHERE y IS NOT NULL;
SELECT CAST(SUM(x) AS REAL) FROM t2 WHERE z IS NOT NULL;
SELECT (SELECT SUM(x) FROM t2) + (SELECT SUM(z) FROM t2) AS combined_sum;
