CREATE TABLE bar (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, value REAL DEFAULT NULL);
INSERT INTO bar (name, value) VALUES ('test1', 10.50);
CREATE TABLE baz (ref_id INTEGER, description TEXT NOT NULL, FOREIGN KEY(ref_id) REFERENCES bar(id));
INSERT INTO baz (ref_id, description) VALUES (1, 'description1');
SELECT * FROM (SELECT * FROM baz) AS subquery WHERE ref_id IS NOT NULL;
