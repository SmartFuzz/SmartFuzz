CREATE TABLE test_table ( id INTEGER PRIMARY KEY AUTOINCREMENT, value TEXT DEFAULT NULL, amount REAL DEFAULT 0.00, created_at DATETIME DEFAULT CURRENT_TIMESTAMP );
INSERT INTO test_table (value, amount) VALUES (NULL, 100.00);
INSERT INTO test_table (value, amount) VALUES ('Test Value', NULL);
UPDATE test_table SET amount = amount + 50 WHERE value IS NOT NULL;
SELECT * FROM test_table WHERE amount > (SELECT AVG(amount) FROM test_table WHERE value IS NOT NULL);
