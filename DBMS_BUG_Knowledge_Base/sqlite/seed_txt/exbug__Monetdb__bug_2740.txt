CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, age INTEGER CHECK (age >= 0));
INSERT INTO users (id, name, age) VALUES (1, 'Alice', 30);
CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, total_amount REAL);
INSERT INTO orders (order_id, user_id, total_amount) VALUES (101, 1, 250.75);
SELECT name, age, (SELECT COUNT(*) FROM orders WHERE user_id = id) AS order_count FROM users;
