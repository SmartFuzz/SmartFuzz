CREATE TABLE users(id INTEGER, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
ALTER TABLE users ADD COLUMN email TEXT DEFAULT 'example@example.com';
INSERT INTO users(id, name) VALUES(1, 'Alice');
SELECT * FROM users WHERE created_at IS NOT NULL;
ALTER TABLE users ADD COLUMN age INTEGER DEFAULT 18;
