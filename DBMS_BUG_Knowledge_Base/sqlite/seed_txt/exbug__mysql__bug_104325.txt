CREATE TABLE Bug(id INTEGER PRIMARY KEY AUTOINCREMENT, data TEXT NOT NULL);
INSERT INTO Bug(data) VALUES ('["123", "456"]');
CREATE TABLE AnotherBug(id INTEGER PRIMARY KEY AUTOINCREMENT, bug_id INTEGER, FOREIGN KEY (bug_id) REFERENCES Bug(id));
INSERT INTO AnotherBug(bug_id) VALUES (1);
SELECT * FROM Bug WHERE 123 IN (SELECT json_each.value FROM json_each(data)) AND EXISTS (SELECT 1 FROM AnotherBug WHERE bug_id = Bug.id);
