CREATE TABLE test (foo INTEGER, bar INTEGER, baz TEXT);
CREATE UNIQUE INDEX foo_idx ON test(foo);
INSERT INTO test (foo, bar, baz) VALUES (1, 2, 'example');
CREATE TABLE orders (order_id INTEGER, order_date DATE, customer_id INTEGER, FOREIGN KEY(customer_id) REFERENCES test(foo));
CREATE TABLE temp_test AS SELECT foo, baz FROM test;
