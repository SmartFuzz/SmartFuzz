CREATE TABLE t(p INTEGER, q INTEGER, r TEXT DEFAULT 'default');
INSERT INTO t(p, q) VALUES (NULL, 1);
SELECT 1;
-- Simulating advisory lock with a dummy query;
UPDATE t SET q = q + 1 WHERE p IS NULL;
UPDATE t SET r = 'updated' WHERE rowid = (SELECT rowid FROM t LIMIT 1);
