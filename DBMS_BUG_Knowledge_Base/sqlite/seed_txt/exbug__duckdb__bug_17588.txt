CREATE TABLE test_table (id INTEGER, status BOOLEAN, description TEXT);
INSERT INTO test_table VALUES (1, 1, 'active'), (2, 0, 'inactive'), (3, 1, 'running');
SELECT CASE WHEN status THEN 'Status is active' ELSE 0 END FROM test_table;
SELECT CASE WHEN status THEN 'Active' ELSE NULL END AS result FROM test_table;
SELECT CASE WHEN id > 1 THEN 'Greater than one' ELSE 'One or less' END FROM test_table;
