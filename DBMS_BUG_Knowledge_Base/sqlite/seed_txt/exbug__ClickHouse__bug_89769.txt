CREATE TABLE repl_bar (id INTEGER, value REAL, created_at DATETIME);
INSERT INTO repl_bar VALUES (1, 3.14, CURRENT_TIMESTAMP), (2, 2.71, CURRENT_TIMESTAMP), (3, NULL, CURRENT_TIMESTAMP);
CREATE TABLE repl_bar_dist AS SELECT * FROM repl_bar;
SELECT value FROM repl_bar_dist WHERE id = 2 ORDER BY created_at DESC LIMIT 1;
SELECT COUNT(*) FROM (SELECT DISTINCT value FROM repl_bar_dist WHERE value IS NOT NULL);
