CREATE TABLE t2 (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2 (name) VALUES ('Test Name');
SELECT * FROM t2 WHERE id = (SELECT MAX(id) FROM t2);
CREATE TABLE d1.t1 (num INTEGER PRIMARY KEY);
SELECT 0 AS innodb_validate_tablespace_paths;
