CREATE TABLE t2 ( id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP );
INSERT INTO t2 (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (51, 'Charlie');
SELECT id, name, created_at FROM t2 WHERE created_at IS NOT NULL;
DELETE FROM t2 WHERE id < 50;
SELECT name, created_at FROM t2;
