CREATE TABLE IF NOT EXISTS test_table (id TEXT PRIMARY KEY, name TEXT, age INTEGER, salary REAL);
INSERT INTO test_table (id, name, age, salary) VALUES (lower(hex(randomblob(16))), 'Alice', 30, 70000.50);
INSERT INTO test_table (id, name, age) VALUES (lower(hex(randomblob(16))), 'Bob', 25);
ALTER TABLE test_table ADD COLUMN is_active INTEGER DEFAULT 1;
SELECT id, name, age, salary, is_active FROM test_table WHERE age > 20 ORDER BY salary DESC;
