DROP TABLE IF EXISTS test_t2;
CREATE TABLE test_t2 (id INTEGER PRIMARY KEY, value REAL, status TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_t2 VALUES (1, 10.50, 'active'), (2, 20.75, 'inactive'), (3, NULL, 'active'), (4, 15.00, 'inactive'), (5, 25.00, NULL);
SELECT id, value, (SELECT value FROM test_t2 t2 WHERE t2.status = test_t2.status ORDER BY created_at DESC LIMIT 1) AS last_val FROM test_t2;
SELECT id, value, COALESCE(value, 0) AS value_coalesced FROM test_t2 WHERE value IS NOT NULL;
