CREATE TABLE source (sid INTEGER PRIMARY KEY, sname TEXT NOT NULL);
INSERT INTO source (sid, sname) VALUES (1, 'Source1');
CREATE TABLE target (tid INTEGER PRIMARY KEY, tval INTEGER DEFAULT 0 CHECK (tval >= 0));
INSERT INTO target (tid, tval) VALUES (1, 1);
UPDATE target SET tval = tval + 1 WHERE tid IN (SELECT sid FROM source);
