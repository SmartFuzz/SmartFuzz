CREATE TABLE test_table (id INTEGER, value TEXT, status BOOLEAN DEFAULT 1);
INSERT INTO test_table (id, value) VALUES (1, 'alpha'), (2, 'beta'), (3, 'gamma');
SELECT CASE WHEN status = 1 THEN 'active' ELSE 'inactive' END AS status FROM test_table;
SELECT * FROM (SELECT CASE WHEN value = 'alpha' THEN 'first' ELSE 'other' END AS value FROM test_table);
SELECT * FROM (SELECT CASE WHEN id = 2 THEN 'second' ELSE 'not second' END AS id FROM test_table);
