DROP TABLE IF EXISTS mysql_test;
CREATE TABLE mysql_test (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, status TEXT NOT NULL CHECK(status IN ('active', 'inactive')));
INSERT INTO mysql_test (name, status) VALUES ('Alice', 'active'), ('Bob', 'inactive'), ('Charlie', 'active');
SELECT * FROM mysql_test t1 WHERE EXISTS (SELECT 1 FROM mysql_test t2 WHERE t2.status = 'active' AND t1.created_at IS NOT NULL);
SELECT * FROM mysql_test t1 WHERE NOT EXISTS (SELECT 1 FROM mysql_test t2 WHERE t2.name = 'NonExistentName' AND t1.status = 'inactive');
