CREATE TABLE test_data (id INTEGER PRIMARY KEY, value REAL, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data SELECT i, RANDOM() % 1000 / 1000.0, 'Name ' || i, DATETIME('now') FROM (SELECT ROW_NUMBER() OVER() AS i FROM (SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1) AS t1, (SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1) AS t2 LIMIT 200000000));
CREATE TABLE filtered_data AS SELECT * FROM test_data WHERE value IS NOT NULL;
SELECT * FROM filtered_data ORDER BY value DESC;
SELECT COUNT(*) FROM filtered_data WHERE value BETWEEN 0.0 AND 1.0;
