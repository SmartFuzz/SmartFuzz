CREATE TABLE test_memory_usage (id INTEGER PRIMARY KEY AUTOINCREMENT, node_id INTEGER NOT NULL, memory_type TEXT NOT NULL, total INTEGER NOT NULL, used INTEGER NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_memory_usage (node_id, memory_type, total, used) VALUES (10, 'Long message buffer', 67108864, 28000000);
INSERT INTO test_memory_usage (node_id, memory_type, total, used) VALUES (11, 'Data memory', 154618822656, 10000000);
SELECT node_id, memory_type, total, used * 1.0 / total * 100 AS memory_used FROM test_memory_usage WHERE used > total * 0.5;
UPDATE test_memory_usage SET used = NULL WHERE node_id = 10 AND memory_type = 'Long message buffer';
