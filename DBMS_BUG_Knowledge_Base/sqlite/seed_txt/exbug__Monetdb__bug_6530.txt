CREATE TABLE test_table (id INTEGER PRIMARY KEY, value INTEGER, description TEXT);
INSERT INTO test_table (id, value, description) VALUES (1, 100, 'First entry'), (2, NULL, 'Second entry'), (3, 200, 'Third entry');
CREATE TABLE reference_table (ref_id INTEGER, ref_value INTEGER NOT NULL);
INSERT INTO reference_table (ref_id, ref_value) VALUES (1, 100), (2, 300), (3, 400);
SELECT value FROM test_table WHERE value NOT IN (SELECT ref_value FROM reference_table WHERE ref_id < 3) OR description IS NULL;
