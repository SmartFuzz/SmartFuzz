CREATE TABLE sbtest1 (id INTEGER PRIMARY KEY, k INTEGER NOT NULL);
CREATE TABLE sbtest2 (id INTEGER PRIMARY KEY, a INTEGER DEFAULT NULL);
INSERT INTO sbtest1 (id, k) SELECT seq, seq * 2 FROM (SELECT ROW_NUMBER() OVER () AS seq FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t LIMIT 10000000);
INSERT INTO sbtest2 (id) SELECT seq FROM (SELECT ROW_NUMBER() OVER () AS seq FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t LIMIT 10000000);
SELECT COUNT(*) FROM sbtest1 WHERE k IN (SELECT a FROM sbtest2 WHERE a IS NOT NULL);
