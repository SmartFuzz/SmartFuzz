DROP TABLE IF EXISTS testUUID;
CREATE TABLE testUUID (ID INTEGER PRIMARY KEY, new_ID TEXT NOT NULL, new_ID2 TEXT DEFAULT NULL, new_ID3 BLOB UNIQUE);
INSERT INTO testUUID (ID) VALUES (1), (2), (3), (4);
UPDATE testUUID SET new_ID = (SELECT hex(randomblob(16))), new_ID2 = REPLACE((SELECT hex(randomblob(16))), '-', ''), new_ID3 = (SELECT randomblob(16)) WHERE ID < 3;
SELECT new_ID, new_ID2 FROM testUUID;
