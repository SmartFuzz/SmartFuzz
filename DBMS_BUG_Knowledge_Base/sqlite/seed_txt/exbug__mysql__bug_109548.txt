CREATE TABLE product_v2 (value INTEGER PRIMARY KEY AUTOINCREMENT, code TEXT NOT NULL, name TEXT DEFAULT NULL, comment TEXT DEFAULT NULL, platform TEXT DEFAULT NULL);
CREATE TABLE temp_product (temp_id INTEGER PRIMARY KEY AUTOINCREMENT, temp_code TEXT, temp_name TEXT);
INSERT INTO product_v2 (code, name) VALUES ('test1', 'Test Product 1');
INSERT INTO temp_product (temp_code, temp_name) SELECT code, name FROM product_v2 WHERE code IS NOT NULL;
SELECT COUNT(*) FROM (SELECT value FROM product_v2 GROUP BY code, name, comment, platform);
