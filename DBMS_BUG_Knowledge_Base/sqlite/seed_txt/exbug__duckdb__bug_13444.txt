CREATE TABLE mixed_data(id INTEGER, str_val TEXT, num_val INTEGER, struct_val TEXT);
INSERT INTO mixed_data VALUES (1, 'test', 10, NULL), (2, NULL, 20, 'value|0'), (3, 'example', NULL, NULL|1);
SELECT str_val, substr(struct_val, instr(struct_val, '|') + 1) AS val, substr(struct_val, instr(struct_val, '|', instr(struct_val, '|') + 1) + 1) AS conflict FROM mixed_data WHERE struct_val IS NOT NULL;
CREATE TABLE nested_data(foo TEXT, bar INTEGER);
INSERT INTO nested_data VALUES ('a,b,c', 1), (NULL, 4), ('d,e', NULL);
