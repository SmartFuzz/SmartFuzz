CREATE TABLE c(id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK (age >= 0));
INSERT INTO c VALUES(1, 'Alice', 30);
INSERT INTO c VALUES(2, 'Bob', NULL);
CREATE VIEW d AS SELECT ROWID AS rn, name FROM c ORDER BY id;
SELECT * FROM d WHERE rn = 1;
