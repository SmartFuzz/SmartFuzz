DROP TABLE IF EXISTS a;
DROP TABLE IF EXISTS b;
CREATE TABLE IF NOT EXISTS a (c1 integer NOT NULL, c2 text, c3 integer DEFAULT 1);
CREATE TABLE IF NOT EXISTS b (c1 integer NOT NULL, c2 text, c3 text);
INSERT INTO a (c1, c2, c3) VALUES (1, 'text_1', NULL), (2, 'text_2', 1), (3, 'text_3', 0);
SELECT * FROM a LEFT JOIN b ON a.c1 = b.c1 WHERE a.c1 = 3;
