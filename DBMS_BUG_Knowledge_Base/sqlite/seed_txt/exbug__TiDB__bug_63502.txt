CREATE TABLE orders (order_id INTEGER PRIMARY KEY AUTOINCREMENT, customer_id INTEGER, order_date DATETIME, amount REAL, INDEX idx_customer_id (customer_id));
CREATE TABLE products (product_id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, price REAL, stock INTEGER);
INSERT INTO customers (name, custinfo) VALUES ('Alice', json_object('zipcode', '12345'));
INSERT INTO orders (customer_id, order_date, amount) VALUES (1, CURRENT_TIMESTAMP, 100.00);
SELECT * FROM orders WHERE customer_id = (SELECT customer_id FROM customers WHERE name = 'Alice');
