CREATE TABLE academic_structure (id INTEGER PRIMARY KEY, name TEXT NOT NULL, type TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO academic_structure (id, name, type) VALUES (1, 'Australian National University', NULL);
SELECT * FROM academic_structure WHERE type IS NULL;
UPDATE academic_structure SET type = 'University' WHERE id = 1;
SELECT name, COALESCE(type, 'Unknown') AS type FROM academic_structure WHERE id = 1;
