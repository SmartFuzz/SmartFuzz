CREATE TABLE test_intervals (id INTEGER PRIMARY KEY, duration INTEGER);
INSERT INTO test_intervals (id, duration) VALUES (1, 2);
SELECT duration * 2 FROM test_intervals WHERE id = 1;
SELECT 2 + 2 AS total_duration;
SELECT (SELECT duration FROM test_intervals WHERE id = 1) * 2 AS multiplied_duration;
