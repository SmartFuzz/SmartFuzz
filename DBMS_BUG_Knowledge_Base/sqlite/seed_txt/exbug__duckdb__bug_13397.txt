CREATE TABLE test_table (id INTEGER, value INTEGER, description TEXT);
INSERT INTO test_table (id, value, description) VALUES (1, 0, 'zero'), (2, 1, 'one'), (3, 2, 'two');
SELECT SUM(CASE WHEN value = 0 THEN 1 ELSE 0 END) AS count_zeros FROM test_table;
SELECT SUM(CASE WHEN value = 1 THEN 1 ELSE 0 END) AS count_ones FROM test_table WHERE value IS NOT NULL;
SELECT SUM(CASE WHEN x = 0 THEN 1 ELSE 0 END) AS count_zeros FROM (SELECT value AS x FROM (VALUES (NULL), (0), (1), (2))) AS temp;
