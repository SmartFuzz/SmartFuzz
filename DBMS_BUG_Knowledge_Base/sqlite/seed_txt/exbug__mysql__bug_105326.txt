CREATE TABLE t (id INTEGER NOT NULL, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE 't#' (id INTEGER NOT NULL, description TEXT, amount REAL DEFAULT 0.00);
INSERT INTO t (id, name) VALUES (1, 'Sample');
SELECT * FROM t WHERE id = 1;
SELECT * FROM 't#' WHERE id = 1;
