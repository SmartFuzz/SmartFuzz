CREATE TABLE Course(id INTEGER PRIMARY KEY, title TEXT NOT NULL, credits INTEGER CHECK (credits > 0));
INSERT INTO Course VALUES (1, 'Mathematics', 3), (2, 'Physics', 4), (3, 'Chemistry', 3);
CREATE TABLE Enrollment(student_id INTEGER, course_id INTEGER, enrollment_date TEXT DEFAULT (DATE('now')), FOREIGN KEY(student_id) REFERENCES Student(id), FOREIGN KEY(course_id) REFERENCES Course(id));
INSERT INTO Enrollment VALUES (0, 1, '2023-09-01'), (1, 2, '2023-09-02'), (2, 3, '2023-09-03');
SELECT e.student_id, e.course_id, c.title FROM Enrollment e JOIN Course c ON e.course_id = c.id WHERE e.student_id IS NOT NULL;
