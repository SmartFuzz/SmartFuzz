CREATE TABLE joinit (i INTEGER PRIMARY KEY AUTOINCREMENT, s TEXT DEFAULT NULL, t TIME NOT NULL, g INTEGER NOT NULL);
INSERT INTO joinit (s, t, g) VALUES (hex(randomblob(16)), TIME('now'), CAST((1 + ABS(random() % 60)) AS INTEGER));
INSERT INTO joinit (s, t, g) SELECT hex(randomblob(16)), TIME('now'), CAST((1 + ABS(random() % 60)) AS INTEGER) FROM joinit WHERE g < 30;
SELECT COUNT(*) FROM joinit WHERE g IS NULL OR g > 50;
