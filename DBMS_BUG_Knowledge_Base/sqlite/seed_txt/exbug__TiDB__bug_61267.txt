CREATE TABLE dp_organisation_extended (OrganisationId INTEGER PRIMARY KEY, ParentId INTEGER, Name TEXT, Active INTEGER DEFAULT 1);
INSERT INTO dp_organisation_extended VALUES (1, NULL, 'Org A', 1), (2, 1, 'Org B', 1), (3, 2, 'Org C', 0);
CREATE TABLE dp_member (MemberId INTEGER PRIMARY KEY, OrganisationId INTEGER, JoinDate TEXT, FOREIGN KEY (OrganisationId) REFERENCES dp_organisation_extended(OrganisationId));
INSERT INTO dp_member VALUES (100, 1, '2023-01-01'), (200, 2, '2023-02-01'), (300, 3, '2023-03-01');
SELECT * FROM dp_member m WHERE m.OrganisationId IN (SELECT OrganisationId FROM dp_organisation_extended WHERE Active = 1);
