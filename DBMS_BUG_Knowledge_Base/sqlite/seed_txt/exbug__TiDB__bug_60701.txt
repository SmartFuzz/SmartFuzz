CREATE TABLE t2(id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK (age >= 0));
CREATE TABLE t3(id TEXT NOT NULL, description TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2(id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', NULL);
INSERT INTO t3(id, description) VALUES ('1', 'First entry'), ('2', 'Second entry');
EXPLAIN QUERY PLAN SELECT * FROM t2 WHERE age IN (SELECT CAST(id AS INTEGER) FROM t3);
