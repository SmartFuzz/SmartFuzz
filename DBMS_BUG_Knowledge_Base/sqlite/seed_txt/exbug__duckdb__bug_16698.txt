CREATE TABLE users (user_id INTEGER PRIMARY KEY, username TEXT, age INTEGER);
INSERT INTO users VALUES (1, 'Alice', 30);
INSERT INTO users (user_id, username) VALUES (1, 'Bob') ON CONFLICT(user_id) DO UPDATE SET username = 'Bob';
SELECT * FROM users;
INSERT INTO users (user_id, age) VALUES (2, NULL) ON CONFLICT(user_id) DO UPDATE SET age = NULL;
