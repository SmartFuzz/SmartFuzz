CREATE TABLE test_t2(c1 INT, c2 TEXT, c3 TEXT, c4 REAL);
INSERT INTO test_t2 VALUES(1, 'A', '2023-01-01', 1.1), (2, 'B', NULL, 2.2), (3, 'C', '2023-03-03', 3.3), (4, 'D', '2023-04-04', NULL);
CREATE TABLE test_t3(c1 INT PRIMARY KEY, c2 INT NOT NULL);
INSERT INTO test_t3 SELECT c1, COUNT(*) FROM (SELECT c1, c2 FROM test_t2) GROUP BY c1;
SELECT c1, ROW_NUMBER() OVER() FROM (SELECT c1, c2 FROM test_t2 WHERE c3 IS NOT NULL) AS subquery;
