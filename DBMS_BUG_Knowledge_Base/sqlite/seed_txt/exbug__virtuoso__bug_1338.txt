CREATE TABLE test_graphs (id INTEGER PRIMARY KEY, graph_name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_graphs (id, graph_name) VALUES (1, 'graph1'), (2, 'graph2'), (3, 'graph3');
DELETE FROM rdf_quad WHERE g IN (SELECT graph_name FROM test_graphs WHERE id IN (1, 2));
DELETE FROM rdf_quad WHERE g IN (SELECT graph_name FROM test_graphs WHERE id IN (3));
VACUUM;
