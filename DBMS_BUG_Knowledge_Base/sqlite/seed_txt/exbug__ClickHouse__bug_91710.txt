DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INTEGER, name TEXT, value REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table SELECT number, substr(random(), 1, 5), random() FROM (SELECT rowid AS number FROM sqlite_sequence LIMIT 100);
SELECT * FROM test_table ORDER BY name, length(name);
DROP TABLE test_table;
