DROP TABLE IF EXISTS job_sample;
CREATE TABLE job_sample (id INTEGER, posted_at DATE, discovered_at DATETIME, url TEXT, title TEXT, company_name TEXT, description TEXT);
INSERT INTO job_sample (id, posted_at, discovered_at, url, title, company_name, description) VALUES (1, '2025-01-01', '2025-01-01 12:00:00', 'http://example.com', 'Job Title', 'Company Name', 'Description with Python');
ALTER TABLE job_sample ADD COLUMN description_matches_python INTEGER;
UPDATE job_sample SET description_matches_python = CASE WHEN description LIKE '%Python%' THEN 1 ELSE 0 END;
