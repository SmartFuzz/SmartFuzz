CREATE TABLE ARRAY(id INTEGER NOT NULL, value TEXT DEFAULT NULL, PRIMARY KEY (id));
CREATE TABLE FULL(id INTEGER NOT NULL, description TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
CREATE TABLE INTERSECT(id INTEGER NOT NULL, amount REAL CHECK (amount >= 0), PRIMARY KEY (id));
INSERT INTO FULL (id, description) VALUES (1, 'Test entry for FULL');
SELECT name FROM sqlite_master WHERE type='table';
