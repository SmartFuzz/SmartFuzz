CREATE TABLE test_identity (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, value INTEGER CHECK (value >= 0));
INSERT INTO test_identity (name, value) VALUES ('Test1', 10), ('Test2', NULL), ('Test3', -5);
SELECT id, name, value, CASE WHEN id > 0 THEN 1 ELSE 0 END AS is_positive FROM test_identity;
CREATE TABLE related_data (rid INTEGER PRIMARY KEY AUTOINCREMENT, test_id INTEGER, description TEXT, FOREIGN KEY(test_id) REFERENCES test_identity(id));
INSERT INTO related_data (test_id, description) SELECT id, 'Description ' || id FROM test_identity WHERE value IS NOT NULL;
