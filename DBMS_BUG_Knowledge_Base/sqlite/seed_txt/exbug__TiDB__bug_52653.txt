CREATE TABLE test_table ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, status INTEGER DEFAULT 0);
INSERT INTO test_table (name, status) VALUES ('Test Name', 1);
CREATE TABLE related_table ( related_id INTEGER PRIMARY KEY AUTOINCREMENT, test_id INTEGER NOT NULL, description TEXT, FOREIGN KEY (test_id) REFERENCES test_table(id) ON DELETE CASCADE);
INSERT INTO related_table (test_id, description) VALUES (1, 'Related Description');
SELECT t.id, t.name, r.description FROM test_table t LEFT JOIN related_table r ON t.id = r.test_id WHERE t.status = 1 AND r.description IS NOT NULL ORDER BY t.created_at DESC LIMIT 5;
