CREATE TABLE test (a INTEGER, b TEXT, c DATE DEFAULT NULL);
ALTER TABLE test ADD COLUMN d REAL;
INSERT INTO test (a, b) VALUES (1, 'test1'), (2, 'test2');
UPDATE test SET c = DATE('now') WHERE a = 1;
SELECT a, b, COALESCE(c, 'No Date') AS date_info FROM test;
