CREATE TABLE test_table (id INTEGER PRIMARY KEY AUTOINCREMENT, value INTEGER, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (value, name) VALUES (1, 'Test1'), (2, 'Test2'), (3, NULL);
SELECT * FROM test_table WHERE value IS NOT NULL;
SELECT * FROM test_table WHERE created_at > DATETIME('now', '-1 day');
SELECT * FROM (SELECT 0 AS num UNION ALL SELECT num + 1 FROM (SELECT 0 AS num LIMIT 10000) AS t1);
