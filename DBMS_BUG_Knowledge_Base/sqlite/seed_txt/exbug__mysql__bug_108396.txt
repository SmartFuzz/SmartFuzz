CREATE TABLE e2 (x INTEGER NOT NULL, y TEXT, z TEXT, w REAL DEFAULT 0.0, PRIMARY KEY (x));
INSERT INTO e2 (x, y, z) VALUES (1, 'test', '2023-01-01');
ALTER TABLE e2 ADD COLUMN v DATETIME DEFAULT CURRENT_TIMESTAMP;
UPDATE e2 SET y = NULL WHERE x = 1;
SELECT * FROM e2 WHERE w > 0.0 OR z IS NULL;
