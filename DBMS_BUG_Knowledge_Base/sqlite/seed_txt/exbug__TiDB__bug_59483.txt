DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (id INTEGER PRIMARY KEY, value TEXT, status INTEGER);
INSERT INTO t2 (id, value, status) VALUES (1, 'test', 1), (2, 'example', 0);
SELECT id FROM t2 WHERE CASE WHEN status = 1 THEN id ELSE NULL END IS NOT NULL;
SELECT value FROM t2 WHERE CASE WHEN id < 3 THEN id ELSE NULL END IS NOT NULL;
