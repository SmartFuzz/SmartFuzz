CREATE TABLE test_table (id INTEGER PRIMARY KEY, value TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, value) VALUES (1, 'test1'), (2, 'test2'), (3, NULL);
SELECT CASE WHEN (SELECT COUNT(*) FROM test_table WHERE value IS NOT NULL) > (SELECT MAX(id) FROM test_table) THEN 'valid' ELSE 'invalid' END AS status;
UPDATE test_table SET value = 'updated' WHERE id = 2;
SELECT value FROM test_table WHERE created_at > (SELECT MIN(created_at) FROM test_table WHERE value IS NULL);
