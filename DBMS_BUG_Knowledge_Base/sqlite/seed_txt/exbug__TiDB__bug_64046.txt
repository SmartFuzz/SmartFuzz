CREATE TABLE t4 (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, value REAL NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t4 (name, value) VALUES ('test1', 123.45);
INSERT INTO t4 (name, value) VALUES ('test2', NULL);
SELECT id, name, ROW_NUMBER() OVER (ORDER BY created_at DESC) AS row_num FROM (SELECT id, name, created_at FROM t4 WHERE value IS NOT NULL) AS sub;
SELECT id, name, ROW_NUMBER() OVER (ORDER BY created_at ASC) AS row_num FROM (SELECT id, name, value, created_at FROM t4) AS sub;
