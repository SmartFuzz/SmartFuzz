CREATE TABLE fuzz_test (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, value REAL NOT NULL);
CREATE TRIGGER fuzz_test_BEFORE_INSERT BEFORE INSERT ON fuzz_test FOR EACH ROW BEGIN SELECT CASE WHEN NEW.value IS NULL THEN 0 ELSE NEW.value END INTO NEW.value;
END;
INSERT INTO fuzz_test(name, value) VALUES ('test', NULL);
SELECT * FROM fuzz_test;
UPDATE fuzz_test SET value = 10.50 WHERE name = 'test';
