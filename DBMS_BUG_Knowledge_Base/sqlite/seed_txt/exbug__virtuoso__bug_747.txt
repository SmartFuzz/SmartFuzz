CREATE TABLE language_tags (id INTEGER PRIMARY KEY, tag TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO language_tags (id, tag) VALUES (1, 'en'), (2, 'fr'), (3, 'de'), (4, NULL), (5, 'es');
UPDATE language_tags SET tag = 'it' WHERE id = 3;
SELECT DISTINCT tag, LENGTH(tag) AS lang FROM language_tags WHERE tag IS NOT NULL;
DELETE FROM language_tags WHERE tag IS NULL;
