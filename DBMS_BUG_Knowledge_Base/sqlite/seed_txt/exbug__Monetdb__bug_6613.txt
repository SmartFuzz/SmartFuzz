DROP TABLE IF EXISTS segments;
CREATE TABLE segments (meter INTEGER NOT NULL, distance INTEGER CHECK (distance >= 0), speed REAL DEFAULT 0.0);
INSERT INTO segments (meter, distance, speed) VALUES (1, 1, 1), (9, 9, 9), (NULL, 5, 3);
SELECT meter, distance, speed FROM segments;
SELECT meter, distance, speed FROM segments WHERE speed IS NOT NULL;
