CREATE TABLE test_intervals (id INTEGER PRIMARY KEY, duration TEXT, description TEXT);
INSERT INTO test_intervals (id, duration, description) VALUES (1, '1 year', 'One year interval');
INSERT INTO test_intervals (id, duration, description) VALUES (2, NULL, 'Null interval');
SELECT id, duration FROM test_intervals WHERE duration IS NOT NULL;
SELECT '0 years' AS interval_test;
