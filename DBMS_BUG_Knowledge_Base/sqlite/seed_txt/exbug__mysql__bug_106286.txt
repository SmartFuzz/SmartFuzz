CREATE TABLE test (id INTEGER PRIMARY KEY, ts TEXT);
INSERT INTO test (id, ts) VALUES (1, '2023-01-01 00:00:00');
CREATE TRIGGER tr_bi_check_uniqueness_with_nulls BEFORE INSERT ON test FOR EACH ROW BEGIN SELECT CASE WHEN (SELECT 1 FROM test WHERE test.id = NEW.id AND test.ts = NEW.ts) IS NOT NULL THEN RAISE(ABORT, 'Duplicated values not allowed.');
END;
END;
INSERT INTO test (id, ts) VALUES (1, NULL);
