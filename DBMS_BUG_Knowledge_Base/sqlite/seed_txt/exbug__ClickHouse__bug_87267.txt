DROP TABLE IF EXISTS test_db;
CREATE TABLE test_db (id INTEGER, name TEXT, created_at DATETIME);
CREATE VIEW test_view1 AS SELECT id, name FROM test_db WHERE created_at > datetime('now', '-1 day');
SELECT * FROM test_view1 WHERE id = NULL;
