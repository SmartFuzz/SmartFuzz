CREATE TABLE sbtest2 ( id INTEGER PRIMARY KEY AUTOINCREMENT, k INTEGER NOT NULL DEFAULT 0, c TEXT NOT NULL DEFAULT '', pad TEXT NOT NULL DEFAULT '', ec1 TEXT DEFAULT NULL );
INSERT INTO sbtest2 (k, c, pad, ec1) VALUES (1, 'test', 'padding', NULL);
UPDATE sbtest2 SET ec1 = 'updated' WHERE id = 1;
SELECT * FROM sbtest2 WHERE k IN (SELECT k FROM sbtest2 WHERE ec1 IS NOT NULL);
VACUUM;
