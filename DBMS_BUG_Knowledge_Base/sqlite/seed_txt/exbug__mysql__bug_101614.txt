CREATE TABLE test_table (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);
SELECT id, name, age FROM test_table ORDER BY age;
SELECT id, name, age FROM test_table ORDER BY created_at;
SELECT id, name, age FROM test_table ORDER BY name;
