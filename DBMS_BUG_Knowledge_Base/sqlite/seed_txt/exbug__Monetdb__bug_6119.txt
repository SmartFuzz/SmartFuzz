DROP TABLE IF EXISTS subt;
DROP TABLE IF EXISTS t;
CREATE TABLE subt (A1 INTEGER NOT NULL, A2 TEXT);
CREATE TABLE t (B INTEGER, C REAL DEFAULT 0.0);
INSERT INTO subt (A1, A2) VALUES (1, 'test'), (2, 'example'), (NULL, 'null_value');
SELECT (B IN (SELECT A1 FROM subt WHERE A2 IS NOT NULL)) FROM t;
