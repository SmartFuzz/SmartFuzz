CREATE TABLE t_fts_extended (id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL, content TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t_fts_extended (title, content) VALUES ('Example Title 1', 'This is some example content for testing purposes.');
INSERT INTO t_fts_extended (title, content) SELECT 'Example Title ' || id, 'Content for ID ' || id FROM (SELECT 1 AS id UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5);
SELECT id FROM t_fts_extended WHERE title LIKE '%example%' LIMIT 10;
DELETE FROM t_fts_extended WHERE id IN (SELECT id FROM (SELECT id FROM t_fts_extended ORDER BY created_at DESC LIMIT 5));
