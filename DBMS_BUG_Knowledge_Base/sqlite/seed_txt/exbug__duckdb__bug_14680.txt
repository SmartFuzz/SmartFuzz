CREATE TABLE test_table (id INTEGER, name TEXT, value BLOB);
INSERT INTO test_table VALUES (1, 'test' || CHAR(0), NULL);
CREATE TABLE another_table AS SELECT id, name FROM test_table WHERE value IS NULL;
.mode tabs;
.headers on;
.output output.tsv;
