CREATE TABLE t2 (id INTEGER, name TEXT, created_at DATETIME);
INSERT INTO t2 VALUES (1, 'test', CURRENT_TIMESTAMP);
SELECT * FROM t2 WHERE created_at >= '2023-01-01' OR id < 2;
SELECT * FROM t2 WHERE created_at >= 'abc' OR id < 2;
