CREATE TABLE test_data (id INTEGER, values TEXT);
INSERT INTO test_data VALUES (1, '1,2,3'), (2, '4,5,NULL'), (3, '6,7,8');
SELECT id, SUM(CAST(value AS INTEGER)) FROM test_data, json_each(values) WHERE value IS NOT NULL GROUP BY id;
CREATE TEMPORARY TABLE custom_sum AS SELECT id, AVG(CAST(value AS INTEGER)) AS avg_value FROM test_data, json_each(values) WHERE value IS NOT NULL GROUP BY id;
SELECT id, avg_value FROM custom_sum;
