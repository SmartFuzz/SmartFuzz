CREATE TABLE test_table (id INTEGER PRIMARY KEY, value TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, value) VALUES (1, 'test1'), (2, 'test2'), (3, NULL);
CREATE VIEW test_view AS SELECT id, COUNT(value) AS distinct_count FROM (SELECT DISTINCT id, value FROM test_table) GROUP BY id;
SELECT * FROM test_view;
