CREATE TABLE t2(id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER DEFAULT NULL);
UPDATE t2 SET age = 18 WHERE age IS NULL;
INSERT INTO t2(name, age) VALUES ('Alice', NULL);
SELECT * FROM t2 WHERE age IS NULL;
CREATE TABLE t2_temp(id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER DEFAULT NULL);
INSERT INTO t2_temp SELECT id, name, age FROM t2;
