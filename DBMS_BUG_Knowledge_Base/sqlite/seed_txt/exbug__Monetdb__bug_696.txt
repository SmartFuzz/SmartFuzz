CREATE TABLE users (user_id INTEGER PRIMARY KEY, username TEXT NOT NULL);
CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE);
INSERT INTO users (user_id, username) VALUES (1, 'alice');
INSERT INTO orders (order_id, user_id) VALUES (101, 1);
DELETE FROM users WHERE user_id = 1;
SELECT * FROM orders;
