CREATE TABLE t2(a TEXT NOT NULL, b INTEGER DEFAULT 0);
INSERT INTO t2 VALUES('123-456', 1);
CREATE TABLE t3(a TEXT, b REAL);
INSERT INTO t3 VALUES('789.01', NULL);
WITH test AS (SELECT CASE WHEN a GLOB '[0-9]*.*[0-9]*' THEN CASE WHEN CAST(a AS INTEGER) < 800 THEN 'valid' END END flg FROM t2) SELECT MAX(flg) FROM test;
