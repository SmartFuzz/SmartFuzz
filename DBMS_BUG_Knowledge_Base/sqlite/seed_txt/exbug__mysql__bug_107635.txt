CREATE TABLE test_events (id INTEGER PRIMARY KEY AUTOINCREMENT, event_name TEXT NOT NULL, event_time DATETIME NOT NULL, is_active BOOLEAN DEFAULT 1);
INSERT INTO test_events (event_name, event_time) VALUES ('Test Event 1', DATETIME('now', '+1 second'));
INSERT INTO test_events (event_name, event_time) VALUES ('Scheduled Event', DATETIME('now'));
SELECT * FROM test_events;
