CREATE TABLE users(id INTEGER PRIMARY KEY, name TEXT, age INTEGER);
INSERT INTO users VALUES (1, 'Alice', 30), (2, 'Bob', NULL);
UPDATE users SET age = 25 WHERE id = (SELECT 1);
UPDATE users SET name = 'Charlie' WHERE id = (SELECT 1);
