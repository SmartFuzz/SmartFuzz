CREATE TABLE departments (dept_id INTEGER PRIMARY KEY, dept_name TEXT NOT NULL);
INSERT INTO departments (dept_id, dept_name) VALUES (1, 'HR');
CREATE TABLE employees (id INTEGER PRIMARY KEY, name TEXT, dept_id INTEGER, FOREIGN KEY (dept_id) REFERENCES departments(dept_id));
INSERT INTO employees (id, name, dept_id) VALUES (1, 'Clark', 1);
SELECT * FROM employees;
