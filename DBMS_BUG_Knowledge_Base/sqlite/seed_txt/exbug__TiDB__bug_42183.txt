CREATE TABLE t2 (id INTEGER PRIMARY KEY, name TEXT, age INTEGER CHECK (age >= 0));
INSERT INTO t2 (id, name, age) VALUES (25, 'Alice', 30);
CREATE TABLE t2_temp AS SELECT id, name, age FROM t2 WHERE id < 20;
INSERT INTO t2_temp (id, name, age) SELECT id, name, age FROM t2 WHERE id >= 20;
DROP TABLE t2;
ALTER TABLE t2_temp RENAME TO t2;
