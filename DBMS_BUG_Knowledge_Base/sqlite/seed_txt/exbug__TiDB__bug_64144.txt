CREATE TABLE test_b (id INTEGER PRIMARY KEY, description TEXT NOT NULL, age INTEGER CHECK (age >= 0), UNIQUE (description));
INSERT INTO test_b VALUES (1, 'Sample�Text');
SELECT LENGTH(description) FROM test_b WHERE id = 1;
SELECT * FROM test_b WHERE description = (SELECT description FROM test_b WHERE id = 1);
DELETE FROM test_b WHERE id = 1;
