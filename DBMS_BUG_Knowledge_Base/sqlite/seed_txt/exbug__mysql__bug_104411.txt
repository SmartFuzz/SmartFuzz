CREATE TABLE Accounts (id INTEGER PRIMARY KEY, name TEXT NOT NULL);
INSERT INTO Accounts (id, name) VALUES (1, 'Alice'), (2, 'Bob');
CREATE TABLE Logins (id INTEGER, login_date TEXT NOT NULL, FOREIGN KEY (id) REFERENCES Accounts(id));
INSERT INTO Logins (id, login_date) VALUES (1, '2021-01-01'), (1, '2021-01-02'), (1, '2021-01-03'), (1, '2021-01-04'), (1, '2021-01-05'), (2, '2021-01-01'), (2, '2021-01-02');
SELECT DISTINCT L1.id, A.name FROM Logins L1 INNER JOIN Logins L2 ON L1.id = L2.id AND (julianday(L2.login_date) - julianday(L1.login_date)) BETWEEN 0 AND 4 INNER JOIN Accounts A ON L1.id = A.id GROUP BY L1.id, A.name HAVING COUNT(DISTINCT L2.login_date) = 5;
