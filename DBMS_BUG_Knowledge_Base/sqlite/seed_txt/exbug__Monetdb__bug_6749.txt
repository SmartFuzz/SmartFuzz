CREATE TABLE test_table (id INTEGER, name TEXT, age INTEGER CHECK (age >= 0));
INSERT INTO test_table (id, name, age) VALUES (1, 'Alice', 30);
CREATE UNIQUE INDEX idx_unique_name ON test_table (name);
SELECT * FROM test_table WHERE age IS NULL OR age < 0;
CREATE TABLE temp_table AS SELECT id, name FROM test_table;
DROP TABLE test_table;
