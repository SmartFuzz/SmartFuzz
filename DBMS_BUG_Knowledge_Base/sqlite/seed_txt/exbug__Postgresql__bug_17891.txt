CREATE TABLE za_ifs_data_t2 (b TEXT NOT NULL, c DATE DEFAULT (DATETIME('now', 'localtime')));
INSERT INTO za_ifs_data_t2 (b) VALUES ('test'), (NULL), (''), (REPLACE(REPLACE('a', 'a', 'b'), 'b', 'b'));
CREATE VIEW zp_ifs_views_t2_view AS SELECT b, c FROM za_ifs_data_t2 WHERE c IS NOT NULL;
SELECT * FROM zp_ifs_views_t2_view WHERE b IS NOT NULL OR b = '';
