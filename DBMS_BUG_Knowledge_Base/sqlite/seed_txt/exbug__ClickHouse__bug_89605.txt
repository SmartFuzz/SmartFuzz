DROP TABLE IF EXISTS test_logs;
CREATE TABLE test_logs (id INTEGER PRIMARY KEY, message TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_logs (id, message) VALUES (1, 'zymosimeter'), (2, 'example'), (3, NULL);
SELECT * FROM test_logs WHERE message LIKE '%zymosimeter%';
SELECT COUNT(*) FROM test_logs WHERE created_at > datetime('now', '-1 day');
