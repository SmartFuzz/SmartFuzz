CREATE TABLE partitioned_table (id INTEGER PRIMARY KEY AUTOINCREMENT, value INTEGER, description TEXT);
CREATE TABLE partitioned_table_1 AS SELECT * FROM partitioned_table WHERE 0;
-- Placeholder for partitioned table;
CREATE INDEX idx_value ON partitioned_table(value);
INSERT INTO partitioned_table (value, description) VALUES (5, NULL), (NULL, 'Test'), (15, 'Boundary Test');
SELECT * FROM partitioned_table WHERE value IS NULL OR value < 10;
