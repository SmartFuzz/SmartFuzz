CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, value REAL);
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.5), (2, 'test2', NULL), (3, 'test3', 20.0);
CREATE TABLE another_table (ref_id INTEGER, description TEXT, amount INTEGER);
INSERT INTO another_table (ref_id, description, amount) SELECT id, name || ' description', CAST(value AS INTEGER) FROM test_table WHERE value IS NOT NULL;
SELECT * FROM another_table WHERE amount < 100;
