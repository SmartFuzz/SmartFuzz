DROP TABLE IF EXISTS t;
CREATE TABLE t (id INTEGER PRIMARY KEY, name TEXT, status TEXT CHECK(status IN ('active', 'inactive', 'pending')));
INSERT INTO t (name, status) VALUES ('Alice', 'active'), ('Bob', 'inactive'), ('Charlie', 'pending');
SELECT COUNT(*) FROM t WHERE status = 'active';
ALTER TABLE t RENAME TO t_old;
CREATE TABLE t (id INTEGER PRIMARY KEY, name TEXT, status TEXT);
