CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, age INTEGER DEFAULT NULL);
INSERT INTO users(name, age) VALUES ('Alice', 30);
CREATE TABLE orders (order_id INTEGER NOT NULL, user_id INTEGER NOT NULL, amount REAL NOT NULL, UNIQUE(order_id, user_id));
INSERT INTO orders(order_id, user_id, amount) VALUES (1, 1, 99.99);
DELETE FROM orders WHERE order_id = 1 AND user_id = 1;
