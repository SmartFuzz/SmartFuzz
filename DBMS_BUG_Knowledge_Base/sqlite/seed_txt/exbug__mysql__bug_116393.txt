CREATE TABLE test_hashing (id INTEGER PRIMARY KEY AUTOINCREMENT, data TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_hashing (data) VALUES (||'Test'||hex(md5('Sample Data'))), (NULL), (||'Boundary'||hex(sha256('Edge Case')));
SELECT id, hex(md5(data)) AS md5_hash FROM test_hashing WHERE data IS NOT NULL;
SELECT COUNT(*) FROM test_hashing WHERE sha256(data) IS NOT NULL;
SELECT (SELECT COUNT(*) FROM test_hashing) AS total_rows, (SELECT hex(sha256(data || 'Extra')) FROM test_hashing LIMIT 1) AS example_hash;
