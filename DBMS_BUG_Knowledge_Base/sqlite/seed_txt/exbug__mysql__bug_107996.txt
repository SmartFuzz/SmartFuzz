CREATE TABLE t2 (id INTEGER PRIMARY KEY, geom BLOB DEFAULT NULL);
INSERT INTO t2 (id, geom) VALUES (1, NULL);
SELECT id, geom FROM t2 WHERE geom IS NOT NULL;
CREATE TABLE t3 AS SELECT 'POINT(1 1)' AS geom;
DROP TABLE t2;
