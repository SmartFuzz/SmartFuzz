CREATE TABLE b (id INTEGER PRIMARY KEY AUTOINCREMENT, f TEXT NOT NULL);
CREATE TABLE a (id INTEGER PRIMARY KEY AUTOINCREMENT, b_id INTEGER NOT NULL, description TEXT, FOREIGN KEY (b_id) REFERENCES b (id) ON DELETE CASCADE);
INSERT INTO b (id, f) VALUES (1, 'value1');
INSERT INTO a (id, b_id, description) VALUES (1, 1, 'first entry');
UPDATE b SET id = 2, f = 'updated_value' WHERE id = 1;
SELECT * FROM a;
