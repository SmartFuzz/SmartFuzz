CREATE TABLE test_table (id INTEGER PRIMARY KEY, flag BOOLEAN, description TEXT);
INSERT INTO test_table (id, flag, description) VALUES (1, 1, 'First entry'), (2, 0, 'Second entry'), (3, NULL, 'Third entry');
CREATE VIEW evaluate_flag AS SELECT id, CASE WHEN flag = 1 THEN 'true' WHEN flag = 0 THEN 'false' ELSE 'null' END AS evaluation FROM test_table;
SELECT * FROM evaluate_flag;
SELECT CASE WHEN flag IS NOT NULL AND flag = 0 THEN 'false' ELSE 'true' END AS adjusted_evaluation FROM test_table;
