CREATE TABLE IF NOT EXISTS mixed_types_table (id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, is_active INTEGER);
INSERT INTO mixed_types_table (id, name, is_active) VALUES (1, 'Test User', 1), (2, 'Another User', 0);
SELECT name, (CASE WHEN is_active = 1 THEN 'Active' ELSE 'Inactive' END) AS status FROM mixed_types_table WHERE id = 1;
SELECT name, (CASE WHEN is_active = 1 THEN 'Active' ELSE 'Inactive' END) AS status FROM mixed_types_table WHERE id = 2;
