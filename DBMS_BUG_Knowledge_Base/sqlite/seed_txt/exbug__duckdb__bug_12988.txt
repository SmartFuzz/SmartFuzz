CREATE TABLE employees (id INTEGER PRIMARY KEY, name TEXT, salary REAL, department_id INTEGER);
CREATE TABLE departments (id INTEGER PRIMARY KEY, name TEXT NOT NULL);
INSERT INTO employees VALUES (1, 'John Doe', 50000.00, 1), (2, 'Jane Smith', 60000.00, 2), (3, 'Alice Johnson', 55000.00, 1);
INSERT INTO departments VALUES (1, 'Engineering'), (2, 'Marketing');
WITH ranked_employees AS (SELECT *, ROW_NUMBER() OVER (ORDER BY salary) AS salary_rank FROM employees) SELECT * FROM ranked_employees JOIN departments ON ranked_employees.department_id = departments.id;
