CREATE TABLE bmsql_product ( p_id INTEGER NOT NULL, p_name TEXT NOT NULL, p_price REAL NOT NULL, p_category TEXT DEFAULT NULL, PRIMARY KEY (p_id) );
INSERT INTO bmsql_product(p_id, p_name, p_price, p_category) VALUES(1, 'Product A', 19.99, 'Category 1');
INSERT INTO bmsql_product(p_id, p_name, p_price, p_category) VALUES(2, 'Product B', 29.99, NULL);
SELECT d_w_id, SUM(d_ytd) AS total_ytd FROM bmsql_district GROUP BY d_w_id HAVING total_ytd > 0;
SELECT * FROM (SELECT p_id, p_name FROM bmsql_product) AS p LEFT JOIN (SELECT d_w_id FROM bmsql_district) AS d ON p.p_id = d.d_w_id WHERE d.d_w_id IS NULL;
