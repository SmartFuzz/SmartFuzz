CREATE TABLE test_partitioned (id INTEGER PRIMARY KEY, name TEXT);
CREATE TABLE test_orphaned (id INTEGER NOT NULL, data TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, UNIQUE(id));
INSERT INTO test_partitioned (id, name) VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
INSERT INTO test_orphaned (id, data) VALUES (1, 'orphaned_data1'), (2, 'orphaned_data2');
SELECT A.schema_name, A.table_name FROM (SELECT DISTINCT REPLACE(SUBSTR(NAME, 1, INSTR(NAME, '/') - 1), '@002d', '-') AS schema_name, REPLACE(SUBSTR(NAME, INSTR(NAME, '/') + 1), '@002d', '-') AS table_name FROM sqlite_master WHERE type='table') A LEFT JOIN sqlite_master I ON A.table_name = I.name WHERE I.name IS NULL;
