CREATE VIRTUAL TABLE fts USING fts4(content TEXT);
CREATE TABLE t1(x TEXT PRIMARY KEY, y INTEGER NOT NULL DEFAULT 0);
INSERT INTO t1(x, y) VALUES ('hello world', 1);
INSERT INTO fts(content) VALUES ('hello world');
SELECT DISTINCT fts.rowid, t1.x FROM fts JOIN t1 ON content = x WHERE fts MATCH 'hello world' ORDER BY y;
