CREATE TABLE test_json_data (id INTEGER, data TEXT);
INSERT INTO test_json_data VALUES (1, '{"a":1}'), (2, '[2]');
SELECT MAX(data) AS max_value FROM test_json_data;
SELECT MAX(d) FROM (SELECT json_each.value AS d FROM test_json_data, json_each(data));
SELECT (SELECT MAX(value) FROM (VALUES ('{"b":2}'), ('{"a":1}'))) AS max_json;
