DROP TABLE IF EXISTS xyz;
CREATE TABLE xyz (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO xyz (name, age) VALUES ('Alice', 30), ('Bob', NULL);
CREATE TEMP TABLE temp_xyz AS SELECT * FROM xyz WHERE age IS NOT NULL;
SELECT age FROM temp_xyz LIMIT 1;
ALTER TABLE xyz DROP COLUMN age;
