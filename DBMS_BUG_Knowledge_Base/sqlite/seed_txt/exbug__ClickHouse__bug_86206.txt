CREATE TABLE wikistat_iceberg_v2 (time DATETIME, project TEXT, subproject TEXT, path TEXT, hits INTEGER, created_at DATE DEFAULT (DATE('now')));
INSERT INTO wikistat_iceberg_v2 (time, project, subproject, path, hits, created_at) VALUES (DATETIME('now'), 'project1', 'subproject1', '/path/to/resource', 100, DATE('now'));
CREATE TABLE wikistat_temp (temp_time DATETIME, temp_hits INTEGER);
INSERT INTO wikistat_temp (temp_time, temp_hits) SELECT time, hits FROM wikistat_iceberg_v2 WHERE hits > 50;
SELECT temp_time, SUM(temp_hits) AS total_hits FROM wikistat_temp GROUP BY temp_time HAVING total_hits > 100;
