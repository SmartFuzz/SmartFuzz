CREATE TABLE nextprot_isoform_bases (id TEXT NOT NULL, entry TEXT NOT NULL, PRIMARY KEY(id));
CREATE TABLE nextprot_isoform_cellular_components (isoform TEXT NOT NULL, annotation TEXT, PRIMARY KEY(isoform, annotation));
INSERT INTO nextprot_isoform_bases (id, entry) VALUES ('isoform1', 'entry1'), ('isoform2', 'entry2');
INSERT INTO nextprot_isoform_cellular_components (isoform, annotation) VALUES ('isoform1', 'annotation1'), ('isoform2', NULL);
SELECT * FROM nextprot_isoform_bases b JOIN nextprot_isoform_cellular_components c ON b.id = c.isoform WHERE c.annotation IS NOT NULL;
