CREATE TABLE bar (id INTEGER PRIMARY KEY, value TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO bar (id, value) VALUES (1, 'test1'), (2, 'test2'), (3, NULL);
SELECT value FROM bar WHERE id IN (SELECT id FROM bar WHERE value IS NOT NULL UNION ALL SELECT id FROM bar WHERE created_at < DATETIME('now', '-1 day'));
CREATE TABLE baz (x INTEGER, y REAL, z BOOLEAN);
INSERT INTO baz (x, y, z) VALUES (1, 1.1, 1), (2, 2.2, 0), (3, NULL, NULL);
