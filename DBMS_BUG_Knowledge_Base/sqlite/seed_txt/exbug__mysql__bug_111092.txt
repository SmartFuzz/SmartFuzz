CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, email TEXT UNIQUE);
INSERT INTO users (name, email) VALUES ('Alice', 'alice@example.com'), ('Bob', NULL), ('Charlie', 'charlie@example.com');
CREATE VIEW active_users AS SELECT * FROM users WHERE email IS NOT NULL;
SELECT * FROM active_users;
