CREATE TABLE test_table (id INTEGER, value TEXT, flag INTEGER);
INSERT INTO test_table (id, value, flag) VALUES (1, 'test', 1), (2, NULL, 0), (3, 'example', NULL), (4, 'sample', 1);
WITH cte AS (SELECT id, value FROM test_table WHERE flag IS NOT NULL) SELECT GROUP_CONCAT(value, ', ') AS aggregated_values FROM cte;
SELECT id, CASE WHEN value IS NULL THEN 'NULL' ELSE value END AS value_check FROM test_table;
SELECT COUNT(*) AS total, GROUP_CONCAT(value, '|') AS concatenated FROM test_table WHERE flag = 1;
