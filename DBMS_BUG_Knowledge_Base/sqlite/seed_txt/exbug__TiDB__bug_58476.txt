CREATE TABLE t4 (id INTEGER PRIMARY KEY, c1 TEXT, c2 TEXT GENERATED ALWAYS AS (c1 || ' - ' || c1) VIRTUAL, c3 INTEGER DEFAULT NULL);
INSERT INTO t4(id, c1) VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
SELECT id FROM t4 WHERE c2 IS NOT NULL AND c3 BETWEEN 1 AND 3 GROUP BY id HAVING id < 5;
CREATE TABLE t5 (id INTEGER PRIMARY KEY, c1 TEXT NOT NULL, c2 INTEGER DEFAULT 0, c3 TEXT GENERATED ALWAYS AS (COALESCE(c1, '') || c2) VIRTUAL);
INSERT INTO t5(id, c1, c2) VALUES (1, 'data', 1), (2, NULL, 2);
