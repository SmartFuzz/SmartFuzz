CREATE TABLE testtable (id INTEGER PRIMARY KEY, textColumn TEXT, varcharColumn TEXT, nullableColumn TEXT);
INSERT INTO testtable (id, textColumn, varcharColumn, nullableColumn) VALUES (1, 'Sample text', 'Sample varchar', NULL);
SELECT name AS relname, sql AS definition FROM sqlite_master WHERE name = 'testtable';
CREATE TABLE another_table (ref_id INTEGER, description TEXT NOT NULL, FOREIGN KEY(ref_id) REFERENCES testtable(id));
INSERT INTO another_table (ref_id, description) VALUES (1, 'Reference to testtable');
