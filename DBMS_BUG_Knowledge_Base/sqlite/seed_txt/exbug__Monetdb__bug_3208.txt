CREATE TABLE diverse_types(id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, is_active INTEGER NOT NULL, score REAL CHECK(score >= 0));
INSERT INTO diverse_types(id, name, is_active, score) VALUES(1, 'Test User', 1, 99.5);
CREATE TABLE related_data(user_id INTEGER, description TEXT, FOREIGN KEY(user_id) REFERENCES diverse_types(id));
INSERT INTO related_data(user_id, description) VALUES(1, 'This is a test description.');
SELECT d.name, r.description FROM diverse_types d JOIN related_data r ON d.id = r.user_id WHERE d.is_active = 1;
