CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', NULL);
CREATE VIEW ranked_users AS SELECT id, name, age, ROW_NUMBER() OVER(ORDER BY age DESC) AS r FROM users;
SELECT name, age FROM ranked_users WHERE r <= 2 ORDER BY r;
CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, amount REAL, FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO orders (order_id, user_id, amount) VALUES (1, 1, 100.50), (2, 2, NULL), (3, 3, 75.00);
