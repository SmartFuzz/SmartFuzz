CREATE TABLE releases (version REAL PRIMARY KEY, description TEXT);
CREATE TABLE releases_v2 (version FLOAT, description TEXT);
CREATE TABLE releases_v3 (id INTEGER PRIMARY KEY, version REAL, description TEXT);
INSERT INTO releases_v3 (id, version, description) VALUES (1, 1.0, 'Initial release');
SELECT * FROM releases_v3 WHERE version > 0.5;
