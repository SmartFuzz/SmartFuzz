CREATE TABLE cache_test (id INTEGER PRIMARY KEY, name TEXT, age INTEGER DEFAULT NULL);
INSERT INTO cache_test (id, name) VALUES (1, 'Alice');
ALTER TABLE cache_test ADD COLUMN score INTEGER NOT NULL DEFAULT 0;
ALTER TABLE cache_test ADD COLUMN status TEXT DEFAULT 'active';
SELECT id, name, COALESCE(age, 0) AS age, score, status FROM cache_test;
