CREATE TABLE test_json (id INTEGER, data TEXT, is_active BOOLEAN, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_json (id, data, is_active) VALUES (1, '{"key": "value"}', 1);
SELECT json(data) FROM test_json WHERE is_active = 1;
SELECT json(data) FROM test_json WHERE id = 1;
SELECT json(data) FROM test_json WHERE created_at < CURRENT_TIMESTAMP;
