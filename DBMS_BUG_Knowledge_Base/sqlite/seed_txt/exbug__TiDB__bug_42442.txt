CREATE TABLE t2 (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK (age >= 0));
INSERT INTO t2 (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', NULL);
ALTER TABLE t2 ADD COLUMN email TEXT;
SELECT * FROM t2;
CREATE TABLE t2_new AS SELECT * FROM t2 WHERE id < 100;
