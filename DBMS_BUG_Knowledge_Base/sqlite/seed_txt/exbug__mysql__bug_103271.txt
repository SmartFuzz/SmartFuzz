CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, age INTEGER CHECK (age >= 0), created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', -1);
SELECT * FROM users WHERE age IS NULL OR age < 0;
CREATE TABLE t(id INTEGER PRIMARY KEY AUTOINCREMENT);
INSERT INTO t DEFAULT VALUES;
