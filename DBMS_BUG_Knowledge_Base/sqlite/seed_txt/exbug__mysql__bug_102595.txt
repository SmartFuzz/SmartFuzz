CREATE TABLE test_table (id INTEGER PRIMARY KEY, data TEXT, name TEXT NOT NULL);
INSERT INTO test_table (id, data, name) VALUES (1, json('{"x": null}'), 'test_name');
SELECT json_extract(data, '$.x') FROM test_table WHERE id = 1;
SELECT CASE WHEN json_extract(data, '$.x') IS NULL THEN 'NULL' ELSE json_extract(data, '$.x') END FROM test_table WHERE id = 1;
