CREATE TABLE test_table (id INTEGER PRIMARY KEY, text_column TEXT NOT NULL);
INSERT INTO test_table (id, text_column) VALUES (1, 'Find this pattern'), (2, NULL), (3, 'Another pattern');
CREATE TEMP TABLE temp_func(arg TEXT);
INSERT INTO temp_func SELECT text_column FROM test_table WHERE id = 1;
SELECT CASE WHEN arg IS NULL THEN NULL ELSE INSTR(arg, 'pattern') END FROM temp_func;
SELECT CASE WHEN NULL IS NULL THEN NULL ELSE INSTR(NULL, 'pattern') END;
