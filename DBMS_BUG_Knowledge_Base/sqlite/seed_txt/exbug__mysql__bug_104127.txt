DROP TABLE IF EXISTS tbl_test;
CREATE TABLE tbl_test (id INTEGER PRIMARY KEY, name TEXT, value REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO tbl_test (id, name, value) VALUES (1, 'Test1', 10.50), (2, 'Test2', NULL), (3, 'Test3', 20.00);
WITH RECURSIVE cte_example AS (SELECT 1 AS level, name FROM tbl_test WHERE value IS NOT NULL UNION ALL SELECT level + 1, name FROM cte_example WHERE level < 5) SELECT COUNT(*) FROM cte_example;
WITH RECURSIVE cte_example AS (SELECT 1 AS level, name FROM tbl_test WHERE value IS NOT NULL UNION ALL SELECT level + 1, name FROM cte_example WHERE level < 5 LIMIT 2 OFFSET 1) SELECT * FROM cte_example;
