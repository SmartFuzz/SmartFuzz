CREATE TABLE pt2 (id INTEGER PRIMARY KEY AUTOINCREMENT, val TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO pt2 (val) VALUES ('test1'), ('test2'), (NULL), ('test4');
INSERT INTO pt2 (val) SELECT val || '_copy' FROM pt2 WHERE val IS NOT NULL;
SELECT COUNT(*) FROM pt2 WHERE created_at >= DATETIME('now', '-1 day');
VACUUM;
