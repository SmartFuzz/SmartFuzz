CREATE TABLE sales_partitioned_v2 (sale_id INTEGER PRIMARY KEY AUTOINCREMENT, region TEXT NOT NULL, amount REAL CHECK (amount >= 0), sale_date DATE DEFAULT (CURRENT_DATE));
INSERT INTO sales_partitioned_v2 (region, amount, sale_date) VALUES ('North', 150.00, '2023-02-15');
SELECT sale_id, region, amount FROM sales_partitioned_v2 WHERE sale_date IS NOT NULL;
UPDATE sales_partitioned_v2 SET amount = amount + 50 WHERE region = 'North' AND sale_date < '2023-03-01';
CREATE TABLE sales_partitioned_v2_temp AS SELECT * FROM sales_partitioned_v2 WHERE sale_date < '2024-01-01';
DROP TABLE sales_partitioned_v2;
