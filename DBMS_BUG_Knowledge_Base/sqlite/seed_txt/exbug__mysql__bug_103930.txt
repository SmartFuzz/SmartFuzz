CREATE TABLE data_user_test_v2 (user_id INTEGER NOT NULL, country TEXT NOT NULL DEFAULT '', age INTEGER, create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (user_id), UNIQUE (country));
INSERT INTO data_user_test_v2 VALUES (1, '中国', 30, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP), (2, '日本', 25, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP), (3, '中国', 35, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP), (4, '韩国', NULL, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP);
SELECT country, COUNT(*) AS user_count FROM data_user_test_v2 GROUP BY country HAVING user_count > 1;
SELECT CASE WHEN country='中国' THEN '国内' ELSE '国外' END AS country_type, COUNT(DISTINCT user_id) FROM data_user_test_v2 GROUP BY country_type;
