CREATE TABLE tbl03 (id INTEGER PRIMARY KEY, b INTEGER NOT NULL DEFAULT 0, c TEXT DEFAULT NULL);
INSERT INTO tbl03 (id, b) VALUES (1, NULL);
CREATE TRIGGER trg02 BEFORE INSERT ON tbl03 FOR EACH ROW BEGIN SELECT a INTO NEW.c FROM test01.c WHERE id = 1;
END;
SELECT * FROM tbl03;
