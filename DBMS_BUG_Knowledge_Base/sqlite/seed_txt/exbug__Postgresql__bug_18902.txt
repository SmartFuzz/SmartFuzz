CREATE TABLE test_table (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, status TEXT);
INSERT INTO test_table (name, status) VALUES ('test1', 'foo'), ('test2', 'bar'), ('test3', NULL);
CREATE TABLE foreign_table (id INTEGER, description TEXT);
SELECT * FROM test_table WHERE status IS NOT NULL AND created_at > DATETIME('now', '-1 day');
UPDATE test_table SET status = 'buz' WHERE id IN (SELECT id FROM foreign_table WHERE description LIKE '%test%');
