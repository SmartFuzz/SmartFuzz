CREATE TABLE test_data (id INTEGER PRIMARY KEY, value INTEGER, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, value, name) VALUES (1, 10, 'A'), (2, NULL, 'B'), (3, 30, 'C');
SELECT AVG(value) FROM test_data WHERE value IS NOT NULL GROUP BY name;
SELECT * FROM test_data WHERE id IN (SELECT id FROM test_data WHERE value IS NULL);
