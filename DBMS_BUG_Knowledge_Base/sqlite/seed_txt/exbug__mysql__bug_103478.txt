CREATE TABLE t2 (id INTEGER PRIMARY KEY AUTOINCREMENT, value TEXT NOT NULL, created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, is_active INTEGER DEFAULT 1);
INSERT INTO t2 (value, is_active) VALUES ('test1', 1), ('test2', NULL), ('test3', 0);
SELECT * FROM (SELECT id, value FROM t2 WHERE is_active IS NOT NULL);
UPDATE t2 SET value = value || '_updated' WHERE is_active = 1;
SELECT COUNT(*) FROM (SELECT DISTINCT value FROM t2);
