CREATE TABLE sales ( year INTEGER DEFAULT NULL, country TEXT DEFAULT NULL, product TEXT DEFAULT NULL, profit INTEGER DEFAULT NULL );
INSERT INTO sales (year, country, product, profit) VALUES (2001, 'USA', 'Gadget', 500), (2002, 'USA', 'Gadget', 800), (2003, 'USA', 'Gadget', 1200), (2003, 'Canada', 'Gadget', 1000);
SELECT year + 1 AS y, SUM(profit) AS profit FROM sales GROUP BY year + 1 WITH ROLLUP HAVING y > 2002 ORDER BY y, profit;
CREATE TABLE products ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, category TEXT DEFAULT NULL );
INSERT INTO products (name, category) VALUES ('Gadget', 'Electronics'), ('Widget', 'Tools');
