CREATE TABLE v1 ( id INTEGER PRIMARY KEY, name TEXT, age INTEGER CHECK (age >= 0) );
INSERT INTO v1 (id, name, age) VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', NULL);
CREATE TABLE v2 ( group_id INTEGER, description TEXT, FOREIGN KEY (group_id) REFERENCES v1(id) );
INSERT INTO v2 (group_id, description) VALUES (1, 'Group A'), (2, 'Group B'), (3, 'Group C');
SELECT v1.id, v1.name, (SELECT description FROM v2 WHERE group_id = v1.id ORDER BY group_id DESC LIMIT 1) AS last_description FROM v1 LEFT JOIN v2 ON v1.id = v2.group_id;
