CREATE TABLE bar (id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO bar (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, NULL);
CREATE TABLE baz (value INTEGER, description TEXT NOT NULL);
INSERT INTO baz (value, description) VALUES (10, 'Ten'), (20, 'Twenty'), (NULL, 'Null Value');
SELECT (SELECT COUNT(*) FROM bar) AS inner_count;
