CREATE TABLE test_table (id INTEGER PRIMARY KEY, value TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, value) VALUES (1, 'first'), (2, 'second'), (3, 'third');
CREATE TABLE ordered_table AS SELECT * FROM test_table ORDER BY created_at;
SELECT GROUP_CONCAT(value) AS original_array FROM test_table;
SELECT GROUP_CONCAT(value) AS ordered_array FROM ordered_table;
SELECT GROUP_CONCAT(value) AS non_null_array FROM test_table WHERE id IS NOT NULL;
