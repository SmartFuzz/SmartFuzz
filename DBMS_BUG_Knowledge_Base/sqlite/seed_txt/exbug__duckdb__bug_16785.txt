CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, username TEXT NOT NULL UNIQUE);
CREATE TABLE IF NOT EXISTS orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, amount REAL NOT NULL, FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO users (id, username) VALUES (1, 'user1'), (2, 'user2');
INSERT INTO orders (order_id, user_id, amount) VALUES (101, 1, 50.0), (102, 2, 75.0);
SELECT * FROM users;
SELECT * FROM orders;
