CREATE TABLE T1 (K INTEGER, C0 TEXT, C1 TEXT, C2 REAL);
INSERT INTO T1 SELECT number, (SELECT GROUP_CONCAT(cityHash64(i * number)) FROM (SELECT i FROM generate_series(0, 9) AS i)), CASE WHEN number % 2 = 0 THEN 'even' ELSE NULL END, number * 1.5 FROM generate_series(0, 99) AS number;
CREATE TABLE T2 (K INTEGER, C0 TEXT, C1 INTEGER);
INSERT INTO T2 SELECT K, C0, CAST(K AS INTEGER) FROM T1 WHERE C1 IS NOT NULL;
SELECT * FROM T1 WHERE C1 = 'even';
