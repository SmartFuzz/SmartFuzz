CREATE TABLE t2 (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, description TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2 (name, description) VALUES ('Sample', NULL);
ANALYZE;
SELECT * FROM t2 WHERE name = 'Sample' AND description IS NULL;
