CREATE TABLE rocksDB(A INTEGER, B TEXT);
INSERT INTO rocksDB VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
SELECT A, B FROM rocksDB WHERE A = 1;
SELECT A, B FROM rocksDB WHERE A IS NULL;
SELECT A, B FROM rocksDB WHERE A IN (SELECT A FROM rocksDB WHERE B LIKE 'test%');
