CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT, value REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'test\tname', 10.5), (2, 'another\nname', 20.0), (3, 'name\with\backslash', 30.75);
CREATE TABLE secondary_table (ref_id INTEGER, description TEXT, FOREIGN KEY (ref_id) REFERENCES test_table(id));
INSERT INTO secondary_table (ref_id, description) SELECT id, name || ' description' FROM test_table WHERE value > 15.0;
SELECT t.id, t.name, s.description FROM test_table t LEFT JOIN secondary_table s ON t.id = s.ref_id WHERE t.name LIKE '%\n%';
