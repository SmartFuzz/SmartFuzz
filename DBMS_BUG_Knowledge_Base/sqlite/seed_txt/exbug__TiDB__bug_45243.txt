DROP TABLE IF EXISTS users;
CREATE TABLE users(id INTEGER NOT NULL PRIMARY KEY, name TEXT, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users VALUES (1, 'Alice', 30, NULL), (2, 'Bob', NULL, NULL), (3, 'Charlie', 25, NULL);
SELECT * FROM users;
