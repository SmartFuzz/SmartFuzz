CREATE TABLE t3 ( x INTEGER NOT NULL, y TEXT DEFAULT NULL, z DATETIME DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (x), INDEX ix_y (y), INDEX ix_xy (x, y) );
INSERT INTO t3 (x, y) SELECT a, 'value_' || a FROM (SELECT 1 AS a UNION ALL SELECT a + 1 FROM (SELECT 1 AS a UNION ALL SELECT a + 1 AS a) AS b WHERE a < 100);
CREATE TABLE t4 ( m INTEGER, n INTEGER DEFAULT NULL, o REAL, PRIMARY KEY (m), INDEX im_n (n), INDEX im_no (n, o) );
INSERT INTO t4 (m, n, o) VALUES (1, 9995, 1.5), (2, 9996, 2.5), (3, NULL, 3.5);
EXPLAIN QUERY PLAN SELECT t3.x FROM t3 JOIN t4 ON t3.x = t4.m WHERE t4.n BETWEEN 9990 AND 9999 ORDER BY t3.x LIMIT 3;
