CREATE TABLE products (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, price REAL NOT NULL);
CREATE TABLE orders (id INTEGER PRIMARY KEY AUTOINCREMENT, product_id INTEGER NOT NULL, quantity INTEGER NOT NULL, FOREIGN KEY (product_id) REFERENCES products (id));
INSERT INTO products (name, price) VALUES ('Widget', 19.99);
INSERT INTO orders (product_id, quantity) VALUES (1, 5);
SELECT sql FROM sqlite_master WHERE type='table' AND name='orders';
