DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (id INTEGER, name TEXT, description TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2 (id, name, description) VALUES (1, 'Test', REPLACE('x', 'x', 'x')), (2, 'Sample', REPLACE('y', 'y', 'y'));
SELECT id, GROUP_CONCAT(name) AS concatenated_names FROM t2 GROUP BY id;
SELECT * FROM (SELECT id, GROUP_CONCAT(description) AS concatenated_descriptions FROM t2 GROUP BY id) AS a1 UNION SELECT 3, 'Extra data';
