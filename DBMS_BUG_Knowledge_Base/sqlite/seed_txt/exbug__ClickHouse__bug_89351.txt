CREATE TABLE test_table_v2 (id INTEGER, name TEXT, created_at DATETIME, is_active INTEGER DEFAULT 1);
INSERT INTO test_table_v2 VALUES (3, 'test3', CURRENT_TIMESTAMP, 1), (4, 'test4', CURRENT_TIMESTAMP, NULL);
CREATE TABLE test_table_v3 AS SELECT * FROM test_table_v2 WHERE 0;
-- creates an empty table;
INSERT INTO test_table_v3 SELECT id, name, created_at, is_active FROM test_table_v2 WHERE is_active = 1;
SELECT id, name, created_at FROM test_table_v3 WHERE created_at > DATETIME('now', '-1 day');
