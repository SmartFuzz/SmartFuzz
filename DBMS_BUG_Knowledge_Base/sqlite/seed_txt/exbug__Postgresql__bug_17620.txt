CREATE TABLE t1 (int_id INTEGER PRIMARY KEY, text_id TEXT NOT NULL);
CREATE TABLE t2 (ext_id INTEGER, text_id TEXT, int_id INTEGER, a INTEGER CHECK (a >= 0), b REAL);
INSERT INTO t2 (ext_id, text_id, a, b) VALUES (1, 'sample', 10, 5.5);
CREATE INDEX index_1 ON t1 (text_id);
UPDATE t2 SET int_id = (SELECT int_id FROM t1 WHERE t1.text_id = t2.text_id) WHERE text_id IS NOT NULL;
