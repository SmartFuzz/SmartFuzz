CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE INDEX idx_name ON test_table (name);
INSERT INTO test_table (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);
SELECT * FROM test_table ORDER BY name;
SELECT sqlite3_stmt_status(SELECT * FROM test_table ORDER BY name, age, created_at, SQLITE_STMTSTATUS_FULLSCAN_STEP);
