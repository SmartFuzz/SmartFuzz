CREATE TABLE users(id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK (age >= 0));
CREATE TABLE orders(order_id INTEGER PRIMARY KEY, user_id INTEGER, amount REAL NOT NULL, FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO users VALUES(1, 'Alice', 30);
INSERT INTO orders VALUES(1, 1, 99.99);
SELECT * FROM users;
DROP TABLE orders;
