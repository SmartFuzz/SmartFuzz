CREATE TABLE t1(c0 TEXT, c1 INTEGER NOT NULL GENERATED ALWAYS AS (LENGTH(c0)));
CREATE INDEX i1 ON t1(c1);
INSERT INTO t1(c0) VALUES ('test'), (NULL), ('another test');
VACUUM;
-- SQLite does not have ANALYZE TABLE, using VACUUM instead for optimization;
SELECT * FROM t1 WHERE c1 > 0;
