CREATE TABLE t_proj_test1 (id INTEGER, name TEXT, value REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t_proj_test1 SELECT number, 'Name' || number, number * 1.5, CURRENT_TIMESTAMP FROM (SELECT ROW_NUMBER() OVER () AS number FROM (SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1) AS t1, (SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1) AS t2 LIMIT 10000);
CREATE TABLE t_proj_test2 (user_id INTEGER, score INTEGER, timestamp DATE DEFAULT CURRENT_DATE);
INSERT INTO t_proj_test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, CURRENT_DATE FROM (SELECT ROW_NUMBER() OVER () AS number FROM (SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1) AS t1, (SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1 UNION ALL SELECT 1) AS t2 LIMIT 5000);
SELECT user_id, SUM(score) AS total_score FROM t_proj_test2 GROUP BY user_id ORDER BY total_score DESC;
