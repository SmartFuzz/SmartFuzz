CREATE TABLE persons (id INTEGER PRIMARY KEY, first_name TEXT);
CREATE TABLE functions (id INTEGER PRIMARY KEY, function_name TEXT);
INSERT INTO persons (id, first_name) VALUES (1, 'Alice'), (2, 'Bob');
INSERT INTO functions (id, function_name) VALUES (1, 'Developer'), (2, NULL);
SELECT p.first_name, CASE WHEN f.function_name IS NOT NULL THEN f.function_name || ' (nl)' ELSE NULL END AS function_label FROM persons p LEFT JOIN functions f ON p.id = f.id;
