CREATE TABLE c (ID INTEGER PRIMARY KEY, Name TEXT NOT NULL);
INSERT INTO c VALUES (1, 'Alice');
CREATE TABLE d (ID INTEGER PRIMARY KEY, Name TEXT UNIQUE);
INSERT INTO d (ID, Name) SELECT ID, Name FROM c GROUP BY ID, Name;
INSERT INTO d (ID, Name) SELECT ID, NULL FROM c GROUP BY ID;
