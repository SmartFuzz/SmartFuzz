DROP TABLE IF EXISTS t2;
CREATE TABLE t2(id INTEGER PRIMARY KEY, name TEXT, age INTEGER CHECK (age >= 0));
INSERT INTO t2 VALUES (1, 'Alice', 30), (2, 'Bob', 25), (3, 'Charlie', NULL);
SELECT * FROM t2 WHERE age IS NULL;
EXPLAIN QUERY PLAN SELECT * FROM t2 ORDER BY age LIMIT 2;
