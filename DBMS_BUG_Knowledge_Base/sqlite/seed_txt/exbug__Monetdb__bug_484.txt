CREATE TABLE test_division (a INTEGER, b INTEGER, c REAL, d TEXT);
INSERT INTO test_division VALUES (1, 2, 3.0, 'test1');
INSERT INTO test_division VALUES (3, 0, 4.5, 'test2');
SELECT a * 1.0 / NULLIF(b, 0) FROM test_division WHERE c > 2.0;
SELECT d, a * 1.0 / NULLIF(c, 0) FROM test_division WHERE b = 0;
