CREATE TABLE keytest3 ( date_added INTEGER NOT NULL, hash_key TEXT NOT NULL, dimension1 INTEGER NOT NULL, dimension2 INTEGER NOT NULL, metric1 INTEGER NOT NULL, metric2 INTEGER NOT NULL, UNIQUE (hash_key, date_added));
INSERT INTO keytest3 (date_added, hash_key, dimension1, dimension2, metric1, metric2) VALUES (1, 'abc', 1, 1, 2, 2);
INSERT INTO keytest3 (date_added, hash_key, dimension1, dimension2, metric1, metric2) VALUES (1, 'abc', 2, 2, 3, 3);
SELECT * FROM keytest3 WHERE hash_key IS NOT NULL;
CREATE TABLE keytest4 ( date_added INTEGER NOT NULL, key INTEGER NOT NULL, dimension1 INTEGER NOT NULL, dimension2 INTEGER NOT NULL, metric1 INTEGER NOT NULL, metric2 INTEGER NOT NULL, UNIQUE (key, date_added));
