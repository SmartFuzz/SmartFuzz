CREATE TABLE test_dates (id INTEGER, event_date TEXT, description TEXT);
INSERT INTO test_dates VALUES (1, 'infinity', 'Event with infinite date');
INSERT INTO test_dates VALUES (2, '2025-10-02', 'Regular event date');
SELECT CASE WHEN event_date = 'infinity' THEN 'infinity' ELSE strftime('%Y', event_date) END AS year FROM test_dates WHERE id = 1;
SELECT strftime('%Y', event_date) AS year FROM test_dates WHERE id = 2;
