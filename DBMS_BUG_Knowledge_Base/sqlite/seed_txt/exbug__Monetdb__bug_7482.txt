CREATE TABLE test_table (id INTEGER PRIMARY KEY, value REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, value) VALUES (1, 12.5), (2, NULL), (3, 15.0);
CREATE TABLE another_table (ref_id INTEGER, description TEXT, FOREIGN KEY (ref_id) REFERENCES test_table(id));
INSERT INTO another_table (ref_id, description) VALUES (1, 'Sample 1'), (2, 'Sample 2');
SELECT AVG(value) FROM test_table JOIN another_table ON test_table.id = another_table.ref_id GROUP BY (CASE WHEN created_at IS NULL THEN 0 ELSE 1 END), ref_id;
