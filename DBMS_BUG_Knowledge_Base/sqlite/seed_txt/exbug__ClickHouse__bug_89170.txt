CREATE TABLE test_distinct_oom_v2 ( id INTEGER, value TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP );
INSERT INTO test_distinct_oom_v2 SELECT number, CASE WHEN number % 2 = 0 THEN CAST(number AS TEXT) ELSE NULL END FROM (SELECT rowid AS number FROM sqlite_sequence LIMIT 100000000);
SELECT DISTINCT value FROM test_distinct_oom_v2;
CREATE TABLE test_distinct_oom_v3 ( id INTEGER, value REAL, category TEXT DEFAULT 'unknown' );
INSERT INTO test_distinct_oom_v3 SELECT number, number / 3.14, CAST(number % 10 AS TEXT) FROM (SELECT rowid AS number FROM sqlite_sequence LIMIT 1000000);
