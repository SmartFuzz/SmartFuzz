CREATE TABLE test_labels (id INTEGER PRIMARY KEY, label TEXT NOT NULL);
INSERT INTO test_labels (id, label) VALUES (1, 'leden'), (2, 'únor'), (3, 'Leden'), (4, 'Únor');
SELECT label FROM test_labels WHERE label GLOB 'l*';
SELECT (label GLOB 'U*') AS matches FROM test_labels WHERE label IS NOT NULL;
SELECT COUNT(*) FROM test_labels WHERE label GLOB 'l*' OR label IS NULL;
