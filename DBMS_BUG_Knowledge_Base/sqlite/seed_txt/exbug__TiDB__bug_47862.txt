CREATE TABLE sbtest2 (id INTEGER NOT NULL, name TEXT NOT NULL, age INTEGER DEFAULT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id), UNIQUE (name));
INSERT INTO sbtest2 (id, name, age) VALUES (1, 'Alice', 30);
INSERT INTO sbtest2 (id, name, age) VALUES (2, 'Bob', NULL);
CREATE INDEX idx_age ON sbtest2 (age);
CREATE INDEX idx_name_age ON sbtest2 (name, age);
SELECT * FROM sbtest2 WHERE age IS NULL OR name = 'Alice';
