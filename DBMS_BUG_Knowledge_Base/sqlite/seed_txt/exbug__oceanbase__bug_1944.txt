CREATE TABLE test_table (id INTEGER PRIMARY KEY, value REAL, description TEXT);
INSERT INTO test_table (id, value, description) VALUES (1, 10.0, 'Sample data');
INSERT INTO test_table (id, value, description) VALUES (2, 20.0, 'Another sample');
SELECT AVG(value) FROM test_table WHERE description IS NOT NULL;
SELECT AVG(CASE WHEN value > 15 THEN value ELSE NULL END) FROM test_table;
