CREATE TABLE users (id INTEGER, name TEXT, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users VALUES (1, 'Alice', 30, NULL);
CREATE TABLE orders (order_id INTEGER, user_id INTEGER, amount REAL, order_date DATE);
INSERT INTO orders VALUES (101, 1, 99.99, '2023-10-01');
SELECT u.name, o.amount FROM users u JOIN orders o ON u.id = o.user_id WHERE u.age > 25;
