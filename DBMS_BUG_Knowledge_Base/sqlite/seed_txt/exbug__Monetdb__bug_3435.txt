CREATE TABLE TEST1 ( one TEXT NOT NULL, two INTEGER DEFAULT 0, three DATE );
CREATE TABLE TEST2 ( one TEXT NOT NULL, two INTEGER CHECK (two >= 0), four REAL );
INSERT INTO TEST1 (one, two, three) VALUES ( 'a', 1, '2023-01-01' );
INSERT INTO TEST2 (one, two, four) VALUES ( 'a', 1, 1.5 );
CREATE INDEX idx_test1 ON TEST1 ( one, two );
SELECT * FROM TEST1 JOIN TEST2 ON TEST1.one = TEST2.one;
