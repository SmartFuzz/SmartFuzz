CREATE TABLE t2 (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK (age >= 0), salary REAL CHECK (salary > 0));
INSERT INTO t2 (id, name, age, salary) VALUES (1, 'Alice', 30, 50000.00);
DELETE FROM t2 WHERE age < 0;
ALTER TABLE t2 RENAME TO t2_old;
CREATE TABLE t2 (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER, salary REAL);
