CREATE TABLE lrr_test2(COL1 TEXT DEFAULT NULL, COL2 INTEGER NOT NULL);
INSERT INTO lrr_test2 VALUES('838:59:59', 1);
INSERT INTO lrr_test2 VALUES('23:59:59', 2);
SELECT COL1, CASE WHEN (strftime('%H', COL1) + 10) >= 24 THEN NULL ELSE strftime('%H:%M:%S', 'now', '+10 hours', COL1) END AS new_time FROM lrr_test2 WHERE COL2 = 1 AND (strftime('%H', COL1) + 10) >= 24;
SELECT COL1, COL2 FROM lrr_test2 WHERE COL1 IS NOT NULL AND COL2 > 0;
