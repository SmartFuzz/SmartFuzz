CREATE TABLE bar (id INTEGER PRIMARY KEY, value TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP) WITHOUT ROWID;
INSERT INTO bar (value) VALUES ('test1');
INSERT INTO bar (value) VALUES (NULL);
SELECT * FROM bar WHERE value IS NULL;
DROP TABLE bar;
