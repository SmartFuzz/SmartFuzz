CREATE TABLE test_data (id INTEGER, name TEXT, score REAL, group_id INTEGER);
INSERT INTO test_data VALUES (1, 'Alice', NULL, 100);
INSERT INTO test_data VALUES (2, 'Bob', NULL, 100);
SELECT MIN(score) OVER (PARTITION BY group_id) FROM test_data WHERE score IS NOT NULL;
SELECT MAX(score) OVER (PARTITION BY group_id) FROM test_data WHERE score IS NOT NULL;
