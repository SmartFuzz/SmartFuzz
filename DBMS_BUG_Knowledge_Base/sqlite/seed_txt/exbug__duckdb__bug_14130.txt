CREATE TABLE Bar (id INTEGER PRIMARY KEY, description TEXT, created_at DATETIME);
INSERT INTO Bar VALUES (1, 'First entry', CURRENT_TIMESTAMP);
CREATE TABLE Baz (id INTEGER PRIMARY KEY, value REAL, bar_id INTEGER, FOREIGN KEY (bar_id) REFERENCES Bar(id));
INSERT INTO Baz VALUES (1, 3.14, 1);
SELECT b.id, b.description, COUNT(z.id) FROM Bar b LEFT JOIN Baz z ON b.id = z.bar_id GROUP BY b.id, b.description;
