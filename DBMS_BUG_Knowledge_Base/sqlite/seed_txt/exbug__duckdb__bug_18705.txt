CREATE TABLE test_strings (id INTEGER PRIMARY KEY, str_value TEXT);
INSERT INTO test_strings (id, str_value) VALUES (1, 'HELLO'), (2, 'world'), (3, 'HELLO world');
SELECT INSTR(LOWER(str_value), LOWER('el')) FROM test_strings WHERE id = 1;
SELECT INSTR(LOWER(str_value), LOWER('hEllO')) FROM test_strings WHERE id = 1;
SELECT str_value LIKE '%HELLO%' FROM test_strings WHERE id = 3;
