CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, amount REAL, order_date TEXT, status TEXT DEFAULT 'pending');
INSERT INTO orders VALUES (1, 1, 250.00, '2024-01-05 09:00:00', 'completed'), (2, 2, 150.50, '2024-01-06 10:30:00', NULL), (3, 3, 75.75, '2024-01-07 14:45:00', 'pending');
CREATE TABLE users (user_id INTEGER PRIMARY KEY, name TEXT NOT NULL, email TEXT UNIQUE);
INSERT INTO users VALUES (1, 'Alice', 'alice@example.com'), (2, 'Bob', 'bob@example.com'), (3, 'Charlie', 'charlie@example.com');
SELECT o.order_id, u.name, o.amount FROM orders o LEFT JOIN users u ON o.user_id = u.user_id WHERE o.status IS NULL;
