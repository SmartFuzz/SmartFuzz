CREATE TABLE t2(a INTEGER, b TEXT, c REAL, d TEXT, e INTEGER);
INSERT INTO t2 VALUES (1, 'test', 1.1, '2023-01-01', 1), (2, 'example', 2.2, '2023-01-02', 0);
INSERT INTO t2 SELECT a, b, c, d, e FROM t2 WHERE a > 1;
SELECT SUM(a) FROM t2 WHERE e IS NOT NULL GROUP BY b ORDER BY c;
UPDATE t2 SET c = c + 1 WHERE a = (SELECT MAX(a) FROM t2);
