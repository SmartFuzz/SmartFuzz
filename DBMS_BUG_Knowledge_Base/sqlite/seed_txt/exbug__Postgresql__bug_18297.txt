CREATE TABLE parent (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE child1 (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE child2 (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
ALTER TABLE parent ADD COLUMN age INTEGER CHECK (age >= 0);
INSERT INTO parent (name, age) VALUES ('Alice', 30), ('Bob', NULL);
