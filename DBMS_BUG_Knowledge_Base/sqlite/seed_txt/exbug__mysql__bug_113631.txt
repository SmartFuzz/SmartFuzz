CREATE TABLE t(a INTEGER, b TEXT, c REAL, d TEXT);
INSERT INTO t VALUES (NULL, 'test1', 1.1, '2023-01-01'), (1, 'test2', 2.2, '2023-01-02'), (2, 'test3', NULL, '2023-01-03');
SELECT a, (SELECT MIN(a) FROM t AS t2 WHERE t2.a IS NOT NULL AND t2.a <= t.a) AS min_a FROM t;
CREATE TABLE u(x INTEGER NOT NULL, y INTEGER DEFAULT 0);
INSERT INTO u SELECT a, (SELECT MIN(a) FROM t WHERE a IS NOT NULL) FROM t GROUP BY a;
