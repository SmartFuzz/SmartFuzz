CREATE TABLE distributions (id INTEGER PRIMARY KEY, title TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO distributions (id, title) VALUES (1, 'Distribution One');
INSERT INTO distributions (id, title) VALUES (2, NULL);
SELECT id, title, hex(substr('x' || title, 2)) AS title_hash FROM distributions WHERE id = 1;
INSERT INTO distributions (id, title) SELECT 3, 'Distribution ' || id FROM distributions WHERE id < 3;
