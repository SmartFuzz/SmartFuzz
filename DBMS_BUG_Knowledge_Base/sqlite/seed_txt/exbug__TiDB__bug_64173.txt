CREATE TABLE departments (dept_id INTEGER NOT NULL, dept_name TEXT NOT NULL, PRIMARY KEY (dept_id));
ALTER TABLE employees1 ADD COLUMN dept_id INTEGER;
INSERT INTO departments (dept_id, dept_name) VALUES (1, 'HR'), (2, 'Engineering');
UPDATE employees1 SET dept_id = 1 WHERE id IN (1, 2);
SELECT e.fname, e.lname, d.dept_name FROM employees1 e LEFT JOIN departments d ON e.dept_id = d.dept_id WHERE d.dept_name IS NOT NULL;
