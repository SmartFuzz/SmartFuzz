CREATE TABLE office_data (id INTEGER PRIMARY KEY, name TEXT, location TEXT, active BOOLEAN);
INSERT INTO office_data (id, name, location, active) VALUES (1, 'Main Office', 'New York', 1);
INSERT INTO office_data (id, name, location, active) VALUES (2, 'Branch Office', 'Los Angeles', 0);
SELECT DISTINCT CASE WHEN active = 1 THEN 'true' ELSE 'false' END AS direct, location AS link FROM office_data WHERE active = 1 UNION SELECT DISTINCT CASE WHEN active = 1 THEN 'true' ELSE 'false' END AS direct, location AS link FROM office_data WHERE active = 0;
SELECT COUNT(*) FROM office_data WHERE location IS NOT NULL;
