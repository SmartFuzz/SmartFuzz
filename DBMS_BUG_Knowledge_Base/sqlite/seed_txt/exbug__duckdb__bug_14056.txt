CREATE TABLE test_table (id INTEGER, data TEXT);
INSERT INTO test_table VALUES (1, '[1, 2, 3]'), (2, 'not_a_list');
SELECT CASE WHEN data IS NOT NULL THEN json_each.value ELSE 0 END AS x FROM test_table, json_each(test_table.data) WHERE id = 1;
SELECT CASE WHEN id = 2 THEN json_each.value ELSE 'No List' END AS x FROM test_table, json_each(test_table.data);
SELECT CASE WHEN id < 3 THEN json_each.value ELSE NULL END AS x FROM test_table, json_each(test_table.data);
