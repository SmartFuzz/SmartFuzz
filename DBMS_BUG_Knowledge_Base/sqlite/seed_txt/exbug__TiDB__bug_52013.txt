CREATE TABLE departments ( dept_id INTEGER NOT NULL PRIMARY KEY, dept_name TEXT NOT NULL, budget REAL DEFAULT 0.00 );
INSERT INTO departments (dept_id, dept_name, budget) VALUES (1, 'HR', 50000.00);
INSERT INTO departments (dept_id, dept_name, budget) VALUES (2, 'Engineering', NULL);
SELECT dept_name FROM departments WHERE budget IS NULL;
SELECT dept_id FROM departments WHERE dept_id = 99999;
