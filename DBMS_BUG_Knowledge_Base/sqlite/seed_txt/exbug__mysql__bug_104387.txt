CREATE TABLE colltest2 ( id INTEGER PRIMARY KEY AUTOINCREMENT, cbin BLOB, ctxt TEXT, cstr TEXT, cnum INTEGER DEFAULT NULL );
INSERT INTO colltest2 (cbin, ctxt, cstr, cnum) VALUES (X'as646466', 'asdf', 'asdf', NULL);
SELECT * FROM colltest2 WHERE cbin LIKE '%' || cstr || '%';
SELECT * FROM colltest2 WHERE cbin LIKE '%' || ctxt || '%';
SELECT * FROM colltest2 WHERE cnum IS NULL;
