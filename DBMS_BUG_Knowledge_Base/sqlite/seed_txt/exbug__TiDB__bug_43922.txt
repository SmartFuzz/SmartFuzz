CREATE TABLE test_t2(c1 INT, c2 TEXT, c3 DATE, c4 REAL DEFAULT 0.0);
INSERT INTO test_t2 VALUES(1, 'a', '2023-01-01', 1.1), (2, 'b', NULL, 2.2), (3, 'c', '2023-01-03', NULL), (4, 'd', '2023-01-04', 4.4);
CREATE TABLE test_t3(c1 INT PRIMARY KEY, c2 INT NOT NULL);
INSERT INTO test_t3 SELECT c1, COUNT(*) FROM test_t2 GROUP BY c1;
SELECT c1, ROW_NUMBER() OVER() FROM (SELECT c1 FROM test_t3 WHERE c2 IS NOT NULL);
