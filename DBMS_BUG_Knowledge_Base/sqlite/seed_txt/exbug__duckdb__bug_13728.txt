CREATE TABLE t2 (a INTEGER, b TEXT, c REAL, d INTEGER);
INSERT INTO t2 VALUES (1, 'test', 3.14, 1), (2, 'example', NULL, 0);
CREATE TEMP TABLE unpivoted AS SELECT a, b FROM t2 UNION ALL SELECT a, c FROM t2 WHERE c IS NOT NULL UNION ALL SELECT a, d FROM t2 WHERE d IS NOT NULL;
SELECT * FROM unpivoted;
