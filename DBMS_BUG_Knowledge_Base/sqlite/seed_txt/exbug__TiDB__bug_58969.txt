CREATE TABLE t2 ( x INTEGER DEFAULT NULL, y TEXT DEFAULT NULL, z DATETIME DEFAULT NULL, UNIQUE (x), UNIQUE (y) );
INSERT INTO t2 (x, y, z) VALUES (5, 'test', CURRENT_TIMESTAMP);
INSERT INTO t2 (x, y, z) VALUES (15, 'example', CURRENT_TIMESTAMP);
SELECT * FROM t2 WHERE y = 'example';
SELECT * FROM t2 WHERE x = 5;
