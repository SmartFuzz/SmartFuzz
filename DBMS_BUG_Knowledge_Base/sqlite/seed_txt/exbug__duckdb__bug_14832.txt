CREATE TABLE test_strings (id INTEGER PRIMARY KEY, str_value TEXT NOT NULL);
INSERT INTO test_strings (id, str_value) VALUES (1, 'abc'), (2, 'def'), (3, NULL);
SELECT id, str_value, (CASE WHEN 'abc' LIKE '%' || 'bc' THEN 1 ELSE 0 END) AS ends_with_bc FROM test_strings WHERE str_value IS NOT NULL;
SELECT id FROM test_strings WHERE str_value IS NULL;
SELECT id, str_value FROM test_strings WHERE str_value LIKE '%c';
