CREATE TABLE t2 (id INTEGER NOT NULL PRIMARY KEY, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE VIEW v2 AS SELECT id, created_at FROM t2 WHERE created_at IS NOT NULL;
INSERT INTO t2 (id) VALUES (1);
SELECT * FROM v2 WHERE created_at IS NULL;
CREATE TABLE t3 (event_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP, description TEXT);
