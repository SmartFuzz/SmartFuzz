CREATE TABLE employee (emp_id INTEGER PRIMARY KEY, emp_name TEXT, emp_age INTEGER, emp_salary REAL);
CREATE UNIQUE INDEX emp_idx ON employee (emp_name, emp_age);
INSERT INTO employee (emp_id, emp_name, emp_age, emp_salary) VALUES (1, 'Alice', 30, 70000.00);
SELECT name, age FROM employee WHERE emp_id = 1;
CREATE TABLE department (dept_id INTEGER PRIMARY KEY, dept_name TEXT, budget REAL);
