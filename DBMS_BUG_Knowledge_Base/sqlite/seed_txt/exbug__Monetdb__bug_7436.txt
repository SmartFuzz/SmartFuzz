CREATE TABLE v1 ( id INTEGER PRIMARY KEY, value TEXT NOT NULL );
INSERT INTO v1 (id, value) VALUES (1, 'test'), (2, NULL), (3, 'example');
CREATE TABLE v2 ( a REAL, b INTEGER DEFAULT 0, c DATE );
INSERT INTO v2 (a, b, c) VALUES (NULL, 1, '2023-01-01'), (5.5, 2, NULL), (3.14, NULL, '2023-01-03');
DELETE FROM v2 WHERE ( a IS NULL OR ( SELECT COUNT(*) FROM (SELECT a, RANK() OVER (PARTITION BY b ORDER BY a) AS rnk FROM v2) WHERE rnk < 2 ) );
