DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INTEGER NOT NULL, value REAL, category TEXT NOT NULL);
INSERT INTO test_table (id, value, category) VALUES (1, 10.50, 'A'), (2, NULL, 'B'), (3, 20.00, 'A'), (4, 30.00, 'C');
SELECT value FROM (SELECT value, category, id, ROW_NUMBER() OVER (PARTITION BY category ORDER BY id) AS rn FROM test_table) WHERE rn = 1 OR rn = (SELECT COUNT(*) FROM test_table t2 WHERE t2.category = test_table.category);
SELECT value FROM (SELECT value, category, id, ROW_NUMBER() OVER (PARTITION BY category ORDER BY id) AS rn FROM test_table) WHERE rn = 1 OR rn = (SELECT COUNT(*) FROM test_table t2 WHERE t2.category = test_table.category);
