CREATE TABLE test_strings (id INTEGER PRIMARY KEY, name TEXT NOT NULL);
INSERT INTO test_strings SELECT i, repeat('abc', 1000) FROM (SELECT rowid AS i FROM sqlite_sequence LIMIT 100);
CREATE TABLE test_numbers (num INTEGER, name TEXT);
INSERT INTO test_numbers SELECT i, repeat('xyz', 200) FROM (SELECT rowid AS i FROM sqlite_sequence LIMIT 50);
SELECT t1.id, t2.name FROM test_strings t1 JOIN test_numbers t2 ON t1.id = t2.num WHERE t1.name IS NOT NULL;
