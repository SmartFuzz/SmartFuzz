CREATE TABLE t3(id INTEGER DEFAULT NULL, name TEXT, UNIQUE(index_on_id));
INSERT INTO t3 VALUES (NULL, 'Alice'), (NULL, 'Bob'), (1, 'Charlie');
VACUUM;
-- SQLite does not have ANALYZE TABLE, but VACUUM can help with statistics.;
EXPLAIN QUERY PLAN SELECT COUNT(*) FROM t3 WHERE id IS NULL;
SELECT COUNT(*) FROM t3 WHERE name IS NOT NULL;
