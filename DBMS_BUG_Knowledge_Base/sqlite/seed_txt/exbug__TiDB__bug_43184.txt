CREATE TABLE test_D (id INTEGER PRIMARY KEY, b TEXT NOT NULL, c REAL DEFAULT 0.0);
CREATE TABLE test_E (id INTEGER, d DATE, FOREIGN KEY (id) REFERENCES test_D(id));
INSERT INTO test_D (id, b) VALUES (1, 'test1'), (2, 'test2');
INSERT INTO test_E (id, d) VALUES (1, '2023-01-01'), (2, NULL);
SELECT a.id, a.b, COALESCE(b.d, 'No Date') AS d_date FROM test_D a LEFT JOIN test_E b ON a.id = b.id;
