CREATE TABLE test_gist (p TEXT NOT NULL, t TEXT DEFAULT 'default' CHECK (length(t) > 0));
CREATE INDEX test_gist_idx ON test_gist (p);
INSERT INTO test_gist (p, t) VALUES ('(1, 2)', NULL), ('(3, 4)', 'text'), ('(5, 6)', '');
SELECT * FROM test_gist WHERE t IS NOT NULL AND length(t) > 0;
