CREATE TABLE source (id INTEGER PRIMARY KEY, value TEXT);
INSERT INTO source VALUES (1, 'test');
CREATE TABLE dest (id INTEGER PRIMARY KEY, value TEXT, extra INTEGER DEFAULT 0);
INSERT INTO dest (id, value, extra) SELECT id, value, 0 FROM source;
SELECT * FROM dest;
