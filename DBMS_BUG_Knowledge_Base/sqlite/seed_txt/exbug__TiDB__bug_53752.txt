CREATE TABLE departments (id INTEGER NOT NULL PRIMARY KEY, name TEXT NOT NULL, budget REAL NOT NULL);
INSERT INTO departments (id, name, budget) VALUES (1, 'HR', 100000.00), (2, 'Engineering', 250000.00), (3, 'Sales', 150000.00);
CREATE TABLE employees (id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, fname TEXT NOT NULL, lname TEXT NOT NULL, store_id INTEGER NOT NULL, department_id INTEGER NOT NULL, FOREIGN KEY (department_id) REFERENCES departments(id));
INSERT INTO employees (fname, lname, store_id, department_id) VALUES ('Alice', 'Smith', 1, 1), ('Bob', 'Johnson', 2, 2), ('Charlie', 'Brown', 3, 3);
SELECT * FROM employees;
