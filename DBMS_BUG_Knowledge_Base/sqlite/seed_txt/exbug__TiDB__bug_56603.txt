CREATE TABLE test1(a INTEGER, b TEXT, c DATE);
INSERT INTO test1(a, b, c) VALUES (1, 'test1', '2023-01-01'), (2, 'test2', NULL), (3, 'test3', '2023-01-03');
CREATE TABLE test2(d INTEGER, e REAL DEFAULT 0.0);
INSERT INTO test2(d, e) VALUES (1, 1.1), (2, NULL), (3, 3.3);
WITH RECURSIVE CTE(x) AS (SELECT a FROM test1 WHERE b IS NOT NULL GROUP BY a), CTE1(x) AS (SELECT a FROM test1 UNION ALL SELECT CTE.x FROM CTE JOIN CTE1 ON CTE.x = CTE1.x) SELECT * FROM CTE1;
