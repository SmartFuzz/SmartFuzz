CREATE TABLE test_intervals (id INTEGER PRIMARY KEY AUTOINCREMENT, interval_col TEXT, description TEXT);
INSERT INTO test_intervals (interval_col, description) VALUES ('1-2', 'Valid interval');
INSERT INTO test_intervals (interval_col, description) VALUES (NULL, 'NULL interval');
SELECT id, interval_col FROM test_intervals WHERE interval_col IS NOT NULL;
SELECT id, '2-3' AS new_interval FROM test_intervals;
