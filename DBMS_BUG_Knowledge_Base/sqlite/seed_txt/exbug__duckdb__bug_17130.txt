CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table VALUES (1, 'Alice', NULL);
CREATE VIEW test_view AS SELECT ROWID AS view_rowid, id, name FROM test_table WHERE id IS NOT NULL;
SELECT * FROM test_view;
PRAGMA table_info(test_view);
