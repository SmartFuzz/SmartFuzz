CREATE TABLE test_blob(COL1 BLOB DEFAULT NULL, COL2 TEXT DEFAULT NULL);
INSERT INTO test_blob VALUES(X'FF'), (X'7F'), (X'F9'), (X'FA'), (NULL);
SELECT COL1, LOWER(COL1) FROM test_blob WHERE LOWER(COL1) NOT LIKE LOWER(X'FF');
SELECT COL1, LOWER(COL1) FROM test_blob WHERE COL2 IS NULL;
SELECT COL1 FROM test_blob WHERE LOWER(COL1) LIKE LOWER(X'7F');
