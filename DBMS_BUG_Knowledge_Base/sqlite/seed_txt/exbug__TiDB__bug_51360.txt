CREATE TABLE tt4(x int, y int, z int AS (x * y), w TEXT DEFAULT 'default_value');
INSERT INTO tt4 (x, y) VALUES (1, 2), (NULL, 3), (4, NULL);
SELECT * FROM tt4 WHERE z > 0 LIMIT 3;
CREATE TABLE tt5(a REAL, b int, c DATETIME DEFAULT CURRENT_TIMESTAMP, d int AS (CAST(a AS INTEGER) + b));
