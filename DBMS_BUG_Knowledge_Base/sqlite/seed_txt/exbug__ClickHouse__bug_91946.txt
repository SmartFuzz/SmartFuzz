CREATE TABLE users_extended (uid INTEGER, name TEXT, age INTEGER, deleted INTEGER, version INTEGER, salary REAL);
INSERT INTO users_extended VALUES (1, 'John', 33, 0, 1, 50000.0);
INSERT INTO users_extended VALUES (2, 'Ksenia', 48, 0, 1, 60000.0);
SELECT AVG(salary) FROM users_extended WHERE age > 30 AND deleted = 0 GROUP BY age;
