CREATE TABLE test_table (id INTEGER PRIMARY KEY, value REAL, description TEXT);
INSERT INTO test_table (id, value, description) VALUES (1, random(), 'First entry'), (2, random(), 'Second entry'), (3, random(), 'Third entry');
CREATE TEMPORARY FUNCTION random_value() RETURNS REAL AS (SELECT random());
SELECT random_value();
SELECT (SELECT random()) AS random_result, id FROM test_table;
