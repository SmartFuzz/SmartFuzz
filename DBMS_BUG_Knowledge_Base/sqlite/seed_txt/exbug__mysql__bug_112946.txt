CREATE TABLE test_user_table_v2 (id INTEGER PRIMARY KEY AUTOINCREMENT, insert_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, user TEXT NOT NULL, action TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active', 'inactive')));
CREATE TABLE mysql_user_table_v2 (id INTEGER PRIMARY KEY AUTOINCREMENT, insert_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, user TEXT NOT NULL, action TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active', 'inactive')));
INSERT INTO test_user_table_v2(user, action, status) VALUES (CURRENT_USER(),'logged in', 'active');
INSERT INTO mysql_user_table_v2(user, action, status) VALUES (CURRENT_USER(),'logged in', 'active');
ALTER TABLE test_user_table_v2 ADD COLUMN uh_oh INTEGER DEFAULT NULL;
