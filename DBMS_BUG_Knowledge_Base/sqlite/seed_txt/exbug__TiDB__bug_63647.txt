CREATE TABLE t1(id INTEGER PRIMARY KEY, name TEXT, active INTEGER);
INSERT INTO t1 VALUES (1, 'Alice', 1), (2, 'Bob', 0);
SELECT NULL AS x;
SELECT 1 AS y;
SELECT (CASE WHEN NULLIF(x, t1.id) IS NOT NULL THEN 1 ELSE t1.active END) FROM t1 WHERE t1.id = 1;
