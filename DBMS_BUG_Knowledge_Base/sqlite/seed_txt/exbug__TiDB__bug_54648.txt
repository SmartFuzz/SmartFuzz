CREATE TABLE test_table1 ( id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP );
INSERT INTO test_table1 (name) VALUES ('Alice'), ('Bob'), ('Charlie'), ('David'), ('Eve');
CREATE TABLE test_table2 ( ref_id INTEGER NOT NULL, description TEXT, amount REAL NOT NULL, FOREIGN KEY (ref_id) REFERENCES test_table1(id) );
INSERT INTO test_table2 (ref_id, description, amount) VALUES (1, 'Payment for services', 150.00), (2, 'Refund', -50.00), (3, 'Donation', 200.00);
SELECT name, (SELECT COUNT(*) FROM test_table2 WHERE ref_id = test_table1.id) AS transaction_count FROM test_table1 WHERE id < 3 LIMIT 1;
