CREATE TABLE test_table ( id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK (age >= 0) );
INSERT INTO test_table VALUES (1, 'Alice', 30);
CREATE TABLE another_table ( ref_id INTEGER, score REAL CHECK (score BETWEEN 0 AND 100), FOREIGN KEY (ref_id) REFERENCES test_table(id) );
INSERT INTO another_table VALUES (1, 95.5);
SELECT * FROM test_table WHERE age IS NOT NULL AND id IN (SELECT ref_id FROM another_table WHERE score > 90);
