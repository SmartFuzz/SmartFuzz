CREATE TABLE parent (key INTEGER PRIMARY KEY, name TEXT);
CREATE TABLE child (parent_key INTEGER, value REAL NOT NULL, FOREIGN KEY (parent_key) REFERENCES parent (key) ON DELETE CASCADE);
INSERT INTO parent (key, name) VALUES (1, 'Parent 1');
INSERT INTO child (parent_key, value) VALUES (1, 10.5);
DELETE FROM parent WHERE key = 1;
SELECT * FROM child;
