CREATE TABLE sales (year INTEGER NOT NULL, country TEXT NOT NULL, product TEXT NOT NULL, profit INTEGER DEFAULT 0, PRIMARY KEY (year, country, product));
INSERT INTO sales (year, country, product, profit) VALUES (2020, 'USA', 'Gadget', 1500), (2020, 'USA', 'Widget', 2000), (2021, 'Canada', 'Gadget', 2500);
SELECT year, country, product, CASE WHEN year IS NULL OR country IS NULL OR product IS NULL THEN 1 ELSE 0 END AS grouping FROM sales GROUP BY year, country, product;
CREATE TABLE products (product_id INTEGER NOT NULL PRIMARY KEY, product_name TEXT NOT NULL);
INSERT INTO products (product_id, product_name) VALUES (1, 'Gadget'), (2, 'Widget');
