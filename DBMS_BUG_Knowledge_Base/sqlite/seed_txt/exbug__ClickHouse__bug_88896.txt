DROP TABLE IF EXISTS source_table;
CREATE TABLE source_table (date TEXT, value INTEGER);
CREATE TABLE mv_table (date TEXT, number INTEGER);
INSERT INTO mv_table (date, number) SELECT date, value FROM source_table WHERE value IS NOT NULL;
CREATE USER user_test_fuzz;
