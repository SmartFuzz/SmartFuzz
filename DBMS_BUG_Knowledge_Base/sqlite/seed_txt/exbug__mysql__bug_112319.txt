CREATE TABLE account (id INTEGER PRIMARY KEY, global_account_id INTEGER NOT NULL, latest_version_id INTEGER);
CREATE TABLE account_version (id INTEGER PRIMARY KEY, account_type_id INTEGER NOT NULL, global_account_id INTEGER NOT NULL);
CREATE TABLE account_type (id INTEGER PRIMARY KEY, type TEXT NOT NULL, description TEXT);
INSERT INTO account VALUES (1, 1, 1), (2, 2, 4), (3, 2, 6);
INSERT INTO account_version VALUES (1, 1, 1), (4, 2, 2), (6, 3, 2);
INSERT INTO account_type VALUES (1, 'customer', 'Customer'), (2, 'manager', 'Manager'), (3, 'other', NULL);
