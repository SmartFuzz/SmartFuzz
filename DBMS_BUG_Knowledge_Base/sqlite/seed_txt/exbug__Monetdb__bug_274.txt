CREATE TABLE a (id INTEGER PRIMARY KEY, value TEXT);
INSERT INTO a (id, value) VALUES (1, 'test1');
CREATE TABLE b (id INTEGER, value TEXT NOT NULL);
INSERT INTO b (id, value) VALUES (1, 'test2'), (2, 'default_value');
CREATE VIEW aview AS SELECT id, value FROM a WHERE id > 0;
