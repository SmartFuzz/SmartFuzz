{
  "url": "https://www.sqlite.org/src/tktview/e6f1f2e34d",
  "title": "REPLACE can insert a default NULL into a NOT NULL column",
  "vulnerable_version": "3.26.0",
  "date": "2018-12-22 00:10:516.64 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_e6f1f2e34d.json",
  "poc": [
    "CREATE TABLE t1(x NOT NULL DEFAULT NULL);",
    "REPLACE INTO t1 DEFAULT VALUES;",
    "SELECT quote(x) FROM t1;"
  ],
  "vulnerable_database": "sqlite",
  "description": "drh added on 2018-12-22 00:10:51:\nThe REPLACE statement is able to put a NULL value into a NOT NULL column.\nIf the DEFAULT value for a NOT NULL column is NULL, and if you try to insert a NULL,\nthen the inserted NULL is detected and the \"REPLACE\" algorithm kicks in and\nsubstitutes the default value.  But if the default is also NULL, the second\nNULL is never detected.  For example:\nCREATE TABLE t1(x NOT NULL DEFAULT NULL);\nREPLACE INTO t1 DEFAULT VALUES;\nSELECT quote(x) FROM t1;\nThis bug has existed in the code since before SQLite version 3.1.0 (2005-01-21).\nThe problem was detected by the Chromium developers using a fuzzer.",
  "key_bug_features": [
    "REPLACE inserts NULL into NOT NULL column with NULL default"
  ],
  "bug_features_seed": [
    "CREATE TABLE t2(y INTEGER NOT NULL DEFAULT NULL);",
    "REPLACE INTO t2 DEFAULT VALUES;",
    "SELECT quote(y) FROM t2;",
    "CREATE TABLE t3(z TEXT NOT NULL DEFAULT NULL);",
    "REPLACE INTO t3 VALUES(NULL);"
  ]
}