{
  "url": "https://www.sqlite.org/src/tktview/1a1308d253",
  "title": "Nested correlated subquery fails when indices used",
  "vulnerable_version": "",
  "date": "2011-09-16 16:20:3613.90 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_1a1308d253.json",
  "poc": [],
  "vulnerable_database": "sqlite",
  "description": "未找到User Comments标签",
  "key_bug_features": [
    "Nested correlated subquery with indices fails"
  ],
  "bug_features_seed": [
    "CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
    "CREATE TABLE comments (id INTEGER PRIMARY KEY, user_id INTEGER, content TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(user_id) REFERENCES users(id));",
    "INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);",
    "INSERT INTO comments (user_id, content) VALUES (1, 'First comment'), (2, 'Second comment'), (1, 'Third comment');",
    "SELECT u.name, (SELECT COUNT(*) FROM comments c WHERE c.user_id = u.id) AS comment_count FROM users u WHERE u.age > (SELECT AVG(age) FROM users) ORDER BY comment_count DESC;"
  ]
}