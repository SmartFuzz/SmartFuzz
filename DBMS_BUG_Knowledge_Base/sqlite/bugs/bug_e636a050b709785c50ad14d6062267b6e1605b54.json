{
  "url": "https://www.sqlite.org/src/tktview/e636a050b7",
  "title": "Database corruption in shared cache mode",
  "vulnerable_version": "3.7.16.2",
  "date": "2013-05-13 18:00:0012.25 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_e636a050b7.json",
  "poc": [],
  "vulnerable_database": "sqlite",
  "description": "drh added on 2013-05-13 18:00:00:\nWhen two database connections share the same cache and one connection is in \nthe middle of a write transaction and the other connection closes (with\nsqlite3_close) or rolls back a transaction on a different attached database,\nand the first connection continues to make changes to the database file and\nthen commits, the first connection can cause the database file to go corrupt.",
  "key_bug_features": [
    "Shared cache mode with concurrent write transactions"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT NOT NULL, value REAL DEFAULT 0.0);",
    "INSERT INTO test_table (name, value) VALUES ('Test1', 1.5);",
    "BEGIN TRANSACTION;",
    "UPDATE test_table SET value = value + 2.5 WHERE id = 1;",
    "SELECT * FROM test_table WHERE value IS NOT NULL;",
    "ROLLBACK;"
  ]
}