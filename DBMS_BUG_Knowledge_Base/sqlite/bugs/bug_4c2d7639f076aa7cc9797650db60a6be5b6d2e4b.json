{
  "url": "https://www.sqlite.org/src/tktview/4c2d7639f0",
  "title": "CAST('-' AS NUMERIC) computes 0.0",
  "vulnerable_version": "",
  "date": "2019-05-25 10:06:106.21 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_4c2d7639f0.json",
  "poc": [
    "SELECT CAST('-' AS NUMERIC);",
    "SELECT CAST('-0' AS NUMERIC);",
    "SELECT CAST('+' AS NUMERIC);",
    "SELECT CAST('/' AS NUMERIC);"
  ],
  "vulnerable_database": "sqlite",
  "description": "mrigger added on 2019-05-25 10:06:10:\nConsider the following example:\nSELECT CAST('-' AS NUMERIC); -- unexpected: computes 0.0\nI would expect that 0 is computed by the expression, as, for example, in the expressions below:\nSELECT CAST('-0' AS NUMERIC); -- 0\nSELECT CAST('+' AS NUMERIC); -- 0\nSELECT CAST('/' AS NUMERIC); -- 0",
  "key_bug_features": [
    "CAST of negative string computes unexpected numeric value"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, value TEXT NOT NULL);",
    "INSERT INTO test_table (value) VALUES ('-'), ('-0'), ('+'), ('/');",
    "SELECT id, CAST(value AS NUMERIC) AS numeric_value FROM test_table;",
    "SELECT id FROM test_table WHERE CAST(value AS NUMERIC) IS NULL;",
    "SELECT id, value FROM test_table WHERE CAST(value AS NUMERIC) = 0.0;"
  ]
}