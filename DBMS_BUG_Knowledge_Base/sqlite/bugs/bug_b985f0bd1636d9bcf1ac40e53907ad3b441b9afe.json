{
  "url": "https://www.sqlite.org/src/tktview/b985f0bd16",
  "title": "Assertion `0' failed in  sqlite3IsLikeFunction",
  "vulnerable_version": "3.31.0",
  "date": "2020-01-22 21:49:065.55 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_b985f0bd16.json",
  "poc": [
    "create table a(b);",
    "EXPLAIN SELECT * FROM a WHERE x(1);"
  ],
  "vulnerable_database": "sqlite",
  "description": "yongheng added on 2020-01-22 21:49:06:\nConsider the following test case:\n---\ncreate table a(b);\nEXPLAIN SELECT * FROM a WHERE x(1);\n---\n\nIt triggers assertion in debug build of latest chunk.\ndrh added on 2020-01-22 23:13:45:\nNotes:\nThis only comes up when compiled with -DSQLITE_ENABLE_UNKNOWN_SQL_FUNCTION\nThis only comes up when compiled with -DSQLITE_DEBUG.  Release builds are\n     unaffected.\nSQLite functions correctly, exhibits no UB, and gives the correct answer\n     if the assert() is simply removed.\nThe resolution was to fix the assert() so that it works correctly even if\n     -DSQLITE_ENABLE_UNKNOWN_SQL_FUNCTION is defined.",
  "key_bug_features": [
    "EXPLAIN SELECT with unknown SQL function causes assertion failure"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table(id INTEGER PRIMARY KEY, name TEXT, value REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table(name, value) VALUES('test1', 1.5);",
    "INSERT INTO test_table(name, value) VALUES(NULL, 2.5);",
    "EXPLAIN SELECT * FROM test_table WHERE unknown_function(value);",
    "SELECT * FROM test_table WHERE id IN (SELECT id FROM test_table WHERE value IS NULL);"
  ]
}