{
  "url": "https://www.sqlite.org/src/tktview/e4b8a2ba6e",
  "title": "Problem with DISTINCT in sub-queries in a FROM clause.",
  "vulnerable_version": "3.7.1",
  "date": "2010-08-13 16:19:3914.99 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_e4b8a2ba6e.json",
  "poc": [],
  "vulnerable_database": "sqlite",
  "description": "未找到User Comments标签",
  "key_bug_features": [
    "DISTINCT in sub-queries in a FROM clause"
  ],
  "bug_features_seed": [
    "CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER, email TEXT UNIQUE);",
    "INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');",
    "INSERT INTO users (name, age, email) VALUES ('Bob', NULL, 'bob@example.com');",
    "SELECT DISTINCT name FROM (SELECT name FROM users WHERE age IS NOT NULL) AS subquery;",
    "SELECT DISTINCT age FROM (SELECT age FROM users WHERE email LIKE '%@example.com') AS subquery;"
  ]
}