{
  "url": "https://www.sqlite.org/src/tktview/412bba9b22",
  "title": "CAST('.' AS NUMERIC) computes 0.0 rather than 0",
  "vulnerable_version": "",
  "date": "2019-06-10 19:53:546.17 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_412bba9b22.json",
  "poc": [
    "SELECT -'.';",
    "with a floating-point number when used with the unary minus. This is also the case for other arithmetic operators (e.g., see below).\nSELECT '.' + 0;",
    "SELECT -CAST('.' AS NUMERIC);"
  ],
  "vulnerable_database": "sqlite",
  "description": "mrigger added on 2019-06-10 19:53:54:\nConsider the following statement:\nSELECT -'.'; -- expected: 0, actual: 0.0\nIt seems that the '.' is confused with a floating-point number when used with the unary minus. This is also the case for other arithmetic operators (e.g., see below).\nSELECT '.' + 0; -- expected: 0, actual: 0.0\nThis is unexpected, since according to https://www.sqlite.org/syntax/numeric-literal.html, the '.' should be followed by at least one digit to be a valid number.\nPreviously, this worked as expected and still works as expected when casting '.' to NUMERIC:\nSELECT -CAST('.' AS NUMERIC); -- 0\nSee [4c2d7639f0], where CAST('-' AS NUMERIC) previously computed 0.0.",
  "key_bug_features": [
    "CASTing '.' as NUMERIC yields unexpected results"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, value NUMERIC, description TEXT);",
    "INSERT INTO test_table (value, description) VALUES (CAST('.' AS NUMERIC), 'Test case for dot as numeric');",
    "SELECT -value FROM test_table WHERE id = 1;",
    "SELECT value + 0 FROM test_table WHERE description LIKE 'Test%';",
    "SELECT -CAST('.' AS NUMERIC) AS result;"
  ]
}