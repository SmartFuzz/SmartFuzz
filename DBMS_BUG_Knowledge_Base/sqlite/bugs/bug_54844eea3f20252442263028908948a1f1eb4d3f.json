{
  "url": "https://www.sqlite.org/src/tktview/54844eea3f",
  "title": "Incorrect caching of sub-query results in the FROM clause of a scalar sub-query.",
  "vulnerable_version": "3.7.7",
  "date": "2011-07-08 15:49:5214.09 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_54844eea3f.json",
  "poc": [],
  "vulnerable_database": "sqlite",
  "description": "未找到User Comments标签",
  "key_bug_features": [
    "Caching of sub-query results in FROM clause"
  ],
  "bug_features_seed": [
    "CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO users (name, age) VALUES ('Alice', 30);",
    "INSERT INTO users (name, age) VALUES ('Bob', NULL);",
    "SELECT (SELECT COUNT(*) FROM users WHERE age IS NOT NULL) AS valid_user_count, (SELECT COUNT(*) FROM users) AS total_user_count;",
    "SELECT name FROM users WHERE id IN (SELECT id FROM users WHERE age > 25);"
  ]
}