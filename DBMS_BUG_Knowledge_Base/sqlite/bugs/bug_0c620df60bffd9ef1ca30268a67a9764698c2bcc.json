{
  "url": "https://www.sqlite.org/src/tktview/0c620df60b",
  "title": "LIKELY(), UNLIKELY() and LIKELIHOOD() have affinities",
  "vulnerable_version": "",
  "date": "2019-06-11 19:24:146.17 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_0c620df60b.json",
  "poc": [
    "SELECT LIKELY(CAST(1 AS INT)) = '1';",
    "SELECT UNLIKELY(CAST(1 AS INT)) = '1';",
    "SELECT LIKELIHOOD(CAST(1 AS INT), 0.5) = '1';"
  ],
  "vulnerable_database": "sqlite",
  "description": "mrigger added on 2019-06-11 19:24:14:\nConsider the statements below:\nSELECT LIKELY(CAST(1 AS INT)) = '1'; -- expected: 0, actual: 1\nSELECT UNLIKELY(CAST(1 AS INT)) = '1'; -- expected: 0, actual: 1\nSELECT LIKELIHOOD(CAST(1 AS INT), 0.5) = '1'; -- expected: 0, actual: 1\nAll statements return 1, which is somehow unexpected, since functions are expected to have no affinity; however, it seems that for these functions, the affinity of the function argument is assumed (for LIKELIHOOD, the affinity of the first argument is taken).\nThis bug report is similar to [d60b3cd7cb], where it was reported that the COLLATE operator takes the affinity of the expression applied to, and resulted in a documentation update.",
  "key_bug_features": [
    "Functions assume argument affinity unexpectedly"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, value TEXT, score REAL);",
    "INSERT INTO test_table (value, score) VALUES ('1', 0.5);",
    "SELECT LIKELY(CAST(score AS INT)) = '1' FROM test_table WHERE id = 1;",
    "SELECT UNLIKELY(CAST(value AS INT)) = '1' FROM test_table WHERE id = 1;",
    "SELECT LIKELIHOOD(CAST(value AS INT), 0.5) = '1' FROM test_table WHERE id = 1;"
  ]
}