{
  "url": "https://www.sqlite.org/src/tktview/f74beaabde",
  "title": "Problem with 3-way joins and the USING clause",
  "vulnerable_version": "3.6.21",
  "date": "2009-12-09 16:24:2915.67 years ago",
  "path": "/home/crawl_bugs/sqlite/ticket_output/ticket_info_f74beaabde.json",
  "poc": [],
  "vulnerable_database": "sqlite",
  "description": "未找到User Comments标签",
  "key_bug_features": [
    "3-way joins with USING clause"
  ],
  "bug_feature_seed": [
    "CREATE TABLE t1(a INTEGER, b INTEGER);",
    "CREATE TABLE t2(a INTEGER, c INTEGER);",
    "CREATE TABLE t3(a INTEGER, d INTEGER);",
    "INSERT INTO t1 VALUES(1,10),(2,20);",
    "INSERT INTO t2 VALUES(1,100),(3,300);",
    "INSERT INTO t3 VALUES(1,1000),(2,2000);",
    "SELECT * FROM t1 JOIN t2 USING(a) JOIN t3 USING(a);"
  ],
  "bug_features_seed": [
    "CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER);",
    "CREATE TABLE posts (id INTEGER PRIMARY KEY, user_id INTEGER, content TEXT, FOREIGN KEY(user_id) REFERENCES users(id));",
    "CREATE TABLE comments (id INTEGER PRIMARY KEY, post_id INTEGER, user_id INTEGER, comment TEXT, FOREIGN KEY(post_id) REFERENCES posts(id), FOREIGN KEY(user_id) REFERENCES users(id));",
    "INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', 25), ('Charlie', NULL);",
    "SELECT u.name, p.content, c.comment FROM users u JOIN posts p USING (user_id) JOIN comments c USING (post_id) WHERE u.age IS NOT NULL;"
  ]
}