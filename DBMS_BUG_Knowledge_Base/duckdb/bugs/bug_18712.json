{
  "url": "https://github.com/duckdb/duckdb/issues/18712",
  "title": "`query` table function fails on \"Single SELECT statement\"",
  "vulnerable_database": "DuckDB",
  "date": "2025-08-24 11:38:20",
  "vulnerable_version": "",
  "description": "### What happens?\n\n```sql\nFROM query('SELECT * FROM (PIVOT (SELECT 1 AS col) ON col using first(col))');\n```\n```\n Expected a single SELECT statement\n```\n\nSince I *am* passing a single `SELECT` statement to `query`, either the error message needs fixing or the functionality needs fixing.\n\n### To Reproduce\n\n.\n\n### OS:\n\nLinux\n\n### DuckDB Version:\n\n1.4\n\n### DuckDB Client:\n\n.\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nSoeren Wolfers\n\n### Affiliation:\n\nG-Research\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a nightly build\n\n### Did you include all relevant data sets for reproducing the issue?\n\nNot applicable - the reproduction does not require a data set\n\n### Did you include all code required to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [x] Yes, I have",
  "poc": [
    "SELECT * FROM (PIVOT (SELECT 1 AS col) ON col using first(col))');"
  ],
  "key_bug_features": [
    "query function fails with single SELECT statement"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table (id, value) VALUES (1, 'test'), (2, NULL), (3, 'example');",
    "SELECT * FROM (SELECT id, value FROM test_table WHERE value IS NOT NULL) AS filtered;",
    "SELECT * FROM (PIVOT (SELECT id, value FROM test_table) ON value USING FIRST(value));",
    "SELECT COUNT(*) FROM (SELECT DISTINCT id FROM test_table WHERE id > 0) AS unique_ids;"
  ]
}