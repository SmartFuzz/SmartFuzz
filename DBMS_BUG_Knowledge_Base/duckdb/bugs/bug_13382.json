{
  "url": "https://github.com/duckdb/duckdb/issues/13382",
  "title": "Confusing error message when creating a MACRO to parameterize the argument of read_csv_auto()",
  "vulnerable_database": "DuckDB",
  "date": "2024-08-11 20:07:45",
  "vulnerable_version": "",
  "description": "### What happens?\r\n\r\nThe SQL command:\r\n```\r\nCREATE MACRO read(fn) AS (SELECT * FROM read_csv_auto(fn));\r\n```\r\nresults in a Binder Error:\r\n```\r\nTable function \"read_csv_auto\" does not support lateral join column parameters - cannot use column \"fn\" in this context\r\n```\r\nThe same issue arises with similar \"read\" functions, e.g. read_parquet.\r\n\r\nIt would be more helpful if the error message could suggest that the TABLE keyword is missing.\r\n\r\n\r\n### To Reproduce\r\n\r\n```\r\nCREATE MACRO read(fn) AS (SELECT * FROM read_csv_auto(fn));\r\n```\r\n\r\n### OS:\r\n\r\nMacOS\r\n\r\n### DuckDB Version:\r\n\r\nv1.0.1-dev4052\r\n\r\n### DuckDB Client:\r\n\r\nCLI\r\n\r\n### Full Name:\r\n\r\nPeter Koppstein\r\n\r\n### Affiliation:\r\n\r\nPrinceton University\r\n\r\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\r\n\r\nI have tested with a source build\r\n\r\n### Did you include all relevant data sets for reproducing the issue?\r\n\r\nNot applicable - the reproduction does not require a data set\r\n\r\n### Did you include all code required to reproduce the issue?\r\n\r\n- [X] Yes, I have\r\n\r\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\r\n\r\n- [X] Yes, I have",
  "poc": [
    "SELECT * FROM read_csv_auto(fn));",
    "SELECT * FROM range(fn));",
    "SELECT read(i) from (values (5), (10), (20)) as c(i);",
    "SELECT sum(i) FROM range(min, max) as r(i) ORDER BY 1\r\n);",
    "CREATE TABLE t1 (a INT, b INT);",
    "INSERT INTO t1 VALUES (4, 7), (6, 10);",
    "SELECT range_sum(a, b) FROM t1;"
  ],
  "key_bug_features": [
    "Creating MACRO with read_csv_auto() causes Binder Error"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_data (id INT, name VARCHAR, value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_data VALUES (1, 'Alice', 10.5, '2023-01-01 10:00:00'), (2, 'Bob', NULL, '2023-01-02 11:00:00'), (3, 'Charlie', 15.0, NULL);",
    "CREATE MACRO read(fn) AS (SELECT * FROM read_csv_auto(fn));",
    "SELECT * FROM test_data WHERE value IS NULL;",
    "SELECT read('data.csv');"
  ]
}