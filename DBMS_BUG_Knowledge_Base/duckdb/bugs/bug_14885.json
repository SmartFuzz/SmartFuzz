{
  "url": "https://github.com/duckdb/duckdb/issues/14885",
  "title": "Strange macro results",
  "vulnerable_database": "DuckDB",
  "date": "2024-11-18 20:39:47",
  "vulnerable_version": "",
  "description": "### What happens?\n\nI have this macro to calculate year quarter:\r\n\r\n```sql\r\ncreate or replace macro yearqtr(x) as \r\n(SELECT (((date_part('year', CAST(x AS DATE)) * 10) + ((date_part('month', CAST(x AS DATE)) - 1) // 3)) + 1));\r\n```\r\n\r\nNow, when i try to use this i get some very weird results for a particular date. Here's the query in question:\r\n\r\n```sql\r\nwith df as (select unnest(generate_series('2000-01-01'::date,'2099-12-31'::date,interval 1 day)::date[]) as dt) \r\nselect dt,yearqtr(dt) as yq,yearqtr('2025-12-31') as yq1,yearqtr(dt)//10 as yq2,yearqtr(dt)//3 as yq3,yearqtr(dt)%3 as yq4 from df where dt='2050-12-31';\r\n```\r\n\r\nWhat is evey weirder is that if you run this query a few times i get different values for `yq`, `yq2`, `yq3`! I suspect that this has got something to do with the `//` operator.\r\n\r\nI'm unsure if this strangeness is only for the date '2050-12-31' or i was unlucky enough to stumble upon this edge case.\n\n### To Reproduce\n\n```sql\r\ncreate or replace macro yearqtr(x) as \r\n(SELECT (((date_part('year', CAST(x AS DATE)) * 10) + ((date_part('month', CAST(x AS DATE)) - 1) // 3)) + 1));\r\n```\r\n\r\nNow run the following query a few times:\r\n\r\n```sql\r\nwith df as (select unnest(generate_series('2000-01-01'::date,'2099-12-31'::date,interval 1 day)::date[]) as dt) \r\nselect dt,yearqtr(dt) as yq,yearqtr('2025-12-31') as yq1,yearqtr(dt)//10 as yq2,yearqtr(dt)//3 as yq3,yearqtr(dt)%3 as yq4 from df where dt='2050-12-31';\r\n```\n\n### OS:\n\nmacos arm64 (23.4.0 Darwin Kernel Version 23.4.0)\n\n### DuckDB Version:\n\nv1.1.3 19864453f7\n\n### DuckDB Client:\n\nCLI\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nVijay Lulla\n\n### Affiliation:\n\nSweetwater\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have not tested with any build\n\n### Did you include all relevant data sets for reproducing the issue?\n\nNot applicable - the reproduction does not require a data set\n\n### Did you include all code required to reproduce the issue?\n\n- [X] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [X] Yes, I have",
  "poc": [
    "SELECT (((date_part('year', CAST(x AS DATE)) * 10) + ((date_part('month', CAST(x AS DATE)) - 1) // 3)) + 1));",
    "select unnest(generate_series('2000-01-01'::date,'2099-12-31'::date,interval 1 day)::date[]) as dt) \r\nselect dt,yearqtr(dt) as yq,yearqtr('2025-12-31') as yq1,yearqtr(dt)//10 as yq2,yearqtr(dt)//3 as yq3,yearqtr(dt)%3 as yq4 from df where dt='2050-12-31';"
  ],
  "key_bug_features": [
    "Inconsistent results using the '//' operator in macro"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_dates (id INTEGER PRIMARY KEY, dt DATE NOT NULL, value INTEGER);",
    "INSERT INTO test_dates (id, dt, value) VALUES (1, '2050-12-31', 100), (2, '2025-12-31', 200), (3, '2000-01-01', 300);",
    "CREATE OR REPLACE MACRO yearqtr(x) AS (SELECT (((date_part('year', CAST(x AS DATE)) * 10) + ((date_part('month', CAST(x AS DATE)) - 1) // 3)) + 1));",
    "SELECT dt, yearqtr(dt) AS yq, yearqtr(dt) // 10 AS yq2, yearqtr(dt) // 3 AS yq3, yearqtr(dt) % 3 AS yq4 FROM test_dates WHERE dt = '2050-12-31';",
    "SELECT dt, yearqtr(dt) AS yq, yearqtr(dt) // 10 AS yq2, yearqtr(dt) // 3 AS yq3, yearqtr(dt) % 3 AS yq4 FROM test_dates WHERE dt IS NOT NULL;"
  ]
}