{
  "url": "https://www.sqlite.org/src/tktview/acd1299088",
  "title": "CREATE TABLE AS fails with a false report of database corruption",
  "description": "drh added on 2015-09-09 12:34:14:\nThe second statement in the following script reports an SQLITE_CORRUPT error:\nCREATE TABLE t1(a);\nCREATE TABLE t2 AS SELECT name FROM sqlite_master;\nThe database is not actually corrupt.  This is a malfunction in the CREATE TABLE AS statement.\nBisecting indicates that this problem was introduced by check-in\n[972881c6d3] on 2013-11-21, first released with version 3.8.2.",
  "date": "2015-09-09 12:34:149.92 years ago",
  "vulnerable_version": "3.8.11.1",
  "poc_type": "sql_text",
  "vulnerable_database": "duckdb",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users(id INTEGER PRIMARY KEY, name VARCHAR NOT NULL, age INTEGER CHECK(age >= 0));",
    "INSERT INTO users(name, age) VALUES('Alice', 30);",
    "CREATE TABLE temp_users AS SELECT * FROM users WHERE age > 25;",
    "CREATE TABLE orders(order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DOUBLE NOT NULL);",
    "INSERT INTO orders(user_id, amount) VALUES((SELECT id FROM users WHERE name = 'Alice'), 99.99);"
  ],
  "key_bug_features": [
    "CREATE TABLE AS statement malfunction"
  ]
}