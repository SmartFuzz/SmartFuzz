{
  "url": "https://www.sqlite.org/src/tktview/8c63ff0eca",
  "title": "Incorrect query result on a UNION ALL",
  "description": "drh added on 2014-02-25 18:46:01:\nTemp registers can sometimes be used in concurrent co-routines, resulting in a\nincorrect answer.  The attached script demonstrations one manifestation of this.\nThe attachment fails with any check-in after the introduction of the NGQP.\nBut the register allocation problem predates that change.  The cut-over of the\nNGQP only makes the attached test case work.  Probably other test cases can be\nmade to fail for versions prior to the NGQP.",
  "date": "2014-02-25 18:46:0111.46 years ago",
  "vulnerable_version": "3.8.3.1",
  "poc_type": "sql_text",
  "vulnerable_database": "duckdb",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR, value DOUBLE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table (name, value) VALUES ('Alice', 10.5);",
    "INSERT INTO test_table (name, value) VALUES ('Bob', NULL);",
    "SELECT id, name, value FROM test_table WHERE value IS NOT NULL UNION ALL SELECT id, name, 0.0 FROM test_table WHERE value IS NULL;",
    "SELECT COUNT(*) FROM (SELECT id FROM test_table WHERE value > 5.0);"
  ],
  "key_bug_features": [
    "Concurrent co-routines using temp registers incorrectly"
  ]
}