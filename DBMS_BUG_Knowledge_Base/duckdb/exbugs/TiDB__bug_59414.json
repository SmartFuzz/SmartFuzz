{
  "url": "https://github.com/pingcap/tidb/issues/59414",
  "title": "`ROUND` has different result with MySQL for even positive precision",
  "description": "## Bug Report\n\nPlease answer these questions before submitting your issue. Thanks!\n\n### 1. Minimal reproduce step (Required)\n\n```\nSELECT ROUND(123.4, 5);\n```\n\nRef https://github.com/pingcap/tidb/issues/26993, which is about the negative precision.\n\n### 2. What did you expect to see? (Required)\n\n```\nmysql> SELECT ROUND(123.4, 5);\n+-----------------+\n| ROUND(123.4, 5) |\n+-----------------+\n|           123.4 |\n+-----------------+\n1 row in set (0.04 sec)\n```\n\n### 3. What did you see instead (Required)\n\n```\nmysql> SELECT ROUND(123.4, 5);\n+-----------------+\n| ROUND(123.4, 5) |\n+-----------------+\n|       123.40000 |\n+-----------------+\n1 row in set (0.00 sec)\n```\n\n### 4. What is your TiDB version? (Required)\n\n<!-- Paste the output of SELECT tidb_version() -->\n\n",
  "date": "2025-02-11T14:42:41Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "duckdb",
  "source_database": "TiDB",
  "bug_features_seed": [
    "CREATE TABLE test_rounding (id INTEGER PRIMARY KEY, value DECIMAL(10, 5));",
    "INSERT INTO test_rounding (id, value) VALUES (1, 123.4);",
    "SELECT ROUND(value, 5) FROM test_rounding WHERE id = 1;",
    "SELECT ROUND(value, 0) FROM test_rounding;",
    "SELECT ROUND(NULL, 5) AS rounded_value;"
  ],
  "key_bug_features": [
    "ROUND function with even positive precision produces different results"
  ]
}