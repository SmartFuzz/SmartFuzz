{
  "url": "https://github.com/MonetDB/MonetDB/issues/7016",
  "title": "Database crashes when use similarity function on a table with more than 200k records ",
  "description": "In we use the built-in similarity function in query sql and the nubmer of rows in table exceed 200K, the database crashes.\n\n**To Reproduce**\n-- Create a table named t_199999 and populated with 199999 rows;\nCREATE TABLE t_199999 (s1 string, s2 string);\n-- Create a table named t_200000 and populated with 200000 rows;\nCREATE TABLE t_200000 (s1 string, s2 string);\n\nexecute the sql query:\n-- this works well\nselect count(1) from t_199999 where similarity(s1, s2)> 0.5;  \n-- this crashes the database\nselect count(1) from t_200000 where similarity(s1, s2)> 0.5;  \n\n**Expected behavior**\nthe database should work well if we use similarity function even if the table row numbers exceed 200000.\n\n**Software versions**\n - MonetDB version number [x86_64-20201013]\n - OS and version: [both Window 7 and Centos 7]\n - Installed from release package",
  "date": "2020-12-05T06:14:42Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "duckdb",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE t_199999 (s1 VARCHAR, s2 VARCHAR, s3 INT CHECK (s3 >= 0));",
    "CREATE TABLE t_200000 (s1 VARCHAR, s2 VARCHAR, s3 INT CHECK (s3 >= 0));",
    "INSERT INTO t_199999 (s1, s2, s3) SELECT 'value' || i, 'value' || i, i FROM range(1, 199999) AS i;",
    "INSERT INTO t_200000 (s1, s2, s3) SELECT 'value' || i, 'value' || i, i FROM range(1, 200000) AS i;",
    "SELECT count(1) FROM t_200000 WHERE s1 IS NOT NULL AND s2 IS NOT NULL AND s3 IS NOT NULL;"
  ],
  "key_bug_features": [
    "similarity function on tables exceeding 200k records"
  ]
}