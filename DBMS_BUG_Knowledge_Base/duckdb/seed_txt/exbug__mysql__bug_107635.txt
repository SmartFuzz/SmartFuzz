CREATE TABLE test_events (id INTEGER PRIMARY KEY, event_name VARCHAR(255) NOT NULL, event_time TIMESTAMP NOT NULL, is_active BOOLEAN DEFAULT TRUE);
INSERT INTO test_events (event_name, event_time) VALUES ('Test Event 1', CURRENT_TIMESTAMP + INTERVAL '1 second');
CREATE TABLE scheduled_events (event_name VARCHAR(255) NOT NULL, event_time TIMESTAMP NOT NULL);
INSERT INTO scheduled_events (event_name, event_time) SELECT 'Scheduled Event', CURRENT_TIMESTAMP;
SELECT * FROM scheduled_events;
