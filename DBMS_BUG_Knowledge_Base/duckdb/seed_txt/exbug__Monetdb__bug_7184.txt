CREATE TABLE IF NOT EXISTS beta (id INTEGER PRIMARY KEY AUTOINCREMENT, value VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO beta (value) VALUES ('test1'), ('test2'), (NULL), ('test3');
CREATE TABLE IF NOT EXISTS gamma (id BIGINT PRIMARY KEY, ref_id BIGINT, amount DECIMAL(10,2) CHECK (amount >= 0));
INSERT INTO gamma (id, ref_id, amount) SELECT generate_series(1, 5), id, CAST(rand() * 100 AS DECIMAL(10,2)) FROM beta;
SELECT * FROM gamma WHERE amount > (SELECT AVG(amount) FROM gamma);
