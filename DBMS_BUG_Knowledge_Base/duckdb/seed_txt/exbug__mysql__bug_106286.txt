CREATE TABLE test (id INT PRIMARY KEY, ts TIMESTAMP);
INSERT INTO test (id, ts) VALUES (1, '2023-01-01 00:00:00');
CREATE OR REPLACE FUNCTION check_uniqueness_with_nulls(new_id INT, new_ts TIMESTAMP) RETURNS VOID AS $$ BEGIN IF (SELECT COUNT(*) FROM test WHERE test.id = new_id AND test.ts IS NULL) > 0 THEN RAISE EXCEPTION 'Duplicated values not allowed.';
END IF;
END;
$$ LANGUAGE plpgsql;
