CREATE TABLE test2 (id BIGINT, value BIGINT, arr LIST<VARCHAR>);
INSERT INTO test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, ['a', 'b', 'c'] FROM range(10);
CREATE TABLE test3 AS SELECT * FROM test2;
SELECT arr, cardinality(arr) FROM test2 WHERE value IS NOT NULL ORDER BY value LIMIT 3;
SELECT id, value, arr[1] FROM test3 WHERE cardinality(arr) > 0;
