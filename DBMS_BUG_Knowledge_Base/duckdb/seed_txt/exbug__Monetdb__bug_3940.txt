CREATE TABLE bar (id INTEGER PRIMARY KEY, start_date DATE, end_date DATE, duration INTEGER CHECK (duration >= 0));
INSERT INTO bar VALUES (1, DATE '2023-01-01', DATE '2023-01-10', DATE_DIFF(end_date, start_date));
INSERT INTO bar VALUES (2, DATE '2023-02-01', DATE '2023-02-05', DATE_DIFF(end_date, start_date));
SELECT id, duration, (end_date - start_date) AS calculated_duration FROM bar WHERE duration <> (end_date - start_date);
SELECT COUNT(*) FROM bar WHERE start_date IS NULL OR end_date IS NULL;
