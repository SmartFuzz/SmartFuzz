CREATE TABLE t2(id INTEGER PRIMARY KEY, data BLOB, info VARCHAR NOT NULL DEFAULT 'default');
CREATE INDEX t2data_info ON t2(data, info);
INSERT INTO t2(id, data, info) VALUES(1, repeat(chr(0), 16), 'test');
INSERT INTO t2(id, data, info) VALUES(2, repeat(chr(0), 0), NULL);
SELECT id, data, length(data), info FROM t2;
