CREATE TABLE proteins (id INTEGER PRIMARY KEY, featureId VARCHAR, sequence TEXT, annotation VARCHAR);
INSERT INTO proteins (id, featureId, sequence, annotation) VALUES (1, 'PRO_0000024369', 'MKTAYIAKQRQISFVKSHFSRQD', 'http://purl.uniprot.org/annotation/PRO_0000024369');
CREATE TABLE ranges (id INTEGER PRIMARY KEY, begin INTEGER, end INTEGER, protein_id INTEGER, FOREIGN KEY (protein_id) REFERENCES proteins(id));
INSERT INTO ranges (id, begin, end, protein_id) VALUES (1, 1, 10, 1);
SELECT CONCAT('>', SUBSTR(CAST(protein_id AS VARCHAR), 33), '#', 'PRO_0000024369', '\n', SUBSTR(sequence, begin, end - begin + 1)) AS fasta FROM proteins JOIN ranges ON proteins.id = ranges.protein_id WHERE featureId = 'PRO_0000024369' LIMIT 10;
