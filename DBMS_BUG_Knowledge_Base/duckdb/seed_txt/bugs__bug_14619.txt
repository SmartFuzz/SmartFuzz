CREATE TABLE test_table (id INT, values LIST<INT>);
INSERT INTO test_table VALUES (1, [10, 20, 30]), (2, [NULL, 40, 50]), (3, [60, 70, NULL]);
SELECT id, list_transform(values, (x, ix) -> ix) AS transformed_ix FROM test_table;
SELECT id, list_reduce(values, (acc, x, ix) -> ix) AS reduced_ix FROM test_table;
SELECT id, list_transform(values, (x, ix) -> x + 1) AS incremented_values FROM test_table WHERE values IS NOT NULL;
