CREATE TABLE t2 (id BIGINT NOT NULL, name VARCHAR(255) NOT NULL, description TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO t2 (name, description) VALUES ('Sample', 'This is a sample description.');
ANALYZE t2;
SELECT * FROM t2 WHERE name = 'Sample' AND description IS NULL;
