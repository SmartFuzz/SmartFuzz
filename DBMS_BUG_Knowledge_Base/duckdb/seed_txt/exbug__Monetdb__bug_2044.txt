CREATE TABLE test_table1 (id INTEGER PRIMARY KEY AUTOINCREMENT, name VARCHAR(50), age INTEGER CHECK (age >= 0));
CREATE TABLE test_table2 (id INTEGER PRIMARY KEY AUTOINCREMENT, score FLOAT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table1 (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', -5);
INSERT INTO test_table2 (score) VALUES (NULL), (99.9), (CAST('100' AS FLOAT));
SELECT t1.name, t2.score FROM test_table1 t1 LEFT JOIN test_table2 t2 ON t1.id = t2.id WHERE t1.age IS NOT NULL;
