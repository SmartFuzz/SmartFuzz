CREATE TABLE test_data (id INTEGER, value DOUBLE, category VARCHAR);
INSERT INTO test_data VALUES (1, 50.0, 'A'), (2, 50.0, 'B'), (3, NULL, 'A'), (4, 100.0, 'B');
WITH union_data AS (SELECT value FROM test_data WHERE category = 'A' UNION ALL SELECT value FROM test_data WHERE category = 'B') SELECT value, SUM(value) OVER (ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS cumulative_sum FROM union_data;
WITH union_data AS (SELECT value FROM test_data WHERE category = 'A' UNION SELECT value FROM test_data WHERE category = 'B') SELECT value, SUM(value) OVER (ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS cumulative_sum FROM union_data;
CREATE TABLE boundary_test (num INTEGER NOT NULL CHECK (num >= 0));
