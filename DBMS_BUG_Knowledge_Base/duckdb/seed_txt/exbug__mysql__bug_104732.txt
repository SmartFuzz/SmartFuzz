CREATE DATABASE test_db;
CREATE TABLE users (id INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY, username VARCHAR(50) NOT NULL, email VARCHAR(100) UNIQUE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE VIEW active_users AS SELECT id, username FROM users WHERE created_at > CURRENT_TIMESTAMP - INTERVAL '30 days';
SELECT * FROM active_users;
