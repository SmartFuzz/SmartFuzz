SET GLOBAL analyze_version = 1;
DROP TABLE IF EXISTS t;
CREATE TABLE t ( a INT, b INT, c INT DEFAULT 0, PRIMARY KEY (a) );
INSERT INTO t (a, b) VALUES (1, 1), (2, 2), (3, 3), (15, 15), (25, 25), (35, 35);
CREATE UNIQUE INDEX idx ON t(b);
EXPLAIN SELECT b FROM t WHERE b < 16 ORDER BY b;
