DROP TABLE IF EXISTS t2;
CREATE TABLE t2(id INTEGER PRIMARY KEY, c1 VARCHAR, c2 FLOAT, c3 DATE);
INSERT INTO t2(c1, c2, c3) VALUES ('test1', 1.5, '2023-01-01'), ('test2', NULL, '2023-02-01'), ('test3', 2.5, NULL);
SELECT LAST(c1) OVER (ORDER BY id) AS last_value FROM t2 CROSS JOIN (SELECT * FROM t0) AS t0;
SELECT LAST(c2) OVER (ORDER BY id) AS last_value FROM t2 CROSS JOIN (SELECT * FROM t1) AS t1;
