CREATE TABLE test_data (id INTEGER PRIMARY KEY, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, value) VALUES (1, 'first'), (2, 'second'), (3, 'third'), (4, 'fourth');
SELECT last(value) FROM test_data WHERE id IS NOT NULL;
SELECT last(value) FROM read_csv('seq.txt', header=false, sep='', columns={'line': VARCHAR}, parallel=true);
SELECT last(value ORDER BY created_at) FROM test_data;
