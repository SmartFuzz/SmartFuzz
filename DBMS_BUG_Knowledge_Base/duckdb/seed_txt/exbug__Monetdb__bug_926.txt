CREATE TABLE test_intervals (id INTEGER PRIMARY KEY AUTOINCREMENT, duration INTERVAL NOT NULL);
INSERT INTO test_intervals (duration) VALUES (INTERVAL '60 seconds');
SELECT duration, duration AS time_value FROM test_intervals;
SELECT id, duration AS time_value FROM test_intervals WHERE duration IS NOT NULL;
SELECT id, INTERVAL '120 seconds' AS time_value FROM test_intervals;
