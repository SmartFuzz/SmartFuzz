CREATE TABLE bar (id INTEGER PRIMARY KEY, name TEXT, value FLOAT, created_at TIMESTAMP, is_active BOOLEAN);
INSERT INTO bar (id, name, value, created_at, is_active) VALUES (1, 'Event A', 10.5, '2023-01-01 10:00:00', TRUE);
INSERT INTO bar (id, name, value, created_at, is_active) VALUES (2, 'Event B', 20.0, '2023-01-02 11:00:00', FALSE);
SELECT name, value FROM (SELECT unnest(arg_max(bar, created_at)) FROM bar GROUP BY is_active);
SELECT name, value FROM (SELECT unnest(arg_max(t, value)) FROM (SELECT id, name, value FROM bar WHERE is_active = TRUE) AS t GROUP BY name);
