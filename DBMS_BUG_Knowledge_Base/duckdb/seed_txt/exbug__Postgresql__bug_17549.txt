CREATE TABLE def (x INTEGER, y TEXT, z BOOLEAN DEFAULT TRUE);
INSERT INTO def SELECT CAST(random() * 10000 AS INTEGER), CAST(random() * 10000 AS TEXT), (random() > 0.5);
CREATE INDEX ON def(x, lower(y));
CREATE TABLE app_settings (key TEXT, value TEXT);
INSERT INTO app_settings VALUES ('app.x', '1');
