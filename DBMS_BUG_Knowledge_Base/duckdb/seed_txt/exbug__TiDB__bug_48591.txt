CREATE TABLE sbtest2 ( id INTEGER NOT NULL, k INTEGER NOT NULL DEFAULT 0, c VARCHAR(255) NOT NULL, pad TEXT NOT NULL, PRIMARY KEY (id), UNIQUE (k));
INSERT INTO sbtest2 (k, c, pad) VALUES (1, 'test1', 'padding1');
INSERT INTO sbtest2 (k, c, pad) VALUES (2, NULL, 'padding2');
SELECT COUNT(*) FROM sbtest2 WHERE k IS NULL OR c IS NULL;
ALTER TABLE sbtest2 ADD COLUMN id INTEGER NOT NULL AUTO_INCREMENT;
