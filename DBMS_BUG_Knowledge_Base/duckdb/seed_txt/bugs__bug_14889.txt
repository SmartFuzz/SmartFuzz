CREATE TABLE products (id INTEGER PRIMARY KEY, details JSON NOT NULL);
INSERT INTO products VALUES (1, '{ "price": 10.50 }');
SELECT details::JSON->'price'::decimal(5,2) AS price FROM products;
SELECT (details::JSON->'price')::decimal(5,2) AS price FROM products;
SELECT (SELECT details::JSON->'price' FROM products WHERE id = 1)::decimal(5,2) AS price;
