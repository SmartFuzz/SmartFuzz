BEGIN;
CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT NOT NULL, value DOUBLE DEFAULT 0.0);
CREATE TABLE temp_table AS SELECT * FROM test_table WHERE value > (SELECT AVG(value) FROM test_table);
CREATE TABLE faulty_table AS SELECT CAST(NULL AS BLOB) AS blob_data;
SELECT COUNT(*) FROM test_table;
COMMIT;
