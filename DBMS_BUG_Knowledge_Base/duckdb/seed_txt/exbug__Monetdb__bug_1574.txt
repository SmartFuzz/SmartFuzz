CREATE TABLE test2(id INTEGER NOT NULL PRIMARY KEY, value VARCHAR(50), created_at DATE);
INSERT INTO test2 VALUES (1, 'First', DATE '2023-01-01');
INSERT INTO test2 VALUES (2, 'Second', DATE '2023-01-02');
SELECT id, (SELECT value FROM test2 WHERE created_at > t2.created_at LIMIT 1) AS next_value FROM test2 t2;
SELECT created_at, COUNT(*) AS count_before FROM test2 WHERE created_at <= created_at GROUP BY created_at;
