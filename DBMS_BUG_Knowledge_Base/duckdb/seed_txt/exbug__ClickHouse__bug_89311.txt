CREATE TABLE IF NOT EXISTS test_v2 (list_id UUID, item VARCHAR, created_at TIMESTAMP, price DOUBLE, is_active BOOLEAN);
INSERT INTO test_v2 SELECT list_id, item, created_at, price, is_active FROM (SELECT generate_series(1, 100) AS id, random() AS price, random() AS is_active) AS g CROSS JOIN (SELECT uuid_generate_v4() AS list_id) AS t LIMIT 100000;
CREATE INDEX prj_list_id_created_at ON test_v2 (list_id, created_at);
SELECT * FROM test_v2 WHERE list_id = '019a369d-0a96-7fbd-853c-a4335fd2ee9f' ORDER BY created_at LIMIT 100 OFFSET 6000;
