CREATE TABLE tbl03 (id INTEGER PRIMARY KEY, b INTEGER NOT NULL DEFAULT 0, c VARCHAR(255));
INSERT INTO tbl03 (id, b) VALUES (1, NULL);
CREATE TABLE test01.c (id INTEGER PRIMARY KEY, a TIMESTAMP);
INSERT INTO test01.c (id, a) VALUES (1, NOW());
CREATE OR REPLACE FUNCTION trg02() RETURNS TRIGGER AS $$ BEGIN NEW.c := (SELECT a FROM test01.c WHERE id = 1);
RETURN NEW;
