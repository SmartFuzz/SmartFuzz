CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR NOT NULL, age INTEGER CHECK(age >= 0), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, age) VALUES ('Alice', 30);
INSERT INTO test_table (name, age) VALUES ('Bob', NULL);
SELECT * FROM test_table WHERE age IS NULL OR age < 18;
UPDATE test_table SET age = age + 1 WHERE name = 'Alice';
