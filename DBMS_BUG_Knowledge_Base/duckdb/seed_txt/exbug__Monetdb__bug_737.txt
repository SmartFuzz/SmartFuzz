CREATE TABLE sequences (id INTEGER PRIMARY KEY, value VARCHAR);
INSERT INTO sequences (id, value) VALUES (1, 'first'), (2, 'second'), (3, 'third');
SELECT * FROM sequences LIMIT 1;
SELECT * FROM sequences ORDER BY id LIMIT 2 OFFSET 1;
SELECT value FROM sequences WHERE id IN (SELECT id FROM sequences LIMIT 1);
