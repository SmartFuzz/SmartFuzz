CREATE TABLE pt1 (a int PRIMARY KEY, b text NOT NULL);
CREATE TABLE p1 AS SELECT * FROM pt1 WHERE a BETWEEN 0 AND 0;
CREATE TABLE pt2 (a int, c numeric CHECK (c >= 0));
CREATE TABLE p2 (a int, FOREIGN KEY (a) REFERENCES pt1 ON DELETE CASCADE);
INSERT INTO pt2 SELECT 0, 1.0;
