SET recursive_triggers = true;
CREATE TABLE t1(id INTEGER PRIMARY KEY, value VARCHAR, amount DOUBLE CHECK(amount >= 0));
INSERT INTO t1(value, amount) VALUES ('test1', 10.5), ('test2', 20.0);
CREATE OR REPLACE FUNCTION tr1() RETURNS VOID AS $$ BEGIN UPDATE t1 SET amount = NULL WHERE id = OLD.id;
END;
$$ LANGUAGE plpgsql;
