CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR, description TEXT);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description.');
SELECT regexp_replace(description, '[*]', 'X') FROM test_table WHERE id = 1;
SELECT regexp_matches(name, '[*]') FROM test_table WHERE id = 1;
SELECT regexp_replace(NULL, '[*]', 'X') AS result;
