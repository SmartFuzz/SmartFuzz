CREATE TABLE actors (id INTEGER PRIMARY KEY, name VARCHAR(100), birth_year INTEGER, is_active BOOLEAN);
CREATE TABLE movies (id INTEGER PRIMARY KEY, title VARCHAR(100), release_year INTEGER, director_id INTEGER);
INSERT INTO actors (id, name, birth_year, is_active) VALUES (1, 'Mel Gibson', 1956, TRUE);
INSERT INTO movies (id, title, release_year, director_id) VALUES (1, 'Braveheart', 1995, 1);
SELECT * FROM movies WHERE director_id IN (SELECT id FROM actors WHERE is_active IS NOT NULL);
