CREATE TABLE mixed_types (id INTEGER, name VARCHAR, created_at TIMESTAMP, is_active BOOLEAN);
INSERT INTO mixed_types (id, name, created_at, is_active) VALUES (1, 'Test User', '2023-10-01 10:00:00', TRUE);
CREATE TABLE integers (i INTEGER, j INTEGER);
ALTER TABLE integers ADD PRIMARY KEY (i);
SELECT * FROM mixed_types WHERE is_active IS NOT NULL;
