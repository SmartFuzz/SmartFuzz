CREATE TABLE test_cte (id INTEGER PRIMARY KEY, name VARCHAR(50), value DECIMAL(10,2));
INSERT INTO test_cte (id, name, value) VALUES (1, 'A', 10.50), (2, 'B', NULL), (3, 'C', 20.00);
WITH cte AS (SELECT id, name FROM test_cte WHERE value IS NOT NULL) SELECT * FROM cte;
CREATE TABLE another_table (ref_id INTEGER, description TEXT, FOREIGN KEY (ref_id) REFERENCES test_cte(id));
INSERT INTO another_table (ref_id, description) VALUES (1, 'Description 1'), (2, 'Description 2');
