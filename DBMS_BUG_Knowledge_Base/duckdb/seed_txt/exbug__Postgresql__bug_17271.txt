CREATE TABLE example (id INTEGER PRIMARY KEY AUTOINCREMENT, direction VARCHAR NOT NULL, below BOOLEAN NOT NULL);
INSERT INTO example (direction, below) VALUES ('UP', TRUE), ('DOWN', FALSE);
ALTER TABLE example ADD CONSTRAINT ck_example_direction CHECK ((direction = 'DOWN' AND below) OR (NOT below));
ALTER TABLE example DROP CONSTRAINT ck_example_direction;
ALTER TABLE example ALTER COLUMN direction TYPE VARCHAR;
