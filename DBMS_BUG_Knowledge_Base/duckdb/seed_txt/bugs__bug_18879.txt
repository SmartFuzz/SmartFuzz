CREATE TABLE c (id int4, value varchar);
INSERT INTO c (id, value) VALUES (1, 'one'), (2, 'two'), (3, 'three');
CREATE TABLE d (id int4, description varchar);
INSERT INTO d (id, description) VALUES (2, 'two'), (3, 'three'), (4, 'four');
SELECT c.id, COUNT(*), COUNT(c.*) AS c_star, COUNT(d.*) AS d_star, COUNT(d.id) AS d_id FROM c LEFT JOIN d ON c.id = d.id GROUP BY c.id;
