DROP TABLE IF EXISTS vs_server;
CREATE TABLE vs_server (nGroupId INT NOT NULL, nName VARCHAR(50));
INSERT INTO vs_server(nGroupId, nName) VALUES (15, 'Group A'), (21, 'Group B');
CREATE OR REPLACE FUNCTION test_proc() RETURNS INT AS $$ DECLARE nVal INT := 0;
BEGIN IF (SELECT COUNT(*) FROM vs_server INNER JOIN (SELECT 199 AS group_id) AS tmp_table ON vs_server.nGroupId = tmp_table.group_id) > 0 THEN nVal := 1;
END IF;
