CREATE TABLE test_table1 (id BIGINT, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE test_table2 (total DOUBLE, id_bit BLOB, description VARCHAR);
INSERT INTO test_table1 SELECT number AS id, CAST(number AS VARCHAR) AS value, CURRENT_TIMESTAMP FROM range(100000);
INSERT INTO test_table2 SELECT 1.5 AS total, BITMAP_AGG(rand() % 1000) AS id_bit, NULL AS description FROM range(100);
SELECT COUNT(DISTINCT id_bit) FROM test_table2 WHERE total > 1.0;
