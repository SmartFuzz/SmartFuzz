CREATE TABLE parent (id INTEGER PRIMARY KEY, name VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE child1 AS SELECT * FROM parent LIMIT 0;
CREATE TABLE child2 AS SELECT * FROM child1 LIMIT 0;
ALTER TABLE parent ADD COLUMN age INT CHECK (age >= 0);
INSERT INTO parent (name, age) VALUES ('Alice', 30), ('Bob', NULL);
