DROP TABLE IF EXISTS testUUID;
CREATE TABLE testUUID (ID INT PRIMARY KEY, new_ID VARCHAR(100) NOT NULL, new_ID2 VARCHAR(100), new_ID3 BINARY(16) UNIQUE);
INSERT INTO testUUID (ID) VALUES (1), (2), (3), (4);
UPDATE testUUID SET new_ID = uuid(), new_ID2 = REPLACE(uuid(), '-', ''), new_ID3 = decode(REPLACE(uuid(), '-', ''), 'hex') WHERE ID < 3;
SELECT new_ID, new_ID2 FROM testUUID;
