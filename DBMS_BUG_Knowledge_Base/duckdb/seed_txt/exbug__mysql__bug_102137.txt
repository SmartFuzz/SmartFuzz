CREATE DATABASE d2;
CREATE TABLE t2 (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, name VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2 (name) VALUES ('Test Name');
SELECT * FROM t2 WHERE id = (SELECT MAX(id) FROM t2);
SELECT '172.17.0.2:3306' AS clone_valid_donor_list;
