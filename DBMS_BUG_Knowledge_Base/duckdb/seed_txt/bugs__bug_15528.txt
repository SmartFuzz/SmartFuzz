CREATE TABLE one (a INTEGER, b VARCHAR);
CREATE TABLE two (a INTEGER, b FLOAT);
INSERT INTO one (a, b) VALUES (1, 'test1'), (2, 'test2');
INSERT INTO two (a, b) VALUES (1, 1.1), (2, 2.2);
SELECT one.a, two.a FROM one JOIN two ON one.a = two.a;
