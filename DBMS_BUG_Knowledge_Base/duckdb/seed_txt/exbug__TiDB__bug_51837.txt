CREATE TABLE products ( id INTEGER NOT NULL, name VARCHAR(100) NOT NULL, price DECIMAL(10,2) NOT NULL, PRIMARY KEY (id) );
CREATE TABLE orders ( id INTEGER NOT NULL, product_id INTEGER NOT NULL, quantity INTEGER NOT NULL, PRIMARY KEY (id), FOREIGN KEY (product_id) REFERENCES products (id) );
INSERT INTO products (name, price) VALUES ('Widget', 19.99);
INSERT INTO orders (product_id, quantity) VALUES (1, 5);
SELECT * FROM orders;
