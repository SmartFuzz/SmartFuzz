CREATE TABLE davuser_data (id INTEGER PRIMARY KEY, content VARCHAR NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO davuser_data (id, content) VALUES (1, 'This content is not Turtle.');
CREATE TABLE davuser_permissions (user_id INTEGER, permission VARCHAR, FOREIGN KEY (user_id) REFERENCES davuser_data(id));
INSERT INTO davuser_permissions (user_id, permission) VALUES (1, 'read');
SELECT * FROM davuser_data WHERE id = (SELECT MAX(id) FROM davuser_data) AND content IS NOT NULL;
