CREATE TABLE test_table (id INT, name VARCHAR, value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'test', 10.5), (2, 'example', NULL), (3, 'sample', 20.0);
SELECT COLUMNS(x -> x.value > 15.0) FROM test_table;
SELECT COLUMNS(x -> x.name = 'test' OR x.value IS NULL) FROM test_table;
SELECT COLUMNS(x -> x.id + 1 = 3) FROM test_table;
