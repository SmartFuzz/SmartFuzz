DROP TABLE IF EXISTS b_products;
CREATE TABLE b_products (product_id BIGINT, product_name VARCHAR, price DOUBLE);
INSERT INTO b_products SELECT number, 'Product_' || number, number * 10.5 FROM generate_series(0, 99) AS number;
DROP TABLE IF EXISTS b_sales;
CREATE TABLE b_sales (sale_id BIGINT, product_id BIGINT, quantity INT);
