CREATE TABLE t_proj_test1 (id UInt32, name VARCHAR, value DOUBLE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t_proj_test1 SELECT number, CONCAT('Name', number::VARCHAR), number * 1.5, CURRENT_TIMESTAMP FROM range(10000);
CREATE TABLE t_proj_test2 (user_id UInt32, score INT, timestamp DATE DEFAULT CURRENT_DATE);
INSERT INTO t_proj_test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, CURRENT_DATE FROM range(5000);
SELECT user_id, SUM(score) AS total_score FROM t_proj_test2 GROUP BY user_id ORDER BY total_score DESC;
