CREATE TABLE t2 ( id INTEGER PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP );
INSERT INTO t2 (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
CREATE TABLE t3 ( x INTEGER, y INTEGER, z INTEGER DEFAULT 0, UNIQUE (y) );
INSERT INTO t3 (x, y) VALUES (5, 10), (15, 20), (25, 30);
SELECT * FROM t3 WHERE y IN (SELECT y FROM t3 WHERE x < 20) ORDER BY z DESC;
