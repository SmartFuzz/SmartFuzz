CREATE TABLE test_partition (id INTEGER NOT NULL, name VARCHAR(100) NOT NULL, value DECIMAL(10,2), created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, data JSON NOT NULL, PRIMARY KEY (id));
INSERT INTO test_partition VALUES (1, 'test1', 100.00, '2024-01-01 00:00:00', '{"key": "value1"}');
INSERT INTO test_partition VALUES (2, 'test2', NULL, '2024-01-02 00:00:00', '{"key": "value2"}');
SELECT AVG(value) AS avg_value, COUNT(name) AS count_name FROM test_partition WHERE data->>'key' != 'value1' GROUP BY name;
SELECT id, name, COALESCE(value, 0) AS value FROM test_partition WHERE created_at < '2024-01-03 00:00:00' ORDER BY id DESC;
