CREATE TABLE table1(table1_id INTEGER PRIMARY KEY, i INTEGER);
CREATE TABLE table2(table2_id INTEGER PRIMARY KEY, i INTEGER);
CREATE TABLE table3(table3_id INTEGER PRIMARY KEY, i INTEGER, a1 VARCHAR, a2 VARCHAR, a3 VARCHAR, a4 VARCHAR, a5 VARCHAR, a6 VARCHAR, a7 VARCHAR);
INSERT INTO table1 (i) SELECT i FROM (SELECT ROW_NUMBER() OVER () AS i FROM range(1, 1001)) AS r;
INSERT INTO table2 (i) SELECT i FROM (SELECT ROW_NUMBER() OVER () AS i FROM range(1, 1001)) AS r;
INSERT INTO table3 (i, a1, a2, a3, a4, a5, a6, a7) SELECT i, random()::VARCHAR, random()::VARCHAR, random()::VARCHAR, random()::VARCHAR, random()::VARCHAR, random()::VARCHAR, random()::VARCHAR FROM (SELECT ROW_NUMBER() OVER () AS i FROM range(1, 1001)) AS r;
