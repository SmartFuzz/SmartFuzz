CREATE TABLE test_person (id INTEGER PRIMARY KEY, name VARCHAR(255) NOT NULL, age INTEGER CHECK (age >= 0), bio TEXT);
INSERT INTO test_person (id, name, age, bio) VALUES (1, 'Alice', 30, 'A software engineer from New York.');
INSERT INTO test_person (id, name, age, bio) VALUES (2, 'Bob', NULL, 'An artist from Paris.');
SELECT id, name, age, COALESCE(bio, 'No bio available') AS bio_info FROM test_person WHERE age IS NOT NULL;
UPDATE test_person SET age = age + 1 WHERE id = 1;
