CREATE TABLE t(a INT, b VARCHAR(10), c FLOAT, d DATE);
INSERT INTO t VALUES (NULL, 'test1', 1.1, DATE '2023-01-01'), (1, 'test2', 2.2, DATE '2023-01-02'), (2, 'test3', NULL, DATE '2023-01-03');
SELECT a, MIN(a) OVER (ORDER BY a NULLS FIRST ROWS UNBOUNDED PRECEDING) FROM t;
CREATE TABLE u(x INT NOT NULL, y INT DEFAULT 0);
INSERT INTO u SELECT a, MIN(a) FROM t GROUP BY a;
