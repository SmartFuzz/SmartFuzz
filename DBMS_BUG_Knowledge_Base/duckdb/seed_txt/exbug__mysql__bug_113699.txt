CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
SET optimizer_switch = 'prefer_ordering_index=off';
SELECT * FROM test_table WHERE id IS NULL OR name = 'Alice' ORDER BY created_at LIMIT 1;
SELECT * FROM test_table WHERE id > 2 ORDER BY name DESC LIMIT 1;
