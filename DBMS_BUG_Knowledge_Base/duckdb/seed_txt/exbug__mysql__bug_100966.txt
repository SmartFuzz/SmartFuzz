CREATE TABLE user_card_info_hash_4 (id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, amount DECIMAL(10, 2) CHECK (amount >= 0));
INSERT INTO user_card_info_hash_4 (id, name, amount) SELECT ROW_NUMBER() OVER (), CONCAT('User', ROW_NUMBER() OVER ()) , ROUND(RANDOM() * 1000, 2) FROM range(1, 10000000);
SELECT COUNT(*) FROM user_card_info_hash_4;
COMMIT;
