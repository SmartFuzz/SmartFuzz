CREATE TEMPORARY TABLE tbTestBad2 (id INT, value DECIMAL(10, 2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO tbTestBad2 (id, value) VALUES (1, NULL), (2, 10.50), (3, 20.00);
CREATE OR REPLACE FUNCTION spTestBad() RETURNS TABLE(id INT, total_value DECIMAL(10, 2)) AS $$ BEGIN RETURN SELECT id, (SUM(value) OVER()) AS total_value FROM tbTestBad2;
END;
$$ LANGUAGE SQL;
SELECT * FROM spTestBad();
