CREATE TABLE data_user_test_v2 ( user_id INTEGER NOT NULL, country VARCHAR(50) NOT NULL DEFAULT '', age INTEGER, create_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, update_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (user_id));
INSERT INTO data_user_test_v2 VALUES (1, '中国', 30, NOW(), NOW()), (2, '日本', 25, NOW(), NOW()), (3, '中国', 35, NOW(), NOW()), (4, '韩国', NULL, NOW(), NOW());
SELECT country, COUNT(*) AS user_count FROM data_user_test_v2 GROUP BY country HAVING user_count > 1;
SELECT CASE WHEN country='中国' THEN '国内' ELSE '国外' END AS country_type, COUNT(DISTINCT user_id) FROM data_user_test_v2 GROUP BY country_type;
