CREATE TABLE test1 (n_a ARRAY<STRUCT<aa BIGINT, bb BIGINT>> DEFAULT ARRAY[ROW(0, 0)]);
INSERT INTO test1 SELECT ARRAY[ROW(1, 2), ROW(3, 4), ROW(5, 6)];
ALTER TABLE test1 ADD COLUMN n_c ARRAY<STRUCT<cc BIGINT, dd BIGINT>> DEFAULT ARRAY[ROW(0, 0)];
SELECT n_a, n_c FROM test1 WHERE EXISTS(SELECT 1 FROM unnest(n_a) AS x WHERE x.aa > 2);
CREATE TABLE test2 AS SELECT * FROM test1;
