CREATE TABLE test_dates (id INTEGER PRIMARY KEY, event_date TIMESTAMP NOT NULL);
INSERT INTO test_dates (id, event_date) VALUES (1, '2023-03-21 12:00:00');
SELECT event_date + INTERVAL '9 days' FROM test_dates WHERE id = 1;
SELECT event_date + INTERVAL '3 days' FROM test_dates WHERE id = 1;
SELECT event_date + INTERVAL (SELECT COUNT(*) FROM test_dates) DAY FROM test_dates WHERE id = 1;
