CREATE SCHEMA test_schema;
CREATE TABLE test_schema.test (id INT NOT NULL, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
CREATE OR REPLACE FUNCTION test_trigger() RETURNS TRIGGER AS $$ BEGIN INSERT INTO test_schema.test VALUES (OLD.id, OLD.name);
RETURN OLD;
END;
$$ LANGUAGE plpgsql;
