CREATE TABLE repro.test_table (id BIGINT, timestamp TIMESTAMP, value1 VARCHAR, value2 DOUBLE, value3 BIGINT);
INSERT INTO repro.test_table SELECT id, TIMESTAMP '2025-01-01 00:00:00' + INTERVAL (RANDOM() * 86400) SECOND AS timestamp, value1, value2, value3 FROM (SELECT ROW_NUMBER() OVER () AS id, 'value' || ROW_NUMBER() OVER () AS value1, RANDOM() AS value2, ROW_NUMBER() OVER () AS value3 FROM range(20)) LIMIT 100000;
CREATE TABLE repro.another_table (id BIGINT, timestamp TIMESTAMP, value1 VARCHAR, value2 DOUBLE, value3 BIGINT);
INSERT INTO repro.another_table SELECT id, TIMESTAMP '2025-01-01 00:00:00' + INTERVAL (RANDOM() * 86400) SECOND AS timestamp, value1, value2, value3 FROM (SELECT ROW_NUMBER() OVER () AS id, 'value' || ROW_NUMBER() OVER () AS value1, RANDOM() AS value2, ROW_NUMBER() OVER () AS value3 FROM range(20)) LIMIT 50;
SELECT * FROM repro.test_table WHERE (timestamp >= TIMESTAMP '2025-01-01 00:00:00') AND (timestamp <= TIMESTAMP '2025-01-02 00:00:00') AND (value2 IS NOT NULL);
