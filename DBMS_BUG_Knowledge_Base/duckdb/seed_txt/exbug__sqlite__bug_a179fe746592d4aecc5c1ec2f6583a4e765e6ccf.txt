CREATE TABLE products(id INTEGER PRIMARY KEY, name VARCHAR NOT NULL, category_id INTEGER);
CREATE TABLE categories(id INTEGER PRIMARY KEY, name VARCHAR NOT NULL);
INSERT INTO categories VALUES(1, 'Electronics'), (2, 'Books'), (3, 'Clothing');
INSERT INTO products VALUES(1, 'Laptop', 1), (2, 'Novel', 2), (3, 'T-shirt', 3), (4, 'Smartphone', 1), (5, 'Magazine', 2);
SELECT c.name, COUNT(p.id) FROM categories c LEFT JOIN products p ON c.id = p.category_id GROUP BY c.name ORDER BY c.name;
