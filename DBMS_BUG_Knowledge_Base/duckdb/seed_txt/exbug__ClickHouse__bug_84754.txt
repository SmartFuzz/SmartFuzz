CREATE TABLE IF NOT EXISTS stats.ledgers_test (interval VARCHAR, interval_start TIMESTAMP, owner VARCHAR, mint VARCHAR, amount BIGINT, after BIGINT, decimals INTEGER);
INSERT INTO stats.ledgers_test VALUES ('15m', CURRENT_TIMESTAMP, 'owner1', 'mint1', 100, 200, 18);
INSERT INTO stats.ledgers_test VALUES ('15m', CURRENT_TIMESTAMP - INTERVAL '1 hour', 'owner2', 'mint2', 150, 250, 20);
SELECT mint, MAX(after) AS latest_after FROM stats.ledgers_test WHERE interval = '15m' GROUP BY mint;
SELECT * FROM stats.ledgers_test WHERE amount > 0 AND interval_start >= CURRENT_TIMESTAMP - INTERVAL '1 day';
