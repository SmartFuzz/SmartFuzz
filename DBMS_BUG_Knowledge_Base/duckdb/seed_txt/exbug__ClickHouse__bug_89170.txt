CREATE TABLE test_distinct_oom_v2 ( id BIGINT, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP );
INSERT INTO test_distinct_oom_v2 SELECT number, CASE WHEN number % 2 = 0 THEN CAST(number AS VARCHAR) ELSE NULL END FROM range(100000000);
SELECT DISTINCT value FROM test_distinct_oom_v2;
CREATE TABLE test_distinct_oom_v3 ( id INTEGER, value DOUBLE, category VARCHAR DEFAULT 'unknown' );
INSERT INTO test_distinct_oom_v3 SELECT number, number / 3.14, CAST(number % 10 AS VARCHAR) FROM range(1000000);
