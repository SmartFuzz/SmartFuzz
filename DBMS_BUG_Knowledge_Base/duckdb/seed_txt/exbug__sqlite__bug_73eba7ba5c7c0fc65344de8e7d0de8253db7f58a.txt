CREATE TABLE users(id INTEGER PRIMARY KEY, name TEXT NOT NULL, age INTEGER CHECK(age >= 0));
INSERT INTO users(name, age) VALUES('Alice', 30);
SELECT * FROM users;
WITH temp AS (SELECT id FROM users WHERE age IS NULL) SELECT * FROM temp;
CREATE TABLE orders(order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DOUBLE DEFAULT 0.0);
