CREATE TABLE messages (id BIGINT NOT NULL, muid VARCHAR(100), conversationId VARCHAR(255), sender VARCHAR(100) NOT NULL, receiverType VARCHAR(10) NOT NULL DEFAULT 'user', receiver VARCHAR(100) NOT NULL, PRIMARY KEY (id));
INSERT INTO messages (id, muid, conversationId, sender, receiverType, receiver) VALUES (730170668, 'msg1', 'conv1', 'user1', 'user', 'user2');
INSERT INTO messages (id, muid, conversationId, sender, receiverType, receiver) VALUES (730170669, 'msg2', 'conv2', 'user2', 'group', 'group1');
SELECT id, updatedAt FROM messages WHERE id >= 14609942 ORDER BY id LIMIT 10;
UPDATE messages SET updatedAt = NULL WHERE id = 730170668;
