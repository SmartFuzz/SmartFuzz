CREATE TABLE B (COL1 INTEGER, COL2 INTEGER, COL3 FLOAT);
INSERT INTO B VALUES (100, 200, 1.5);
INSERT INTO B VALUES (NULL, 300, 2.5);
SELECT SUM(NULLIF(:var, 0) * COL1 * COL2) FROM B;
SELECT SUM(COALESCE(COL3, 0) * COL1) FROM B WHERE COL2 > 150;
