CREATE TABLE test2.raw_source_local (id INT, time TIMESTAMP, value DOUBLE DEFAULT 0.0);
INSERT INTO test2.raw_source_local (id, time, value) SELECT number AS id, TIMESTAMP '2024-01-01 00:00:00' + INTERVAL number SECOND AS time, RANDOM() * 100 AS value FROM range(1, 1000);
CREATE TABLE test2.table_for_mv (sum_value DOUBLE, date DATE, count_id INT);
CREATE VIEW test2.mv_from_raw_source_local_to_table AS SELECT SUM(value) AS sum_value, DATE(time) AS date, COUNT(*) AS count_id FROM test2.raw_source_local GROUP BY DATE(time);
INSERT INTO test2.table_for_mv SELECT sum_value, date, count_id FROM test2.mv_from_raw_source_local_to_table;
