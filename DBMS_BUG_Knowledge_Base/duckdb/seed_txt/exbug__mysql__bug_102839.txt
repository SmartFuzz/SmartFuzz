CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR, value DECIMAL(10,2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.50), (2, 'test2', NULL), (3, 'test3', 20.00);
SELECT regexp_extract(name, '[^,]+', 1) AS extracted FROM test_table WHERE value IS NOT NULL;
SELECT regexp_extract('a,b,c', '[^,]+', 50) AS test_case;
WITH RECURSIVE cte AS (SELECT 1 AS a UNION ALL SELECT a + 1 FROM cte WHERE a < 100) SELECT DISTINCT regexp_extract('example1,example2,example3', '[^,]+', a) AS result FROM cte;
