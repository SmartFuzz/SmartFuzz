CREATE TABLE test_table (id INTEGER PRIMARY KEY, name TEXT, value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'Test A', 10.5), (2, 'Test B', NULL), (3, 'Test C', 20.0);
SET VARIABLE filename = 'exported_data.parquet';
COPY (SELECT id, name, value FROM test_table WHERE value IS NOT NULL) TO getvariable('filename');
SELECT * FROM test_table WHERE created_at > CURRENT_TIMESTAMP - INTERVAL '1 day';
