CREATE TABLE testTableDates (d DATE NOT NULL, id INTEGER PRIMARY KEY);
INSERT INTO testTableDates (d, id) VALUES ('2023-01-01', 1);
INSERT INTO testTableDates (d, id) VALUES (NULL, 2);
SELECT * FROM testTableDates WHERE d IS NULL;
SELECT d FROM testTableDates WHERE id = 1;
