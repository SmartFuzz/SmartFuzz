CREATE TABLE users (id INTEGER PRIMARY KEY, name VARCHAR, age INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users (name, age) VALUES ('Alice', 30);
INSERT INTO users (name, age) VALUES ('Bob', NULL);
SELECT * FROM users WHERE age::VARCHAR = '30';
SELECT * FROM users WHERE created_at IS NOT NULL;
