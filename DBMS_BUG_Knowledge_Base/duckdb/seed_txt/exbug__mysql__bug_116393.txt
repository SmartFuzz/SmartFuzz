CREATE TABLE test_hashing (id INTEGER PRIMARY KEY, data VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_hashing (data) VALUES (CONCAT('Test', MD5('Sample Data'))), (NULL), (CONCAT('Boundary', SHA256('Edge Case')));
SELECT id, MD5(data) AS md5_hash FROM test_hashing WHERE data IS NOT NULL;
SELECT COUNT(*) FROM test_hashing WHERE SHA256(data) IS NOT NULL;
SELECT SHA256(CONCAT(data, 'Extra')) FROM test_hashing;
