CREATE DATABASE polaris_iceberg;
CREATE TABLE polaris_iceberg."teslasource\u0001kafkav6\u0001source-data" (id INTEGER, name VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO polaris_iceberg."teslasource\u0001kafkav6\u0001source-data" VALUES (1, 'test1', CURRENT_TIMESTAMP), (2, 'test2', CURRENT_TIMESTAMP);
SELECT * FROM polaris_iceberg."teslasource\u0001kafkav6\u0001source-data" WHERE id IS NOT NULL;
SELECT * FROM information_schema.tables WHERE table_schema = 'polaris_iceberg';
