DROP TABLE IF EXISTS t1, t2, t3;
CREATE TABLE t1 (a INT, b VARCHAR(10), c FLOAT, d DATE);
CREATE TABLE t2 (a INT, b INT NOT NULL, c TEXT DEFAULT 'default');
INSERT INTO t1 VALUES (1, 'test', 1.1, '2023-01-01');
SELECT one.a, one.b AS b2 FROM t1 one ORDER BY (SELECT two.b FROM t2 two WHERE two.a = CAST(one.b AS INT));
