CREATE TABLE a(id INTEGER PRIMARY KEY, name VARCHAR NOT NULL, age INTEGER CHECK(age >= 0));
CREATE TABLE b(id INTEGER PRIMARY KEY, a_id INTEGER, FOREIGN KEY(a_id) REFERENCES a(id));
CREATE TABLE c(id INTEGER PRIMARY KEY, value DOUBLE DEFAULT 0.0);
INSERT INTO a(name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', NULL);
SELECT * FROM a JOIN b ON a.id = b.a_id WHERE (a.id, a.age) IN (SELECT id, age FROM a WHERE age IS NOT NULL);
