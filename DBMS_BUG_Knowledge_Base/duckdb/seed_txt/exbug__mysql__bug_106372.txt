CREATE TABLE test_partitioned (id INT PRIMARY KEY, name VARCHAR(255));
CREATE TABLE test_orphaned (id INT NOT NULL, data TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, UNIQUE (id));
INSERT INTO test_partitioned (id, name) VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
INSERT INTO test_orphaned (id, data) VALUES (1, 'orphaned_data1'), (2, 'orphaned_data2');
SELECT A.schema_name, A.table_name FROM (SELECT DISTINCT REPLACE(SUBSTRING_INDEX(NAME, '/', 1), '@002d', '-') AS schema_name, REPLACE(SUBSTRING_INDEX(SUBSTRING_INDEX(NAME, '/', -1), '#', 1), '@002d', '-') AS table_name FROM information_schema.innodb_sys_tables WHERE NAME LIKE '%/%') A LEFT JOIN information_schema.tables I ON A.table_name = I.table_name AND A.schema_name = I.table_schema WHERE I.table_name IS NULL;
