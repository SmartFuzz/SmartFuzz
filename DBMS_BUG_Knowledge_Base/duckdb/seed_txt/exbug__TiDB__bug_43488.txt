CREATE TABLE users(id INTEGER PRIMARY KEY, name VARCHAR(50), age INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users VALUES(1, 'Alice', 30, NULL), (2, 'Bob', 25, NULL), (3, 'Charlie', 35, NULL);
SELECT * FROM users WHERE age > (SELECT AVG(age) FROM users);
SELECT COUNT(*) FROM users;
