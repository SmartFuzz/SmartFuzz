CREATE TABLE test_table (id INTEGER PRIMARY KEY AUTOINCREMENT, name VARCHAR(50) NOT NULL, description TEXT);
INSERT INTO test_table (name, description) VALUES ('foo', 'A test entry'), ('BAR', 'Another test entry');
CREATE INDEX test_idx ON test_table (lower(name));
SELECT lower(name) FROM test_table WHERE lower(name) LIKE '%o%';
