CREATE TABLE test_table (id INTEGER PRIMARY KEY, value INTEGER NOT NULL);
INSERT INTO test_table (id, value) VALUES (1, 10), (2, 20), (3, 10), (4, 30);
SELECT list(distinct value ORDER BY value) AS distinct_values FROM test_table;
SELECT list(distinct value % 3 ORDER BY id) AS mod_values FROM test_table;
SELECT list(distinct CASE WHEN value > 15 THEN 'high' ELSE 'low' END ORDER BY value) AS categories FROM test_table;
