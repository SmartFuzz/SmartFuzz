CREATE TABLE test_data (id INT, values VARCHAR[]);
INSERT INTO test_data VALUES (1, ['1', '2', '3']), (2, ['4', '5', NULL]), (3, ['6', '7', '8']);
SELECT id, list_sum(values) FROM test_data GROUP BY id;
CREATE OR REPLACE FUNCTION custom_sum(lst VARCHAR[]) AS (SELECT list_sum(lst));
SELECT id, custom_sum(values) FROM test_data WHERE values IS NOT NULL GROUP BY id;
