CREATE TABLE tbl1 (id1 VARCHAR, id2 VARCHAR, v BIGINT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO tbl1 VALUES ('x', 'y', 10), ('x', 'z', 20), (NULL, 'y', 30);
CREATE TABLE join_engine1 (id1 VARCHAR, id2 VARCHAR, v BIGINT, status VARCHAR);
INSERT INTO join_engine1 VALUES ('x', 'y', 10, 'active'), ('x', 'z', 20, 'inactive');
SELECT COUNT(DISTINCT id2) AS count FROM tbl1 WHERE id1 IS NOT NULL AND v = (SELECT v FROM join_engine1 WHERE id1 = tbl1.id1 AND id2 = tbl1.id2);
