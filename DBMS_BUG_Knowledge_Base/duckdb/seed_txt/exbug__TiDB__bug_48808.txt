DROP TABLE IF EXISTS t2;
CREATE TABLE t2(c1 INT, c2 VARCHAR, c3 DATE);
INSERT INTO t2 VALUES(0, 'test', DATE '2023-01-01'), (1, NULL, DATE '2023-01-02'), (2, 'example', NULL), (3, 3, DATE '2023-01-04');
WITH RECURSIVE cte2(c1, c2) AS (SELECT c1, c2 FROM t2 WHERE c1 < 3 UNION ALL SELECT c1 + 1, CONCAT(c2, '_next') FROM cte2 WHERE c1 < 3) SELECT * FROM cte2;
