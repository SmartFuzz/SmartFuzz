CREATE TABLE t_partition_test (id INTEGER NOT NULL, value VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
CREATE TABLE t_partition_test_partitions AS SELECT *, CASE WHEN value < 'a' THEN 0 WHEN value < 'm' THEN 1 ELSE 2 END AS partition_id FROM (SELECT * FROM t_partition_test) AS sub;
INSERT INTO t_partition_test (value) VALUES ('apple'), ('banana'), ('cherry'), ('date'), ('fig');
SELECT * FROM t_partition_test WHERE value BETWEEN 'a' AND 'z' OR value IN ('banana', 'date') GROUP BY value HAVING COUNT(id) > 0;
CREATE TABLE t_subquery_test (id INTEGER NOT NULL, ref_id INTEGER, PRIMARY KEY (id));
INSERT INTO t_subquery_test (id, ref_id) VALUES (1, 1), (2, 2), (3, NULL);
