CREATE TABLE test_users (id INTEGER PRIMARY KEY, username VARCHAR(50) NOT NULL, password VARCHAR(50) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_users (id, username, password) VALUES (1, 'test_user', 'Test-1234');
SELECT username FROM test_users WHERE id = 1;
SELECT * FROM test_users;
