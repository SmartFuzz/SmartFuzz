CREATE TABLE a(id INTEGER PRIMARY KEY, new_name TEXT NOT NULL, value DOUBLE DEFAULT 0.0);
CREATE TABLE b(id INTEGER PRIMARY KEY, a_id INTEGER, FOREIGN KEY(a_id) REFERENCES a(id));
INSERT INTO a(new_name, value) VALUES('test', NULL);
CREATE VIEW c AS SELECT a.id, b.id AS b_id FROM a LEFT JOIN b ON a.id = b.a_id;
ALTER TABLE a RENAME COLUMN new_name TO name;
