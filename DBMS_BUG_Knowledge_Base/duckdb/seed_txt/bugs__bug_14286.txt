CREATE TABLE test_data (id INTEGER, name VARCHAR, category VARCHAR);
INSERT INTO test_data VALUES (1, '大熊猫', '熊'), (2, '大熊猫', '猫'), (3, '小熊猫', '猫');
SELECT category, array_agg(distinct name) FILTER (WHERE id IS NOT NULL) AS a_list FROM test_data GROUP BY category;
SELECT category, array_agg(name) FILTER (WHERE id IS NOT NULL) AS b_list FROM test_data GROUP BY category;
SELECT category, array_agg(STRUCT(id, name, category)) AS c_list FROM test_data GROUP BY category;
