CREATE TABLE test_table (id INT NOT NULL, val VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO test_table (id, val) VALUES (1, 'test value'), (2, NULL), (3, 'another value');
UPDATE test_table SET val = 'updated value' WHERE id = 1;
SELECT id, val FROM test_table WHERE val IS NULL;
INSERT INTO test_table (id, val) SELECT a.N + b.N * 10 + 1, 'value ' || (a.N + b.N * 10 + 1) FROM (SELECT 0 AS N UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4) a JOIN (SELECT 0 AS N UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4) b LIMIT 5;
