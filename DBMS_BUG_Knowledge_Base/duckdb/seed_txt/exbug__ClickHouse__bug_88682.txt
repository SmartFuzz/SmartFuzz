CREATE TABLE c (a BIGINT, b BIGINT);
INSERT INTO c SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END FROM range(10);
CREATE TABLE d (m BIGINT, n BIGINT);
INSERT INTO d SELECT number, number + 1 FROM range(5);
SELECT c.a, d.n FROM c LEFT JOIN d ON c.a > d.m;
