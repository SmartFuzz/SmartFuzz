CREATE TABLE t(textid VARCHAR NOT NULL);
INSERT INTO t VALUES('12'), ('34'), (NULL);
CREATE TABLE i(intid INTEGER PRIMARY KEY CHECK(intid > 0));
INSERT INTO i VALUES(12), (34), (0);
SELECT t1.textid AS a, i.intid AS b, t2.textid AS c FROM t t1 JOIN i ON t1.textid = CAST(i.intid AS VARCHAR) JOIN t t2 ON t1.textid = t2.textid;
