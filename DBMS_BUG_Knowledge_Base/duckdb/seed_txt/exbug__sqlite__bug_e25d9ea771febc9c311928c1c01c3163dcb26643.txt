CREATE TABLE Users (id INTEGER PRIMARY KEY, name VARCHAR NOT NULL, email VARCHAR UNIQUE);
CREATE TABLE Comments (id INTEGER PRIMARY KEY, user_id INTEGER, content VARCHAR, FOREIGN KEY (user_id) REFERENCES Users(id) ON DELETE CASCADE);
INSERT INTO Users (name, email) VALUES ('Alice', 'alice@example.com');
INSERT INTO Comments (user_id, content) VALUES (1, 'This is a comment.');
DELETE FROM Users WHERE id = 1;
SELECT * FROM Comments;
