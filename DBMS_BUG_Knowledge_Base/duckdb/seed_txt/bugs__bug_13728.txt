CREATE TABLE t2 (a INTEGER, b VARCHAR, c DOUBLE, d BOOLEAN);
INSERT INTO t2 VALUES (1, 'test', 3.14, TRUE), (2, 'example', NULL, FALSE);
CREATE OR REPLACE FUNCTION unpivot_test(vars) AS TABLE (UNPIVOT t2 ON columns(vars));
SELECT * FROM unpivot_test(['a', 'b']);
CREATE OR REPLACE FUNCTION unpivot_with_null() AS TABLE (UNPIVOT t2 ON columns(['c', 'd']));
