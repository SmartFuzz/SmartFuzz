CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(255), data BLOB);
INSERT INTO test_table (id, name, data) VALUES (1, 'Test', CAST('' AS BLOB));
SELECT id, name, data FROM test_table WHERE data IS NULL;
UPDATE test_table SET data = CAST('' AS BLOB) WHERE id = 1;
SELECT CAST('' AS BLOB) AS binary_data, CASE WHEN data IS NULL THEN 'NULL' ELSE 'NOT NULL' END AS null_check FROM test_table;
