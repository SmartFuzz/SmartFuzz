CREATE TABLE nested_structs (id INTEGER, data STRUCT<name VARCHAR, value INTEGER>);
INSERT INTO nested_structs VALUES (1, (SELECT 'item1', 100));
INSERT INTO nested_structs VALUES (2, (SELECT 'item2', NULL));
SELECT id, data.name, data.value FROM nested_structs WHERE data.value IS NOT NULL;
SELECT id, data.name, COALESCE(data.value, 0) AS value FROM nested_structs;
