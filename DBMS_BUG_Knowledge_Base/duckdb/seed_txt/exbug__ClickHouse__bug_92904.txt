CREATE TABLE test_1 (ts DATE, a INT, b DOUBLE, c VARCHAR, d TIMESTAMP);
INSERT INTO test_1 SELECT CURRENT_DATE - (RANDOM() % 100), RANDOM() % 100, CAST(RANDOM() % 100 AS VARCHAR), CURRENT_TIMESTAMP;
CREATE TABLE test_2 (id BIGINT, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_2 SELECT ROW_NUMBER() OVER (), CAST(RANDOM() % 100 AS VARCHAR) FROM range(1000000);
SELECT a, COUNT(*) FROM (SELECT a, COUNT(*) FILTER (WHERE b > 50 OR c IS NULL) AS fun_res FROM test_1 GROUP BY a) GROUP BY fun_res;
