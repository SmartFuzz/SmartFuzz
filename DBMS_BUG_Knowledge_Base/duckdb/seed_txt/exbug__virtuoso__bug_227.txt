CREATE TABLE test_labels (id INTEGER PRIMARY KEY, label VARCHAR(100) NOT NULL);
INSERT INTO test_labels (id, label) VALUES (1, 'leden'), (2, 'únor'), (3, 'Leden'), (4, 'Únor');
SELECT label FROM test_labels WHERE label LIKE 'l%' OR label LIKE 'ú%';
SELECT (CASE WHEN label LIKE 'L%' THEN TRUE ELSE FALSE END) AS matches FROM test_labels WHERE label IS NOT NULL;
SELECT COUNT(*) FROM test_labels WHERE label LIKE 'l%' OR label IS NULL;
