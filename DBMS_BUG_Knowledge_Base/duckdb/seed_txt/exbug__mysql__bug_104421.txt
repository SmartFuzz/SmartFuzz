CREATE TABLE orders (order_id BIGINT NOT NULL, customer_id INTEGER NOT NULL, order_date TIMESTAMP NOT NULL, status VARCHAR(20) NOT NULL, PRIMARY KEY (order_id), INDEX customer_idx (customer_id));
INSERT INTO orders (customer_id, order_date, status) VALUES (1, NOW(), 'pending');
INSERT INTO orders (customer_id, order_date, status) VALUES (2, NOW(), 'completed');
SELECT * FROM orders WHERE customer_id = (SELECT customer_id FROM orders WHERE status = 'completed' LIMIT 1) ORDER BY order_date DESC LIMIT 5;
UPDATE orders SET status = 'shipped' WHERE order_id = (SELECT order_id FROM orders WHERE customer_id = 1 LIMIT 1);
