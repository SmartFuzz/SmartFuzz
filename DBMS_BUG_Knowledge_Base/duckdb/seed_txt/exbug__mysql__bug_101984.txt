CREATE TABLE tp3 (s1 VARCHAR(64) NOT NULL, s2 INT NOT NULL, PRIMARY KEY (s1, s2)) PARTITION BY HASH (s1) PARTITIONS 8;
CREATE TABLE tp4 (s1 VARCHAR(64) NOT NULL, s2 INT NOT NULL, PRIMARY KEY (s1, s2)) PARTITION BY HASH (s1) PARTITIONS 9;
INSERT INTO tp3 (s1, s2) SELECT CONCAT('value', ROW_NUMBER() OVER()), FLOOR(RAND() * 100) FROM (SELECT NULL FROM (SELECT 1 UNION ALL SELECT 2) AS select1 CROSS JOIN (SELECT 1 UNION ALL SELECT 2) AS select2 LIMIT 600) AS select_data;
INSERT INTO tp4 (s1, s2) SELECT CONCAT('value', ROW_NUMBER() OVER()), FLOOR(RAND() * 100) FROM (SELECT NULL FROM (SELECT 1 UNION ALL SELECT 2) AS select1 CROSS JOIN (SELECT 1 UNION ALL SELECT 2) AS select2 LIMIT 600) AS select_data;
SELECT partition_name, data_length FROM information_schema.tables WHERE table_name = 'tp3';
