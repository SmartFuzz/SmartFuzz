DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (b VARCHAR, c INTEGER, d DATE, PRIMARY KEY (b));
INSERT INTO t2 (b, c, d) VALUES (NULL, 1, DATE '2023-01-01');
ANALYZE t2;
EXPLAIN SELECT * FROM t2 WHERE c IS NULL;
