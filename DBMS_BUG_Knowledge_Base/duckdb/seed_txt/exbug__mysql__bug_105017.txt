CREATE TABLE t2(id INT PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2 VALUES (1, 'Alice', NULL), (2, 'Bob', '2023-01-01 10:00:00'), (3, 'Charlie', NULL);
ALTER TABLE t2 ADD COLUMN age INT CHECK (age >= 0);
UPDATE t2 SET age = 30 WHERE name = 'Alice';
SELECT name, age FROM t2 WHERE age IS NOT NULL;
