DROP TABLE IF EXISTS VEHICLES;
CREATE TABLE VEHICLES (id INT PRIMARY KEY, make VARCHAR(30), model VARCHAR(30), year INT, price DECIMAL(10, 2));
INSERT INTO VEHICLES VALUES (1, 'Ford', 'Mustang', 2020, 55000.00);
INSERT INTO VEHICLES VALUES (2, 'Chevrolet', NULL, NULL, 45000.00);
SELECT LAST_VALUE(make) OVER (ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_make FROM VEHICLES;
