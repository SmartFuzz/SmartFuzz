CREATE TABLE test_data (id INT, name VARCHAR, score FLOAT, group_id INT);
INSERT INTO test_data VALUES (1, 'Alice', NULL, 100);
INSERT INTO test_data VALUES (2, 'Bob', NULL, 100);
SELECT first_value(score ORDER BY id IGNORE NULLS) OVER (PARTITION BY group_id) FROM test_data;
SELECT last_value(score ORDER BY id IGNORE NULLS) OVER (PARTITION BY group_id) FROM test_data;
