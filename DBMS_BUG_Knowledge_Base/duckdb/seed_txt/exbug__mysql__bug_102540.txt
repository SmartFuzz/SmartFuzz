CREATE TABLE fuzztest (id BIGINT NOT NULL, username VARCHAR(20) NOT NULL, email VARCHAR(50), score FLOAT DEFAULT 0.0, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO fuzztest (id, username, email) VALUES (1, 'user1', 'user1@example.com');
UPDATE fuzztest SET email = 'user1_updated@example.com' WHERE id = 1;
UPDATE fuzztest SET score = NULL WHERE id = 1;
SELECT * FROM fuzztest WHERE score IS NULL;
