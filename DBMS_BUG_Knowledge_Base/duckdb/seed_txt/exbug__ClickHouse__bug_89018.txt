CREATE TABLE rocksDB(A BIGINT, B VARCHAR);
INSERT INTO rocksDB VALUES (1, 'test1'), (2, 'test2'), (3, 'test3');
INSERT INTO rocksDB SELECT number, CAST(city_hash(number) AS VARCHAR) FROM range(100000);
SELECT B FROM rocksDB WHERE A = 1;
SELECT B FROM rocksDB WHERE A IS NULL;
