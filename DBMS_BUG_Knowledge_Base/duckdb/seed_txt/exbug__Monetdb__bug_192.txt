CREATE TABLE pk1 (a1 INTEGER, a2 INTEGER, PRIMARY KEY (a1));
CREATE TABLE pk2 (b1 INTEGER PRIMARY KEY, b2 VARCHAR);
CREATE TABLE fk1 (c1 INTEGER, c2 VARCHAR, FOREIGN KEY (c1) REFERENCES pk1 (a1));
INSERT INTO pk1 (a1, a2) VALUES (1, 100);
INSERT INTO fk1 (c1, c2) VALUES (1, NULL);
SELECT * FROM fk1;
