CREATE TABLE t2 (
 id INTEGER NOT NULL,
 custom_prefix VARCHAR(14) NOT NULL,
 created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
 CONSTRAINT ck_t2_custom_prefix CHECK (custom_prefix ~ '^[a-z0-9-]+$'),
 PRIMARY KEY (id)
);
INSERT INTO t2 (id, custom_prefix) VALUES (1, 'valid-prefix'), (2, 'invalid_prefix!');
SELECT * FROM t2 WHERE custom_prefix IS NOT NULL;
UPDATE t2 SET custom_prefix = NULL WHERE id = 1;
ALTER TABLE t2 DROP CONSTRAINT ck_t2_custom_prefix;
