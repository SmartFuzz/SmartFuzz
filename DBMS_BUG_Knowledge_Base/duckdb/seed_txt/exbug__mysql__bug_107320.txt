CREATE TABLE spatial_test_v2 (id INT NOT NULL, latitude DECIMAL(10,8) NOT NULL, longitude DECIMAL(10,8) NOT NULL, geo_point POINT GENERATED ALWAYS AS (ST_SRID(Point(longitude, latitude), 4326)) STORED NOT NULL, PRIMARY KEY (id));
INSERT INTO spatial_test_v2 (latitude, longitude) VALUES (NULL, -75.417531);
INSERT INTO spatial_test_v2 (latitude, longitude) VALUES (42.062961, NULL);
SELECT * FROM spatial_test_v2 WHERE MBRContains(ST_SRID(LineString(Point(-75.5, 42.1), Point(-75.4, 42.0)), 4326), geo_point);
SELECT * FROM spatial_test_v2 WHERE geo_point IS NOT NULL AND latitude > 40.0;
