CREATE TABLE comment (id INTEGER PRIMARY KEY AUTOINCREMENT, path VARCHAR NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO comment (path) VALUES ('0.1.2.3'), ('0.1.2.4'), (NULL);
INSERT INTO comment (path) SELECT '0.' || i FROM (SELECT i FROM range(1, 500)) AS t(i);
CREATE INDEX idx_path ON comment (path);
SELECT * FROM comment;
