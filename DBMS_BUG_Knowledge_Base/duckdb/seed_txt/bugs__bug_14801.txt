CREATE TABLE test_data(id INTEGER, str VARCHAR, re VARCHAR, replacement VARCHAR);
INSERT INTO test_data VALUES (1, 'abracadabra', '^([^r]*r[^r]*)r(.*)', 'F');
CREATE OR REPLACE FUNCTION s(str VARCHAR, re VARCHAR, replacement VARCHAR) AS (SELECT regexp_extract(str, re, ['head', 'tail']) AS r FROM (SELECT r['head'] || replacement || r['tail'] FROM (SELECT regexp_extract(str, re, ['head', 'tail']) AS r) AS cte LIMIT 1));
SELECT s(str, re, replacement) FROM test_data WHERE id = 1;
CREATE TABLE patterns(pat VARCHAR);
