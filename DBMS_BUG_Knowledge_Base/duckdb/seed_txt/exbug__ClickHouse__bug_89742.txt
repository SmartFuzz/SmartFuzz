CREATE TABLE n3 (key BIGINT, value DOUBLE);
CREATE VIEW mv2 AS SELECT n1.key as k, n1.value as v1, n3.value as v2 FROM n1 JOIN n3 ON n1.key = n3.key;
INSERT INTO n3 SELECT number, number / 2.0 FROM generate_series(0, 9) as number;
INSERT INTO n1 SELECT number as key, CAST(number AS VARCHAR) FROM generate_series(0, 9) as number;
INSERT INTO n2 SELECT number, number * 10 FROM generate_series(0, 4) as number;
