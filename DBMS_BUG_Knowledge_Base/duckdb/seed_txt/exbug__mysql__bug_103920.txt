CREATE TABLE t_fts_extended (id INTEGER NOT NULL, title VARCHAR(255) NOT NULL, content TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO t_fts_extended (title, content) VALUES ('Example Title 1', 'This is some example content for testing purposes.');
INSERT INTO t_fts_extended (title, content) SELECT CONCAT('Example Title ', id), CONCAT('Content for ID ', id) FROM (SELECT ROW_NUMBER() OVER () AS id FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t) AS temp;
SELECT id FROM t_fts_extended WHERE title LIKE '%example%' OR content LIKE '%example%' LIMIT 10;
DELETE FROM t_fts_extended WHERE id IN (SELECT id FROM (SELECT id FROM t_fts_extended ORDER BY created_at DESC LIMIT 5) AS subquery);
