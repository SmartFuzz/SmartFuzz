CREATE TABLE t2 (id INTEGER NOT NULL, value VARCHAR(255), PRIMARY KEY (id));
INSERT INTO t2 (id, value) VALUES (1, 'test1'), (2, NULL), (3, 'test3');
CREATE OR REPLACE FUNCTION sp_test(p_value INTEGER) RETURNS TABLE(id INTEGER, value VARCHAR) AS $$ BEGIN RETURN QUERY SELECT * FROM t2 WHERE id = p_value;
END;
$$ LANGUAGE plpgsql;
SELECT * FROM sp_test(1);
