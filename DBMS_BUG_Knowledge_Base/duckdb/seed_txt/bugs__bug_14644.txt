CREATE TABLE vdata2 AS SELECT * FROM (VALUES (1), (2), (3)) v(id);
CREATE VIEW v2 AS SELECT * FROM vdata2;
CREATE TABLE t2(
        v_id INTEGER,
        FOREIGN KEY (v_id) REFERENCES v2 (id),
        CHECK (v_id > 0)
    );
INSERT INTO vdata2 VALUES (4);
INSERT INTO t2 (v_id) VALUES (4);
