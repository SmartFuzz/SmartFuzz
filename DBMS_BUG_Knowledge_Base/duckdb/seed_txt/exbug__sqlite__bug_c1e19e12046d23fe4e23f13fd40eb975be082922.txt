SET temp.recursive_triggers = true;
CREATE TABLE t2(id INTEGER, value TEXT, unique_value TEXT, PRIMARY KEY(id), UNIQUE(unique_value));
CREATE OR REPLACE FUNCTION trg_before_insert() RETURNS TRIGGER AS $$ BEGIN INSERT INTO t2(value) VALUES('triggered');
RETURN NEW;
END;
$$ LANGUAGE plpgsql;
