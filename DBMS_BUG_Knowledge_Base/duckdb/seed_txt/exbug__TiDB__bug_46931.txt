CREATE TABLE users (id INTEGER PRIMARY KEY, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
ALTER TABLE users ALTER COLUMN name SET DEFAULT (CONCAT('User_', id));
CREATE TABLE orders (order_id INTEGER PRIMARY KEY, user_id INTEGER, amount DECIMAL(10, 2), FOREIGN KEY (user_id) REFERENCES users(id));
INSERT INTO users (id, name) VALUES (1, NULL);
INSERT INTO orders (order_id, user_id, amount) VALUES (1, 1, NULL);
SELECT * FROM users;
