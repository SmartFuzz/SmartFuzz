CREATE TABLE test_identity (id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY, name VARCHAR(50), value INTEGER CHECK (value >= 0));
INSERT INTO test_identity (name, value) VALUES ('Test1', 10), ('Test2', NULL), ('Test3', -5);
SELECT id, name, value, id > 0 AS is_positive FROM test_identity;
CREATE TABLE related_data (rid INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY, test_id INTEGER REFERENCES test_identity(id), description TEXT);
INSERT INTO related_data (test_id, description) SELECT id, 'Description ' || id FROM test_identity WHERE value IS NOT NULL;
