CREATE TABLE t1 (c1 INTEGER, c2 VARCHAR, c3 DOUBLE) AS SELECT * FROM (VALUES (1, 'test', 1.5));
CREATE TABLE t2 (c4 INTEGER, c5 TIMESTAMP) AS SELECT * FROM (VALUES (NULL, CURRENT_TIMESTAMP));
INSERT INTO t1 VALUES (1, 'test', 1.5);
INSERT INTO t2 VALUES (NULL, CURRENT_TIMESTAMP);
SELECT tx.c1, ty.c4 FROM t1 tx JOIN t2 ty ON tx.c1 = ty.c4 WHERE tx.c3 > 1.0;
