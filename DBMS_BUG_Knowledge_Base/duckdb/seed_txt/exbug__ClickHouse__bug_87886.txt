CREATE TABLE test_improve_prewhere__fuzz_20 (id UInt32, name VARCHAR, created_at TIMESTAMP, status VARCHAR);
INSERT INTO test_improve_prewhere__fuzz_20 SELECT number, ['active', 'inactive', 'pending'][(number % 3) + 1], TIMESTAMP '2025-08-01 12:00:00' + INTERVAL number SECOND, CASE WHEN number % 2 = 0 THEN 'active' ELSE NULL END FROM range(10);
CREATE TABLE test_improve_prewhere__fuzz_21 (user_id UInt32, email VARCHAR, signup_date DATE, last_login TIMESTAMP);
INSERT INTO test_improve_prewhere__fuzz_21 SELECT number, concat('user', number, '@example.com'), DATE '2025-08-01' + number, TIMESTAMP '2025-09-01 08:00:00' + INTERVAL number SECOND FROM range(10);
SELECT * FROM test_improve_prewhere__fuzz_21 WHERE (signup_date IS NOT NULL) AND (last_login < TIMESTAMP '2025-09-05 00:00:00') AND (email LIKE '%@example.com');
