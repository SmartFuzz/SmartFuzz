CREATE TABLE t_sva(sva text, id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY);
INSERT INTO t_sva(sva) VALUES('1118090878'), ('2345010477'), (NULL);
CREATE OR REPLACE FUNCTION normalize_si(text) RETURNS text AS 'BEGIN RETURN substring($1,9,2) || substring($1,7,2) || substring($1,5,2) || substring($1,1,4);
END;
' LANGUAGE plpgsql;
CREATE OR REPLACE FUNCTION si_same(text,text) RETURNS int AS 'BEGIN IF normalize_si($1) < normalize_si($2) THEN RETURN -1;
