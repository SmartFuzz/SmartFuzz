CREATE TABLE bar (id INTEGER PRIMARY KEY AUTOINCREMENT, name VARCHAR(50), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO bar (name) VALUES ('test1'), ('test2'), (NULL);
BEGIN;
SELECT * FROM bar WHERE created_at IS NOT NULL;
ROLLBACK;
