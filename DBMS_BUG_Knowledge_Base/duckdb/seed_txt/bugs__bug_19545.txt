CREATE TABLE test_table (id INTEGER PRIMARY KEY, text_column BLOB, num_column INTEGER);
INSERT INTO test_table (text_column, num_column) VALUES (CAST('abc' AS BLOB), 1), (CAST('d"f' AS BLOB), 2), (CAST('g''h' AS BLOB), 3);
SELECT text_column, LENGTH(text_column) AS length_blob, text_column::VARCHAR AS converted_text, LENGTH(converted_text) AS length_converted FROM test_table;
SELECT id, text_column, text_column::VARCHAR AS converted_text, converted_text[1] AS first_char, converted_text[2] AS second_char FROM test_table;
SELECT a.text_column, b.text_column::VARCHAR AS converted_text FROM (SELECT text_column FROM test_table) AS a JOIN (SELECT text_column FROM test_table) AS b ON a.text_column = b.text_column;
