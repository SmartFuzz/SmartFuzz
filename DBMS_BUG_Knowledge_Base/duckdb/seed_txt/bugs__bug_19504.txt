CREATE TABLE documents_extended (document_identifier VARCHAR, text_content VARCHAR, author VARCHAR, doc_version INTEGER, published_date DATE);
INSERT INTO documents_extended VALUES ('doc1', 'The mallard is a dabbling duck that breeds throughout the temperate.', 'Hannes Mühleisen', 3, '2021-01-01'), ('doc2', 'The cat is a domestic species of small carnivorous mammal.', 'Laurens Kuiper', 2, '2020-05-15');
CREATE TABLE queries_extended (query VARCHAR, query_type VARCHAR DEFAULT 'animal');
INSERT INTO queries_extended VALUES ('cat', 'mammal'), ('dog', 'mammal'), ('duck', 'bird');
SELECT q.query, d.text_content FROM queries_extended q LEFT JOIN LATERAL (SELECT *, fts_main_documents.match_bm25(document_identifier, q.query) AS score FROM documents_extended d WHERE score IS NOT NULL ORDER BY score DESC LIMIT 1) AS matched ON TRUE;
