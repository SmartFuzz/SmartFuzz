CREATE TABLE test (id INTEGER PRIMARY KEY, message TEXT NOT NULL);
INSERT INTO test VALUES (1, 'hello\r\nworld');
INSERT INTO test VALUES (2, 'hello\fworld');
SELECT id, message FROM test WHERE message LIKE '%world%';
SELECT COUNT(*) FROM test WHERE message IS NULL;
