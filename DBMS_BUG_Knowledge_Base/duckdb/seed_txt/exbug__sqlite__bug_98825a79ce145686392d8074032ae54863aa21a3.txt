CREATE TABLE t2(y TEXT NOT NULL, z INTEGER DEFAULT 0);
INSERT INTO t2 VALUES('right'),('wrong'),(NULL),('right');
SELECT DISTINCT y FROM (SELECT y FROM t2 GROUP BY y HAVING COUNT(z) > 0) WHERE y='right' ORDER BY y;
CREATE TABLE t3(a DOUBLE, b TEXT);
INSERT INTO t3 VALUES(1.0, 'test'), (2.0, 'test'), (NULL, 'test');
