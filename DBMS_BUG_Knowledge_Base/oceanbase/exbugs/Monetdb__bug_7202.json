{
  "url": "https://github.com/MonetDB/MonetDB/issues/7202",
  "title": "DISTINCT does not work when sorting by additional columns",
  "description": "Performing a DISTINCT results in duplicate rows if we sort the table by additional columns than the ones that are projected. This is not critical as the additional columns can be removed from the sort, however it can cause problems with dynamically generated queries (which is how this bug was discovered).\n\n\n**To Reproduce**\n- Create a table and populate with some data:\n    ```sql\n    CREATE TABLE T (t1 int, t2 int);\n    INSERT INTO t VALUES (1, 1), (1, 2);\n    ```\n\n- DISTINCT with single sort column works as expected:  \n    ```sql\n    SELECT DISTINCT t1\n    FROM T \n    ORDER BY t1;\n\n    Returns:\n    +------+\n    | t1   |\n    +======+\n    |    1 |\n    +------+\n    ```\n\n- DISTINCT when sorting by both columns returns repeated rows:\n    ```sql\n    SELECT DISTINCT t1\n    FROM T\n    ORDER BY t1, t2;\n\n    Returns:\n    +------+\n    | t1   |\n    +======+\n    |    1 |\n    |    1 |\n    +------+\n    ```\n\n**Expected behavior**\nReturn just one row with value 1.\n\n**Software versions**\n- `monetdb -v`: `MonetDB Database Server Toolkit v11.41.11 (Jul2021-SP1)`.\n- OS: `Ubuntu 20.04.3 LTS`;\n- Monetdb installed from release packages with `apt` (packages `monetdb5-sql` and `monetdb-client`);",
  "date": "2021-11-22T16:12:50Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE T (t1 int, t2 int, t3 varchar(10));",
    "INSERT INTO T VALUES (1, 1, 'a'), (1, 2, 'b'), (2, 1, 'c');",
    "SELECT DISTINCT t1 FROM T ORDER BY t1;",
    "SELECT t1 FROM T GROUP BY t1 ORDER BY t1;",
    "SELECT t1, t3 FROM T GROUP BY t1, t3 ORDER BY t1, t3;"
  ],
  "key_bug_features": [
    "DISTINCT fails when sorting by additional columns"
  ]
}