{
  "url": "https://www.sqlite.org/src/tktview/38cb5df375",
  "title": "LIMIT ignored on compound query with subqueries",
  "description": "未找到User Comments标签",
  "date": "2010-10-06 17:06:4714.85 years ago",
  "vulnerable_version": "3.7.2",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users (id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO users (name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', 25);",
    "CREATE TABLE orders (id INT PRIMARY KEY, user_id INT, amount FLOAT, FOREIGN KEY(user_id) REFERENCES users(id));",
    "INSERT INTO orders (user_id, amount) VALUES (1, 100.50), (2, 200.75), (1, NULL);",
    "SELECT * FROM (SELECT * FROM users WHERE age IS NOT NULL UNION SELECT * FROM orders WHERE amount > 100) WHERE ROWNUM <= 1;"
  ],
  "key_bug_features": [
    "LIMIT ignored on compound query with subqueries"
  ]
}