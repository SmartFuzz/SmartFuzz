{
  "url": "https://www.sqlite.org/src/tktview/c04a8b8a4f",
  "title": "Trailing comments not stripped from views, invalid dumps",
  "description": "未找到User Comments标签",
  "date": "2012-01-27 18:15:3113.54 years ago",
  "vulnerable_version": "3.7.10",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE users (id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT CHECK(age >= 0), email VARCHAR(255) UNIQUE);",
    "CREATE VIEW user_info AS SELECT id, name, age FROM users;",
    "INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');",
    "SELECT * FROM user_info WHERE age IS NULL;",
    "SELECT * FROM user_info WHERE id IN (SELECT id FROM users WHERE age < 25);"
  ],
  "key_bug_features": [
    "Trailing comments not stripped from database views"
  ]
}