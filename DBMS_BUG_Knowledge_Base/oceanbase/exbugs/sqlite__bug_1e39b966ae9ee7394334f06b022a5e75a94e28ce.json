{
  "url": "https://www.sqlite.org/src/tktview/1e39b966ae",
  "title": "LEFT JOIN strength reduction optimization causes an error.",
  "description": "drh added on 2018-04-10 12:05:34:\nThe new LEFT JOIN strength reduction optimization is sometimes applied in\ncases where it should not be, resulting in an error.  The following\nquery demonstrates:\nCREATE TABLE t1(a INTEGER PRIMARY KEY, b);\nCREATE TABLE t2(x INTEGER PRIMARY KEY, y);\nINSERT INTO t1(a,b) VALUES(1,null);\n\nSELECT a FROM t1 LEFT JOIN t2 ON (b=x) \n WHERE NOT ( b IS NOT NULL AND y='xyz' );\nThe problem was caused by the NOT operator, which the prover did not\nrecognize as inverting the logic of its operand, effectively converting\nthe AND into an OR.\nThe problem was discovered by Raphael Michel and reported on the sqlite-users\nmailing list.",
  "date": "2018-04-10 12:05:347.34 years ago",
  "vulnerable_version": "3.23.0",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE t1(a INTEGER PRIMARY KEY, b VARCHAR(255), c FLOAT);",
    "CREATE TABLE t2(x INTEGER PRIMARY KEY, y VARCHAR(255), z INTEGER DEFAULT 0);",
    "INSERT INTO t1(a, b, c) VALUES(1, NULL, 3.14);",
    "INSERT INTO t2(x, y) VALUES(1, 'xyz'), (2, NULL), (3, 'abc');",
    "SELECT a FROM t1 LEFT JOIN t2 ON (b = y) WHERE NOT (b IS NOT NULL AND z = 0);"
  ],
  "key_bug_features": [
    "LEFT JOIN strength reduction optimization with NOT operator"
  ]
}