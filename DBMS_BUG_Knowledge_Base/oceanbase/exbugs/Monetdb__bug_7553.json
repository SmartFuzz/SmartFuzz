{
  "url": "https://github.com/MonetDB/MonetDB/issues/7553",
  "title": "Assertion failure when using `INNER JOIN` on `STARTSWITH`",
  "description": "The following test case crashed the monetdb server.",
  "date": "2024-07-17T06:38:35Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "DROP TABLE IF EXISTS t0;",
    "DROP TABLE IF EXISTS t1;",
    "CREATE TABLE t0 (c0 VARCHAR(500), c1 INT CHECK (c1 >= 0));",
    "CREATE TABLE t1 (c0 BOOLEAN NOT NULL DEFAULT TRUE);",
    "SELECT * FROM t1 INNER JOIN t0 ON t0.c0 LIKE CONCAT(CAST(t1.c0 AS CHAR), '%');"
  ],
  "key_bug_features": [
    "INNER JOIN using STARTSWITH with incompatible data types"
  ]
}