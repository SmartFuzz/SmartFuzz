{
  "url": "https://github.com/MonetDB/MonetDB/issues/3813",
  "title": "COPY INTO fails on perfectly clean CSV file",
  "description": "",
  "date": "2020-11-30T13:17:27Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "Monetdb",
  "bug_features_seed": [
    "CREATE TABLE Batting (playerID VARCHAR(255) NOT NULL, yearID INT CHECK (yearID > 1900), stint INT DEFAULT 1, teamID VARCHAR(255), lgID VARCHAR(255), G INT DEFAULT 0, AB INT DEFAULT 0, R INT DEFAULT 0, H INT DEFAULT 0, X2B INT DEFAULT 0, X3B INT DEFAULT 0, HR INT DEFAULT 0, RBI INT DEFAULT 0, SB INT DEFAULT 0, CS INT DEFAULT 0, BB INT DEFAULT 0, SO INT DEFAULT 0, IBB INT DEFAULT 0, HBP INT DEFAULT 0, SH INT DEFAULT 0, SF INT DEFAULT 0, GIDP INT DEFAULT 0);",
    "LOAD DATA INFILE '/path/to/clean.csv' INTO TABLE Batting FIELDS TERMINATED BY ',';",
    "SELECT playerID, yearID, teamID FROM Batting WHERE yearID = (SELECT MAX(yearID) FROM Batting);",
    "INSERT INTO Batting (playerID, yearID, teamID) VALUES ('player1', 2023, 'teamA');",
    "SELECT COUNT(*) FROM Batting WHERE G IS NULL OR AB IS NULL;"
  ],
  "key_bug_features": [
    "COPY INTO fails on clean CSV file"
  ]
}