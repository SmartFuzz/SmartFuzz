{
  "url": "https://github.com/openlink/virtuoso-opensource/issues/391",
  "title": "No results with VALUES + non-matching OPTIONAL",
  "description": "The following query unexpectedly returns an empty result set on the http://dbpedia.org/sparql endpoint (Virtuoso 7.20).\nprefix dbr: <http://dbpedia.org/resource/>\n\nselect ?a ?i where {\n\n    values ?a {\n        dbr:Fiat_500\n        dbr:Smart_Fortwo\n    }\n\n    optional { ?a <http://schema.org/image> ?i }\n\n}\n\nThe query breaks with a pattern like\noptional { ?a <http://schema.org/image> ?i }\n\nwhich doesn't match any triples.\nIt works as expected with a a matching pattern like:\noptional { ?a <http://dbpedia.org/ontology/thumbnail> ?i }",
  "date": "2015-04-29 10:07:53",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "virtuoso",
  "bug_features_seed": [
    "CREATE TABLE vehicles (id INT PRIMARY KEY, name VARCHAR(255), image_url VARCHAR(255));",
    "INSERT INTO vehicles (id, name, image_url) VALUES (1, 'Fiat 500', NULL), (2, 'Smart Fortwo', NULL);",
    "CREATE TABLE images (vehicle_id INT, image_url VARCHAR(255), FOREIGN KEY (vehicle_id) REFERENCES vehicles(id));",
    "INSERT INTO images (vehicle_id, image_url) VALUES (1, 'http://example.com/fiat500.jpg');",
    "SELECT v.name, i.image_url FROM vehicles v LEFT JOIN images i ON v.id = i.vehicle_id WHERE v.name IN ('Fiat 500', 'Smart Fortwo');"
  ],
  "key_bug_features": [
    "VALUES with non-matching OPTIONAL returns empty result set"
  ]
}