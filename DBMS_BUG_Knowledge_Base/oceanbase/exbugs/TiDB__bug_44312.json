{
  "url": "https://github.com/pingcap/tidb/issues/44312",
  "title": "After exchange partition, the indexLookUp will return wrong result with keepOrder",
  "description": "## Bug Report\r\n\r\nPlease answer these questions before submitting your issue. Thanks!\r\n\r\n### 1. Minimal reproduce step (Required)\r\n\r\n```\r\ncreate table t (id int not null, store_id int not null )  partition by range (store_id) (partition p0 values less than (6), partition p1 values less than (11), partition p2 values less than (16), partition p3 values less than (21) );\r\ncreate table t1(id int not null, store_id int not null);\r\ninsert into t values (1, 1);\r\ninsert into t values (2, 17);\r\ninsert into t1 values (0, 18);\r\nalter table t exchange partition p3 with table t1;\r\nalter table t add index idx(id);\r\nanalyze table t;\r\nselect *,_tidb_rowid from t use index(idx) order by id limit 2;\r\n```\r\n\r\n### 2. What did you expect to see? (Required)\r\n\r\n```\r\nmysql> select *,_tidb_rowid from t use index(idx) order by id limit 2;\r\n+----+----------+-------------+\r\n| id | store_id | _tidb_rowid |\r\n+----+----------+-------------+\r\n|  0 |       18 |           1 |\r\n|  1 |        1 |           1 |\r\n+----+----------+-------------+\r\n2 rows in set (0.00 sec)\r\n```\r\n\r\n### 3. What did you see instead (Required)\r\n\r\n```\r\nmysql> select *,_tidb_rowid from t use index(idx) order by id limit 2;\r\n+----+----------+-------------+\r\n| id | store_id | _tidb_rowid |\r\n+----+----------+-------------+\r\n|  1 |        1 |           1 |\r\n|  0 |       18 |           1 |\r\n+----+----------+-------------+\r\n2 rows in set (0.00 sec)\r\n```\r\n\r\n### 4. What is your TiDB version? (Required)\r\n\r\n<!-- Paste the output of SELECT tidb_version() -->\r\n\r\n",
  "date": "2023-05-31T13:08:33Z",
  "vulnerable_version": "Unknown",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "TiDB",
  "bug_features_seed": [
    "CREATE TABLE t2 (id INT NOT NULL, store_id INT NOT NULL, value VARCHAR(255) DEFAULT NULL) PARTITION BY RANGE (store_id) (PARTITION p0 VALUES LESS THAN (10), PARTITION p1 VALUES LESS THAN (20));",
    "CREATE TABLE t3 (id INT NOT NULL, store_id INT NOT NULL, description TEXT);",
    "INSERT INTO t2 VALUES (1, 5, 'A'), (2, 15, 'B'), (3, 25, 'C');",
    "INSERT INTO t3 VALUES (0, 18, 'Description 1'), (1, 19, 'Description 2');",
    "ALTER TABLE t2 EXCHANGE PARTITION p1 WITH TABLE t3;",
    "SELECT *, ROWID FROM t2 ORDER BY id LIMIT 2;"
  ],
  "key_bug_features": [
    "Index lookup returns incorrect results after partition exchange"
  ]
}