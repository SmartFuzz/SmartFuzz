{
  "url": "https://github.com/duckdb/duckdb/issues/16064",
  "title": "VIEW created after a USING statement fails when queried without a USING statement, even when queried with its qualified name",
  "description": "### What happens?\n\nIt looks like queries to a `VIEW` in a custom database fail if its `SELECT` part does not specify the database name. The queries work after executing a `USING` statement.\n\n### To Reproduce\n\n```sql\nATTACH IF NOT EXISTS ':memory:' AS \"memory1\";\nATTACH IF NOT EXISTS ':memory:' AS \"memory2\";\n\nUSE 'memory1';\nCREATE TABLE integers1(i INT, j INT, k INT);\nCREATE OR REPLACE VIEW view1 AS FROM integers1;\nSELECT * FROM view1;\n```\n```text\n┌───────┬───────┬───────┐\n│   i   │   j   │   k   │\n│ int32 │ int32 │ int32 │\n├───────┴───────┴───────┤\n│        0 rows         │\n└───────────────────────┘\n```\n```sql\nUSE 'memory2';\nSELECT * FROM memory1.view1;\n```\n```console\nCatalog Error: Table with name INTEGERS1 does not exist!\nDid you mean \"memory1.INTEGERS1\"?\n```\n\n### OS:\n\nios\n\n### DuckDB Version:\n\n1.0.0\n\n### DuckDB Client:\n\nCLI\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nParesh Thatte\n\n### Affiliation:\n\nBloomberg LP\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a stable release\n\n### Did you include all relevant data sets for reproducing the issue?\n\nYes\n\n### Did you include all code required to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [x] Yes, I have",
  "date": "2025-02-04 15:30:02",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE memory1.integers1(i INT, j INT, k INT DEFAULT 0);",
    "CREATE OR REPLACE VIEW memory1.view1 AS SELECT * FROM memory1.integers1;",
    "INSERT INTO memory1.integers1(i, j) VALUES (1, NULL), (2, 3);",
    "SELECT * FROM memory1.view1;",
    "SELECT * FROM memory1.integers1;"
  ],
  "key_bug_features": [
    "VIEW fails when queried without a USING statement"
  ]
}