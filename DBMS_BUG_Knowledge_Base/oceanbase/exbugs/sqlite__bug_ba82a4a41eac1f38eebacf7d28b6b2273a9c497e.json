{
  "url": "https://www.sqlite.org/src/tktview/ba82a4a41e",
  "title": "Query optimizer removes ORDER BY when it is needed",
  "description": "drh added on 2013-04-22 18:16:12:\nIn the following SQL, the query optimizer omits the ORDER BY clause under\nthe assumption that the output will appear in the correct order naturally,\nbut this assumption is in error.  The output is not correctly ordered by\nwithout the sorting step and so an incorrect result is returned:\nCREATE VIRTUAL TABLE test_fts4 USING FTS4 (word TEXT);\nINSERT INTO test_fts4 VALUES ('one two');\nINSERT INTO test_fts4 VALUES ('one three');\nINSERT INTO test_fts4 VALUES ('one two three');\nSELECT docid\n  FROM test_fts4,\n  (SELECT 'one two' terms UNION SELECT 'three') candidates\nWHERE\n  test_fts4 MATCH candidates.terms\nORDER BY docid;\nThis bug appears to have been introduced by the ORDER BY query optimizer\nenhancements of check-in [1e874629d7cf568] on 2012-09-28 and first appeared\nin release 3.7.15.\ndrh added on 2013-04-22 18:26:56:\nSee also ticket [a179fe746592d4].",
  "date": "2013-04-22 18:16:1212.30 years ago",
  "vulnerable_version": "3.7.16.2",
  "poc_type": "sql_text",
  "vulnerable_database": "oceanbase",
  "source_database": "sqlite",
  "bug_features_seed": [
    "CREATE TABLE test_fts4 (word VARCHAR(255) NOT NULL);",
    "INSERT INTO test_fts4 VALUES ('one two');",
    "INSERT INTO test_fts4 VALUES ('one three');",
    "INSERT INTO test_fts4 VALUES ('one two three');",
    "SELECT docid FROM test_fts4, (SELECT 'one two' AS terms UNION SELECT NULL) candidates WHERE test_fts4.word LIKE '%' || candidates.terms || '%' ORDER BY docid;"
  ],
  "key_bug_features": [
    "Query optimizer omits necessary ORDER BY clause"
  ]
}