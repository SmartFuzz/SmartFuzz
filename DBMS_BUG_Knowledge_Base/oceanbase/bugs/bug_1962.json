{
  "url": "https://github.com/oceanbase/oceanbase/issues/1962",
  "title": "[Bug]: 含有 `ORDER BY` 子句时有时没有正确排序",
  "vulnerable_database": "oceanbase",
  "date": "2024-05-11T06:56:21Z",
  "vulnerable_version": "Unknown",
  "description": "master分支版本，含有 `ORDER BY` 子句时有时没有正确排序",
  "poc": [
    "DROP TENANT mysql FORCE;",
    "DROP RESOURCE POOL pool_for_tenant_mysql;",
    "CREATE RESOURCE UNIT 4c7g MAX_CPU 4, MIN_CPU 4, MEMORY_SIZE '7G';",
    "create resource pool pool_test unit='4c7g', unit_num=1, zone_list=('zone1' ,'zone2', 'zone3');",
    "create tenant test resource_pool_list=('pool_test'), primary_zone='RANDOM',comment 'test tenant', charset='utf8' set ob_tcp_invited_nodes='%', recyclebin=OFF, ob_query_timeout=5000000;"
  ],
  "key_bug_features": [
    "Incorrect sorting with ORDER BY clause"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_ordering (id INT PRIMARY KEY, name VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_ordering (id, name) VALUES (1, 'Alice'), (2, 'Bob'), (3, NULL), (4, 'Charlie');",
    "SELECT * FROM test_ordering ORDER BY name ASC;",
    "SELECT * FROM test_ordering WHERE created_at IS NOT NULL ORDER BY created_at DESC;",
    "CREATE TABLE test_ordering_subquery AS SELECT id, name FROM test_ordering WHERE name IS NOT NULL ORDER BY id;"
  ]
}