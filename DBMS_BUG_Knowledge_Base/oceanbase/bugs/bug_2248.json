{
  "url": "https://github.com/oceanbase/oceanbase/issues/2248",
  "title": "[Bug]: Unexpected result under repeatable read isolation level with consistent snapshot",
  "vulnerable_database": "oceanbase",
  "date": "2025-03-26T07:52:56Z",
  "vulnerable_version": "Unknown",
  "description": "<!-- A clear and concise description of bug. -->\nWhen I tested the database, I found a problem.\nAs in the example below, with repeatable read isolation level, transaction 2 reads the update from transaction 1. In my opinion, transaction 2 shouldn't read because the snapshot was created at the beginning of the transaction and it can only see its own snapshot.",
  "poc": [
    "DROP TABLE IF EXISTS tAzuAxbi;",
    "CREATE TABLE tAzuAxbi (",
    "  ID INT,",
    "  VAL INT,",
    "  c0 INT UNSIGNED,",
    "  c1 VARCHAR(100),",
    "  c2 BOOLEAN",
    ");",
    "INSERT INTO tAzuAxbi (ID, VAL, c0, c1, c2) VALUES (106, 119, 8750, 'wW#}v', 1);",
    "INSERT INTO tAzuAxbi (ID, VAL, c0, c1, c2) VALUES (107, 120, 272, '%wIcpm#%?', 1);",
    "INSERT INTO tAzuAxbi (ID, VAL, c0, c1, c2) VALUES (108, 121, 7271, '9751.366766637913', 0);",
    "SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;",
    "UPDATE tAzuAxbi",
    "SET VAL = 122, c0 = 2753, c2 = 1",
    "WHERE (c1 <= 'wW#}v' AND c1 >= '');",
    "COMMIT;",
    "SELECT ID, VAL, c0, c1, c2",
    "FROM tAzuAxbi",
    "WHERE (c1 <= 'wW#}v' AND c1 >= '')",
    "ORDER BY ID;"
  ],
  "key_bug_features": [
    "Transaction reads updates outside its snapshot visibility"
  ],
  "bug_features_seed": [
    "DROP TABLE IF EXISTS tTest;",
    "CREATE TABLE tTest (ID INT PRIMARY KEY, VAL INT, c0 INT UNSIGNED NOT NULL, c1 VARCHAR(100) DEFAULT '', c2 BOOLEAN DEFAULT FALSE);",
    "INSERT INTO tTest (ID, VAL, c0, c1, c2) VALUES (1, 100, 5000, 'test1', TRUE);",
    "INSERT INTO tTest (ID, VAL, c0, c1, c2) VALUES (2, 200, 6000, 'test2', FALSE);",
    "SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;"
  ]
}