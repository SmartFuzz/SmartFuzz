{
  "url": "https://github.com/oceanbase/oceanbase/issues/2211",
  "title": "[Bug]: The comparison result between a zero DATETIME value and NULL does not match the expectation.",
  "vulnerable_database": "oceanbase",
  "date": "2024-12-24T07:35:45Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "DROP TABLE IF EXISTS t2;",
    "CREATE TABLE IF NOT EXISTS t2(\r\n    c1 DATETIME PRIMARY KEY\r\n) WITH COLUMN GROUP (ALL COLUMNS, EACH COLUMN);",
    "INSERT INTO t2 VALUES ('0000-00-00 00:00:00');",
    "SELECT /*+ USE_COLUMN_TABLE(t2) */ TRUE FROM t2 WHERE (t2.c1) IS NULL;",
    "CREATE TABLE IF NOT EXISTS t2(\r\n    ->     c1 DATETIME PRIMARY KEY\r\n    -> ) WITH COLUMN GROUP (ALL COLUMNS, EACH COLUMN);",
    "SELECT VERSION();",
    "SELECT @@sql_mode;",
    "do not use the PRIMARY KEY constraint on column c1, the query behavior is correct.\r\n\r\n```sql\r\nDROP TABLE IF EXISTS t2;",
    "CREATE TABLE IF NOT EXISTS t2(\r\n    c1 DATETIME\r\n) WITH COLUMN GROUP (ALL COLUMNS, EACH COLUMN);",
    "CREATE TABLE IF NOT EXISTS t2(\r\n    ->     c1 DATETIME\r\n    -> ) WITH COLUMN GROUP (ALL COLUMNS, EACH COLUMN);"
  ],
  "key_bug_features": [
    "Comparison of zero DATETIME value with NULL"
  ],
  "bug_features_seed": [
    "DROP TABLE IF EXISTS t1;",
    "CREATE TABLE t1 (c1 DATETIME, c2 INT);",
    "INSERT INTO t1 VALUES ('0000-00-00 00:00:00', 1);",
    "SELECT * FROM t1 WHERE c1 IS NULL;",
    "SELECT * FROM t1 WHERE c1 = '0000-00-00 00:00:00' AND c2 IS NOT NULL;"
  ]
}