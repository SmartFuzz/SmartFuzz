{
  "url": "https://github.com/oceanbase/oceanbase/issues/2018",
  "title": "[Bug]: Bit shift for BLOB type behaves incosistently with MySQL8",
  "vulnerable_database": "oceanbase",
  "date": "2024-06-19T11:46:35Z",
  "vulnerable_version": "Unknown",
  "description": "<!-- A clear and concise description of bug. -->\n\nMySQL's right shift supports two types: bigint and binary [1]. So data in BLOB type evaluates to a binary. But OB applies an implicit cast to bigint and causes inconsistency.\n\n\n[1] https://dev.mysql.com/doc/refman/8.4/en/bit-functions.html#operator_right-shift\n\n- OB Version(`LD_LIBRARY_PATH=../lib:$LD_LIBRARY_PATH ./observer -V`):",
  "poc": [
    "create table t2(a blob);",
    "create table t3(a blob);",
    "insert into t2 values(0xC2A0);",
    "insert into t3 values(0xC2);",
    "select * from t2,t3 where (t2.a >> 4) = t3.a;",
    "explain select * from t2,t3 where (t2.a >> 4) = t3.a;"
  ],
  "key_bug_features": [
    "Implicit cast from BLOB to bigint during right shift"
  ],
  "bug_features_seed": [
    "CREATE TABLE t1(a BLOB NOT NULL, b BIGINT);",
    "CREATE TABLE t2(a BLOB, c INT DEFAULT NULL);",
    "INSERT INTO t1 VALUES(0xC2A0, 10);",
    "INSERT INTO t2 VALUES(0xC2, NULL);",
    "SELECT * FROM t1, t2 WHERE (t1.a >> 4) = t2.a AND t2.c IS NULL;"
  ]
}