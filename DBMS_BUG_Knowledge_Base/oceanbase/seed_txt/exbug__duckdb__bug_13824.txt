CREATE TABLE test2(id int, name varchar(255), age int);
INSERT INTO test2 VALUES (1, 'a'), (2, 'b'), (3, 'c'), (4, 'A'), (5, 'G'), (6, 'd'), (7, NULL);
SELECT MIN(name) FROM test2 ORDER BY name COLLATE 'en_US';
SELECT MAX(name) FROM test2 ORDER BY name COLLATE 'en_US';
SELECT * FROM test2 ORDER BY name COLLATE 'en_US';
