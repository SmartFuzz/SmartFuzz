CREATE TABLE test_table (id INT PRIMARY KEY, value INT, flag TINYINT(1));
INSERT INTO test_table (value, flag) VALUES (1, 1), (2, 0), (3, NULL);
SELECT (1 IN (SELECT CASE WHEN 2 = 1 THEN 1 ELSE 0 END)) AS result;
SELECT * FROM test_table WHERE flag = 1;
SELECT (NULL IN (SELECT CASE WHEN 1 = 1 THEN 1 ELSE 0 END)) AS null_result;
