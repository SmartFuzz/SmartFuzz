CREATE TABLE test (data JSON) PRIMARY KEY (data->>'a');
ALTER TABLE test MODIFY COLUMN data JSON(b VARCHAR);
ALTER TABLE test MODIFY COLUMN data JSON(a BIGINT, b VARCHAR);
INSERT INTO test VALUES (JSON('{"a": 1}'));
SELECT * FROM test;
