CREATE TABLE users(id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT);
INSERT INTO users(name, age) VALUES('Alice', 30);
INSERT INTO users(name, age) VALUES('Bob', NULL);
SELECT users.id, users.name FROM users LEFT JOIN (SELECT id, name FROM users WHERE age IS NOT NULL) filtered_users ON users.id = filtered_users.id;
SELECT users.name, COALESCE(filtered_users.name, 'Unknown') filtered_name FROM users LEFT JOIN (SELECT id, name FROM users WHERE age < 25) filtered_users ON users.id = filtered_users.id;
