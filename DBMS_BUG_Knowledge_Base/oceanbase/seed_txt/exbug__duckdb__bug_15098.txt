CREATE TABLE test_data (id INTEGER PRIMARY KEY, value VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, value) VALUES (1, 'first'), (2, 'second'), (3, 'third'), (4, 'fourth');
SELECT MAX(value) FROM test_data WHERE id IS NOT NULL;
SELECT MAX(line) FROM (SELECT line FROM read_csv('seq.txt', header=false, sep='', columns('line' VARCHAR)) ORDER BY rowid) AS subquery;
SELECT MAX(value) FROM test_data ORDER BY created_at;
