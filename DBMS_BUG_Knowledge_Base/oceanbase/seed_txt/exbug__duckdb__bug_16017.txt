CREATE TABLE users (id INT PRIMARY KEY, name VARCHAR(255), age INT);
INSERT INTO users (id, name, age) VALUES (1, 'Alice', 30);
SELECT id, name AS user_name FROM users;
SELECT age AS user_age FROM users WHERE id = 1;
SELECT id AS user_id, name AS user_name FROM (SELECT id, name FROM users) subquery;
