CREATE TABLE test_table (id INTEGER PRIMARY KEY, value VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, value) VALUES (1, 'test1'), (2, 'test2'), (3, NULL);
CREATE OR REPLACE FUNCTION example_macro(x VARCHAR) RETURNS VARCHAR AS BEGIN RETURN (SELECT value FROM test_table WHERE id = 1);
END;
SELECT example_macro(value) FROM test_table WHERE id = 2;
SELECT ARRAY_AGG(z * 2) AS doubled_values FROM (SELECT 1 AS z UNION ALL SELECT 2 UNION ALL SELECT 3) AS temp;
