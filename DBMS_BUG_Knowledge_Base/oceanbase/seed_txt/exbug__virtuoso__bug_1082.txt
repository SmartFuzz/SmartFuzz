CREATE TABLE DB.DBA.TEST_GRAPH (ID INT PRIMARY KEY, GRAPH_URI VARCHAR(255) NOT NULL, HOST VARCHAR(100));
INSERT INTO DB.DBA.TEST_GRAPH (ID, GRAPH_URI, HOST) VALUES (1, 'https://example.com/graph', 'example.com');
INSERT INTO DB.DBA.TEST_GRAPH (ID, GRAPH_URI, HOST) VALUES (2, 'https://example.com/graph', 'example.com');
SELECT * FROM DB.DBA.TEST_GRAPH WHERE GRAPH_URI LIKE 'https://%';
UPDATE DB.DBA.TEST_GRAPH SET GRAPH_URI = NULL WHERE ID = 1;
