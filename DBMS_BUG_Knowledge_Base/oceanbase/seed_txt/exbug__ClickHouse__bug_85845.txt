CREATE TABLE IF NOT EXISTS test_table (id UUID DEFAULT SYS_GUID(), name VARCHAR(255), age INT, salary FLOAT) PARTITION BY HASH(id) PARTITIONS 4;
INSERT INTO test_table (name, age, salary) VALUES ('Alice', 30, 70000.50);
INSERT INTO test_table (name, age) VALUES ('Bob', 25, NULL);
ALTER TABLE test_table ADD COLUMN is_active TINYINT DEFAULT 1;
SELECT id, name, age, salary, is_active FROM test_table WHERE age > 20 ORDER BY salary DESC;
