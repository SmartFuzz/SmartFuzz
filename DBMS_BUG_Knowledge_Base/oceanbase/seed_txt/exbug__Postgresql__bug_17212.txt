CREATE TABLE temp_users(id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(100) NOT NULL, age INT CHECK (age >= 0));
CREATE INDEX temp_users_name_idx ON temp_users(name);
INSERT INTO temp_users(name, age) VALUES ('Alice', 30), (NULL, 25), ('Bob', -1);
SELECT * FROM temp_users WHERE age IS NULL OR age < 0;
SELECT SLEEP(3) FROM temp_users WHERE id IN (SELECT id FROM temp_users WHERE age > 20);
