CREATE TABLE n4 (key UInt64, value Float64) ENGINE = MergeTree ORDER BY key;
INSERT INTO n4 SELECT number, number / 2.0 FROM dual CONNECT BY LEVEL <= 10;
CREATE MATERIALIZED VIEW mv2 AS SELECT n1.key as k, n1.value as v1, n4.value as v2 FROM n1 JOIN n4 ON n1.key = n4.key;
INSERT INTO n1 SELECT number as key, to_char(number + 50) FROM dual CONNECT BY LEVEL <= 10;
SELECT * FROM mv2 JOIN n3 ON mv2.k = n3.key WHERE mv2.v2 IS NOT NULL ORDER BY mv2.k, n3.key;
