CREATE TABLE r2(id INT, x1 FLOAT, x2 FLOAT, y1 FLOAT, y2 FLOAT, PRIMARY KEY(id));
CREATE TABLE t2(id INT PRIMARY KEY, x1 FLOAT, x2 FLOAT, y1 FLOAT, y2 FLOAT NOT NULL CHECK(y2 > y1));
CREATE TRIGGER tr2 AFTER INSERT ON t2 FOR EACH ROW INSERT INTO r2 VALUES(NEW.id, NEW.x1, NEW.x2, NEW.y1, NEW.y2);
INSERT INTO t2 VALUES(1, 1.5, 2.5, 3.5, 4.5);
CREATE TABLE newtab(a VARCHAR(255), b INT DEFAULT 0);
