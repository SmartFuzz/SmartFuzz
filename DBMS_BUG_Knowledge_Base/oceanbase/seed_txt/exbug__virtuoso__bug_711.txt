CREATE TABLE locations (id INT PRIMARY KEY, name VARCHAR(255), area FLOAT, type VARCHAR(100), neighborhood_id INT);
INSERT INTO locations (id, name, area, type, neighborhood_id) VALUES (1, 'County A', 500.0, 'place_with_neighborhoods', NULL);
INSERT INTO locations (id, name, area, type, neighborhood_id) VALUES (2, 'County B', 300.0, 'place_with_neighborhoods', 1);
SELECT l1.name, l2.name, l1.area FROM locations l1 JOIN locations l2 ON l1.id = l2.neighborhood_id WHERE l1.type = 'place_with_neighborhoods' ORDER BY l1.area DESC LIMIT 1;
SELECT name, area FROM locations WHERE area IS NOT NULL AND area > 250.0 ORDER BY area ASC;
