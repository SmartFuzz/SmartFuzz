CREATE TABLE test_boolean (id INT PRIMARY KEY, isIndirect TINYINT NOT NULL);
INSERT INTO test_boolean (id, isIndirect) VALUES (1, 1);
INSERT INTO test_boolean (id, isIndirect) VALUES (2, 0);
SELECT id, isIndirect, CASE WHEN isIndirect = 1 THEN 'http://www.w3.org/2001/XMLSchema#boolean' ELSE 'http://www.w3.org/2001/XMLSchema#integer' END AS datatype FROM test_boolean;
SELECT id FROM test_boolean WHERE isIndirect IS NULL;
