CREATE TABLE wikistat_iceberg_v2 (time TIMESTAMP, project VARCHAR(255), subproject VARCHAR(255), path VARCHAR(255), hits BIGINT, created_at DATE DEFAULT CURRENT_DATE()) ENGINE = OBEngine PARTITION BY (path, created_at);
INSERT INTO wikistat_iceberg_v2 SELECT CURRENT_TIMESTAMP AS time, 'project1' AS project, 'subproject1' AS subproject, '/path/to/resource' AS path, CAST(100 AS BIGINT), CURRENT_DATE() AS created_at;
CREATE TABLE wikistat_temp (temp_time TIMESTAMP, temp_hits BIGINT) ENGINE = MEMORY;
INSERT INTO wikistat_temp SELECT time, hits FROM wikistat_iceberg_v2 WHERE hits > 50;
SELECT temp_time, SUM(temp_hits) AS total_hits FROM wikistat_temp GROUP BY temp_time HAVING total_hits > 100;
