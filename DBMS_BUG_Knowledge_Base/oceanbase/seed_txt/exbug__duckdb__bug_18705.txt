CREATE TABLE test_strings (id INTEGER PRIMARY KEY, str_value VARCHAR(100));
INSERT INTO test_strings (id, str_value) VALUES (1, 'HELLO'), (2, 'world'), (3, 'HELLO world');
SELECT INSTR(UPPER(str_value), 'EL') FROM test_strings WHERE id = 1;
SELECT INSTR(UPPER(str_value), 'HELLO') FROM test_strings WHERE id = 3;
SELECT CASE WHEN INSTR(UPPER(str_value), 'HELLO') > 0 THEN 1 ELSE 0 END FROM test_strings WHERE id = 3;
