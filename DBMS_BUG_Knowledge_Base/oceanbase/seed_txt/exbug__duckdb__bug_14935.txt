CREATE TABLE t2 (id INTEGER PRIMARY KEY, status VARCHAR(10) NOT NULL);
INSERT INTO t2 VALUES (1, 'a'), (2, 'b'), (3, 'a');
INSERT INTO t2 VALUES (4, 'c');
SELECT CASE WHEN COUNT(DISTINCT status) = 1 THEN MIN(status) END AS result FROM t2;
