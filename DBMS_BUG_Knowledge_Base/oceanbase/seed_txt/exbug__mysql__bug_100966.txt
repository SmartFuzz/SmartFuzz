CREATE TABLE user_card_info_hash_4 (id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, amount DECIMAL(10, 2) CHECK (amount >= 0));
BEGIN;
INSERT INTO user_card_info_hash_4 (name, amount) SELECT CONCAT('User', id), ROUND(RAND() * 1000, 2) FROM (SELECT ROW_NUMBER() OVER () AS id FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t1, (SELECT 1) t2) AS numbers LIMIT 10000000;
SELECT COUNT(*) FROM user_card_info_hash_4;
COMMIT;
