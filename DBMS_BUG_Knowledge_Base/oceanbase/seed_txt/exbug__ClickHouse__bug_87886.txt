CREATE TABLE test_improve_prewhere__fuzz_20 (`id` UInt32, `name` String, `created_at` Nullable(DateTime), `status` Nullable(String)) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_improve_prewhere__fuzz_20 SELECT number, CASE number % 3 WHEN 1 THEN 'active' WHEN 2 THEN 'inactive' ELSE 'pending' END, toDateTime('2025-08-01 12:00:00') + number, CASE WHEN number % 2 = 0 THEN 'active' ELSE NULL END FROM (SELECT ROW_NUMBER() OVER () AS number FROM dual CONNECT BY LEVEL <= 10);
CREATE TABLE test_improve_prewhere__fuzz_21 (`user_id` UInt32, `email` String, `signup_date` Nullable(Date), `last_login` Nullable(DateTime)) ENGINE = MergeTree() ORDER BY user_id;
INSERT INTO test_improve_prewhere__fuzz_21 SELECT number, concat('user', number, '@example.com'), toDate('2025-08-01') + number, toDateTime('2025-09-01 08:00:00') + number FROM (SELECT ROW_NUMBER() OVER () AS number FROM dual CONNECT BY LEVEL <= 10);
SELECT * FROM test_improve_prewhere__fuzz_21 WHERE (signup_date IS NOT NULL) AND (last_login < toDateTime('2025-09-05 00:00:00')) AND (email LIKE '%@example.com');
