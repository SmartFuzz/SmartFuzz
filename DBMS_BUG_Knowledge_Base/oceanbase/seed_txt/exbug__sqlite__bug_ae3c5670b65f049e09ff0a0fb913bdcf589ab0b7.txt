CREATE TABLE fuzz_t(a INT);
INSERT INTO fuzz_t VALUES(1);
CREATE TRIGGER fuzz_tr AFTER INSERT ON fuzz_t FOR EACH ROW BEGIN SELECT 1+1 AS result1, 1+1 AS result2, 1+1 AS result3;
END;
INSERT INTO fuzz_t VALUES(2);
SELECT * FROM fuzz_t;
