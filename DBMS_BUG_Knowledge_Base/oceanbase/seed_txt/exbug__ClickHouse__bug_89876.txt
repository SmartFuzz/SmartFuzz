CREATE TABLE test2 (id INT, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP) ENGINE = REPLICA('/oceanbase/tables/{database}/{table}', '{replica}') ORDER BY id;
CREATE TABLE test2_dist AS test2 ENGINE = DISTRIBUTED(default, default, test2);
INSERT INTO test2 SELECT number, CONCAT('Name', number) FROM dual CONNECT BY LEVEL <= 100;
ALTER TABLE test2 ATTACH PARTITION () FROM test2_dist;
SELECT COUNT(*) FROM test2_dist WHERE 1=1;
