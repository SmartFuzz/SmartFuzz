CREATE TABLE t222 (id INT PRIMARY KEY, name VARCHAR(1024), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t222 VALUES (1, 'first_entry'), (2, 'second_entry'), (3, NULL), (4, 'fourth_entry');
CREATE TRIGGER update_name BEFORE INSERT ON t222 FOR EACH ROW BEGIN IF NEW.name IS NULL THEN SET NEW.name = 'default_name';
END IF;
END;
INSERT INTO t222 (id, name) SELECT id + 10, name FROM t222 WHERE id < 3;
