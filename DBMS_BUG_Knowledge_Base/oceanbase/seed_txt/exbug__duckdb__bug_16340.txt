CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), description TEXT);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description.');
SELECT CASE WHEN description IS NOT NULL THEN REGEXP_REPLACE(description, '[*]', 'X') ELSE NULL END FROM test_table WHERE id = 1;
SELECT CASE WHEN name IS NOT NULL THEN REGEXP_SUBSTR(name, '[*]') ELSE NULL END FROM test_table WHERE id = 1;
SELECT REGEXP_REPLACE(NULL, '[*]', 'X') AS result;
