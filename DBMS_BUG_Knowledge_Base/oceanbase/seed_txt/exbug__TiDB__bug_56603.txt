CREATE TABLE test1(a INT, b VARCHAR(255), c DATE);
INSERT INTO test1(a, b, c) VALUES (1, 'test1', '2023-01-01'), (2, 'test2', NULL), (3, 'test3', '2023-01-03');
CREATE TABLE test2(d INT, e FLOAT DEFAULT 0.0);
INSERT INTO test2(d, e) VALUES (1, 1.1), (2, NULL), (3, 3.3);
WITH CTE AS (SELECT a FROM test1 WHERE b IS NOT NULL GROUP BY a), CTE1 AS (SELECT a FROM test1 UNION ALL SELECT CTE.a FROM CTE JOIN CTE1 ON CTE.a = CTE1.a) SELECT * FROM CTE1;
