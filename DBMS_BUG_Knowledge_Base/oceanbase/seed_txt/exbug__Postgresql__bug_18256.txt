CREATE TABLE partitioned_table (id INT PRIMARY KEY AUTO_INCREMENT, value INT, description TEXT) PARTITION BY RANGE (value);
CREATE TABLE partitioned_table_1 PARTITION OF partitioned_table FOR VALUES FROM (1) TO (10);
CREATE INDEX idx_value ON partitioned_table (value);
INSERT INTO partitioned_table (value, description) VALUES (5, NULL), (NULL, 'Test'), (15, 'Boundary Test');
SELECT * FROM partitioned_table WHERE value IS NULL OR value < 10;
