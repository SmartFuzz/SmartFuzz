CREATE TABLE test_table (id INT PRIMARY KEY, value VARCHAR(255));
INSERT INTO test_table (value) VALUES (''), (''), (NULL);
SELECT CASE WHEN COUNT(value) = 0 THEN '' ELSE group_concat(value, '') END AS result FROM test_table;
INSERT INTO test_table (value) VALUES ('a'), ('b'), ('');
SELECT CASE WHEN COUNT(value) = 0 THEN '' ELSE group_concat(value, '') END AS result FROM test_table WHERE value IS NOT NULL;
