CREATE TABLE random_numbers AS SELECT * FROM (SELECT LEVEL AS number FROM DUAL CONNECT BY LEVEL <= 100);
SELECT DBMS_RANDOM.SEED(0.5);
SELECT * FROM random_numbers SAMPLE(10);
CREATE TABLE mixed_types (id INT, description VARCHAR(255), optional_value VARCHAR(255));
INSERT INTO mixed_types VALUES (1, 'text', NULL);
SELECT * FROM mixed_types WHERE optional_value IS NULL;
