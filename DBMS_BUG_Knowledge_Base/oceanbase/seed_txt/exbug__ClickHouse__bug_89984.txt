CREATE TABLE products (id Int32, name VARCHAR(255), price FLOAT, created_at TIMESTAMP);
INSERT INTO products VALUES (1, 'Laptop', 999.99, CURRENT_TIMESTAMP);
INSERT INTO products VALUES (2, 'Smartphone', 499.99, CURRENT_TIMESTAMP);
SELECT id, AVG(price) FROM products GROUP BY id;
SELECT id, COUNT(*) FROM products GROUP BY id, created_at;
