CREATE TABLE test_table (id INTEGER, data VARCHAR(255));
INSERT INTO test_table VALUES (1, '[1, 2, 3]'), (2, 'not_a_list');
SELECT CASE WHEN data IS NOT NULL THEN JSON_VALUE(data, '$[0]') ELSE '0' END AS x FROM test_table WHERE id = 1;
SELECT CASE WHEN id = 2 THEN JSON_VALUE(data, '$[0]') ELSE 'No List' END AS x FROM test_table;
SELECT CASE WHEN id < 3 THEN JSON_VALUE(data, '$[0]') ELSE NULL END AS x FROM test_table;
