CREATE TABLE test_data (id INTEGER PRIMARY KEY, url VARCHAR(255), format VARCHAR(10) CHECK (format IN ('JSON', 'CSV'));
INSERT INTO test_data (id, url, format) VALUES (1, 'http://example.com/data.json', 'JSON');
INSERT INTO test_data (id, url, format) VALUES (2, 'http://example.com/data.csv', 'CSV');
SELECT id, url, format, CASE WHEN format = 'JSON' THEN (SELECT * FROM read_json_auto(url)) ELSE (SELECT * FROM read_csv(url)) END AS data FROM test_data;
CREATE TABLE processed_data AS SELECT id, url, format FROM test_data WHERE url IS NOT NULL;
