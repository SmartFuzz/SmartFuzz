DROP AGGREGATE IF EXISTS python_aggregate;
CREATE FUNCTION python_aggregate(val INTEGER) RETURNS INTEGER AS $$ BEGIN DECLARE result INTEGER;
SELECT COALESCE(SUM(val), 0) INTO result FROM (SELECT val FROM test_empty) AS subquery;
RETURN result;
END;
$$ LANGUAGE plpgsql;
