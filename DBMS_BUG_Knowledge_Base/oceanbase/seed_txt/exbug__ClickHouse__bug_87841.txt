CREATE TABLE table_src (order_id Int64, random_date Date, amount Float64) ENGINE = MergeTree() ORDER BY order_id;
CREATE TABLE table_dst (order_id Int64, random_date Date, amount Float64) ENGINE = MergeTree() PARTITION BY EXTRACT(YEAR_MONTH FROM random_date) ORDER BY order_id;
INSERT INTO table_src SELECT number AS order_id, DATE_ADD(CURRENT_DATE, INTERVAL -FLOOR(RAND() * 3650) DAY) AS random_date, RAND() * 100 AS amount FROM dual CONNECT BY LEVEL <= 1000000;
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src WHERE amount > 50.0;
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src ORDER BY EXTRACT(YEAR_MONTH FROM random_date);
