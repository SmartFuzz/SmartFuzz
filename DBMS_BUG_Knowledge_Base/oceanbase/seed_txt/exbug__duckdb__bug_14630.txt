CREATE TABLE test_uuid (id INTEGER PRIMARY KEY, uuid_col VARCHAR(36) NOT NULL);
INSERT INTO test_uuid (id, uuid_col) VALUES (1, UUID_TO_STRING(UUID())), (2, UUID_TO_STRING(UUID())), (3, UUID_TO_STRING(UUID()));
SELECT id, CAST(uuid_col AS CHAR) AS uuid_col FROM test_uuid WHERE id = 1;
UPDATE test_uuid SET uuid_col = UUID_TO_STRING(UUID()) WHERE id = 2;
SELECT id, COALESCE(uuid_col, UUID_TO_STRING(UUID())) AS new_uuid FROM test_uuid;
