CREATE TABLE shard_Test (a VARCHAR(255), b FLOAT, c FLOAT, d FLOAT AS (b + c), e FLOAT AS (b + c)) ENGINE = OBTableEngine() PARTITION BY HASH(a) PARTITIONS 4;
CREATE TABLE Test (a VARCHAR(255), b FLOAT, c FLOAT, d FLOAT AS (b + c), e FLOAT AS (b + c)) ENGINE = Distributed('{cluster}', 'default', 'shard_Test', rand());
INSERT INTO shard_Test VALUES ('row1', 1.0, 2.0);
INSERT INTO shard_Test VALUES ('row2', 3.0, NULL);
SELECT SUM(d) AS f, SUM(e) AS g FROM Test;
