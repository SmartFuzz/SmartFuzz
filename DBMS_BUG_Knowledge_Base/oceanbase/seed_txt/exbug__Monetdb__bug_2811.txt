CREATE TABLE geo_values (id INT NOT NULL, strdfgeo GEOMETRY);
CREATE TABLE label_values (id INT NOT NULL, value VARCHAR(255) NOT NULL);
INSERT INTO geo_values (id, strdfgeo) VALUES (1, ST_GeomFromText('POINT(1 1)'));
INSERT INTO label_values (id, value) VALUES (1, 'Label 1');
SELECT DISTINCT h3.obj, COALESCE(u.value, l.value) FROM triples t0 INNER JOIN triples h1 ON (h1.pred = 21 AND h1.obj = 42 AND h1.subj = t0.subj) INNER JOIN label_values l ON (l.id = h1.obj) INNER JOIN triples h3 ON (h3.pred = 20 AND h3.subj = t0.subj);
