CREATE TABLE t2 (id int, name varchar(50), created_at datetime);
INSERT INTO t2 VALUES (1, 'test', NOW());
PREPARE stmt1 FROM 'SELECT * FROM t2 WHERE created_at >= ? OR id < ?';
SET @param1 = '2023-01-01', @param2 = 2;
EXECUTE stmt1 USING @param1, @param2;
SHOW WARNINGS;
