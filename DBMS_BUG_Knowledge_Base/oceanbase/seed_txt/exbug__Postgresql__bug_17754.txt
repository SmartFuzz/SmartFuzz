DROP TABLE IF EXISTS atable;
DROP TABLE IF EXISTS btable;
CREATE TABLE atable (a int, b int, c varchar(255) DEFAULT 'default', d boolean NOT NULL);
CREATE TABLE btable (c int, d int, e float CHECK (e > 0));
INSERT INTO atable SELECT ROW_NUMBER() OVER () AS a, ROW_NUMBER() OVER () AS b, NULL, TRUE;
