DROP TABLE IF EXISTS demo;
CREATE TABLE demo(val BIGINT, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP());
CREATE INDEX demo_idx ON demo(val);
INSERT INTO demo(val, name) SELECT val, CONCAT('Name ', val) FROM (SELECT ROW_NUMBER() OVER () AS val FROM dual CONNECT BY LEVEL <= 10000000) temp;
ANALYZE TABLE demo;
