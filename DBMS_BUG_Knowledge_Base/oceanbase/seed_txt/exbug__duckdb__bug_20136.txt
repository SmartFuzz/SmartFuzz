CREATE TABLE test_data (id INT, name VARCHAR(255), score FLOAT, group_id INT);
INSERT INTO test_data VALUES (1, 'Alice', NULL, 100);
INSERT INTO test_data VALUES (2, 'Bob', NULL, 100);
SELECT MAX(score) OVER (PARTITION BY group_id ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS first_value FROM test_data;
SELECT MIN(score) OVER (PARTITION BY group_id ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_value FROM test_data;
