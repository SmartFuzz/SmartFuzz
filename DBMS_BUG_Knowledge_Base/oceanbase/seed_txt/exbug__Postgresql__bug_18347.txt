CREATE ROLE user_c;
CREATE SCHEMA c AUTHORIZATION user_c;
CREATE TABLE c.test(data INT, description TEXT, created_at TIMESTAMP DEFAULT SYSTIMESTAMP());
INSERT INTO c.test(data, description) VALUES (1, 'First entry'), (NULL, 'Second entry'), (2, 'Third entry');
CREATE INDEX idx_description ON c.test (description);
