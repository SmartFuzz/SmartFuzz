CREATE TABLE t2 (id INTEGER PRIMARY KEY, status VARCHAR(10) NOT NULL CHECK (status IN ('a', 'b', 'c')));
INSERT INTO t2 VALUES (1, 'a');
INSERT INTO t2 VALUES (2, 'b');
SELECT CASE WHEN COUNT(DISTINCT status) = 1 THEN MAX(status) END AS result FROM t2;
SELECT COUNT(*) FROM t2;
