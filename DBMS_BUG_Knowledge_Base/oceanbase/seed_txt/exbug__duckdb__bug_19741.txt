CREATE TABLE three (x INTEGER, y VARCHAR(255), z FLOAT, w BOOLEAN);
INSERT INTO three VALUES (1, 'test', 1.1, TRUE), (2, 'example', 2.2, FALSE);
INSERT INTO three VALUES (3, 'sample', 3.3, NULL);
CREATE TABLE three_parquet AS SELECT * FROM three;
CREATE VIEW three_view AS SELECT * FROM three_parquet;
SELECT * FROM three_view;
