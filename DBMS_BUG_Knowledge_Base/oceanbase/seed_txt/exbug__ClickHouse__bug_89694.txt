DROP TABLE IF EXISTS client;
CREATE TABLE client (code VARCHAR(255), name VARCHAR(255), email VARCHAR(255), contact_number VARCHAR(255), last_reviewed_at TIMESTAMP(3)) PARTITION BY HASH(code) PARTITIONS 4;
INSERT INTO client (code, name, email, contact_number, last_reviewed_at) VALUES ('A002', 'Jane Smith', 'JANE@EXAMPLE.COM', '0123456789', CURRENT_TIMESTAMP());
INSERT INTO client (name, code, email, contact_number, last_reviewed_at) VALUES (CONCAT('A', LPAD('', 10000, 'B')), 'A003', 'TEST@EXAMPLE.COM', '0198765432', CURRENT_TIMESTAMP());
SELECT * FROM client WHERE LENGTH(name) > 50;
