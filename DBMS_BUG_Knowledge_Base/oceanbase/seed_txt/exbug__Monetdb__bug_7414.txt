CREATE TABLE v1 ( id INT PRIMARY KEY, value FLOAT, description VARCHAR(255) );
INSERT INTO v1 (id, value, description) VALUES (1, 10.5, 'First entry');
CREATE TABLE v2 ( ref_id INT, amount DECIMAL(10, 2), FOREIGN KEY (ref_id) REFERENCES v1(id) );
INSERT INTO v2 (ref_id, amount) VALUES (1, NULLIF(20.0, 20.0));
DELETE FROM v1 WHERE value IS NULL OR value = (SELECT CORR(value, value) OVER (ORDER BY id ROWS BETWEEN 1 PRECEDING AND CURRENT ROW) FROM v1);
