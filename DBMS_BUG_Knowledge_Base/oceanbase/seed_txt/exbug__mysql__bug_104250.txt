CREATE DATABASE test_db;
USE test_db;
CREATE TABLE users(id INT PRIMARY KEY, name VARCHAR(100) NOT NULL, age INT CHECK (age >= 0));
INSERT INTO users VALUES(1, 'Alice', 30);
INSERT INTO users VALUES(2, NULL, 25);
SELECT * FROM users;
