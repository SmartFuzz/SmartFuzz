CREATE TABLE table2 (id INT, name VARCHAR(50), value DECIMAL(10,2), PRIMARY KEY(id));
INSERT INTO table2 VALUES (1, 'test', 10.50);
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
BEGIN;
UPDATE table2 SET value = 20.75 WHERE id = 1;
SELECT * FROM table2 WHERE id = 1;
