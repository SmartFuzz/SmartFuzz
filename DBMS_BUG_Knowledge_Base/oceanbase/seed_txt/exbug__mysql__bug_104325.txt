CREATE TABLE Bug(id BIGINT PRIMARY KEY, data JSON NOT NULL, INDEX idx(data));
INSERT INTO Bug(data) VALUES ('["123", "456"]');
CREATE TABLE AnotherBug(id INT PRIMARY KEY, bug_id BIGINT, FOREIGN KEY (bug_id) REFERENCES Bug(id));
INSERT INTO AnotherBug(bug_id) VALUES (1);
SELECT * FROM Bug WHERE JSON_CONTAINS(data, '123') AND EXISTS (SELECT 1 FROM AnotherBug WHERE bug_id = Bug.id);
