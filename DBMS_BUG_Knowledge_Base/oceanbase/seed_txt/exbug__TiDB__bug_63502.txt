CREATE TABLE orders (order_id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY, customer_id BIGINT, order_date DATETIME, amount DECIMAL(10, 2), INDEX idx_customer_id (customer_id));
CREATE TABLE products (product_id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY, name VARCHAR(50), price DECIMAL(10, 2), stock INT);
INSERT INTO customers (name, custinfo) VALUES ('Alice', JSON_OBJECT('zipcode', '12345'));
INSERT INTO orders (customer_id, order_date, amount) VALUES (1, NOW(), 100.00);
SELECT * FROM orders WHERE customer_id = (SELECT customer_id FROM customers WHERE name = 'Alice') AND customer_id IS NOT NULL;
