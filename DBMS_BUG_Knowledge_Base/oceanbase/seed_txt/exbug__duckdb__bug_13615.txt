CREATE TABLE tab2 (x int, y float, z varchar(10));
INSERT INTO tab2 VALUES (3, 2.5, 'test1');
INSERT INTO tab2 VALUES (7, NULL, 'test2');
CREATE OR REPLACE FUNCTION s3(x int) RETURNS float AS 'SELECT SUM(y) OVER() FROM (SELECT x AS y)';
SELECT s3(x), SUM(x) OVER() FROM tab2;
