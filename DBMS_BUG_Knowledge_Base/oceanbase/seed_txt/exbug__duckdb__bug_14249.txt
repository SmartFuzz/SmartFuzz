CREATE TABLE test_corr (a DOUBLE, b DOUBLE);
INSERT INTO test_corr VALUES (1.0, 1.0), (1.0, 1.0);
SELECT CASE WHEN COUNT(DISTINCT a) = 1 THEN 'NaN' ELSE corr(a, a) END AS corr_result FROM test_corr;
CREATE TABLE constant_test (c INT DEFAULT 5);
SELECT CASE WHEN COUNT(DISTINCT c) = 1 THEN 'NaN' ELSE corr(c, c) END AS corr_result FROM constant_test;
