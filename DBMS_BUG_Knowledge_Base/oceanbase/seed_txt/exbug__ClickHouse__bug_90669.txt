CREATE TABLE test_table (id INT UNSIGNED, name VARCHAR(255), value FLOAT, created_at TIMESTAMP) ENGINE = OBTableEngine() ORDER BY id;
INSERT INTO test_table VALUES (1, 'test1', 1.23, NOW()), (2, 'test2', NULL, NOW()), (3, NULL, 3.45, NOW());
CREATE TABLE string_or_string (string1 VARCHAR(255), string2 VARCHAR(255)) ENGINE = OBTableEngine() ORDER BY id;
INSERT INTO string_or_string VALUES ('str1', ''), ('', 'str2'), (NULL, 'str3');
SELECT *, CASE WHEN string1 IS NOT NULL THEN 'string1' ELSE 'string2' END AS string_oneof_present FROM string_or_string;
