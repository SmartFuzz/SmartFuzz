CREATE TABLE users(id INT PRIMARY KEY, name VARCHAR(50), age INT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users VALUES(1, 'Alice', 30, NULL);
CREATE TABLE orders(order_id INT PRIMARY KEY, user_id INT, amount DECIMAL(10, 2), FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO orders VALUES(101, 1, 99.99);
SELECT * FROM users WHERE age > 30 AND id IN (SELECT user_id FROM orders WHERE amount > 50.00);
