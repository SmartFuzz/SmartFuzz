CREATE TABLE test2 (id UInt64, value Nullable(Int32), arr Array(String));
INSERT INTO test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, ['a', 'b', 'c'] FROM dual CONNECT BY LEVEL <= 10;
CREATE TABLE test3 AS test2;
SELECT arr, CARDINALITY(arr) FROM test2 WHERE value IS NOT NULL ORDER BY value FETCH FIRST 3 ROWS ONLY;
SELECT id, value, arr[1] FROM test3 WHERE CARDINALITY(arr) > 0;
