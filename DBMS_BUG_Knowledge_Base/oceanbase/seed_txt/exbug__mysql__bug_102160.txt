CREATE TABLE t2 (x INT, y VARCHAR(10), z FLOAT, PRIMARY KEY (x));
INSERT INTO t2 VALUES (1, 'test', 1.5), (2, 'example', 2.5), (3, 'sample', 3.5);
SELECT * FROM t2 WHERE x = CAST(5.0 AS UNSIGNED) / 2;
CREATE INDEX idx_y ON t2(y);
SELECT * FROM t2 WHERE y = 'example' AND z > 2.0;
ANALYZE TABLE t2;
