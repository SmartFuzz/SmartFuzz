CREATE TABLE test_array_distinct (id INTEGER, values VARCHAR[]);
INSERT INTO test_array_distinct VALUES (1, ARRAY['A', 'A', 'B', NULL, NULL]), (2, ARRAY['B', 'C', 'C', 'A', NULL]);
SELECT DISTINCT value FROM (SELECT UNNEST(values) AS value FROM test_array_distinct WHERE id = 1) AS subquery WHERE value IS NOT NULL;
SELECT DISTINCT value FROM (SELECT UNNEST(ARRAY_CONCAT(values, ARRAY['D', 'D'])) AS value FROM test_array_distinct WHERE id = 2) AS subquery WHERE value IS NOT NULL;
SELECT DISTINCT value FROM (SELECT UNNEST(ARRAY[1, 2, NULL, 2, NULL]) AS value) AS subquery WHERE value IS NOT NULL;
