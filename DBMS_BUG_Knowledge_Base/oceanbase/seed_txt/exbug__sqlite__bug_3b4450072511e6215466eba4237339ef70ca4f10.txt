CREATE TABLE t1(x INT, y VARCHAR(255));
INSERT INTO t1(x, y) VALUES(1, 'test');
SELECT a FROM (SELECT x AS a, y FROM t1);
CREATE TABLE t2 AS SELECT a FROM (SELECT x AS a, y FROM t1);
SELECT COLUMN_NAME FROM USER_TAB_COLUMNS WHERE TABLE_NAME = 'T2';
