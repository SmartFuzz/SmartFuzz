CREATE TABLE test_table1 (id BIGINT, value VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP) ENGINE=OBSERVER;
CREATE TABLE test_table2 (total FLOAT, id_bit BLOB, description VARCHAR(255)) ENGINE=OBSERVER;
INSERT INTO test_table1 SELECT number AS id, CAST(number AS CHAR) AS value, CURRENT_TIMESTAMP FROM dual CONNECT BY LEVEL <= 100000;
INSERT INTO test_table2 SELECT 1.5 AS total, BITMAP_AGG(rand() % 1000) AS id_bit, NULL AS description FROM dual CONNECT BY LEVEL <= 100;
SELECT COUNT(DISTINCT id_bit) FROM test_table2 WHERE total > 1.0;
