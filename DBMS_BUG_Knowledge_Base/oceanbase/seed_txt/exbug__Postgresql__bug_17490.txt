DROP DATABASE IF EXISTS restart_seq_test;
CREATE DATABASE restart_seq_test OWNER dumbowner;
CREATE TABLE test_table (id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, val text NOT NULL, created_at timestamp DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (val) SELECT 'test' || g.v FROM (SELECT LEVEL AS v FROM dual CONNECT BY LEVEL <= 10) g;
GRANT SELECT, UPDATE ON test_table TO dumbuser;
