CREATE TABLE bar (id INT PRIMARY KEY, start_date DATE, end_date DATE, duration INT CHECK (duration >= 0));
INSERT INTO bar VALUES (1, '2023-01-01', '2023-01-10', TIMESTAMPDIFF(DAY, start_date, end_date));
INSERT INTO bar VALUES (2, '2023-02-01', '2023-02-05', TIMESTAMPDIFF(DAY, start_date, end_date));
SELECT id, duration, (end_date - start_date) AS calculated_duration FROM bar WHERE duration <> TIMESTAMPDIFF(DAY, start_date, end_date);
SELECT COUNT(*) FROM bar WHERE start_date IS NULL OR end_date IS NULL;
