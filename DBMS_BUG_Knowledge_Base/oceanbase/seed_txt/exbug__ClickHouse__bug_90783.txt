CREATE TABLE fact_shards.vehicle_vins_view_test (vehicle_id UUID, vin VARCHAR(255), zip_code INT UNSIGNED, window_sticker VARCHAR(255), auction_vehicle TINYINT, vehicle_updated_at TIMESTAMP, vehicle_created_at TIMESTAMP) ENGINE = OBReplicatedMergeTree('/oceanbase/tables/{shard}/fact_shards/vehicle_vins_view_test', '{replica}') PARTITION BY MOD(CAST(cityHash64(vin) AS UNSIGNED), 1000) ORDER BY (vin, vehicle_id);
INSERT INTO fact_shards.vehicle_vins_view_test (vehicle_id, vin, zip_code, window_sticker, auction_vehicle, vehicle_updated_at, vehicle_created_at) VALUES (UUID_TO_BIN(UUID()), 'KL7CJPSB2JB613659', 12345, 'Sticker1', 1, NOW(), NOW());
INSERT INTO fact_shards.vehicle_vins_view_test (vehicle_id, vin, zip_code, window_sticker, auction_vehicle, vehicle_updated_at, vehicle_created_at) VALUES (UUID_TO_BIN(UUID()), 'INVALID_VIN', 54321, 'Sticker2', 0, NOW(), NOW());
SELECT * FROM fact_shards.vehicle_vins_view_distr WHERE vin = 'KL7CJPSB2JB613659';
SELECT * FROM fact_shards.vehicle_vins_view_distr WHERE vin = 'INVALID_VIN';
