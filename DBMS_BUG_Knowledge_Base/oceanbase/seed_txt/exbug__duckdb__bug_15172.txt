CREATE TABLE test_data (id INTEGER PRIMARY KEY, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, name) VALUES (1, 'Sample Data');
SELECT * FROM test_data WHERE id = 1;
CREATE USER 'user' IDENTIFIED BY 'password';
DROP USER 'user';
