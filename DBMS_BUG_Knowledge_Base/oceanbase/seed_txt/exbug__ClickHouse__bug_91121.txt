CREATE TABLE lag_test_1 (a INT, b FLOAT) PARTITION BY HASH(a) PARTITIONS 4;
INSERT INTO lag_test_1 VALUES (1, 100.5), (1, 101.5), (1, 102.5);
SELECT l.*, LAG(b, 1, NULL) OVER (PARTITION BY a ORDER BY b) AS lag_b FROM lag_test_1 l;
SELECT l.*, LEAD(b, 1, NULL) OVER (PARTITION BY a ORDER BY b) AS lead_b FROM lag_test_1 l;
SELECT * FROM lag_test_1;
