CREATE DATABASE d1 ENGINE = DataLakeCatalog('thrift://hive:9083', 'minio_user', 'minio_password') SETTINGS catalog_type = 'hive', warehouse = 'htest', storage_endpoint = 'http://minio1:9001/warehouse-hms';
CREATE TABLE d1.t1 (`id` UInt32, `name` String, `tags` Array(Nullable(String)), `created_at` DateTime DEFAULT CURRENT_TIMESTAMP) ENGINE = OBStore() PARTITION BY toYYYYMM(created_at);
INSERT INTO d1.t1 (id, name, tags) VALUES (1, 'test', ['tag1', NULL, 'tag3']);
SELECT id, name, tag FROM d1.t1, UNNEST(tags) AS tag WHERE created_at >= '2023-01-01 00:00:00';
CREATE TABLE d1.t2 (`ref_id` UInt32, `description` String) ENGINE = MergeTree() ORDER BY ref_id;
