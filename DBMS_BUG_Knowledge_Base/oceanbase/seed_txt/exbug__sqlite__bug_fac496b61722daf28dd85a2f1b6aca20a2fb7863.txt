CREATE TABLE t1(x INT, y VARCHAR(255));
INSERT INTO t1 VALUES(1, 'a'), (2, NULL);
CREATE TABLE t2(y INT, z FLOAT);
INSERT INTO t2 VALUES(2, 1.0), (NULL, 2.0);
WITH t1_cte AS (SELECT * FROM t1), t2_cte AS (SELECT * FROM t2) SELECT * FROM t1_cte LEFT JOIN t2_cte ON t1_cte.x = t2_cte.y JOIN (SELECT 1 AS start, 3 AS stop) AS gs ON gs.start = t2_cte.z AND gs.stop = 2;
