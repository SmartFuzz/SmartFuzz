CREATE TEMPORARY TABLE t1 (a VARCHAR(255), b INT, c JSON);
CREATE INDEX idx1 ON t1 USING GIST (c);
INSERT INTO t1 (a, b, c) SELECT CONCAT('text', g), g, JSON_OBJECT('key', g) FROM (SELECT LEVEL g FROM dual CONNECT BY LEVEL <= 1000) g;
INSERT INTO t1 (a, b, c) SELECT NULL, NULL, JSON_ARRAY(NULL) FROM (SELECT LEVEL FROM dual CONNECT BY LEVEL <= 50);
INSERT INTO t1 (a, b, c) SELECT CONCAT('boundary', g), CAST(g AS INT), JSON_OBJECT('boundary', g) FROM (SELECT LEVEL g FROM dual CONNECT BY LEVEL <= 600) g;
