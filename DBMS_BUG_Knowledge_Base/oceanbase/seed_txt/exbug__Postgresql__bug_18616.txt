CREATE TABLE t1(id INT PRIMARY KEY AUTO_INCREMENT, value TEXT NOT NULL);
INSERT INTO t1(value) SELECT 'test' || (ROWNUM + 1) FROM dual CONNECT BY LEVEL <= 100;
CREATE TABLE t2(a INT, b VARCHAR(255), c TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2(a, b) SELECT i, NULL FROM (SELECT ROWNUM AS i FROM dual CONNECT BY LEVEL <= 1000);
CREATE INDEX idx_t2_a ON t2 USING HASH (a);
