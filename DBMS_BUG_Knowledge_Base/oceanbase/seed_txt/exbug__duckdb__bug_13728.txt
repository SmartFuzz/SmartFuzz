CREATE TABLE t2 (a INTEGER, b VARCHAR(255), c DOUBLE, d BOOLEAN);
INSERT INTO t2 VALUES (1, 'test', 3.14, TRUE), (2, 'example', NULL, FALSE);
CREATE OR REPLACE PROCEDURE unpivot_test(vars VARCHAR(255)) AS BEGIN EXECUTE IMMEDIATE 'SELECT * FROM t2 UNPIVOT (value FOR column IN (' || vars || '))';
END;
CALL unpivot_test('a, b');
CREATE OR REPLACE PROCEDURE unpivot_with_null() AS BEGIN EXECUTE IMMEDIATE 'SELECT * FROM t2 UNPIVOT (value FOR column IN (c, d))';
