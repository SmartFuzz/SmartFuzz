CREATE TABLE TEST_DATA_1 (ID INTEGER, DATE_FIELD VARCHAR);
INSERT INTO TEST_DATA_1 VALUES (1, '2020-01-01XXXX');
CREATE TABLE TEST_DATA_2 AS SELECT '2020-01-01XXXX' AS DATE3;
SELECT CASE WHEN DATE3 IS NOT NULL AND LENGTH(DATE3) = 10 THEN TO_DATE(DATE3, 'YYYY-MM-DD') ELSE NULL END AS CASTED_DATE, DATE3 FROM TEST_DATA_2;
SELECT ID, CASE WHEN DATE_FIELD IS NOT NULL AND LENGTH(DATE_FIELD) = 10 THEN TO_DATE(DATE_FIELD, 'YYYY-MM-DD') ELSE NULL END FROM TEST_DATA_1 WHERE ID = 1;
