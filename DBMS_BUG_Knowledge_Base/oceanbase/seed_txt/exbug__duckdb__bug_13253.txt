CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(255), value FLOAT);
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.5);
INSERT INTO test_table (id, name, value) VALUES (2, 'test2', NULL);
SELECT CASE WHEN COUNT(name) > 0 THEN MIN(name) ELSE NULL END AS least_name FROM test_table;
SELECT CASE WHEN COUNT(value) > 0 THEN MIN(value) ELSE NULL END AS least_value FROM test_table WHERE value IS NOT NULL;
