CREATE TABLE employee (emp_id INTEGER PRIMARY KEY, emp_name VARCHAR(255), emp_age TINYINT, emp_salary DECIMAL(10, 2));
CREATE UNIQUE INDEX emp_idx ON employee (emp_name, emp_age);
INSERT INTO employee (emp_id, emp_name, emp_age, emp_salary) VALUES (1, 'Alice', 30, 70000.00);
SELECT * FROM user_indexes WHERE table_name = 'employee';
CREATE TABLE department (dept_id INTEGER PRIMARY KEY, dept_name VARCHAR(255), budget DECIMAL(15, 2));
