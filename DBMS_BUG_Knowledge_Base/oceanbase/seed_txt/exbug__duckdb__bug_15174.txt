CREATE TABLE test_data (id INTEGER PRIMARY KEY, value DOUBLE, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data SELECT i, RANDOM(), CONCAT('Name ', i), SYSTIMESTAMP FROM (SELECT LEVEL AS i FROM dual CONNECT BY LEVEL <= 200000000);
CREATE TABLE filtered_data AS SELECT * FROM test_data WHERE value IS NOT NULL;
INSERT INTO filtered_data SELECT * FROM test_data WHERE value IS NOT NULL ORDER BY value DESC;
SELECT COUNT(*) FROM filtered_data WHERE value BETWEEN 0.0 AND 1.0;
