DROP TABLE IF EXISTS test_logs;
CREATE TABLE test_logs (id INT, message VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP) ENGINE = OBTable ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_logs (id, message) VALUES (1, 'zymosimeter'), (2, 'example'), (3, NULL);
SELECT * FROM test_logs WHERE message LIKE '%zymosimeter%';
SELECT COUNT(*) FROM test_logs WHERE created_at > NOW() - INTERVAL '1' DAY;
