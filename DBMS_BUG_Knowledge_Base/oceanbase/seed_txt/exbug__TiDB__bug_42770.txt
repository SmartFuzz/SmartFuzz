CREATE TABLE test_rpad (id INT PRIMARY KEY, text_col VARCHAR(255), num_col INT);
INSERT INTO test_rpad (id, text_col, num_col) VALUES (1, 'test', 10);
SELECT RPAD(text_col, 100, 'x') FROM test_rpad WHERE num_col = 10;
SELECT RPAD(text_col, CASE WHEN num_col = 0 THEN NULL ELSE num_col END, 'y') FROM test_rpad;
SELECT RPAD(text_col, LEAST(CAST(4611686018427387904 AS UNSIGNED), 255), 'z') FROM test_rpad;
