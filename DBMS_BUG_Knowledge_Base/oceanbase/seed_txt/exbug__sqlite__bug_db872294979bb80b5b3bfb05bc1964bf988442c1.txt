CREATE TABLE t1(a INT NOT NULL, b VARCHAR(255) DEFAULT 'default');
CREATE TABLE t2(b INT PRIMARY KEY);
INSERT INTO t1 VALUES(1, 'one'), (1, 'one'), (2, 'two');
INSERT INTO t2 VALUES(1), (2);
SELECT * FROM t2 WHERE b IN (SELECT a FROM (SELECT DISTINCT a FROM t1 WHERE a > 0) LIMIT 2);
