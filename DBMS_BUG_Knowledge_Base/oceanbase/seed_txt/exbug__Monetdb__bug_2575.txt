CREATE TABLE sys.fulltext (documentid BIGINT NOT NULL, seq SMALLINT NOT NULL, trigram CHAR(3) NOT NULL);
INSERT INTO sys.fulltext (documentid, seq, trigram) VALUES (1, 1, 'abc');
INSERT INTO sys.fulltext (documentid, seq, trigram) VALUES (2, 2, NULL);
SELECT * FROM sys.fulltext WHERE trigram IS NOT NULL;
LOAD DATA INFILE '/path/to/data.csv' INTO TABLE sys.fulltext FIELDS TERMINATED BY ',';
