CREATE TABLE test_order_by (id INTEGER, value DOUBLE, created_at TIMESTAMP, category VARCHAR, is_active BOOLEAN);
INSERT INTO test_order_by VALUES (1, 10.5, '2023-01-01 10:00:00', 'A', TRUE), (2, 20.0, '2023-01-01 09:00:00', 'B', FALSE), (3, 15.0, '2023-01-01 11:00:00', 'A', TRUE);
CREATE TABLE partitioned_data AS SELECT category, COUNT(*) AS count FROM test_order_by GROUP BY category;
SELECT * FROM partitioned_data ORDER BY count DESC INTO OUTFILE 'partitioned_output.csv' FIELDS TERMINATED BY ',' ENCLOSED BY '"' LINES TERMINATED BY '\n';
CREATE TABLE large_data AS SELECT ROW_NUMBER() OVER () AS num, DBMS_RANDOM.VALUE() AS rand_value FROM dual CONNECT BY LEVEL <= 1000000;
