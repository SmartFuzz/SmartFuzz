CREATE TABLE t (label VARCHAR(10), step INTEGER, value REAL NOT NULL DEFAULT 0.0);
INSERT INTO t VALUES('a', 1, 1.1);
INSERT INTO t VALUES('a', 1, 1.1);
INSERT INTO t VALUES('b', 1, 2.2);
WITH cte AS (SELECT label, step FROM t GROUP BY label, step UNION ALL SELECT label, step + 1 FROM cte WHERE step < 3) SELECT * FROM cte;
