CREATE TABLE test_division (a INT, b INT, c FLOAT, d VARCHAR(10));
INSERT INTO test_division VALUES (1, 2, 3.0, 'test1');
INSERT INTO test_division VALUES (3, NULL, 4.5, 'test2');
SELECT a / NULLIF(b, 0) FROM test_division WHERE c > 2.0;
SELECT d, a / NULLIF(c, 0) FROM test_division WHERE b IS NULL;
