CREATE TABLE test_table (id INT UNSIGNED, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, value FLOAT) ENGINE = OBTable;
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.5), (2, 'test2', NULL), (3, 'test3', 20.0);
CREATE TABLE another_table (ref_id INT UNSIGNED, description VARCHAR(255), amount INT) ENGINE = OBTable;
INSERT INTO another_table (ref_id, description, amount) SELECT id, CONCAT(name, ' description'), CAST(value AS SIGNED) FROM test_table WHERE value IS NOT NULL;
SELECT * FROM infinite_counter WHERE counter < 100;
