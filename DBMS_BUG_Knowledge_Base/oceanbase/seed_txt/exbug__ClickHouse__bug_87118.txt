CREATE TABLE default.t_proj_test1 (id UInt32, name String, value Float64, created_at DateTime DEFAULT now()) ENGINE = MERGE TREE ORDER BY id;
INSERT INTO default.t_proj_test1 SELECT number, concat('Name', toString(number)), number * 1.5, now() FROM dual CONNECT BY LEVEL <= 10000;
CREATE TABLE default.t_proj_test2 (user_id UInt32, score Nullable(Int32), timestamp Date DEFAULT CURRENT_DATE) ENGINE = MERGE TREE ORDER BY user_id;
INSERT INTO default.t_proj_test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, CURRENT_DATE FROM dual CONNECT BY LEVEL <= 5000;
SELECT user_id, SUM(score) AS total_score FROM default.t_proj_test2 GROUP BY user_id ORDER BY total_score DESC;
