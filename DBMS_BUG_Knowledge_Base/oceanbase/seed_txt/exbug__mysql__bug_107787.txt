CREATE TABLE `a3` (`nId` INT AUTO_INCREMENT PRIMARY KEY, `wstrText` VARCHAR(200) CHARACTER SET utf8 COLLATE ucs2_unicode_ci);
INSERT INTO `a3` (wstrText) VALUES ('Patchwork');
CREATE TABLE `a4` (`nId` INT AUTO_INCREMENT PRIMARY KEY, `wstrText` VARCHAR(200) CHARACTER SET utf8 COLLATE utf8_general_ci);
INSERT INTO `a4` (wstrText) VALUES ('Patch');
SELECT `als`.`wstrText` FROM (SELECT DISTINCT `a2`.`wstrText` AS wstrText FROM `a2` WHERE `a2`.`wstrText` LIKE 'Patc%' ESCAPE '\' UNION ALL SELECT DISTINCT `a3`.`wstrText` AS wstrText FROM `a3` WHERE (`a3`.`wstrText` LIKE 'Patc%' ESCAPE '\')) als WHERE `als`.`wstrText` LIKE 'P%' COLLATE utf8_general_ci ESCAPE '\';
