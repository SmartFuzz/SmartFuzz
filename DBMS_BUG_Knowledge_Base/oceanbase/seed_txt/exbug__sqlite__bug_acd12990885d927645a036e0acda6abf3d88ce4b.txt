CREATE TABLE users(id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT CHECK(age >= 0));
INSERT INTO users(name, age) VALUES('Alice', 30);
CREATE TABLE temp_users AS SELECT * FROM users WHERE age > 25;
CREATE TABLE orders(order_id INT PRIMARY KEY, user_id INT, amount FLOAT NOT NULL, FOREIGN KEY(user_id) REFERENCES users(id));
INSERT INTO orders(user_id, amount) VALUES((SELECT id FROM users WHERE name = 'Alice'), 99.99);
