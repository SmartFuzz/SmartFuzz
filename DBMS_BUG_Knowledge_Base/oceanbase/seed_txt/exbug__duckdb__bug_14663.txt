CREATE TABLE test_table (id INTEGER, values_list ARRAY<INTEGER>);
INSERT INTO test_table VALUES (1, [1, 2]), (2, []), (3, NULL);
SELECT id, ARRAY_CONCAT(values_list, [5, 6]) AS list_concat, CASE WHEN values_list IS NULL THEN NULL ELSE ARRAY_CONCAT(values_list, [5, 6]) END AS concat_op FROM test_table;
SELECT NULL AS null_value, ARRAY_CONCAT(NULL, [7, 8]) AS list_concat_null, CASE WHEN NULL IS NULL THEN NULL ELSE ARRAY_CONCAT(NULL, [7, 8]) END AS concat_null;
SELECT ARRAY_CONCAT([10, 11], NULL) AS list_concat_with_null;
