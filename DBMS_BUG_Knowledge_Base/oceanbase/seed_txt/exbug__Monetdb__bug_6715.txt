CREATE TABLE ProductTarget (ProductID INT AUTO_INCREMENT PRIMARY KEY, Name VARCHAR(100) NOT NULL, ProductNumber BIGINT, Color VARCHAR(30) NOT NULL);
CREATE TABLE ProductSource (ProductID INT AUTO_INCREMENT PRIMARY KEY, Name VARCHAR(100) NOT NULL, ProductNumber BIGINT, Color VARCHAR(30) NOT NULL);
INSERT INTO ProductSource (Name, ProductNumber, Color) VALUES ('Widget', 1001, 'Red');
INSERT INTO ProductTarget (Name, ProductNumber, Color) VALUES ('Gadget', NULL, 'Blue');
UPDATE ProductTarget T SET T.Name = (SELECT S.Name FROM ProductSource S WHERE T.ProductID = S.ProductID), T.Color = COALESCE((SELECT S.Color FROM ProductSource S WHERE T.ProductID = S.ProductID), 'Unknown') WHERE EXISTS (SELECT 1 FROM ProductSource S WHERE T.ProductID = S.ProductID);
