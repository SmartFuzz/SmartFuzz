CREATE TABLE nextprot.isoform_bases (id VARCHAR(255) NOT NULL, entry VARCHAR(255) NOT NULL, PRIMARY KEY(id));
CREATE TABLE nextprot.isoform_cellular_components (isoform VARCHAR(255) NOT NULL, annotation VARCHAR(255), PRIMARY KEY(isoform, annotation));
INSERT INTO nextprot.isoform_bases (id, entry) VALUES ('isoform1', 'entry1'), ('isoform2', 'entry2');
INSERT INTO nextprot.isoform_cellular_components (isoform, annotation) VALUES ('isoform1', 'annotation1'), ('isoform2', NULL);
SELECT b.*, c.* FROM nextprot.isoform_bases b JOIN nextprot.isoform_cellular_components c ON b.id = c.isoform WHERE c.annotation IS NOT NULL;
