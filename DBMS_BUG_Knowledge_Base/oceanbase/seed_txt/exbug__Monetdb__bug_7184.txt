CREATE TABLE IF NOT EXISTS beta (id bigint AUTO_INCREMENT PRIMARY KEY, value varchar(255) NOT NULL, created_at timestamp DEFAULT CURRENT_TIMESTAMP);
INSERT INTO beta (value) VALUES ('test1'), ('test2'), (NULL), ('test3');
CREATE TABLE IF NOT EXISTS gamma (id bigint PRIMARY KEY, ref_id bigint REFERENCES beta(id), amount decimal(10,2) CHECK (amount >= 0));
INSERT INTO gamma (id, ref_id, amount) SELECT seq.id, beta.id, ROUND(RAND() * 100, 2) FROM (SELECT ROW_NUMBER() OVER () AS id FROM dual CONNECT BY LEVEL <= 5) seq, beta;
SELECT * FROM gamma WHERE amount > (SELECT AVG(amount) FROM gamma);
