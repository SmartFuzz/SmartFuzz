CREATE TABLE courses (course_id INTEGER PRIMARY KEY, course_name VARCHAR(255), credits INTEGER CHECK (credits > 0));
CREATE TABLE enrollments (enrollment_id INTEGER PRIMARY KEY, student_id INTEGER REFERENCES students(id), course_id INTEGER REFERENCES courses(course_id), grade INTEGER);
INSERT INTO courses VALUES (1, 'Mathematics', 3);
INSERT INTO enrollments VALUES (1, 1, 1, NULL);
SELECT course_name, (SELECT COUNT(*) FROM enrollments WHERE course_id = c.course_id) AS enrollment_count FROM courses AS c WHERE credits >= 3;
