CREATE DATABASE IF NOT EXISTS test_db;
CREATE TABLE IF NOT EXISTS test_db.test_table (id UInt32, name String, created_at DateTime DEFAULT now(), value Float64, timestamp UInt32) ENGINE = MergeTree() ORDER BY id;
INSERT INTO test_db.test_table (id, name, value, timestamp) VALUES (1, 'test1', 10.5, 1), (2, 'test2', 20.0, 2), (3, 'test3', 30.0, 3);
INSERT INTO test_db.test_table (id, name, value, timestamp) SELECT number, concat('test', number), number * 1.5, number FROM dual CONNECT BY LEVEL <= 5;
SELECT * FROM test_db.test_table WHERE timestamp IS NULL OR timestamp > 2;
