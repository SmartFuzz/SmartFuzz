CREATE TABLE test1 (id INTEGER PRIMARY KEY, name VARCHAR(255), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test1 (name, value) VALUES ('Sample A', 10.5);
CREATE TABLE test2 (id INTEGER, description VARCHAR(255), FOREIGN KEY (id) REFERENCES test1(id));
INSERT INTO test2 (id, description) VALUES (1, 'Description for Sample A');
SELECT t1.name, t2.description FROM test1 t1 JOIN test2 t2 ON t1.id = t2.id WHERE t1.value > 5.0;
