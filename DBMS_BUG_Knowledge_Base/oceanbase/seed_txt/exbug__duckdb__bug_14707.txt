CREATE TABLE test_table (foo INT, year INT, month INT, day INT);
INSERT INTO test_table VALUES (1, 2024, 11, 5);
INSERT INTO test_table SELECT foo, year, month, day FROM test_table;
SELECT * FROM test_table INTO OUTFILE './test/test.parquet' PARTITION BY (year, month, day);
INSERT INTO test_table SELECT foo, year, month, day FROM test_table WHERE year IS NOT NULL;
SELECT * FROM test_table INTO OUTFILE './test_csv/test.csv' PARTITION BY (year, month, day);
