CREATE TABLE test_round_cast (value DECIMAL(18, 1));
INSERT INTO test_round_cast VALUES (-100.3);
SELECT ROUND(CAST(value AS FLOAT), 1) FROM test_round_cast;
CREATE TABLE test_null_handling (id INTEGER, value DECIMAL(18, 1));
SELECT ROUND(CAST(value AS FLOAT), 1) FROM test_null_handling WHERE id IS NULL;
