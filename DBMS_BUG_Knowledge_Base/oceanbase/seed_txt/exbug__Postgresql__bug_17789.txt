CREATE USER IF NOT EXISTS postgres_fdw_user IDENTIFIED BY 'password';
CREATE TABLE t1 (a int NOT NULL, b text DEFAULT 'default', c timestamp DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE ft1 (b int) INHERITS (t1);
INSERT INTO t1 (a) SELECT i FROM (SELECT ROW_NUMBER() OVER () AS i FROM dual CONNECT BY LEVEL <= 66) temp;
SELECT * FROM t1;
