CREATE TABLE Bar (id int primary key, description varchar(255), created_at timestamp);
INSERT INTO Bar VALUES (1, 'First entry', SYSTIMESTAMP);
CREATE TABLE Baz (id int primary key, value float, bar_id int, CONSTRAINT fk_bar FOREIGN KEY (bar_id) REFERENCES Bar(id));
INSERT INTO Baz VALUES (1, 3.14, 1);
SELECT b.id, b.description, COUNT(z.id) FROM Bar b LEFT JOIN Baz z ON b.id = z.bar_id GROUP BY b.id, b.description;
