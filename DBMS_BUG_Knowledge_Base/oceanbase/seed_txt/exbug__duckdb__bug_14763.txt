CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(255), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'Test A', 10.5), (2, 'Test B', NULL), (3, 'Test C', 20.0);
SELECT COUNT(*) FROM (SELECT id, name, value FROM test_table WHERE value IS NOT NULL) AS cte;
CREATE OR REPLACE FUNCTION get_value(id INTEGER) RETURNS FLOAT AS (BEGIN RETURN (SELECT value FROM test_table WHERE id = id);
END);
SELECT get_value(2) AS value_result;
