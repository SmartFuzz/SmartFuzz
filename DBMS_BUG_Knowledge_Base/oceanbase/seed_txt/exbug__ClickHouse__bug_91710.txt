DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INT UNSIGNED, name VARCHAR(255), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) SELECT number, SUBSTRING(MD5(RAND()), 1, 5), RAND() FROM dual CONNECT BY LEVEL <= 100;
SELECT * FROM test_table ORDER BY name, LENGTH(name);
DROP TABLE test_table;
