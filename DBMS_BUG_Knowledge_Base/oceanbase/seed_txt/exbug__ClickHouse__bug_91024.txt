CREATE TABLE lc_00906 (id INT UNSIGNED, value VARCHAR(255)) ENGINE = OBTable ENGINE=MERGE TREE ORDER BY id;
INSERT INTO lc_00906 SELECT number, '0123456789' FROM dual CONNECT BY LEVEL <= 100000000;
CREATE TABLE lc_test (id INT UNSIGNED, data INT NULL) ENGINE = OBTable ENGINE=MERGE TREE ORDER BY id;
INSERT INTO lc_test SELECT number, CASE WHEN MOD(number, 2) = 0 THEN number ELSE NULL END FROM dual CONNECT BY LEVEL <= 100;
SELECT COUNT(*) FROM lc_test WHERE data IS NULL;
