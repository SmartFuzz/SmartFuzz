CREATE TABLE users (id INT, name VARCHAR(255), age TINYINT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP) PARTITION BY HASH(id) ENABLE ROW MOVEMENT;
INSERT INTO users VALUES (1, 'Alice', 30, NULL);
CREATE TABLE orders (order_id INT, user_id INT, amount FLOAT, order_date DATE) PARTITION BY HASH(order_id) ENABLE ROW MOVEMENT;
INSERT INTO orders VALUES (101, 1, 99.99, '2023-10-01');
SELECT u.name, o.amount FROM users u JOIN orders o ON u.id = o.user_id WHERE u.age > 25;
