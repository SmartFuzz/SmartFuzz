CREATE TABLE repl.bar (id INT UNSIGNED, value FLOAT, created_at TIMESTAMP) PARTITION BY RANGE (YEAR(created_at)) (PARTITION p1 VALUES LESS THAN (2023));
INSERT INTO repl.bar VALUES (1, 3.14, NOW()), (2, 2.71, NOW()), (3, NULL, NOW());
CREATE TABLE repl.bar_dist AS repl.bar ENGINE = Distributed('{cluster}', repl, bar);
SELECT value FROM repl.bar_dist WHERE id = 2 ORDER BY created_at DESC LIMIT 1;
SELECT COUNT(*) FROM (SELECT DISTINCT value FROM repl.bar_dist WHERE value IS NOT NULL) AS subquery;
