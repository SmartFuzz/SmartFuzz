CREATE TABLE tbl1 (id1 VARCHAR(255), id2 VARCHAR(255), v BIGINT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP) ENGINE = OBTable;
INSERT INTO tbl1 VALUES ('x', 'y', 10), ('x', 'z', 20), (NULL, 'y', 30);
CREATE TABLE join_engine1 (id1 VARCHAR(255), id2 VARCHAR(255), v BIGINT, status ENUM('active', 'inactive')) ENGINE = JOIN(id1, id2);
INSERT INTO join_engine1 VALUES ('x', 'y', 10, 'active'), ('x', 'z', 20, 'inactive');
SELECT COUNT(DISTINCT id2) AS count FROM tbl1 WHERE joinGet('join_engine1', 'v', id1, id2) = v AND id1 IS NOT NULL;
