CREATE TABLE hobbies_r (name VARCHAR(255) NOT NULL, person VARCHAR(255) NOT NULL, PRIMARY KEY (name, person));
INSERT INTO hobbies_r (name, person) VALUES ('reading', 'mike');
CREATE TABLE equipment_r (name VARCHAR(255) NOT NULL, hobby VARCHAR(255) NOT NULL, FOREIGN KEY (hobby) REFERENCES hobbies_r(name));
INSERT INTO equipment_r (name, hobby) VALUES ('book', 'reading');
SELECT (SELECT name FROM equipment_r WHERE hobby = 'reading' LIMIT 1) AS equipment_name;
