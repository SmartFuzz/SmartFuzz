CREATE TABLE t(p VARCHAR(50), id INT AUTO_INCREMENT PRIMARY KEY);
INSERT INTO t(p) SELECT CONCAT('(', g, ',', g, ')') FROM (SELECT g FROM (SELECT ROW_NUMBER() OVER () AS g FROM dual CONNECT BY LEVEL <= 1000) temp) temp2;
INSERT INTO t(p) VALUES ('(0,NaN)');
CREATE INDEX spgist_idx ON t(p);
SELECT * FROM t WHERE p IS NULL;
