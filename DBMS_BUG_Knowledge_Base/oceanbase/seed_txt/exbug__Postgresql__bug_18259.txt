CREATE TABLE test_table(id INT PRIMARY KEY AUTO_INCREMENT, name TEXT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table(name) VALUES ('test1'), ('test2'), (NULL), ('test3');
CREATE TABLE filler(a INT, b TEXT);
INSERT INTO filler SELECT g, REPEAT('x', 1000) FROM (SELECT LEVEL g FROM DUAL CONNECT BY LEVEL <= 50000) tmp;
DROP TABLE IF EXISTS filler;
