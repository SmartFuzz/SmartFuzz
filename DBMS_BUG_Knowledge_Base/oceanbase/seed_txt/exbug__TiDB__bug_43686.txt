SET GLOBAL ob_partition_prune_mode='dynamic';
DROP TABLE IF EXISTS tt3;
CREATE TABLE tt3 (id INT NOT NULL, listid INT, name VARCHAR(20) NOT NULL DEFAULT 'default_name') PARTITION BY LIST (listid) (PARTITION p1 VALUES IN (1), PARTITION p2 VALUES IN (2));
INSERT INTO tt3 VALUES (1, 1, 'Alice'), (2, 2, 'Bob'), (3, 3, 'Charlie');
EXPLAIN SELECT /*+ inl_join(tt3)*/ COUNT(*) FROM tt3 LEFT JOIN (SELECT listid FROM tt3 WHERE listid = 5) AS subquery ON tt3.listid = subquery.listid;
