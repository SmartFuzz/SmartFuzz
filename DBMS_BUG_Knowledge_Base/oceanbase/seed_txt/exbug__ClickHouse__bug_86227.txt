CREATE TABLE shard_Test ( `a` VARCHAR(255), `b` FLOAT, `c` FLOAT, `d` FLOAT, `e` FLOAT GENERATED ALWAYS AS (b + c), `f` FLOAT GENERATED ALWAYS AS (c + d), `g` INT NULL ) ENGINE = OBTable ENGINE = MergeTree ORDER BY a;
CREATE TABLE Test ( `a` VARCHAR(255), `b` FLOAT, `c` FLOAT, `d` FLOAT, `e` FLOAT GENERATED ALWAYS AS (b + c), `f` FLOAT GENERATED ALWAYS AS (c + d), `g` INT NULL ) ENGINE = Distributed('{cluster}', 'default', 'shard_Test', rand());
INSERT INTO Test VALUES('a', 1, 2, 3, NULL), ('b', 4, 5, 6, 10), ('c', 7, 8, 9, 15);
SELECT e, f FROM Test WHERE g IS NULL ORDER BY a ASC;
SELECT e, f FROM Test ORDER BY e DESC;
