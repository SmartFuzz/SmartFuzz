CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), value BLOB, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (name, value) VALUES ('test1', EMPTY_BLOB());
CREATE VIEW test_view AS SELECT name, value FROM test_table WHERE value IS NOT NULL;
SELECT DISTINCT name FROM test_view WHERE LENGTH(value) IN (SELECT LENGTH(EMPTY_BLOB()) FROM dual);
UPDATE test_table SET value = NULL WHERE id = 1;
