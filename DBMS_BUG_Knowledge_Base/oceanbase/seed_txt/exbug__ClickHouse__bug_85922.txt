CREATE TABLE IF NOT EXISTS users (id UInt64, name String, created_at DateTime);
CREATE TABLE IF NOT EXISTS user_logs (user_id UInt64, log_message String, log_time DateTime);
INSERT INTO users VALUES (1, 'Alice', now());
INSERT INTO user_logs SELECT id, 'User logged in' AS log_message, now() FROM users WHERE id = 1;
CREATE TABLE user_summary AS SELECT user_id, count(*) AS log_count FROM user_logs GROUP BY user_id;
