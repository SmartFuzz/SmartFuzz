CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name) VALUES (1, 'Test Name');
SELECT * FROM test_table WHERE id = (SELECT MAX(id) FROM test_table);
UPDATE test_table SET name = NULL WHERE id = 1;
SELECT JSON_OBJECT('result', CASE WHEN EXISTS (SELECT 1 FROM test_table WHERE id = 1) THEN TRUE ELSE FALSE END) AS json_output;
