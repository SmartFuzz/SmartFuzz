CREATE SEQUENCE id_seq START WITH 1;
CREATE TABLE names(id BIGINT DEFAULT NEXTVAL(id_seq) PRIMARY KEY, name VARCHAR(255) UNIQUE NOT NULL, count BIGINT NOT NULL, description TEXT);
INSERT INTO names (name, count, description) VALUES ('xyz', 10, 'Sample description') ON DUPLICATE KEY UPDATE count = VALUES(count);
INSERT INTO names (name, count, description) VALUES ('xyz', 15, 'Updated description') ON DUPLICATE KEY UPDATE count = VALUES(count);
INSERT INTO names (name, count) VALUES (NULL, 25) ON DUPLICATE KEY UPDATE count = VALUES(count);
SELECT * FROM names;
