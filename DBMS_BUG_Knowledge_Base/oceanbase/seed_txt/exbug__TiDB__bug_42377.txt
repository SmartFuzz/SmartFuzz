CREATE TABLE t2(c1 INT CHECK (c1 >= 0) DEFAULT 0);
INSERT INTO t2 VALUES (NULL), (1), (2), (3);
CREATE TABLE t3(c2 VARCHAR(50) NOT NULL DEFAULT 'default');
INSERT INTO t3 VALUES ('a'), (NULL), ('b');
SELECT t3.c2, CASE WHEN t2.c1 = 0 THEN 'zero' WHEN t2.c1 = 1 THEN 'one' WHEN t2.c1 = 2 THEN 'two' ELSE NULL END AS value FROM t2 RIGHT JOIN t3 ON t2.c1 = LENGTH(t3.c2) WHERE CASE WHEN t2.c1 = 0 THEN 'zero' WHEN t2.c1 IS NULL THEN NULL ELSE NULL END IS NULL;
