CREATE TABLE test.c (id INT PRIMARY KEY, value VARCHAR(64) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test.c VALUES (1, 'x'), (2, 'y'), (3, NULL), (4, 'z');
CREATE TABLE test.d (id INT, value VARCHAR(64), CONSTRAINT fk_id FOREIGN KEY (id) REFERENCES test.c(id));
INSERT INTO test.d VALUES (1, 'a'), (2, 'b'), (3, 'c');
SELECT * FROM test.c t1 JOIN test.d t2 ON t1.id = t2.id WHERE t1.value IS NOT NULL;
