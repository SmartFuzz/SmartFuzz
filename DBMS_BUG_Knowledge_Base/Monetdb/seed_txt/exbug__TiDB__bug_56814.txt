CREATE TABLE messages (id BIGINT NOT NULL, muid VARCHAR(100), conversationId VARCHAR(255), sender VARCHAR(100) NOT NULL, receiverType VARCHAR(10) NOT NULL DEFAULT 'user', receiver VARCHAR(100) NOT NULL, PRIMARY KEY (id));
ALTER TABLE messages ALTER COLUMN id SET DEFAULT NEXTVAL('messages_id_seq');
CREATE SEQUENCE messages_id_seq START WITH 730234878;
INSERT INTO messages (muid, conversationId, sender, receiverType, receiver) VALUES ('msg1', 'conv1', 'user1', 'user', 'user2');
INSERT INTO messages (muid, conversationId, sender, receiverType, receiver) VALUES ('msg2', 'conv2', 'user2', 'group', 'group1');
SELECT id FROM messages WHERE id >= 14609942 ORDER BY id LIMIT 10;
