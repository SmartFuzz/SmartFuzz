CREATE TABLE async_pt (a int, b int, c text DEFAULT 'default' NOT NULL);
CREATE TABLE base_tbl2 (a int, b int, c varchar(255) UNIQUE);
CREATE TABLE async_p2 (a int, b int, c varchar(255)) PARTITION OF async_pt FOR VALUES FROM (2000) TO (3000);
INSERT INTO async_pt SELECT i, CAST(i AS text) FROM (SELECT 3000 + n AS i FROM (SELECT ROW_NUMBER() OVER () AS n FROM system_range(1, 101)) AS seq) AS series;
DELETE FROM async_pt WHERE a IS NOT NULL;
