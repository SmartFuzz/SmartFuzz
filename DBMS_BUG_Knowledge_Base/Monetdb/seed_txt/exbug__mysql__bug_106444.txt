CREATE TABLE v1 ( c1 TEXT NOT NULL, c2 INT DEFAULT 0 );
INSERT INTO v1 VALUES ('test', 1);
SELECT * FROM v1 WHERE c1 LIKE c1 ESCAPE 'x';
SELECT (c1 LIKE c1 ESCAPE NULL) FROM v1;
SELECT * FROM v1 WHERE c1 LIKE c1 ESCAPE 0;
SELECT (c1 LIKE c1 ESCAPE 1) FROM v1;
