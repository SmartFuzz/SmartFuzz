CREATE TABLE users(id INT PRIMARY KEY, name VARCHAR(255), age INT, salary FLOAT);
INSERT INTO users(name, age, salary) VALUES('Alice', 30, 50000.00), ('Bob', NULL, 60000.00), ('Charlie', 25, 70000.00);
CREATE TABLE departments(id INT PRIMARY KEY, dept_name VARCHAR(255));
INSERT INTO departments(dept_name) VALUES('HR'), ('Engineering'), ('Sales');
CREATE VIEW user_salaries AS SELECT age, salary FROM users ORDER BY salary;
