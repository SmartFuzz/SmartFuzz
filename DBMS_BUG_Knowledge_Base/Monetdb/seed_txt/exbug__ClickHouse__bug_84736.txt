CREATE TABLE users (id INT, name VARCHAR(255), age TINYINT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO users VALUES (1, 'Alice', 30, NULL);
CREATE TABLE orders (order_id INT, user_id INT, amount DOUBLE PRECISION, order_date DATE);
INSERT INTO orders VALUES (101, 1, 99.99, '2023-10-01');
SELECT u.name, o.amount FROM users u JOIN orders o ON u.id = o.user_id WHERE u.age > 25;
