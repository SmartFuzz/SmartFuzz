CREATE TABLE nums ( id int PRIMARY KEY, value int CHECK (value >= 0) );
INSERT INTO nums VALUES (1, ? + 10);
CREATE TABLE mixed ( id int, text_value varchar(50), float_value float );
INSERT INTO mixed VALUES (2, 'Test', ? * 2.5);
SELECT id, value, (value - 5) AS adjusted_value FROM nums WHERE value IS NOT NULL;
