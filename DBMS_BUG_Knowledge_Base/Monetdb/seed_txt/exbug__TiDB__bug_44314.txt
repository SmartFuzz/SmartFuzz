CREATE TABLE queries_user ( id int NOT NULL, username varchar(50) NOT NULL, email varchar(100) DEFAULT NULL, PRIMARY KEY (id) );
INSERT INTO queries_user (id, username, email) VALUES (1, 'user1', 'user1@example.com'), (2, 2, NULL);
CREATE TABLE queries_order ( id int NOT NULL, user_id int NOT NULL, amount decimal(10,2) NOT NULL, PRIMARY KEY (id), FOREIGN KEY (user_id) REFERENCES queries_user (id) );
INSERT INTO queries_order (id, user_id, amount) VALUES (1, 1, 99.99), (2, 2, 49.50);
SELECT queries_user.id, queries_user.username, queries_order.amount FROM queries_user LEFT JOIN queries_order ON queries_user.id = queries_order.user_id WHERE queries_order.amount IS NOT NULL ORDER BY queries_user.id ASC;
