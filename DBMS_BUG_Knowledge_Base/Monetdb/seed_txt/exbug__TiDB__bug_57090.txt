CREATE TABLE user_data ( user_id int NOT NULL, username varchar(50) NOT NULL, email varchar(100) NOT NULL, created_at timestamp DEFAULT CURRENT_TIMESTAMP, preferences json DEFAULT NULL, PRIMARY KEY (user_id));
INSERT INTO user_data (username, email, preferences) VALUES ('john_doe', 'john@example.com', '{"theme": "dark", "notifications": true}');
CREATE TABLE transaction_log ( transaction_id int NOT NULL, user_id int NOT NULL, amount decimal(10,2) NOT NULL, transaction_date timestamp DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES user_data(user_id), PRIMARY KEY (transaction_id));
INSERT INTO transaction_log (user_id, amount) VALUES (1, 150.75);
SELECT u.username, t.amount FROM user_data u JOIN transaction_log t ON u.user_id = t.user_id WHERE t.amount > 100.00 ORDER BY t.transaction_date DESC;
