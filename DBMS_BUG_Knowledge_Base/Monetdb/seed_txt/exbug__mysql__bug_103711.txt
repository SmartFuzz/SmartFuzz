CREATE TABLE orders ( order_id int unsigned NOT NULL, product_id int unsigned NOT NULL, client_id int unsigned NOT NULL, order_date timestamp NOT NULL, PRIMARY KEY (order_id), FOREIGN KEY (product_id) REFERENCES products(product_id) ON DELETE CASCADE );
INSERT INTO orders (product_id, client_id, order_date) VALUES (1, 4, CURRENT_TIMESTAMP);
INSERT INTO orders (product_id, client_id, order_date) SELECT product_id, client_id, CURRENT_TIMESTAMP FROM products WHERE client_id IS NOT NULL;
SELECT o.order_id, p.payload1 FROM orders o JOIN products p ON o.product_id = p.product_id WHERE o.client_id = 4;
UPDATE products SET payload1 = NULL WHERE client_id = 4;
