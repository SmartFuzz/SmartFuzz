CREATE TABLE test_data (id INT, name VARCHAR, score FLOAT, group_id INT);
INSERT INTO test_data VALUES (1, 'Alice', NULL, 100);
INSERT INTO test_data VALUES (2, 'Bob', NULL, 100);
SELECT MAX(score) OVER (PARTITION BY group_id) FROM test_data;
SELECT MIN(score) OVER (PARTITION BY group_id) FROM test_data;
