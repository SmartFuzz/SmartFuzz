CREATE TABLE comment (id INT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, path VARCHAR(255) NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO comment (path) VALUES ('0.1.2.3'), ('0.1.2.4'), (NULL);
INSERT INTO comment (path) SELECT '0.' || i FROM (SELECT ROW_NUMBER() OVER () AS i FROM system::table(1, 500)) AS series;
CREATE INDEX ON comment (path);
SELECT * FROM comment;
