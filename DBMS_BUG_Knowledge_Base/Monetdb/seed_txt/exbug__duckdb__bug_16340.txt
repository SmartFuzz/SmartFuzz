CREATE TABLE test_table (id INTEGER PRIMARY KEY, name VARCHAR(255), description TEXT);
INSERT INTO test_table (id, name, description) VALUES (1, 'Test', 'This is a test description.');
SELECT CASE WHEN description IS NOT NULL THEN REPLACE(description, '*', 'X') ELSE NULL END FROM test_table WHERE id = 1;
SELECT CASE WHEN name IS NOT NULL THEN REPLACE(name, '*', '') ELSE NULL END FROM test_table WHERE id = 1;
SELECT CASE WHEN NULL IS NOT NULL THEN REPLACE(NULL, '*', 'X') ELSE NULL END AS result;
