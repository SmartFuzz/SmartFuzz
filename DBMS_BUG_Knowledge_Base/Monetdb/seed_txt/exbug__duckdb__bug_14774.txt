CREATE TABLE test_table (id INTEGER PRIMARY KEY, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, value) VALUES (1, 'a'), (2, 'a'), (3, 'b'), (4, 'a');
SELECT s(array_agg(value), 'a', 2, 'X') FROM test_table;
SELECT s(array_agg(value), 'a', 2, 'Y') FROM test_table;
SELECT s(array_agg(value), 'a', 2, 'X') AS result_x, s(array_agg(value), 'a', 2, 'Y') AS result_y FROM test_table;
