CREATE TABLE test_table(id INT PRIMARY KEY, name TEXT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table(name) VALUES ('test1'), ('test2'), (NULL), ('test3');
CREATE TABLE filler(a INT, b TEXT);
INSERT INTO filler SELECT g, repeat('x', 1000) FROM (SELECT ROW_NUMBER() OVER () AS g FROM (SELECT 1 UNION ALL SELECT 1) AS t1, (SELECT 1 UNION ALL SELECT 1) AS t2) AS series WHERE g <= 50000;
DROP TABLE IF EXISTS filler;
