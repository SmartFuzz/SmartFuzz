CREATE TABLE test_array_distinct (id INTEGER, values VARCHAR[]);
INSERT INTO test_array_distinct VALUES (1, ARRAY['A', 'A', 'B', NULL, NULL]), (2, ARRAY['B', 'C', 'C', 'A', NULL]);
SELECT DISTINCT UNNEST(values) AS distinct_values FROM test_array_distinct WHERE id = 1;
SELECT DISTINCT UNNEST(ARRAY_APPEND(values, 'D')) AS distinct_values FROM test_array_distinct WHERE id = 2;
SELECT DISTINCT UNNEST(ARRAY[1, 2, NULL, 2, NULL]) AS distinct_numbers;
