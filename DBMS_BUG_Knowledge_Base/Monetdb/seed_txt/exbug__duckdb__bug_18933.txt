CREATE TABLE testtable (id INTEGER PRIMARY KEY, textColumn TEXT, varcharColumn VARCHAR(255), nullableColumn VARCHAR);
INSERT INTO testtable (id, textColumn, varcharColumn, nullableColumn) VALUES (1, 'Sample text', 'Sample varchar', NULL);
SELECT c.name AS relname, a.name AS attname, a.type AS atttypid FROM sys.tables c JOIN sys.columns a ON a.object_id = c.object_id WHERE a.name = 'textColumn';
CREATE TABLE another_table (ref_id INTEGER REFERENCES testtable(id), description TEXT NOT NULL);
INSERT INTO another_table (ref_id, description) VALUES (1, 'Reference to testtable');
