CREATE TABLE t2(c1 INT NOT NULL, c2 VARCHAR(255), PRIMARY KEY(c1));
INSERT INTO t2(c1, c2) VALUES (1, 'test'), (2, NULL), (3, 'example');
SET @b = 5;
SELECT CASE WHEN @b IS NOT NULL THEN t2.c2 ELSE t2.c2 END FROM t2 WHERE t2.c1 > 0 GROUP BY t2.c1;
SELECT * FROM t2;
