CREATE TABLE test_data (id INTEGER PRIMARY KEY, value DOUBLE PRECISION, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data SELECT i, RANDOM(), CONCAT('Name ', i), NOW() FROM (SELECT ROW_NUMBER() OVER() AS i FROM sysibm.sysdummy1) AS seq WHERE i <= 200000000;
CREATE TABLE filtered_data AS SELECT * FROM test_data WHERE value IS NOT NULL;
COPY filtered_data TO '/tmp/filtered_test.parquet' USING DELIMITERS ',';
SELECT COUNT(*) FROM filtered_data WHERE value BETWEEN 0.0 AND 1.0;
