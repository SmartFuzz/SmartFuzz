CREATE TABLE tradeDate(date int not null constraint pk_tradeDate primary key, price decimal(10, 2) not null);
INSERT INTO tradeDate(date, price) VALUES (20230101, 100.50);
CREATE TABLE tradeDetails(trade_id int not null, trade_date int not null, quantity int not null, constraint pk_tradeDetails primary key(trade_id, trade_date), constraint fk_tradeDate foreign key(trade_date) references tradeDate(date));
INSERT INTO tradeDetails(trade_id, trade_date, quantity) VALUES (1, 20230101, 10);
SELECT ROW_NUMBER() OVER (PARTITION BY trade_date ORDER BY quantity) AS row_num, td.trade_id FROM tradeDetails td JOIN tradeDate t ON td.trade_date = t.date;
