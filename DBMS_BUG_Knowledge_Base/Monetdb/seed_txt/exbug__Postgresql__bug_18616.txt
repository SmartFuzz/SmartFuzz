CREATE TABLE t1(id INT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, value TEXT NOT NULL);
INSERT INTO t1(value) SELECT 'test' || i FROM (SELECT ROW_NUMBER() OVER () AS i FROM t1 LIMIT 100) AS series;
CREATE TABLE t2(a INT, b VARCHAR(255), c TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO t2(a, b) SELECT i, NULL FROM (SELECT ROW_NUMBER() OVER () AS i FROM t2 LIMIT 1000) AS series;
CREATE INDEX idx_t2_a ON t2 (a);
