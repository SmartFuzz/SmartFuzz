CREATE TABLE graph1 (subject URI, predicate URI, object STRING);
CREATE TABLE graph2 (subject URI, predicate URI, object STRING);
INSERT INTO graph1 VALUES (<urn:subject1>, rdfs:label, 'Subject one');
INSERT INTO graph2 VALUES (<urn:subject2>, rdfs:label, 'Subject two');
SELECT DISTINCT g.subject FROM graph1 AS g;
