CREATE TABLE test_table (id SERIAL PRIMARY KEY, value TEXT);
INSERT INTO test_table (value) VALUES (''), (''), (NULL);
SELECT CASE WHEN COUNT(value) = 0 THEN '' ELSE CONCAT_WS('', value) END AS concatenated_value FROM test_table;
INSERT INTO test_table (value) VALUES ('a'), ('b'), ('');
SELECT CASE WHEN COUNT(value) = 0 THEN '' ELSE CONCAT_WS('', value) END AS concatenated_value FROM test_table WHERE value IS NOT NULL;
