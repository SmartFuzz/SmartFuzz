DROP TABLE IF EXISTS t2;
CREATE TABLE t2 (c1 INT, c2 FLOAT, c3 VARCHAR(10));
INSERT INTO t2 VALUES (1, NULL, 'test');
SELECT CASE WHEN c1 IS NULL THEN NULL ELSE TRUNCATE(c1 + NULL, c2) END FROM t2;
SELECT CASE WHEN NULL IS NULL THEN NULL ELSE TRUNCATE(NULL, 5.5) END FROM t2;
