CREATE TABLE a (id integer PRIMARY KEY, value varchar(50));
INSERT INTO a VALUES (1, 'test1');
CREATE TABLE b (id integer, value varchar(50) NOT NULL);
INSERT INTO b VALUES (1, 'test2'), (2, NULL);
CREATE VIEW aview AS SELECT id, value FROM a WHERE id > 0;
