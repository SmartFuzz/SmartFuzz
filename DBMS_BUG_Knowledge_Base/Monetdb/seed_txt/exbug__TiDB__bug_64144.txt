CREATE TABLE test.b (id BIGINT PRIMARY KEY, description VARCHAR(20) NOT NULL, age INT CHECK (age >= 0), UNIQUE (description));
INSERT INTO test.b VALUES (1, 'Sample�Text');
SELECT description, LENGTH(description) FROM test.b WHERE id = 1;
SELECT * FROM test.b WHERE description = (SELECT description FROM test.b WHERE id = 1);
DELETE FROM test.b WHERE id = 1;
