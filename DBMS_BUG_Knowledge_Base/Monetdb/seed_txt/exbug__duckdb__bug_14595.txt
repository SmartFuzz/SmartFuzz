CREATE TABLE test_data (id INTEGER, category VARCHAR(1), value INTEGER);
INSERT INTO test_data VALUES (1, 'X', 150), (2, 'Y', 250), (3, 'X', 350), (4, 'Y', 450), (5, NULL, 550);
SELECT category, value, COUNT(*) OVER (PARTITION BY category) AS count_per_category FROM test_data;
CREATE TABLE boundary_data (id INTEGER PRIMARY KEY, amount DECIMAL(10, 2) NOT NULL CHECK (amount >= 0));
INSERT INTO boundary_data (id, amount) VALUES (1, 0.00), (2, 9999999999.99);
SELECT amount, ROW_NUMBER() OVER (ORDER BY amount) AS row_num FROM boundary_data;
