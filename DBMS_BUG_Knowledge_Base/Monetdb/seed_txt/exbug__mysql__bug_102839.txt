CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), value DECIMAL(10,2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_table (id, name, value) VALUES (1, 'test1', 10.50), (2, 'test2', NULL), (3, 'test3', 20.00);
SELECT SUBSTRING(name, 1, POSITION(',' IN name) - 1) AS extracted FROM test_table WHERE value IS NOT NULL;
SELECT SUBSTRING('a,b,c', 1, POSITION(',' IN 'a,b,c') - 1) AS test_case;
WITH RECURSIVE cte AS (SELECT 1 AS a UNION ALL SELECT a + 1 FROM cte WHERE a < 100) SELECT DISTINCT SUBSTRING('example1,example2,example3', 1, POSITION(',' IN 'example1,example2,example3') - 1) AS result FROM cte;
