CREATE TABLE trig_test (id INT PRIMARY KEY, angle FLOAT);
INSERT INTO trig_test (id, angle) VALUES (1, 0), (2, 2 * PI());
SELECT id, SIN(angle) AS sin_value FROM trig_test;
SELECT SIN(0 + 2 * PI()) AS sin_with_offset, SIN(0) AS sin_zero;
SELECT SIN(angle + 2 * PI()) FROM trig_test WHERE id = 1;
