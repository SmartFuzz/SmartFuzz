CREATE TABLE nested_structs (id INTEGER, data VARCHAR, value INTEGER);
INSERT INTO nested_structs VALUES (1, 'item1', 100);
INSERT INTO nested_structs VALUES (2, 'item2', NULL);
SELECT id, data, value FROM nested_structs WHERE value IS NOT NULL;
SELECT id, data, COALESCE(value, 0) AS value FROM nested_structs;
