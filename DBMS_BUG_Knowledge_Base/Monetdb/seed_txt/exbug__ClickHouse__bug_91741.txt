CREATE TABLE test_parallel_replicas (id INT, value VARCHAR(255), timestamp TIMESTAMP);
INSERT INTO test_parallel_replicas SELECT number, CAST(number AS VARCHAR) AS value, NOW() FROM generate_series(0, 99);
CREATE TABLE test_parallel_replicas_2 (id INT, value VARCHAR(255), timestamp TIMESTAMP);
INSERT INTO test_parallel_replicas_2 SELECT number, CASE WHEN number % 2 = 0 THEN CAST(number AS VARCHAR) ELSE NULL END AS value, NOW() FROM generate_series(0, 99);
SELECT COUNT(*) FROM test_parallel_replicas WHERE value IS NOT NULL AND timestamp > NOW() - INTERVAL '1 DAY';
