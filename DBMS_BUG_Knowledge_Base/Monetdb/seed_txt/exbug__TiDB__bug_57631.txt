CREATE TABLE sbtest2 ( id int NOT NULL, k int NOT NULL DEFAULT 0, c char(120) NOT NULL DEFAULT '', pad char(60) NOT NULL DEFAULT '', ec1 varchar(40), PRIMARY KEY (id), INDEX k_2 (k) );
INSERT INTO sbtest2 (k, c, pad, ec1) VALUES (1, 'test', 'padding', NULL);
UPDATE sbtest2 SET ec1 = 'updated' WHERE id = 1;
SELECT * FROM sbtest2 WHERE k IN (SELECT k FROM sbtest2 WHERE ec1 IS NOT NULL);
ANALYZE TABLE sbtest2;
