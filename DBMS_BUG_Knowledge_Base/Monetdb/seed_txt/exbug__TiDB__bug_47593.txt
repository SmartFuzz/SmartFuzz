CREATE TABLE users ( user_id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY, username VARCHAR(50) NOT NULL, email VARCHAR(100) DEFAULT NULL, PRIMARY KEY (user_id), UNIQUE (username));
INSERT INTO users (username, email) VALUES ('testuser', 'test@example.com');
CREATE TABLE orders ( order_id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY, user_id INT NOT NULL, amount DECIMAL(10,2) NOT NULL, PRIMARY KEY (order_id), FOREIGN KEY (user_id) REFERENCES users(user_id));
INSERT INTO orders (user_id, amount) VALUES (1, 99.99);
SELECT * FROM users;
