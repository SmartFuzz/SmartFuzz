CREATE TABLE chunks_2 ( id CHAR(32) NOT NULL, embedding BLOB DEFAULT NULL, PRIMARY KEY (id) );
INSERT INTO chunks_2 (id, embedding) VALUES ('1', '[0.1, 0.2, 0.3, ...]'), ('2', '[0.4, 0.5, 0.6, ...]'), ('3', '[0.7, 0.8, 0.9, ...]');
SELECT * FROM chunks_2 WHERE embedding IS NOT NULL ORDER BY (SELECT VEC_COSINE_DISTANCE(embedding, '[0.1, 0.2, 0.3, ...]')) LIMIT 5;
CREATE TABLE documents ( document_id INT NOT NULL, content TEXT DEFAULT NULL, PRIMARY KEY (document_id) );
INSERT INTO documents (document_id, content) VALUES (1, 'Document 1'), (2, 'Document 2');
