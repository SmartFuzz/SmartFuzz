CREATE TABLE example (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, direction VARCHAR(20) NOT NULL, below BOOLEAN NOT NULL);
ALTER TABLE example ADD CONSTRAINT ck_example_direction CHECK ((direction = 'DOWN' AND below) OR (NOT below));
ALTER TABLE example ALTER COLUMN direction TYPE VARCHAR(20);
ALTER TABLE example DROP CONSTRAINT ck_example_direction;
ALTER TABLE example ADD CONSTRAINT ck_example_direction CHECK ((direction = 'DOWN' AND below) OR (NOT below));
