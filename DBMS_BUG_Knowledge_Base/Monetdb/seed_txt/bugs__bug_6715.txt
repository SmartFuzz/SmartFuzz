CREATE TABLE ProductTarget (ProductID SERIAL PRIMARY KEY, Name VARCHAR(100) NOT NULL, ProductNumber BIGINT, Color VARCHAR(30) NOT NULL);
CREATE TABLE ProductSource (ProductID SERIAL PRIMARY KEY, Name VARCHAR(100) NOT NULL, ProductNumber BIGINT, Color VARCHAR(30) NOT NULL);
INSERT INTO ProductSource (Name, ProductNumber, Color) VALUES ('Widget', 1001, 'Red');
INSERT INTO ProductTarget (Name, ProductNumber, Color) VALUES ('Gadget', NULL, 'Blue');
MERGE INTO ProductTarget AS T USING ProductSource AS S ON T.ProductID = S.ProductID WHEN MATCHED THEN UPDATE SET T.Name = S.Name, T.Color = COALESCE(S.Color, 'Unknown');
