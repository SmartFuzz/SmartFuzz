DROP TABLE IF EXISTS test_table;
CREATE TABLE test_table (id INT, name VARCHAR(255), age TINYINT, score FLOAT, created_at TIMESTAMP);
INSERT INTO test_table VALUES (1, 'Alice', 25, 88.5, CURRENT_TIMESTAMP), (2, 'Bob', 30, 92.0, CURRENT_TIMESTAMP);
INSERT INTO test_table SELECT number, CAST(number AS VARCHAR), number % 100, number / 10.0, CURRENT_TIMESTAMP FROM (SELECT ROW_NUMBER() OVER () - 1 AS number FROM (SELECT 1 UNION ALL SELECT 2 UNION ALL SELECT 3 UNION ALL SELECT 4 UNION ALL SELECT 5));
SELECT id FROM test_table WHERE name = 'Bob' AND age < 30;
