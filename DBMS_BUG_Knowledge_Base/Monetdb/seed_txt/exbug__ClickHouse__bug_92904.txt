CREATE TABLE test_1 (ts DATE, a INT, b DOUBLE PRECISION, c VARCHAR, d TIMESTAMP);
INSERT INTO test_1 SELECT CURRENT_DATE - (RANDOM() % 100), RANDOM() % 100, CASE WHEN RANDOM() % 2 = 0 THEN NULL ELSE CAST(RANDOM() % 100 AS VARCHAR) END, CURRENT_TIMESTAMP FROM SYSTEM GENERATE SERIES 1 TO 100000;
CREATE TABLE test_2 (id BIGINT, value VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_2 SELECT number, CAST(RANDOM() % 100 AS VARCHAR) FROM SYSTEM GENERATE SERIES 1 TO 1000000;
SELECT a, COUNT(*) FROM (SELECT a, CASE WHEN b > 50 THEN 1 ELSE 0 END AS fun_res FROM test_1) GROUP BY fun_res;
