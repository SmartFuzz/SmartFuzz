DROP TABLE IF EXISTS t2 CASCADE;
CREATE TABLE t2(c0 VARCHAR, c1 INTEGER, c2 FLOAT, PRIMARY KEY(c0));
INSERT INTO t2 VALUES ('x', 1, 1.1), ('y', 2, 2.2), ('z', 3, NULL);
CREATE VIEW v1 AS SELECT c0, SUM(c2) FROM t2 GROUP BY c0 HAVING COUNT(c1) > 1;
CREATE VIEW v2 AS SELECT ('b' || t2.c0) FROM t2 GROUP BY (CASE WHEN t2.c1 IS NULL THEN 'NULL' ELSE 'NOT NULL' END);
