CREATE TABLE t3 (a INT, b INT, c DECIMAL(10,4));
INSERT INTO t3 (a, b, c) VALUES (1, 1, 0.0001);
SELECT (b / 0.0001 / 2) AS x FROM t3 GROUP BY x;
SELECT (a / 0.0001 / 2) AS x FROM t3 GROUP BY x ORDER BY x;
SELECT (c / NULLIF(c, c)) AS x FROM t3 WHERE c IS NOT NULL;
