CREATE TABLE test_dates (id INT PRIMARY KEY, event_date TIMESTAMP NOT NULL);
INSERT INTO test_dates (id, event_date) VALUES (1, '2023-03-21 12:00:00');
SELECT event_date + (3 * INTERVAL '1 DAY') FROM test_dates WHERE id = 1;
SELECT event_date + INTERVAL '3 DAY' FROM test_dates WHERE id = 1;
SELECT event_date + (COUNT(*) * INTERVAL '1 DAY') FROM test_dates WHERE id = 1;
