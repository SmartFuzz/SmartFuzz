CREATE TABLE tp3 (s1 VARCHAR(64) NOT NULL, s2 INT NOT NULL, PRIMARY KEY (s1, s2)) PARTITION BY HASH (s1) PARTITIONS 8;
CREATE TABLE tp4 (s1 VARCHAR(64) NOT NULL, s2 INT NOT NULL, PRIMARY KEY (s1, s2)) PARTITION BY HASH (s1) PARTITIONS 9;
INSERT INTO tp3 (s1, s2) SELECT CONCAT('value', counter), FLOOR(RANDOM() * 100) FROM (SELECT ROW_NUMBER() OVER () AS counter FROM (SELECT NULL FROM system.tables LIMIT 600) AS select1) AS select_counter;
INSERT INTO tp4 (s1, s2) SELECT CONCAT('value', counter), FLOOR(RANDOM() * 100) FROM (SELECT ROW_NUMBER() OVER () AS counter FROM (SELECT NULL FROM system.tables LIMIT 600) AS select1) AS select_counter;
SELECT partition_name, data_length FROM sys.partitions WHERE table_name = 'tp3';
