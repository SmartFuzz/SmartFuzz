DROP TABLE IF EXISTS t2 CASCADE;
CREATE TABLE t2(c1 INT, c2 VARCHAR(100) NOT NULL);
INSERT INTO t2 VALUES (1, 'test'), (2, NULL), (3, 'example');
SELECT * FROM t2 WHERE c1 IN (SELECT c1 FROM t2 WHERE c2 IS NOT NULL) AND CONTAINS(c2, 'test');
SELECT * FROM t2 INNER JOIN (SELECT c1 FROM t2 WHERE c2 IS NULL) AS subquery ON t2.c1 = subquery.c1 WHERE NOT CONTAINS(c2, 'example');
