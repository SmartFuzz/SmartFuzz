CREATE TABLE test_intervals (id SERIAL PRIMARY KEY, interval_col INTERVAL YEAR TO MONTH, description TEXT);
INSERT INTO test_intervals (interval_col, description) VALUES (CAST('1-2' AS INTERVAL YEAR TO MONTH), 'Valid interval');
INSERT INTO test_intervals (interval_col, description) VALUES (NULL, 'NULL interval');
SELECT id, interval_col FROM test_intervals WHERE interval_col IS NOT NULL;
SELECT id, CAST('2-3' AS INTERVAL YEAR TO MONTH) AS new_interval FROM test_intervals;
