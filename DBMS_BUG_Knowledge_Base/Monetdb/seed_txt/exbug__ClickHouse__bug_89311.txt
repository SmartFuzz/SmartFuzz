CREATE TABLE IF NOT EXISTS test_v2 (list_id UUID, item VARCHAR(255), created_at TIMESTAMP, price FLOAT, is_active BOOLEAN);
INSERT INTO test_v2 (list_id, item, created_at, price, is_active) SELECT UUID(), 'item' || i, NOW() + INTERVAL i SECOND, RANDOM() * 100, RANDOM() > 0.5 FROM generate_series(1, 100000);
CREATE INDEX prj_list_id_created_at ON test_v2 (list_id, created_at);
SELECT * FROM test_v2 WHERE list_id = '019a369d-0a96-7fbd-853c-a4335fd2ee9f' ORDER BY created_at LIMIT 100 OFFSET 6000;
