CREATE TABLE test_table (id INTEGER, data VARCHAR);
INSERT INTO test_table VALUES (1, '[1, 2, 3]'), (2, 'not_a_list');
SELECT CASE WHEN data IS NOT NULL THEN (SELECT unnest(STRING_TO_ARRAY(data, ','))) ELSE '0' END AS x FROM test_table WHERE id = 1;
SELECT CASE WHEN id = 2 THEN (SELECT unnest(STRING_TO_ARRAY(data, ','))) ELSE 'No List' END AS x FROM test_table;
SELECT CASE WHEN id < 3 THEN (SELECT unnest(STRING_TO_ARRAY(data, ','))) ELSE NULL END AS x FROM test_table;
