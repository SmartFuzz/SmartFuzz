CREATE TABLE test_table (id INTEGER PRIMARY KEY, arr VARCHAR[] NOT NULL);
INSERT INTO test_table (id, arr) VALUES (1, ARRAY['a,c', 'b']);
SELECT arr, ARRAY_LENGTH(arr) AS arr_length FROM test_table WHERE id = 1;
SELECT '''' || STRING_AGG(arr[i], ''', ''') || '''' AS escaped_arr FROM test_table, generate_series(1, ARRAY_LENGTH(arr)) AS i WHERE id = 1;
SELECT ARRAY_LENGTH(ARRAY['hello, world']) AS before, ARRAY_LENGTH(ARRAY['hello, world']) AS after;
