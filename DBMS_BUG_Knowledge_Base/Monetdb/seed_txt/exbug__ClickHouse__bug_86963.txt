DROP TABLE IF EXISTS test_data;
CREATE TABLE test_data (id BIGINT, value VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data SELECT number, CAST(number AS VARCHAR) FROM generate_series(1, 100);
SELECT id, value FROM test_data WHERE id % 2 = 0;
ALTER TABLE test_data ADD COLUMN updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP;
