CREATE TABLE p ( groupid BIGINT DEFAULT NULL, PRIMARY KEY (groupid));
CREATE TABLE g ( groupid BIGINT DEFAULT NULL, parentid BIGINT NOT NULL, PRIMARY KEY (groupid, parentid));
INSERT INTO p (groupid) VALUES (1), (2), (3);
INSERT INTO g (groupid, parentid) VALUES (1, 0), (2, 1), (3, 1);
SELECT 1 FROM g WHERE groupid IN (SELECT groupid FROM (SELECT groupid FROM p UNION SELECT g.groupid FROM g JOIN (SELECT groupid FROM p) AS w ON g.parentid = w.groupid) AS w);
