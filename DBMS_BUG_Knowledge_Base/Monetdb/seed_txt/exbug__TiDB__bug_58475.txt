DROP TABLE IF EXISTS tp;
CREATE TABLE tp (id INT, c1 JSON, c2 JSON, c3 INT NOT NULL DEFAULT 0, PRIMARY KEY (id));
ALTER TABLE tp ADD COLUMN c2 JSON GENERATED ALWAYS AS (c1);
INSERT INTO tp (id, c1, c3) VALUES (1, '[1]', 10);
INSERT INTO tp (id, c1) VALUES (0, '[2]');
SELECT id FROM tp WHERE c2 @> '1'::json OR c3 IS NOT NULL GROUP BY id HAVING id IN (0, 1);
