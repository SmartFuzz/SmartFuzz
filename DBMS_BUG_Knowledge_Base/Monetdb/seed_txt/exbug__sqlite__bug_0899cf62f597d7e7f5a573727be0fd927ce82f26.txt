CREATE TABLE v1 ( id INTEGER PRIMARY KEY, value TEXT NOT NULL );
INSERT INTO v1 (value) VALUES ('A'), ('B'), ('C');
CREATE TABLE v2 ( id INTEGER PRIMARY KEY, ref_id INTEGER, amount DOUBLE PRECISION );
INSERT INTO v2 (ref_id, amount) VALUES (1, 10.5), (1, 20.0), (2, NULL);
SELECT DISTINCT value, LEAD(value) OVER() AS next_value FROM v1 ORDER BY value;
