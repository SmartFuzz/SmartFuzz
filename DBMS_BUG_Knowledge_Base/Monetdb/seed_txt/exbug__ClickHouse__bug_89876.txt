CREATE TABLE test2 (id INT, name VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP) ENGINE = MERCATOR;
CREATE TABLE test2_dist AS test2;
INSERT INTO test2 SELECT number, CONCAT('Name', number) FROM generate_series(1, 100);
ALTER TABLE test2 ADD PARTITION (SELECT * FROM test2_dist);
SELECT COUNT(*) FROM test2_dist;
