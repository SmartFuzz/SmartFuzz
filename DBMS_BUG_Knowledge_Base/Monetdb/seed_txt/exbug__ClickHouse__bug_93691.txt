CREATE TABLE test_enum1 (status VARCHAR(20) CHECK (status IN ('active', 'inactive', 'pending', 'suspended')));
CREATE TABLE test_enum2 (level VARCHAR(20) CHECK (level IN ('low', 'medium', 'high')));
INSERT INTO test_enum1 (status) VALUES ('active'), ('inactive'), ('pending');
INSERT INTO test_enum2 (level) VALUES ('low'), ('medium'), ('high');
SELECT * FROM test_enum1 WHERE status = 'pending';
