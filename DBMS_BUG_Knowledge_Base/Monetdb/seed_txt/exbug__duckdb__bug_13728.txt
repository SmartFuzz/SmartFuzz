CREATE TABLE t2 (a INTEGER, b VARCHAR, c DOUBLE, d BOOLEAN);
INSERT INTO t2 VALUES (1, 'test', 3.14, TRUE), (2, 'example', NULL, FALSE);
CREATE OR REPLACE FUNCTION unpivot_test() RETURNS TABLE (column_name VARCHAR, value VARCHAR) AS $$ SELECT 'a' AS column_name, CAST(a AS VARCHAR) AS value FROM t2 UNION ALL SELECT 'b', b FROM t2;
$$ LANGUAGE SQL;
SELECT * FROM unpivot_test();
CREATE OR REPLACE FUNCTION unpivot_with_null() RETURNS TABLE (column_name VARCHAR, value VARCHAR) AS $$ SELECT 'c' AS column_name, CAST(c AS VARCHAR) AS value FROM t2 UNION ALL SELECT 'd', CAST(d AS VARCHAR) FROM t2;
