CREATE TABLE items (id INT PRIMARY KEY, name TEXT, quantity INT, created_at TIMESTAMP);
INSERT INTO items VALUES (1, 'item1', 10, TIMESTAMP '2024-01-01 10:00:00');
INSERT INTO items VALUES (2, 'item2', 5, TIMESTAMP '2024-01-01 10:05:00');
SELECT * FROM (SELECT id, (SELECT COUNT(*) > 0 FROM items c WHERE i.id = c.id AND quantity > 0) AS has_stock FROM items i) AS item_counts WHERE has_stock;
SELECT * FROM (SELECT id, (SELECT COUNT(*) > 0 FROM items c WHERE i.id = c.id AND quantity > 0) AS has_stock FROM items i) AS item_counts WHERE NOT has_stock;
