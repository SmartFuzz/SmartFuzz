CREATE TABLE a_table (id integer PRIMARY KEY, name varchar(50));
CREATE TABLE b_table (id integer, value float CHECK (value > 0));
INSERT INTO a_table VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');
INSERT INTO b_table VALUES (1, 10.5), (2, 20.0), (3, 30.0);
DELETE FROM a_table WHERE id IN (SELECT non_existent FROM b_table);
