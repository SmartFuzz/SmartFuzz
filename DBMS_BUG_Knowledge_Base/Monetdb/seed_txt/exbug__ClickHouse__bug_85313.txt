CREATE TABLE events_v2 (app_id BIGINT, network VARCHAR(255), name_asset_group_id BIGINT, date DATE, status VARCHAR(255));
INSERT INTO events_v2 VALUES (0, 'snapchat', 123, '2023-08-04', NULL), (42, 'snapchat', 0, '2023-08-04', 'active');
SELECT 'new analyzer', CASE WHEN app_id = 0 THEN 1 ELSE app_id END AS app_id, network, name_asset_group_id, date, status FROM events_v2 WHERE app_id != 0 AND status IS NOT NULL;
SELECT 'old analyzer', CASE WHEN app_id = 0 THEN 1 ELSE app_id END AS app_id, network, name_asset_group_id, date, status FROM events_v2 WHERE app_id != 0 AND status IS NOT NULL;
