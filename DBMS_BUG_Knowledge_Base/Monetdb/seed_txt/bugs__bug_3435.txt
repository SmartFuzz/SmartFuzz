CREATE TABLE TEST1 ( one VARCHAR(10) NOT NULL, two INT DEFAULT 0, three DATE );
CREATE TABLE TEST2 ( one VARCHAR(10) NOT NULL, two INT CHECK (two >= 0), four FLOAT );
INSERT INTO TEST1 VALUES ( 'a', 1, '2023-01-01' );
INSERT INTO TEST2 VALUES ( 'a', 1, 1.5 );
CREATE INDEX idx_test1 ON TEST1 ( one, two );
