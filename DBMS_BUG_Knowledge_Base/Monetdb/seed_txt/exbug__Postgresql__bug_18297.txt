CREATE TABLE parent (id INT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, name VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE child1 (LIKE parent INCLUDING ALL);
CREATE TABLE child2 (LIKE child1 INCLUDING ALL);
ALTER TABLE parent ADD COLUMN age INT CHECK (age >= 0);
INSERT INTO parent (name, age) VALUES ('Alice', 30), ('Bob', NULL);
