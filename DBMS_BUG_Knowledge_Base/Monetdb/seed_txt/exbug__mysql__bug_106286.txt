CREATE TABLE test (id INT PRIMARY KEY, ts TIMESTAMP NULL);
INSERT INTO test (id, ts) VALUES (1, '2023-01-01 00:00:00');
CREATE TRIGGER tr_bi_check_uniqueness_with_nulls BEFORE INSERT ON test FOR EACH ROW BEGIN IF EXISTS ( SELECT NULL FROM test WHERE test.id = NEW.id AND test.ts IS NOT DISTINCT FROM NEW.ts ) THEN RAISE EXCEPTION 'Duplicated values not allowed.';
END IF;
END;
INSERT INTO test (id, ts) VALUES (1, NULL);
