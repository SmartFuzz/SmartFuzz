CREATE TABLE products ( id INT NOT NULL, name VARCHAR(100) NOT NULL, price DECIMAL(10,2) NOT NULL, PRIMARY KEY (id) );
CREATE TABLE orders ( id INT NOT NULL, product_id INT NOT NULL, quantity INT NOT NULL, PRIMARY KEY (id), CONSTRAINT fk_product FOREIGN KEY (product_id) REFERENCES products (id) );
INSERT INTO products (name, price) VALUES ('Widget', 19.99);
INSERT INTO orders (product_id, quantity) VALUES (1, 5);
SELECT * FROM orders;
