CREATE TABLE test1 (a ARRAY(INT), b INT);
INSERT INTO test1 SELECT number % 10, number FROM generate_series(0, 99);
CREATE TABLE test2 (x FLOAT8, y VARCHAR);
INSERT INTO test2 SELECT number::FLOAT8, number::VARCHAR FROM generate_series(0, 49);
CREATE TABLE test3 (id BIGINT, data ARRAY(VARCHAR));
