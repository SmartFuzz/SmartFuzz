CREATE TABLE t(p BOOL, q INT, r TEXT DEFAULT 'default');
INSERT INTO t(p, q) VALUES (NULL, 1);
SELECT lock_table('t', 'EXCLUSIVE');
UPDATE t SET q = q + 1 WHERE p IS NULL RETURNING p, q;
UPDATE t SET r = 'updated' WHERE p IS NULL RETURNING r;
