CREATE TABLE test_data (id INTEGER PRIMARY KEY, parent_id INTEGER, json_data VARCHAR, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO test_data (id, parent_id, json_data) VALUES (1, NULL, '{"id": "1", "parent": null}');
INSERT INTO test_data (id, parent_id, json_data) VALUES (2, 1, '{"id": "2", "parent": "1"}');
WITH RECURSIVE cte (id, parent_id) AS (SELECT id, parent_id FROM test_data WHERE parent_id IS NULL UNION ALL SELECT td.id, td.parent_id FROM test_data td JOIN cte ON td.parent_id = cte.id) SELECT * FROM cte;
SELECT json_data AS json, json_data AS id, json_data AS parent FROM test_data WHERE json_data IS NOT NULL ORDER BY id;
