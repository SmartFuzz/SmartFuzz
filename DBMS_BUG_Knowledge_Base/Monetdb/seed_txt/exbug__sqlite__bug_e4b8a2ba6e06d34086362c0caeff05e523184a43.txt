CREATE TABLE users (id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT, email VARCHAR(255) UNIQUE);
INSERT INTO users (name, age, email) VALUES ('Alice', 30, 'alice@example.com');
INSERT INTO users (name, age, email) VALUES ('Bob', NULL, 'bob@example.com');
SELECT DISTINCT name FROM users WHERE age IS NOT NULL;
SELECT DISTINCT age FROM users WHERE email LIKE '%@example.com';
