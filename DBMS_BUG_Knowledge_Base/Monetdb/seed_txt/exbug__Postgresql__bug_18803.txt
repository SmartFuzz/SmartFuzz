CREATE TABLE Users(UserID BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, UserName TEXT NOT NULL);
CREATE TABLE Orders(MessageID TEXT NOT NULL, OrderDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE Transfers(TransferID BIGINT PRIMARY KEY, Amount DECIMAL CHECK (Amount > 0));
INSERT INTO Users(UserName) VALUES ('Alice'), ('Bob'), ('Charlie');
INSERT INTO Orders(MessageID) VALUES (NULL), ('Order1'), ('Order2');
SELECT COUNT(*) AS Total FROM Orders;
