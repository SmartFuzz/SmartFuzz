CREATE TABLE a3 (nId INT PRIMARY KEY, wstrText VARCHAR(200) UNICODE);
INSERT INTO a3 (wstrText) VALUES ('Patchwork');
CREATE TABLE a4 (nId INT PRIMARY KEY, wstrText VARCHAR(200) UNICODE);
INSERT INTO a4 (wstrText) VALUES ('Patch');
SELECT als.wstrText FROM (SELECT DISTINCT a2.wstrText AS wstrText FROM a2 WHERE a2.wstrText LIKE 'Patc%' ESCAPE '\' UNION ALL SELECT DISTINCT a3.wstrText AS wstrText FROM a3 WHERE (a3.wstrText LIKE 'Patc%' ESCAPE '\')) als WHERE als.wstrText LIKE 'P%';
