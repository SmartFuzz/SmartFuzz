CREATE TABLE json_test (id INT PRIMARY KEY, data VARCHAR(255) NOT NULL);
INSERT INTO json_test (id, data) VALUES (1, '{"const": false}');
INSERT INTO json_test (id, data) VALUES (2, '{"$ref": "#/$defs/test"}');
SELECT CASE WHEN data IS NOT NULL AND data = '{"const": false}' THEN 1 ELSE 0 END FROM json_test WHERE id = 1;
SELECT CASE WHEN data IS NOT NULL AND data = '{"$ref": "#/$defs/test"}' THEN 1 ELSE 0 END FROM json_test WHERE id = 2;
