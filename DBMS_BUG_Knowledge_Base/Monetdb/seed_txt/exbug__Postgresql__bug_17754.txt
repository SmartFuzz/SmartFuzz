DROP TABLE IF EXISTS atable;
DROP TABLE IF EXISTS btable;
CREATE TABLE atable (a int, b int, c text DEFAULT 'default', d boolean NOT NULL);
CREATE TABLE btable (c int, d int, e double precision CHECK (e > 0));
INSERT INTO atable SELECT series(1, 10) AS a, series(1, 10) AS b, NULL, TRUE;
