CREATE TABLE test2 (id BIGINT, value BIGINT NULL, arr VARCHAR(255)[]) USING MERGE;
INSERT INTO test2 SELECT number, CASE WHEN number % 2 = 0 THEN number ELSE NULL END, ARRAY['a', 'b', 'c'] FROM SYSTEM_RANGE(0, 10);
CREATE TABLE test3 AS SELECT * FROM test2;
SELECT arr, ARRAY_LENGTH(arr) FROM test2 WHERE value IS NOT NULL ORDER BY value LIMIT 3;
SELECT id, value, arr[1] FROM test3 WHERE ARRAY_LENGTH(arr) > 0;
