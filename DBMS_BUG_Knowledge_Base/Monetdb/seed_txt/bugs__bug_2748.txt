CREATE TABLE t (x VARCHAR(1), y VARCHAR(10) NOT NULL);
INSERT INTO t (x, y) VALUES ('\', 'valid');
INSERT INTO t (x, y) VALUES (NULL, 'another valid');
SELECT * FROM t WHERE x IS NOT NULL;
COPY INTO t FROM (SELECT '\' AS x, 'test' AS y);
