CREATE TABLE users(id INT PRIMARY KEY, name TEXT NOT NULL, age INT);
INSERT INTO users(name, age) VALUES('Alice', 30);
INSERT INTO users(name, age) VALUES('Bob', NULL);
SELECT users.id, users.name FROM users LEFT JOIN (SELECT id, name FROM users WHERE age IS NOT NULL) AS filtered_users ON users.id = filtered_users.id;
SELECT users.name, COALESCE(filtered_users.name, 'Unknown') AS filtered_name FROM users LEFT JOIN (SELECT id, name FROM users WHERE age < 25) AS filtered_users ON users.id = filtered_users.id;
