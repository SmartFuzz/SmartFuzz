CREATE TABLE test_json_data (id INTEGER PRIMARY KEY, data JSON);
INSERT INTO test_json_data (id, data) VALUES (1, NULL), (2, '[]'::json), (3, 'null'::json);
SELECT CASE WHEN data IS NULL THEN 0 ELSE json_array_length(data) END AS length FROM test_json_data WHERE id = 1;
SELECT CASE WHEN data IS NULL THEN 0 ELSE json_array_length(data) END AS length FROM test_json_data WHERE id = 2;
SELECT CASE WHEN data IS NULL THEN 0 ELSE json_array_length(data) END AS length FROM test_json_data WHERE id = 3;
