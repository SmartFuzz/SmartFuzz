CREATE TABLE t_sva(sva text, id integer NOT NULL AUTO_INCREMENT PRIMARY KEY);
INSERT INTO t_sva(sva) VALUES('1118090878'), ('2345010477'), (NULL);
CREATE FUNCTION normalize_si(sva text) RETURNS text AS 'BEGIN RETURN substring(sva,9,2) || substring(sva,7,2) || substring(sva,5,2) || substring(sva,1,4);
END;
' LANGUAGE SQL;
CREATE FUNCTION si_lt(a text, b text) RETURNS boolean AS 'BEGIN RETURN normalize_si(a) < normalize_si(b);
