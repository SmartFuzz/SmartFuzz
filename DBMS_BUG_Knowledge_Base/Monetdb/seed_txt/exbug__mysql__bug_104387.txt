CREATE TABLE colltest2 ( id INT NOT NULL PRIMARY KEY, cbin VARBINARY(140), ctxt TEXT, cstr VARCHAR(255), cnum INT DEFAULT NULL);
INSERT INTO colltest2 (id, cbin, ctxt, cstr, cnum) VALUES (1, B'asdf', 'asdf', 'asdf', NULL);
SELECT * FROM colltest2 WHERE cbin LIKE cstr;
SELECT * FROM colltest2 WHERE cbin LIKE ctxt;
SELECT * FROM colltest2 WHERE cnum IS NULL;
