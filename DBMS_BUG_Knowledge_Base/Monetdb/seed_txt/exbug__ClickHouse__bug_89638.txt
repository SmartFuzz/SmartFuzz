CREATE TABLE test_partitioned (id INT, dt TIMESTAMP, value DOUBLE PRECISION) USING MERGE;
INSERT INTO test_partitioned SELECT number, DATE '2015-01-01' + (number % 3650), RANDOM() FROM SYSTEM GENERATE SERIES 1 TO 10000000;
CREATE TABLE test_non_partitioned (id INT, dt TIMESTAMP, value DOUBLE PRECISION) USING MERGE;
INSERT INTO test_non_partitioned SELECT number, DATE '2015-01-01' + (number % 3650), RANDOM() FROM SYSTEM GENERATE SERIES 1 TO 10000000;
SELECT dt FROM test_partitioned WHERE value IS NOT NULL ORDER BY dt LIMIT 20;
