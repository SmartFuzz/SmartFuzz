CREATE DATABASE my_fuzz_db;
CREATE TABLE x (_id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, value VARCHAR(100) NOT NULL);
CREATE TABLE y (_id INT NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, ref_id INT, description TEXT, FOREIGN KEY (ref_id) REFERENCES x(_id));
INSERT INTO x (value) VALUES ('test1'), ('test2'), ('test3');
SELECT * FROM x;
