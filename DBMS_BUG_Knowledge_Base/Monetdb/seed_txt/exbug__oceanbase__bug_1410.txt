DROP PROCEDURE IF EXISTS p_json_test;
CREATE PROCEDURE p_json_test() BEGIN DECLARE jdata LONGTEXT;
END;
SELECT CONCAT('{"test":', COALESCE(region, 'null'), '}') INTO jdata FROM (SELECT NULL AS region) AS x;
CALL p_json_test();
SELECT jdata FROM (SELECT CONCAT('{"test":', COALESCE(region, 'null'), '}') AS jdata FROM (SELECT NULL AS region) AS x) AS y;
