CREATE TABLE sbtest2 ( id int NOT NULL, k int NOT NULL DEFAULT 0, c varchar(255) NOT NULL, pad text NOT NULL, PRIMARY KEY (id), UNIQUE (k));
INSERT INTO sbtest2 (k, c, pad) VALUES (1, 'test1', 'padding1');
INSERT INTO sbtest2 (k, c, pad) VALUES (2, NULL, 'padding2');
SELECT COUNT(*) FROM sbtest2 WHERE k IS NULL OR c IS NULL;
ALTER TABLE sbtest2 ALTER COLUMN id SET DEFAULT 1;
