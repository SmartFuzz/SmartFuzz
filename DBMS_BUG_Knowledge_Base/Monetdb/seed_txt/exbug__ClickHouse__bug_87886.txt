CREATE TABLE test_improve_prewhere__fuzz_20 (id INT, name VARCHAR(255), created_at TIMESTAMP NULL, status VARCHAR(255) NULL) PRIMARY KEY (id);
INSERT INTO test_improve_prewhere__fuzz_20 SELECT number, CASE number % 3 WHEN 1 THEN 'active' WHEN 2 THEN 'inactive' ELSE 'pending' END, TIMESTAMP '2025-08-01 12:00:00' + INTERVAL number SECOND, CASE WHEN number % 2 = 0 THEN 'active' ELSE NULL END FROM (SELECT ROW_NUMBER() OVER () - 1 AS number FROM sysibm.sysdummy1 LIMIT 10);
CREATE TABLE test_improve_prewhere__fuzz_21 (user_id INT, email VARCHAR(255), signup_date TIMESTAMP NULL, last_login TIMESTAMP NULL) PRIMARY KEY (user_id);
INSERT INTO test_improve_prewhere__fuzz_21 SELECT number, CONCAT('user', number, '@example.com'), TIMESTAMP '2025-08-01' + INTERVAL number DAY, TIMESTAMP '2025-09-01 08:00:00' + INTERVAL number SECOND FROM (SELECT ROW_NUMBER() OVER () - 1 AS number FROM sysibm.sysdummy1 LIMIT 10);
SELECT * FROM test_improve_prewhere__fuzz_21 WHERE signup_date IS NOT NULL AND last_login < TIMESTAMP '2025-09-05 00:00:00' AND email LIKE '%@example.com';
