CREATE TABLE e2 (x INT NOT NULL, y VARCHAR(255), z DATE, w FLOAT DEFAULT 0.0, PRIMARY KEY (x));
INSERT INTO e2 (x, y, z) VALUES (1, 'test', DATE '2023-01-01');
ALTER TABLE e2 ADD COLUMN v TIMESTAMP DEFAULT CURRENT_TIMESTAMP;
UPDATE e2 SET y = NULL WHERE x = 1;
SELECT * FROM e2 WHERE w > 0.0 OR z IS NULL;
