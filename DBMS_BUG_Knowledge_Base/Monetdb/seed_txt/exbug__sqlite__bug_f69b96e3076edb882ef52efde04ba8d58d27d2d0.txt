CREATE TABLE test_table(content TEXT);
INSERT INTO test_table(content) VALUES('apple'), ('banana'), ('cherry'), ('date');
CREATE TABLE orders(id SERIAL PRIMARY KEY, product TEXT NOT NULL, quantity INTEGER DEFAULT 0);
INSERT INTO orders(product, quantity) VALUES('apple', 10), ('banana', 5), ('cherry', 15);
SELECT * FROM orders WHERE product IN (SELECT content FROM test_table) ORDER BY quantity;
