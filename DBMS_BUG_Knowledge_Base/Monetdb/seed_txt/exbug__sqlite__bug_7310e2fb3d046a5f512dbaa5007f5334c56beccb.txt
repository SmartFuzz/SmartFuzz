CREATE TABLE a(id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, age INT CHECK(age >= 0));
CREATE TABLE b(id INT PRIMARY KEY, a_id INT, FOREIGN KEY(a_id) REFERENCES a(id));
CREATE TABLE c(id INT PRIMARY KEY, value DOUBLE DEFAULT 0.0);
INSERT INTO a(name, age) VALUES ('Alice', 30), ('Bob', NULL), ('Charlie', NULL);
SELECT * FROM a JOIN b ON a.id = b.a_id WHERE a.age IS NOT NULL AND a.id IN (SELECT id FROM a WHERE age IS NOT NULL);
