CREATE TABLE test_random (id INTEGER PRIMARY KEY, value DOUBLE);
INSERT INTO test_random SELECT i, RANDOM() FROM (SELECT ROW_NUMBER() OVER () AS i FROM system_range(1, 100000));
SELECT COUNT(DISTINCT value) FROM test_random;
SELECT COUNT(*) FROM (SELECT DISTINCT RANDOM() AS a FROM (SELECT ROW_NUMBER() OVER () AS i FROM system_range(1, 100000)));
SELECT COUNT(*) FROM (SELECT value FROM test_random WHERE value IS NOT NULL);
