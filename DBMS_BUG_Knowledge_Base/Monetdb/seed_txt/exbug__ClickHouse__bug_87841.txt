CREATE TABLE table_src (order_id BIGINT, random_date DATE, amount DOUBLE PRECISION);
CREATE TABLE table_dst (order_id BIGINT, random_date DATE, amount DOUBLE PRECISION) PARTITION BY MONTH(random_date);
INSERT INTO table_src SELECT number AS order_id, CURRENT_DATE - (random() % 3650) AS random_date, random() * 100 AS amount FROM SYSTEM_RANGE(1, 1000000);
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src WHERE amount > 50.0;
INSERT INTO table_dst SELECT order_id, random_date, amount FROM table_src ORDER BY MONTH(random_date);
