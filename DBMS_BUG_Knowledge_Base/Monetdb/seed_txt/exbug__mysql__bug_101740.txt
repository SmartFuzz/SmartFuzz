CREATE DATABASE rollup_test;
CREATE TABLE tmp_table_rollup (id BIGINT NOT NULL, groupKey VARCHAR(250), groupValue VARCHAR(250), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (id));
INSERT INTO tmp_table_rollup (groupKey, groupValue) VALUES ('A', 'Value1'), ('B', 'Value2'), (NULL, 'Value3'), ('C', NULL);
CREATE VIEW tmp_view_rollup AS SELECT groupKey, COUNT(id) AS groupCount FROM tmp_table_rollup GROUP BY groupKey UNION ALL SELECT NULL, COUNT(id) FROM tmp_table_rollup;
