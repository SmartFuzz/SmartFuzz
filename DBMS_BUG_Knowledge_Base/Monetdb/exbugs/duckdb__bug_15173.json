{
  "url": "https://github.com/duckdb/duckdb/issues/15173",
  "title": "Multiple in-memory duckdb instances clash when writing to temp directory",
  "description": "### What happens?\r\n\r\nMultiple in-memory duckdb instances clash when writing to the temp-directory, no matter the client.\r\nThis could be avoided by setting the temp directory manually, but I wonder whether duckdb could either create sub-directories or unique filenames to avoid this automatically.  \r\n\r\n### To Reproduce\r\n\r\nRun the following snippet in two separate Terminal or Python duckdb instances at roughly the same time.\r\n\r\n```sql\r\nSET memory_limit='1GB';\r\nCREATE OR REPLACE TABLE df AS (SELECT random() FROM range(1_000_000_000));\r\nSELECT * FROM df ORDER BY \"random()\";\r\n```\r\nOne of them crashes with\r\n```\r\nIO Error: Could not read enough bytes from file \".tmp/duckdb_temp_storage-1.tmp\": attempted to read 262144 bytes from location 818675712\r\n```\r\n\r\n\r\n### OS:\r\n\r\nLinux\r\n\r\n### DuckDB Version:\r\n\r\n1.1.3\r\n\r\n### DuckDB Client:\r\n\r\nPython & Terminal\r\n\r\n### Hardware:\r\n\r\nAMD64\r\n\r\n### Full Name:\r\n\r\nSoeren Wolfers\r\n\r\n### Affiliation:\r\n\r\nG-Research\r\n\r\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\r\n\r\nI have tested with a stable release\r\n\r\n### Did you include all relevant data sets for reproducing the issue?\r\n\r\nNot applicable - the reproduction does not require a data set\r\n\r\n### Did you include all code required to reproduce the issue?\r\n\r\n- [X] Yes, I have\r\n\r\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\r\n\r\n- [X] Yes, I have",
  "date": "2024-12-07 01:28:17",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "Monetdb",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, value VARCHAR(255), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table (id, value) VALUES (1, 'test_value_1'), (2, NULL), (3, 'test_value_3');",
    "CREATE TABLE temp_table AS SELECT id, value FROM test_table WHERE value IS NOT NULL;",
    "SELECT * FROM temp_table WHERE id IN (SELECT id FROM test_table WHERE value LIKE 'test_value_%');",
    "UPDATE test_table SET value = 'updated_value' WHERE id = 2;"
  ],
  "key_bug_features": [
    "Multiple in-memory instances clash writing to temp directory"
  ]
}