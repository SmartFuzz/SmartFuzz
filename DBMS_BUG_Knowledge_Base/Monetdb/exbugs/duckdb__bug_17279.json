{
  "url": "https://github.com/duckdb/duckdb/issues/17279",
  "title": "An unexpected behavior in a query involving CTEs, DISTINCT ON, and JOIN",
  "description": "### What happens?\n\nI encountered a strange behavior from DuckDB. Let’s say I have this table:\n\n\n```sql\nCREATE TABLE log (\nLogin text,\nTitle text,\nTS integer\n);\n\nINSERT INTO log(Login, Title, TS) VALUES\n('kroiboi', 'rat', 1),\n('missTiq', 'medic', 2),\n('kroiboi', 'conman', 3),\n('Joe', 'lordling', 4),\n('Joe', 'meatball', 5),\n('kroiboi', NULL, 100)\n;\n```\n\nI’m trying to count entries (rows) for each Login, and JOIN the latest non-NULL Title for each user (Login), according to TS — timestamps.\n\nThe following query produces the expected result in Postgres, but not in DuckDB.\n\n```sql\nWITH login_titles AS (\n  SELECT DISTINCT ON(Login) *\n  FROM log\n  WHERE Title IS NOT NULL\n  ORDER BY Login, TS DESC\n),\nq AS (\n  SELECT\n  Login,\n  count(*) AS reqs\n  FROM log\n  GROUP BY Login\n)\nSELECT q.reqs, login_titles.*\nFROM q\nJOIN login_titles\nON q.Login = login_titles.Login\nORDER BY reqs DESC\n;\n```\n\nExpected (PostgreSQL 17.4):\n```\n reqs |  login  |  title   | ts\n------+---------+----------+----\n    3 | kroiboi | conman   |  3\n    2 | Joe     | meatball |  5\n    1 | missTiq | medic    |  2\n```\n\nDuckDB:\n```\n┌───────┬─────────┬──────────┬───────┐\n│ reqs  │  Login  │  Title   │  TS   │\n│ int64 │ varchar │ varchar  │ int32 │\n├───────┼─────────┼──────────┼───────┤\n│     2 │ Joe     │ meatball │     5 │\n│     1 │ missTiq │ medic    │     2 │\n└───────┴─────────┴──────────┴───────┘\n```\n\nBoth CTEs by themselves (`q` and `login_titles`) produce comparable and expected results, both in Postgres and in DuckDB:\n\n`login_titles`:\n```\n┌─────────┬──────────┬───────┐\n│  Login  │  Title   │  TS   │\n│ varchar │ varchar  │ int32 │\n├─────────┼──────────┼───────┤\n│ Joe     │ meatball │     5 │\n│ kroiboi │ conman   │     3 │\n│ missTiq │ medic    │     2 │\n└─────────┴──────────┴───────┘\n```\n\n`q`:\n```\n┌─────────┬───────┐\n│  Login  │ reqs  │\n│ varchar │ int64 │\n├─────────┼───────┤\n│ Joe     │     2 │\n│ kroiboi │     3 │\n│ missTiq │     1 │\n└─────────┴───────┘\n```\n\nBut DuckDB drops “kroiboi” row when those are JOIN-ed. Is this a bug or am I missing something?\n\n\n\n\n\n### To Reproduce\n\n.\n\n### OS:\n\nLinux 6.12.23-1-lts\n\n### DuckDB Version:\n\nDuckDB v1.2.2 7c039464e4\n\n### DuckDB Client:\n\nduckdb CLI\n\n### Hardware:\n\n_No response_\n\n### Full Name:\n\nZach\n\n### Affiliation:\n\nunemployed hobo\n\n### What is the latest build you tested with? If possible, we recommend testing with the latest nightly build.\n\nI have tested with a stable release\n\n### Did you include all relevant data sets for reproducing the issue?\n\nYes\n\n### Did you include all code required to reproduce the issue?\n\n- [x] Yes, I have\n\n### Did you include all relevant configuration (e.g., CPU architecture, Python version, Linux distribution) to reproduce the issue?\n\n- [x] Yes, I have",
  "date": "2025-04-28 15:42:21",
  "vulnerable_version": "",
  "poc_type": "sql_text",
  "vulnerable_database": "Monetdb",
  "source_database": "duckdb",
  "bug_features_seed": [
    "CREATE TABLE log (Login text, Title text, TS integer);",
    "INSERT INTO log(Login, Title, TS) VALUES ('kroiboi', 'rat', 1), ('missTiq', 'medic', 2), ('kroiboi', 'conman', 3), ('Joe', 'lordling', 4), ('Joe', 'meatball', 5), ('kroiboi', NULL, 100);",
    "CREATE TABLE login_counts AS SELECT Login, COUNT(*) AS reqs FROM log GROUP BY Login;",
    "CREATE TABLE latest_titles AS SELECT Login, Title, TS FROM log WHERE Title IS NOT NULL ORDER BY Login, TS DESC;",
    "SELECT lc.reqs, lt.Title, lt.TS FROM login_counts lc JOIN latest_titles lt ON lc.Login = lt.Login ORDER BY lc.reqs DESC;"
  ],
  "key_bug_features": [
    "JOIN with DISTINCT ON and CTEs drops expected rows"
  ]
}