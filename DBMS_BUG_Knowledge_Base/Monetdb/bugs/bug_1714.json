{
  "url": "https://github.com/MonetDB/MonetDB/issues/1714",
  "title": "schema from declared table...",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T09:53:40Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "create table t1 (id int, age int);",
    "SELECT id, age\\r\\n                 FROM t1;"
  ],
  "key_bug_features": [
    "SELECT statement on declared table schema"
  ],
  "bug_features_seed": [
    "CREATE TABLE t2 (id SERIAL PRIMARY KEY, name VARCHAR(50), age INT CHECK (age >= 0));",
    "INSERT INTO t2 (name, age) VALUES ('Alice', 30);",
    "INSERT INTO t2 (name, age) VALUES ('Bob', NULL);",
    "SELECT id, name, age FROM t2 WHERE age IS NOT NULL;",
    "SELECT id, name FROM t2 WHERE age < (SELECT AVG(age) FROM t2 WHERE age IS NOT NULL);"
  ]
}