{
  "url": "https://github.com/MonetDB/MonetDB/issues/6141",
  "title": "Getting an error message regarding a non-GROUP-BY column rather than an unknown identifier",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T15:28:36Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE t1 (c2 INT);",
    "select count(c1) as c2 from (select count(*) AS c1, c2 from t1 group by c2) AS t2 group by c2;",
    "select count(c1_) as c2 from (select count(*) AS c1, c2 from t1 group by c2) AS t2 group by c2;"
  ],
  "key_bug_features": [
    "Error on non-GROUP-BY column in aggregate query"
  ],
  "bug_features_seed": [
    "CREATE TABLE t1 (c1 INT, c2 VARCHAR(50), c3 DATE);",
    "INSERT INTO t1 (c1, c2, c3) VALUES (1, 'test', '2023-01-01');",
    "INSERT INTO t1 (c1, c2, c3) VALUES (2, NULL, '2023-01-02');",
    "SELECT count(c1) AS c2 FROM (SELECT count(*) AS c1, c2 FROM t1 GROUP BY c2) AS t2 GROUP BY c3;",
    "SELECT count(c1_) AS c2 FROM (SELECT count(*) AS c1, c2 FROM t1 GROUP BY c2) AS t2 GROUP BY c2;"
  ]
}