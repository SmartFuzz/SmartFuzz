{
  "url": "https://github.com/MonetDB/MonetDB/issues/3388",
  "title": "case statement in \"order by\" clause doesn't work when used together with \"group by\"",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T12:35:40Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "SELECT sum(b) from a group by k order by case when sum(b) is null then 1 else 0 end,sum(b);",
    "CREATE table a (k int,b int);",
    "INSERT into a values (1,2);",
    "INSERT into a values (2,2);",
    "INSERT into a values (3,3);",
    "INSERT into a values (4,65);",
    "INSERT into a values (5,21);",
    "INSERT into a values (6,null);",
    "INSERT into a values (7,null);",
    "INSERT into a values (8,null);",
    "INSERT into a values (9,null);"
  ],
  "key_bug_features": [
    "case statement in order by with group by"
  ],
  "bug_features_seed": [
    "CREATE TABLE a (k int, b int, c varchar(10), d float CHECK (d >= 0));",
    "INSERT INTO a VALUES (1, 2, 'test1', 10.5);",
    "INSERT INTO a VALUES (2, 2, 'test2', NULL);",
    "INSERT INTO a VALUES (3, 3, 'test3', 15.0);",
    "SELECT sum(b) FROM a GROUP BY c ORDER BY CASE WHEN sum(b) IS NULL THEN 1 ELSE 0 END, sum(b);"
  ]
}