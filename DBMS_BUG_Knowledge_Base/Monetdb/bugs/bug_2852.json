{
  "url": "https://github.com/MonetDB/MonetDB/issues/2852",
  "title": "Query with CASE and nested functions results in very long execution",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T11:44:27Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE t (c INT);",
    "INSERT INTO t VALUES (0);"
  ],
  "key_bug_features": [
    "Query with CASE and nested functions"
  ],
  "bug_features_seed": [
    "CREATE TABLE users (id INT PRIMARY KEY, name VARCHAR(50), age INT CHECK (age >= 0));",
    "INSERT INTO users VALUES (1, 'Alice', 30);",
    "INSERT INTO users VALUES (2, 'Bob', NULL);",
    "SELECT CASE WHEN age IS NULL THEN 'Unknown' ELSE name END AS user_name FROM users;",
    "SELECT CASE WHEN id = 1 THEN (SELECT COUNT(*) FROM users) ELSE (SELECT AVG(age) FROM users) END AS result FROM users;"
  ]
}