{
  "url": "https://github.com/MonetDB/MonetDB/issues/3383",
  "title": "Bad performance with DISTINCT GROUP BY",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T12:35:27Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "SELECT \"a\", COUNT(DISTINCT \"b\") FROM \"test\".\"table1\" GROUP BY \"a\";",
    "SELECT \"a\", COUNT(\"b\") FROM (SELECT DISTINCT \"a\", \"b\" FROM \"test\".\"table1\") AS t GROUP BY \"a\";",
    "SELECT \"a\", COUNT(1) FROM (SELECT DISTINCT \"a\", \"b\" FROM \"test\".\"table1\") AS t GROUP BY \"a\";",
    "SELECT \"a\", COUNT(DISTINCT(\"b\")) FROM \"test\".\"table1\" GROUP BY \"a\";",
    "SELECT 0, COUNT(DISTINCT \"b\") FROM \"test\".\"table1\" WHERE \"a\" = 0\r\n     UNION SELECT 1, COUNT(DISTINCT \"b\") FROM \"test\".\"table1\" WHERE \"a\" = 1\r\n     UNION SELECT 2, COUNT(DISTINCT \"b\") FROM \"test\".\"table1\" WHERE \"a\" = 2\r\n     UNION SELECT 3, COUNT(DISTINCT \"b\") FROM \"test\".\"table1\" WHERE \"a\" = 3\r\n     UNION SELECT 4, COUNT(DISTINCT \"b\") FROM \"test\".\"table1\" WHERE \"a\" = 4\r\n     UNION SELECT 5, COUNT(DISTINCT \"b\") FROM \"test\".\"table1\" WHERE \"a\" = 5\r\n ;",
    "SELECT \"a\", COUNT(\"b\") FROM (SELECT DISTINCT \"b\",\"a\" FROM \"test\".\"table1\") AS t GROUP BY \"a\";",
    "SELECT \"a\", COUNT(\"b\") FROM (SELECT DISTINCT \"a\",\"b\" FROM \"test\".\"table1\") AS t GROUP BY \"a\";",
    "SELECT \"a\", COUNT(\"b\") FROM (SELECT DISTINCT \"b\", \"a\" FROM \"test\".\"table1\") AS t GROUP BY \"a\";",
    "select a, b from .. group by a, b) as t group by a;"
  ],
  "key_bug_features": [
    "Bad performance with DISTINCT in GROUP BY queries"
  ],
  "bug_features_seed": [
    "CREATE TABLE test.table1 (a INT, b VARCHAR(255), c DATE, d FLOAT, PRIMARY KEY (a, b));",
    "INSERT INTO test.table1 (a, b, c, d) VALUES (1, 'value1', '2023-01-01', 1.1), (1, 'value2', '2023-01-02', 1.2), (2, 'value1', '2023-01-01', 2.1), (2, NULL, '2023-01-03', 2.2);",
    "SELECT a, COUNT(DISTINCT b) FROM test.table1 GROUP BY a;",
    "SELECT a, COUNT(b) FROM (SELECT DISTINCT a, b FROM test.table1 WHERE c IS NOT NULL) AS t GROUP BY a;",
    "SELECT a, COUNT(DISTINCT b) FROM (SELECT DISTINCT a, b FROM test.table1 WHERE d > 1.0) AS t GROUP BY a;"
  ]
}