{
  "url": "https://github.com/MonetDB/MonetDB/issues/3852",
  "title": "CASE statement produces GDK error on multithreaded database: BATproject does not match always",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T13:19:28Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "create table test (x int);",
    "select 1 from test where CASE WHEN FALSE THEN TRUE ELSE (x > 0) END;",
    "select 1 from test where CASE WHEN (x > 0) THEN TRUE ELSE FALSE END;"
  ],
  "key_bug_features": [
    "CASE statement in multithreaded database queries"
  ],
  "bug_features_seed": [
    "CREATE TABLE test1 (id INT PRIMARY KEY, value VARCHAR(50) NOT NULL);",
    "INSERT INTO test1 (id, value) VALUES (1, 'A'), (2, 'B'), (3, 'C');",
    "CREATE TABLE test2 (x INT, y BOOLEAN DEFAULT FALSE);",
    "INSERT INTO test2 (x) VALUES (0), (1), (2);",
    "SELECT CASE WHEN x < 1 THEN 'Negative or Zero' ELSE 'Positive' END AS status FROM test2;"
  ]
}