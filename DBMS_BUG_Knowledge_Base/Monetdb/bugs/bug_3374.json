{
  "url": "https://github.com/MonetDB/MonetDB/issues/3374",
  "title": "UNIQUE constraint does not set tkey property on the corresponding BAT",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T12:35:03Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "create table x as select distinct name from sys.tables with data;",
    "select distinct name from sys.tables with data;"
  ],
  "key_bug_features": [
    "UNIQUE constraint fails to set tkey property"
  ],
  "bug_features_seed": [
    "CREATE TABLE users (id INT PRIMARY KEY, name VARCHAR(255) UNIQUE, age INT CHECK (age >= 0));",
    "INSERT INTO users (id, name, age) VALUES (1, 'Alice', 30);",
    "INSERT INTO users (id, name, age) VALUES (2, 'Bob', NULL);",
    "INSERT INTO users (id, name, age) VALUES (3, 'Alice', 25);",
    "SELECT DISTINCT name FROM users WHERE age IS NOT NULL;"
  ]
}