{
  "url": "https://github.com/MonetDB/MonetDB/issues/7347",
  "title": "A bug where an exception occurs even though it is a query with normal syntax (Merge Table)",
  "vulnerable_database": "monetdb",
  "date": "2022-12-22T01:17:04Z",
  "vulnerable_version": "Unknown",
  "description": "An error occurs in the case of a merge table that contain remote tables. When executing a select query with the same column name and alias",
  "poc": [
    "CREATE TABLE alias_test (dest VARCHAR(10), dep_delay INT, arr_delay INT);",
    "insert into alias_test values('AA', 20,5);",
    "insert into alias_test values('AB', 30,6);",
    "insert into alias_test values('AC', 40,7);",
    "select\r\n\tdest as category,\r\n\tdep_delay as dep_delay,\r\n\tarr_delay as arr_delay\r\nfrom alias_test  \r\norder by dest desc limit 1;",
    "select\r\n\tdest as category,\r\n\tdep_delay as dep_delay_diff,\r\n\tarr_delay as arr_delay_diff\r\nfrom alias_test  \r\norder by dest desc limit 1;"
  ],
  "key_bug_features": [
    "Exception on select query with same column name and alias"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(50), value FLOAT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table (id, name, value) VALUES (1, 'Item A', 10.5);",
    "INSERT INTO test_table (id, name, value) VALUES (2, 'Item B', NULL);",
    "SELECT id AS identifier, name AS identifier, value FROM test_table ORDER BY created_at DESC LIMIT 1;",
    "SELECT id AS id_value, name AS name_value, value AS value_value FROM test_table WHERE value IS NOT NULL;"
  ]
}