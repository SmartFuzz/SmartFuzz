{
  "url": "https://github.com/MonetDB/MonetDB/issues/3173",
  "title": "SQL queries with limit and offset clauses are unable to use bind parameters for either",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T12:14:36Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE the_table (id BIGINT);",
    "SELECT * FROM the_table LIMIT 5 OFFSET 100;",
    "SELECT * FROM the_table LIMIT ?;",
    "SELECT * FROM the_table OFFSET ?;",
    "SELECT * FROM the_table LIMIT ? OFFSET ?;",
    "SELECT * FROM the_table LIMIT 5 OFFSET ?;",
    "SELECT * FROM the_table LIMIT ? OFFSET 100;",
    "CREATE TABLE t (x BIGINT, a BIGINT);",
    "select x from t where a > 10;",
    "select x from t where a > 3;",
    "CREATE TABLE test_table(id BIGINT);",
    "SELECT * FROM test_table LIMIT ? OFFSET ?;"
  ],
  "key_bug_features": [
    "Limit and offset clauses cannot use bind parameters"
  ],
  "bug_features_seed": [
    "CREATE TABLE users (id BIGINT PRIMARY KEY, name VARCHAR(100), age INT);",
    "INSERT INTO users (id, name, age) VALUES (1, 'Alice', 30);",
    "INSERT INTO users (id, name, age) VALUES (2, 'Bob', NULL);",
    "SELECT * FROM users LIMIT ? OFFSET 0;",
    "SELECT * FROM users LIMIT 5 OFFSET ?;"
  ]
}