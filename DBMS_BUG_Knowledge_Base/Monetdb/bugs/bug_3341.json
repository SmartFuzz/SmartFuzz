{
  "url": "https://github.com/MonetDB/MonetDB/issues/3341",
  "title": "segfault caused by select rank() on a newly-loaded table",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T12:33:39Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE orders (o_custkey INT);",
    "select rank() over(partition by o_custkey) from orders;"
  ],
  "key_bug_features": [
    "select rank() on a newly-loaded table"
  ],
  "bug_features_seed": [
    "CREATE TABLE orders (o_custkey INT, o_orderdate DATE, o_amount DECIMAL(10, 2), o_status VARCHAR(20) NOT NULL DEFAULT 'pending');",
    "INSERT INTO orders (o_custkey, o_orderdate, o_amount, o_status) VALUES (1, '2023-01-01', 100.00, 'completed'), (2, NULL, 200.50, 'pending'), (NULL, '2023-01-03', 150.75, 'shipped');",
    "CREATE TABLE customers (c_custkey INT PRIMARY KEY, c_name VARCHAR(50), c_email VARCHAR(100) UNIQUE);",
    "INSERT INTO customers (c_custkey, c_name, c_email) VALUES (1, 'Alice', 'alice@example.com'), (2, 'Bob', NULL);",
    "SELECT rank() OVER (PARTITION BY o_custkey ORDER BY o_amount DESC) FROM orders;"
  ]
}