{
  "url": "https://github.com/MonetDB/MonetDB/issues/3871",
  "title": "NOT x LIKE triggers \"too many nested operators\"",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T13:20:27Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE foo (col CHAR(8));",
    "INSERT INTO foo VALUES ('bee');",
    "SELECT * FROM foo WHERE NOT col LIKE 'b%';"
  ],
  "key_bug_features": [
    "NOT x LIKE with too many nested operators"
  ],
  "bug_features_seed": [
    "CREATE TABLE bar (id INT PRIMARY KEY, name VARCHAR(20), age INT CHECK (age >= 0));",
    "INSERT INTO bar VALUES (1, 'Alice', 30);",
    "INSERT INTO bar VALUES (2, 'Bob', NULL);",
    "SELECT * FROM bar WHERE NOT name LIKE 'A%';",
    "SELECT * FROM bar WHERE NOT (age IS NULL OR name LIKE 'B%') AND id < 3;"
  ]
}