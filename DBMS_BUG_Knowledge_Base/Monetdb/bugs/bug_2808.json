{
  "url": "https://github.com/MonetDB/MonetDB/issues/2808",
  "title": "wrong results for count()/count(distinct) on the same attribute",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T11:42:20Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE tables (name VARCHAR(255));",
    "select count(name) from tables;",
    "select count(distinct name) from tables;",
    "select count(name) , count(distinct name) from tables;"
  ],
  "key_bug_features": [
    "count() and count(distinct) on same attribute"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(255), age INT, salary DECIMAL(10, 2));",
    "INSERT INTO test_table (id, name, age, salary) VALUES (1, 'Alice', 30, 50000.00), (2, 'Bob', 25, 60000.00), (3, NULL, 28, 55000.00), (4, 'Alice', 30, 50000.00);",
    "SELECT COUNT(name) FROM test_table;",
    "SELECT COUNT(DISTINCT name) FROM test_table;",
    "SELECT COUNT(name), COUNT(DISTINCT name) FROM test_table;"
  ]
}