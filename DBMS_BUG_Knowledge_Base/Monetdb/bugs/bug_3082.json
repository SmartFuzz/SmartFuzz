{
  "url": "https://github.com/MonetDB/MonetDB/issues/3082",
  "title": "Very poor performance (or hang?) of n-most-common query over varchar",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T12:10:23Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "create table foo (n varchar(64) not null);",
    "select n,count(*) as c from foo group by n order by c desc limit 10;",
    "select count(distinct n) from foo;"
  ],
  "key_bug_features": [
    "n-most-common query over varchar data type"
  ],
  "bug_features_seed": [
    "CREATE TABLE foo (n VARCHAR(64) NOT NULL, m INT DEFAULT 0, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO foo (n, m) VALUES ('apple', 1), ('banana', 2), ('apple', 3), ('orange', 1), ('banana', 1), ('kiwi', 2), (NULL, 0);",
    "SELECT n, COUNT(*) AS c FROM foo GROUP BY n ORDER BY c DESC LIMIT 10;",
    "SELECT COUNT(DISTINCT n) FROM foo WHERE n IS NOT NULL;",
    "SELECT n, COUNT(*) AS c FROM foo WHERE m > 0 GROUP BY n HAVING c > 1 ORDER BY c DESC;"
  ]
}