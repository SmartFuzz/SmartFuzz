{
  "url": "https://github.com/MonetDB/MonetDB/issues/1924",
  "title": "row_number() columns should be materialized after selection",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T10:08:09Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE a(n varchar(255));",
    "INSERT INTO a VALUES('ONE');",
    "INSERT INTO a VALUES('TWO');",
    "INSERT INTO a VALUES('THREE');",
    "CREATE VIEW b AS\r\n SELECT row_number() over () AS id, n\r\n FROM   a;",
    "select * from b;",
    "SELECT * FROM b\r\n WHERE  n = 'TWO';"
  ],
  "key_bug_features": [
    "row_number() columns not materialized after selection"
  ],
  "bug_features_seed": [
    "CREATE TABLE c(id INT PRIMARY KEY, name VARCHAR(100) NOT NULL, age INT CHECK (age >= 0));",
    "INSERT INTO c VALUES(1, 'Alice', 30);",
    "INSERT INTO c VALUES(2, 'Bob', NULL);",
    "CREATE VIEW d AS SELECT row_number() OVER (ORDER BY id) AS rn, name FROM c;",
    "SELECT * FROM d WHERE rn = 1;"
  ]
}