{
  "url": "https://github.com/MonetDB/MonetDB/issues/1886",
  "title": "Output isn't complete",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T10:06:05Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE way_nds (way INT, to_node INT);",
    "CREATE TABLE nodes_legacy (id INT);",
    "select distinct way_nds.way, way_nds.to_node from way_nds left join nodes_legacy on way_nds.to_node = nodes_legacy.id where nodes_legacy.id is NULL;"
  ],
  "key_bug_features": [
    "Left join with NULL condition on nodes_legacy"
  ],
  "bug_features_seed": [
    "CREATE TABLE way_nds (way INT NOT NULL, to_node INT DEFAULT NULL);",
    "CREATE TABLE nodes_legacy (id INT PRIMARY KEY, description VARCHAR(255));",
    "INSERT INTO way_nds (way, to_node) VALUES (1, NULL), (2, 3), (3, NULL);",
    "INSERT INTO nodes_legacy (id, description) VALUES (1, 'Node 1'), (2, 'Node 2');",
    "SELECT DISTINCT way_nds.way, way_nds.to_node FROM way_nds LEFT JOIN nodes_legacy ON way_nds.to_node = nodes_legacy.id WHERE nodes_legacy.id IS NULL;"
  ]
}