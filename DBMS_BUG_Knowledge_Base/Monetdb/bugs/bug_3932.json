{
  "url": "https://github.com/MonetDB/MonetDB/issues/3932",
  "title": "CASE expressions with constants are not evaluated correctly",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T13:23:31Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "Create table a (a varchar(5), b varchar(10));",
    "insert into a values ('12345','1234567890');",
    "SELECT CASE WHEN 1=2 THEN a ELSE b END from a;",
    "SELECT CASE WHEN 1=2 THEN a ELSE 'lala' END from a;"
  ],
  "key_bug_features": [
    "CASE expressions with constants not evaluated correctly"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_case (id INT PRIMARY KEY, name VARCHAR(50), value INT CHECK (value >= 0));",
    "INSERT INTO test_case VALUES (1, 'test1', 10), (2, 'test2', -5), (3, 'test3', 20);",
    "SELECT CASE WHEN value < 0 THEN name ELSE 'valid' END AS status FROM test_case;",
    "CREATE TABLE another_table (ref_id INT, description VARCHAR(100));",
    "INSERT INTO another_table VALUES (1, 'description1'), (2, 'description2');"
  ]
}