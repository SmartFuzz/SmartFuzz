{
  "url": "https://github.com/MonetDB/MonetDB/issues/3349",
  "title": "When selecting everything from a joined result, column contents are incorrect in one of my use cases.",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T12:33:59Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "CREATE TABLE test (x int, y int);",
    "insert into test (x, y) VALUES (1, 1);",
    "insert into test (x, y) VALUES (1, 2);",
    "SELECT *\r\n FROM (\r\n     SELECT a1.x, a1.y, a2.x, a2.y\r\n     FROM (\r\n         SELECT *\r\n         FROM test\r\n     ) AS a1 JOIN (\r\n         SELECT *\r\n         FROM test\r\n     ) AS a2 ON a1.x = a2.x\r\n ) AS t;",
    "SELECT *\r\n FROM (\r\n     SELECT a1.x AS x1, a1.y AS y1, a2.x AS x2, a2.y AS y2\r\n     FROM (\r\n         SELECT *\r\n         FROM test\r\n     ) AS a1 JOIN (\r\n         SELECT *\r\n         FROM test\r\n     ) AS a2 ON a1.x = a2.x\r\n ) AS t;"
  ],
  "key_bug_features": [
    "Incorrect column contents in joined result selection"
  ],
  "bug_features_seed": [
    "CREATE TABLE test2 (a int, b varchar(10), c float, d date);",
    "INSERT INTO test2 (a, b, c, d) VALUES (1, 'test1', 1.1, '2023-01-01');",
    "INSERT INTO test2 (a, b, c, d) VALUES (2, 'test2', 2.2, '2023-01-02');",
    "SELECT a, b, c, d FROM (SELECT a, b, c FROM test2) AS sub1 JOIN (SELECT a, c FROM test2) AS sub2 ON sub1.a = sub2.a;",
    "SELECT a, b, c, d FROM (SELECT a, b, c FROM test2 WHERE c IS NOT NULL) AS sub1 LEFT JOIN (SELECT a, d FROM test2 WHERE d IS NULL) AS sub2 ON sub1.a = sub2.a;"
  ]
}