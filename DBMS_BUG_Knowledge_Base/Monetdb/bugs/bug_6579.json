{
  "url": "https://github.com/MonetDB/MonetDB/issues/6579",
  "title": "Sqlitelogic test infinite loop while compiling SQL query",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T16:27:01Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "SELECT ALL - 70 + + - 89 - + ( SUM ( + CASE - NULLIF ( + 87, CASE CAST ( NULL AS INTEGER ) WHEN 13 / + ( 68 * - ( 64 ) )\r\n THEN + 39 WHEN 70 * - 95 THEN 60 ELSE 15 + + 25 END * 5 ) WHEN + 36 THEN NULL WHEN 24 THEN NULL ELSE 66 END ) ) AS col2;"
  ],
  "key_bug_features": [
    "Infinite loop while compiling complex SQL query"
  ],
  "bug_features_seed": [
    "CREATE TABLE test_table (id INTEGER PRIMARY KEY, value VARCHAR(255), amount DECIMAL(10, 2), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);",
    "INSERT INTO test_table (id, value, amount) VALUES (1, 'test1', 100.50), (2, 'test2', NULL), (3, 'test3', 200.00);",
    "SELECT id, value, amount, (CASE WHEN amount IS NULL THEN 'No Amount' ELSE 'Has Amount' END) AS amount_status FROM test_table WHERE id IN (SELECT id FROM test_table WHERE amount > 50);",
    "UPDATE test_table SET amount = amount + 10 WHERE value LIKE 'test%';",
    "SELECT SUM(CASE WHEN amount IS NULL THEN 0 ELSE amount END) AS total_amount FROM test_table;"
  ]
}