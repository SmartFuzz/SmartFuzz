{
  "url": "https://github.com/MonetDB/MonetDB/issues/6133",
  "title": "A crash occurs when preparing an INSERT on joined tables during the query semantic phase",
  "vulnerable_database": "monetdb",
  "date": "2020-11-30T15:28:09Z",
  "vulnerable_version": "Unknown",
  "description": "",
  "poc": [
    "DROP TABLE test105;",
    "CREATE TABLE test105 (A INT, B INT);",
    "INSERT INTO test105\r\n SELECT temp.t1, parent.B\r\n FROM ( VALUES (?, ?) ) AS temp(t1, t2)\r\n JOIN test105 parent ON (parent.A = temp.t2)\r\n ;"
  ],
  "key_bug_features": [
    "INSERT on joined tables during query semantic phase"
  ],
  "bug_features_seed": [
    "DROP TABLE IF EXISTS test201;",
    "CREATE TABLE test201 (id INT PRIMARY KEY, name VARCHAR(50), value DECIMAL(10, 2));",
    "INSERT INTO test201 (id, name, value) VALUES (1, 'Test', 10.50);",
    "INSERT INTO test201 (id, name, value) SELECT temp.id, temp.name, parent.value FROM (VALUES (2, 'Sample')) AS temp(id, name) JOIN test201 parent ON (parent.id = temp.id);",
    "INSERT INTO test201 (id, name, value) SELECT temp.id, temp.name, NULL FROM (VALUES (3, 'Example')) AS temp(id, name) LEFT JOIN test201 parent ON (parent.id = temp.id);"
  ]
}