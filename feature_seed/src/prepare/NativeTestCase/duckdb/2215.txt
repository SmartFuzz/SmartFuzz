PRAGMA force_compression='bitpacking';
PRAGMA force_bitpacking_mode='constant';
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT + 3000000000 FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT + 200000000000 FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT + 10000000000000000000 FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT + 500000000000000000000 FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT + 50000000000000000000000000000 FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT + 300000000000000000000000000000000 FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a HUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::HUGEINT + 20000000000000000000000000000000000000 FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'HUGEINT';
DROP TABLE test;