SELECT first(a), first(b), typeof(first(a)), typeof(first(b)), COUNT(*) FROM read_csv(     '{DATA_DIR}/csv/error/flush_cast.csv',     columns = {'a': 'DATE', 'b': 'VARCHAR'},     store_rejects = true,     delim = ',',     dateformat = '%d-%m-%Y');
SELECT * EXCLUDE (scan_id) FROM reject_errors order by all;