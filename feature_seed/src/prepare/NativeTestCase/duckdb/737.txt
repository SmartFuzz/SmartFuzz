PRAGMA enable_verification;
CREATE TABLE test (a INTEGER, b VARCHAR, c INTEGER);
COPY test FROM '{DATA_DIR}/csv/test/test_long_line.csv';
SELECT LENGTH(b) FROM test ORDER BY a;
SELECT SUM(a), SUM(c) FROM test;