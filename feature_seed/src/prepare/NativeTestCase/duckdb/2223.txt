PRAGMA force_compression='bitpacking';
PRAGMA force_bitpacking_mode='constant';
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT + 3000000000::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT + 200000000000::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT + 10000000000000000000::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT + 500000000000000000000::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT + 50000000000000000000000000000::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT + 300000000000000000000000000000000::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;
CREATE TABLE test (id VARCHAR, a UHUGEINT);
INSERT INTO test SELECT i::VARCHAR, i::UHUGEINT + 20000000000000000000000000000000000000::UHUGEINT FROM range(0, 16) tbl(i); INSERT INTO test SELECT i::VARCHAR, 13371337 FROM range(0, 16) tbl(i);
checkpoint;
select a from test limit 5;
SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'UHUGEINT';
DROP TABLE test;