CREATE OR REPLACE TABLE SampleTable AS SELECT DISTINCT id FROM (VALUES     ('one'),     ('two'),     ('three') ) AS t(id);
BEGIN TRANSACTION;
DELETE FROM sampletable;
DROP TABLE SampleTable;
COMMIT TRANSACTION;