SET variant_minimum_shredding_size = 0;
create table tbl (col VARIANT);
insert into tbl select "${col}"::VARIANT from test_all_types();
checkpoint;
checkpoint;
select * from tbl;
set variable my_type = ( 	select column_type from ( 		describe from test_all_types() 	) 	where 		column_type NOT ILIKE '%union%' AND 		column_type NOT ILIKE '%enum%' AND 		column_type NOT ILIKE '%map%' 	limit 1 offset ${i} );
SET force_variant_shredding = getvariable('my_type');
create or replace table "tbl2" as select * from tbl;
checkpoint;
select * from "tbl2";
SET variant_minimum_shredding_size = -1;
create or replace table "tbl2" as select * from tbl;
checkpoint;
select * from "tbl2";