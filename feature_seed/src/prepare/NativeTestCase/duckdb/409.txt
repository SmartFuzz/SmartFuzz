attach ':memory:' AS inmemorydb;
use inmemorydb;
create or replace macro m(s varchar) as s || 'c';
drop macro m;
attach '__TEST_DIR__/test_macro_type_overloads_olderdb.db' AS olderdb (storage_version 'v1.3.0');
use olderdb;
create or replace macro m(s varchar) as s || 'c';
create or replace temporary macro m(s varchar) as s || 'c';
drop macro m;
attach '__TEST_DIR__/test_macro_type_overloads_newerdb.db' AS newerdb (storage_version 'v1.4.0');
use newerdb;
create view v as select 42;
create or replace macro m(s varchar) as s || 'c';
select m('ab');
select m(s := 'ab');
select m([42]);
select m(s := [42]);
create or replace macro m(s varchar := 'cc') as s || 'c';
select m('ab');
select m(s := 'ab');
select m([42]);
select m(s := [42]);
create or replace macro m     (a tinyint) as a + 1,     (a smallint) as a + 2,     (a integer) as a + 3,     (a bigint) as a + 4,     (a hugeint) as a + 5,     (a) as a + 10;
select parameter_types[1] from duckdb_functions() where function_name = 'm' and function_type = 'macro' order by all;
select     m(0::tinyint),     m(0::smallint),     m(0::integer),     m(0::bigint),     m(0::hugeint),     m(0::double);
select     m(0::utinyint),     m(0::usmallint),     m(0::uinteger),     m(0::ubigint),     m(0::uhugeint),     m(0::float);
create or replace macro m     (i int, j) as i + j,     (i, j int) as i - j;
select m(0, 42);
select m(0, 42::float);
select m(0::float, 42);
create or replace type cool_string as varchar;
create or replace macro m(s cool_string) as s;
select m('duck');
create or replace macro m(s varchar := 42) as s;
create or replace macro m(s varchar := 42::varchar) as s;
create or replace macro m(i bigint := 42::integer) as i;
select typeof(m(42::integer));
create or replace macro m(s varchar) as table (select s || 'c');
from m('ab');
from m(s := 'ab');
from m([42]);
from m(s := [42]);
create or replace macro m(s varchar := 'cc') as table (select s || 'c');
from m('ab');
from m(s := 'ab');
from m([42]);
from m(s := [42]);
create or replace macro m     (a tinyint) as table (select a + 1),     (a smallint) as table (select a + 2),     (a integer) as table (select a + 3),     (a bigint) as table (select a + 4),     (a hugeint) as table (select a + 5),     (a) as table (select a + 10);
select parameter_types[1] from duckdb_functions() where function_name = 'm' and function_type = 'table_macro' order by all;
from m(0::tinyint) union all from m(0::smallint) union all from m(0::integer) union all from m(0::bigint) union all from m(0::hugeint) union all from m(0::double);
from m(0::utinyint) union all from m(0::usmallint) union all from m(0::uinteger) union all from m(0::ubigint) union all from m(0::uhugeint) union all from m(0::float);
create or replace macro m     (i int, j) as table (select i + j),     (i, j int) as table (select i - j);
from m(0, 42);
from m(0, 42::float);
from m(0::float, 42);
create or replace type cool_string as varchar;
create or replace macro m(s cool_string) as table (select s);
from m('duck');
create or replace macro m(s varchar := 42) as table (select s);
create or replace macro m(s varchar := 42::varchar) as table (select s);
create or replace macro m(i bigint := 42::integer) as table (select i as i);
select typeof(i) from m(42::integer);