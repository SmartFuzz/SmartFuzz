PRAGMA enable_verification;
CREATE TABLE tbl ( 	price VARCHAR, 	also_price VARCHAR AS (price) COLLATE NOCASE );
CREATE TABLE coll ( 	g1 AS (x), 	g2 AS (x), 	x VARCHAR COLLATE NOCASE );
INSERT INTO coll VALUES ('string'), ('STRING');
SELECT * FROM coll;