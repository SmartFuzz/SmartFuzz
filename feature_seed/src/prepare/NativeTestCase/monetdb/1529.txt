set optimizer = 'sequential_pipe';
explain show details select count(*) from v1;
explain show details select id       from v1 order by id;
explain show details select id , v1  from v1 order by id;
set optimizer = 'default_pipe';