create table foo (n int, m text);
insert into foo values (1, 'one'), (NULL, 'mighty null'), (2, 'two');
select * from foo where n is null;
explain show details select * from foo where n is null;
select * from foo where not n is null;
explain show details select * from foo where not n is null;
select * from foo where n is not null;
explain show details select * from foo where n is not null;
select * from foo where not n is not null;
explain show details select * from foo where not n is not null;
select * from foo where sys.isnull(n) = true;
explain show details select * from foo where sys.isnull(n) = true;
select * from foo where sys.isnull(n) = false;
explain show details select * from foo where sys.isnull(n) = false;
select * from foo where sys.isnull(n) != true;
explain show details select * from foo where sys.isnull(n) != true;
select * from foo where sys.isnull(n) != false;
explain show details select * from foo where sys.isnull(n) != false;
select * from foo where sys.isnotnull(n) = true;
explain show details select * from foo where sys.isnotnull(n) = true;
select * from foo where sys.isnotnull(n) = false;
explain show details select * from foo where sys.isnotnull(n) = false;
select * from foo where sys.isnotnull(n) != true;
explain show details select * from foo where sys.isnotnull(n) != true;
select * from foo where sys.isnotnull(n) != false;
explain show details select * from foo where sys.isnotnull(n) != false;