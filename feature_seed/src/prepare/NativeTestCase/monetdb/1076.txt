CREATE SCHEMA "ttt";
CREATE STREAM TABLE "ttt"."strt" ( "id" INTEGER       NOT NULL, "nm" VARCHAR(123)  NOT NULL, CONSTRAINT "strt_id_pkey" PRIMARY KEY ("id") );
CREATE merge TABLE "ttt"."strt" ( "id" INTEGER       NOT NULL, "nm" VARCHAR(123)  NOT NULL, CONSTRAINT "strt_id_pkey" PRIMARY KEY ("id") );
analyze "ttt"."strt";
select (count(*) > 0) as has_rows from statistics;
CREATE TABLE "ttt"."a"("id" INTEGER);
analyze ttt;
select (count(*) > 0) as has_rows from statistics;
drop table "ttt"."strt";
analyze ttt;
select (count(*) > 0) as has_rows from statistics;
drop schema "ttt" cascade;