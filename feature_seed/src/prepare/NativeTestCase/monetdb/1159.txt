START TRANSACTION;
CREATE FUNCTION pyfunc(i integer) returns table (pyi integer) language P { x = range(1, i + 1) return([x]) };
CREATE FUNCTION ups(i integer) returns table (i integer) BEGIN RETURN SELECT pyi FROM pyfunc(i); END;
COMMIT;
select i from ups(1);
select i from ups(1);
select i from ups(1);
DROP FUNCTION ups;
DROP FUNCTION pyfunc;