CREATE MERGE TABLE mysensor( sensor VARCHAR(64) NOT NULL, val INTEGER NOT NULL, exponent INTEGER NOT NULL, CONSTRAINT mysensor_pk PRIMARY KEY(sensor) ) PARTITION BY RANGE ON (sensor);
CREATE TABLE mysensor_ac( sensor VARCHAR(64) NOT NULL, val INTEGER NOT NULL, exponent INTEGER NOT NULL, CONSTRAINT mysensor_ac_pk PRIMARY KEY(sensor) );
ALTER TABLE mysensor ADD TABLE mysensor_ac AS PARTITION FROM 'a' TO 'c';
INSERT INTO mysensor(sensor, val, exponent) VALUES('a', 123, -1);
INSERT INTO mysensor_ac(sensor, val, exponent) VALUES('a', 123, -1);
ALTER TABLE mysensor DROP TABLE mysensor_ac;
DROP TABLE mysensor;
DROP TABLE mysensor_ac;