set optimizer = 'sequential_pipe';
explain physical select count(*) from v2;
explain physical select id       from v2 order by id;
explain physical select id , v2  from v2 order by id;
set optimizer = 'default_pipe';