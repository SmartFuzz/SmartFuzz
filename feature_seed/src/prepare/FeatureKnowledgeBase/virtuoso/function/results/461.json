{
    "HTML": [
        "https://docs.openlinksw.com/virtuoso/fn_exec/"
    ],
    "Title": [
        "exec"
    ],
    "Feature": [
        "exec(str, state, message, params, maxrows, metadata, rows, cursor_handle)"
    ],
    "Description": [
        "dynamic execution of SQL returning state and result set",
        "This function provides dynamic SQL capabilities in Virtuoso PL.\n    The first argument is an arbitrary SQL statement, which may contain\n    parameter placeholders. The function returns as output parameters a\n    SQL state, error message, column metadata and result set rows if\n    the statement is a select.",
        "A stored procedure can be invoked by exec but a procedure's\n    result set will not be received in the rows output parameter but rather\n    sent to the client."
    ],
    "Examples": [
        "create procedure tb_is_empty (in tb varchar)\n{\n  declare state, msg, descs, rows any;\n  state := '00000';\n  exec (sprintf ('select 1 from %s', tb), state,\n    msg, vector (), 1, descs, rows);\n  if (state <> '00000')\n    signal (state, msg);\n  if (length (rows) = 0)\n    return 1;\n  else\n    return 0;\n}"
    ],
    "Category": [
        "SQL function"
    ]
}