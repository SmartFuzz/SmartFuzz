{
    "HTML": [
        "https://docs.openlinksw.com/virtuoso/fn_http_request_header/"
    ],
    "Title": [
        "http_request_header"
    ],
    "Feature": [
        "anyhttp_request_header(array, field_name, attr-name, default_value)"
    ],
    "Description": [
        "returns array of HTTP request header lines",
        "This function provides access to the HTTP request header lines."
    ],
    "Examples": [
        "<p>Your request follows</p>\n  <?vsp\n    declare header any;\n    declare ix, len integer;\n    declare host varchar;\n    header := http_request_header ();\n    len := length (header);\n    ix := 0;\n    while (ix < len)\n    {\n      http (aref (header, ix));\n\t\t  ix := ix + 1;\n    }\n    host := http_request_header(lines, 'Host', null, '*** NO HOST IN REQUEST ***')\n  ?>"
    ],
    "Category": [
        "Web & Internet function"
    ]
}