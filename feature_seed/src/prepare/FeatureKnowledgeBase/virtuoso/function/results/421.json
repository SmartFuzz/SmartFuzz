{
    "HTML": [
        "https://docs.openlinksw.com/virtuoso/fn_group_digest/"
    ],
    "Title": [
        "DB.DBA.GROUP_DIGEST"
    ],
    "Feature": [
        "DB.DBA.GROUP_DIGEST(token, delim, maxlen, mode)"
    ],
    "Description": [
        "returns an arbitrary value from the multiset passed to it.",
        "DB.DBA.GROUP_DIGEST is aggregate function that performs a string concatenation across\n    \tthe values of an expression with a group. The order of the strings is not specified.\n    \tThe separator character used in the concatenation may be given with the scalar\n    \targument delimiter. The function is an extended version of sql:GROUP_CONCAT(), with\n    \ttwo more arguments: maxlen and mode.",
        "Note: the \"sql:\" prefix is mandatory when this aggregate is used in SPARQL queries.\n    \tIn SQL queries the prefix is \"DB.DBA\"."
    ],
    "Examples": [
        "SQL>SPARQL\nSELECT ?view (sql:GROUP_DIGEST (?path, ' ', 1000, 1)) as ?path_list\n    (sql:GROUP_DIGEST (?asset, ' ', 1000, 1)) as ?asset_list\nFROM <http://mygraph.com>\nWHERE\n  {\n    ?view <viewPath> ?path ;\n      <viewContent> ?asset ;\n      <viewType>  'phyview'.\n  };"
    ],
    "Category": [
        "RDF data function"
    ]
}