{
    "HTML": [
        "https://www.monetdb.org/documentation-Aug2024/user-guide/sql-summary/"
    ],
    "Title": [
        "CREATE IMPRINTS INDEX"
    ],
    "Feature": [
        "CREATE IMPRINTS INDEX"
    ],
    "Description": [
        "On numerical columns it creates an index which stores meta data (min, max,\nnulls) on segments of column data. An imprint is used during query evaluation to\nlimit data access and thus minimize memory traffic. It can speedup queries which\nhave a column selection condition (such as: AGE IS NULL OR AGE BETWEEN 25 AND\n65). The storage overhead is just a few percent over the size of the columns\nbeing indexed.",
        "On string columns it creates an index that speeds up LIKE queries by using a\nfast pre-filtering algorithm. It can only be created on read only tables (use\nthe syntax ALTER TABLE foo SET READ ONLY to mark table foo as read only),\nbecause the creation operation is expensive.",
        "Example:",
        "Note : This command is MonetDB specific.",
        "The imprints index is a new type of experimental column index.",
        "Limitations are: Only 1 column can be indexed per index. The index name must be\nunique within the schema of the table. You can not specify the schema for the\nindex. It will be placed in the same schema as the table. For string columns the\nindex can only be created on read only tables.",
        "Warning : The index is not maintained automatically and will become inactive\nafter inserts, deletes or updates are done on the column data. An imprints index\nis removed using the ordinary DROP INDEX statement. There is no DROP IMPRINTS\nINDEX statement.",
        "For details see: IndexDefinitions",
        "See also: CREATE ORDERED INDEX , CREATE INDEX , COMMENT ON INDEX , DROP INDEX .",
        "Associated system table: sys.idxs , sys.storage ."
    ],
    "Examples": [
        "CREATE IMPRINTS INDEX\nindex_name\nON [\nschema_name\n'.' ]\ntable_name\n'('\ncolumn_name\n')'",
        "CREATE IMPRINTS INDEX my_impr_idx ON myschema.mytable ( my_num_column );"
    ],
    "Category": [
        "MonetDB SQL Statement"
    ]
}