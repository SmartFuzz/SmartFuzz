{
    "feature_type": "operator",
    "feature_name": "a - b",
    "sql_array": [
        "CREATE TABLE test_table (a Int32, b Int32, result Int32) ENGINE = Memory;",
        "INSERT INTO test_table (a, b) VALUES (10, 5);",
        "UPDATE test_table SET result = a - b WHERE a > b;",
        "SELECT a, b, result FROM test_table;",
        "SELECT a, b, (a - b) AS difference FROM test_table WHERE difference < 5;"
    ]
}