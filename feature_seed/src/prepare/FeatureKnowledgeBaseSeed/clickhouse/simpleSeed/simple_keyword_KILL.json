{
    "feature_type": "keyword",
    "feature_name": "KILL",
    "sql_array": [
        "CREATE TABLE test_table (id UInt32, name String) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_table VALUES (1, 'test1'), (2, 'test2');",
        "SELECT * FROM test_table WHERE id = 1;",
        "KILL QUERY WHERE query_id IN (SELECT query_id FROM system.processes WHERE elapsed >= 30);",
        "KILL MUTATION WHERE mutation_id IN (SELECT mutation_id FROM system.mutations WHERE is_done = 0);"
    ]
}