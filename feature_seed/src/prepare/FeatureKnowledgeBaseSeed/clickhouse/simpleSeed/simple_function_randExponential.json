{
    "feature_type": "function",
    "feature_name": "randExponential",
    "sql_array": [
        "CREATE TABLE test_exponential (value Float64) ENGINE = Memory;",
        "INSERT INTO test_exponential VALUES (randExponential(0.5)), (randExponential(1.0)), (randExponential(1.5));",
        "UPDATE test_exponential SET value = randExponential(0.2) WHERE value < 1.0;",
        "SELECT value FROM test_exponential WHERE value > randExponential(0.3);",
        "SELECT AVG(value) AS avg_value FROM test_exponential WHERE value < randExponential(0.4) GROUP BY value;"
    ]
}