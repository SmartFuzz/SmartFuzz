{
    "feature_type": "function",
    "feature_name": "URLHash(url[, N])",
    "sql_array": [
        "CREATE TABLE url_hash_test (url String, hash UInt64) ENGINE = Memory;",
        "INSERT INTO url_hash_test (url, hash) VALUES ('https://example.com/path/to/resource', URLHash('https://example.com/path/to/resource'));",
        "INSERT INTO url_hash_test (url, hash) VALUES ('https://example.com/path/to/resource/', URLHash('https://example.com/path/to/resource/'));",
        "SELECT url, URLHash(url) AS hash FROM url_hash_test WHERE hash = URLHash('https://example.com/path/to/resource');",
        "UPDATE url_hash_test SET hash = URLHash(url) WHERE url LIKE 'https://example.com/%';"
    ]
}