{
    "feature_type": "function",
    "feature_name": "filesystemUnreserved",
    "sql_array": [
        "CREATE TABLE filesystem_test (id UInt32, free_space UInt64) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO filesystem_test VALUES (1, filesystemUnreserved());",
        "SELECT id, formatReadableSize(free_space) AS readable_free_space FROM filesystem_test;",
        "UPDATE filesystem_test SET free_space = filesystemUnreserved() WHERE id = 1;",
        "SELECT COUNT(*) FROM filesystem_test WHERE free_space > 0;"
    ]
}