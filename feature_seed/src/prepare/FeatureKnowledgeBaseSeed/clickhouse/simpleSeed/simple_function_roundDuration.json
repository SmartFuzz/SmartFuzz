{
    "feature_type": "function",
    "feature_name": "roundDuration",
    "sql_array": [
        "CREATE TABLE test_round_duration (id UInt32, duration Float) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_round_duration VALUES (1, 0.5), (2, 9.9), (3, 30.5), (4, 150.2), (5, 3600.8);",
        "UPDATE test_round_duration SET duration = roundDuration(duration) WHERE id IN (1, 3, 5);",
        "SELECT *, roundDuration(duration) AS rounded_duration FROM test_round_duration WHERE duration > 0;",
        "DELETE FROM test_round_duration WHERE roundDuration(duration) = 1800;"
    ]
}