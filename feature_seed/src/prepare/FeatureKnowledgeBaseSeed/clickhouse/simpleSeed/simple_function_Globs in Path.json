{
    "feature_type": "function",
    "feature_name": "Globs in Path",
    "sql_array": [
        "CREATE TABLE test_files (id UInt32, file_path String) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_files VALUES (1, '/data/*.csv');",
        "SELECT * FROM test_files WHERE file_path LIKE '/data/%';",
        "UPDATE test_files SET file_path = '/data/new_folder/*.txt' WHERE id = 1;",
        "SELECT GlobsInPath(file_path) FROM test_files;"
    ]
}