{
    "feature_type": "function",
    "feature_name": "quantileExactExclusive",
    "sql_array": [
        "CREATE TABLE test_data AS SELECT number AS x FROM numbers(1000);",
        "INSERT INTO test_data VALUES (1), (2), (3), (4), (5), (6);",
        "UPDATE test_data SET x = x + 10 WHERE x < 5;",
        "SELECT quantileExactExclusive(0.25)(x) FROM test_data;",
        "SELECT x, quantileExactExclusive(0.75)(x) FROM test_data GROUP BY x;"
    ]
}