{
    "feature_type": "function",
    "feature_name": "generateSnowflakeID",
    "sql_array": [
        "CREATE TABLE test_snowflake (id UInt64) ENGINE = Memory;",
        "INSERT INTO test_snowflake SELECT generateSnowflakeID();",
        "INSERT INTO test_snowflake SELECT generateSnowflakeID(1);",
        "SELECT id, generateSnowflakeID(2) AS new_id FROM test_snowflake;",
        "UPDATE test_snowflake SET id = generateSnowflakeID() WHERE id < 500;"
    ]
}