{
    "feature_type": "function",
    "feature_name": "aggThrow",
    "sql_array": [
        "CREATE TABLE test_aggThrow (id Int32, value Float64) ENGINE = Memory;",
        "INSERT INTO test_aggThrow VALUES (1, 1.5), (2, 2.5), (3, 3.5);",
        "SELECT id, aggThrow(0.5) FROM test_aggThrow GROUP BY id;",
        "UPDATE test_aggThrow SET value = aggThrow(0.3) WHERE id = 1;",
        "SELECT value, aggThrow(0.1) FROM test_aggThrow WHERE value > 2.0 GROUP BY value;"
    ]
}