{
    "feature_type": "function",
    "feature_name": "groupBitmap",
    "sql_array": [
        "CREATE TABLE users (UserID UInt32, UserName String) ENGINE = MergeTree() ORDER BY UserID;",
        "INSERT INTO users (UserID, UserName) VALUES (1, 'Alice'), (2, 'Bob'), (3, 'Charlie');",
        "SELECT groupBitmap(UserID) as user_bitmap FROM users;",
        "UPDATE users SET UserName = 'Alicia' WHERE UserID = 1;",
        "SELECT groupBitmap(UserID) as num_users FROM users WHERE UserID > 1;"
    ]
}