{
    "feature_type": "function",
    "feature_name": "ngramMinHashArgCaseInsensitive",
    "sql_array": [
        "CREATE TABLE test_strings (id UInt32, sample_string String) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_strings VALUES (1, 'HelloWorld'), (2, 'ClickHouse'), (3, 'FuzzTesting');",
        "UPDATE test_strings SET sample_string = 'UpdatedString' WHERE id = 2;",
        "SELECT ngramMinHashArgCaseInsensitive(sample_string, 4, 5) FROM test_strings;",
        "SELECT id, ngramMinHashArgCaseInsensitive(sample_string) FROM test_strings WHERE id < 3;"
    ]
}