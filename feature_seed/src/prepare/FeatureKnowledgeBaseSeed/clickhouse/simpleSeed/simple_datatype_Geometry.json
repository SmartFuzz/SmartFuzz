{
    "feature_type": "datatype",
    "feature_name": "Geometry",
    "sql_array": [
        "CREATE TABLE IF NOT EXISTS spatial_data (geom Geometry) ENGINE = Memory();",
        "INSERT INTO spatial_data VALUES (readWkt('POINT(1 1)'));",
        "INSERT INTO spatial_data VALUES (readWkt('LINESTRING(0 0, 1 1, 2 2)'));",
        "SELECT geom, area(geom) AS area_value FROM spatial_data WHERE geom IS NOT NULL;",
        "UPDATE spatial_data SET geom = readWkt('POLYGON((0 0, 5 0, 5 5, 0 5, 0 0))') WHERE geom = readWkt('POINT(1 1)';"
    ]
}