{
    "feature_type": "function",
    "feature_name": "splitByChar",
    "sql_array": [
        "CREATE TABLE test_split (id Int32, data String);",
        "INSERT INTO test_split VALUES (1, 'apple,orange,banana');",
        "SELECT splitByChar(',', data) FROM test_split WHERE id = 1;",
        "UPDATE test_split SET data = 'grape,melon' WHERE id = 1;",
        "SELECT splitByChar(',', data, 1) FROM test_split WHERE id = 1;"
    ]
}