{
    "feature_type": "function",
    "feature_name": "filesystemAvailable",
    "sql_array": [
        "CREATE TABLE test_space (id UInt32, space_available UInt64) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_space VALUES (1, filesystemAvailable());",
        "UPDATE test_space SET space_available = filesystemAvailable() WHERE id = 1;",
        "SELECT id, formatReadableSize(space_available) AS 'Readable Space' FROM test_space;",
        "SELECT filesystemAvailable() AS 'Free Space Check';"
    ]
}