{
    "feature_type": "function",
    "feature_name": "isIPv4String",
    "sql_array": [
        "CREATE TABLE ip_addresses (addr String);",
        "INSERT INTO ip_addresses (addr) VALUES ('192.168.1.1'), ('::1'), ('255.255.255.255');",
        "SELECT addr, isIPv4String(addr) AS is_ipv4 FROM ip_addresses;",
        "UPDATE ip_addresses SET addr = '10.0.0.1' WHERE isIPv4String(addr) = 1;",
        "SELECT addr FROM ip_addresses WHERE isIPv4String(addr) = 1 AND addr LIKE '%.1';"
    ]
}