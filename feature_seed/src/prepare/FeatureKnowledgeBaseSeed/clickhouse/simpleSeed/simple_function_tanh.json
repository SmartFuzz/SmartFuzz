{
    "feature_type": "function",
    "feature_name": "tanh",
    "sql_array": [
        "CREATE TABLE test_tanh (value Float32);",
        "INSERT INTO test_tanh VALUES (0), (1), (-1), (2), (-2);",
        "SELECT value, tanh(value) AS tanh_value FROM test_tanh;",
        "UPDATE test_tanh SET value = tanh(value) WHERE value > 0;",
        "SELECT value, tanh(value) AS tanh_value FROM test_tanh WHERE tanh(value) > 0.5;"
    ]
}