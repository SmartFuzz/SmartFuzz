{
    "feature_type": "operator",
    "feature_name": "[x1, ...]",
    "sql_array": [
        "CREATE TABLE test_array (id Int32, values Array(Int32)) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_array VALUES (1, [1, 2, 3]), (2, [4, 5, 6]);",
        "UPDATE test_array SET values = [7, 8, 9] WHERE id = 1;",
        "SELECT id, arrayJoin(values) AS value FROM test_array;",
        "SELECT id, arrayLength(values) AS length FROM test_array WHERE arrayHas(values, 5);"
    ]
}