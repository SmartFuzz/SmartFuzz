{
    "feature_type": "function",
    "feature_name": "maxIntersections",
    "sql_array": [
        "CREATE TABLE event_intervals (start UInt32, end UInt32) ENGINE = MergeTree() ORDER BY tuple();",
        "INSERT INTO event_intervals VALUES (10, 20), (15, 25), (17, 30), (5, 15);",
        "SELECT maxIntersections(start, end) FROM event_intervals;",
        "INSERT INTO event_intervals VALUES (12, 18), (14, 24);",
        "SELECT maxIntersections(start, end) FROM event_intervals WHERE start > 0;"
    ]
}