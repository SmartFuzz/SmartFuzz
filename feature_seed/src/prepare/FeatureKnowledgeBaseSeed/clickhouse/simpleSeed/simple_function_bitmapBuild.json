{
    "feature_type": "function",
    "feature_name": "bitmapBuild",
    "sql_array": [
        "CREATE TABLE test_bitmap (id UInt32, values Array(UInt32));",
        "INSERT INTO test_bitmap (id, values) VALUES (1, [1, 2, 3]), (2, [4, 5, 6]);",
        "UPDATE test_bitmap SET values = [7, 8, 9] WHERE id = 1;",
        "SELECT id, bitmapBuild(values) AS bitmap FROM test_bitmap;",
        "SELECT id, bitmapBuild(arrayJoin(values)) AS bitmap FROM test_bitmap GROUP BY id;"
    ]
}