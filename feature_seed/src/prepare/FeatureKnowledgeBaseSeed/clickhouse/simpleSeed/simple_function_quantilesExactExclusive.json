{
    "feature_type": "function",
    "feature_name": "quantilesExactExclusive",
    "sql_array": [
        "CREATE TABLE test_quantiles AS SELECT number AS x FROM numbers(1000);",
        "INSERT INTO test_quantiles VALUES (1), (2), (3), (4), (5);",
        "SELECT quantilesExactExclusive(0.1, 0.5, 0.9)(x) FROM test_quantiles;",
        "UPDATE test_quantiles SET x = x * 2 WHERE x < 3;",
        "SELECT quantilesExactExclusive(0.25, 0.75)(x) FROM test_quantiles WHERE x > 0;"
    ]
}