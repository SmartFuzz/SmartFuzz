{
    "feature_type": "keyword",
    "feature_name": "ALL",
    "sql_array": [
        "CREATE TABLE test_all (id Int32, value String);",
        "INSERT INTO test_all VALUES (1, 'foo'), (2, 'bar'), (1, 'baz');",
        "SELECT ALL value FROM test_all WHERE id = 1;",
        "UPDATE test_all SET value = 'updated' WHERE id = 2;",
        "SELECT id, COUNT(value) AS count_value FROM test_all GROUP BY id HAVING count_value > 1;"
    ]
}