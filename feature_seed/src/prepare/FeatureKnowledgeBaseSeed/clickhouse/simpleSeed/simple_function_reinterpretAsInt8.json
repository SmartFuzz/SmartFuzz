{
    "feature_type": "function",
    "feature_name": "reinterpretAsInt8",
    "sql_array": [
        "CREATE TABLE test_table (id UInt8, value String);",
        "INSERT INTO test_table VALUES (1, 'A'), (2, 'B');",
        "UPDATE test_table SET value = reinterpretAsInt8(id) WHERE id = 1;",
        "SELECT id, value, reinterpretAsInt8(id) AS reinterpreted_value FROM test_table;",
        "SELECT reinterpretAsInt8(toUInt16(300)) AS negative_value;"
    ]
}