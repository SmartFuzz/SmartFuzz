{
    "feature_type": "keyword",
    "feature_name": "STATISTICS",
    "sql_array": [
        "CREATE TABLE test_table (id UInt32, value Float64) ENGINE = MergeTree() PRIMARY KEY id;",
        "ALTER TABLE test_table ADD STATISTICS IF NOT EXISTS (value) TYPE (quantile(0.5), count());",
        "INSERT INTO test_table VALUES (1, 10.0), (2, 20.0), (3, 30.0);",
        "ALTER TABLE test_table MATERIALIZE STATISTICS (value);",
        "SELECT id, value, statistics_type from system.columns WHERE table = 'test_table';"
    ]
}