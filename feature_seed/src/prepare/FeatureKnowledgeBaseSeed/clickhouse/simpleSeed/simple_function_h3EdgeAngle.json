{
    "feature_type": "function",
    "feature_name": "h3EdgeAngle",
    "sql_array": [
        "CREATE TABLE h3_test (id UInt32, resolution UInt8) ENGINE = Memory;",
        "INSERT INTO h3_test (id, resolution) VALUES (1, 10);",
        "UPDATE h3_test SET resolution = 5 WHERE id = 1;",
        "SELECT id, h3EdgeAngle(resolution) AS edgeAngle FROM h3_test;",
        "SELECT id, h3EdgeAngle(resolution + 1) AS adjustedEdgeAngle FROM h3_test WHERE resolution > 0;"
    ]
}