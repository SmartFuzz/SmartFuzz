{
    "feature_type": "function",
    "feature_name": "formatRowNoNewline",
    "sql_array": [
        "CREATE TABLE test_table (number Int32, status String);",
        "INSERT INTO test_table (number, status) VALUES (1, 'good'), (2, 'bad'), (3, 'neutral');",
        "SELECT formatRowNoNewline('CSV', number, status) FROM test_table WHERE number > 1;",
        "UPDATE test_table SET status = 'excellent' WHERE number = 1;",
        "SELECT formatRowNoNewline('TSV', number, status) FROM test_table;"
    ]
}