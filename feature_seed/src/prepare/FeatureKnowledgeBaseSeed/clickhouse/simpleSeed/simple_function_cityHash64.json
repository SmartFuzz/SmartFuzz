{
    "feature_type": "function",
    "feature_name": "cityHash64",
    "sql_array": [
        "CREATE TABLE test_hash (id UInt64, value String) ENGINE = Memory;",
        "INSERT INTO test_hash VALUES (1, 'hello'), (2, 'world'), (3, 'clickhouse');",
        "UPDATE test_hash SET value = cityHash64(value) WHERE id = 1;",
        "SELECT cityHash64(value) AS hash_value FROM test_hash;",
        "SELECT id, cityHash64(value) AS hash_value FROM test_hash ORDER BY hash_value;"
    ]
}