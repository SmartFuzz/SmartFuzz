{
    "feature_type": "function",
    "feature_name": "argMin",
    "sql_array": [
        "CREATE TABLE test_table (user String, salary Int64) ENGINE = Memory;",
        "INSERT INTO test_table VALUES ('Alice', 3000), ('Bob', 2000), ('Charlie', 2500), ('David', 3000), ('Eve', NULL);",
        "SELECT argMin(user, salary) FROM test_table;",
        "SELECT argMin(user, (salary, user)) FROM test_table WHERE salary IS NOT NULL;",
        "SELECT argMin(tuple(user, salary), salary) as user_salary_min FROM test_table;"
    ]
}