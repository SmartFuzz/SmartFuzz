{
    "feature_type": "function",
    "feature_name": "lessOrEquals, <= operator",
    "sql_array": [
        "CREATE TABLE test_less_than_equals (id UInt32, value UInt32) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_less_than_equals (id, value) VALUES (1, 10), (2, 20), (3, 30);",
        "UPDATE test_less_than_equals SET value = 15 WHERE lessOrEquals(value, 20);",
        "SELECT * FROM test_less_than_equals WHERE lessOrEquals(value, 25);",
        "DELETE FROM test_less_than_equals WHERE lessOrEquals(id, 2);"
    ]
}