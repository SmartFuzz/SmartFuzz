{
    "feature_type": "function",
    "feature_name": "Parameters",
    "sql_array": [
        "CREATE TABLE memory_profiling (id UInt64, traces Array(UInt64)) ENGINE = Memory;",
        "INSERT INTO memory_profiling (id, traces) VALUES (1, [100, 200, 300]);",
        "SELECT Parameters(traces) FROM memory_profiling;",
        "UPDATE memory_profiling SET traces = [400, 500] WHERE id = 1;",
        "SELECT id, Parameters(traces) FROM memory_profiling WHERE traces.size > 1;"
    ]
}