{
    "feature_type": "function",
    "feature_name": "kurtPop",
    "sql_array": [
        "CREATE TABLE test_series (id UInt64, value Float64) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_series (id, value) VALUES (1, 2.5), (2, 3.0), (3, 2.8), (4, 3.2);",
        "UPDATE test_series SET value = value + 1 WHERE id = 2;",
        "SELECT kurtPop(value) FROM test_series;",
        "SELECT id, kurtPop(value) AS kurtosis FROM test_series GROUP BY id;"
    ]
}