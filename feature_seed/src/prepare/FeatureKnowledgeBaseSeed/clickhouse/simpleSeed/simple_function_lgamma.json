{
    "feature_type": "function",
    "feature_name": "lgamma",
    "sql_array": [
        "CREATE TABLE test_lgamma (value Float64);",
        "INSERT INTO test_lgamma (value) VALUES (1.5);",
        "UPDATE test_lgamma SET value = lgamma(value) WHERE value > 1;",
        "SELECT lgamma(value) AS gamma_log FROM test_lgamma;",
        "SELECT COUNT(*) FROM test_lgamma WHERE lgamma(value) < 0;"
    ]
}