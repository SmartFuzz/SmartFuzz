{
    "feature_type": "function",
    "feature_name": "roundToExp2",
    "sql_array": [
        "CREATE TABLE test_rounding (num Float64);",
        "INSERT INTO test_rounding (num) VALUES (0), (1), (2.5), (3.9), (8);",
        "SELECT num, roundToExp2(num) AS rounded FROM test_rounding;",
        "UPDATE test_rounding SET num = roundToExp2(num) WHERE num >= 1;",
        "SELECT *, roundToExp2(num) FROM test_rounding WHERE num < 10;"
    ]
}