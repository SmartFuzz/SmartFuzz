{
    "feature_type": "function",
    "feature_name": "loop",
    "sql_array": [
        "CREATE TABLE test_table (number UInt64) ENGINE = MergeTree() ORDER BY number;",
        "INSERT INTO test_table SELECT number FROM loop(numbers(5)) LIMIT 5;",
        "SELECT * FROM loop(test_table) LIMIT 10;",
        "UPDATE test_table SET number = number + 1 WHERE number < 3;",
        "SELECT * FROM loop(test_table) WHERE number > 2;"
    ]
}