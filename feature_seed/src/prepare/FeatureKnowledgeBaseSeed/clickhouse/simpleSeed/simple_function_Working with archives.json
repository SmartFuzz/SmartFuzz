{
    "feature_type": "function",
    "feature_name": "Working with archives",
    "sql_array": [
        "CREATE TABLE archive_data (id UInt32, domain String) ENGINE = Memory;",
        "INSERT INTO archive_data SELECT * FROM s3('https://s3-us-west-1.amazonaws.com/umbrella-static/top-1m-2018-01-1{0..2}.csv.zip :: *.csv');",
        "SELECT COUNT(*) FROM archive_data WHERE domain LIKE '%example.com';",
        "UPDATE archive_data SET domain = 'updated.com' WHERE domain LIKE '%old.com';",
        "SELECT domain FROM archive_data WHERE id IN (SELECT id FROM archive_data WHERE domain LIKE '%test.com');"
    ]
}