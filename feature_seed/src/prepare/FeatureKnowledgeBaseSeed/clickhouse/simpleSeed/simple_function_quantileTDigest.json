{
    "feature_type": "function",
    "feature_name": "quantileTDigest",
    "sql_array": [
        "CREATE TABLE test_data (id UInt32, value Float64) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_data (id, value) VALUES (1, 10.0), (2, 20.0), (3, 30.0);",
        "SELECT quantileTDigest(0.5)(value) FROM test_data;",
        "UPDATE test_data SET value = value * 1.1 WHERE id = 2;",
        "SELECT quantileTDigest(0.75)(value) FROM test_data WHERE id > 1;"
    ]
}