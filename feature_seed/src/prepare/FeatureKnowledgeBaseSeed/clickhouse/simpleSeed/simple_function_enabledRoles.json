{
    "feature_type": "function",
    "feature_name": "enabledRoles",
    "sql_array": [
        "CREATE TABLE test_roles (id UInt32, role_name String) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_roles (id, role_name) VALUES (1, 'admin'), (2, 'user');",
        "SELECT role_name FROM test_roles WHERE id = 1;",
        "SELECT COUNT(*) FROM test_roles WHERE role_name IN (SELECT arrayJoin(enabledRoles()));",
        "UPDATE test_roles SET role_name = 'superuser' WHERE id = 2;"
    ]
}