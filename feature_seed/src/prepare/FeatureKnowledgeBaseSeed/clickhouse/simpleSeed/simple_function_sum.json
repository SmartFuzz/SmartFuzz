{
    "feature_type": "function",
    "feature_name": "sum",
    "sql_array": [
        "CREATE TABLE department(`id` UInt32, `name` String, `budget` UInt32) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO department VALUES (1, 'HR', 50000), (2, 'Engineering', 150000), (3, 'Sales', 75000);",
        "SELECT sum(budget) AS total_budget FROM department;",
        "UPDATE department SET budget = budget + 10000 WHERE id = 1;",
        "SELECT name, sum(budget) OVER () AS overall_budget FROM department;"
    ]
}