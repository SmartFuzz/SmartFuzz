{
    "feature_type": "function",
    "feature_name": "version",
    "sql_array": [
        "CREATE TABLE test_version (id Int32, version_info String) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_version (id, version_info) VALUES (1, version());",
        "UPDATE test_version SET version_info = version() WHERE id = 1;",
        "SELECT id, version_info FROM test_version;",
        "SELECT version() AS current_version, COUNT(*) FROM test_version GROUP BY current_version;"
    ]
}