{
    "feature_type": "function",
    "feature_name": "rightPad",
    "sql_array": [
        "CREATE TABLE test_strings (id UInt32, text String);",
        "INSERT INTO test_strings VALUES (1, 'abc'), (2, 'defg');",
        "UPDATE test_strings SET text = rightPad(text, 6, '-') WHERE id = 1;",
        "SELECT rightPad(text, 5) AS padded_text FROM test_strings WHERE id = 2;",
        "SELECT rightPad(text, 8, '*') AS padded_text FROM test_strings;"
    ]
}