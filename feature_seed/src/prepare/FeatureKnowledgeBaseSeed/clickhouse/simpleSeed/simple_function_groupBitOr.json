{
    "feature_type": "function",
    "feature_name": "groupBitOr",
    "sql_array": [
        "CREATE TABLE test_table (id UInt32, num UInt32) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_table (id, num) VALUES (1, 3), (2, 5), (3, 2);",
        "UPDATE test_table SET num = num + 1 WHERE id = 1;",
        "SELECT groupBitOr(num) FROM test_table;",
        "SELECT id, groupBitOr(num) FROM test_table GROUP BY id;"
    ]
}