{
    "feature_type": "function",
    "feature_name": "basename",
    "sql_array": [
        "CREATE TABLE test_paths (path String);",
        "INSERT INTO test_paths VALUES ('/user/local/bin/program'), ('C:\\Program Files\\app\\executable');",
        "UPDATE test_paths SET path = basename(path);",
        "SELECT path, basename(path) AS filename FROM test_paths;",
        "SELECT COUNT(*), basename(path) AS filename FROM test_paths GROUP BY filename;"
    ]
}