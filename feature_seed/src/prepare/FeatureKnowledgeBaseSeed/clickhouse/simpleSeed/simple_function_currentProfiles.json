{
    "feature_type": "function",
    "feature_name": "currentProfiles",
    "sql_array": [
        "CREATE TABLE test_profiles (id UInt32, name String, profile String) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_profiles (id, name, profile) VALUES (1, 'Alice', 'profile1');",
        "UPDATE test_profiles SET profile = 'profile2' WHERE id = 1;",
        "SELECT currentProfiles();",
        "SELECT name FROM test_profiles WHERE profile IN (SELECT currentProfiles());"
    ]
}