{
    "feature_type": "keyword",
    "feature_name": "ARRAY JOIN",
    "sql_array": [
        "CREATE TABLE test_array_join (id UInt32, values Array(UInt32)) ENGINE = Memory;",
        "INSERT INTO test_array_join VALUES (1, [10, 20, 30]), (2, [40, 50]);",
        "SELECT id, arrayJoin(values) AS value FROM test_array_join;",
        "CREATE TABLE test_nested (user UInt32, properties Nested (key String, value Int32)) ENGINE = Memory;",
        "SELECT user, arrayJoin(properties.value) AS prop_value FROM test_nested;"
    ]
}