{
    "feature_type": "function",
    "feature_name": "now",
    "sql_array": [
        "CREATE TABLE test_times (id UInt32, event_time DateTime DEFAULT now());",
        "INSERT INTO test_times (id) VALUES (1), (2), (3);",
        "SELECT id, event_time FROM test_times WHERE event_time >= now() - INTERVAL 1 DAY;",
        "UPDATE test_times SET event_time = now() WHERE id = 1;",
        "SELECT count(*) FROM test_times WHERE event_time < now();"
    ]
}