{
    "feature_type": "function",
    "feature_name": "reinterpretAsInt64",
    "sql_array": [
        "CREATE TABLE test_reinterpret (value UInt8);",
        "INSERT INTO test_reinterpret VALUES (255);",
        "SELECT reinterpretAsInt64(value) AS int64_value FROM test_reinterpret;",
        "UPDATE test_reinterpret SET value = reinterpretAsInt64(256) WHERE value = 255;",
        "SELECT value, reinterpretAsInt64(value) AS reinterpret_value FROM test_reinterpret WHERE value > 0;"
    ]
}