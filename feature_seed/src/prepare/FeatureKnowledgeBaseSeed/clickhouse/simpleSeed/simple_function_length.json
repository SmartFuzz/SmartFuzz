{
    "feature_type": "function",
    "feature_name": "length",
    "sql_array": [
        "CREATE TABLE test_strings (str String);",
        "INSERT INTO test_strings VALUES ('Hello, world!');",
        "SELECT length(str) FROM test_strings;",
        "UPDATE test_strings SET str = 'New string value' WHERE length(str) > 5;",
        "SELECT length(['a', 'b', 'c']) AS array_length;"
    ]
}