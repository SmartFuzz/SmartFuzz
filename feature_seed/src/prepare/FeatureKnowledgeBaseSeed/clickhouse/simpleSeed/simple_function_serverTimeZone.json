{
    "feature_type": "function",
    "feature_name": "serverTimeZone",
    "sql_array": [
        "CREATE TABLE test_timezone (id Int32, tz String) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_timezone VALUES (1, serverTimeZone()), (2, 'UTC');",
        "SELECT id, tz FROM test_timezone WHERE tz = serverTimeZone();",
        "UPDATE test_timezone SET tz = 'America/New_York' WHERE id = 1;",
        "SELECT serverTimeZone() AS current_timezone, count() FROM test_timezone GROUP BY current_timezone;"
    ]
}