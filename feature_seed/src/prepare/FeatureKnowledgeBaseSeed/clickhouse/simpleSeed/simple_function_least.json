{
    "feature_type": "function",
    "feature_name": "least",
    "sql_array": [
        "CREATE TABLE test_least (a Int32, b Int32);",
        "INSERT INTO test_least (a, b) VALUES (5, 10), (15, 8), (3, 20);",
        "SELECT least(a, b) AS min_value FROM test_least;",
        "UPDATE test_least SET a = least(a, b) WHERE a > b;",
        "SELECT count(*) FROM test_least WHERE least(a, b) < 10;"
    ]
}