{
    "feature_type": "function",
    "feature_name": "bitSlice(s, offset, length)",
    "sql_array": [
        "CREATE TABLE test_table (id UInt32, binary_data String);",
        "INSERT INTO test_table VALUES (1, 'Hello');",
        "UPDATE test_table SET binary_data = bitSlice(binary_data, 1, 8) WHERE id = 1;",
        "SELECT binary_data, bitSlice(binary_data, 1, 2) AS sliced_bits FROM test_table;",
        "SELECT bitSlice(binary_data, -4, 8) AS last_four_bits FROM test_table;"
    ]
}