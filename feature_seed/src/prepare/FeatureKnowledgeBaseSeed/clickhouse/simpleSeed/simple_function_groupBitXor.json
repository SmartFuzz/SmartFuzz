{
    "feature_type": "function",
    "feature_name": "groupBitXor",
    "sql_array": [
        "CREATE TABLE test_table (id UInt32, num UInt32) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_table (id, num) VALUES (1, 3), (2, 5), (3, 7);",
        "UPDATE test_table SET num = num + 1 WHERE id = 2;",
        "SELECT groupBitXor(num) FROM test_table;",
        "SELECT id, groupBitXor(num) OVER (PARTITION BY id) AS bit_xor_result FROM test_table;"
    ]
}