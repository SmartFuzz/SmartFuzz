{
    "feature_type": "function",
    "feature_name": "byteSlice(s, offset, length)",
    "sql_array": [
        "CREATE TABLE test_bytes (data String);",
        "INSERT INTO test_bytes VALUES ('Hello, ClickHouse!');",
        "SELECT byteSlice(data, 8, 9) FROM test_bytes;",
        "UPDATE test_bytes SET data = byteSlice(data, 0, 5) WHERE data LIKE 'Hello%';",
        "SELECT byteSlice('abcdefg', 3, 4) AS sliced_data;"
    ]
}