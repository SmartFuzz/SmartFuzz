{
    "feature_type": "function",
    "feature_name": "generate_series",
    "sql_array": [
        "CREATE TABLE test_generate_series (value UInt64);",
        "INSERT INTO test_generate_series SELECT * FROM generate_series(1, 10);",
        "SELECT value FROM test_generate_series WHERE value % 2 = 0;",
        "UPDATE test_generate_series SET value = value + 5 WHERE value <= 5;",
        "SELECT * FROM test_generate_series WHERE value IN (SELECT * FROM generate_series(1, 5, 2));"
    ]
}