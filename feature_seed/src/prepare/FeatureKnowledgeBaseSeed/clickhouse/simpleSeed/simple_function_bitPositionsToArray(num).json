{
    "feature_type": "function",
    "feature_name": "bitPositionsToArray(num)",
    "sql_array": [
        "CREATE TABLE test_bit_positions (num Int64) ENGINE = Memory;",
        "INSERT INTO test_bit_positions (num) VALUES (3), (4), (5);",
        "SELECT num, bitPositionsToArray(num) AS bit_positions FROM test_bit_positions;",
        "UPDATE test_bit_positions SET num = 7 WHERE num = 5;",
        "SELECT num, bitPositionsToArray(toUInt64(num)) AS bit_positions FROM test_bit_positions WHERE num > 0;"
    ]
}