{
    "feature_type": "function",
    "feature_name": "revision",
    "sql_array": [
        "CREATE TABLE test_revision (id UInt32, rev UInt32) ENGINE = MergeTree() ORDER BY id;",
        "INSERT INTO test_revision (id, rev) VALUES (1, revision());",
        "UPDATE test_revision SET rev = revision() WHERE id = 1;",
        "SELECT id, rev FROM test_revision WHERE rev = revision();",
        "SELECT COUNT(*) FROM test_revision WHERE rev > (revision() - 10);"
    ]
}