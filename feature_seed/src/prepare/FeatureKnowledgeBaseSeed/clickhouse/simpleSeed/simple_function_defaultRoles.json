{
    "feature_type": "function",
    "feature_name": "defaultRoles",
    "sql_array": [
        "CREATE TABLE test_roles (role_name String) ENGINE = MergeTree() ORDER BY role_name;",
        "INSERT INTO test_roles VALUES ('admin'), ('editor'), ('viewer');",
        "SELECT role_name FROM test_roles WHERE role_name IN (SELECT arrayJoin(defaultRoles()));",
        "UPDATE test_roles SET role_name = 'superadmin' WHERE role_name = 'admin';",
        "SELECT defaultRoles() AS default_user_roles;"
    ]
}