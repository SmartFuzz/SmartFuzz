{
    "feature_type": "function",
    "feature_name": "splitByWhitespace",
    "sql_array": [
        "CREATE TABLE test_split (data String);",
        "INSERT INTO test_split VALUES ('   apple   orange   banana   ');",
        "SELECT splitByWhitespace(data) FROM test_split;",
        "UPDATE test_split SET data = '  cat dog mouse  ' WHERE data = '   apple   orange   banana   ';",
        "SELECT splitByWhitespace(data, 2) AS limited_split FROM test_split;"
    ]
}