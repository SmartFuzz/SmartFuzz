{
    "feature_type": "function",
    "feature_name": "listagg(col_expr, separator_str)",
    "sql_array": [
        "CREATE TABLE test_listagg (id INT, value VARCHAR(255));",
        "INSERT INTO test_listagg (id, value) VALUES (1, 'apple'), (2, 'banana'), (3, 'cherry');",
        "UPDATE test_listagg SET value = 'orange' WHERE id = 2;",
        "SELECT listagg(value, ', ') FROM test_listagg GROUP BY id;",
        "SELECT id, listagg(value, ' | ') OVER (ORDER BY id) AS concatenated_values FROM test_listagg;"
    ]
}