{
    "feature_type": "function",
    "feature_name": "sys.getport(url)",
    "sql_array": [
        "CREATE TABLE url_test (id INTEGER PRIMARY KEY, url CLOB);",
        "INSERT INTO url_test (id, url) VALUES (1, 'http://example.com:8080/path');",
        "SELECT sys.getport(url) FROM url_test WHERE id = 1;",
        "UPDATE url_test SET url = 'https://example.org:3000/newpath' WHERE id = 1;",
        "SELECT sys.getport(url) AS port_number FROM url_test WHERE url LIKE 'http%';"
    ]
}