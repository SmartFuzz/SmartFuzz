{
    "feature_type": "function",
    "feature_name": "sys.getfile(url)",
    "sql_array": [
        "CREATE TABLE url_data (id INT PRIMARY KEY, url CLOB);",
        "INSERT INTO url_data (id, url) VALUES (1, 'http://example.com/path/to/file.txt');",
        "SELECT sys.getfile(url) FROM url_data WHERE id = 1;",
        "UPDATE url_data SET url = 'https://anotherexample.com/dir/file2.txt' WHERE id = 1;",
        "SELECT sys.getfile(url) AS filename FROM url_data WHERE url LIKE '%.txt';"
    ]
}