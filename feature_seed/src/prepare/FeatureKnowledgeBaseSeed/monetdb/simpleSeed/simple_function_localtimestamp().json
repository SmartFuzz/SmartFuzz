{
    "feature_type": "function",
    "feature_name": "localtimestamp()",
    "sql_array": [
        "CREATE TABLE test_timestamps (id INT, created_at TIMESTAMP DEFAULT localtimestamp());",
        "INSERT INTO test_timestamps (id) VALUES (1);",
        "UPDATE test_timestamps SET created_at = localtimestamp() WHERE id = 1;",
        "SELECT id, created_at FROM test_timestamps WHERE created_at < localtimestamp();",
        "DELETE FROM test_timestamps WHERE created_at < localtimestamp() - INTERVAL '1 DAY';"
    ]
}