{
    "feature_type": "keyword",
    "feature_name": "DROP WINDOW",
    "sql_array": [
        "CREATE WINDOW my_window AS SELECT COUNT(*) OVER (PARTITION BY col1 ORDER BY col2) FROM my_table;",
        "DROP WINDOW IF EXISTS my_window;",
        "CREATE TABLE test_table (id INT, value VARCHAR(100));",
        "INSERT INTO test_table (id, value) VALUES (1, 'test_value');",
        "SELECT * FROM test_table;"
    ]
}