{
    "feature_type": "function",
    "feature_name": "sys.getbasename(url)",
    "sql_array": [
        "CREATE TABLE urls(id INT PRIMARY KEY, url CLOB);",
        "INSERT INTO urls VALUES (1, 'http://example.com/path/to/resource.html');",
        "INSERT INTO urls VALUES (2, 'https://example.com/directory/item.jpg');",
        "SELECT sys.getbasename(url) FROM urls WHERE id = 1;",
        "SELECT sys.getbasename(url) FROM urls WHERE id = 2;"
    ]
}