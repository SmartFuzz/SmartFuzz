{
    "feature_type": "datatype",
    "feature_name": "INTERVAL HOUR",
    "sql_array": [
        "CREATE TABLE test_interval (id SERIAL PRIMARY KEY, duration INTERVAL HOUR);",
        "INSERT INTO test_interval (duration) VALUES (INTERVAL '2' HOUR);",
        "UPDATE test_interval SET duration = INTERVAL '3.5' HOUR WHERE id = 1;",
        "SELECT id, duration FROM test_interval WHERE duration > INTERVAL '1' HOUR;",
        "SELECT id, duration, duration + INTERVAL '2' HOUR AS extended_duration FROM test_interval;"
    ]
}