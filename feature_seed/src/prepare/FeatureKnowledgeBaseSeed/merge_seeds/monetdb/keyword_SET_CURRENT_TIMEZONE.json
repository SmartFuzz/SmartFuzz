CREATE TABLE test_table (id INT PRIMARY KEY, name VARCHAR(100));
INSERT INTO test_table VALUES (1, 'Alice');
INSERT INTO test_table VALUES (2, 'Bob');
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = 0;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = 3600;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = 7200;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = -7200;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = -3600;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = 60;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = -60;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = 1800;
SELECT CURRENT_TIMEZONE;
SET CURRENT_TIMEZONE = 18000;
SELECT id, name, CURRENT_TIMEZONE FROM test_table WHERE name IN (SELECT name FROM test_table WHERE id = 1);
SET CURRENT_TIMEZONE = (SELECT AVG(CURRENT_TIMEZONE) FROM (SELECT CURRENT_TIMEZONE) AS tz);
SET CURRENT_TIMEZONE = CASE WHEN (SELECT COUNT(*) FROM test_table) > 1 THEN 3600 ELSE -3600 END;
SELECT CURRENT_TIMEZONE + 3600 AS new_timezone;
SET CURRENT_TIMEZONE = -18000 + (SELECT CURRENT_TIMEZONE FROM test_table LIMIT 1);