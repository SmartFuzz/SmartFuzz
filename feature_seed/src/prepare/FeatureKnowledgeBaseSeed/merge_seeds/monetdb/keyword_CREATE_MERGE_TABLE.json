CREATE MERGE TABLE IF NOT EXISTS schema1.merge_table1 (id INT, name VARCHAR(100)) PARTITION BY RANGE ON (id);
CREATE MERGE TABLE schema1.merge_table2 (order_id INT, product_id INT, quantity INT) PARTITION BY VALUES ON (product_id);
CREATE MERGE TABLE IF NOT EXISTS schema1.merge_table3 (customer_id INT, order_date DATE, status VARCHAR(20)) PARTITION BY VALUES ON (status);
CREATE MERGE TABLE schema1.merge_table4 (employee_id INT, department_id INT, salary DECIMAL(10,2)) PARTITION BY RANGE ON (salary);
CREATE MERGE TABLE IF NOT EXISTS schema1.merge_table5 (transaction_id INT, user_id INT, amount DECIMAL(8,2)) PARTITION BY RANGE ON (amount);
CREATE MERGE TABLE schema1.merge_table6 (review_id INT, product_id INT, rating INT, review TEXT) PARTITION BY VALUES ON (rating);
ALTER TABLE schema1.merge_table1 ADD TABLE schema1.partition_table1;
ALTER TABLE schema1.merge_table2 ADD TABLE schema1.partition_table2;
DROP TABLE schema1.merge_table3;
CREATE MERGE TABLE schema1.merge_table7 (id INT, city VARCHAR(50)) PARTITION BY RANGE ON (id);
ALTER TABLE schema1.merge_table4 ADD TABLE schema1.partition_table3;
ALTER TABLE schema1.merge_table5 DROP TABLE schema1.partition_table4;
CREATE MERGE TABLE IF NOT EXISTS schema1.merge_table8 (project_id INT, start_date DATE, end_date DATE) PARTITION BY RANGE ON (start_date);