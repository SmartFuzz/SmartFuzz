CREATE TABLE instances (id SERIAL PRIMARY KEY, name VARCHAR(255), status VARCHAR(50));
INSERT INTO instances (name, status) VALUES ('my_instance_of_linux', 'running');
DECLARE access_key VARCHAR, sec_token VARCHAR, p_name VARCHAR, instance_status VARCHAR;
SET access_key = 'AKIAJI7ZL427TI5EDF5A';
SET sec_token = 'CGI/UMaXf2LRUctaj/HGJ57UNy/t7fNCshh8wpJg';
SET p_name = (SELECT name FROM instances WHERE id = 1);
SET instance_status = (SELECT status FROM instances WHERE name = p_name);
IF instance_status = 'running' THEN DB.DBA.AMAZON_STOP_INSTANCE(access_key, p_name, sec_token);
END IF;
SELECT name, status FROM instances WHERE status = 'running';
UPDATE instances SET status = 'stopped' WHERE name = 'my_instance_of_linux' AND status = 'running';
CREATE TABLE instance_logs (log_id SERIAL PRIMARY KEY, instance_name VARCHAR(255), action VARCHAR(100), timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
INSERT INTO instance_logs (instance_name, action) VALUES ('my_instance_of_linux', 'stopped');
SELECT p_name, (SELECT COUNT(*) FROM instance_logs WHERE instance_name = p_name) AS action_count FROM instances WHERE status = 'stopped';
DECLARE temp_name VARCHAR;
SET temp_name = 'my_instance_of_linux';
IF (SELECT COUNT(*) FROM instances WHERE name = temp_name) > 0 THEN DB.DBA.AMAZON_STOP_INSTANCE(access_key, temp_name, sec_token);
END IF;