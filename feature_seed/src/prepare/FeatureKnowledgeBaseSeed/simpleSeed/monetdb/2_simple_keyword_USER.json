CREATE TABLE test_users (id INT PRIMARY KEY, username VARCHAR(50), created_by VARCHAR(50) DEFAULT USER);
INSERT INTO test_users (id, username) VALUES (1, 'test_user_1');
UPDATE test_users SET created_by = USER WHERE id = 1;
SELECT username, created_by FROM test_users WHERE created_by = USER;
DELETE FROM test_users WHERE created_by = USER;