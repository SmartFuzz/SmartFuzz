CREATE TABLE test_timestamps (id INT PRIMARY KEY, created_at TIMESTAMPTZ DEFAULT now());
INSERT INTO test_timestamps (id) VALUES (1);
UPDATE test_timestamps SET created_at = now() WHERE id = 1;
SELECT id, created_at FROM test_timestamps WHERE created_at < now();
SELECT COUNT(*) FROM test_timestamps WHERE created_at > now() - INTERVAL '1 day';