CREATE TABLE test_logic (id INT PRIMARY KEY, is_active BOOLEAN);
INSERT INTO test_logic (id, is_active) VALUES (1, true);
UPDATE test_logic SET is_active = not(is_active) WHERE id = 1;
SELECT id, not(is_active) AS is_inactive FROM test_logic;
DELETE FROM test_logic WHERE not(is_active) = true;