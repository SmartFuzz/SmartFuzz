CREATE TABLE mysch.articles (id INT PRIMARY KEY, title TEXT);
CREATE INDEX article_id ON mysch.articles (id);
COMMENT ON INDEX mysch.article_id IS 'index for article id';
SELECT id, title FROM mysch.articles WHERE id IS NOT NULL;
COMMENT ON INDEX mysch.article_id IS NULL;