CREATE TABLE test_cfg (item_name VARCHAR(255));
INSERT INTO test_cfg (item_name) VALUES ('ServerPort'), ('Database'), ('UserName');
SELECT cfg_item_name(virtuoso_ini_path(), 'Parameters', 0);
SELECT cfg_item_name(virtuoso_ini_path(), 'Parameters', 1);
SELECT cfg_item_name(virtuoso_ini_path(), 'Parameters', 2);
SELECT cfg_item_name(virtuoso_ini_path(), 'Parameters', 10);
SELECT cfg_item_name(virtuoso_ini_path(), 'Parameters', -1);
SELECT cfg_item_name(virtuoso_ini_path(), 'Parameters', (SELECT COUNT(*) FROM test_cfg));
SELECT * FROM (SELECT cfg_item_name(virtuoso_ini_path(), 'Parameters', 0) AS first_item, cfg_item_name(virtuoso_ini_path(), 'Parameters', 1) AS second_item) AS subquery;
SELECT IIF(cfg_item_name(virtuoso_ini_path(), 'Parameters', 0) IS NULL, 'No item found', cfg_item_name(virtuoso_ini_path(), 'Parameters', 0));