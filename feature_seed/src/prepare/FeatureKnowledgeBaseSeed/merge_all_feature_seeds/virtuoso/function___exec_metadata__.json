CREATE TABLE employees (id INT PRIMARY KEY, name VARCHAR(255), department_id INT);
CREATE TABLE departments (id INT PRIMARY KEY, name VARCHAR(255));
INSERT INTO departments (id, name) VALUES (1, 'HR'), (2, 'Engineering'), (3, 'Sales');
INSERT INTO employees (id, name, department_id) VALUES (1, 'John Doe', 2), (2, 'Jane Smith', 1), (3, 'Emily Johnson', 3);
EXEC_METADATA('SELECT * FROM employees;
', state, message, mdta);
EXEC_METADATA('SELECT e.name, d.name FROM employees e JOIN departments d ON e.department_id = d.id;
', state, message, mdta);
EXEC_METADATA('SELECT e.name FROM employees e WHERE e.id IN (SELECT id FROM employees WHERE department_id = 1);
', state, message, mdta);
EXEC_METADATA('SELECT d.name, COUNT(e.id) FROM departments d LEFT JOIN employees e ON d.id = e.department_id GROUP BY d.name;
', state, message, mdta);
EXEC_METADATA('SELECT e.name, (SELECT AVG(salary) FROM salaries WHERE employee_id = e.id) AS avg_salary FROM employees e;
', state, message, mdta);
EXEC_METADATA('SELECT * FROM employees WHERE department_id = 1 AND name LIKE ''J%'';
', state, message, mdta);
EXEC_METADATA('SELECT * FROM (SELECT e.id, e.name FROM employees e WHERE e.department_id IS NOT NULL) AS subquery;
', state, message, mdta);