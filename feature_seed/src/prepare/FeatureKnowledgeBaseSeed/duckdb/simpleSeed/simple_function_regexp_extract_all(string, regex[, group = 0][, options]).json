{
    "feature_type": "function",
    "feature_name": "regexp_extract_all(string, regex[, group = 0][, options])",
    "sql_array": [
        "CREATE TABLE test_table (text_column TEXT);",
        "INSERT INTO test_table (text_column) VALUES ('hello_world hello_universe hello_universe_earth');",
        "SELECT regexp_extract_all(text_column, '([a-z]+)_?', 1) FROM test_table;",
        "UPDATE test_table SET text_column = 'new_value_1 new_value_2' WHERE text_column = 'hello_world';",
        "SELECT regexp_extract_all(text_column, '([a-z]+)_([a-z]+)', 0) FROM test_table;"
    ]
}