{
    "feature_type": "function",
    "feature_name": "list_slice(list, begin, end)",
    "sql_array": [
        "CREATE TABLE test_list (id INTEGER, values LIST<Integer>);",
        "INSERT INTO test_list VALUES (1, [10, 20, 30, 40, 50]);",
        "SELECT list_slice(values, 2, 4) FROM test_list WHERE id = 1;",
        "UPDATE test_list SET values = list_slice(values, -4, -1) WHERE id = 1;",
        "SELECT id, list_slice(values, 0, 3) AS sliced_values FROM test_list;"
    ]
}