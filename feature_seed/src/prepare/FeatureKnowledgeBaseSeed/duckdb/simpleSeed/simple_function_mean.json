{
    "feature_type": "function",
    "feature_name": "mean",
    "sql_array": [
        "CREATE TABLE test_table (id INTEGER, value DOUBLE);",
        "INSERT INTO test_table (id, value) VALUES (1, 10.0), (2, 20.0), (3, 30.0);",
        "UPDATE test_table SET value = value + 5 WHERE id = 1;",
        "SELECT mean(value) FROM test_table WHERE id > 1;",
        "SELECT id, mean(value) OVER (PARTITION BY id) AS avg_value FROM test_table;"
    ]
}