{
    "feature_type": "function",
    "feature_name": "lead(column, 3, 0)",
    "sql_array": [
        "CREATE TABLE sales (id INTEGER, amount INTEGER);",
        "INSERT INTO sales (id, amount) VALUES (1, 100), (2, 200), (3, 300);",
        "SELECT id, amount, lead(amount, 3, 0) OVER (ORDER BY id) AS future_amount FROM sales;",
        "UPDATE sales SET amount = amount + 50 WHERE id = 2;",
        "SELECT id, amount, lead(amount, 1) OVER (ORDER BY id) AS next_amount FROM sales ORDER BY id DESC;"
    ]
}