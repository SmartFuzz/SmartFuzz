{
    "feature_type": "keyword",
    "feature_name": "Transaction Management",
    "sql_array": [
        "CREATE TABLE employees (id BIGINT, name VARCHAR, salary BIGINT);",
        "BEGIN TRANSACTION; INSERT INTO employees VALUES (1, 'Alice', 60000); COMMIT;",
        "BEGIN TRANSACTION; INSERT INTO employees VALUES (2, 'Bob', 50000); ROLLBACK;",
        "BEGIN TRANSACTION; UPDATE employees SET salary = 70000 WHERE name = 'Alice'; COMMIT;",
        "SELECT * FROM employees;"
    ]
}