{
    "feature_type": "function",
    "feature_name": "rpad(string, count, character)",
    "sql_array": [
        "CREATE TABLE example_table (text_column VARCHAR);",
        "INSERT INTO example_table (text_column) VALUES ('hello');",
        "UPDATE example_table SET text_column = rpad(text_column, 10, '<') WHERE text_column = 'hello';",
        "SELECT rpad(text_column, 10, '<') AS padded_text FROM example_table;",
        "SELECT COUNT(*) FROM example_table WHERE text_column = rpad('world', 10, '*');"
    ]
}