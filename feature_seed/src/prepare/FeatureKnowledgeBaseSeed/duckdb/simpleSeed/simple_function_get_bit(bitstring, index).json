{
    "feature_type": "function",
    "feature_name": "get_bit(bitstring, index)",
    "sql_array": [
        "CREATE TABLE test_bits (id INT, bitvalue BITSTRING);",
        "INSERT INTO test_bits (id, bitvalue) VALUES (1, '0110010'::BITSTRING);",
        "SELECT get_bit(bitvalue, 2) FROM test_bits WHERE id = 1;",
        "UPDATE test_bits SET bitvalue = '1100110'::BITSTRING WHERE id = 1;",
        "SELECT get_bit(bitvalue, 0) AS first_bit, get_bit(bitvalue, 1) AS second_bit FROM test_bits;"
    ]
}