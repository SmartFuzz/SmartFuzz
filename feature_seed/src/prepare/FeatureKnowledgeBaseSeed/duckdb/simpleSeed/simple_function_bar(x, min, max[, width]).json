{
    "feature_type": "function",
    "feature_name": "bar(x, min, max[, width])",
    "sql_array": [
        "CREATE TABLE test_scores (id INTEGER, score INTEGER);",
        "INSERT INTO test_scores (id, score) VALUES (1, 5);",
        "UPDATE test_scores SET score = 15 WHERE id = 1;",
        "SELECT id, bar(score, 0, 20, 10) AS score_bar FROM test_scores;",
        "SELECT id, bar(score, 0, 100) AS score_bar FROM test_scores WHERE score > 10;"
    ]
}