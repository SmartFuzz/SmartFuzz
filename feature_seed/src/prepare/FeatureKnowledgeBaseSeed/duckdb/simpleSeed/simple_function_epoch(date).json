{
    "feature_type": "function",
    "feature_name": "epoch(date)",
    "sql_array": [
        "CREATE TABLE test_dates (id INTEGER, event_date DATE);",
        "INSERT INTO test_dates VALUES (1, date '1992-02-15');",
        "UPDATE test_dates SET event_date = date '2000-01-01' WHERE id = 1;",
        "SELECT id, epoch(event_date) AS epoch_seconds FROM test_dates;",
        "SELECT COUNT(*) FROM test_dates WHERE epoch(event_date) > 1000000000;"
    ]
}