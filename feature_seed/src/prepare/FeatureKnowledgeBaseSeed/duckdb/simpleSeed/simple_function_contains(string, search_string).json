{
    "feature_type": "function",
    "feature_name": "contains(string, search_string)",
    "sql_array": [
        "CREATE TABLE test_strings (id INTEGER, text STRING);",
        "INSERT INTO test_strings (id, text) VALUES (1, 'abc'), (2, 'def');",
        "UPDATE test_strings SET text = 'abcdef' WHERE id = 1;",
        "SELECT id FROM test_strings WHERE contains(text, 'a');",
        "SELECT id, text FROM test_strings WHERE contains(text, 'ab') OR contains(text, 'ef');"
    ]
}