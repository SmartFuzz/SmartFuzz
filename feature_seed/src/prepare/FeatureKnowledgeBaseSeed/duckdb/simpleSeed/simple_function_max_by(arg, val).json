{
    "feature_type": "function",
    "feature_name": "max_by(arg, val)",
    "sql_array": [
        "CREATE TABLE employees (id INT, name VARCHAR, salary INT);",
        "INSERT INTO employees VALUES (1, 'Alice', 60000), (2, 'Bob', 80000), (3, 'Charlie', 70000);",
        "SELECT max_by(name, salary) FROM employees;",
        "UPDATE employees SET salary = salary + 5000 WHERE id = (SELECT id FROM employees ORDER BY salary DESC LIMIT 1);",
        "SELECT max_by(name, salary) AS highest_paid FROM employees GROUP BY salary;"
    ]
}