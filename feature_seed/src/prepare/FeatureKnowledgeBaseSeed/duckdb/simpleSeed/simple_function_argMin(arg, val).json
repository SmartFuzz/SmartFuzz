{
    "feature_type": "function",
    "feature_name": "argMin(arg, val)",
    "sql_array": [
        "CREATE TABLE test_table (A INTEGER, B INTEGER);",
        "INSERT INTO test_table (A, B) VALUES (1, 10), (2, 20), (3, 5);",
        "SELECT argMin(A, B) FROM test_table;",
        "UPDATE test_table SET B = B + 5 WHERE A = (SELECT argMin(A, B) FROM test_table);",
        "SELECT A, B, argMin(A, B) OVER (ORDER BY B) AS min_arg FROM test_table;"
    ]
}