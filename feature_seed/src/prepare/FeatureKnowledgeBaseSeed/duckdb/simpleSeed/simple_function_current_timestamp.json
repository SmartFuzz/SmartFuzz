{
    "feature_type": "function",
    "feature_name": "current_timestamp",
    "sql_array": [
        "CREATE TABLE test_timestamps (id INTEGER, created_at TIMESTAMP DEFAULT current_timestamp);",
        "INSERT INTO test_timestamps (id) VALUES (1);",
        "UPDATE test_timestamps SET created_at = current_timestamp WHERE id = 1;",
        "SELECT id, created_at FROM test_timestamps WHERE created_at < current_timestamp;",
        "CREATE TABLE event_logs (event_id INTEGER, event_time TIMESTAMP DEFAULT current_timestamp);"
    ]
}