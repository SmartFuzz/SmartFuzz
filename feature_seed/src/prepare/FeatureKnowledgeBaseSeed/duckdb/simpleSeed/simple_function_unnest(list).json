{
    "feature_type": "function",
    "feature_name": "unnest(list)",
    "sql_array": [
        "CREATE TABLE test_list (id INTEGER, values LIST<INTEGER>);",
        "INSERT INTO test_list VALUES (1, [1, 2, 3]), (2, [4, 5, 6]);",
        "SELECT id, unnest(values) AS value FROM test_list;",
        "UPDATE test_list SET values = [7, 8, 9] WHERE id = 1;",
        "SELECT id, unnest(VALUES) AS value FROM test_list WHERE id = 2;"
    ]
}