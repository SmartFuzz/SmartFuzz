{
    "feature_type": "function",
    "feature_name": "parse_dirpath(path, separator)",
    "sql_array": [
        "CREATE TABLE test_paths (id INTEGER, file_path TEXT);",
        "INSERT INTO test_paths VALUES (1, '/path/to/file.csv');",
        "SELECT parse_dirpath(file_path, 'forward_slash') FROM test_paths WHERE id = 1;",
        "UPDATE test_paths SET file_path = '/new/path/to/file.txt' WHERE id = 1;",
        "SELECT parse_dirpath(file_path, 'both_slash') FROM test_paths;"
    ]
}