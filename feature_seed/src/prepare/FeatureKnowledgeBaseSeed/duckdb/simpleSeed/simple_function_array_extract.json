{
    "feature_type": "function",
    "feature_name": "array_extract",
    "sql_array": [
        "CREATE TABLE test_list (id INTEGER, values LIST<INTEGER>);",
        "INSERT INTO test_list VALUES (1, [10, 20, 30]);",
        "SELECT array_extract(values, 2) AS second_element FROM test_list WHERE id = 1;",
        "UPDATE test_list SET values = array_extract(values, 1) WHERE id = 1;",
        "SELECT id, array_extract(values, 1) + array_extract(values, 3) AS sum FROM test_list;"
    ]
}