{
    "feature_type": "keyword",
    "feature_name": "CREATE TABLE",
    "sql_array": [
        "CREATE TABLE employees (id INTEGER PRIMARY KEY, name VARCHAR, salary FLOAT);",
        "INSERT INTO employees VALUES (1, 'Alice', 70000.00);",
        "INSERT INTO employees VALUES (2, 'Bob', 60000.00);",
        "UPDATE employees SET salary = salary * 1.1 WHERE id = 1;",
        "SELECT name, salary FROM employees WHERE salary > 65000;"
    ]
}