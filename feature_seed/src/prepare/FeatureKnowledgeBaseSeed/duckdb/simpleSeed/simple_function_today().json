{
    "feature_type": "function",
    "feature_name": "today()",
    "sql_array": [
        "CREATE TABLE test_dates (id INTEGER, date_column DATE);",
        "INSERT INTO test_dates (id, date_column) VALUES (1, today());",
        "UPDATE test_dates SET date_column = today() WHERE id = 1;",
        "SELECT * FROM test_dates WHERE date_column = today();",
        "SELECT id FROM test_dates WHERE date_column < today();"
    ]
}