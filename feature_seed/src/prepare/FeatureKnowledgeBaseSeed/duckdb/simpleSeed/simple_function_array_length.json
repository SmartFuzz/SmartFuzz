{
    "feature_type": "function",
    "feature_name": "array_length",
    "sql_array": [
        "CREATE TABLE test_list (id INTEGER, values LIST<INTEGER>);",
        "INSERT INTO test_list VALUES (1, [1, 2, 3]), (2, [4, 5]);",
        "SELECT id, array_length(values) AS length FROM test_list;",
        "UPDATE test_list SET values = [1, 2, 3, 4, 5] WHERE id = 1;",
        "SELECT id FROM test_list WHERE array_length(values) > 2;"
    ]
}