{
    "feature_type": "keyword",
    "feature_name": "COPY",
    "sql_array": [
        "CREATE TABLE lineitem (l_orderkey INTEGER, l_partkey INTEGER, l_quantity INTEGER, l_extendedprice REAL);",
        "INSERT INTO lineitem VALUES (1, 100, 5, 200.0);",
        "COPY lineitem TO 'lineitem_export.csv' (FORMAT 'csv', HEADER true);",
        "SELECT * FROM lineitem WHERE l_quantity > 3;",
        "UPDATE lineitem SET l_extendedprice = l_extendedprice * 1.1 WHERE l_orderkey = 1;"
    ]
}