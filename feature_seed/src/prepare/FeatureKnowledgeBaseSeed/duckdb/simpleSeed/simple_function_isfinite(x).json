{
    "feature_type": "function",
    "feature_name": "isfinite(x)",
    "sql_array": [
        "CREATE TABLE test_finite (value FLOAT);",
        "INSERT INTO test_finite (value) VALUES (5.5), (INFINITY), (NaN);",
        "SELECT value, isfinite(value) AS is_finite FROM test_finite;",
        "UPDATE test_finite SET value = 10.0 WHERE isfinite(value) = false;",
        "SELECT COUNT(*) FROM test_finite WHERE isfinite(value) = true;"
    ]
}