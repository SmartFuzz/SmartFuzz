{
    "feature_type": "function",
    "feature_name": "list_pack",
    "sql_array": [
        "CREATE TABLE test_list (id INT, values LIST<Integer>);",
        "INSERT INTO test_list VALUES (1, list_value(10, 20, 30));",
        "UPDATE test_list SET values = list_value(40, 50) WHERE id = 1;",
        "SELECT id, values FROM test_list WHERE list_contains(values, 20);",
        "SELECT list_value(1, 2, 3, 4, 5) AS my_list;"
    ]
}