{
    "feature_type": "function",
    "feature_name": "rtrim(string, characters)",
    "sql_array": [
        "CREATE TABLE test_strings (id INTEGER, str VARCHAR);",
        "INSERT INTO test_strings VALUES (1, '>>>>test<<');",
        "UPDATE test_strings SET str = rtrim(str, '><') WHERE id = 1;",
        "SELECT id, rtrim(str, '<>') AS trimmed_string FROM test_strings;",
        "SELECT rtrim('--------example--------', '-') AS cleaned_string;"
    ]
}