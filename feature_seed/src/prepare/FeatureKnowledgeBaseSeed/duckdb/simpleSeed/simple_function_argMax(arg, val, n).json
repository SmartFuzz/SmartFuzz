{
    "feature_type": "function",
    "feature_name": "argMax(arg, val, n)",
    "sql_array": [
        "CREATE TABLE test_table (A INTEGER, B INTEGER);",
        "INSERT INTO test_table (A, B) VALUES (1, 10), (2, 20), (3, 30);",
        "SELECT arg_max(A, B, 2) FROM test_table GROUP BY A;",
        "UPDATE test_table SET B = B + 5 WHERE A = 1;",
        "SELECT arg_max(A, B, 1) FROM test_table;"
    ]
}