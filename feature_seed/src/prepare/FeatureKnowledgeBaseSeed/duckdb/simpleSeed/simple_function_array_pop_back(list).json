{
    "feature_type": "function",
    "feature_name": "array_pop_back(list)",
    "sql_array": [
        "CREATE TABLE test_list (id INTEGER, numbers LIST<INTEGER>);",
        "INSERT INTO test_list VALUES (1, [1, 2, 3, 4, 5]);",
        "UPDATE test_list SET numbers = array_pop_back(numbers) WHERE id = 1;",
        "SELECT array_pop_back(numbers) AS modified_numbers FROM test_list;",
        "SELECT id, array_pop_back(numbers) FROM test_list WHERE id = 1;"
    ]
}