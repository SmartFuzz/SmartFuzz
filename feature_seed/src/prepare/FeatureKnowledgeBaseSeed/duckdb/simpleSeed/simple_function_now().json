{
    "feature_type": "function",
    "feature_name": "now()",
    "sql_array": [
        "CREATE TABLE test_timestamps (event_time TIMESTAMP);",
        "INSERT INTO test_timestamps (event_time) VALUES (now());",
        "UPDATE test_timestamps SET event_time = now() WHERE event_time IS NOT NULL;",
        "SELECT event_time FROM test_timestamps WHERE event_time > now() - INTERVAL '1 hour';",
        "SELECT COUNT(*) FROM test_timestamps WHERE event_time < now();"
    ]
}