{
    "feature_type": "function",
    "feature_name": "last(arg)",
    "sql_array": [
        "CREATE TABLE test_table (id INTEGER, value INTEGER);",
        "INSERT INTO test_table (id, value) VALUES (1, 10), (2, 20), (3, 30);",
        "UPDATE test_table SET value = value + 5 WHERE id = 2;",
        "SELECT last(value) FROM test_table ORDER BY id;",
        "SELECT id, last(value) OVER (ORDER BY id) as last_value FROM test_table;"
    ]
}