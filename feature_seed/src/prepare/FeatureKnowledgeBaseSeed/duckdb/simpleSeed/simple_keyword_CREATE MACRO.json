{
    "feature_type": "keyword",
    "feature_name": "CREATE MACRO",
    "sql_array": [
        "CREATE TABLE test_data AS SELECT 1 AS id, 'sample' AS name;",
        "CREATE OR REPLACE MACRO add_one(x) AS x + 1;",
        "INSERT INTO test_data SELECT add_one(1), 'new_entry';",
        "SELECT id, add_one(id) AS incremented_id FROM test_data;",
        "CREATE OR REPLACE MACRO concat_name(first, last) AS first || ' ' || last;"
    ]
}