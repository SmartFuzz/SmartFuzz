{
    "feature_type": "function",
    "feature_name": "bitstring_agg(arg)",
    "sql_array": [
        "CREATE TABLE test_table (id INTEGER, value INTEGER);",
        "INSERT INTO test_table (id, value) VALUES (1, 10), (2, 20), (3, 10), (4, 30);",
        "UPDATE test_table SET value = 25 WHERE id = 2;",
        "SELECT bitstring_agg(value) FROM test_table;",
        "SELECT id, bitstring_agg(value) OVER (PARTITION BY id) AS bitstring_result FROM test_table;"
    ]
}