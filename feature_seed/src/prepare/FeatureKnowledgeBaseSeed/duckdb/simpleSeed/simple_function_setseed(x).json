{
    "feature_type": "function",
    "feature_name": "setseed(x)",
    "sql_array": [
        "CREATE TABLE test_random (id INTEGER, value DOUBLE);",
        "INSERT INTO test_random VALUES (1, random()), (2, random()), (3, random());",
        "SET seed(0.5);",
        "SELECT id, value FROM test_random ORDER BY value DESC;",
        "UPDATE test_random SET value = random() WHERE id = 1;"
    ]
}