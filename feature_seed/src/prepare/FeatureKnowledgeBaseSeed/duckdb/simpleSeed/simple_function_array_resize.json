{
    "feature_type": "function",
    "feature_name": "array_resize",
    "sql_array": [
        "CREATE TABLE test_list (id INTEGER PRIMARY KEY, values LIST<INTEGER>);",
        "INSERT INTO test_list (id, values) VALUES (1, [1, 2, 3]);",
        "UPDATE test_list SET values = list_resize(values, 5, 0) WHERE id = 1;",
        "SELECT id, values FROM test_list;",
        "SELECT id, list_resize(values, 10) AS resized_values FROM test_list;"
    ]
}