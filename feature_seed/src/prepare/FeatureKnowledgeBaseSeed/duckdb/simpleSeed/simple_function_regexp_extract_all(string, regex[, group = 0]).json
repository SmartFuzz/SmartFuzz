{
    "feature_type": "function",
    "feature_name": "regexp_extract_all(string, regex[, group = 0])",
    "sql_array": [
        "CREATE TABLE test_data (info VARCHAR);",
        "INSERT INTO test_data (info) VALUES ('hello_world'), ('duckdb_query_test'), ('sql_example_1');",
        "SELECT regexp_extract_all(info, '([a-z ]+)_?', 1) FROM test_data;",
        "UPDATE test_data SET info = regexp_extract_all(info, '([a-z]+)_([^_]+)', 1) WHERE info LIKE '%_%';",
        "SELECT info, regexp_extract_all(info, '([a-z]+)_([^_]+)', 2) FROM test_data WHERE info LIKE '%_%';"
    ]
}