{
    "feature_type": "function",
    "feature_name": "regexp_escape(string)",
    "sql_array": [
        "CREATE TABLE test_regex (url TEXT);",
        "INSERT INTO test_regex (url) VALUES ('http://example.com');",
        "UPDATE test_regex SET url = regexp_escape(url) WHERE url IS NOT NULL;",
        "SELECT regexp_escape(url) AS escaped_url FROM test_regex;",
        "DELETE FROM test_regex WHERE url = regexp_escape('http://example.com');"
    ]
}