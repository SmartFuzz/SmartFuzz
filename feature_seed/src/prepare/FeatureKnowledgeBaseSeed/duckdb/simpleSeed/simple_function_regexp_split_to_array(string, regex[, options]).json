{
    "feature_type": "function",
    "feature_name": "regexp_split_to_array(string, regex[, options])",
    "sql_array": [
        "CREATE TABLE test_split (id INTEGER, text STRING);",
        "INSERT INTO test_split VALUES (1, 'hello world; 42'), (2, 'foo;bar;baz');",
        "SELECT regexp_split_to_array(text, ';') FROM test_split WHERE id = 1;",
        "UPDATE test_split SET text = regexp_split_to_array(text, ';') WHERE id = 2;",
        "SELECT id, regexp_split_to_array(text, '; ') AS split_text FROM test_split;"
    ]
}