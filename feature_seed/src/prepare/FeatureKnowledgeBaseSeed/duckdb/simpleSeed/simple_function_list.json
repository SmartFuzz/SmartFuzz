{
    "feature_type": "function",
    "feature_name": "list",
    "sql_array": [
        "CREATE TABLE test_table (id INT, value VARCHAR);",
        "INSERT INTO test_table (id, value) VALUES (1, 'apple'), (2, 'banana'), (3, 'cherry');",
        "SELECT array_agg(value ORDER BY id) FROM test_table;",
        "UPDATE test_table SET value = 'orange' WHERE id = 2;",
        "SELECT array_agg(value ORDER BY id DESC) FROM test_table;"
    ]
}