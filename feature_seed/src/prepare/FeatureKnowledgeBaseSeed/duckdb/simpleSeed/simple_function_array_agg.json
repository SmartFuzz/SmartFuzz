{
    "feature_type": "function",
    "feature_name": "array_agg",
    "sql_array": [
        "CREATE TABLE test_table (id INT, value VARCHAR);",
        "INSERT INTO test_table (id, value) VALUES (1, 'A'), (2, 'B'), (3, 'C');",
        "UPDATE test_table SET value = 'D' WHERE id = 2;",
        "SELECT array_agg(value ORDER BY id) FROM test_table;",
        "SELECT id, array_agg(value ORDER BY id) AS value_list FROM test_table GROUP BY id;"
    ]
}