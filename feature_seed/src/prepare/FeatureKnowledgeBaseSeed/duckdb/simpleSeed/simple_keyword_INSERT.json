{
    "feature_type": "keyword",
    "feature_name": "INSERT",
    "sql_array": [
        "CREATE TABLE test_table (id INTEGER PRIMARY KEY, value INTEGER);",
        "INSERT INTO test_table VALUES (1, 100);",
        "INSERT INTO test_table VALUES (2, 200) ON CONFLICT (id) DO UPDATE SET value = EXCLUDED.value + 100;",
        "SELECT * FROM test_table;",
        "UPDATE test_table SET value = value * 2 WHERE id = 1;"
    ]
}