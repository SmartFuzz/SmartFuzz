{
    "feature_type": "function",
    "feature_name": "http_request_header",
    "sql_array": [
        "CREATE TABLE http_headers (id INTEGER PRIMARY KEY, header_line VARCHAR);",
        "INSERT INTO http_headers (header_line) VALUES (http_request_header());",
        "UPDATE http_headers SET header_line = http_request_header() WHERE id = 1;",
        "SELECT header_line FROM http_headers WHERE header_line LIKE '%Host%';",
        "DELETE FROM http_headers WHERE header_line = http_request_header();"
    ]
}