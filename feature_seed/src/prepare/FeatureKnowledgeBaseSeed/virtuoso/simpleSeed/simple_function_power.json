{
    "feature_type": "function",
    "feature_name": "power",
    "sql_array": [
        "CREATE TABLE powers (base INTEGER, exponent INTEGER, result DOUBLE PRECISION);",
        "INSERT INTO powers (base, exponent, result) VALUES (2, 3, power(2, 3));",
        "UPDATE powers SET result = power(base, exponent) WHERE base = 3;",
        "SELECT base, exponent, result FROM powers WHERE result > 10;",
        "DELETE FROM powers WHERE result < power(2, 5);"
    ]
}