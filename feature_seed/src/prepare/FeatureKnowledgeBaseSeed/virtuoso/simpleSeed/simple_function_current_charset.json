{
    "feature_type": "function",
    "feature_name": "current_charset",
    "sql_array": [
        "CREATE TABLE test_charset (id INT, charset_name VARCHAR(255));",
        "INSERT INTO test_charset (id, charset_name) VALUES (1, current_charset());",
        "UPDATE test_charset SET charset_name = current_charset() WHERE id = 1;",
        "SELECT id, current_charset() AS current_charset FROM test_charset;",
        "DELETE FROM test_charset WHERE charset_name = current_charset();"
    ]
}